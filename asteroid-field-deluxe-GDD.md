# ASTEROID FIELD DELUXE — Game Design Document & Handoff Guide

**Version:** 3.0 shipped (starfield/parallax, fire-rate rebalance, targets HUD, pause-from-any-state, Difficulty expiry modes, growing tow cap + chain retune, tiered achievements, new tiered/event/weekly achievements); v3.1 shipped (smaller 2560×1440 world + clamped spawn/dock rings; wrap-jump discontinuity fixed; garbage mutual magnetism + coalescence — 12 neglected canisters clump into a Hunter); **v3.2 in progress — Phase 3 of N shipped** (garbage clumps became physical & un-hookable [P1], one shot shatters a clump into its pieces [P2], and now **salvage is permanent — garbage never decays; a canister leaves the field only by being recycled or consumed into a Hunter, and a coalescence-born Hunter drops no garbage so the economy closes** [P3]). See Version History and companion docs below.
**File:** `asteroids-deluxe.html` (single self-contained HTML file)
**Stack:** Vanilla JavaScript, HTML5 Canvas 2D, Web Audio API. No dependencies, no build step.
**Logical resolution:** 1280×720 viewport (`VIEW_W×VIEW_H`), scaled to fit window with letterboxing (CSS scaling, canvas stays 1280×720 internally). As of v1.2 the simulation runs in a larger toroidal world (`WORLD_W×WORLD_H`, **2560×1440** as of v3.1 P1) that the viewport scrolls across, keeping the ship centered — see §2.11.

**Companion documents (read alongside this one):**
- **`STATUS.md`** — running session log + build reality, same as always.
- **Active (the v3.1 planning cycle — being built now):** `PLANNED-FEATURES-v3.1.md` (full specs + rationale + flagged decisions for the v3.1 change set) and `IMPLEMENTATION-PHASES.md` (the v3.1 dependency-ordered phase build plan, at the repo root). These describe **planned** work not yet built; a v3.1 feature's spec moves into Section 2 only when its phase ships, exactly as v2.0/v3.0 did.
- **Archived (historical, superseded — see `archive/README.md`):** `archive/PLANNED-FEATURES-v2.md` + `archive/IMPLEMENTATION-PHASES-v2.md` (the v2.0 feature specs + phase plan) and `archive/PLANNED-FEATURES-v3.md` + `archive/IMPLEMENTATION-PHASES-v3.md` (the v3.0 feature specs + phase plan). All were retired once their phases shipped and their specs moved into Section 2 below. They are **not** active specs — this GDD's Section 2 plus `STATUS.md` are authoritative.

---

## 1. Vision & Design Pillars

A faithful-in-spirit knockoff of Atari's *Asteroids Deluxe* (1981), the harder sequel to *Asteroids*. Everything added to this game should be tested against these pillars:

1. **Vector purity.** All game objects are glowing stroked polygons on near-black. No fills (except tiny bullet dots and particles), no sprites, no textures. The blue-white palette echoes the Deluxe cabinet's blue-tinted mirror overlay.
2. **Momentum is the game.** The ship drifts. Mastery = managing inertia, not twitch aiming. Any new mechanic should interact with momentum, not bypass it.
3. **Pressure, not chaos.** Deluxe was famous for being *mean* — homing enemies, faster rocks — but every threat is readable and has a counterplay. New enemies need a visible tell and a fair answer.
4. **The shield is a resource, not a panic button.** Energy management is the core Deluxe innovation. New mechanics can spend, restore, or threaten shield energy.
5. **Greed is a choice.** (v1.1) Bonus systems like the salvage chain must always be optional, must degrade the player's core capabilities while pursued, and must be losable right up to the payoff. Reward the player for *choosing* risk, never force the risk on them.
6. **Ease players in.** (v1.5) Opening waves should be approachable; intensity ramps gradually with wave number, not all at once. Any threat parameter (speed, fire rate, accuracy, spawn frequency, density) is expressed as a value that interpolates from an easy wave-1 **floor** up to a full-intensity **ceiling** through one shared, wave-driven `difficultyFactor(wave)` curve (§2.13), so the whole game's pacing tunes from one knob (`RAMP_WAVES`). Complements Pillar 3 — pressure should arrive *on a schedule*, not slam the player at wave 1. As of v1.6 both the saucers (§2.6) and the Hunter Satellites (§2.5) scale off this curve.

---

## 2. Current Mechanics Specification

### 2.1 Ship
- Rotation 4.2 rad/s; thrust 340 px/s²; max speed 520 px/s; drag 35%/s (exponential).
- Collision radius 13 px. Wraps at the world edge.
- **Health:** a single HP pool (`SHIP_MAX_HP` = 250) for the whole run — no lives, no respawns. Unshielded hazard/bullet hits subtract source-specific damage; 0 HP is a permanent game over. See §2.7 and §2.12.
- **Hit-stun invulnerability:** any non-lethal hit grants `HIT_STUN_DURATION` (1.0 s) of invulnerability (rendered as blinking) during which further hits are ignored, and shoves the ship away from the hazard at `KNOCKBACK_SPEED` (250 px/s). There is no longer a separate 2.5 s spawn invulnerability — a run starts immediately vulnerable (the first wave spawns ≥220 px away). `ship.invuln` is now purely the hit-stun timer.

### 2.2 Weapons
- Max 4 player bullets alive at once (`MAX_BULLETS`); base fire cooldown `FIRE_COOLDOWN` 0.20 s; bullet speed 620 px/s + ship velocity inherited; 1.05 s lifetime. Bullets wrap.
- **The bullet cap flexes with the weapon powerups (v1.7, §2.14).** `maxBullets()` returns the base 4, or 8 with Rapid Fire, or 12 with Triple Shot (Triple also fires a 3-bullet spread); with both active it returns the higher cap (12), never the product. The fire block reads `maxBullets()` instead of the raw `MAX_BULLETS` constant.
- **Rapid Fire genuinely speeds up cadence (v3.0 P2, B-6).** The fire block sets its post-shot cooldown to `RAPID_FIRE_COOLDOWN` (0.09 s) when Rapid is active, else the base `FIRE_COOLDOWN` (0.20 s) — previously Rapid only raised the bullet cap, leaving cadence unchanged, which read as a bug. Triple Shot stays on the base cadence regardless of Rapid; Rapid+Triple together is therefore the strongest combo (fires fast *and* wide) — flagged for playtest, not rebalanced yet (FLAG B-6-a).

### 2.3 Shield (hold Shift)
- Energy 0→1. Drains 0.55/s while held; recharges 0.12/s while off; each deflection costs an extra 0.22. Cuts out below 0.02 energy.
- Radius 26 px. Deflects debris satellites and the large Hunter core elastically (bounced away at 1.1× their speed, pushed out of overlap). Blocks hostile bullets (costs deflection energy). **Destroys homing Hunters (medium/small) on contact** — costs a deflection's energy, and they still split (a medium shield-kill spawns 3 smalls that scatter outward), the same tension the old wedges carried (§2.5).

### 2.4 Debris Satellites (v1.4)

The passive main hazard (formerly "Asteroids" — the `Asteroid` class was renamed `DebrisSatellite` and its array `game.asteroids` → `game.debris` in Phase 3). They drift randomly and screen-wrap exactly as the old rocks did; the change is visual identity, split fan-out, and guaranteed salvage.

- **Three sizes.** Radius / base drift speed / score: large 46 px / 70 / 20 · medium 26 px / 110 / 50 · small 13 px / 160 / 100 (`DEBRIS_RADII` / `DEBRIS_SPEEDS` / `DEBRIS_SCORE` — scores unchanged from the old asteroid values).
- Speeds get ±30% random variance and a per-wave multiplier of `1 + (wave−1) × 0.08`.
- **3-way splits (F3).** Large → **3 mediums**; medium → **3 smalls**; small → destroyed (no children). This replaces the old 2-way split.
- **Guaranteed garbage at every tier (F3).** *Every* destruction — including the final small-tier kill — emits exactly `DEBRIS_GARBAGE` (**3**) canisters, each with a random outward kick so they fan out. This replaces the old probabilistic single drop (`GARBAGE_DROP` by size). One fully-cleared large lineage (1 large → 3 mediums → 9 smalls = 13 kills) therefore yields **39** canisters, all mass 1.0 (see §2.10). This drove the Phase-3 balance pass (a since-removed decay timer, trimmed wave counts — §2.7, §2.10.1); as of v3.2 P3 garbage is permanent and density is held by coalescence + shatter instead of decay.
- **Shape.** A broken-satellite silhouette: an irregular hull polygon (`n = 6 + size` vertices, radius jitter 0.65–1.05) plus 1–2 protruding antenna/panel shards (open polylines with a small crossbar at the tip), drawn via `drawPoly`/`glowStroke` in `COLOR.debris` (the same blue as the old asteroids, distinct from the teal Hunter satellites). Collision stays circle-vs-circle at the hull radius; the antennas are cosmetic.
- **Contact damage** carries over from F2 unchanged: `DEBRIS_DAMAGE` = large 50 / medium 35 / small 20 (`DMG_LARGE/MEDIUM/SMALL`).

### 2.5 Hunter Satellites (the Deluxe signature) (v1.6)

The active-homing threat — the *Asteroids Deluxe* signature enemy, reshaped. In Phase 5 the shipped killer `Satellite` + homing `Wedge` classes were **merged into one `HunterSatellite` class** with three size tiers (the Debris 3/2/1 convention: **3** large / **2** medium / **1** small). It reads as a **diamond** silhouette in teal (`COLOR.satellite`), staying visually and behaviourally distinct from the passive blue Debris Satellites (§2.4) — Debris drift, Hunters home — the readable tell Pillar 3 requires.

- **Large core (tier 3).** A symmetric diamond (radius 24) that **drifts passively** toward the ship's position *at spawn time* and never re-aims (confirmed design), tumbling slowly in place so it reads as "not aiming at you." Enters from just beyond a viewport edge relative to the ship. The **timed spawn** appears from wave 2 onward on a 14–22 s (first of a run) / 20–32 s (subsequent) timer, and that path is still **one lineage at a time** — it only fires when the whole Hunter family is clear (`game.hunterTimer`). A large core can *also* form by **garbage coalescence** (§2.5.1), and that path is **not** gated by the timer — coalesced cores are independent, so multiple concurrent lineages can exist (a coalesced core alongside a timed one, or several coalesced ones). Worth 200.
- **3-way splits at every splitting tier.** Shot large → **3 mediums**; shot medium → **3 smalls**; shot small → destroyed. (Replaces the old 3-then-2 fan-out.)
- **Medium (tier 2) & small (tier 1) actively home.** A forward-elongated kite-diamond (radius 15 / 10) that points along its pursuit heading. After a brief `HUNTER_SCATTER` (0.6 s) outward scatter, it relentlessly re-aims toward the ship every frame — wrap-aware, chasing across world edges. Worth 150 (medium) / 250 (small — the fastest, most agile thing in the game and the hardest to hit, so worth the most; the old small-wedge value preserved).
- **Difficulty-scaled from the start (F4 × F10).** Every tier's speed and turn rate is a *ceiling* the difficulty ramp (§2.13) climbs toward: each is `ramp(ceiling × HUNTER_FLOOR_FRAC, ceiling, wave)`, sampled once at spawn (wave is fixed for a Hunter's lifetime — Hunters block wave-clear). At wave 1 the whole family moves at `HUNTER_FLOOR_FRAC` = **0.58** of full speed (a gentle ~60%), ramping to ~96% by wave 20. Ceilings (`HUNTER_SPEED_CEIL` / `HUNTER_TURN_CEIL`): large drift **70 px/s**; medium **120 px/s / 1.6 rad/s**; small **175 px/s / 2.6 rad/s** (anchored on the old wedge numbers, extended across three tiers). The large core's passive drift *speed* scales too, but its behaviour stays passive at all difficulties.
- **Garbage at every tier (F4 × F5).** A large or medium kill emits `HUNTER_GARBAGE` (**3**) normal-mass (1.0) canisters. A small kill emits a larger burst — `HUNTER_SMALL_GARBAGE` (**6**) **low-mass** (`HUNTER_SMALL_MASS` = **0.5**) canisters that tow easily and render in a paler mint tint (`COLOR.garbageLight`) — a valuable, easy-to-haul bonus for finishing off a Hunter line. A fully-cleared large lineage (1 + 3 + 9 = 13 kills) yields 12 normal-mass + 54 low-mass = **66 canisters**, total towable mass **39** (the same haul weight as a cleared Debris lineage, just lighter and more numerous at the tail).
- **Contact damage (F2/F4):** `HUNTER_DAMAGE` = large **60** / medium **45** / small **30** — a step above the equivalent Debris tier (§2.12), since Hunters are the more dangerous, actively-seeking threat.
- **Shield interaction (§2.3).** The large core bounces off the shield elastically (you must shoot it); medium/small homers are **destroyed on shield contact and still split** (a shield-killed medium spawns 3 scattering smalls), preserving the old wedge tension — the shield answer and the "just shoot it" answer stay in tension, not stacked.
- **Chain vulnerability (carried forward).** A Hunter body touching a tow-chain node severs the chain exactly as the old satellite/wedge did (§2.10) — the Hunter survives; the touched node and everything aft break loose.
- **Kill path.** All of the above is handled by `destroyHunter(h)` (a flow function parallel to `destroyDebris`, not a `split()` method): score → boom → tier garbage → 3-way split (or, at the small tier, the low-mass burst and no children). Called both when a bullet hits and when a homer dies on the shield.

#### 2.5.1 Garbage coalescence → new Hunter (v3.1 P3 / v3.2 P1–P3)

Neglected salvage is a *standing threat*, not ephemeral clutter: canisters are **permanent** (they never decay — v3.2 P3, §2.10) and uncollected ones drift together into a **physical clump** — a heavy, growing body with real mass and size — that, once enough pieces have fused, collapses into a fresh large Hunter core. This is the "neglect breeds danger" pressure — the field you leave lying around can turn on you. Handled by `coalesceGarbage(dt)`, an O(n²) pass over free garbage run once per frame in `update()`, **after** the pickup/magnet loop and **before** `updateChain(dt)`. All measurement is wrap-aware (`dist2`/`shortDelta`).

> **v3.2 P1 SUPERSEDES FORK-2.** The shipped v3.1 model kept a clump's mass fixed at 1.0 and let it tow as one normal canister — correct *only while clumps were hookable*. v3.2 makes a clump **un-hookable** (you must shoot it apart first — see the shatter rule below, P2), which removes FORK-2's entire reason to exist. A clump is now a real body: its **mass sums**, its **radius grows**, its merges and attraction are **momentum-conserving**, and it renders as a **cluster**. The old "clump tows as one mass-1.0 node" rule is gone.

- **Activation delay (v3.2 P3 retune: 1.0 → 6.0 s — a playtest knob).** Every canister carries `coalesceDelay` (`GARBAGE_COALESCE_DELAY` = **6.0 s**) and a `pieces` count (default **1**), inherited by all three emission sites *and* `Garbage.fromNode`. A piece is **active** only once `coalesceDelay` has counted down to ≤ 0. **Both** the attraction and the merge require both pieces active, so a fresh burst rides its own explosion kick *apart* before it can start clumping. At the 20–55 px/s emission kick, 6 s carries same-explosion siblings **120–330 px apart** — so a burst drifts apart and *later happens upon strangers* instead of instantly re-clumping (the "float through space and find other garbage" feel). Coalescence grows from scrap that's been sitting and drifting, not from a just-shot cloud.
- **Mass, size, silhouette (v3.2 P1).** `pieces` stays the **1..11 threat counter** (at 12 it transforms). A clump's **`mass` is the sum** of its absorbed pieces' masses — a wad of 4 normal canisters is mass 4.0; a wad of 4 Hunter-scrap canisters is 2.0. Its **`radius` derives** as `7 × √pieces` (area-proportional; a single piece still reads the base 7, an 11-clump ~23 px — comparable to a large Hunter core). Both are recomputed at every merge.
- **Mass-weighted mutual magnetism (v3.2 P1; range retuned P3).** Each active pair within `GARBAGE_MAGNET_RANGE` (**260 px** as of v3.2 P3 — was 140) attracts gravity-like: each piece accelerates toward the other in proportion to the *other's* mass — `a.v += (dx/d)·GARBAGE_MAGNET_PULL·b.mass·dt`, `b.v −= (dx/d)·GARBAGE_MAGNET_PULL·a.mass·dt`. This is momentum-conserving (`mₐ·accₐ == m_b·acc_b`), so heavy clumps become slow **anchors** that loose singles fall into. It **reduces exactly to the shipped symmetric pull when both masses are 1.0**, so `GARBAGE_MAGNET_PULL` needed no retune. The range grew because 140 was tuned when garbage lived 12 s and only had to find siblings in a fresh, tight cloud; with **permanent** garbage, pieces that drift more than the range apart would *never* re-find each other and the field would silt up with inert junk — 260 is a starting point and **the number most likely to need tuning by feel** (FLAG D-1). An optional off-by-default speed clamp (`GARBAGE_CLUMP_MAXSPD` = `Infinity`) is a playtest knob against runaway clump speed.
- **Momentum-conserving merge (v3.2 P1).** Two active pieces within `GARBAGE_MERGE_DIST` (**12 px**) fuse: the survivor's velocity becomes the **momentum sum** computed with the *pre-merge* masses — `v = (mₐvₐ + m_b v_b) / (mₐ + m_b)` — its `mass` sums, `pieces += other.pieces`, `radius` is recomputed, and the other is marked dead. **This replaces the old literal vector sum** (`survivor.vx += other.vx`): with mass now summing and garbage soon permanent, a literal sum made every merge *add* speed, so clumps became the fastest things on screen — backwards from "a heavy wad of junk." Now a heavy clump barely speeds up when it eats a fast light piece. Low-mass Hunter scrap (mass 0.5) still counts as **1 piece** — the count is canister-based, the mass is mass-based.
- **Un-hookable (v3.2 P1, load-bearing).** A clump (`pieces > 1`) **cannot be towed** — the pickup hook additionally requires `g.pieces === 1`, and the **Magnet powerup ignores clumps** too (pulling something you can't hook is just noise). You must break a clump open first (a shot — see the shatter rule below) before its pieces can be banked. This is the rule the whole v3.2 changeset turns on.
- **Shatter (v3.2 P2) — the tool that reopens un-hookable.** One **player** bullet hitting a clump (`pieces > 1`, tested against its derived `radius`) destroys it via `shatterClump(g)` and emits exactly `pieces` fresh single canisters at its position — each with `mass = clump.mass / pieces` (per-piece mass identity is averaged back out; a deliberate fidelity trade to keep `mass` a single number, invisible in play), a **full, re-armed `coalesceDelay`** (the constructor default, so the wad disperses before it can start re-clumping — same knob as the 1 s activation delay above), and an outward `GARBAGE_SHATTER_KICK` (40–90 px/s) kick added to a fraction of the clump's own drift, mirroring the `destroyDebris` fan-out. A single canister (`pieces === 1`) is **not** a bullet target — player bullets still pass straight through it, exactly as before. **Hostile (saucer) bullets pass through all garbage**, clumps included — shattering is a player-only tool. **No score** is awarded for the shatter itself; the reward is the 2–11 now-hookable canisters. The loop this creates: a clump sitting at 9 pieces is a countdown — shoot it for a near-full cargo run and a big dock combo, or ignore it and let it hit 12 to fight what it becomes. *Greed and neglect now share the same object as their fulcrum.*
- **Cluster render (v3.2 P1).** In `Garbage.draw()`, a clump draws `pieces` mini-canisters (via the existing `drawCanister`) at deterministic golden-angle / phyllotaxis offsets — for `k` in `0..pieces-1`: `θ = k·2.39996`, `r = 5·√k` — the whole wad rotated by `this.spin` so it tumbles as one body. No stored per-clump state, no numerals. It reads unmistakably as "a wad of garbage," not a new enemy. `pieces === 1` collapses to a single centered canister, byte-identical to before. The `mass < 1` paler-tint rule still applies to the whole clump.
- **Non-solid to the ship (FORK-A2).** A clump does **zero** contact damage and is **not** added to the `hazards` array — the ship passes straight through. Garbage isn't the threat; what it *becomes* is. (The player is meant to shoot a clump apart, not bump it.)
- **Transform at 12.** When a survivor reaches `HUNTER_COALESCE_COUNT` (**12**) pieces it dies and a `new HunterSatellite(x, y, 3)` is pushed to `game.hunters`, with an `AudioSys.hunterborn()` warning cue (§2.8). This is **independent of `game.hunterTimer`** — coalescence can add lineages the timed spawn never would (multiple concurrent lineages, FORK-1). The existing end-of-frame dead-filter compacts the consumed pieces; no new filter.
- **A scrap-born Hunter drops NO garbage (v3.2 P3, FORK-B / B1).** The coalesced core is tagged **`bornOfScrap = true`**, and that flag **propagates to its split children** in `destroyHunter` (both generations — large → 3 medium → 9 small), so the **whole lineage emits zero garbage at every tier** when killed. Its score, its splits, its contact damage, and its small-tier powerup drop are all **unchanged** — only the garbage emission is suppressed. **Why this is required, not cosmetic:** with garbage now permanent, a normal 13-member lineage drops `12 + 54 = 66` canisters (§2.10). If a scrap-born lineage did the same, coalescence would be a **5.5× amplifier** — 12 pieces in, 66 out — and the field's garbage supply would diverge without bound. B1 closes the loop: **12 pieces in, 0 out.** Read in-fiction, that scrap *has already been spent — it IS the Hunter now.* It also creates a real choice: letting a clump transform yields a pure-score lineage with **no cleanup bill**, a genuine tension against shooting the clump apart to harvest it. **Timer-spawned Hunters (`bornOfScrap = false`) are untouched and still drop the full 66.** A per-game stat `game.stats.hunterCoalesced` is incremented once at the transform site (drives **Waste Not**, §2.17).
- **Coalescing vs recycling — the two exits (v3.2 P3).** Garbage no longer decays, so a canister leaves the field by exactly **two** doors: **recycled** (towed to the dock) or **consumed into a Hunter** (coalescence). Coalescing into a Hunter is the fate **Waste Not** now watches (`hunterCoalesced === 0`), replacing the removed "zero canisters expired" test. Shattering a clump is *not* an exit — it just breaks one body back into its `pieces` canisters, which remain in the field.
- **Magnet is the counter-play — for singles.** The Magnet powerup (ship↔garbage pull, §2.14) is a separate, stronger, powerup-gated force — vacuuming *loose singles* before they clump is the natural way to prevent coalescence. Once pieces have fused into a clump the Magnet no longer helps (it ignores `pieces > 1`); a shot (the shatter rule above) is the tool that reopens it.

### 2.6 Saucers

Shape, movement, scores, and bullets are the shipped v1.1 saucers; **as of v1.5 their pacing is wave-scaled** by the difficulty ramp (§2.13) so early waves are gentler and full intensity arrives around wave 20–25. Every scaled value interpolates `floor + (ceiling − floor) × difficultyFactor(wave)` from named floor/ceiling constants in the "Difficulty ramp & early-game pacing (F10)" block, so all early-game pacing tunes from the constants block. Behavior and shape are unchanged.

- One at a time. **Spawn gap** interpolates from a wide wave-1 floor `rand(20, 30) s` down toward `rand(12, 16) s` at full difficulty (re-expresses the old `rand(12,22) − min(6,wave)`; the first saucer of a run waits the wave-1 floor). Enter from a side edge, zig-zag vertically, exit the far side (no horizontal wrap; despawn after crossing).
- **Fire rate:** both saucers' between-shot intervals (and the first-shot delay) are multiplied by a **fire multiplier** that starts at `1.8×` at wave 1 (slower) and eases to `1.0×` at full difficulty, via the shared `Saucer.rollFireTimer(range)` method. Base ranges are `SAUCER_FIRE_BIG` (0.9–1.6 s), `SAUCER_FIRE_SMALL` (0.7–1.1 s), `SAUCER_FIRE_INIT` (0.6–1.4 s first shot).
- Big saucer: radius 20, 100 px/s, fires **randomly**. Worth 200. (Random fire is already the "easy" case, so only its fire *rate* scales — no accuracy change.)
- Small saucer: radius 12, 150 px/s, fires **aimed** — but the aim **error widens** from `±0.35 rad` (a barely-aimed spray) at wave 1 down to the shipped `±0.09 rad` precision at full difficulty. Worth 1000.
- **Small-saucer appearance chance** scales from `15%` at wave 1 to `60%` at full difficulty. This *folds in* the old score-gated logic (was: 25%, rising to 60% above 8000 points) — the escalation is now purely wave-driven so there's one difficulty system instead of two (§2.13, §2.10.1 rationale carries: greed is a choice, pressure on a schedule).
- Saucer bullets are red, 380 px/s, 1.4 s life, and destroy debris satellites (no score awarded, but they still emit their 3 canisters — see §2.4).

### 2.7 Waves, Health & Scoring

- Wave N spawns `min(3 + N, 9)` large Debris Satellites, placed in a ring 220–1100 px around the ship's *current* world position (see §2.11). *(Trimmed from `min(4 + N, 11)` in Phase 3 to offset the ~8× higher garbage volume from guaranteed 3-per-tier drops — see §2.10.1. As of v3.2 P3 garbage no longer decays; coalescence + shatter are the density levers, and F10 may raise this count again for a fuller field.)*
- Next wave starts 2.5 s after the field is clear of debris, satellites, AND wedges — the exact gate is `game.debris.length === 0 && game.hunters.length === 0`. Free garbage and towed cargo do **not** block wave-clear; both persist into the next wave (the dock relocates).
- **`TARGETS n` HUD readout (v3.0 P3)** shows `n = game.debris.length + game.hunters.length` — literally the wave-clear gate, so 0 means the wave is about to end. The count can **rise** mid-wave: a Debris split (a large → 3 mediums, +2), a Hunter lineage arriving on its own 14–32 s timer (Hunters only appear from wave 2 on — they're absent at wave start), or, as of v3.1 P3, **a Hunter coalescing out of neglected garbage** (§2.5.1) — which, unlike the timer, can happen on *any* wave, at any time, and even off-screen (the `hunterborn` cue is the tell). A coalesced Hunter lands in `game.hunters`, so it counts toward `TARGETS` and **blocks wave-clear** until killed. This is intentional, not a bug — the wave genuinely isn't over until the Hunters clear too, so the HUD reflects the truth rather than a fixed at-start budget (hence the label `TARGETS`, not "satellites left").
- **Health, not lives (v1.3).** The ship has one HP pool (`SHIP_MAX_HP` = 250); there are no discrete lives, no respawns, and no continues — one hull for the whole run. Any unshielded hazard contact or hostile bullet subtracts source-specific damage, applies knockback, and opens a 1.0 s hit-stun window (see §2.12). At 0 HP the ship explodes, the tow load scatters, and the run ends (true game over).
- **Score milestones repair the hull.** Every 10,000 points (`REPAIR_MILESTONE`) grants +25 HP (`REPAIR_AMOUNT`), capped at max; if already at full HP the milestone instead pays a flat `REPAIR_FULL_BONUS` (2,500) points so it's never wasted. This replaces the old "extra life every 10,000 points."
- **Health powerups also restore HP (v1.7, §2.14).** An ambient Health pickup restores +25 HP (`POWERUP_HEALTH_AMOUNT`, same value as the milestone repair), capped at max, on pickup — a second HP-restore path alongside the score milestone.

### 2.8 Audio (all synthesized, no assets)
- Fire: square-wave pitch drop. Explosions: filtered noise bursts, deeper/longer by size. Thrust: looped low-passed noise. Saucer: LFO-warbled triangle (higher/faster for small). Heartbeat: alternating two-tone sine thump whose interval shrinks with wave number and field density (floor 0.28 s). Shield ping / extra life: sine chirp. Menu blip (`ui`, v1.9): a short square pip, higher for confirm/select, lower for move/back/adjust.
- **`hunterborn` (v3.1 P3)** — the warning cue when a garbage clump coalesces into a Hunter (§2.5.1). Two low sawtooth voices (~196→138 Hz and ~139→98 Hz) a dissonant tritone (~√2) apart, both **descending** over ~0.4 s — deliberately dark, ominous, and distinct from `explosion`. Audio isn't spatialized, so it plays whether the Hunter forms on- or off-screen, making it the only tell for an off-screen coalescence.
- **Gain routing (v1.9, F8).** Every voice now connects through a two-stage bus — voice → **SFX gain** → **master gain** → speakers — so the Options volume sliders actually attenuate output (`AudioSys.setVol('sfx'|'master'|'music', 0..1)`; 1 = unity = the pre-v1.9 loudness). A **music gain** node also hangs off master and is driven by the Music slider, but **nothing is connected to its input — there is no music track in the game.** The slider and node exist so a future track can be dropped in without re-plumbing; today the Music slider changes nothing audible (documented, not a placeholder track).

### 2.9 States & Controls
- States: `title` → `playing` → `gameover`. Gameplay: rotate ←→/A/D, thrust ↑/W, fire Space, shield Shift — all **rebindable** as of v1.9 (§2.16).
- **Corrected control scheme (v3.0 P4 — supersedes the earlier B-1-a mapping).** The menu system is reachable from *every* state, and the controls read the same on keyboard and controller:
  - **Start a game** (from `title`/`gameover`): **Enter** or controller **A**, *or* controller **Start**.
  - **Pause** (during `playing`): **ESC** or controller **Start**. (`P` is retired — ESC is the pause key.)
  - **Open the Options/Achievements system menu** (from `title`/`gameover`): **O** or controller **B**. Its root is **Options / Achievements / Back**; Back closes the overlay back to the underlying screen.
  - **Controller Start is a session toggle**: title/gameover → start a game; playing → open pause; while paused → dismiss & resume.
  - **ESC and controller B are context-aware**: ESC = pause outside a menu / back inside one; B = open the system menu on title/gameover / back when a menu is open. In-menu, **Enter/A** confirm, **ESC/B** back, **Start** resumes.
- **Gamepad (v1.8, §2.15).** The first connected controller works alongside the keyboard: D-Pad / Left Stick rotate + thrust, A fires, right trigger/bumper raises the shield. System reads per the corrected scheme above (Start = session toggle, A = confirm/start, B = open-menu/back). Gameplay defaults are player-editable via Options → Controls (v1.9, §2.16).
- **Pause menu / Options (v1.9, §2.16).** The root is context-aware: **Continue / Options / Quit** when paused mid-game, **Options / Achievements / Back** as the title/gameover system menu. Options has SFX/Music/Master volume sliders + a Controls rebinding screen + an **Achievements** viewer (v2.0, §2.17); navigable by keyboard **or** gamepad. Settings persist via `localStorage`.

### 2.10 Radioactive Salvage, Tow Chain & Recycling Dock (v1.1)

**Garbage drops (F3-updated).** Every destroyed Debris Satellite emits exactly `DEBRIS_GARBAGE` (3) radioactive canisters at *every* tier, including the final small-tier kill (39 per fully-cleared large lineage — see §2.4). This replaced the old probabilistic single drop. Canisters inherit 25% of the debris's velocity plus a random outward kick (so the trio fans out), drift, spin, and screen-wrap. Each canister carries a **`mass`** field (F5; default **1.0** for all Debris-sourced scrap — see below). **Canisters no longer decay (v3.2 P3).** Salvage is a permanent, standing field object: a canister leaves the field by exactly **two doors** — **recycled** (towed to the dock) or **consumed into a Hunter** (coalescence, §2.5.1) — never by expiring on a timer. *(The old `GARBAGE_DECAY` / `GARBAGE_SEVER_DECAY` blink-out timers are gone; density is now held by coalescence, which consumes pieces, and shatter-to-harvest, which makes the player want them — see §2.10.1.)*

**Cargo mass (F5).** Each `Garbage` and each chain node carries a `mass` (default 1.0). `Garbage.fromNode()` carries `mass` back when a node is severed to free garbage; pickup copies the canister's `mass` onto the new chain node. The chain-handling penalties (below) scale with the chain's **total mass** — `chainMass()` = Σ node.mass — not its node count. All current (Debris-sourced) scrap is mass 1.0, so a chain's mass sum equals its length and handling is unchanged from v1.3; lower-mass scrap (planned for Hunter Satellites, F4) will tow more easily. See §3.4.

**Collection & chain.** Flying within 18 px of a **single** canister (`pieces === 1`) hooks it onto a tow chain behind the ship (up to the current tow cap `game.cargoMax`; extras stay in the field). As of **v3.2 P1 the hook refuses clumps** (`pieces > 1`) — a fused wad of garbage is un-hookable and must be shot apart into singles first (§2.5.1); the Magnet powerup ignores clumps for the same reason. The cap is a **runtime value that grows during a run** (§2.10.2) from `CARGO_BASE` (12) toward `CARGO_CAP_MAX` (20). The chain is simulated: verlet nodes with 20 px distance constraints, anchored to a hitch point 12 px behind the ship, relaxed over `CHAIN_ITER` (4) passes/frame. Physics effects:
- *Mass penalty:* thrust is divided by `1 + m×CARGO_THRUST` and top speed by `1 + m×CARGO_MAXSPD`, where **`m` = the chain's total mass** (`chainMass()` = Σ node.mass, F5). As of the B-8 retune the coefficients are softened (`CARGO_THRUST` 0.10→**0.06**, `CARGO_MAXSPD` 0.05→**0.03**) so a *full chain at the new ceiling* (m = 20) ≈ **45% thrust / 63% top speed** — i.e. it lands on the old full-12 feel, while a base-12 chain is now genuinely lighter (~58% thrust / ~74% top speed), leaving handling headroom to grow into. The per-node penalty shrinks as the ceiling rises; the "cap is a handling ceiling" rationale (§2.10.1) is preserved at the higher node count. (For all-mass-1.0 cargo, m equals the node count.)
- *Momentum tug:* when the first link is stretched taut, the ship receives a pull-back acceleration of `CHAIN_TUG (26) × stretch-px × massFactor` (massFactor = min(1.4, m×`CARGO_MASS`), same total-mass `m`). `CARGO_MASS` was dropped 0.12→**0.07** in lock-step so the 1.4 tug cap is reached at m≈20 (was m≈12) — the tug scales down with the other two penalties, so a base-12 chain tugs at 0.84 (lighter) and only a full 20-chain hits the max 1.4. Hard turns and burns get the ship yanked by its own cargo.
- The chain is wrap-aware and follows the ship across screen edges.

**Chain vulnerability.** Any debris satellite, wedge, or killer-satellite body — or any hostile bullet — touching a chain node destroys that canister; **everything aft of it breaks loose** as free garbage (scatter velocity, mass preserved). As of **v3.2 P3** severed scrap no longer gets a (shorter) decay — like all garbage it is now **permanent**, so knocked-loose canisters drift, coalesce, or wait to be re-collected exactly like any other free canister. Ship death scatters the entire load. Player bullets pass through **single** canisters and the chain (they're debris, not targets) — but as of **v3.2 P2**, a player bullet hitting a free **clump** (`pieces > 1`) is a target: it shatters the clump into hookable singles (§2.5.1). Hostile bullets still pass through all garbage, clumped or not.

**Recycling dock.** A green octagonal station (radius 44) placed randomly each wave, 260–900 px from the ship's *current* world position (see §2.11). Non-solid — purely a delivery zone. While the ship is within radius+10 and carrying cargo, canisters peel off the **tail** every 0.13 s. Delivery score escalates within a single visit: `50 + 25×(n−1)` for the nth canister (full 12-chain = 2,250). The combo counter resets when the ship leaves the dock's neighborhood (radius+40). Delivery pitch rises per canister; score popups float up.

**HUD.** `CARGO n/{cargoMax}` readout (lit when carrying), where the denominator is the current runtime cap (12→20); a green chevron orbiting the ship points toward the dock whenever cargo > 0.

#### 2.10.2 Tow-capacity progression (v3.0 P6 / B-8)

The tow cap is no longer the fixed `CHAIN_MAX = 12`. It's a runtime `game.cargoMax` that **starts at `CARGO_BASE` (12) and grows by +1 per `CARGO_GROW_PER` (30) canisters recycled (delivered) in the current run, bounded by `CARGO_CAP_MAX` (20)**. Growth is checked at the dock-offload site right after the per-run delivered count increments, so the cap climbs at most one step per canister; each increase pushes a brief **"TOW +1"** float. `startGame` resets `cargoMax` to `CARGO_BASE`, so capacity is earned fresh each run.

- **Why per-delivery, not per-wave (FORK A-fork-2):** tying capacity to *engaging the salvage loop* directly rewards the greed mechanic (Pillar 5) and self-paces — heavier haulers earn heavier hauling — rather than passively handing capacity out by wave.
- **The physics retune is the load-bearing half.** A naive m=20 chain under the old coefficients would be ~33% thrust / 50% top speed — the "broken, not heavy" zone §2.10.1 warns about. The softened coefficients (§2.10) put the full 20-chain on the old full-12 feel instead, and the relaxation iterations were bumped 3→4 (`CHAIN_ITER`) to keep the longer chain from sagging (§3.4).
- **"Full chain" achievements decouple from the cap (FLAG B-8-b).** Heavy Hauler / Freight Baron / The Long Haul must key on a **fixed ≥12** delivered in one dock visit, *not* on `game.cargoMax` — otherwise their meaning drifts as the cap grows. The existing full-visit hook keys on a literal `12`; the achievements-overhaul phases own keeping it fixed.

> Constants live in the salvage/chain block: `CARGO_BASE` (12), `CARGO_CAP_MAX` (20), `CARGO_GROW_PER` (30), the retuned `CARGO_THRUST`/`CARGO_MAXSPD`/`CARGO_MASS`, and `CHAIN_ITER` (4).

#### 2.10.1 Design considerations & rationale (read before changing this system)

These were deliberate calls, not accidents — future sessions should understand *why* before tuning:

- **Guaranteed 3-per-tier drops (F3) supersede the old inverted probabilistic model.** The v1.1 design dropped smalls reliably and larges rarely (≈5 per lineage) so garbage appeared only where a lineage was *finished*. Phase 3 replaced that with a flat 3 canisters at every tier (39 per fully-cleared large lineage) — a deliberate, request-driven ~8× volume increase to make salvage a much bigger, more constant temptation (Pillar 5: greed is a choice). The player is **not** expected to collect it all — the point is a field rich with *optional* risk/reward, not housekeeping. As of v3.2 P3, garbage is **permanent** (it never decays), so density is now controlled by the two *consuming* forces below plus the tow ceiling (`game.cargoMax`, 12→20 — §2.10.2), not by making drops rare or by expiry.
- **Density is held by coalescence + shatter-to-harvest, NOT decay (v3.2 P3 — supersedes the old "decay protects readability" rule).** *This bullet used to argue the opposite — that `GARBAGE_DECAY` was load-bearing and must never be removed. That argument is now false; do not "restore" it.* The old reasoning: with ~8× the drop volume, persistent garbage would carpet the field into visual noise, so a 12 s decay timer was the primary density lever and the blink-out was the player's countdown. **What changed:** v3.2 made salvage a *standing, adversarial* field object — canisters that pile up **coalesce into Hunters** (§2.5.1), which is a real consequence a decay timer can't express, and it made garbage un-hookable-until-shot so the player now has a *reason to hunt clumps* (shatter-to-harvest). Those two forces are the new density regulators: **coalescence consumes pieces** (12 in → 1 Hunter, and a scrap-born Hunter drops **0** back out — FORK-B/B1, §2.5.1), and **shatter-to-harvest makes the player want the pieces** instead of ignoring them. Density is now a *gameplay* pressure, not a housekeeping timer. If a future session finds late-wave clutter, the levers are `GARBAGE_MAGNET_RANGE` (bigger → pieces find each other and clump/transform faster) and `DEBRIS_GARBAGE`/`HUNTER_SMALL_GARBAGE` (drop volume, held in reserve) — **not** re-adding decay. **Open tuning question:** whether permanent garbage + a 260 px magnet range keeps the field "richly populated" vs "silted up with inert junk" is the top v3.2 P3 playtest ask (`GARBAGE_MAGNET_RANGE` is the number most likely to be wrong — FLAG D-1).
- **The tow cap is a handling ceiling, not a scoring one.** Beyond a node count the thrust penalty makes the ship feel broken rather than heavy, and long constraint chains start whipping. The cap now *grows* to `CARGO_CAP_MAX` (20) during a run (§2.10.2), so the handling ceiling was moved out with a matching physics retune rather than left in place: the mass-penalty coefficients (`CARGO_THRUST`/`CARGO_MAXSPD`/`CARGO_MASS`) were softened together so a full chain *at the new ceiling* reproduces the old full-12 feel (~45% thrust / ~63% top speed), and `CHAIN_ITER` was bumped 3→4 for stability at 20 nodes (§3.4). If the ceiling is ever raised again, re-soften the coefficients and re-validate the stability envelope the same way — don't just bump the number.
- **The tail-peel offload (chain.pop) matters visually** — the chain visibly shortens from the far end while the ship holds in the dock, which reads as "unloading." Popping from the head would make the whole chain teleport forward one link per tick.
- **Escalating per-piece scores (not a flat rate) is the whole risk/reward engine.** A 12-chain delivery (2,250) is worth far more than four 3-chain trips (450). This is what makes players tow long, dangerous chains past homing wedges. Flatten this and the mechanic collapses into an errand.
- **Aft-severing (losing everything behind a hit node) instead of single-node loss** makes chain *position* strategic: your most recent pickups are the most exposed. It also creates dramatic "lost the whole tail" moments that flat attrition wouldn't.
- **The dock relocates every wave** to prevent a dominant orbit-the-dock strategy and to keep the risk of the haul fresh each wave.
- **The dock is non-solid.** Making it collidable punishes the exact maneuver the game asks for (flying into it while handling badly). Hazards pass over it too — the dock is a zone, not cover.
- **The shield does not protect the chain.** The shield radius (26) only covers the ship; deflected debris can and will hit your own cargo. This is intentional — the shield answer and the cargo answer should be in tension, not stacked.
- **Momentum tug uses pre-constraint stretch** as its force signal (see 3.4). This is a stability decision: spring forces on a hard-constrained chain oscillate; measuring how much the cargo "resisted" the ship this frame and applying only that gives a heavy feel without energy injection.

### 2.11 Larger Scrolling World & Camera (v1.2, retuned v3.1 P1/P2)

The simulation space and the screen used to be the same 1280×720 rectangle. As of v1.2 they are decoupled: the world is a large torus and the screen is a camera window onto it.

- **World vs viewport.** The world is `WORLD_W × WORLD_H` = **2560×1440** (shrunk from the original v1.2 3840×2160 in v3.1 P1 — 2×2 viewports, 4× area ≈ 44% of the old world, cutting the dead space the bigger torus left between spawns) and still wraps at its own edges — same torus topology as before, just smaller. The **viewport** is `VIEW_W × VIEW_H` = **1280×720**, still the canvas size and still CSS-scaled/letterboxed to the window. All world/simulation math (wrap, distance, aiming, chain links) uses `WORLD_W/WORLD_H`; only the canvas, HUD, and title screen use `VIEW_W/VIEW_H`.
- **Camera.** `game.camera.x/y` tracks the ship's world position every frame — no smoothing, no clamping (the world wraps, so there is no edge for the camera to bump into). The ship is therefore always drawn at the center of the viewport. On respawn the ship returns to world center and the camera snaps with it.
- **Render pipeline.** `draw()` clears the viewport, draws the starfield, then (for gameplay) translates the context by `(VIEW_W/2 − camera.x, VIEW_H/2 − camera.y)` and draws every world-space entity inside that transform. The HUD (score, lives, cargo readout, dock chevron, wave, shield bar) and the pause/game-over overlays are drawn **after** restoring the untranslated context, so they stay screen-fixed. Each world entity is drawn at its **nearest wrapped image** relative to the camera (`wrapOffset`), so entities on the far side of the world seam still render in the right place when the ship is near a world edge (a single naive translate would drop them off-screen).
- **World-boundary line (v2.1).** `drawWorldBoundary()` strokes a dim, dashed `WORLD_W×WORLD_H` rectangle (`COLOR.dim`, `setLineDash([BOUNDARY_DASH, BOUNDARY_GAP])`, low glow) as the first thing inside the camera transform — beneath every entity. It's drawn at the world-origin corner's **nearest wrapped image** relative to the camera (`wrapOffset` on both axes), so the seam is visible whether the ship approaches from world x≈0 or x≈WORLD_W (y likewise), and stays continuous as the ship crosses it. Purely cosmetic — an interim wrap-visibility aid, no collision or gameplay effect. (Full off-screen threat awareness remains deferred to v3.0 — see Culling.)
- **Culling.** `onScreen(e, margin)` (wrap-aware) skips drawing any entity more than `CULL_MARGIN` (100 px) outside the viewport. Culled entities still `update()` normally — off-screen hazards keep simulating in the larger world. (Off-screen *threat awareness* — warning the player about hazards approaching from beyond the viewport — is deliberately deferred; see `archive/PLANNED-FEATURES-v2.md` F10.)
- **Starfield (v3.0 P1: legibility + parallax).** Two layers. The **far layer** is unchanged in
  structure — stars scattered once across the whole world (count scales with world area,
  `STAR_DENSITY` raised 40→80), rendered wrap-aware in screen space at 1.0× the camera offset so it
  scrolls seamlessly with no tiling repeat or edge seam — but each star now carries its own
  brightness (`a` in `[STAR_BRIGHT_MIN, STAR_BRIGHT_MAX]`) drawn in a brighter cool blue
  (`rgba(120,160,210,a)`) than the old flat `#1a2a40`, so the field reads without competing with
  entities (Pillar 1). A second, sparser, brighter **near layer** (`starsNear`,
  `STAR_NEAR_DENSITY`, `rgba(160,195,235,a)`) adds a depth/motion cue: it moves at
  `STAR_PARALLAX_FACTOR` (0.5×) of the camera offset. Because a ≠1× layer is not world-fixed, it
  can't reuse the toroidal wrap math — instead it's tiled in **screen space**, modulo its own
  `VIEW_W×VIEW_H` tile, keyed off `camera * STAR_PARALLAX_FACTOR` (a 3×3 tile stamp each frame,
  culled to the viewport), which sidesteps seam artifacts entirely. Purely cosmetic — no gameplay
  effect either layer.
- **Ship-relative spawning.** Because a fixed-rect spawn could drop a whole wave unreachably far away in the big world, spawns are now placed relative to the ship's *current* world position: new large debris satellites in a ring `[SPAWN_MIN_DIST, SPAWN_MAX_DIST]` = **[220, 640]** px; the recycling dock in `[DOCK_MIN_DIST, DOCK_MAX_DIST]` = **[260, 620]** px (retuned in v3.1 P1 — see below). Killer satellites and saucers enter from just beyond a **viewport** edge relative to the ship (offset by `VIEW_W/2`/`VIEW_H/2` + a margin, then folded into the world with `wrapPos`), preserving their shipped "slide in from off-screen, cross the screen" feel; their speeds, homing, fire rates, and split behavior are unchanged (those are Phase 4/5 concerns).
- **Spawn-ring clamp (v3.1 P1).** On the smaller 2560×1440 world, a spawn ring reaching past half the shorter axis could wrap around and land on the far side of the ship — effectively on top of it. The binding limit is `min(WORLD_W, WORLD_H)/2 − 60 = 660`. `SPAWN_MAX_DIST` was clamped 1100→640 and `DOCK_MAX_DIST` 900→620 (mins unchanged at 220/260) so both rings stay safely under that limit, with `DOCK_MAX_DIST < SPAWN_MAX_DIST` preserved.
- **Starfield density preserved automatically.** `STAR_COUNT` derives from world area (`STAR_DENSITY * WORLD_W*WORLD_H / (VIEW_W*VIEW_H)`), so shrinking the world to ~44% of its old area scales the far-layer star count down to match — visual density is unchanged with no constant edits. The near (screen-space) layer is unaffected by world size.
- **Wrap jump is exactly `WORLD_W`/`WORLD_H` (v3.1 P2).** `wrap()` and the chain node-wrap (`wrapNode`) previously teleported entities by `WORLD_W + 120` / `WORLD_H + 120` — an undocumented +120 px mismatch against the period (`WORLD_W`/`WORLD_H`) every distance/aim/render helper (`dist2`, `angleTo`, `shortDelta`, `wrapOffset`, `wrapPos`) actually uses. That mismatch shifted every entity ~120 px relative to the ship at each seam crossing. Both jumps now teleport by exactly `WORLD_W`/`WORLD_H`; the ±60 px trigger margins are unchanged (they're anti-flicker hysteresis — a jump of exactly `WORLD_W` from `x = −61` lands at `WORLD_W − 61`, safely inside the `[−60, WORLD_W+60]` band with no re-trigger). `wrapNode` shifts `px`/`py` by the same delta as `x`/`y` so implied velocity survives the teleport (§3 rule).

> New tuning constants live in the "Larger world & scrolling camera (v1.2)" block at the top: `CULL_MARGIN`, `SPAWN_MIN_DIST`, `SPAWN_MAX_DIST`, `DOCK_MIN_DIST`, `DOCK_MAX_DIST`, `STAR_DENSITY`, plus `WORLD_W/H` and `VIEW_W/H`. v2.1 adds `BOUNDARY_DASH`/`BOUNDARY_GAP`/`BOUNDARY_WIDTH`/`BOUNDARY_GLOW` (the wrap-seam line) to the same block. v3.0 (P1) retunes `STAR_DENSITY` (40→80) and adds `STAR_PARALLAX_FACTOR`, `STAR_BRIGHT_MIN/MAX`, `STAR_NEAR_DENSITY`, `STAR_NEAR_BRIGHT_MIN/MAX` (the starfield legibility/parallax pass). v3.1 (P1) shrinks `WORLD_W/H` (3840×2160→2560×1440) and clamps `SPAWN_MAX_DIST` (1100→640) / `DOCK_MAX_DIST` (900→620) to match — `STAR_DENSITY` untouched, since `STAR_COUNT`'s area-derived formula auto-scales the far layer.

### 2.12 Health, Damage & Knockback (v1.3)

Discrete lives (3 stock + an extra life per 10,000 points) are gone. The ship carries a single HP pool for the entire run; survival is about managing that pool, not stock count. This keeps Pillar 4 intact — the shield is still the "free," energy-priced answer that prevents damage outright — while making one misjudged contact a costly setback rather than an instant stock loss.

- **HP pool.** `ship.hp` starts at `SHIP_MAX_HP` (250). The HUD shows it as a "HULL" bar top-left, styled exactly like the shield-energy bar (same stroke/fill pattern; `COLOR.hp` green, switching to red below 30%).
- **Damage table** (contact = ramming a hazard body; every value is a named constant in the tuning block — first-pass and tunable):

  | Source | Damage | Constant |
  |---|---|---|
  | Small hazard (small debris satellite, small saucer) | 20 | `DMG_SMALL` |
  | Medium hazard (medium debris satellite, big saucer) | 35 | `DMG_MEDIUM` |
  | Large hazard (large debris satellite) | 50 | `DMG_LARGE` |
  | Hunter Satellite (large / medium / small) | 60 / 45 / 30 | `HUNTER_DAMAGE` |
  | Hostile (saucer) bullet | 15 | `DMG_BULLET` |

  Each hazard carries its own `damage` field, set in its constructor. Debris satellites map onto the shared tiers via the `DEBRIS_DAMAGE` size table; **Hunter Satellites (v1.6) carry their own higher per-tier values** (`HUNTER_DAMAGE` = 60/45/30) since they are the actively-seeking threat; saucers still use `DMG_MEDIUM`/`DMG_SMALL` by size. The shared `DMG_*` constants and the bullet value remain first-pass and tunable.
- **Knockback.** A non-lethal unshielded hit **sets** (not adds) the ship's velocity to `KNOCKBACK_SPEED` (250 px/s) pointing directly away from the hazard's center (wrap-aware, via `shortDelta`), so the ship physically separates instead of grinding inside the hazard. Setting rather than adding guarantees clear separation regardless of prior momentum; drag then bleeds the impulse off naturally over the following second.
- **Hit-stun.** The same hit sets `ship.invuln = HIT_STUN_DURATION` (1.0 s) of invulnerability (the existing blink render doubles as the hit-stun tell). Further hits are ignored until it expires, so no single hazard can chew through the pool in consecutive frames, and several hazards overlapping in one frame still only land one hit. `damageShip()` self-guards on shield/hit-stun/dead, so the collision passes don't need to break early.
- **Death.** At 0 HP `killShip()` runs the explosion boom + particles, `scatterChain()` drops the whole tow load as free garbage, and the state goes straight to `gameover`. There is no respawn path — the old "respawn only when the center is clear" logic (former §2.1) is deleted entirely.
- **Shield unchanged.** A raised shield deflects/blocks at its usual energy cost, and a shielded hit takes **no** damage, knockback, or hit-stun — knockback is strictly the unshielded fallback. The shield still does not protect the tow chain (§2.10.1).

> Tuning constants live in the "Health, damage & knockback (F2)" block at the top: `SHIP_MAX_HP`, `DMG_SMALL/MEDIUM/LARGE`, `DMG_BULLET`, `KNOCKBACK_SPEED`, `HIT_STUN_DURATION`, `REPAIR_MILESTONE`, `REPAIR_AMOUNT`, `REPAIR_FULL_BONUS` (the per-size contact-damage map `DEBRIS_DAMAGE` lives with the debris constants).

### 2.13 Difficulty Ramp & Early-Game Pacing (v1.5)

Implements Pillar 6 ("Ease players in", §1). One shared, wave-driven curve makes the opening waves approachable and lets intensity build on a schedule, tuned from a single knob.

- **The curve.** `difficultyFactor(wave)` = `1 − Math.exp(−(wave − 1) / RAMP_WAVES)` — **0 at wave 1**, climbing asymptotically toward (but never reaching) 1 over many waves. With `RAMP_WAVES = 8` (the one knob that sets how fast the whole game gets hard): wave 1 ≈ 0.00, wave 2 ≈ 0.12, wave 3 ≈ 0.22, wave 5 ≈ 0.39, wave 8 ≈ 0.58, wave 12 ≈ 0.75, wave 20 ≈ 0.91, plateauing near full by ~wave 25. Larger `RAMP_WAVES` = slower ramp (longer "enjoy the early game" window); smaller = steeper.
- **The interpolation helper.** `ramp(floor, ceil, wave)` = `floor + (ceil − floor) × difficultyFactor(wave)`. **Every** wave-driven threat parameter is expressed this way from a named floor (easy wave-1 value) / ceiling (full-intensity value) pair in the constants block, so the entire game's early-game pacing lives in one place. At wave 1 a parameter sits exactly on its floor; by the late waves it has closed most of the way to its ceiling.
- **What scales as of v1.5 — the saucers (§2.6):** spawn gap (`rand(20,30)s` floor → `rand(12,16)s` ceiling), fire-rate multiplier (`1.8×` → `1.0×`), small-saucer aim error (`±0.35` → `±0.09` rad), and small-saucer appearance chance (`15%` → `60%`, folding in the old >8000-point gate so escalation is wave-driven in one place, not two).
- **What also scales (v1.6):** the **Hunter Satellites** (§2.5) wire every drift/homing speed and turn rate into this same `ramp`/`difficultyFactor` — their full-intensity numbers are the ceilings, `HUNTER_FLOOR_FRAC` sets the gentle wave-1 floor. So at wave 1 the whole Hunter family moves at ~60% speed, ramping up over ~20 waves.
- **What will scale next (not yet built):** the **Debris Satellite density** retune (the joint F3+F10 pass) still lands later; with garbage now permanent (v3.2 P3) it leans on coalescence + `GARBAGE_MAGNET_RANGE` and the drop-volume constants for density control (§2.10.1) rather than on decay or the difficulty curve.

> Tuning constants live in the "Difficulty ramp & early-game pacing (F10)" block at the top: `RAMP_WAVES`; the saucer floor/ceiling pairs `SAUCER_GAP_FLOOR_MIN/MAX`, `SAUCER_GAP_CEIL_MIN/MAX`, `SAUCER_FIRE_MULT_FLOOR/CEIL`, `SAUCER_AIM_ERR_FLOOR/CEIL`, `SAUCER_SMALL_CHANCE_FLOOR/CEIL`; and the saucer base fire ranges `SAUCER_FIRE_INIT/BIG/SMALL`. The curve helpers `difficultyFactor` and `ramp` live in the Helpers block.

### 2.14 Powerups (v1.7)

Five free-floating pickups (the `Powerup` entity class, standard contract) add temporary player buffs and an HP top-up. Each reads as a rotating hexagon housing (so all five register as one "collect me" family) wrapping a distinct vector-glow glyph, drawn via the shared `drawPowerupGlyph()` helper (reused by the HUD). Colors are hued apart for a readable tell (`POWERUP_COLOR`): Rapid amber, Triple magenta, Health red, Magnet blue, Engine mint.

- **Spawn sources.**
  - **Health** spawns *ambiently* on a saucer-like timer (`POWERUP_HEALTH_GAP` = 18–26 s between spawns), one at a time (its own `POWERUP_DECAY` bounds coexistence, like the one-saucer-at-a-time gate), in a reachable ring `[POWERUP_HEALTH_MIN_DIST, POWERUP_HEALTH_MAX_DIST]` = [220, 520] px around the ship. It reads as something that *shows up in the field*, not loot.
  - **Rapid Fire / Triple Shot / Magnet / Engine** drop from **small-tier Debris/Hunter Satellite kills** at `POWERUP_DROP_CHANCE` (10%, tunable in the 8–12% band), rolled at the single choke point `maybeDropPowerup()` inside `destroyDebris`/`destroyHunter`'s small-tier branch. This ties powerup access to finishing off a lineage. Health is **not** in the drop pool (`POWERUP_DROP_TYPES`).
- **Effects** — the four dropped types are *timed* (`POWERUP_DURATION` = 15 s); Health is *instant*:
  - **Rapid Fire:** raises the effective bullet cap from 4 to `RAPID_MAX_BULLETS` (8) while active (§2.2).
  - **Triple Shot:** fires 3 bullets in a narrow `TRIPLE_SPREAD` (0.14 rad) fan per shot and raises the cap to `TRIPLE_MAX_BULLETS` (12). **Rapid + Triple both active uses the higher cap (12), never the product (24)** — `maxBullets()` returns Triple's cap when Triple is up, regardless of Rapid.
  - **Health:** instant — restores +`POWERUP_HEALTH_AMOUNT` (25) HP, capped at max (§2.7). No timer.
  - **Magnet:** while active, free garbage within an extended radius (`MAGNET_RANGE_MULT` × `GARBAGE_PICKUP` = 3× ≈ 54 px) is pulled toward the ship by a real attraction force (`MAGNET_PULL` accel with `MAGNET_DAMP` in-range velocity damping so it glides rather than jitters), and the effective pickup radius grows by `MAGNET_PICKUP_MULT` (1.6×). The pull is a velocity nudge, not a teleport — it respects momentum (Pillar 2).
  - **Engine:** while active, halves the effective towed-mass sum returned by `chainMass()` (`ENGINE_MASS_MULT` = 0.5). Because that one function feeds the thrust penalty, top-speed penalty (both in `Ship.update`) and the momentum-tug `massFactor` (in `updateChain`), all three lighten at once — "easier to maneuver even when towing garbage."
- **Expiry modes (v3.0 P5 — configurable via §2.16 Options → Difficulty).** By default every timed effect expires on the `POWERUP_DURATION` clock (above). Two opt-in settings can swap the clock for a **use budget** on some effects: `shotPowerupMode` (`"time"` | `"shots"`) governs **Rapid Fire + Triple Shot** — a budget of `RAPID_SHOTS` / `TRIPLE_SHOTS` **trigger-pulls**, where a Triple 3-fan is **one** pull and, with both active, each budget decrements once per pull and ends **independently** (Triple can run out while Rapid keeps single shots going); `magnetMode` (`"time"` | `"pieces"`) governs the **Magnet** — a budget of `MAGNET_PIECES` **canisters hooked**, counted at the hook (not the pull), so a magnet that draws-then-hooks a piece spends only once. **Engine is always time-based** (not player-configurable); Health is always instant. Count budgets live in `game.powerBudget {rapid, triple, magnet}` (reset with the run, parallel to `powerFx`). A single predicate **`powerActive(type)`** resolves "is this effect live?" against the effect's mode (`powerFx` seconds in time mode, `powerBudget` count otherwise), and **`powerMode(type)`** returns the governing mode — the fire block, `maxBullets()`, `chainMass()`, the Magnet pull and the HUD all read through these so the two modes can never diverge. Both settings default to `"time"`, so shipped behaviour is unchanged until opted in.
- **Stacking rule.** A same-type pickup while already active **refreshes** to full — the timer to `POWERUP_DURATION` (time mode) *or* the budget to `POWERUP_BUDGET[type]` (count mode) — never adding duration/shots or stacking magnitude; different types run independently (Magnet + Rapid together is fine). Active-effect timers live in `game.powerFx` `{rapid, triple, magnet, engine}` (remaining seconds; 0 = inactive), counted down each frame in `update()`; count budgets live in `game.powerBudget`.
- **HUD.** A row under the CARGO readout shows one entry per **active** effect (`powerActive`): the type glyph + a shield-bar-style remaining bar. In time mode the bar is `remaining / POWERUP_DURATION`; in a count mode it's `remaining / POWERUP_BUDGET[type]` with the numeric count (shots/pieces left) as a label beside it — one renderer, two data sources. Health, being instant, never appears there. A `FloatText` label and a rising two-note `AudioSys.powerup()` chime confirm each pickup.
- **Pickup / lifecycle.** Collected when the ship comes within `POWERUP_RADIUS + SHIP_RADIUS`. Uncollected powerups drift (a settling drop-kick), screen-wrap, and fade after `POWERUP_DECAY` (14 s) with the same blink countdown as garbage. Drawn above the hazards in z-order so they stay readable.

> Tuning constants live in the "Powerups (F6)" block at the top: `POWERUP_DURATION`, `POWERUP_RADIUS`, `POWERUP_DECAY`, `POWERUP_DROP_CHANCE`, `POWERUP_DROP_TYPES`, `POWERUP_HEALTH_GAP`, `POWERUP_HEALTH_AMOUNT`, `POWERUP_HEALTH_MIN/MAX_DIST`, `RAPID_MAX_BULLETS`, `TRIPLE_MAX_BULLETS`, `TRIPLE_SPREAD`, `MAGNET_RANGE_MULT`, `MAGNET_PICKUP_MULT`, `MAGNET_PULL`, `MAGNET_DAMP`, `ENGINE_MASS_MULT`, plus the v3.0 P5 count-budget knobs `RAPID_SHOTS` (40), `TRIPLE_SHOTS` (30), `MAGNET_PIECES` (20) and the `POWERUP_BUDGET` lookup that maps each type to its full budget. `POWERUP_COLOR`/`POWERUP_LABEL` sit with `COLOR`; the `Powerup` class + `drawPowerupGlyph` are in the Entities block; `powerMode`/`powerActive`/`maxBullets`/`maybeDropPowerup`/`spawnHealthPowerup`/`applyPowerup` are Flow functions. The two expiry-mode settings (`shotPowerupMode`, `magnetMode`) live on the persisted `settings` object (§2.16).

### 2.15 Controller Input (v1.8)

Gamepad support layered onto the existing keyboard input with **no change to any call site** — everything downstream still calls `input.left()`, `input.thrust()`, etc. The input layer now resolves each action against *either* the keyboard *or* the first connected gamepad, driven by a single data-driven binding table so Phase 8's rebinding UI has one structure to edit. **These are hardcoded defaults; the rebinding UI itself is F8/Phase 8.**

- **Binding table (`bindings`).** One entry per action, expressed as data — not per-action logic: each lists its keyboard `keys` (lowercased, matching the `keys{}` map), gamepad `buttons` (Standard-Gamepad indices from the `GP` enum), an optional analog `axis` (`{index, dir}`, where `dir < 0`/`> 0` is the negative/positive half past the deadzone), and a `fixed` flag (F7 spec: gameplay actions are rebindable → `fixed:false`; menu/system actions are non-configurable → `fixed:true`).
- **Gameplay defaults** (rebindable in F8): **D-Pad Left / Left Stick X < −0.25 → rotate CCW**; **D-Pad Right / Left Stick X > 0.25 → rotate CW**; **D-Pad Up / Left Stick Y < −0.25 → thrust**; **A → fire**; **Right Trigger *or* Right Bumper → shield**. The stick threshold is `GP_DEADZONE` (0.25, the one controller-*feel* tuning knob), applied with a strict `>` so exactly-`0.25` does not register. *The shield binding was not specified in F7 — right trigger/bumper is a deliberate gap-fill (see the delivery notes / STATUS.md); everything else is Paul's spec.*
- **Menu / system reads** (fixed, wired now for later phases): **Start → start / pause / unpause** (mirrors Enter on title/gameover and `P` while playing), **A → confirm** (title/gameover only, so it never collides with A = fire during play), **B → back** (reserved — no menu screen to back out of until F8). These are **edge-triggered** (fire once per press, not every held frame) via `gpPressed()`, which compares the current button snapshot against the previous frame's.
- **Polling.** `pollGamepad()` runs once per frame in the main loop, *before* `update()`. It reads `navigator.getGamepads()`, takes the **first connected** pad (skipping empty `null` slots) and ignores any others, and snapshots its buttons/axes (plus the prior frame's buttons for edge detection). `handleGamepadMenu()` runs right after it to process the edge-triggered menu reads. Held-state gameplay actions are resolved lazily inside `input.*()` from that snapshot, so `Ship.update` reads them exactly as it reads the keyboard.
- **Audio unlock caveat.** A controller Start/A press makes a best-effort `AudioSys.init()`/`resume()` (the same call the keyboard does on keydown). Browsers do not reliably treat gamepad input as a user-activation gesture for the Web Audio autoplay policy, so a controller-only player may still need one keyboard press to enable sound — flagged as a playtest item, not a code bug.

> Tuning constant: `GP_DEADZONE` (0.25) in the "Controller / gamepad input (F7)" block at the top. The `GP` button/axis-index enum, the `bindings` table, `pollGamepad`/`handleGamepadMenu`, and the resolution helpers (`kbActive`/`gpActive`/`actionHeld`/`gpPressed`) all live in the Input section; `input` and the keyboard listeners are unchanged in shape.

### 2.16 Pause Menu, Options & Control Rebinding (v1.9)

The game's first real menu/UI state machine, layered on top of the F7 input layer (§2.15) so every screen is navigable by keyboard **or** the first connected gamepad. It turns v1.8's hardcoded `bindings` into player-editable ones and gives the volume sliders something to drive.

- **State machine.** `game.paused` is true **iff** a pause-menu screen is open; `game.menu.screen` names it: `null` (closed) → `"root"` → `"options"` (SFX / Music / Master sliders + Controls + Achievements + Difficulty + Back) → `"controls"` (per-action rebind grid + Return to Defaults + Back), plus `"achievements"` (read-only viewer, §2.17) and `"difficulty"` (powerup-expiry mode toggles, v3.0 P5 — below). `game.menu` also carries `index` (cursor for root/options), `row`/`col` (cursor for the controls grid: one row per rebindable action, col 0 = keyboard, col 1 = gamepad; plus Return-to-Defaults and Back rows), `rebinding` (`{action, device}` while capturing, else `null`), and `achReturn` (`"root"` | `"options"` — which parent the Achievements viewer backs out to, since it has two entry points). **Quit returns to the title screen** (a webpage can't reliably close its own tab).
- **Opened from any state (v3.0 P4 — supersedes B-1-a; closes the long-standing "menu reachable only via pause").** `openPause()` is now callable from `title`/`gameover` as well as `playing`; `update()` already skips the sim outside `playing`, so `game.paused` is safe in any state. The **root layout is context-aware**, keyed purely on `game.state` (pausing mid-game keeps state `"playing"`; the system menu keeps `title`/`gameover`): `rootItems()` returns `MENU_ROOT_PLAY = ["Continue","Options","Quit"]` when playing, else `MENU_ROOT_SYS = ["Options","Achievements","Back"]`. `menuRoot()` dispatches **by label** so one handler serves both layouts; "Back" (system root) and "Continue" (play root) both `closePause()`. The Options/Controls/Achievements sub-tree is reused **unchanged**; the only sub-tree tweak is `achReturn`, so the Achievements viewer backs to the system root when entered there and to Options when entered via Options. The panel title reads "PAUSED" mid-game, "MENU" for the system menu.
- **Input routing & separation (the core correctness concern).** Both input methods funnel discrete actions (`up/down/left/right/confirm/back/pause`) into one `menuInput(action)` dispatcher — keyboard via the `keydown` handler (arrows/WASD nav, Enter confirm, **Esc back**, read from the same `bindings.confirm/back/pause` entries), gamepad via `handleGamepadMenu()` (edge-detected D-Pad/Left-Stick nav through a `menuNav` latch; A confirm / B back / Start resume). Both handlers check **three contexts in priority order**: (1) **rebind capture** — the pressed key/button *is* the new binding and is consumed entirely (never written to `keys{}`, never routed to nav); (2) **menu open** — input drives navigation only; (3) **normal play/title/gameover**. Because gameplay reads (`input.left()` …) happen only inside `update()`, which early-returns while `paused` (or outside `playing`), **menu input can never move the ship**.
  - **v3.0 P4 — the two newly context-dependent inputs.** `bindings.pause.keys = ["escape"]` (P retired, FLAG P4-a) and `bindings.back.keys = ["escape"]` **share** ESC: in-menu, `handleMenuKey` resolves **confirm → back → pause** in order, so ESC (matching `back` first) means "back"; outside a menu, the normal branch's `pause`-during-`playing` check makes ESC pause. Controller **Start** is the session toggle (`playing → openPause`, else `startGame`; while paused the menu branch routes Start → `menuInput("pause")` = dismiss/resume). Controller **B** mirrors ESC's dual meaning: menu-open → `back`; no menu on `title`/`gameover` → `openPause()` (the system menu). Keyboard **"O"** is B's keyboard twin for opening the system menu (handled inline in the normal branch, not the binding table). In the gamepad normal branch, Start/B/A are an **else-if chain** so one frame can't both open a menu and start a game (**FLAG P4-b**); on keyboard the menu-open context returns before the title "press to start" line, giving the same guarantee.
- **Volume.** `AudioSys` routes every voice through an SFX gain → master gain bus (+ an unconnected music gain), so the three sliders attenuate real output (§2.8). Values live on `AudioSys.vol {master,sfx,music}` (0..1, clamped), applied to the live gain nodes by `setVol()`; the sliders nudge by `VOL_STEP` (0.1).
- **Rebinding.** Selecting a binding cell and confirming arms capture for that `(action, device)`; the next key (keyboard) or button (gamepad) rewrites `bindings[action].keys`/`.buttons`. A **gamepad** rebind replaces the button set and **drops the analog axis** for that action (so the displayed binding matches what actually fires — a stick direction can't be captured, only a button). Esc cancels a capture. **Return to Defaults** restores the five rebindable actions to `DEFAULT_BINDINGS` — a pristine snapshot taken at load, *before* saved settings are applied. Only the F7 gameplay actions (`fixed:false`: rotate CCW/CW, thrust, fire, shield) are editable; menu/system actions (A/B/Start) stay fixed.
- **Difficulty screen (v3.0 P5, B-4/B-5).** A `"difficulty"` sub-screen reached from Options → Difficulty (mirrors the tested `menuOptions`/`drawOptionsMenu` pattern via `menuDifficulty`/`drawDifficulty`). Two left/right toggle rows — **"Shot powerups expire: Time | Shots"** (`settings.shotPowerupMode`) and **"Magnet expires: Time | Pieces"** (`settings.magnetMode`) — plus Back; ◄ selects the timed default, ► the count variant. Flipping a toggle persists immediately (`saveSettings`). These two settings drive the powerup expiry logic in §2.14; both default to `"time"`. The screen is deliberately structured to grow (a `DIFFICULTY_ROWS` list + a switch case + a renderer row) as future difficulty knobs land (FLAG B-4-c).
- **Persistence.** Volumes + custom bindings **+ the two difficulty modes** (`shotPowerupMode`, `magnetMode`) save to `localStorage` (`afd_settings_v1`) on every change and load once at startup, over the shipped defaults. The v3.0 P5 modes are **additive fields** — no schema-version bump: an older save missing them simply keeps the `"time"` default, and a corrupt/unknown value is ignored on load. **All storage access is `typeof`-guarded and try/caught** — `localStorage` is unavailable in the claude.ai artifact sandbox (and can throw in privacy modes), and a storage failure must never crash the game (GDD §4 item 1). *Persistence itself still needs verifying in a real browser outside the sandbox — see STATUS.md.*

> Tuning/structure: `MENU_ROOT`/`MENU_OPTIONS`/`VOL_CATS`/`VOL_STEP`/`STORAGE_KEY` and the whole state machine (`openPause`/`closePause`/`quitToTitle`/`menuInput`/`menuRoot`/`menuOptions`/`menuControls`/capture/`returnToDefaults`/`saveSettings`/`loadSettings`) live in a new **"Menu state machine, Options & Rebinding (F8)"** section after Input; `REBINDABLE`/`DEFAULT_BINDINGS`/`GP_NAME` sit with the `bindings` table; `AudioSys.setVol`/`ui` + the gain graph are in AudioSys; `drawMenu` and the label formatters are near `drawText`. (v2.0 adds an **Achievements** row to `MENU_OPTIONS`, opening a `"achievements"` screen — §2.17. v3.0 P5 adds a **Difficulty** row + a `"difficulty"` screen — `DIFFICULTY_ROWS`/`menuDifficulty` in the menu section, `drawDifficulty` near `drawText` — and the persisted `settings` object `{shotPowerupMode, magnetMode}` alongside `VOL_STEP`; both saved into `afd_settings_v1`.)

### 2.17 Achievements (v2.0; tiered lifetime v3.0 P7; new tiered/event/weekly v3.0 P8)

A progress-tracking layer that **observes** stats the rest of the game already produces and never drives gameplay — the widest-reaching but lightest-touch system, all funnelled through one `Achievements` module (defined after the powerup flow functions). Two pools, 35 achievements total.

- **Two pools.** **Weekly** — 16 single-game challenges, of which a **calendar-deterministic 5-wide slice** is active each ISO week (so every player worldwide sees the same 5 on the same days); a fresh week resets weekly-unlock progress. **Lifetime** — 19 achievements that accumulate across all games (total deliveries, kills by type, best wave, play time, etc.). Each definition is `{ id, pool, name, desc, cur(s,l), text?, goal | tiers }`; `cur()` reads the per-game stats bag (`game.stats`) and the persisted lifetime counters (`Achievements.lifetime`), plus `game.wave` for the "reach wave N" ones.
- **Single-goal vs tiered (v3.0 P7/P8).** A **single-goal** entry (all 16 weekly + the 5 non-tiered lifetime) carries a `goal` and is **unlocked when `cur >= goal`** — one unlock, one toast. A **tiered** lifetime entry (14 of the 19) carries an ascending `tiers:[bronze,silver,gold,titanium,platinum,diamond]` ladder instead of a `goal`; `evaluate()` unlocks **every newly-crossed tier in one pass** (a big jump can pop several toasts at once, ascending, each naming its tier — "Recycling Magnate — Gold"), and the module persists the **highest tier index reached per id** in `lifetimeTiers{id:idx}`. `TIER_NAMES` + a warm-metal→white `TIER_COLOR[]` (kept in the vector-glow palette so a badge never reads as a hazard tell — FLAG A-1-a) live with `COLOR`.
- **Two lifetime counter kinds — SUM vs MAX (FLAG A-1-b).** Most lifetime counters are **sums** (running totals, `l.X++` at the event). Four tiered ones tier on a **personal-best MAX** instead — updated with `Math.max(...)` at the event, never `+=`: **Master of the Field** (`l.maxWave`), **No Powerups Needed** (`l.maxWaveNoPowerup` — best wave reached while `game.stats.powerupsPicked === 0`, updated at each wave-clear and **frozen** for the rest of that game the moment any powerup is picked), and the two P8 personal-bests **Salvage King** (`l.bestDeliveredGame`, most canisters delivered in one game) + **Field Sweeper** (`l.bestDebrisGame`, most Debris destroyed in one game — both `Math.max`'d at their event site as the per-game count climbs). The tier machinery is identical, only the counter's update rule differs.
- **Weekly rotation (deterministic, no randomness).** At launch the real UTC date yields an ISO week + week-year via `isoWeekYear()` (Monday-based, Thursday-of-week rule); the pool index is **`(isoYear × 52 + isoWeek) % 16`** and the active set is the 5 consecutive pool entries from that index, **wrapping** around the 16. Every player in the same calendar week gets the same 5.
- **The 16 weekly:** Scrap Runner (deliver 20 canisters/game), Heavy Hauler (a full 12-chain in one dock visit), Glass Cannon (reach wave 5 with no Health powerup), Satellite Buster (15 Debris/game), Hunter's Bane (wipe a whole 13-member Hunter line from one spawn), Pacifist Tow (5 deliveries in a row without firing), Close Shave (survive a Hunter hit under 10 HP), Shield Surfer (10 shield deflects/game), Small Ball (10 small saucers/game), No Scratches (clear wave 3 damage-free), Combo Collector (delivery combo 8+ in one visit), Speed Recycler (first delivery within 60 s), Powered Up (use all four drop powerups/game), Diamond Cutter (3 large Hunter cores/game), Waste Not (**v3.2 P3, repurposed in place** — finish a game with **no Hunters born from neglected scrap**, i.e. `s.hunterCoalesced === 0`; the id + pool slot are kept so the weekly rotation math is unchanged. Its old meaning, "zero canisters expired," auto-unlocked every game once decay was removed, so it was repurposed to a live challenge with the same "don't let your mess pile up" spirit — §2.5.1), and **Flawless Run (v3.0 P8** — clear **wave 8 or later** damage-free; a deliberately harder, late-wave counterpart to No Scratches, FLAG A-6-b, keyed off `s.flawlessLateWave` set in the wave-clear block).
- **The 19 lifetime.** **Fourteen are tiered** — bronze→diamond ladders. *Shipped v3.0 P7:* **Recycling Magnate** (deliver, SUM `l.delivered`, 1k/5k/10k/25k/50k/100k), **Ghost Protocol** (Hunters, SUM, 50/250/1k/2.5k/5k/10k), **Saucer Hunter** (saucers, SUM, 250/500/1k/2.5k/5k/10k), **Perfect Wave** (damage-free waves, SUM, 5/10/50/100/250/500), **Iron Hull** (hits survived, SUM, 100/500/1k/2.5k/5k/10k), **Master of the Field** (best wave, **MAX**, 5/10/25/50/75/100), **No Powerups Needed** (best wave powerup-free, **MAX**, 2/5/11/17/23/29 — diamond is a deliberate "is this even possible" ceiling, FLAG A-6-a). *Added v3.0 P8:* **Sharpshooter** (small saucers, SUM `l.smallSaucerKills`, 100/500/1k/2.5k/5k/**7500** — diamond lowered from 10k so it doesn't out-grind Saucer Hunter's combined ladder, FLAG A-8-a), **Freight Baron** (≥12-canister dock visits, SUM per-EVENT `l.heavyHaulerEvents`, 1/10/50/150/400/1000), **Zen Master** (5-in-a-row no-fire hauls, SUM per-EVENT `l.pacifistTowEvents`, 1/10/50/150/400/1000), **Daredevil** (Glass-Cannon games, SUM once-per-game `l.glassCannonGames`, 1/5/20/50/125/300 — softened because it can fire at most once/game, FLAG A-12-a), **Wave Rider** (Shield-Surfer games, SUM once-per-game `l.shieldSurferGames`, 1/5/20/50/125/300 — same softening), **Salvage King** (**MAX** `l.bestDeliveredGame`, 10/20/40/75/125/200), **Field Sweeper** (**MAX** `l.bestDebrisGame`, 15/40/80/150/250/400). *(Per-EVENT vs per-game resolved FORK A-fork-1: Freight Baron/Zen Master count **every** qualifying haul in a game; Daredevil/Wave Rider are inherently once-per-game.)* **Five stay single-goal:** Century Club (reach wave 25), Untouchable (reach wave 10 never below 50% HP), Ton of Scrap (10,000 pts from deliveries), **The Long Haul** (deliver **≥12** canisters in one dock visit **10×** — a *fixed* 12 threshold decoupled from the growing `game.cargoMax`, FLAG A-5-a / B-8-b; keys `l.fullChains`), Marathon Runner (10 h play time).
- **Stat hooks (observe-at-the-source, no new indirection).** Per-game counters/flags live in a flat `game.stats` bag (built by `resetGameStats()`, reset in `startGame`); lifetime counters live on `Achievements.lifetime`. Hooks are one-liners placed where each event already fires: `destroyDebris`/`destroyHunter` (kill tallies, Hunter-lineage completion, large-core count, **+ Field Sweeper `Math.max`**), the dock offload block in `update()` (delivered/combo/full-chain/first-canister-timing + lifetime delivered/score, **+ Freight Baron latch on the fixed 12-in-a-visit, Zen Master latch on `pacifistStreak === 5`, Salvage King `Math.max`**), `damageShip` (per-wave damage, survived-hit tally, below-half flag), the Close-Shave check at the Hunter-vs-ship contact site, `shieldDeflect` + the three other shield-absorb sites (deflect count), the saucer-kill sites (saucer tallies **+ Sharpshooter lifetime small-saucer total**), `applyPowerup` (powerup usage), **`coalesceGarbage`'s transform site (`hunterCoalesced++` for Waste Not — v3.2 P3, replacing the removed `Garbage.update` decay-expiry hook)**, the wave-clear block (perfect-wave detection **+ Flawless Run at wave ≥ 8**), `nextWave` (best-wave + per-wave damage reset), and **`killShip` (Daredevil + Wave Rider tallied once per game from `game.stats.healthPicked`/`deflects`)**. `Achievements.evaluate()` runs once per frame (already-unlocked entries short-circuit) and again at game over (so end-of-game checks like Waste Not, Daredevil, Wave Rider can fire).
- **Notification + viewer.** An unlock pushes a non-blocking **toast** (a glow-stroked gold banner, `ACH_TOAST_TIME` = 4.5 s, top-centre, fade in/out, stacks — a multi-tier jump stacks one toast per crossed tier) and plays a distinct `AudioSys.achievement()` fanfare; toasts age out in the main loop (`updateToasts`, independent of pause/state). A read-only **viewer** (`drawAchievements`) reachable via **Pause → Options → Achievements** lists this week's 5 (left) and the 19 lifetime across **two columns** (P8 grew the set past a single column, so the panel widened to a three-column layout: weekly | lifetime | lifetime-cont.). Single-goal rows show a ✓ (gold) when earned or a `cur/goal` readout; **tiered rows show a tier badge + progress to the *next* tier in that tier's colour** (e.g. "Gold · 12,431 → 25,000"; "Diamond ✓ MAX" at the top; "— · 0 → 1,000" pre-bronze — one row per achievement, `drawAchRow`). *(Chosen entry point: the pause Options menu, reusing the tested F8 state machine; a title-screen entry remains an easy future add — the title screen shows a hint pointing players to Pause → Options.)*
- **Persistence (`afd_achievements_v2`, v3.0 P7).** Progress + unlock state save to their **own** `localStorage` key (separate from settings' `afd_settings_v1`), new shape: `{ lifetime, lifetimeTiers:{id:idx}, lifetimeUnlocked:[…non-tiered ids], weekly:{ key, unlocked[] } }`. Saved on each unlock, at game over, on Quit, and periodically (`ACH_SAVE_EVERY` = 30 s). Weekly unlocks only carry over within the same calendar-week key; a different stored week starts the weekly set fresh (lifetime always retained). **Best-effort migration (FLAG A-2 / A-2-a):** if no `_v2` save exists but an old `_v1` one does, its raw lifetime **counters** are kept and the tier badges + non-tiered unlocks **re-derive** silently from them (via `deriveLifetime()`, no toast storm); the old flat `_v1` unlock sets are ignored (a returning player's current-week weekly unlocks may reset once). `deriveLifetime()` also runs after every load so badges match the counters before the first in-game `evaluate()`. **All access `typeof`-guarded + try/caught** (same contract as F8) — a corrupt payload or storage failure never crashes. *Real cross-session persistence still needs a browser check outside the artifact sandbox — see STATUS.md.*

> Tuning constants live in the "Achievements (F9)" block at the top: `ACH_TOAST_TIME`, `ACH_WEEKLY_SHOWN` (5), `ACH_LINEAGE_FULL` (13), `ACH_SAVE_EVERY`. `TIER_NAMES` + `TIER_COLOR[]` (v3.0 P7) sit with `COLOR`. The `Achievements` module (pools + `isoWeekYear`/`poolIndex`/`selectWeekly`/`evaluate`/`deriveLifetime`/`onUnlock`/`tierIndex`/`tierStatusText`/`tick`/`loadCounters`/`save`/`init`) sits after the powerup flow functions; `resetGameStats` + `game.stats`/`game.toasts` are with the game object; `fmtCommas`/`drawAchievements`/`drawAchRow`/`drawToasts` are near `drawText`; `AudioSys.achievement` is in AudioSys.

---

## 3. Code Architecture Map

Everything lives in one `<script>` block. Reading order top to bottom:

| Section | Contents | Notes for modification |
|---|---|---|
| **Constants** | All tuning values (SHIP_*, BULLET_*, SHIELD_*, `DEBRIS_*`, scores) + v1.1 salvage block (GARBAGE_*, CHAIN_*, CARGO_*, DOCK_*) + v1.2 world block (`WORLD_W/H`, `VIEW_W/H`, `CULL_MARGIN`, `SPAWN_MIN/MAX_DIST`, `DOCK_MIN/MAX_DIST`, `STAR_DENSITY`) + v1.3 HP block (`SHIP_MAX_HP`, `DMG_SMALL/MEDIUM/LARGE`, `DMG_BULLET`, `DEBRIS_DAMAGE`, `KNOCKBACK_SPEED`, `HIT_STUN_DURATION`, `REPAIR_*`). v1.4 (F3): the debris table is `DEBRIS_SPEEDS/RADII/SCORE/DAMAGE` (was `AST_*`) plus `DEBRIS_GARBAGE` (3 canisters/kill); `GARBAGE_DROP` was removed; `GARBAGE_DECAY` 20→12 and a new `GARBAGE_SEVER_DECAY` (10) replaced the inline 15. v1.5 (F10): a "Difficulty ramp & early-game pacing" block — `RAMP_WAVES` (the ramp knob) + the saucer floor/ceiling pairs and base fire ranges (see §2.13). v1.6 (F4): a "Hunter Satellites" block replacing `SAT_SCORE`/`WEDGE_SCORE` — `HUNTER_RADII/SCORE/DAMAGE`, `HUNTER_SPEED_CEIL/TURN_CEIL` (the difficulty ceilings), `HUNTER_FLOOR_FRAC` (wave-1 gentleness knob), `HUNTER_SCATTER`, `HUNTER_GARBAGE/SMALL_GARBAGE/SMALL_MASS`; plus `COLOR.garbageLight` for low-mass scrap (see §2.5). v1.7 (F6): a "Powerups" block — `POWERUP_DURATION/RADIUS/DECAY/DROP_CHANCE/DROP_TYPES`, `POWERUP_HEALTH_*`, the weapon caps `RAPID_MAX_BULLETS`/`TRIPLE_MAX_BULLETS`/`TRIPLE_SPREAD`, the `MAGNET_*` (range/pickup/pull/damp) and `ENGINE_MASS_MULT` (see §2.14); `POWERUP_COLOR`/`POWERUP_LABEL` sit with `COLOR`. v1.8 (F7): a "Controller / gamepad input" block — just `GP_DEADZONE` (0.25, the analog-stick threshold / the one controller-feel knob); the `GP` index enum and `bindings` table are structural, not balance, and live in the Input section (see §2.15). v2.0 (F9): an "Achievements" block — `ACH_TOAST_TIME` (4.5), `ACH_WEEKLY_SHOWN` (5), `ACH_LINEAGE_FULL` (13), `ACH_SAVE_EVERY` (30); the achievement pools/logic are structural and live in the Achievements module (see §2.17). v2.1: `BOUNDARY_DASH`/`BOUNDARY_GAP`/`BOUNDARY_WIDTH`/`BOUNDARY_GLOW` in the v1.2 world block — the dashed wrap-seam visibility line (cosmetic, uses `COLOR.dim`). v3.0 (P1): `STAR_DENSITY` retuned 40→80; new `STAR_PARALLAX_FACTOR`, `STAR_BRIGHT_MIN/MAX`, `STAR_NEAR_DENSITY`, `STAR_NEAR_BRIGHT_MIN/MAX` in the same v1.2 world block — starfield legibility + a near parallax layer (cosmetic). v3.0 (P2): `FIRE_COOLDOWN` retuned 0.16→0.20 (base gun, slightly slower); new `RAPID_FIRE_COOLDOWN` (0.09) in the same weapons block — Rapid Fire now genuinely speeds up cadence, not just the bullet cap (see §2.2). v3.0 (P5): count-based powerup-expiry budgets in the Powerups block — `RAPID_SHOTS` (40), `TRIPLE_SHOTS` (30), `MAGNET_PIECES` (20) + the `POWERUP_BUDGET` lookup; the two expiry-mode settings (`shotPowerupMode`/`magnetMode`, default `"time"`) live on the persisted `settings` object in the menu section (see §2.14/§2.16). v3.0 (P6/B-8): the salvage/chain block gains a growing-tow-cap group — `CARGO_BASE` (12, replaces the removed `CHAIN_MAX`), `CARGO_CAP_MAX` (20), `CARGO_GROW_PER` (30) + a new `CHAIN_ITER` (4) relaxation-pass count; and the mass-penalty coefficients were retuned **down** (`CARGO_THRUST` 0.10→0.06, `CARGO_MAXSPD` 0.05→0.03, `CARGO_MASS` 0.12→0.07) so a full chain at the higher ceiling matches the old full-12 feel (see §2.10.2). v3.0 (P7): `TIER_NAMES` + `TIER_COLOR[]` (the bronze→diamond badge palette) sit with `COLOR`; the tier ladders themselves are `tiers:[…]` arrays on the tiered LIFETIME definitions (structural, in the Achievements module — see §2.17). v3.1 (P1): `WORLD_W/H` shrunk 3840×2160→2560×1440; `SPAWN_MAX_DIST` 1100→640, `DOCK_MAX_DIST` 900→620 clamped to match (mins unchanged); `STAR_DENSITY` untouched (see §2.11). v3.1 (P3): a garbage-coalescence group in the salvage/chain block (near `GARBAGE_DECAY`) — `GARBAGE_COALESCE_DELAY` (1.0 s activation delay), `GARBAGE_MAGNET_RANGE` (140), `GARBAGE_MAGNET_PULL` (40), `GARBAGE_MERGE_DIST` (12), `HUNTER_COALESCE_COUNT` (12 → a Hunter), and the off-by-default `GARBAGE_CLUMP_MAXSPD` (Infinity) playtest clamp (see §2.5.1). v3.2 (P2): `GARBAGE_SHATTER_KICK` (a `[40, 90]` px/s outward-kick range, playtest knob) joins the same group — the shatter burst reuses `GARBAGE_COALESCE_DELAY` rather than adding a second delay constant (see §2.5.1). **v3.2 (P3): `GARBAGE_DECAY` and `GARBAGE_SEVER_DECAY` are REMOVED — garbage is permanent (§2.10). `GARBAGE_COALESCE_DELAY` retuned 1.0→6.0 s and `GARBAGE_MAGNET_RANGE` 140→260 (both playtest knobs; the range is the number most likely to need tuning — FLAG D-1). `DEBRIS_GARBAGE`/`HUNTER_GARBAGE`/`HUNTER_SMALL_GARBAGE`/`GARBAGE_CLUMP_MAXSPD` unchanged (held in reserve).** | **Change balance here first.** Never hardcode magic numbers deeper in the file. `WORLD_*` = the wrap boundary; `VIEW_*` = the screen — keep the distinction (see §2.11). **Garbage density tunes from coalescence (`GARBAGE_MAGNET_RANGE`, `GARBAGE_COALESCE_DELAY`, `HUNTER_COALESCE_COUNT`) + the drop-volume constants + the `nextWave` count — decay is gone (v3.2 P3).** **Early-game pacing tunes from `RAMP_WAVES` + the saucer floor/ceiling pairs — every wave-scaled value is `ramp(floor, ceil, wave)` (§2.13).** |
| **Canvas/scaling** | `resize()` — CSS scaling, fixed 1280×720 logical viewport | Same pattern as Atomic Dustbin Dan. `resize()` uses `VIEW_W/VIEW_H` (screen); all *world* math uses `WORLD_W/WORLD_H`, never window size. |
| **AudioSys** | Singleton object; all sounds are methods; init on first keypress (autoplay policy). v1.1 adds `pickup()` and `deliver(count)` (pitch climbs with delivery combo). v1.9 (F8): a **gain-routing graph** — `init()` builds `master`/`sfx`/`music` gain nodes and **every voice connects to the `sfx` bus** (→ master → destination); `vol {master,sfx,music}` + `setVol(cat,v)` drive the Options sliders; `music` has no source connected (no music track — §2.8); `ui(up)` is the menu blip; v2.0 (F9): `achievement()` is the unlock fanfare (a rising triad); v3.1 (P3): **`hunterborn()`** is the garbage-coalescence warning cue (two low descending sawtooth voices a dissonant tritone apart, ~0.4 s — §2.5.1/§2.8) | Continuous sounds (thrust, saucer) are start/stop node pairs. **Add new SFX connecting to `this.sfx`, not `this.ctx.destination`** (so master/SFX volume applies); follow the `now()` + gain-envelope pattern. `vol` values persist even before `ctx` exists (loadSettings sets them; init applies them). |
| **Input** | `keys{}` map + the data-driven **`bindings`** table (v1.8) + the `GP` button/axis enum + `GP_NAME` (display labels) + `REBINDABLE`/`DEFAULT_BINDINGS` (v1.9) + `gamepad` snapshot + `pollGamepad`/`handleGamepadMenu` + resolution helpers (`kbActive`/`gpActive`/`actionHeld`/`gpPressed`) + menu-nav helpers (`menuNavEdges`/`resetMenuNav`/`firstPressedButton`, v1.9) + the `input` predicate object | v1.8 (F7): `input.*()` resolves keyboard **or** first gamepad via `actionHeld(name)` → the `bindings` entry (keyboard `keys` / gamepad `buttons` / analog `axis` past `GP_DEADZONE`). **Add or rebind by editing `bindings`, never by reading `keys`/gamepad inline.** v1.9 (F8): the `keydown` handler and `handleGamepadMenu` each check **three contexts in priority order — rebind-capture, menu-open, normal-play** — so menu/capture input can't leak into `keys{}` or the ship (see §2.16). `DEFAULT_BINDINGS` is a pristine snapshot (before loadSettings) for Return-to-Defaults; `REBINDABLE` = the `fixed:false` actions. Menu/system reads (Start/A/B) remain **edge-triggered** via `gpPressed`; directional menu nav is edge-detected via the `menuNav` latch. |
| **Helpers** | `rand`, `randSign`, **`difficultyFactor`/`ramp`** (v1.5), `wrap`, `dist2`, `angleTo`, **`shortDelta`** (v1.1), **`wrapOffset`/`wrapPos`** (v1.2), `glowStroke`, `drawPoly`, `COLOR` | ⚠️ `wrap`, `dist2`, `angleTo`, `shortDelta` are **wrap-aware** and now operate against `WORLD_W/WORLD_H`. Always use these for distance/aiming/link math — naive `Math.hypot` deltas break near edges. `wrapOffset` gives the nearest wrapped image of a point relative to the camera (rendering); `wrapPos` folds a fresh spawn into `[0,WORLD)`. **`difficultyFactor(wave)`** = `1−e^(−(wave−1)/RAMP_WAVES)` (0→1 ramp); **`ramp(floor, ceil, wave)`** interpolates any threat parameter through it — use these for any new wave-scaled value (§2.13). |
| **Entity classes** | `Ship`, `Bullet`, `DebrisSatellite`, `HunterSatellite`, `Saucer`, `Particle`, and (v1.1) `Garbage`, `FloatText`, `Dock`, (v1.7) `Powerup` + `drawCanister()`/`drawPowerupGlyph()` shared renderers | Uniform contract: constructor, `update(dt)`, `draw()`, `dead` flag. v1.4: `Asteroid` → **`DebrisSatellite`** (broken-satellite silhouette — a `hull` polygon + `shards` open polylines; splits handled in `destroyDebris`, not a `split()` method). `Garbage` gained a **`mass`** field (default 1.0); `Garbage.fromNode(n)` carries `n.mass` back when a severed node becomes free garbage; `drawCanister()` takes an optional tint so low-mass scrap renders paler (v1.6). v3.1 P3 added coalescence fields (`coalesceDelay`, `pieces`); **v3.2 P1** makes `mass` **sum** on merge and `radius` **derive** as `7·√pieces`, `draw()` render a **cluster** of `pieces` mini-canisters, and a `pieces > 1` clump **un-hookable** (§2.5.1) — reversing FORK-2. **v3.2 P2** adds no new entity fields — a clump is destroyed via the new flow function `shatterClump(g)` (below), which pushes `pieces` fresh plain `new Garbage(...)` singles. **v3.2 P3: `Garbage` lost its `decay` field entirely — canisters are permanent** (the ctor's decay param and `Garbage.fromNode`'s decay param are gone; `mass` is now the 5th ctor arg); and **`HunterSatellite` gained a `bornOfScrap` flag** (default false; true only for a coalescence-born core, propagated to split children in `destroyHunter`) that suppresses the lineage's garbage emission (§2.5.1). v1.3: `Ship` has an `hp` field; every hazard (`DebrisSatellite`/`HunterSatellite`/`Saucer`) carries a `damage` field (contact damage — see §2.12). v1.5 (F10): `Saucer` gained a **`rollFireTimer(range)`** method (wave-scaled reload/first-shot via the fire multiplier) and its aimed-fire error is now wave-scaled; shape/movement unchanged (§2.6, §2.13). v1.6 (F4): the killer `Satellite` + homing `Wedge` classes were **merged into `HunterSatellite`** — one diamond family, tiers 3/2/1, `homing` flag (large passive drift / medium+small active homing), speeds & turn rates wave-scaled at construction; splits/garbage handled in `destroyHunter`, and a `static spawnCore()` places a fresh large core off a viewport edge (§2.5). v1.7 (F6): a new **`Powerup`** class (`type` ∈ rapid/triple/health/magnet/engine) with a rotating-hexagon housing + a per-type glyph via **`drawPowerupGlyph()`** (reused by the HUD); Health is instant, the rest are timed (§2.14). |
| **game object** | Central state: entity arrays, score/wave, spawn timers. The passive hazard array is **`game.debris`** (was `game.asteroids`, v1.4); the active-homer array is **`game.hunters`** (v1.6 — merged `game.satellites` + `game.wedges` into one), fed by **`game.hunterTimer`** (was `satelliteTimer`) and, as of v3.1 P3, **also by `coalesceGarbage`** — a coalesced Hunter is pushed straight to `game.hunters` **independent of `hunterTimer`**, so concurrent lineages are possible (§2.5.1). v1.1 adds `garbage`, `chain`, `floaters`, `dock`, `deliveryCount`, `offloadTimer`; v3.1 P3: each `game.garbage` canister carries **`coalesceDelay`** (v3.2 P3: a **6 s** activation countdown, was 1 s) and **`pieces`** (fused-clump count); v3.2 P1: `pieces > 1` now also drives a summed **`mass`**, a derived **`radius`**, a cluster render, and the **un-hookable** pickup gate (§2.5.1); **v3.2 P3: canisters are permanent (no `decay` field), and a coalesced Hunter carries `bornOfScrap` so its lineage drops no garbage** (§2.5.1). The per-game `game.stats` bag swapped `garbageDecayed` for **`hunterCoalesced`** (Waste Not, §2.17). v3.0 P6 (B-8) adds **`cargoMax`** (runtime tow cap, `CARGO_BASE`→`CARGO_CAP_MAX`, reset in `startGame`, grown at the dock-offload site). v1.2 adds **`camera` `{x,y}`**. v1.3 removes `lives`/`nextExtraLife`/`respawnTimer` and adds **`nextRepair`**; HP lives on `game.ship.hp`. v1.7 (F6) adds **`powerups`**, **`powerFx`** `{rapid,triple,magnet,engine}`, and **`healthTimer`**; v3.0 P5 adds **`powerBudget`** `{rapid,triple,magnet}` (count-mode shots/pieces remaining, parallel to `powerFx`; the persisted `settings` `{shotPowerupMode,magnetMode}` object is module-level, not on `game`). v1.9 (F8) adds **`game.menu`** `{screen, index, row, col, rebinding}` (v3.0 P4 also `achReturn`) — the pause-menu cursor/capture state; `game.paused` is true iff a menu screen is open (v3.0 P4: **in any state**, not only `playing`). v2.0 (F9) adds **`game.stats`** (a flat per-game achievement-counter bag, built by `resetGameStats()`) and **`game.toasts`** (active unlock banners) | Single mutable global. **`game.chain` is NOT a class-entity array** — it holds plain verlet nodes (see 3.4), which now include a `mass` field. `game.camera` is set to the ship's position each frame in `update()`. **`game.paused` and `game.menu.screen` are toggled only by `openPause`/`closePause`/`quitToTitle`** (keep them in sync — `menuActive()` reads `paused`). `startGame` resets `game.menu`. |
| **Flow functions** | `startGame`, `nextWave` (also places the Dock; spawns `min(3+wave, 9)` debris — v1.4), `addScore`, `boom`, **`destroyDebris`** (v1.4; 3-way split + 3-canister emit per tier — was `destroyAsteroid`), **`destroyHunter`** (v1.6; parallel to `destroyDebris`), **`shatterClump`** (v3.2 P2; parallel to `destroyDebris`/`destroyHunter` — destroys a `pieces > 1` clump and emits `pieces` fresh mass-split singles), **`damageShip`** (v1.3), `killShip`, `shieldDeflect`, and the v1.7 powerup helpers **`maxBullets`** (the flexible cap), **`maybeDropPowerup`** (small-tier drop roll), **`spawnHealthPowerup`**, **`applyPowerup`**, plus the v3.0 P5 mode helpers **`powerMode`** (which expiry mode governs a type) and **`powerActive`** (is an effect live, honouring its mode — the one predicate the fire block/`maxBullets`/`chainMass`/Magnet/HUD all read) | `addScore` handles the HP-repair milestone — always score through it. `destroyDebris(a, awardScore=true)` kills one debris, emits exactly `DEBRIS_GARBAGE` canisters, and pushes 3 children if `size > 1`. `destroyHunter(h, awardScore=true)` kills one Hunter: scores/booms, emits the tier's garbage (3 normal-mass at large/medium; `HUNTER_SMALL_GARBAGE` low-mass at small) **unless `h.bornOfScrap`** (v3.2 P3 — a scrap-born lineage drops NO garbage at any tier; the flag propagates to the 3 homing children), and pushes 3 homing children if `size > 1`; called both when shot and when a homer dies on the shield. `shatterClump(g)` kills one `pieces > 1` clump: no score, boom, then exactly `g.pieces` new `Garbage` at `(g.x,g.y)` each at `mass = g.mass/g.pieces`, `pieces = 1`, a full re-armed `coalesceDelay`, and an outward `GARBAGE_SHATTER_KICK` kick — called from a new player-bullet-vs-garbage collision branch (§2.5.1/§2.10) that only targets `pieces > 1`. `damageShip(...)` is the single entry point for unshielded hits (→ `killShip` at 0 HP); self-guards on shield/hit-stun/dead. `killShip` is a respawn-free game over (still calls `scatterChain`; v2.0 also flags `game.stats.gameEnded` + runs a final `Achievements.evaluate`/`save`). v2.0 (F9): `resetGameStats()` (builds the per-game `game.stats` bag) sits with the game object; the achievement **stat hooks** are one-liners inside `destroyDebris`/`destroyHunter`/`applyPowerup`/`damageShip`/`shieldDeflect`/`nextWave` (observe-at-the-source, no new indirection — §2.17). |
| **Menu / Options / Rebinding** (v1.9, F8) | `openPause`/`closePause`/`quitToTitle`/`gotoScreen`, `menuActive`, `handleMenuKey`, the `menuInput(action)` dispatcher + `menuRoot`/`menuOptions`/`menuControls`, `startRebind`/`captureKeyRebind`/`capturePadRebind`, `returnToDefaults`, and persistence (`storageOK`/`saveSettings`/`loadSettings`, `loadSettings()` called once at load) — a section after Input. v3.0 P4: `MENU_ROOT_PLAY`/`MENU_ROOT_SYS` + **`rootItems()`** (the context-aware root, replacing the single `MENU_ROOT`) | The first nested menu/UI state machine (§2.16). Both input methods funnel into `menuInput`; capture and menu-open contexts are handled **before** gameplay reads so nothing leaks to the ship. All `localStorage` access is `typeof`-guarded + try/caught (never crashes). Editing a binding rewrites the live `bindings` table; **Return to Defaults restores `DEFAULT_BINDINGS`** (the pre-load snapshot). Rendering is separate (`drawMenu`, near `drawText`). v2.0 (F9): an **Achievements** row was added to `MENU_OPTIONS`, opening a read-only `"achievements"` screen (`menuAchievements`) drawn by `drawAchievements` — §2.17. v3.0 P5: a **Difficulty** row was added to `MENU_OPTIONS` (before Back), opening a `"difficulty"` screen (`DIFFICULTY_ROWS` + `menuDifficulty`, drawn by `drawDifficulty`) with the two powerup-expiry toggles; `settings {shotPowerupMode,magnetMode}` persists into `afd_settings_v1` (§2.14/§2.16). **v3.0 P4 (supersedes B-1-a): `openPause()` is callable from *any* state**; the root layout is `rootItems()` keyed on `game.state` (play → Continue/Options/Quit; title/gameover → Options/Achievements/Back). `menuRoot` dispatches **by label**; `menuAchievements` backs to the parent named by `game.menu.achReturn`. Controls: ESC pauses/backs, "O" & controller B open the system menu, controller Start is the session toggle (§2.9/§2.16). |
| **Achievements** (v2.0, F9; tiered v3.0 P7; +new v3.0 P8) | The `Achievements` module: the WEEKLY (16) + LIFETIME (19) pools, `byId`, the persisted `lifetime` counters + `weeklyUnlocked`/`lifetimeUnlocked` sets + **`lifetimeTiers{id:idx}`**, `isoWeekYear`/`poolIndex`/`selectWeekly`/`weekKeyFor`, `evaluate`/**`deriveLifetime`**/`onUnlock`/**`tierIndex`/`tierStatusText`**/`tick`/`loadCounters`, and `save`/`init` (its own **`afd_achievements_v2`** key, migrating raw counters from `_v1`) — a section after the powerup flow functions. `resetGameStats` + `powerTypesUsed` sit alongside; `TIER_NAMES`/`TIER_COLOR[]` with `COLOR`; `fmtCommas` near `drawText` | **Observes, never drives** (§2.17) — `cur()` never mutates. Stat hooks live at each event's existing site (kills/deliveries/hits/deflects/powerups/wave-clear/game-over), NOT in the module. **Tiered lifetime entries** (14 of 19) carry `tiers:[bronze..diamond]`; `evaluate()` unlocks every newly-crossed tier in one pass (ascending toasts) and persists the highest tier idx per id; the four MAX counters (`maxWave`, `maxWaveNoPowerup`, `bestDeliveredGame`, `bestDebrisGame`) update via `Math.max` at their event, the rest are sums. P8 event counters latch per-EVENT (Freight Baron on the fixed 12-in-a-visit, Zen Master on `pacifistStreak === 5`) or once-per-game at `killShip` (Daredevil, Wave Rider). `evaluate()` is called each frame from `update()` + once at game over; `deriveLifetime()` silently seeds tiers from loaded counters (no toast storm on migration). Weekly rotation deterministic (`(isoYear*52+isoWeek) % 16`, 5-wide wrapping slice); weekly unlocks reset on a new ISO week, lifetime retained. All `localStorage` access `typeof`-guarded + try/caught (never crashes). |
| **Chain physics** (v1.1) | `chainAnchor`, **`chainMass()`** (v1.4 — Σ node.mass), `wrapNode`, `updateChain`, `breakChain(i)`, `scatterChain`, `drawLink`, `drawChain` — located directly after `shieldDeflect` | See 3.4 for the physics contract. `chainMass()` drives the thrust/top-speed/tug penalties (mass sum, not count) and, v1.7, applies the Engine powerup's `ENGINE_MASS_MULT` (0.5) so all three penalties lighten from one place. `breakChain(i)` destroys node i and converts nodes i+1..end to free garbage — never splice the chain array directly. v3.0 P6 (B-8): `updateChain` relaxes over **`CHAIN_ITER` (4, was a literal 3)** passes for stability at the higher `CARGO_CAP_MAX` (20) node count; the mass-penalty coefficients feeding `chainMass()`'s consumers were softened (§2.10/§2.10.2). |
| **update(dt)** | ship update → **camera-follow** → entity updates → **pickup/chain/dock pass** (v1.7: Magnet pulls free garbage here) → **`coalesceGarbage(dt)`** (v3.1 P3: garbage↔garbage magnetism + merge; a 12-piece clump becomes a Hunter — runs after the pickup loop, before `updateChain`; v3.2 P1: attraction/merge now mass-weighted + momentum-conserving, mass sums, radius derives; v3.2 P3: the transformed core is tagged `bornOfScrap` and `game.stats.hunterCoalesced++`) → **powerup pickup + `powerFx` countdown** (v1.7) → spawn timers (v1.7: ambient Health) → collision passes → cleanup filters (v1.7: `powerups`) → wave-clear check → heartbeat | Collision order matters (see 3.1). The old respawn-clearing step is gone (v1.3 — no respawn). `game.camera` is set to the ship's position right after `ship.update` (before spawns, so ship-relative spawns use the current position). The **saucer spawn block** now sets the next gap and the small/big roll via `ramp(...)` off `game.wave` (v1.5/F10 — §2.6, §2.13). v1.6: the old satellite+wedge spawn/update/collision/cleanup passes collapsed into single `game.hunters` passes — one bullet-vs-hunter loop calls `destroyHunter`, and hunters are in both the hazard-vs-ship and hazard-vs-chain spread copies. Cleanup uses `.filter(!dead)` — never splice mid-loop. v2.0 (F9): a per-frame `Achievements.tick(dt)` (play-time + periodic save) near the top and a single `Achievements.evaluate()` after the wave-clear check; the perfect-wave (damage-free) test lives in the wave-clear block. |
| **draw()** | Starfield (v3.0 P1: two layers — far, wrap-aware, world-fixed at 1.0×; near, screen-space tiled parallax at 0.5×, both with per-star brightness) → title OR [**camera transform** → (**`drawWorldBoundary()`** (v2.1 — dashed wrap-seam guide, beneath everything) → **dock** → particles → **garbage** → **chain** → debris → hunters → saucers → **powerups** (v1.7) → bullets → ship → **floaters**) → **restore** → HUD (v1.7: active-powerup row under CARGO) → overlays]. v1.9 (F8): when `game.paused`, **`drawMenu()`** paints the pause-menu overlay (root/options/controls) — `drawMenu`/`menuPanel`/`drawRootMenu`/`drawOptionsMenu`/`drawControlsMenu`/`drawBindCell` + the `keyLabel`/`padLabel`/`axisLabel` formatters sit near `drawText`. v3.0 P4: `drawMenu()` now also overlays the **title** (inside the title block, before its early `return`) and **gameover** screens (drawn *after* the GAME OVER text so the system menu sits on top); `drawRootMenu` reads `rootItems()` and titles the panel "PAUSED" (play) or "MENU" (system) | Draw order = z-order: dock is the floor, floaters sit above the ship. World entities go inside the camera translate via `drawEntity` (cull + nearest-image); HUD/overlays are screen-fixed, drawn after `ctx.restore()`. HUD includes the **HULL/HP bar** (v1.3, top-left), the shield bar, cargo counter, **`TARGETS n` count** (v3.0 P3, directly under CARGO — `n = debris.length + hunters.length`, the literal wave-clear gate; lit while `n > 0`), and dock-pointer chevron. The active-powerup row (v1.7) shifted down 20px (`prow` 130→150) to make room; v3.0 P5: it iterates `powerActive` (not the raw timer) and, in a count mode, draws the bar as `remaining/POWERUP_BUDGET[type]` with a numeric shots/pieces label beside it. v3.0 P5 also adds `drawDifficulty` to the `drawMenu` dispatch. The pause overlay (was a plain "PAUSED" text) is now the full **menu** (§2.16), a dimmed backdrop + glow-stroked panel, still purely screen-fixed. v2.0 (F9): `drawAchievements`/`drawAchRow` render the viewer (an `"achievements"` menu screen), and `drawToasts` paints the unlock banners **last** (on top of everything, incl. the pause menu and game-over overlay) — both near `drawText`. v2.1: `drawWorldBoundary()` (near `drawStarfield`) strokes a dashed `WORLD_W×WORLD_H` rect at the wrap seam's nearest wrapped image relative to the camera — first thing inside the transform, beneath all entities, purely cosmetic. v3.0 (P1): `drawStarfield` now draws two star layers — the original `stars[]` (world-fixed, wrap-aware, per-star brightness) plus a new sparser `starsNear[]` layer tiled in screen space modulo a `VIEW_W×VIEW_H` tile and offset by `camera * STAR_PARALLAX_FACTOR` (a 3×3 tile stamp, culled to viewport) so it reads as nearer/faster depth without needing wrap math. Both layers are drawn before the camera transform (screen space), purely cosmetic, no gameplay effect. See §2.11–2.12, §2.16–2.17. |
| **Main loop** | rAF loop, dt clamped to 0.05 s; v1.8: **`pollGamepad()` then `handleGamepadMenu()` run before `update()`**; v2.0: `updateToasts(dt)` runs after `update()` (ages unlock banners in every state, incl. paused/game-over) | Clamp prevents tunneling after tab-switch. The dt clamp also keeps chain constraints stable — keep it. The gamepad snapshot is refreshed once per frame *before* anything reads it (so `input.*()` and the menu edge-reads see a consistent frame). Headless tests drive `update()` directly and never call `loop()`, so they must call `pollGamepad()` themselves to exercise the controller path (§5.4). |

### 3.1 Collision conventions
- All collisions are **circle vs circle** using wrap-aware `dist2` against squared radii. Polygon shapes are cosmetic.
- Pass order in `update()`: garbage pickup (during garbage update) → player bullets → targets; hostile bullets → ship → **chain nodes**; hazard bodies → ship (using a spread copy `[...arrays]` so `split()` pushes during iteration are safe); **hazard bodies → chain nodes** (spread copy, labeled `chainScan` loop, first hit wins per frame); saucer bullets → debris satellites.
- Entities are killed by setting `dead = true`; arrays are filtered once at the end of the frame. **Follow this pattern** — it avoids iterator invalidation bugs. (Exception: chain nodes are removed via `breakChain`/`chain.pop()`, never a dead flag — see 3.4.)
- Ship collision radius switches to `SHIELD_RADIUS` (26) whenever the shield is up. The shield does **not** extend to chain nodes (deliberate — see 2.10.1).
- Chain nodes use an effective radius of 7 vs hazard bodies and 9 vs hostile bullets.
- **Unshielded contact/bullet hits call `damageShip(amount, hazardX, hazardY)`** (v1.3) instead of the old instant-kill `killShip()`: it subtracts source-specific HP, applies knockback + a 1.0 s hit-stun, and only routes to `killShip()` (game over) at 0 HP. Shielded contact deflects/blocks instead (no damage). Because `damageShip` self-guards on shield/hit-stun/dead, the hazard-vs-ship loops no longer `break` after the first hit — a shielded ship still deflects every overlapping hazard, and an unshielded one absorbs exactly one hit per stun window (§2.12).

### 3.2 Rendering conventions
- `drawPoly(points, x, y, angle, color, closed)` + `glowStroke(color, width, blur)` produce the vector look via `shadowBlur`. New entities should define local-space point arrays and reuse these — don't invent per-entity draw pipelines. Canisters (free and chained) share one renderer, `drawCanister()`.
- `drawLink()` skips any link whose wrap-aware length exceeds 120 px — this prevents chain segments from streaking across the screen at the wrap seam. Keep this guard if touching chain rendering.
- `shadowBlur` is the main perf cost. If adding many simultaneous entities (>~80), consider a global glow toggle. The salvage feature adds up to ~12 chain nodes + ~15 free canisters at once; watch frame rate in garbage-heavy late waves.

### 3.3 Known safe extension points
- **New enemy:** copy the `HunterSatellite` or `DebrisSatellite` class shape (constructor / update / draw / dead), add an array to `game`, wire into: `startGame` reset, `update()` entity update + collision passes + cleanup filter, `draw()` z-order, and wave-clear condition if it should block wave end. Decide explicitly whether it can damage the chain (add it to the `chainHazards` spread if so — as `game.hunters` is).
- **New weapon/powerup:** hook `Ship.update` fire block; bullets already carry a `hostile` flag that all collision passes respect. *v1.7 (F6) is the worked example:* weapon caps flow through `maxBullets()` (not the raw `MAX_BULLETS`) and Triple Shot's spread lives in the fire block; timed effects live in `game.powerFx` and count down in `update()`; the Engine/Magnet utility effects hook `chainMass()` and the garbage-pickup pass respectively (not parallel systems). Copy the `Powerup` class shape for any new pickup and add its cleanup filter + `update()` pickup pass.
- **New sound:** add an `AudioSys` method; one-shots follow `fire()`, loops follow `thrust()`.
- **New cargo-adjacent mechanic:** work through the existing chain functions — score through `addScore`, break through `breakChain`, convert nodes with `Garbage.fromNode`.

### 3.4 Chain physics contract (v1.1) — read before touching `updateChain`

The tow chain is the only physics system in the game that is *not* the standard entity pattern. Its rules:

- **Node shape:** plain objects `{ x, y, px, py, spin, spinRate, mass }` in the `game.chain` array, ordered head (nearest ship) → tail. `px/py` are the previous-frame verlet positions; implied velocity is `(x−px, y−py)`. **`mass`** (v1.4/F5) is copied from the picked-up `Garbage` (default 1.0) and drives the handling penalties — **any code that creates a chain node must set `mass`**, or `chainMass()` will `NaN`. There is no `dead` flag and no class — nodes are created inline at pickup and removed only by `breakChain(i)`, `scatterChain()`, or `chain.pop()` at the dock.
- **Integration order per frame** (inside `updateChain`): (1) verlet integrate all nodes with damping `0.18^dt`; (2) measure the **pre-constraint** stretch of the first link and apply the momentum tug to the ship; (3) run `CHAIN_ITER` (4, was 3) relaxation passes of position-based distance constraints (link 0: node takes 100% of the correction, the anchor is kinematic; links 1+: 50/50 split between neighbor nodes); (4) `wrapNode` every node.
- **Why tug-before-constraints:** the pre-constraint stretch is the honest measure of how much the cargo resisted the ship this frame. Applying a spring force *after* constraints (when stretch ≈ 0) does nothing; applying stiff springs *instead of* constraints oscillates. Don't reorder these steps.
- **Wrap handling:** `wrapNode` shifts `px/py` by the same amount as `x/y` so implied velocity survives the teleport — this is why the chain doesn't explode at screen edges. All link deltas (constraints *and* rendering) go through `shortDelta`. Any new code measuring between two nodes must do the same.
- **Constraints are equality (rod-like), not rope-like** (they also correct compression). This is what produces the serpentine follow instead of nodes bunching on the ship when it decelerates. If you change to stretch-only constraints, expect the snake feel to disappear.
- **Cargo penalties live in `Ship.update`**, not in the chain code: `thrustMul` and `maxSp` are computed from **`chainMass()`** (Σ node.mass, v1.4/F5), and the momentum-tug `massFactor` in `updateChain` uses the same sum. Before v1.4 these used `game.chain.length` (a count); for all-mass-1.0 cargo the sum equals the count, so v1.3 balance is preserved. If you add other cargo effects (turn rate, shield drain), put them in `Ship.update` alongside and scale them off `chainMass()` too.
- **Stability envelope:** re-validated for B-8 at dt ≤ 0.05 (the main-loop clamp), **`CARGO_CAP_MAX` (20) nodes, `CHAIN_ITER` (4) constraint iterations** (was 12 nodes / 3 iters). Headless stress (`scratchpad/test-p6.js` (E) + `diag-chain-iter.js`): a 20-node chain under hard thrust-flips across a wrap shows no NaN, no velocity explosion, and worst-case link stretch ≈4 px on a 20 px link (already stable at 3 iters — 4.36 px — but 4 tightens the worst link and restores the pre-B-8 margin at the new node count). The worst link is link 0 (anchor→head, dominated by ship speed), not the tail, so corrections propagate the full length — the chain doesn't sag. If you lengthen the chain or the link length further, revalidate the same way (see 5.4 rule 7) before shipping; the `drawLink` 120 px wrap-skip still reads correctly at 20 nodes.

---

## 4. Enhancement Roadmap (candidate backlog)

> **The large v2.0 feature set** (controller support, HP system, powerups, achievements, a larger scrolling world, and the satellite redesigns) **has now shipped** — its specs live in Section 2, and its retired planning docs are in `archive/` (see `archive/README.md`). Items below that overlapped with v2.0 are struck through; the rest remain a candidate backlog for a future cycle.

> **v3.0 (complete, archived — specs in `archive/PLANNED-FEATURES-v3.md`, build order in `archive/IMPLEMENTATION-PHASES-v3.md`; all 8 phases SHIPPED, "Phase 9" never existed).** The v3.0 change set was an **achievements overhaul** — the **tier infrastructure + converting the seven flagged lifetime achievements into 6-tier bronze→diamond ladders (`afd_achievements_v2`), Phase 7**, and the **seven new tiered lifetime/event achievements (Sharpshooter, Freight Baron, Zen Master, Daredevil, Wave Rider, Salvage King, Field Sweeper) + the new "Flawless Run" weekly (16-pool rotation), Phase 8** (§2.17) — plus **Orbital Overhaul Change Set 1**: (1/2) reach the pause/Options/Achievements menu from *any* state via ESC or keyboard O / controller B, with Start still pausing during play — Phase 4; (3) a more legible starfield/parallax motion reference — Phase 1; (4/5) a new **Options → Difficulty** screen letting shot powerups expire by shots and the Magnet by pieces collected, not only by time — Phase 5; (6) a fire-rate rebalance (slower base gun, genuinely faster Rapid Fire) — Phase 2; (7) a HUD "targets remaining" count — Phase 3; (8) a growing tow-cargo cap (12 → 20) with a chain physics retune — Phase 6. All documented in Section 2 (§2.9, §2.10.2, §2.11, §2.14, §2.15, §2.16, §2.17).
>
> **v3.1 (in progress — specs in `PLANNED-FEATURES-v3.1.md`, build order in `IMPLEMENTATION-PHASES.md`; Phase 2 of N SHIPPED).** Change set: (1) **shrink the toroidal world** 3840×2160→2560×1440 with matching spawn/dock ring clamps — **SHIPPED, Phase 1** (§2.11); (2) **fix the `+120` wrap-jump discontinuity** so entities teleport by exactly `WORLD_W`/`WORLD_H` — **SHIPPED, Phase 2** (§2.11); (3) mutual garbage magnetism + coalescence (12 loose canisters can clump into a Hunter, with a warning cue) — next. See `PLANNED-FEATURES-v3.1.md` / `IMPLEMENTATION-PHASES.md` for full specs and remaining phases.

Ordered roughly by effort. Each is scoped to fit a single working session.

**Small (one short session)**
1. **High-score persistence** — in-memory top-5 table with initials entry on game over. (Note: localStorage does not work in claude.ai artifact preview; fine in a real browser. Gate it with a try/catch.)
2. **Screen shake + flash** on ship death and satellite kill. Add `game.shake` timer, offset ctx translate in `draw()`.
3. **Thump-synced pulse** — starfield or border glow pulses with the heartbeat.
4. ~~**Gamepad support**~~ → shipped in v1.8 (v2.0 Feature F7); see §2.15.

**Medium (one full session)**
5. ~~**Power-up drops**~~ → shipped in v1.7 (v2.0 Feature F6); see §2.14.
6. **Debris Satellite variants** — e.g. "dense" units (2 hits, darker stroke), "volatile" units (explode, damaging neighbors in radius). Add a `type` field to `DebrisSatellite`; keep circle collision. A "hot" variant could drop extra canisters. *F3 (Debris Satellites) has now shipped (v1.4) — this would layer on top of it: a per-type branch in `destroyDebris` and the constructor.*
7. **Boss saucer every 5 waves** — large multi-hit saucer with a visible hull-damage state (drop polygon vertices as it takes hits). Could deliberately target the player's tow chain for extra menace.
8. **Attract mode** — after 15 s idle on title, run a simple AI demo game (rotate toward nearest rock, thrust away from close threats, fire).

**Large (multi-session — split into sub-goals)**
9. **Two-player co-op** (shared screen, second ship on IJKL/etc., friendly-fire toggle).
10. **Touch controls** for mobile — virtual buttons in the letterbox margins; ties into the existing scaling code.
11. **Campaign structure** — named sectors with modifier rules (gravity well at center, asteroid belts that spawn from one edge, "no shield" sector, etc.).

---

## 5. Session Handoff Protocol

This mirrors the Atomic Dustbin Dan workflow: each new Claude session gets three attachments and a short kickoff prompt.

### 5.1 What to attach to every new session
1. **`asteroids-deluxe.html`** — the current build (always the latest working version).
2. **This GDD** — or at minimum Sections 1–3 plus whichever Section 4 item is being built.
3. **`STATUS.md`** — the running status document (template below). Update it at the end of every session.
4. **For a v3.0 phase:** attach `PLANNED-FEATURES-v3.md` and `IMPLEMENTATION-PHASES.md` (both at the repo root) and paste that phase's ready-to-paste prompt. The v2.0 planning docs remain archived under `archive/` (historical reference only, not active specs). When all v3.0 phases ship, these v3.0 docs archive the same way — Section 2 + `STATUS.md` stay authoritative.

### 5.2 Kickoff prompt template

> I'm continuing development of Asteroid Field Deluxe, a single-file HTML5 vector arcade game. Attached: the current build, the game design document, and the status doc.
>
> Read the GDD's Architecture Map (Section 3) before writing code — especially the wrap-aware collision helpers, the dead-flag/filter lifecycle, and the tuning-constants convention.
>
> **This session's goal:** [one specific item, e.g. "Roadmap item 5: power-up drops — rapid fire and shield overcharge only"]
>
> Constraints: keep it a single self-contained HTML file, no external assets or libraries, all changes consistent with the design pillars in GDD Section 1. Deliver the full updated file plus a summary of what changed for the status doc.

### 5.3 STATUS.md template

```markdown
# Asteroid Field Deluxe — STATUS
Last updated: [date] · Build version: [x.y] · Last session: [one line]

## Working / verified
- [features confirmed working in a real browser]

## Known issues
- [bugs, with repro steps if known]

## Balance notes
- [tuning observations from playtesting, e.g. "small wedges feel unfair at turnRate 2.6"]

## Next up
- [the next roadmap item, plus any prep notes]

## Changed this session
- [diff-level summary a future session can trust without re-reading everything]
```

### 5.4 Rules for future sessions (instructions to Claude)

Any Claude session working on this project should:

1. **Deliver the complete file, not fragments.** The build must always be runnable as-is. If the file grows too large for a single response, deliver it in clearly-marked sequential parts and reassemble instructions.
2. **Preserve the tuning-constants convention.** New mechanics get named constants at the top of the script, never inline magic numbers.
3. **Use the wrap-aware helpers** (`dist2`, `angleTo`) for all distance and aiming math. This is the most common way to introduce subtle bugs.
4. **Follow the entity lifecycle**: `dead` flag + end-of-frame `.filter()`. Never remove entities mid-iteration.
5. **Respect the design pillars** (Section 1). If a requested feature conflicts with a pillar, say so and propose an alternative before building.
6. **Route all scoring through `addScore()`** so extra-life logic stays correct.
7. **Syntax-check AND functionally test before delivering.** Extract the script block and run `node --check`. For gameplay logic, use the headless harness pattern established in v1.1: stub `window`, `document.getElementById` (returning a canvas whose `getContext` yields a Proxy that no-ops all methods), and `requestAnimationFrame`; then drive `startGame()` and `update(1/60)` directly, teleporting the ship to set up scenarios. AudioSys is safe headless because every method early-returns when `this.ctx` is null (init only fires on keydown). Verified this way in v1.1: pickup, tow constraint length, screen-wrap chain integrity, dock delivery scoring, and chain severing.
8. **Version bump + changelog.** Increment the version comment at the top of the file and produce a "Changed this session" summary for STATUS.md.
9. **Don't refactor unprompted.** Paul knows this codebase; large structural rewrites make session-to-session diffs unreadable. Propose refactors, don't spring them.
10. **Flag playtest asks.** Claude can't play the game — end each session with 2–3 specific things Paul should verify in the browser (e.g. "confirm the new power-up despawns after 8 s").

---

## 6. Bug Watch List (v1.1)

Areas most likely to hide issues, worth checking during playtests:

- **Shield-deflection edge cases** — deflecting a rock into a wall of other rocks; deflecting at exactly the moment energy hits zero. New in v1.1: deflecting a rock directly into your own tow chain (working as designed, but verify it feels fair).
- ~~**Respawn deadlock**~~ — *resolved by F2 (v1.3): there is no respawn anymore, so this can't occur.*
- **Knockback vs. tow chain (v1.3)** — knockback shoves the ship away from a hazard, which can drag a long chain tail through *other* hazards or fling it across the wrap seam. The chain is wrap-aware and severs per its existing rules, but verify a hit never yanks the ship somewhere that instantly severs the whole haul unfairly.
- **Knockback into a second hazard (v1.3)** — the 1.0 s hit-stun means being knocked straight into another rock is safe *during* the window; verify it doesn't feel like the ship pinballs helplessly once the window expires in a dense field.
- **Saucer sound leak** — the warble is stopped on saucer death/exit and on game over; verify it can't persist if a saucer dies the same frame the ship does.
- **Performance (raised in v1.4, again in v1.6; sharper as of v3.2 P3)** — heavy `shadowBlur`; the salvage feature already raised the entity ceiling, and F3's guaranteed 3-per-tier drops raise the *free-canister* count far higher (a fully-cleared Debris large lineage emits 39; a fully-cleared **timer-spawned Hunter** lineage emits **66** — 12 normal + 54 low-mass; a **scrap-born** lineage emits 0, FORK-B/B1). **v3.2 P3 removed the decay timer that used to bound coexistence** — garbage is now permanent, so the free-canister count only falls via recycling or coalescence, and late waves can carry more standing salvage than before. Render is viewport-culled (`onScreen` in `drawEntity`), so free-canister count doesn't drive *draw* cost; the O(n²) `coalesceGarbage` pass is the watch item (FLAG D-2 — ~80k cheap pair tests at 400 pieces, survivable, and coalescence compacts `n` downward). Watch frame time at wave 10+; if it dips, a global glow toggle (§3.2) is the lever, then the drop-volume constants (`DEBRIS_GARBAGE` / `HUNTER_SMALL_GARBAGE`), then (only if a stress test shows it) a spatial hash for the coalescence pass — explicitly out of scope until then.
- **Hunter shield-kill at point-blank (v1.6)** — destroying a medium Hunter on the shield spawns 3 smalls right at the shield edge; they scatter outward for 0.6 s before homing, so it *shouldn't* be an instant point-blank swarm, but verify it feels fair (a fair "spend energy, but now deal with the smalls" trade, not a death trap) — this is the old wedge tension carried forward.
- **Hunter early-game feel vs. escalation (v1.6)** — the whole family moves at `HUNTER_FLOOR_FRAC` (0.58) of full speed at wave 1. Verify the large core's drift and any split children genuinely feel *gentle* in the opening waves (not still-too-fast) and that both tier-climb and wave-climb read as a smooth escalation, not a cliff. `HUNTER_FLOOR_FRAC` (family-wide early calm) and `RAMP_WAVES` (whole-game pace) are the knobs.
- **Garbage density & the permanent-salvage field (v3.2 P3, SUPERSEDES the old decay-feel ask)** — decay is gone; garbage is permanent. The open questions are now: does `GARBAGE_MAGNET_RANGE` (260) let drifting pieces actually find each other and clump/transform, or does the field **silt up with inert junk** that never clumps and never gets collected (the number most likely to be wrong — FLAG D-1)? Does `GARBAGE_COALESCE_DELAY` (6.0 s) make same-explosion siblings visibly drift apart and *later* happen upon strangers, or do they still snap together / never clump? Does the **shatter loop** actually fire — do you find yourself *hunting* clumps to break them for a cargo run, or ignoring them until they Hunter? And does letting a clump transform feel like a real choice (a scrap-born lineage is pure score, no cleanup bill — B1) or is shooting it always strictly correct? These are the top v3.2 retune asks; F10 (more debris density) will revisit jointly.
- **Powerup drop rate & Magnet feel (v1.7/F6)** — `POWERUP_DROP_CHANCE` (10%) and the ambient `POWERUP_HEALTH_GAP` (18–26 s) are first-pass, no playtest data. Verify drops feel *earned but not stingy* (not so rare they never matter, not so common they stop feeling special), and that the Magnet's pull reads as a satisfying sweep rather than jittery orbiting (levers: `MAGNET_PULL` up-front accel, `MAGNET_DAMP` in-range damping — lower `MAGNET_DAMP` toward 0 to make it settle harder, or drop `MAGNET_PULL` if garbage zips/overshoots). Also confirm the five icons read distinctly at a glance and the HUD duration row is legible.
- **Controller feel & audio unlock (v1.8/F7)** — the binding *logic* is headless-verified, but deadzone comfort, whether right-trigger *or* right-bumper is the right shield button (a gap-fill, not spec'd), and general button-mapping feel can only be judged with a real controller in a browser — **the top v1.8 playtest ask.** `GP_DEADZONE` (0.25) is the one feel knob. Also verify: does the D-Pad-vs-stick split feel natural; does Start pausing/unpausing read cleanly; and — since browsers don't reliably treat gamepad input as an audio-autoplay gesture — does a controller-only start produce sound, or is one keyboard press still needed to unlock audio? (If the latter, that's a known browser limitation, not a bug.)
- **Menu feel & first-time persistence (v1.9/F8)** — the menu *logic* is headless-verified (40 assertions), but three things need a real browser: (1) does menu navigation feel natural on **keyboard and gamepad** (slider granularity at `VOL_STEP` 0.1; edge-detected one-press-one-move gamepad nav vs. wanting held auto-repeat); (2) does a rebound control take effect **immediately** in live play, and are the two flagged quirks acceptable — a gamepad rebind **dropping that action's stick axis**, and **no conflict detection** when rebinding to an already-used key; (3) **do settings actually persist across a real reload** — this is the first feature to write `localStorage`, which is absent in the artifact sandbox, so real persistence is unverified (worst case in the sandbox is "no persistence," never a crash — access is guarded). See §2.16.
- **Chain tug oscillation** — `CHAIN_TUG 26` is stable in headless tests but hasn't been feel-tested against every input pattern. Rapid thrust-flip-thrust with a full chain is the stress case; if the ship judders, lower `CHAIN_TUG` before touching the constraint code.
- ~~**Dock/respawn interaction**~~ — *moot post-F2 (v1.3): no respawn. `killShip()` still calls `scatterChain()`, so a dead ship never keeps a chain.*
- **Wave-transition cargo** — the chain persists across waves (deliberate) but new large debris satellites spawn ≥220 px from the *ship*; a long chain tail can extend beyond that. Verify a wave spawn can't instantly sever a fresh chain unfairly often.
- **Delivery combo reset radius** — combo resets at dock radius+40. Verify a player orbiting the dock edge can't accidentally keep/lose the combo in a confusing way.

---

## 7. Version History

- **v1.0** — Initial build: ship/asteroids/saucers, shield, killer satellite + homing wedges, waves, synth audio.
- **v1.1** — Radioactive salvage: garbage drops with decay, verlet tow chain (mass/momentum penalties, momentum tug), chain vulnerability with aft-severing, relocating recycling dock with escalating delivery scores, cargo HUD + dock pointer, pickup/deliver SFX. New constants block; `shortDelta` helper; `Garbage`/`FloatText`/`Dock` classes; chain physics functions after `shieldDeflect`. Headless-tested: pickup, tow, wrap, delivery scoring, severing.
- **v1.2** — *v2.0 Phase 1 (F1): Larger scrolling world & camera.* Simulation space split from the screen: a 3840×2160 toroidal world (`WORLD_W/H`) with a 1280×720 viewport (`VIEW_W/H`) that scrolls to keep the ship centered (`game.camera`). All wrap-aware helpers (`wrap`/`dist2`/`angleTo`/`shortDelta`/`wrapNode`) retargeted to `WORLD_*`; added `wrapOffset`/`wrapPos`. `draw()` gained a camera transform, wrap-aware per-entity nearest-image rendering + viewport culling (`onScreen`/`drawEntity`), and a world-spanning seamless starfield; HUD/overlays drawn screen-fixed after restore. Spawns (wave asteroids, dock) now ship-relative rings; satellites/saucers enter from viewport edges relative to the ship (feel unchanged). New constants: `CULL_MARGIN`, `SPAWN_MIN/MAX_DIST`, `DOCK_MIN/MAX_DIST`, `STAR_DENSITY`. Headless-tested: camera-follow, world-boundary wrap (x & y), no wrap at old 1280 boundary, ship-relative spawn reachability across the seam, draw() crash-free in all states.
- **v1.3** — *v2.0 Phase 2 (F2): Health Points & Knockback.* Discrete lives replaced by a single HP pool (`SHIP_MAX_HP` = 250) for the whole run; all respawn/lives logic removed (`game.lives`/`nextExtraLife`/`respawnTimer` gone, plus the "respawn only when center is clear" gate). New `damageShip()` handles unshielded hits: source-specific damage (`DMG_SMALL/MEDIUM/LARGE` 20/35/50, `DMG_BULLET` 15, via each hazard's new `damage` field), a `KNOCKBACK_SPEED` (250 px/s) shove away from the hazard, and a `HIT_STUN_DURATION` (1.0 s) i-frame window (reusing `ship.invuln`). `killShip()` is now a respawn-free game over at 0 HP. `addScore()` milestone changed from extra-life to a +25 HP hull repair (`REPAIR_*`), converting to a flat score bonus at full HP. HUD: HULL/HP bar (`COLOR.hp`) replaces the life-ship icons, styled like the shield bar. New `AudioSys.hit()` damage thud. Headless-tested (`scratchpad/test-f2.js`, 54 assertions): per-hit-type damage, hit-stun double-hit prevention, knockback direction/magnitude, 0-HP game over with no respawn, milestone repair, and shield still blocking damage+knockback.
- **v1.4** — *v2.0 Phase 3 (F3 Debris Satellites + F5 variable-mass garbage).* The `Asteroid` class → **`DebrisSatellite`** (array `game.asteroids` → `game.debris`; `AST_*` constants → `DEBRIS_*`; `destroyAsteroid` → `destroyDebris`; `COLOR.asteroid` → `COLOR.debris`); redrawn as a broken-satellite silhouette (irregular `hull` polygon + 1–2 antenna/panel `shards`). Splits changed to 3-way at large/medium (small destroyed), and **every** tier's destruction emits exactly `DEBRIS_GARBAGE` (3) canisters — replacing the probabilistic `GARBAGE_DROP` — so a fully-cleared large lineage yields 39 canisters. F5: `Garbage` and chain nodes gained a **`mass`** field (default 1.0); `Garbage.fromNode()` carries it, pickup copies it onto the node, and the chain thrust/top-speed penalties + momentum-tug `massFactor` now scale off **`chainMass()`** (Σ node.mass) instead of `chain.length` — identical feel for all-mass-1.0 cargo, the groundwork for F4's low-mass Hunter scrap. First-pass balance for the ~8× garbage volume: `GARBAGE_DECAY` 20→12, new `GARBAGE_SEVER_DECAY` 10 (was inline 15), wave spawn `min(4+w,11)`→`min(3+w,9)`. Headless-tested (`scratchpad/test-f3.js`, 28 assertions): 39-canister lineage count, per-tier splits, mass propagation (default/fromNode/pickup/sever), and the mass-sum equivalence of an 8× mass-1.0 chain vs a 16× mass-0.5 chain across thrust, top speed, and tug.
- **v1.5** — *v2.0 Phase 4 (F10 difficulty ramp + saucer calming).* A global difficulty ramp: `difficultyFactor(wave)` = `1 − e^(−(wave−1)/RAMP_WAVES)` (0 at wave 1, → 1 over many waves; `RAMP_WAVES` = 8 is the single ramp knob) and a `ramp(floor, ceil, wave)` interpolation helper, both in the Helpers block. The **saucers** are re-expressed as floor/ceiling pairs scaled through `ramp` (§2.6, §2.13): spawn gap `rand(20,30)s` → `rand(12,16)s` (re-expressing the old `rand(12,22)−min(6,wave)`); a fire-rate multiplier `1.8×` → `1.0×` applied via a new `Saucer.rollFireTimer(range)` to both saucers' reloads and first-shot delay; small-saucer aim error `±0.35` → `±0.09` rad; and small-saucer appearance chance `15%` → `60%`, which **folds in** (replaces) the old score>8000 gate so escalation is wave-driven in one place. Pillar 6 ("Ease players in") flipped from planned to shipped. No behavior/shape changes; the Hunter Satellite (§2.5) and Debris-density scaling are deferred to their own phases. Headless-tested (`scratchpad/test-f4.js`, 30 assertions): the `difficultyFactor` curve shape across waves 1–25, and each saucer parameter (gap/fire/aim/chance) at wave 1 vs wave 20 in the right direction and magnitude — including end-to-end through a real `Saucer`'s fired-bullet aim error and `rollFireTimer`.
- **v1.6** — *v2.0 Phase 5 (F4 Hunter Satellites).* The killer `Satellite` + homing `Wedge` classes were merged into one **`HunterSatellite`** class — a teal diamond family with three tiers (3 large / 2 medium / 1 small). The large core drifts passively toward the ship (spawn-time aim, no re-aim) and, when shot, splits **3-ways** into medium then small children that actively home (wrap-aware, relentless), each faster/more agile than the last; splits/garbage run through a new **`destroyHunter`** (parallel to `destroyDebris`), and a `static spawnCore()` places the off-edge large. **Every speed and turn rate wires into the v1.5 difficulty ramp from the start**: each is `ramp(ceiling × HUNTER_FLOOR_FRAC, ceiling, wave)` (ceilings 70 drift / 120·1.6 medium / 175·2.6 small; `HUNTER_FLOOR_FRAC` = 0.58), sampled at spawn — so the whole family moves at ~60% speed at wave 1, ramping to ~96% by wave 20. Garbage at every tier: 3 normal-mass (1.0) at large/medium; a burst of `HUNTER_SMALL_GARBAGE` (6) **low-mass** (0.5) canisters at small, rendered in a paler tint (`COLOR.garbageLight`) as an easy-to-tow bonus. Contact damage 60/45/30 by tier (`HUNTER_DAMAGE`, above the equivalent Debris tier). Shield: the core bounces, homers are destroyed on contact and still split; Hunters still sever the tow chain. Removed `SAT_SCORE`/`WEDGE_SCORE`; added the `HUNTER_*` constants block. Headless-tested (`scratchpad/test-f5.js`, 40 assertions): full-lineage 13 kills / 66-canister tiered-mass count, per-tier splits & garbage mass, damage/score tables, the difficulty ramp on every tier (wave-1 floor vs wave-20), active-homing re-aim, and chain-severing on contact; f2/f3/f4 regressions updated to the merged entity and re-run green.
- **v1.7** — *v2.0 Phase 6 (F6 Powerups).* Five free-floating pickups via a new **`Powerup`** class (rotating-hexagon housing + a distinct per-type glyph through the shared `drawPowerupGlyph()`, reused by the HUD). **Health** spawns ambiently on a saucer-like timer (`POWERUP_HEALTH_GAP`, one at a time) and instantly repairs +25 HP capped; **Rapid Fire / Triple Shot / Magnet / Engine** drop from small-tier Debris/Hunter kills at `POWERUP_DROP_CHANCE` (10%) via `maybeDropPowerup()` and last `POWERUP_DURATION` (15 s). Rapid raises the bullet cap 4→8; Triple fires a 3-bullet `TRIPLE_SPREAD` fan and raises it 4→12 — both active takes the higher cap (12), not 24, via the new `maxBullets()` the fire block now reads. Magnet pulls free garbage toward the ship inside `MAGNET_RANGE_MULT`×`GARBAGE_PICKUP` (with `MAGNET_DAMP` so it glides) and widens the pickup radius; Engine halves the effective towed mass by applying `ENGINE_MASS_MULT` inside `chainMass()`, so thrust/top-speed/tug all lighten. Same-type pickups refresh the timer (`game.powerFx`) rather than stacking; a HUD row shows each active timed effect with a shield-bar-style duration bar; new `AudioSys.powerup()` chime. Headless-tested (`scratchpad/test-f6.js`, 39 assertions): effect magnitudes + duration expiry, the 12-not-24 combined cap, the triple-shot spread and rapid cap driven through the real fire block, Engine's `chainMass` halving, the Magnet moving garbage toward the ship over frames (and not when inactive), and small-tier-only drops (never large, never Health); f2/f3/f4/f5 regressions re-run green.
- **v1.8** — *v2.0 Phase 7 (F7 Controller Support).* Gamepad input layered onto the keyboard with **no call-site changes** — `input.left()`/`thrust()`/etc. now resolve keyboard **or** the first connected gamepad through a single data-driven **`bindings`** table (per-action keyboard `keys` / gamepad `buttons` / analog `axis` + a `fixed` rebindable flag), so Phase 8's rebinding UI has one structure to build against. `pollGamepad()` snapshots `navigator.getGamepads()[first-connected]` once per frame before `update()` (ignoring extra pads); `input.*()` resolves held-state lazily from that snapshot via `actionHeld`/`kbActive`/`gpActive`. Defaults (F7 spec): D-Pad / Left Stick rotate + thrust past a `GP_DEADZONE` (0.25), A fires, right trigger/bumper raises the shield (**a gap-fill — no controller shield was spec'd**). Fixed menu/system reads wired now for later phases via edge-triggered `gpPressed`: A = confirm, B = back (reserved for F8), Start = start/pause/unpause (Start toggles pause exactly like `P` today); the keyboard menu handler now reads the same `bindings.confirm`/`bindings.pause` entries. **Hardcoded defaults only — the rebinding UI is Phase 8 (F8).** Headless-tested (`scratchpad/test-f7.js`, 52 assertions): keyboard-path regression, each default button/axis resolving to its action, the 0.25 deadzone (including the strict boundary), keyboard-OR-gamepad combining, first-pad-only selection, edge-triggered Start/A menu behavior (single-toggle, confirm-on-title/gameover, no restart mid-play), the binding-table data matching spec, and a gamepad thrust driving the real `Ship.update` through the unchanged input layer; f2–f6 regressions re-run green. See §2.15.
- **v1.9** — *v2.0 Phase 8 (F8 Pause Modal, Options Menu & Control Rebinding).* The game's first nested menu/UI state machine (§2.16): pause (`P` / gamepad Start) opens a modal (Continue / Options / Quit), Options has SFX/Music/Master volume sliders + a Controls rebinding screen, all navigable by keyboard **or** gamepad through a single `menuInput(action)` dispatcher. `game.menu {screen,index,row,col,rebinding}` holds the cursor/capture state; `game.paused` ⇔ a menu is open, toggled only by `openPause`/`closePause`/`quitToTitle` (Quit → title). **AudioSys refactored to a gain-routing graph** — every voice now connects to an `sfx` bus → `master` bus → speakers, with `setVol(cat,v)` driving the sliders (`vol {master,sfx,music}`); a `music` gain node exists but **nothing is connected to it — there is no music track** (the slider is wired for a future one, not a placeholder; §2.8). **Live rebinding**: selecting a binding cell and confirming captures the next key/button into `bindings[action]` (a gamepad rebind drops the analog axis); **Return to Defaults** restores the five `fixed:false` actions to `DEFAULT_BINDINGS` (a pristine pre-load snapshot). The `keydown` handler and `handleGamepadMenu` each resolve **three contexts in priority order — rebind-capture, menu-open, normal-play** — so menu/capture input can never leak into `keys{}` or ship control, and `update()` stays frozen while paused. Settings persist to `localStorage` (`afd_settings_v1`), all access `typeof`-guarded + try/caught so a storage failure never crashes. New `AudioSys.ui()` menu blip. Headless-tested (`scratchpad/test-f8.js`, 40 assertions): the state-machine transitions, volume sliders driving both `vol` and the gain nodes with [0,1] clamping, live keyboard/gamepad rebind taking effect via `input.*()` with **no leak into gameplay keys** + frozen-while-paused, edge-detected gamepad nav, Return-to-Defaults exact restore, and a `localStorage` persistence round-trip; f2–f7 regressions re-run green. **Not yet browser-verified: real-`localStorage` persistence outside the artifact sandbox, and the menu feel/rebind-in-play playtest asks (STATUS.md).** See §2.16.
- **v2.0** — *v2.0 Phase 9 (F9 Achievements System) — v2.0 complete.* A progress-tracking layer that OBSERVES stats the rest of the game already produces (kill counts by hazard type/tier, dock deliveries, HP thresholds, powerup usage, wave reached, damage-free waves, play time) via light one-liner hooks at each event's existing site — no new indirection. Two pools via a single `Achievements` module: **15 weekly** (a calendar-deterministic 5-wide slice active each ISO week — pool index `(isoYear*52 + isoWeek) % 15`, 5 consecutive wrapping — so every player worldwide sees the same 5 on the same days; a new week resets weekly unlocks) and **12 lifetime** (cumulative across games). Per-game counters live in a flat `game.stats` bag (`resetGameStats()`); lifetime counters + unlock sets persist to their own `localStorage` key (`afd_achievements_v1`), guarded + try/caught like F8. An unlock pops a non-blocking gold **toast** (`AudioSys.achievement()` fanfare, aged out in the main loop by `updateToasts`) and is browsable in a read-only **viewer** at Pause → Options → Achievements (`drawAchievements`, this week's 5 + the 12 lifetime with unlock status + `cur/goal` progress). `Achievements.evaluate()` runs each frame (already-unlocked short-circuit) + once at game over (for end-of-game checks like Waste Not). Headless-tested (`scratchpad/test-f9.js`, 62 assertions): ISO-week math incl. a year-boundary (2027-01-01 → ISO 2026-W53), the deterministic 5-slice + wrap-around, weekly-reset-on-new-week + lifetime-retained persistence round-trips, and real-driven gameplay unlocks at exact thresholds (15 debris kills, a 12+8 dock-delivery run, a sub-10-HP Hunter collision, all-four powerups, a full 13-kill Hunter lineage, a damage-free wave 3, and every lifetime just-below/at-goal boundary) + toast lifecycle; f2–f8 regressions re-run green. See GDD §2.17. **This ships the full v2.0 feature set (Phases 1–9).**
- **v2.0 feature set (Phases 1–9, complete)** — larger scrolling world (v1.2); HP & knockback (v1.3); Debris Satellites + variable-mass garbage (v1.4); difficulty ramp + saucer calming (v1.5); Hunter Satellite redesign (v1.6); powerups (v1.7); controller support (v1.8); pause/options menu + control rebinding (v1.9); achievements (v2.0). Still open (a tuning pass, not a feature): the joint F3+F10 Debris-density retune — see STATUS.md.
- **v3.0** — specs archived in `archive/PLANNED-FEATURES-v3.md`, build order in `archive/IMPLEMENTATION-PHASES-v3.md`. Change set: an achievements overhaul — **P7** the 6-tier bronze→diamond ladder infrastructure + conversion of the seven flagged lifetime achievements (`afd_achievements_v2` + `_v1` migration), and **P8** the seven new tiered lifetime/event achievements (Sharpshooter, Freight Baron, Zen Master, Daredevil, Wave Rider, Salvage King, Field Sweeper) + the new "Flawless Run" weekly (16-pool rotation) + the definition audit — both landed (§2.17) — and Orbital Overhaul Change Set 1: P1 starfield/parallax legibility (§2.11), P2 fire-rate rebalance (§2.2), P3 targets-remaining HUD (§2.7), P4 pause/menu-from-any-state + ESC-to-pause (§2.9/§2.16), P5 Difficulty Options screen with shot-count / pieces-collected powerup expiry modes (§2.14/§2.16), and **P6 (B-8) a growing tow-cargo cap (12→20) with a chain physics retune** (§2.10/§2.10.2/§3.4). P6: the fixed `CHAIN_MAX` became a runtime `game.cargoMax` that grows +1 per `CARGO_GROW_PER` (30) canisters delivered up to `CARGO_CAP_MAX` (20); the mass-penalty coefficients were softened (`CARGO_THRUST` 0.10→0.06, `CARGO_MAXSPD` 0.05→0.03, `CARGO_MASS` 0.12→0.07) so a full chain at the new ceiling matches the old full-12 feel (~45% thrust / ~63% top speed) rather than the "broken" ~33%/50% a naive m=20 gives, and `CHAIN_ITER` (relaxation passes) went 3→4 for stability at 20 nodes (stress-revalidated per §3.4 rule 7). Headless-tested (`scratchpad/test-p6.js`, 37 assertions): cap starts at 12, grows +1/30-delivered up to and never past 20 (driven through real deliveries) with a "TOW +1" float, pickup gate + HUD read `cargoMax`, the retuned thrust/top-speed ratios measured through the real `Ship.update` (incl. Engine-at-20 ≡ plain-10 and the 0.07 tug coeff), and 20-node constraint stability across thrust-flips + a wrap (no NaN/explosion, worst link ≈4 px). Also fixed the long-standing `test-f3` Engine-drop flake. **v3.0 ended at Phase 8 — "Phase 9" never existed; the next cycle is v3.1** (`PLANNED-FEATURES-v3.1.md` / `IMPLEMENTATION-PHASES.md`).
- **v3.1 (shipped)** — specs in `PLANNED-FEATURES-v3.1.md`, build order in `IMPLEMENTATION-PHASES.md`. **P1: shrink the toroidal world** `WORLD_W/H` 3840×2160→**2560×1440** (2×2 viewports, ~44% of the old area) — the bigger v1.2 world left too much dead space between spawns. Ship-relative spawn rings retuned to match: `SPAWN_MAX_DIST` 1100→**640**, `DOCK_MAX_DIST` 900→**620** (mins unchanged at 220/260), both now under the binding limit `min(WORLD_W,WORLD_H)/2−60 = 660` so a ring can never wrap around and land near-on-top of the ship. `STAR_DENSITY` untouched — `STAR_COUNT`'s area-derived formula auto-scales the far starfield layer to the smaller world, so visual density is preserved with no edit. Wrap/camera/render logic (`wrapPos`, `dist2`, `angleTo`, `shortDelta`, the camera transform) is correct against `WORLD_W/WORLD_H` symbolically and needed no changes (the `+120` wrap-jump discontinuity is a separate, pre-existing bug — v3.1 P2). Headless-tested (`scratchpad/test-v31-world.js`, 12 assertions): constants at their new/clamped values and within the binding-limit clamp; many real `nextWave()` debris spawns and real `Dock` placements land within `[SPAWN_MIN_DIST, SPAWN_MAX_DIST]` / `[DOCK_MIN_DIST, DOCK_MAX_DIST]` of the ship; `STAR_COUNT` matches the area-derived formula for the new world size. Full regression (`test-f2`–`test-p6`, `test-boundary`, `test-starfield`, `test-firerate`, `test-targets`) re-run green. **P2: fix the wrap-jump discontinuity** — `wrap()` and `wrapNode()` (chain nodes) teleported by `WORLD_W + 120`/`WORLD_H + 120` while every distance/aim/render helper measures against period `WORLD_W`/`WORLD_H`; the undocumented +120 shifted every entity ~120 px relative to the ship at each seam crossing. Both jumps now teleport by exactly `WORLD_W`/`WORLD_H`; the ±60 px anti-flicker trigger margins are unchanged. Headless-tested (`scratchpad/test-v31-wrap.js`, 14 assertions): `shortDelta` between an entity and a fixed partner stays continuous (no ~120 px jump) across a wrap on both axes and from both sides of each seam; a wrapped entity lands in `[0, WORLD_W)`/`[0, WORLD_H)`; a wrapped chain node's `px`/`py` shift by the same delta as `x`/`y` so implied velocity survives the teleport. Full regression (all 16 test files, 774 assertions) re-run green. See §2.11. **P3: garbage magnetism + coalescence → Hunter formation** — uncollected salvage is now a threat. Every `Garbage` gains a `coalesceDelay` (1 s activation) and a `pieces` count (both inherited by all emission sites + `Garbage.fromNode`); a new `coalesceGarbage(dt)` pass (run each frame after the pickup loop, before `updateChain`) makes **active** pieces (past their 1 s delay) mutually attract within `GARBAGE_MAGNET_RANGE` (140) at `GARBAGE_MAGNET_PULL` (40 px/s²), and two active pieces within `GARBAGE_MERGE_DIST` (12) **merge** — survivor takes the literal vector sum of both velocities and `pieces += other.pieces`, but **keeps its base mass** (FORK-2: a clump still tows as one mass-1.0 node). At `HUNTER_COALESCE_COUNT` (12) pieces the clump dies and spawns a `new HunterSatellite(x,y,3)` — **independent of `game.hunterTimer`**, so multiple concurrent lineages are allowed (FORK-1) — with a new ominous `AudioSys.hunterborn()` warning cue (two low descending sawtooth voices a dissonant tritone apart, ~0.4 s; audible even off-screen). Coalescing is a distinct fate: a merge sets `dead` **without** bumping `game.stats.garbageDecayed`, so it never trips Waste Not. New constants near `GARBAGE_DECAY` (incl. the off-by-default `GARBAGE_CLUMP_MAXSPD` = Infinity playtest clamp). Headless-tested (`scratchpad/test-v31-coalesce.js`, 29 assertions): the 1 s gate on both attraction and merge (driven through the real `update()` countdown), a 2-piece merge with exact vector-sum velocity, twelve pieces → exactly one Hunter with `hunterborn` fired once, a wrap-seam merge, `garbageDecayed` untouched by a merge or a transform, a `pieces>1` clump hooking as a single mass-1.0 node through the real `update()`, and default-constructor/`fromNode`/emission-site inheritance. Full regression (all 18 test files, 815 assertions) re-run green. See §2.5.1/§2.7/§2.8.
- **v3.2 (in progress — Phase 3 of N shipped)** — specs in `PLANNED-FEATURES-v3.2.md`, build order in `IMPLEMENTATION-PHASES-v3.2.md`. **P1: garbage clumps become physical, and un-hookable** — reverses the v3.1 FORK-2 decision (which was only correct while clumps were hookable; P2 makes them un-hookable, removing FORK-2's reason). A `Garbage` clump's **`mass` now sums** on merge (a wad of 4 normal canisters is mass 4.0) and its **`radius` derives** as `7·√pieces` (a single piece still reads 7; an 11-clump ~23 px). The merge velocity switched from the literal vector sum to the **momentum sum** `(mₐvₐ + m_b v_b)/(mₐ+m_b)` (pre-merge masses) — with mass summing and garbage soon permanent, a literal sum made clumps the fastest things on screen; now a heavy clump barely speeds up eating a fast light piece. Attraction became **mass-weighted** and gravity-like (each piece accelerates toward the other ∝ the other's mass), momentum-conserving and reducing *exactly* to the shipped symmetric pull at mass 1.0 (so `GARBAGE_MAGNET_PULL` is unretuned) — heavy clumps become slow anchors loose singles fall into. `Garbage.draw()` renders a clump as a **cluster** of `pieces` mini-canisters at golden-angle offsets (`θ=k·2.39996`, `r=5·√k`) tumbling on `this.spin`; `pieces===1` is byte-identical to before. **A clump (`pieces>1`) cannot be hooked** (the pickup gate now requires `pieces===1`) and the **Magnet ignores it** — you must shoot it apart first (the shatter tool is P2, below). Clumps stay **non-solid** to the ship (FORK-A2: zero contact damage, not in `hazards`). No new constant (7 is the existing canister radius); decay/bullet-collision/emission untouched (later phases). Headless-tested (`scratchpad/test-v31-coalesce.js` extended, **48 assertions** — the old exact-vector-sum assertion rewritten to the momentum sum): mass sums across a chain of merges, radius tracks `7·√pieces`, momentum-sum merge (a heavy clump absorbing a fast light piece barely speeds up), the mass-1.0 attraction reduction is bit-exact to the shipped force, a `pieces>1` clump is NOT hooked and the Magnet won't pull it while a single still hooks + is pulled, and `draw()` is crash-free at `pieces=1` and `pieces=11`. Full regression (all 18 test files, **834 assertions**) re-run green (`test-p5`/`test-p6` canister stubs gained `pieces:1` to match the new hook contract). See §2.5.1/§2.10. **P2: shatter a clump with one shot** — the tool that reopens what P1 made un-hookable. A new player-bullet-vs-garbage collision branch (in the non-hostile bullet loop, alongside the existing debris/saucer/hunter branches) targets ONLY `pieces > 1` clumps (tested against the P1-derived `radius`); a `pieces === 1` single is still not a bullet target and player bullets still pass straight through it, and hostile (saucer) bullets still pass through all garbage regardless of `pieces`. A hit calls the new flow function **`shatterClump(g)`** (shaped like `destroyDebris`/`destroyHunter`): `g.dead = true`, a small `boom()`, and exactly `g.pieces` fresh `new Garbage` singles pushed at `(g.x, g.y)`, each at `mass = g.mass / g.pieces` (per-piece mass identity is averaged back out — FLAG A-3, an accepted fidelity trade), `pieces = 1`, a **full, re-armed `coalesceDelay`** (the constructor default — reuses the P3-era constant, not a second one) so the burst disperses before it can re-clump, and an outward kick (new constant **`GARBAGE_SHATTER_KICK`**, `rand(40,90)` px/s) added to a fraction of the clump's own drift, mirroring the `destroyDebris` fan-out pattern. **No score** — the reward is the newly-hookable canisters. Decay, emission counts, the coalescence pass itself, and the Hunter transform are all untouched (P3 owns those). Headless-tested (`scratchpad/test-v31-coalesce.js` extended, **61 assertions**, +13 over P1): one player bullet on a `pieces=7` clump yields exactly 7 live singles, all `pieces===1`, all with a full `coalesceDelay`, all `mass = clumpMass/7` (total mass conserved), and they do NOT re-merge on the very next frame (the delay gate holds, driven through the real `coalesceGarbage`/`update`); a player bullet passes THROUGH a `pieces=1` canister untouched; a hostile bullet passes through a clump untouched; the emitted singles ARE hookable (the P1 gate now lets them through) via two real `update()` frames; shattering leaves the Waste-Not stat unchanged. Full regression (all 18 test files, **847 assertions**) re-run green. See §2.5.1/§2.10. **P3: salvage is permanent; the garbage economy closes** — garbage **no longer decays**. The `decay` field, the `GARBAGE_DECAY`/`GARBAGE_SEVER_DECAY` constants, the decay countdown/death in `Garbage.update`, the blink-out branch in `Garbage.draw`, and the `garbageDecayed` stat are all **removed**; a canister now leaves the field by exactly two doors — **recycled** or **consumed into a Hunter**. This *supersedes* GDD §2.10.1's shipped "decay exists to protect readability — don't remove it" rationale, which is rewritten in place (with the reason it was superseded, so a future session doesn't "restore" it): coalescence + shatter-to-harvest are now the density levers. **FORK-B / B1 closes the loop:** a coalescence-born core is tagged **`bornOfScrap`**, propagated to its split children in `destroyHunter`, and a `bornOfScrap` Hunter emits **NO** garbage at any tier — because without this, a scrap-born lineage would drop `12 + 54 = 66` canisters (a 5.5× amplifier: 12 pieces in → 66 out), and with permanent garbage that diverges. Now it's **12 in, 0 out**; score, splits, damage, and the small-tier powerup drop are all unchanged. Timer-spawned Hunters are untouched and still drop the full 66. Two constants retuned (both playtest knobs): **`GARBAGE_COALESCE_DELAY` 1.0→6.0 s** (same-explosion siblings now drift 120–330 px apart and *happen upon* strangers instead of instantly re-clumping) and **`GARBAGE_MAGNET_RANGE` 140→260** (140 was tuned for 12-second garbage; permanent garbage that drifts further apart would never re-find anything — FLAG D-1, the number most likely to need tuning). The **Waste Not** achievement is repurposed in place (id + pool slot kept, so the weekly-rotation math and `test-f9` slice assertions are unchanged): its old "zero canisters expired" test auto-unlocked every game once decay was gone, so it now reads "finish a game with **no Hunters born from neglected scrap**" (`s.hunterCoalesced === 0`), driven by a new per-game `game.stats.hunterCoalesced` incremented once at the coalescence transform site. Headless-tested (`scratchpad/test-v31-coalesce.js` extended to **80 assertions**; `test-f9.js` to **283**; `test-f3.js` adjusted for the dropped ctor decay param): a canister survives a ~120 s `update()` run and never dies of age; a `bornOfScrap` core killed through its FULL lineage (large → 3 med → 9 small) emits **zero** garbage while awarding the **same** score and still dropping its small-tier powerup; a timer-spawned lineage still emits the full 12 normal + 54 low; the `bornOfScrap` flag survives both split generations; `hunterCoalesced` increments once per transform; Waste Not unlocks on a zero-coalescence game and does **not** after one Hunter is born from scrap; the weekly pool is still length 16 with every `poolIndex` assertion unchanged. Full regression (all 18 test files, **872 assertions**) re-run green. See §2.5.1/§2.10/§2.10.1/§2.17.

---

*End of document. Keep this GDD updated when mechanics change — Section 2 should always describe the current build, not the original one.*