# Asteroid Field Deluxe — STATUS

Last updated: 2026-07-08 · Build version: **3.0 (Phase 6 of 9)** — growing tow cap + chain physics retune · Last session: **v3.0 Phase 6 — tow-capacity progression + chain physics retune (B-8).** The fixed `CHAIN_MAX = 12` is gone, replaced by a runtime **`game.cargoMax`** that starts at **`CARGO_BASE` (12)** and **grows +1 per `CARGO_GROW_PER` (30) canisters recycled (delivered) in the current run**, bounded by **`CARGO_CAP_MAX` (20)** — cap-growth FORK A-fork-2 resolved **per-delivery** (the on-pillar choice: capacity earned by engaging the salvage loop, not handed out by wave). The growth check sits at the dock-offload site right after the per-run `game.stats.delivered++`, computing `min(CARGO_CAP_MAX, CARGO_BASE + floor(delivered / CARGO_GROW_PER))`; since `delivered` climbs by 1 per canister the cap rises at most one step at a time, each bump pushing a brief **"TOW +1"** float. `startGame` resets `cargoMax` to base. The **pickup gate** (`game.chain.length < game.cargoMax`) and **HUD** (`CARGO n/{cargoMax}`) now read the runtime cap. **Part 2 — the physics retune (the real work):** a naive m=20 chain under the old coefficients would be ~33% thrust / 50% top speed (the "broken, not heavy" zone GDD §2.10.1 warns about), so the mass-penalty coefficients were **softened in lock-step** — `CARGO_THRUST` 0.10→**0.06**, `CARGO_MAXSPD` 0.05→**0.03**, `CARGO_MASS` (tug) 0.12→**0.07** — solved so a *full chain at the new ceiling* (m=20) lands on the **old full-12 feel: 1/(1+20·0.06)=45.5% thrust, 1/(1+20·0.03)=62.5% top speed**, and the tug's 1.4 cap is reached at m≈20 (was m≈12). The per-node penalty therefore shrinks as the ceiling rises: a base-12 chain is now genuinely lighter (~58% thrust / ~74% top speed / 0.84 tug), leaving handling headroom to grow into. **Stability revalidation (the load-bearing risk):** the constraint loop's literal `3` became **`CHAIN_ITER` (4)**. A headless stress at 20 nodes (hard thrust-flips across a wrap, 900 frames) shows **no NaN, no velocity explosion, worst-case link stretch ≈4 px on a 20 px link** — already stable at 3 iters (4.36 px), but 4 tightens the worst link (link 0, anchor→head — the tail doesn't sag) and restores the pre-B-8 margin §3.4 validated at 12/3. **FLAG B-8-a resolved:** Engine (halves effective mass) at 20 nodes behaves like plain 10 (headless-asserted), acceptable both with/without. **FLAG B-8-b (for the achievements phases):** Heavy Hauler / Freight Baron / The Long Haul must key on a **fixed ≥12** delivered-in-one-visit, NOT `game.cargoMax` — the existing dock full-visit hook still keys on a literal `12` (`if (game.deliveryCount === 12)`), so it's already decoupled; the achievements-overhaul phases (7–9) must keep it fixed as the cap grows. **FLAG B-8-c (perf):** more towed nodes + higher canister volume = more `shadowBlur` in dense late waves — a browser-only playtest watch (`GARBAGE_DECAY` + a glow toggle remain the levers). **Also fixed the long-standing `test-f3` Engine-drop flake** (its `resetShip` now zeroes `game.powerFx`, so a random Engine drop during a prior `update()` can't halve `chainMass` and fail the (D) mass-sum assertions — 10/10 green now). New/changed constants all in the salvage/chain block: `CARGO_BASE`/`CARGO_CAP_MAX`/`CARGO_GROW_PER`/`CHAIN_ITER` added, `CARGO_THRUST`/`CARGO_MAXSPD`/`CARGO_MASS` retuned, `CHAIN_MAX` removed. **Prior session: v3.0 Phase 5 — Difficulty Options screen + powerup expiry modes (B-4/B-5).** Added an **Options → Difficulty** sub-screen with two opt-in mode toggles — **shot powerups (Rapid + Triple) expire by Time | Shots**, **Magnet expires by Time | Pieces** — plus the count-based expiry logic behind them. Both settings default to **`"time"`**, so *nothing changes for anyone until they opt in* (headless-verified: default reproduces the shipped timed behaviour exactly). Design: two persisted fields in `afd_settings_v1` (`shotPowerupMode`, `magnetMode`) on a new module-level **`settings`** object (additive — an older save missing the keys keeps the default, a corrupt value is ignored on load; no schema bump). Count budgets live in a new **`game.powerBudget {rapid,triple,magnet}`** (reset in `startGame`, parallel to `powerFx`; Engine has no budget — it's always timed, never configurable). The single new coupling point is **`powerActive(type)`** (+ **`powerMode(type)`**): every "is this effect live?" read — the fire block's triple-fan branch and rapid-cadence line, `maxBullets()`, `chainMass()` (Engine), the Magnet pull, and the HUD — now routes through it, so the two modes can never diverge (in time mode it reads `powerFx` seconds; in a count mode it reads `powerBudget` count). Decrements: **Rapid/Triple** each spend one **per trigger-pull** in the fire block (a Triple 3-fan is ONE pull; with both active both budgets tick once per pull and end **independently** — FLAG B-4-b; decrement placed *after* the fan/cadence reads so the last pull is fully a rapid/triple pull); **Magnet** spends one **per canister hooked** at the pickup/hook site — counted at the hook, not the pull, so a magnet that draws-then-hooks a piece spends once (FLAG B-5-a). `applyPowerup` sets the timer (time mode) *or* the budget (count mode), and a same-type pickup **refreshes to full** either way (never stacks) — matching the shipped rule. HUD: the active-effect row now iterates `powerActive` and, in a count mode, draws the bar as `remaining/POWERUP_BUDGET[type]` with a numeric shots/pieces label beside it (one renderer, two data sources). Menu: `"Difficulty"` added to `MENU_OPTIONS` before `"Back"` (Controls stays idx 3, Achievements idx 4, Difficulty idx 5, Back idx 6 — no F8/F9/P4 test broke since those key off labels/idx≤4); a `"difficulty"` sub-screen (`DIFFICULTY_ROWS` + `menuDifficulty`, drawn by `drawDifficulty`) mirrors `menuOptions`/`drawOptionsMenu` — two ◄/► toggle rows (◄ = timed default, ► = count variant) + Back, navigable through the existing `menuInput` dispatcher, persisting on each flip; left **structured to grow** (add a row to `DIFFICULTY_ROWS` + a switch case + a renderer row — FLAG B-4-c). New constants in the Powerups block: `RAPID_SHOTS` (40), `TRIPLE_SHOTS` (30), `MAGNET_PIECES` (20) + a `POWERUP_BUDGET` lookup. **All four FLAGs resolved as recommended** (B-4-a default time; B-4-b independent per-pull budgets; B-4-c growable screen; B-5-a count-at-hook). **One documented edge case:** switching a mode mid-effect (via the pause Difficulty screen) makes that effect read the *other* counter, which is 0 unless refreshed — so an active effect can appear to end on a mid-game mode flip; acceptable ("you changed the rules"), and `startGame` zeroes both so a mode-consistent game never sees staleness. Prior session: **v3.0 Phase 4 (revised) — pause/menu reachable from any state, with the corrected control scheme (B-1/B-2).** **This SUPERSEDES the earlier B-1-a mapping** (Start→menu) that a playtest rejected; the previous P4 attempt was reset out and this started clean at the P3 commit. `openPause()` is now callable from `title`/`gameover` as well as `playing` (`update()` already skips the sim outside `playing`, so `game.paused` is safe in any state). The **root menu is context-aware**, keyed purely on `game.state`: `rootItems()` returns `MENU_ROOT_PLAY = ["Continue","Options","Quit"]` when playing, else `MENU_ROOT_SYS = ["Options","Achievements","Back"]` (the system menu). `menuRoot()` now dispatches **by label** so one handler serves both layouts. The **corrected control scheme, as shipped:** (1) controller **Start = session toggle** — title/gameover → START A GAME, playing → OPEN PAUSE, paused → DISMISS & RESUME (this was already wired correctly on the clean tree, left as-is). (2) Keyboard **"O"** and controller **B** OPEN the Options/Achievements system menu from title/gameover; **Back** closes the overlay → underlying screen. "O" is a literal inline check in the keydown normal branch (not a binding-table entry). (3) Keyboard **ESC** pauses during play and backs out inside a menu: `bindings.pause.keys` is now `["escape"]` (**"P" retired — FLAG P4-a resolved: retire**) and shares ESC with `bindings.back.keys`; `handleMenuKey` resolves **confirm→back→pause** in order, so in-menu ESC matches `back` first (= back), and outside a menu the normal branch's pause-during-playing check makes ESC pause. (4) Controller **B is context-aware, mirroring ESC**: menu-open → back; no menu on title/gameover → open the system menu. (5) A/Enter still start a game. **FLAG P4-b (a single confirm can't both nav a menu AND start a game)** is guaranteed structurally: the gamepad normal branch is an **else-if chain** (Start > B > A, so same-frame B+A opens the menu without also starting), and both input handlers hit the menu-open context (which returns) before the title "press to start" line. New `game.menu.achReturn` (`"root"`|`"options"`) tracks which parent the Achievements viewer backs out to, since it now has two entry points (system root directly, or via Options). `drawMenu()` now also overlays the **title** (added inside the title block before its early `return`) and **gameover** (moved to draw *after* the GAME OVER text so the menu sits on top); `drawRootMenu` reads `rootItems()` and titles the panel "PAUSED" (play) / "MENU" (system). Title/gameover control hints updated (title: "PAUSE: ESC" and "OPTIONS / ACHIEVEMENTS: O (controller: B)"; gameover gained the same options hint). **This closes the long-standing "menu / achievements reachable only via pause" limitation** flagged since F8/F9. Prior session: **v3.0 Phase 3 — "targets remaining" HUD (B-7).** Added a `TARGETS n` readout directly under `CARGO` (`draw()`, HUD block), where `n = game.debris.length + game.hunters.length` — literally the wave-clear gate one line below it in `update()` (`if (game.debris.length === 0 && game.hunters.length === 0)`), so `TARGETS 0` means the wave is about to clear. Went with the single-line `TARGETS n` form recommended in `PLANNED-FEATURES-v3.md` B-7 (not the two-part `DEBRIS n · HUNTERS m` fallback) — flagged for a playtest read on whether the count jump reads clearly. Lit (`COLOR.text`) while `n > 0`, dim otherwise, same convention as CARGO. No sim changes — purely a HUD read of two existing array lengths. The active-powerup row shifted down 20px (`prow` 130→150) to make room for the new line. Documented in-code and in STATUS/GDD that the count can **rise** mid-wave: a Debris split (large → 3 mediums, +2, FLAG B-7-a) or a Hunter lineage arriving on its own 14–32s timer from wave 2 on (FLAG B-7-b) — intentional, not a bug, since the wave genuinely isn't over until Hunters clear too. Prior session: **v3.0 Phase 2 — fire-rate rebalance (B-6).** A single `FIRE_COOLDOWN` (0.16 s) previously gated every shot, and Rapid Fire only raised `maxBullets()` — cadence never changed, the bug Paul felt. (1) Base `FIRE_COOLDOWN` retuned slower, 0.16→0.20 s. (2) New `RAPID_FIRE_COOLDOWN` (0.09 s), clearly faster than base. (3) The fire block in `Ship.update` now sets its post-shot cooldown to `RAPID_FIRE_COOLDOWN` when `game.powerFx.rapid > 0`, else `FIRE_COOLDOWN`. Triple Shot is untouched — it still fires the 3-bullet spread on the base cadence and keeps its higher `maxBullets()` cap — so Rapid+Triple together is now the strongest combo (fires fast *and* wide, FLAG B-6-a), flagged for playtest rather than pre-emptively rebalanced. GDD §2.2 updated with the new cooldown values and the combo flag; Architecture Map constants row updated. Prior session: **v3.0 Phase 1 — starfield legibility & parallax (B-3).** The starfield already existed (`drawStarfield`, `stars[]`, flat `STAR_DENSITY=40` at `#1a2a40`) — this enhances it in place, no second system. (1) Each star now carries a per-star brightness `a` (`STAR_BRIGHT_MIN/MAX` = 0.35–1.0) drawn in a brighter cool blue (`rgba(120,160,210,a)`) so the field reads without competing with entities (Pillar 1). (2) `STAR_DENSITY` raised 40→80 (scales by world area, one knob). (3) Added **one** nearer parallax layer (`starsNear[]`, `STAR_NEAR_DENSITY=22`, brighter `rgba(160,195,235,a)`, `STAR_PARALLAX_FACTOR=0.5`) so the ship's motion produces visible relative drift. Per FLAG B-3-a, a ≠1× layer isn't world-fixed, so it can't reuse `shortDelta`/wrap math — it's tiled in **screen space** instead (a 3×3 `VIEW_W×VIEW_H` tile stamp keyed off `camera * STAR_PARALLAX_FACTOR`, culled to viewport), which sidesteps seam artifacts entirely. The original far layer is untouched structurally (still world-fixed, wrap-aware, 1.0×) and remains the depth reference. Five new constants (`STAR_PARALLAX_FACTOR`, `STAR_BRIGHT_MIN/MAX`, `STAR_NEAR_DENSITY`, `STAR_NEAR_BRIGHT_MIN/MAX`) join the retuned `STAR_DENSITY` in the v1.2 world block. Purely cosmetic — zero gameplay effect, nothing in `update()` touched. Prior session: **added a dotted world-boundary line** — a dim, dashed `WORLD_W×WORLD_H` rectangle rendered at the wrap seam so the player can see the wrap-around coming and crossing it. `drawWorldBoundary()` (near `drawStarfield`) strokes the rect via `setLineDash([BOUNDARY_DASH, BOUNDARY_GAP])` + the existing `glowStroke` at `COLOR.dim` with low glow; it's the first thing drawn inside the camera transform (beneath every entity), positioned at the world-origin corner's **nearest wrapped image** relative to the camera (`wrapOffset` on both axes) so the seam shows whether the ship is near world x≈0 or x≈WORLD_W (y likewise) and stays continuous across the crossing. Purely cosmetic — no collision, no gameplay effect. Four new tuning constants (`BOUNDARY_DASH/GAP/WIDTH/GLOW`) live in the v1.2 world block. This is an **interim** wrap-visibility aid; full off-screen threat awareness stays parked for v3.0. Prior session: **housekeeping — archived the two spent v2.0 planning docs** (`PLANNED-FEATURES-v2.md` + `IMPLEMENTATION-PHASES.md`) into `archive/` after verifying every shipped v2.0 feature has its spec in GDD §2 (no doc-handoff gaps); no code change. Before that: implemented **F9 — Achievements System**, the final v2.0 phase. Added an `Achievements` module holding two pools — **15 weekly** (a calendar-deterministic 5-wide slice active each ISO week) and **12 lifetime** (cumulative across games) — that **observes** stats the rest of the game already produces via one-liner hooks at each event's existing site (kills in `destroyDebris`/`destroyHunter`, deliveries in the dock-offload block, hits in `damageShip`, deflects at the four shield-absorb sites, powerups in `applyPowerup`, expiries in `Garbage.update`, perfect waves in the wave-clear block, best-wave in `nextWave`) — **no new indirection layers, no restructured systems**. Per-game counters live in a flat `game.stats` bag (`resetGameStats()`, reset in `startGame`); lifetime counters + unlock sets persist to their **own** `localStorage` key (`afd_achievements_v1`, separate from settings), guarded + try/caught exactly like F8. Weekly rotation is pure math — pool index `(isoYear*52 + isoWeek) % 15`, 5 consecutive entries wrapping the pool, from the real UTC date at launch — so **every player worldwide sees the same 5 in the same calendar week**; a new week resets weekly unlocks while lifetime is always retained. An unlock pops a non-blocking gold **toast** (`AudioSys.achievement()` fanfare, aged out in the main loop by `updateToasts` so it animates in every state) and is browsable in a read-only **viewer at Pause → Options → Achievements** (`drawAchievements`: this week's 5 + the 12 lifetime, each with unlock status + `cur/goal` progress). `Achievements.evaluate()` runs once per frame (already-unlocked entries short-circuit) and again at game over (so end-of-game checks like Waste Not can fire).

**v2.0 (Phases 1–9) shipped and is archived; v3.0 Phases 1–6 of 9 are now landed.** The tow-cap progression + chain retune (P6/B-8) spec lives in GDD §2.10 + **§2.10.2** + §3.4 (revalidated stability envelope). Starfield spec lives in GDD §2.11 (folded into the existing world/camera section, not a new one); the wave-clear gate + `TARGETS` HUD spec lives in GDD §2.7; the powerup-expiry modes + Difficulty screen (P5) spec lives in GDD §2.14 (powerups) + §2.16 (menu/persistence); the Architecture Map Constants/game-object/Flow/Menu/draw() rows were updated for all. See `PLANNED-FEATURES-v3.md` / `IMPLEMENTATION-PHASES.md` for the rest of the v3.0 phase plan (Phase 6 next: B-8 growing tow-cargo cap + chain physics retune).

## Working / verified

**Headless-verified this session (v3.0 P6, `scratchpad/test-p6.js`, 37 assertions):** drives the REAL dock-offload path, `Ship.update`, and `updateChain` (no reimplementation).
- **Config** — `CARGO_BASE`=12, `CARGO_CAP_MAX`=20, `CARGO_GROW_PER`=30 (positive), `CHAIN_ITER`≥3; retuned `CARGO_THRUST`=0.06 / `CARGO_MAXSPD`=0.03 / `CARGO_MASS`=0.07; the old `CHAIN_MAX` constant is gone from the source.
- **Cap growth is real** — driving actual dock deliveries through `update()`: `cargoMax` starts at 12, stays 12 at 29 delivered, hits 13 at exactly 30, 14 at 60, 20 (ceiling) at 240, and **never exceeds** `CARGO_CAP_MAX` across 250 deliveries (exactly 8 increases, one per threshold); each bump pushes a "TOW +1" float; `startGame` resets it to 12.
- **Pickup gate + HUD read `cargoMax`** — at `cargoMax`=12 a 13th field canister is refused; raising `cargoMax` to 16 lets the chain grow past 12; `draw()` is crash-free with a 20-node chain / cargoMax-20 HUD.
- **Physics retune, measured through the real `Ship.update`** — a full chain at m=20 gives thrustMul ≈ 0.4545 (45%) and top-speed ratio ≈ 0.625 (63%), matching both the formula and the old full-12 feel; a base-12 chain is measurably lighter than the old-12 on both; Engine at 20 nodes ≡ plain 10 nodes (FLAG B-8-a); the momentum-tug massFactor uses the 0.07 coeff (0.84 at m=12 uncapped, capped 1.4 at m=20), backed out from the real `updateChain` tug impulse.
- **Constraint stability at `CARGO_CAP_MAX` (20) nodes** — 900 frames of hard thrust-flips across a wrap: no NaN, no node-velocity explosion, worst-case link stretch < one link length (≈4 px), no nodes lost/duplicated. (Iteration-count rationale reproduced in `scratchpad/diag-chain-iter.js`: 3→4→5 iters give 4.36→3.70→3.24 px worst stretch; 4 chosen.)

**Headless-verified prior session (v3.0 P5, `scratchpad/test-p5.js`, 57 assertions):** drives the REAL fire block, magnet/hook pass, menu state machine, and persistence (no reimplementation).
- **Config + defaults** — both modes default `"time"`; `MENU_OPTIONS` has a `"Difficulty"` row sitting immediately before `"Back"`; `POWERUP_BUDGET` maps rapid/triple/magnet to `RAPID_SHOTS`/`TRIPLE_SHOTS`/`MAGNET_PIECES` (all positive).
- **Mode routing** — `powerMode` sends rapid/triple to `shotPowerupMode`, magnet to `magnetMode`, and Engine to `"time"` **always** (even with count modes set — Engine isn't configurable).
- **Default time = shipped behaviour, exactly** — in time mode `applyPowerup` sets the timer (not the budget); firing does **not** touch `powerBudget`; rapid cadence still applies via the timer; a magnet hook spends **no** budget (and still hooks). Engine always uses the timer.
- **`"shots"` mode ends Rapid at EXACTLY `RAPID_SHOTS` pulls** — one pull spends one; still active with 1 left; inactive the pull that hits 0; the last rapid pull still fired at `RAPID_FIRE_COOLDOWN` and the first post-expiry pull returns to `FIRE_COOLDOWN`.
- **A Triple 3-fan is ONE pull** — one pull fires 3 bullets and spends exactly one `triple` shot.
- **Rapid + Triple budgets are independent** — driving `TRIPLE_SHOTS` pulls exhausts Triple while Rapid is left at exactly `RAPID_SHOTS − TRIPLE_SHOTS` and still active; with Triple gone a pull fires a single bullet at rapid cadence; Rapid then ends on its own budget.
- **`"pieces"` mode ends Magnet at EXACTLY `MAGNET_PIECES` hooks** — one hooked canister spends exactly one piece (B-5-a: a draw-then-hook doesn't double-spend); active with 1 left; inactive at 0; no further spend once inactive.
- **Same-type refresh restores the budget to full, never stacks** — a partially-spent Rapid/Magnet budget returns to full on re-pickup; a second refresh does not exceed full.
- **Difficulty screen** — reachable Options → Difficulty; ◄/► on each row selects time/count; down walks shot → magnet → Back; Back (and the `back` action) return to Options with the cursor on Difficulty; menu toggles persist and reload; a direct `saveSettings`/`loadSettings` round-trips; an **old save missing the mode keys** loads clean (defaults kept); a **bogus** mode value is ignored while a valid sibling still loads.
- **Draw crash-free** — the Difficulty screen (both toggle states), the Options screen with the new row, and the **count-mode HUD** (active Rapid/Triple/Magnet numeric bars + Engine timed bar) all render without throwing.

**Headless-verified prior session (v3.0 P4, `scratchpad/test-p4.js`, 38 assertions):** drives the REAL keydown/gamepad handlers + menu state machine (no reimplementation).
- **Corrected bindings** — `bindings.pause.keys` is `["escape"]` with `"p"` retired; `back` shares ESC; Start/B/A pad + Enter/A confirm unchanged.
- **Controller Start = session toggle** — Start on title → starts a game; Start while playing → opens the pause root (`MENU_ROOT_PLAY`); Start while paused → dismisses & resumes.
- **"O" (kbd) + B (pad) open the system menu** from both title and gameover, root = `MENU_ROOT_SYS` (`["Options","Achievements","Back"]`); **Back closes the overlay** back to the underlying title/gameover screen (state preserved). Opening the menu does not also start a game.
- **ESC pauses outside a menu, backs inside** — ESC while playing opens the pause root; ESC in Options backs to root, ESC at root resumes; ESC on the title with no menu is inert (O/B are the openers).
- **A/Enter still start a game** from title/gameover.
- **FLAG P4-b holds three ways** — Enter inside the system menu navigates and never restarts; a same-frame pad **B+A** opens the menu without also starting (else-if guard); A while the system menu is open confirms in-menu, no start.
- **No leak** — nav keys pressed while a menu is open are never written to `keys{}`, no menu keypress starts a game, and `update()` stays frozen while paused (a held gameplay key can't rotate the ship).
- **Achievements dual-entry backs correctly** — reached from the system root it backs to root (cursor on Achievements, `achReturn="root"`); reached via Options it backs to Options (`achReturn="options"`).

**Headless-verified prior session (v3.0 P3, `scratchpad/test-targets.js`, 12 assertions):**
- **Identity holds** — `targetsLeft` (the HUD's displayed number) always equals `game.debris.length + game.hunters.length`; wave-1 debris count matches the real `min(3+wave,9)` spawn rule, and no Hunters exist on wave 1 (FLAG B-7-b).
- **Debris split raises the count for real** — driving the actual `destroyDebris` on one large (size 3) debris + the real cleanup filter takes the count 1 → 3 (a +2 jump, matching Paul's 3→5 example at lineage scale).
- **Hunter spawn raises the count for real** — forcing `game.hunterTimer` to 0 on wave 2 and driving one real `update()` frame spawns exactly one Hunter core via the actual timer path and the count rises from 0.
- **Reads exactly 0 at the literal wave-clear gate** — `targetsLeft() === 0` when both arrays are empty, matching `game.debris.length === 0 && game.hunters.length === 0` verbatim.
- **`draw()` crash-free** with the new HUD line in both an empty-targets state and a populated one (debris + a spawned Hunter core).

**Headless-verified prior session (v3.0 P2, `scratchpad/test-firerate.js`, 12 assertions):**
- **Constants correct** — `FIRE_COOLDOWN` is 0.20, `RAPID_FIRE_COOLDOWN` is 0.09, and rapid < base.
- **Post-shot cooldown is regime-correct** — driving the real fire block one frame with Rapid inactive sets `ship.cooldown` to exactly `FIRE_COOLDOWN`; with `game.powerFx.rapid > 0` it sets exactly `RAPID_FIRE_COOLDOWN`.
- **Cadence relationship holds over real gameplay frames** — clearing `game.bullets` every frame (so the Rapid/Triple bullet-cap can't mask the cooldown difference) and counting fire events over 1 simulated second via the real `update()`/`Ship.update` fire block: base ≈5 volleys/s (matches `1/0.20`), Rapid ≈10 volleys/s (matches `1/0.09`), Triple-alone stays at ≈5 volleys/s (Triple doesn't touch cooldown, confirming it doesn't accidentally speed up cadence).
- **No crash** running 60 frames of `update()`+`draw()` with Rapid active.

**Headless-verified prior session (v3.0 P1, `scratchpad/test-starfield.js`, 17 assertions):**
- **draw() crash-free** with the two-layer starfield wired in, in both title and playing states.
- **Both layers exist and are correctly proportioned** — `starsNear[]` is populated and sparser than the original `stars[]`.
- **Per-star brightness present and varying** — every far-layer star's `a` falls in `[STAR_BRIGHT_MIN, STAR_BRIGHT_MAX]`, every near-layer star's `a` falls in `[STAR_NEAR_BRIGHT_MIN, STAR_NEAR_BRIGHT_MAX]`, and brightness actually varies across stars (not a uniform fill).
- **Near layer is screen-space tiled, not world-fixed** — shifting the camera by exactly one tile width (`VIEW_W / STAR_PARALLAX_FACTOR`) reproduces an identical set of rendered near-star screen positions (clean periodic tiling, no seam drift); a smaller, non-tile-aligned camera shift visibly moves the pattern (parallax is actually applied, not a static overlay).
- **Far layer unchanged as the world-fixed reference** — the same fractional camera shift moves far-layer stars by the expected 1:1 screen-space delta.
- **Purely cosmetic** — driving `update()` with the ship moving and wrapping across the seam changes no score, hull HP, or entity counts.

**Headless-verified prior session (v2.1, `scratchpad/test-boundary.js`, 20 assertions):**
- **draw() crash-free** with the boundary wired in, in both the title and playing states (recording-ctx harness).
- **Correct primitive** — `drawWorldBoundary` sets `setLineDash([BOUNDARY_DASH, BOUNDARY_GAP])`, strokes a rect sized exactly `WORLD_W×WORLD_H`, then resets the dash to `[]`.
- **Seam visibility from either side + continuity across the wrap** — near world x≈0 exactly one vertical seam is on-screen; near x≈WORLD_W (the *same* seam approached from the far side) still exactly one; the deep interior shows none; and the on-screen seam position sits at ~screen-center and doesn't jump as the camera crosses x=0 (verified via the rect corner's `wrapOffset`). Same three checks pass for the horizontal (y) seam.
- **Z-order** — the line-dash set happens *after* the camera `translate` in `draw()` (drawn inside the transform, beneath entities).
- **Purely cosmetic** — driving the real `update()` with the ship crossing and wrapping over the x=0 seam changes no score, no hull HP, and spawns/destroys no entities; the ship wraps into bounds normally.

**Needs a real-browser playtest (cosmetic — can't be judged headlessly):** does the dashed line **read clearly** at the seam as you approach and cross it? Is the `COLOR.dim` tone + low glow **unobtrusive** (a boundary, not a hazard), or too bright/distracting against a busy field? Are the dash length/gap (`BOUNDARY_DASH` 16 / `BOUNDARY_GAP` 24) a comfortable density at the 1280×720 viewport scale? See the new playtest ask below.

**Headless-verified prior session (v2.0, `scratchpad/test-f9.js`, 62 assertions):**
- **ISO-week math** — `isoWeekYear()` returns the right Monday-based/UTC week + week-year, including a year-boundary case (2027-01-01 → **ISO 2026-W53**, a 53-week year because 2026 starts on a Thursday).
- **Deterministic weekly rotation** — `poolIndex(2026,2)===9`; the week's 5-slice is the expected `[no_scratches, combo_collector, speed_recycler, powered_up, diamond_cutter]`; a wrapping index (`poolIndex(2026,6)===13`) correctly wraps to `[diamond_cutter, waste_not, scrap_runner, heavy_hauler, glass_cannon]`; selection is identical for a fixed date (no randomness).
- **Persistence** — weekly unlocks **reset** when the calendar week rolls over (stored week key ≠ current), while **lifetime progress + lifetime unlocks are retained**; a same-week weekly unlock round-trips through `localStorage`. (Serialize/deserialize only — real cross-session persistence still needs a browser; see playtest asks.)
- **Real-driven gameplay unlocks at exact thresholds** — 14 vs 15 debris kills (Satellite Buster); a real dock-offload run delivering 12 then 8 more (Heavy Hauler / Combo Collector / Speed Recycler / The Long Haul lifetime / Scrap Runner at 20); a real Hunter collision leaving 5 HP (Close Shave + below-half + lifetime hitsSurvived); all-four powerups (Powered Up) with Health disqualifying Glass Cannon and any powerup disqualifying No Powerups Needed; a full 13-kill Hunter lineage (Hunter's Bane) and 3 large cores (Diamond Cutter); a damage-free wave-3 completion driven through the real wave-clear loop (No Scratches + the 10th Perfect Wave); every lifetime counter at its just-below/at-goal boundary; and the play-time progress label formatting ("1.0/10h").
- **Toast lifecycle** — an unlock pushes a named toast; `updateToasts` ages it out.
- **`draw()` runtime-exercised** on all new paths (title with the new hint line, playing + toast, the updated Options menu with the Achievements row, the Achievements viewer with mixed locked/unlocked rows, game-over + toast) — no throw.

**Regression re-run green this session:** `test-f2.js` (54), `test-f3.js` (28 — now stable 10/10 after the `powerFx`-clear flake fix; its (D) mass-sum assertions read the retuned coefficients from the exports, so they auto-track the B-8 numbers), `test-f4.js` (30), `test-f5.js` (47), `test-f6.js` (39 — the `CHAIN_MAX` export it pulled was unused and dropped from its returnList), `test-f7.js` (52), `test-f8.js` (40), `test-f9.js` (62), `test-boundary.js` (20), `test-starfield.js` (17), `test-firerate.js` (12), `test-targets.js` (12), `test-p4.js` (38), `test-p5.js` (57), plus the new `test-p6.js` (37) — all pass (**545 assertions across 15 files**). `node --check` clean on the extracted script.

**✅ RESOLVED this session: the pre-existing `test-f3.js` chain-physics flake.** Its 5 `chainMass` "D" assertions used to fail on some runs with values exactly *halved* (`chainMass of 8×1.0 = 8` → got 4, etc.) — the `ENGINE_MASS_MULT` (0.5) signature: a prior `update()` in the test would randomly drop + apply an Engine powerup, leaving `game.powerFx.engine > 0` when the mass block ran. As flagged, the fix belonged to a chain/powerup-owning session (this one): **`test-f3`'s `resetShip` now zeroes `game.powerFx`**, so every (D) scenario starts powerup-free. Confirmed **10/10 green** after the fix (was ~4/6 failing before). Not a code change to the game — a test-harness fix only.

**The full v2.0 feature set, all headless-verified in its own phase and confirmed still green:**
- v1.2 (F1) larger 3840×2160 scrolling world + camera · v1.3 (F2) HP pool + knockback + hit-stun · v1.4 (F3/F5) Debris Satellites + variable-mass garbage · v1.5 (F10) difficulty ramp + saucer calming · v1.6 (F4) Hunter Satellite redesign · v1.7 (F6) five powerups · v1.8 (F7) gamepad support · v1.9 (F8) pause/options menu + control rebinding · **v2.0 (F9) achievements**.

## Known issues

- **Real-`localStorage` persistence is unverified across the board (v1.9 + v2.0).** Both the settings key (`afd_settings_v1`) and the new achievements key (`afd_achievements_v1`) round-trip headlessly against a stub, but real cross-session persistence can only be confirmed in a browser running the file locally (the claude.ai artifact sandbox has no `localStorage`). All access is `typeof`-guarded + try/caught, so the sandbox worst case is "progress just doesn't persist," never a crash. **Top persistence playtest item.**
- **A few starter achievements are intentionally hard or degenerate (v2.0), by design of the starter pool — not bugs.** *Waste Not* ("zero canisters expired in a game") is near-impossible to earn legitimately (a lineage sheds 39–66 canisters and the chain caps at 12), yet is trivially satisfiable by dying before anything decays — it's implemented literally per F9's list. Curating/balancing the pools (retiring degenerate ones, tuning thresholds) is a **post-playtest pass**, deliberately not done this phase.
- **Weekly achievements reset each calendar week (v2.0), by design.** Unlocking a weekly one records it only for the current ISO week; when the week rolls over the 5 change and weekly-unlock progress starts fresh (lifetime never resets). This is the intended "weekly challenge" model — flagged so a mid-week reset (which won't happen — the week only changes on Monday UTC) isn't mistaken for data loss.
- ~~**Achievements viewer is reachable only from Pause → Options (v2.0).**~~ **RESOLVED in v3.0 P4.** The Options/Achievements system menu now opens directly from the title/gameover screen (keyboard "O", controller B); its root offers Achievements as a first-class entry. The old "start a game to browse achievements" limitation (and the parallel F8 "menu reachable only via pause" note) is closed.
- **Gamepad rebind drops that action's analog axis (v1.9), by design.** Capturing reads a button, not a stick direction; Return to Defaults restores the axis.
- **Audio unlock on a controller-only start is browser-dependent (v1.8).** Best-effort `AudioSys.init()`/`resume()` on Start/A, but the Web Audio autoplay policy generally wants a keyboard/pointer gesture — a controller-only player may hear nothing (incl. the new achievement fanfare + menu blips) until one keyboard press.
- **Soft weapon cap (v1.7), by design:** a Triple volley can transiently exceed the 12-bullet cap by up to 2.
- **Performance watch (v1.4/v1.6):** heavy `shadowBlur`; garbage-dense late waves are the stress case (a cleared Hunter lineage sheds 66 canisters). `GARBAGE_DECAY` is the fastest lever, then a global glow toggle. The v2.0 achievement layer adds negligible per-frame cost (≈17 simple `cur()` compares, already-unlocked short-circuit).

## Balance notes

- **Tow-cap progression + chain retune (v3.0 P6) — first-pass numbers, browser feel unverified.** `CARGO_BASE` (12), `CARGO_CAP_MAX` (20), `CARGO_GROW_PER` (30 delivered per +1) and the retuned `CARGO_THRUST` (0.06) / `CARGO_MAXSPD` (0.03) / `CARGO_MASS` (0.07) / `CHAIN_ITER` (4) are all single-knob tunables in the salvage/chain block. The retune math is exact (full 20-chain reproduces the old full-12 ratios), and stability is headless-solid, but three things are **feel/perf, browser-only:** (1) does a full max chain feel *heavy but drivable* (not broken) — and does the now-lighter base-12 chain feel *too* light? (2) does the cap-growth cadence feel *earned* — is +1 per 30 delivered (≈240 delivered to reach 20, roughly 8–16 waves of active hauling) too slow or about right? (3) any whip/perf issues (`shadowBlur`, FLAG B-8-c) in dense late waves at 20 nodes? `CARGO_GROW_PER` is the cadence lever; the three coefficients are the feel levers; `GARBAGE_DECAY`/glow the perf levers. See the new playtest ask below.
- **Powerup expiry count budgets (v3.0 P5) — first-pass numbers, no playtest feedback yet, and OFF by default.** `RAPID_SHOTS` (40), `TRIPLE_SHOTS` (30), `MAGNET_PIECES` (20) only bite when the player opts into `"shots"`/`"pieces"` on the Difficulty screen; the default `"time"` path is untouched (still `POWERUP_DURATION` = 15 s). These three are single-knob tunables in the Powerups block. Open questions are all *feel*, browser-only: do 40 rapid / 30 triple pulls and 20 magnet hooks feel like a fair "use it or lose it" trade vs. the 15 s clock; is the Rapid+Triple *independent* decrement legible (Triple running out mid-fight while Rapid keeps single shots going); and is the count HUD (numeric label beside the bar) readable at a glance. Note the cross-mode interaction with the P2 fire-rate work: rapid cadence keys off `powerActive("rapid")`, so in shots mode the fast cadence lasts exactly `RAPID_SHOTS` pulls.
- **TARGETS HUD (v3.0 P3) — no balance knobs, a pure readout.** No new tuning constants; it's two existing array lengths (`game.debris.length + game.hunters.length`) read into the HUD. The one open question is presentational, not numeric — whether the single `TARGETS n` count (chosen) reads clearly when it jumps up on a Hunter arrival, vs. the two-part `DEBRIS n · HUNTERS m` fallback named in `PLANNED-FEATURES-v3.md` B-7 (see playtest ask below).
- **Fire-rate rebalance (v3.0 P2) — first-pass numbers, no playtest feedback yet.** `FIRE_COOLDOWN` 0.16→0.20 (base gun ~6.25→5 shots/sec) and new `RAPID_FIRE_COOLDOWN` 0.09 (~11 shots/sec) are single-knob tunables in the weapons block. **Rapid+Triple is now a strictly stronger combo than either alone** (fast cadence + wide spread, FLAG B-6-a) — previously Rapid only raised the cap, so this is a real balance shift, not just a bug fix. Wants a playtest read on whether the slower base feels sluggish and whether Rapid+Triple needs a cap/cooldown interaction to stay in check.
- **Starfield (v3.0 P1) — five first-pass tunables, no playtest feedback yet.** `STAR_DENSITY` (40→80), `STAR_PARALLAX_FACTOR` (0.5), `STAR_BRIGHT_MIN/MAX` (0.35–1.0), `STAR_NEAR_DENSITY` (22), `STAR_NEAR_BRIGHT_MIN/MAX` (0.55–1.0) are all single-knob tunables in the v1.2 world block. Density/brightness/parallax strength all want a playtest read (see Playtest asks below) before calling this feature done.
- **Achievements (v2.0) — thresholds are first-pass, no balance knobs beyond the four `ACH_*` constants.** `ACH_TOAST_TIME` (4.5 s), `ACH_WEEKLY_SHOWN` (5), `ACH_LINEAGE_FULL` (13 = a full Hunter lineage), `ACH_SAVE_EVERY` (30 s periodic save). The 27 achievement goal numbers came straight from F9's starter pools — they want a playtest read on whether they're *earnable but meaningful* (see playtest asks). Adding/retiring achievements is just editing the `WEEKLY`/`LIFETIME` arrays; a longer weekly rotation before repeats is "add more weekly ideas," not a structural change.
- **Carried forward, unchanged:** menu `VOL_STEP` (0.1) and `GP_DEADZONE` (0.25) (v1.9/v1.8); `POWERUP_DROP_CHANCE` (0.10), `POWERUP_HEALTH_GAP` ([18,26]), `POWERUP_DURATION` (15), weapon caps (Rapid 8 / Triple 12), `MAGNET_*`, `ENGINE_MASS_MULT` (0.5) (v1.7); the Hunter tables + `HUNTER_FLOOR_FRAC` (0.58) + shared `RAMP_WAVES` (8) (v1.6/v1.5); `GARBAGE_DECAY` 12 s / `GARBAGE_SEVER_DECAY` 10 s, wave spawn `min(3+wave, 9)` (v1.4).
- **Still open: the joint F3+F10 Debris-density retune.** The one remaining v2.0 tuning lever — with every combat/utility/UI system now in, judge the *combined* early-game field feel in a browser, then lean on `GARBAGE_DECAY` for density. A post-playtest pass, not a number to guess.

## Next up

**v3.0 Phases 1–6 of 9 (starfield legibility, fire-rate rebalance, TARGETS HUD, pause/menu-from-any-state, Difficulty powerup-expiry modes, growing tow cap + chain retune) are landed; Phase 7 is next per `IMPLEMENTATION-PHASES.md`.** v2.0 (Phases 1–9) is fully shipped and archived under `archive/` (`archive/PLANNED-FEATURES-v2.md`, `archive/IMPLEMENTATION-PHASES.md`, `archive/README.md`) — current design authority is `asteroid-field-deluxe-GDD.md` §2 + active `PLANNED-FEATURES-v3.md`/`IMPLEMENTATION-PHASES.md` at the repo root; build reality is this file. Candidate directions from here:
- **Continue the v3.0 phase order** — **Phase 7 next: the achievements overhaul (Part A)** — tiered bronze→diamond ladders + new tiered/event/weekly achievements + a definitions audit. **Carry FLAG B-8-b in:** Heavy Hauler / Freight Baron / The Long Haul must key on a **fixed ≥12** delivered-in-one-visit, decoupled from `game.cargoMax` (which now grows 12→20). The existing dock full-visit hook already keys on a literal `12` (`if (game.deliveryCount === 12)`) — keep that fixed as the cap grows; don't rewrite it to `cargoMax`. See `PLANNED-FEATURES-v3.md` Part A / FLAG A-5.
- **Playtest the starfield change** (new asks below) and, longer-standing, the full v2.0 consolidated asks — especially real `localStorage` persistence and achievement threshold feedback, still unconfirmed.
- **The joint F3+F10 Debris-density retune** (the last v2.0 balance lever — see Balance notes).

Notes for a future session:
- **The tow cap is runtime now (v3.0 P6), not a constant.** Read `game.cargoMax` (never a fixed 12) at any pickup-cap or HUD site; it grows 12→20 per `CARGO_GROW_PER` deliveries and resets in `startGame`. `CHAIN_MAX` no longer exists. The chain's stability envelope is revalidated at **20 nodes / `CHAIN_ITER` (4) iters** (GDD §3.4) — if you ever lengthen the chain or link again, re-soften `CARGO_THRUST`/`CARGO_MAXSPD`/`CARGO_MASS` *together* (so a full chain at the new ceiling ≈ old full-12 feel) and re-run the `test-p6` (E) stress, don't just bump the number. **Achievements must not key "full chain" on `cargoMax`** — use a fixed ≥12 (FLAG B-8-b).
- **The two persistence keys are independent by design** — `afd_settings_v1` (F8, now also the P5 difficulty modes) and `afd_achievements_v1` (F9). Keep them separate. The P5 modes are **additive fields** on `afd_settings_v1` (no schema bump) — always tolerate a missing/unknown value on load.
- **Powerup expiry is mode-aware now (v3.0 P5).** Route every "is effect X active?" read through **`powerActive(type)`** (never `game.powerFx.X > 0` directly) so time/count modes stay consistent — the fire block, `maxBullets`, `chainMass`, the Magnet pull, and the HUD all do. Count budgets live in `game.powerBudget`; the two mode settings live on the module-level persisted `settings` object. **Engine is intentionally always time-based** (not in `powerBudget`, not on the Difficulty screen). Known edge: flipping a mode mid-effect can end that effect (it starts reading the other, zeroed counter) — acceptable ("you changed the rules"). If B-4-c grows the Difficulty screen, add a `DIFFICULTY_ROWS` entry + a `menuDifficulty` switch case + a `drawDifficulty` row.
- **Achievements observe, never drive.** New achievements = add a `{id, pool, name, desc, goal, cur, text?}` entry to `WEEKLY`/`LIFETIME` and, if a new stat is needed, add a counter to `resetGameStats()` + one hook at the event's existing site. Don't route gameplay through the module.
- ~~**The menu is still reachable only via pause.**~~ **Done in v3.0 P4** — the title/gameover system menu (keyboard "O", controller B) delivers the options + achievements title-screen entry point.
- **The corrected P4 control scheme supersedes the planned B-1-a mapping (Start→menu).** If revisiting the menu/input design, note that Start is now the session toggle and B/"O" are the menu openers — don't reintroduce Start→menu. FLAG P4-a resolved as "retire P"; FLAG P4-b guaranteed by the gamepad else-if chain + keyboard menu-open early return.

## Playtest asks (Paul — can't be checked headlessly)

**Consolidated across Phases 1–9 — everything below needs a real browser (some need a controller).** Ordered roughly newest-first.

**Growing tow cap + chain retune (v3.0 P6) — NEW this session (the math + stability are headless-solid; everything here is feel/perf):**
5a. **Does a full max chain feel *heavy but drivable*, not broken?** Grow the cap toward 20 (deliver a lot) and tow a full 20-chain. It's tuned to land on the *old full-12 feel* (~45% thrust / ~63% top speed). Does that read as "a heavy but manageable haul," or does 20 canisters *still* feel broken/whippy despite the retune?
5b. **Is the now-lighter base-12 chain a problem?** Because the per-node penalty shrank, a fresh-run 12-chain is lighter than it used to be (~58% thrust / ~74% top speed / 0.84 tug vs the old 45%/63%/1.4). Does the early game feel *too* floaty when towing, or is the extra early nimbleness fine (you grow into the weight)?
5c. **Does the cap-growth cadence feel *earned*?** +1 per 30 canisters delivered (≈240 to reach 20 — roughly 8–16 waves of active hauling). Too slow (never see 20 in a normal run)? Too fast? Does the "TOW +1" float read clearly as "your cap just went up," and does `CARGO n/20` on the HUD make sense?
5d. **Any whip/perf issues at 20 nodes in dense late waves (FLAG B-8-c)?** More towed nodes + the high canister volume = more `shadowBlur`. Watch frame rate towing a long chain through a busy late wave; if it drops, `GARBAGE_DECAY` and a global glow toggle are the levers.

**Difficulty options / powerup expiry modes (v3.0 P5) — (default `"time"` = nothing changes (default `"time"` = nothing changes; these all need opting in on Options → Difficulty):**
4a. **Do the count budgets feel right?** Turn on **shot powerups = Shots** and play with Rapid/Triple: do 40 rapid pulls / 30 triple pulls read as a fair "use it or lose it" alternative to the 15 s clock, or too stingy / too generous? Same question for **Magnet = Pieces** at 20 canisters hooked.
4b. **Is the count HUD legible?** In a count mode the active-effect bar shows `remaining/budget` with a **numeric** count beside it (instead of a draining timer). Does the number read at a glance during play, and does a count bar sit clearly alongside a *timed* Engine bar when both are up?
4c. **Is the Difficulty screen discoverable and clear?** Pause → Options → Difficulty. Do the two "Time | Shots" / "Time | Pieces" toggles read unambiguously (◄ = time, ► = count), and does ◄/► flipping them feel right on keyboard **and** gamepad? Does it persist across a reload (same `afd_settings_v1` caveat as F8 — real persistence is browser-only)?
4d. **Rapid+Triple independent expiry** — with both on in Shots mode, when Triple's 30 runs out first you keep firing *single* shots at rapid cadence until Rapid's 40 is spent. Does that transition read as intended, or confusing?

**Pause / menu from any state + corrected controls (v3.0 P4) (needs a controller for the first two):**
3a. **Does Start-as-session-toggle feel natural on a controller?** One button that starts a game on the title, opens the pause dialog mid-game, and dismisses/resumes while paused — does that read as intuitive (the standard console idiom), or do you reach for a separate "pause" and "start"?
3b. **Do "O" (keyboard) and controller "B" read as the options/achievements entry from the title/gameover?** Is it discoverable from the on-screen hint ("OPTIONS / ACHIEVEMENTS: O (controller: B)")? Does opening the system menu, browsing Achievements/Options, and backing out feel clean — or does "B to open" fight your instinct that B = cancel?
3c. **Is ESC-vs-B "back" ever ambiguous inside a menu?** ESC and B both mean "back" once a menu is open (and ESC pauses / B opens when no menu is up). Backing out step-by-step (Achievements → Options → root → resume) — does it ever feel like a keypress did the wrong thing, or backed out one level too many/few?
3d. **Is retiring "P" for ESC-as-pause the right call (FLAG P4-a)?** P no longer pauses. If muscle memory wants P, say so and it can be re-added as a secondary playing-pause alias.

**Targets-remaining HUD (v3.0 P3):**
2a. **Does `TARGETS n` under CARGO read clearly at a glance during play?** It's a plain number, no icon — does it register as "how many things are left" without a legend?
2b. **Does the count *rising* mid-wave read as intended, or as confusing/buggy-looking?** Two ways it can happen: a large Debris splitting (jumps +2 on one kill), and — the more likely one to raise an eyebrow — a Hunter lineage sliding in on its own timer well after the Debris field looks nearly clear, ticking `TARGETS` back up from a low number. Does that read as "a new threat arrived," or does it feel like the game moved the goalposts? If it's confusing, the documented fallback is splitting the readout into **`DEBRIS n · HUNTERS m`** so the Hunter arrival reads as an addition rather than a rewind — flag which you'd prefer.
2c. **Is the position (directly under CARGO, powerup row bumped down 20px) crowding the top-left HUD**, especially with 1–2 active powerup bars also showing?

**Fire-rate rebalance (v3.0 P2):**
-1a. **Does the slower base gun (0.20 s, was 0.16 s) feel sluggish, or is it barely noticeable?** Clear waves 1–3 at the base cadence (no Rapid Fire picked up) and compare against memory/feel of the old rate. Time it if useful.
-1b. **Does Rapid Fire now feel meaningfully faster**, not just "more bullets allowed"? Pick one up and fire continuously — does the cadence bump read as intentional and satisfying?
-1c. **Is Rapid+Triple together too dominant?** It's now the strongest combo by design of this fix (fast cadence + wide spread) — flagged as FLAG B-6-a, not yet rebalanced. Does it trivialize waves/Hunters when both are active, or does the 15 s shared duration keep it in check naturally?

**Starfield legibility & parallax (v3.0 P1):**
0a. **Does the field read as brighter/denser without competing with entities?** Compare the title screen and early gameplay against your memory of the old flat, near-invisible field. Does the varied per-star brightness read as a natural starfield rather than noise? Is `STAR_DENSITY=80` a good density, or does it feel sparse/cluttered at the 1280×720 viewport?
0b. **Does the parallax layer read as depth, or as a distracting second field?** Thrust/strafe the ship and watch the near (brighter, sparser) layer drift relative to the far layer. Does the 0.5× `STAR_PARALLAX_FACTOR` make ship motion legible as "something in the world," or is it too subtle to notice / too strong and busy? Is the near layer's brightness (`STAR_NEAR_BRIGHT_MIN/MAX` 0.55–1.0) a good contrast against the far layer?
0c. **Any visible seam or tiling artifact in the near layer** as the camera moves, especially near a world-boundary wrap? The near layer is screen-space tiled (not wrap-math), so this is the one thing headless tests can't fully rule out visually — the periodicity test only confirms the *math* repeats cleanly, not that it *looks* seamless. All five constants (`STAR_DENSITY`, `STAR_PARALLAX_FACTOR`, `STAR_BRIGHT_MIN/MAX`, `STAR_NEAR_DENSITY`, `STAR_NEAR_BRIGHT_MIN/MAX`) are single-knob tunables.

**World-boundary line (v2.1):**
0. **Does the dotted boundary read clearly, and is it the right brightness?** Fly toward a world edge until the dashed rectangle scrolls into view, then cross it. Does the line clearly say "here's the wrap seam" as you approach *and* as you pass through it (it should sit at screen-center at the moment of crossing)? Is the `COLOR.dim` tone + low glow unobtrusive — reads as a boundary, not a hazard/enemy — or is it too bright/distracting over a busy field, or conversely too faint to notice? Is the dash density (`BOUNDARY_DASH` 16 / `BOUNDARY_GAP` 24) comfortable at the viewport scale, or does it want longer/shorter dashes? All four `BOUNDARY_*` constants are single-knob tunables. *(Only judgeable in a real browser — the headless test confirms the geometry/z-order/no-gameplay-effect, not the look.)*

**Achievements (v2.0/F9):**
1. **Do settings AND achievements actually persist across a real reload?** This is the top ask. Earn an achievement (e.g. deliver 20 canisters → Scrap Runner, or destroy 15 debris → Satellite Buster), change some volumes/bindings, close the tab, reopen the file. Are the unlock, the lifetime counters, and the settings all still there? Two separate `localStorage` keys are in play (`afd_settings_v1`, `afd_achievements_v1`) — confirm both survive. *(Only confirmable outside the artifact sandbox — headless tests exercise only the serialize/deserialize logic.)*
2. **Does the unlock toast read clearly and feel good?** Non-blocking gold banner top-centre, ~4.5 s, fade in/out, stacks if several land at once, plus a rising-triad chime. Is it legible over a busy field? Too long/short? Does it distract during intense play (it's drawn on top of everything, including the pause menu)?
3. **Is the Achievements viewer legible?** Pause → Options → Achievements: this week's 5 (left) + the 12 lifetime (right), each with a ✓/`cur/goal`, earned rows in gold. 17 rows in one panel — does it feel cramped, and do the progress numbers read at a glance? Would you want a first-class title-screen entry (currently only the hint is there)?
4. **Do the achievement thresholds feel earnable-but-meaningful?** The 27 goals are first-pass from the starter pools. Any that feel unreachable, trivial, or degenerate (Waste Not especially — flagged as near-impossible-yet-cheesable)? This decides which to retune/retire in a curation pass.
5. **Sanity-check the "same 5 every week" premise informally** — the active weekly set is fixed by the ISO calendar week; it should be stable all week and change on Monday (UTC). No action needed unless it visibly misbehaves.

**Menu / options / rebinding (v1.9/F8):**
6. Does menu navigation feel natural on **keyboard and gamepad** (slider granularity at `VOL_STEP` 0.1; edge-detected one-press-one-move gamepad nav vs. wanting held auto-repeat)?
7. Does a **rebound control take effect immediately** in live play? Are the two quirks acceptable — a gamepad rebind **dropping that action's stick axis**, and **no conflict detection** when rebinding to an already-used key?

**Controller (v1.8/F7):** deadzone comfort at `GP_DEADZONE` 0.25; shield on RT/RB (a gap-fill — right button?); D-Pad-vs-stick feel; whether a controller-only start produces sound or needs one keyboard press to unlock audio (known Web Audio limitation).

**Powerups (v1.7/F6):** drop rate (`POWERUP_DROP_CHANCE` 0.10) + ambient Health cadence feel *earned but not stingy*; Magnet pull reads as a sweep not a jitter; the five icons + HUD duration row read clearly.

**Hunters (v1.6/F4):** early-game calm at `HUNTER_FLOOR_FRAC` 0.58 vs. a smooth escalation; the point-blank shield-kill of a medium (spawns 3 scattering smalls) feels *fair*, not a death trap.

**Saucers / pacing (v1.5/F10):** waves 1–4 feel calm; ramp intensity reads as a schedule, not a cliff.

**Salvage / garbage (v1.4/F3 + v1.3):** the field reads as "richly optional salvage," not "carpeted in clutter"; 12 s decay is long enough to collect a lineage without feeling frantic; knockback never yanks the ship somewhere that unfairly severs the whole haul; a fresh wave spawn can't instantly sever a fresh chain too often. **And the deferred joint F3+F10 Debris-density retune** — with every system now in, this is the last major early-game balance pass; judge it once the combined field feel is confirmed.
