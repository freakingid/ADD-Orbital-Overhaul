# Orbital Overhaul — STATUS

Last updated: 2026-07-12 · Build version: **3.3 (Phases 1–2 of 4)** — Orbital Overhaul rename; opaque dialogs; single-hull danger-tinted clumps; LEVEL in the HUD; **authored satellite line art for the Debris Satellites** · Last session: **v3.3 Phase 2 — real satellite drawings for `DebrisSatellite` (Changeset-4 items 2 and 5).** Scope was `DebrisSatellite` ONLY — the Hunter's diamond is untouched (it's the threat tell; FORK-1 resolved: NO, took the recommendation). (1) **New module-level `SAT_ART` table** (placed just above `class DebrisSatellite`): **six original, wrecked satellite silhouettes** — comms bus + twin solar panels (right panel snapped/skewed), dish relay (drooping/cracked parabolic dish), boxy Earth-observer with an instrument boom (one panel hanging broken), a Sputnik-ish sphere with radial whip antennas (one bent), a cylindrical spent upper-stage/booster (dented skin, flared nozzle), and a truss/array segment (one rail snapped short, torn end cap). Each entry is `{ full, small }`, where each of those is an array of `{ pts, closed }` polylines authored in **unit space** (every coord inside the radius-1 circle so it scales cleanly to any tier by ×`this.radius`), drawn through the **existing `drawPoly`/`glowStroke`** at `COLOR.debris` — **no fills, no new render primitive, no external assets** (original stylized art authored in code, nothing traced/imported — FLAG A-7; the file stays a self-contained GPL-3.0 HTML). (2) **`DebrisSatellite` rewired** — the constructor now picks one archetype per instance (`this.art = SAT_ART[floor(rand(0, SAT_ART.length))]`; splits re-roll independently since each `new DebrisSatellite(...)` re-runs the ctor), selects the `small` variant at the small tier (`size === 1`) else `full`, and **bakes a per-instance copy ONCE** — each polyline's points scaled to `this.radius` with a small random per-vertex jitter (`0.045·radius` px) for a "no two alike / wrecked" look — into `this.art`. The old procedural `this.hull` + `this.shards` fields are **gone**; `draw()` just iterates `this.art` (`for (const pl of this.art) drawPoly(pl.pts, this.x, this.y, this.angle, COLOR.debris, pl.closed)`). Jitter is applied in the ctor, **never per frame**. (3) **Random spawn rotation preserved (item 5)** — `this.angle = rand(0, TAU)` and `this.spin = rand(-1.2, 1.2)` survive the draw-path rewrite; nothing anchors art to a fixed "up" (the draw path only rotates by `this.angle`, which `update()` advances by `this.spin`). Asserted with a 40-instance spread. (4) **Small-tier legibility gate** — at `radius = 13` a 10+ segment design is mush, so each archetype's `small` variant is a simplified 2–4 stroke reduction (body + one feature); the small tier draws it instead of `full`. A look-call, tuned by eye. (5) **Collision unchanged** — `this.radius`/`DEBRIS_RADII` are untouched; art is cosmetic, collision stays a circle (GDD §3.1). The title screen's decorative `titleDebris` uses `DebrisSatellite`, so it gets the new art for free. **Headless-tested** (`scratchpad/test-v33-p2.js`, new, **28 assertions**, driving the REAL class + table — no reimplementation): `SAT_ART` is 5–6 entries, every `full`/`small` an array of `{ pts:[[x,y]…], closed:bool }` with **every unit-space coord inside the radius-1 circle** (caught one truss corner at magnitude 1.007 and pulled it in); a fresh `DebrisSatellite` of each size has a non-empty baked `art`, a random `angle` in `[0,TAU)` and `spin` in `[-1.2,1.2]`, unchanged collision `radius`; a 40-instance spread confirms `angle` is per-instance random (not anchored) and `spin` tumbles both ways; `draw()` is crash-free at all three sizes; each archetype's `small` variant is ≤ its `full` in polyline count; per-instance jitter makes instances differ and the baked `art` is a copy, not the shared `SAT_ART` reference; `hull`/`shards` are gone. **Also hardened `test-firerate.js`** — its section D counts fire-cadence volleys over 1 s of real `update()` but didn't clear ambient hazards, so a wave-1 debris/saucer drifting into the stationary test ship could land a hit whose hit-stun interrupts firing and drops the volley count below the expected band — a **pre-existing** rare RNG-timing flake (~0.7% on HEAD, same class as the documented `test-p5`/`test-starfield` flakes) whose rate rides on the shared `Math.random` sequence, so P2's extra per-instance ctor `rand()` calls lifted it to ~4%. The fix clears `game.debris`/`hunters`/`saucers` each frame **exactly as it already clears `game.bullets`** — the test's own stated intent (its comments) is to isolate *cooldown-driven* cadence — taking it from 6/150 to **0/250** flakes; **no game logic changed** (verified: HEAD flaked too, and my change only reshuffles RNG, not the fire code). **Full regression re-run green: all 20 test files, 916 assertions** (up from 888 — the new file adds 28; no other existing test needed a change beyond the firerate isolation hardening). `node --check` clean on the extracted script. GDD updated in place: **§2.4** (the Shape bullet — `SAT_ART` supersedes the procedural silhouette, with the archetype list + the small-tier gate + the "art is cosmetic, collision is a circle" note), the Architecture Map (**Constants** row — the new `SAT_ART` table; **Entity classes** row — `hull`/`shards` → `art`), the top Version line, and a new §7 Version History v3.3 P2 entry. Did NOT push. **Next up per `IMPLEMENTATION-PHASES-v3.3.md`: Phase 3 — the Scoop powerup + weighted drop economy** (High complexity, keep it alone in its session; depends on P1's HUD layout; blocked on FORK-2 [oriented box vs circle — rec: box] and FORK-3 [scoopHits reset on level gain — rec: no] sign-off). ⚠️ **Prior session: v3.3 Phase 1 — the presentation pass (Changeset-4 items 1, 3, 4, 7).** Render and strings only — no sim/collision/constant touched. (1) **Rename** — "Asteroid Field Deluxe" → **"Orbital Overhaul"** everywhere: the `<title>` tag, the header banner comment's first line (version-log body kept as-is), the title screen (`"ASTEROID FIELD"`/`"D E L U X E"` → `"ORBITAL"`/`"O V E R H A U L"`, same sizes/colors/positions), and the doc set — `asteroid-field-deluxe-GDD.md` → **`orbital-overhaul-GDD.md`** (git-mv'd, H1 + Version line updated), `CLAUDE.md`'s doc-layer pointer, and this file's own H1 + design-authority reference. **The two `localStorage` keys (`afd_settings_v1`, `afd_achievements_v2`) are deliberately NOT renamed** — the `afd_` prefix is load-bearing (renaming either wipes every player's saved settings/bindings/achievement tiers on next load); an in-code comment now sits at each declaration saying so. (2) **Dialog opacity** — `menuPanel()` previously only `strokeRect`'d its panel rect, so the world showed straight through and menu text was hard to read; it now `fillRect`s (`rgba(2,6,14,0.95)`) *before* the existing glow-stroke. All five menu screens (root/options/difficulty/controls/achievements) route through `menuPanel`, so this one edit fixed all five. `drawMenu()`'s backdrop dim was also raised `rgba(4,10,20,0.74)` → `~0.88` (the achievements screen's own redundant backdrop fill, drawn on top, bumped to match). Both alphas are commented as look-call playtest knobs. (3) **Clump silhouette** — in `Garbage.draw()`, `pieces > 1` now takes a completely different branch from `pieces === 1` (unchanged, byte-identical): ONE closed hull polygon (7–9 vertices jittered around the derived `radius = 7·√pieces`) replaces the v3.2 P1 mini-canister cluster, because playtest read the cluster as "a pile of pickups" rather than "a target." A new `makeClumpHull(radius)` helper generates the hull **once**, cached on `this.hull` — set in the constructor, regenerated only at merge time inside `coalesceGarbage` (never per frame) — and rotated live by `this.spin` so it still tumbles. A new `lerpColor()` helper tints the hull from `COLOR.garbage` toward a new **`COLOR.clumpHot`** (`#ff5a2a`) as `pieces / HUNTER_COALESCE_COUNT → 1`, so the clump visibly reddens as it nears the Hunter transform — a free threat meter layered on the new silhouette. Radioactive alpha flicker + `glowStroke` kept; `shatterClump`/`coalesceGarbage`'s merge math untouched. (4) **LEVEL HUD** — the old `WAVE n` top-right readout (22px, `COLOR.dim` — technically present but too dim/small/cornered to read) is gone; a new **`LEVEL n`** readout (22px, `COLOR.text`) sits in the left column directly under the HULL bar, with CARGO/TARGETS/the powerup row's `prow` start all shifted down ~22px to make room. Display-label rename only — `game.wave`/`nextWave()`/`difficultyFactor(wave)`/`ramp(...)`/every achievement reading `game.wave` are all untouched; "level" is now the player-facing word, "wave" stays the code term (both documented in GDD §2.7). **Headless-tested** (`scratchpad/test-v33-p1.js`, new, **16 assertions**, driving the REAL `menuPanel`/`Garbage`/`coalesceGarbage` — no reimplementation): `menuPanel` calls `fillRect` before `strokeRect` (an instrumented recording canvas ctx asserts call order) and still returns `{x,y}`; `Garbage.draw()` is crash-free at `pieces` 1, 2, and 11; the cached hull is stable (same array reference + point values) across five repeated `draw()` calls and regenerates (new reference, re-derived radius) after a real merge driven through `coalesceGarbage`; the `pieces===1` branch stays crash-free; `COLOR.clumpHot` is a hex color distinct from `COLOR.garbage`. **Full regression re-run green: all 19 test files, 888 assertions** (up from 872 — the new file adds 16; no existing test file needed adjustment, confirming the render-only scope held). `node --check` clean on the extracted script. GDD updated in place (git-mv'd to `orbital-overhaul-GDD.md`): the top H1 + Version line, §2.5.1 (clump render marked superseded with the new description), §2.7 (LEVEL/wave vocabulary split), §2.16 (dialog opacity + the frozen-key note), the companion-docs intro (now points at the active v3.3 planning docs instead of stale v3.1 references), the Architecture Map (Constants/Entity classes/`draw()` rows), and a new §7 Version History v3.3 P1 entry. Did NOT push. **Next up per `IMPLEMENTATION-PHASES-v3.3.md`: Phase 2 — real satellite line art for `DebrisSatellite`** (independent of P1, can ship in either order; blocked on FORK-1 sign-off — recommendation is NO Hunter art change, the diamond stays the threat tell). ⚠️ **Prior session: v3.2 Phase 3 — salvage is permanent; the garbage economy closes.** Garbage stops being ephemeral clutter on a timer and becomes a standing field object the player must actively manage. Changes: (1) **Decay removed entirely** — the `decay` field, the `GARBAGE_DECAY` (12 s) / `GARBAGE_SEVER_DECAY` (10 s) constants, the decay countdown + death in `Garbage.update()`, the blink-out branch in `Garbage.draw()`, and the `game.stats.garbageDecayed` increment are all gone; `Garbage`'s ctor dropped its `decay` param (mass is now the 5th arg) and `Garbage.fromNode` dropped its `decay` param. A canister now leaves the field by exactly **two doors: RECYCLED or CONSUMED INTO A HUNTER** — never by aging out. (Powerup decay is a separate system — untouched.) (2) **Economy closed (FORK-B / B1)** — the coalescence transform site tags its new core `bornOfScrap = true`; `HunterSatellite` gained a `bornOfScrap` field (default `false`); `destroyHunter` propagates the flag to the 3 split children (both generations) and **skips ALL garbage emission for a `bornOfScrap` Hunter at every tier** (large/medium 3-normal burst AND the small-tier 6-low-mass burst). Score, splits, contact damage, and the small-tier powerup drop are **unchanged** — only the garbage emission is suppressed. Rationale: with decay gone, a normal 13-member lineage drops `12 + 54 = 66` canisters, so an un-suppressed scrap-born lineage would be a **5.5× amplifier** (12 pieces in → 66 out) and the field would diverge without bound; B1 makes it **12 in, 0 out** (that scrap has already been spent — it IS the Hunter now), and gives the player a real reason to *let* a clump transform occasionally (a scrap-born lineage is pure score, no cleanup bill). Timer-spawned Hunters (`bornOfScrap = false`) are untouched and still drop the full 66. (3) **Two constants retuned** (both commented as playtest knobs): `GARBAGE_COALESCE_DELAY` **1.0 → 6.0 s** (at the 20–55 px/s emission kick, 6 s carries same-explosion siblings 120–330 px apart, so a burst drifts apart and *later happens upon strangers* instead of instantly re-clumping — Phase 2's shatter path reuses this same constant, one knob); `GARBAGE_MAGNET_RANGE` **140 → 260 px** (140 was tuned when garbage lived 12 s and only had to find siblings in a fresh tight cloud; with permanent garbage, pieces that drift further apart would never re-find each other and the field would silt up — flagged in-code as the first thing to tune by feel, FLAG D-1). (4) **"Waste Not" repurposed in place** — id + pool slot kept (so the 16-entry weekly pool length, `poolIndex` rotation math, and `test-f9`'s hardcoded slice values need NO recompute); new per-game stat `game.stats.hunterCoalesced` (incremented once at the coalescence transform, replacing the removed `garbageDecayed`); desc + `cur()` rewritten to `(s.gameEnded && s.hunterCoalesced === 0)` — "finish a game with no Hunters born from neglected scrap." Kept the display name (it still fits: you wasted no salvage to the coalescence sink). **Did NOT** (per the phase's explicit non-scope): add a spatial hash to `coalesceGarbage` (O(n²) with the early-continue is fine at realistic counts, render is viewport-culled — follow-up only if a stress test shows it, FLAG D-2); change `DEBRIS_GARBAGE`/`HUNTER_GARBAGE`/`HUNTER_SMALL_GARBAGE` (held in reserve as playtest levers); or touch `GARBAGE_CLUMP_MAXSPD`. **Headless-tested** — `scratchpad/test-v31-coalesce.js` extended 61 → **80 assertions** and `scratchpad/test-f9.js` 277 → **283**, both driving the REAL `update`/`destroyHunter`/`coalesceGarbage`/`evaluate` (no reimplementation): a canister survives a ~120 s `update()` run and never dies of age; `garbageDecayed` is gone from `game.stats`; a `bornOfScrap` core killed through its FULL lineage (large → 3 med → 9 small, 13 kills) emits **ZERO** garbage while awarding the **same** score (2900) as a normal lineage and still dropping its small-tier powerup (RNG-forced, 9/9); a **timer-spawned** Hunter still emits the full **12 normal + 54 low = 66**; the `bornOfScrap` flag survives **both** split generations; `hunterCoalesced` increments exactly once per 12-piece transform; the constant retunes are asserted (6.0 / 260) and section (1)'s delay-gate test was reworked to advance past the 6 s delay via the constant; Waste Not unlocks on a zero-coalescence finished game and does NOT unlock after one Hunter is born from scrap; the weekly pool is still length 16 with every `poolIndex` assertion **unchanged**. `test-f3.js` adjusted (dropped the two removed constants from its symbol list, moved mass to the 5th ctor arg, and swapped the sever-decay assertion for a "severed garbage carries no `decay` field" check). **Full regression re-run green: all 18 test files, 872 assertions** (up from 847 — coalesce +19, f9 +6; `node --check` clean on the extracted script). GDD updated in place: **§2.10** "Garbage drops" + "Chain vulnerability" (canisters no longer decay / severed scrap no longer gets a shorter decay), **§2.10.1** — the load-bearing "Decay exists to protect readability — don't remove it" bullet **rewritten** to say coalescence + shatter-to-harvest are the new density levers, *with the reason the old rationale was superseded so a future session doesn't restore it*, **§2.5.1** (the `bornOfScrap` rule + rationale, the 6.0 s delay, the 260 px range, the two-exits framing), **§2.17** (repurposed Waste Not), plus the top Version line, Architecture Map (Constants / Entity classes / game-object / Flow-functions / update(dt) rows), the §2.4/§2.7/§4-roadmap/perf-risk/playtest-ask mentions of decay-as-a-lever, and a new §7 Version History v3.2 P3 entry. Did NOT push. **Next up per `IMPLEMENTATION-PHASES-v3.2.md`:** whatever Phase 4+ scopes (v3.2 is Phase 3 of N); the round's playtest asks (magnet range, coalesce delay, shatter-loop engagement, clump heft/legibility, late-wave perf, B1-as-a-real-choice) are logged below for the next round. ⚠️ **Prior session: v3.2 Phase 2 — shatter a clump with one shot.** Phase 1 made a `pieces>1` clump un-hookable; this phase is the tool that reopens it. Changes: (1) **New collision branch** — in the non-hostile bullet loop, alongside the existing debris/saucer/hunter branches, a player bullet is now tested against every `pieces > 1` clump (`dist2` against the clump's P1-derived `radius`); on a hit `b.dead = true` and `shatterClump(g)` fires, then `break`. A `pieces === 1` single is still **not** a bullet target — player bullets pass straight through it exactly as before — and hostile (saucer) bullets still pass through **all** garbage regardless of `pieces` (untouched hostile branch). (2) **New flow function `shatterClump(g)`**, placed right after `destroyHunter` (shaped like `destroyDebris`/`destroyHunter` — a flow function, not a method): `g.dead = true`; a small `boom(g.x, g.y, 1, COLOR.garbage)` for the pop; then exactly `g.pieces` fresh `new Garbage(...)` singles pushed at `(g.x, g.y)`, each getting `mass = g.mass / g.pieces` (per-piece mass identity from mixed merges is averaged back out — FLAG A-3, an accepted, invisible-in-play fidelity trade), the constructor's default `pieces = 1` + **full, re-armed `coalesceDelay`** (no special-casing needed — `new Garbage()` already starts at `pieces=1` and a full delay), and an outward kick of `rand(GARBAGE_SHATTER_KICK[0], GARBAGE_SHATTER_KICK[1])` px/s added to `0.25×` the clump's own drift, mirroring the `destroyDebris` fan-out pattern exactly. **No score** — `addScore()` is deliberately not called; the reward is the newly-hookable canisters themselves. (3) **New constant `GARBAGE_SHATTER_KICK = [40, 90]`** (px/s outward-kick range), added to the salvage/chain constants block next to the other `GARBAGE_*` values, commented as a playtest knob. Did NOT touch decay, emission counts, the coalescence pass itself, or the Hunter transform (Phase 3 owns those). **Headless-tested** (`scratchpad/test-v31-coalesce.js` extended 48→**61 assertions** — drives the REAL bullet loop / `update()`/`shatterClump`, no reimplementation; `Bullet` and `shatterClump` added to the test harness's returned-symbol list): one player bullet on a `pieces=7` clump (mass 3.5) yields exactly 7 live singles, all `pieces===1`, all with a full re-armed `coalesceDelay`, all `mass === clumpMass/7` (0.5), total emitted mass conserves the clump's mass exactly, and `game.stats.garbageDecayed` is untouched by the shatter; those 7 do **not** immediately re-merge on the very next `update()` frame (the 1 s delay gate holds); a player bullet passes straight **through** a `pieces=1` canister (neither dies); a hostile bullet passes straight through a `pieces=6` clump (clump survives); and — the point of the whole phase — the 7 emitted pieces **are** hookable once in pickup range: shattering a `pieces=3` clump sitting on the ship, then driving one further real `update()` frame, hooks all 3 resulting singles onto the chain (the P1 `pieces===1` pickup gate now lets them through). Full regression re-run green across **all 18 test files (847 assertions**, up from 834 — the coalesce file added 13; no other test file needed adjustment, confirming the new collision branch and flow function don't interact with any other system's fixtures). `node --check` clean on the extracted script. GDD updated in place: **§2.5.1 gained a new "Shatter" bullet** (the mechanics, the FLAG A-3 note, the no-score rule, and the "greed/neglect share a fulcrum" framing) plus small wording fixes elsewhere in that section replacing "the shatter tool arrives next phase" with references to the shipped rule; **§2.10 "Chain vulnerability"** corrected — "player bullets pass through garbage" is now qualified as true only of **single** canisters, with the P2 clump-shatter exception spelled out. Architecture Map updated: **Entity classes** row (no new fields; `shatterClump` is the mutator), **Flow functions** row (new `shatterClump` entry), **Constants** row (new `GARBAGE_SHATTER_KICK`, noted as reusing the P3-era `GARBAGE_COALESCE_DELAY` rather than adding a second delay constant). Version line + a new §7 Version History v3.2 P2 entry (appended to the existing P1 entry) also updated. **Next up per `IMPLEMENTATION-PHASES-v3.2.md`: Phase 3 — garbage never decays + close the escalation loop** (removing `GARBAGE_DECAY`/`GARBAGE_SEVER_DECAY`, the `bornOfScrap` no-garbage-emission rule for coalescence-born Hunters per FORK-B/B1, retuning `GARBAGE_COALESCE_DELAY` 1.0→6.0 and `GARBAGE_MAGNET_RANGE` 140→260, and repurposing the "Waste Not" achievement in place). ⚠️ **Prior session: v3.2 Phase 1 — garbage clumps gain real mass, size, and a cluster silhouette; clumps are now un-hookable.** Reverses the shipped v3.1 **FORK-2** decision (GDD §2.5.1): FORK-2 kept a clump's mass at 1.0 so it towed as one normal canister — correct *only while clumps were hookable*. This phase makes clumps un-hookable, removing FORK-2's reason. Changes, all in `coalesceGarbage`/`Garbage`/the pickup pass: (1) **`Garbage.mass` SUMS on merge** — the survivor takes `a.mass + b.mass` (was: kept its base mass); `pieces` keeps its meaning (the 1..11 threat counter, Hunter at 12). (2) **`Garbage.radius` is derived** — `7 * Math.sqrt(this.pieces)`, recomputed in the constructor and at every merge (a single piece still reads 7 — no new constant). (3) **Merge velocity is now momentum-conserving** — `v = (a.mass*a.v + b.mass*b.v)/(a.mass+b.mass)` with the PRE-merge masses, replacing the shipped literal vector sum (with mass summing + garbage soon permanent, a literal sum made every merge ADD speed → clumps became the fastest things on screen). (4) **Attraction is mass-weighted** — `a.v += (dx/d)*GARBAGE_MAGNET_PULL*b.mass*dt`, `b.v -= (dx/d)*GARBAGE_MAGNET_PULL*a.mass*dt` (gravity-like, momentum-conserving; reduces EXACTLY to the shipped symmetric pull at mass 1.0, so `GARBAGE_MAGNET_PULL` is unretuned) — heavy clumps become slow anchors loose singles fall into. The wrap-aware `dist2`/`shortDelta` math and the off-by-default `GARBAGE_CLUMP_MAXSPD` clamp are untouched. (5) **Cluster render** — `Garbage.draw()` at `pieces>1` draws `pieces` mini-canisters via the existing `drawCanister` at golden-angle offsets (`theta = k*2.39996`, `r = 5*sqrt(k)` for `k` in `0..pieces-1`), all rotated by `this.spin` so the wad tumbles as one body; `pieces===1` collapses to a single centered canister, byte-identical to before; the `mass<1` paler-tint rule kept. (6) **A clump cannot be towed** — the pickup hook now additionally requires `g.pieces === 1` (the load-bearing rule; the next phase adds the shatter tool), and the **Magnet powerup pull also skips `pieces>1`**. (7) Clumps stay **non-solid** to the ship (FORK-A2 — zero contact damage, not added to `hazards`; hazard loop untouched). Did NOT touch decay, bullet collision, shatter, or emission counts (those are Phases 2/3). **Headless-tested** (`scratchpad/test-v31-coalesce.js` extended 29→**48 assertions** — drives the REAL `coalesceGarbage`/`update`/`Garbage`, no reimplementation): the old exact-vector-sum assertion was **rewritten** to the momentum sum (not deleted); mass sums across a chain of merges; radius tracks `7*sqrt(pieces)`; a heavy clump absorbing a fast light piece barely speeds up (momentum, 100→9.09); two mass-1.0 singles attract bit-identically to the shipped force (reduction guard); a `pieces>1` clump on the ship is NOT hooked while a `pieces=1` canister still hooks + is pulled; the Magnet won't pull a clump; `draw()` crash-free at `pieces=1` and `pieces=11`. Full regression re-run green across **all 18 test files (834 assertions**, up from 815 — the coalesce file added 19; `test-p5`'s `fakeCanister` and `test-p6`'s two garbage stubs each gained `pieces:1` to match the new hook contract). **Newly observed: `test-p5` has a rare (~1/100 runs) intermittent flake in its Magnet-hook section (a random ambient hazard occasionally perturbs the ship mid-hook-loop) — confirmed present on the committed HEAD build too (it flaked once in 150 runs there on a *different* magnet-hook assertion), so it is PRE-EXISTING and not caused by this change; re-runs clean, like the documented `test-starfield` flake.** `node --check` clean on the extracted script. GDD updated in place: **§2.5.1 rewritten** (mass sums, derived radius, momentum-conserving merge, mass-weighted attraction, cluster render, un-hookable — with an explicit "SUPERSEDES FORK-2 and why" callout), §2.10 "Collection & chain" (hook now refuses clumps), the Architecture Map (Entity classes / game-object / update(dt) rows), the top Version line, and a new §7 Version History v3.2 P1 entry. No new constant added. **Next up per `IMPLEMENTATION-PHASES-v3.2.md`: Phase 2 — shatter a clump with one shot** (the tool that makes un-hookable a game, not a wall) — then Phase 3 (garbage never decays + close the escalation loop). ⚠️ **Prior session: v3.1 Phase 3 — mutual garbage magnetism + coalescence → Hunter formation.** Uncollected salvage is now a *threat*, not just decaying scrap. Added a per-frame **`coalesceGarbage(dt)`** pass (defined just before `updateChain`, called in `update()` immediately after the garbage pickup/magnet loop and before `updateChain(dt)`). Every `Garbage` gained two constructor defaults — **`coalesceDelay = GARBAGE_COALESCE_DELAY` (1.0 s)** and **`pieces = 1`** — inherited by all three emission sites (`destroyDebris`, `destroyHunter` large/medium, `destroyHunter` small) **and** `Garbage.fromNode`; `coalesceDelay` counts down in `Garbage.update()`, and a piece is **active** once it's ≤ 0. `coalesceGarbage` is O(n²) over active pieces (skipping any already dead this pass), all wrap-aware (`dist2`/`shortDelta`): **both** attraction and merging require **both** pieces active, so a fresh burst rides its explosion kick apart for 1 s before it can clump. *Attraction:* each active pair within `GARBAGE_MAGNET_RANGE` (140) accelerates toward the other at `GARBAGE_MAGNET_PULL` (40 px/s²), symmetric (equal & opposite); an off-by-default `GARBAGE_CLUMP_MAXSPD` (Infinity) speed clamp is a playtest knob (applied only in the attraction branch, via a `clampGarbageSpeed` helper — kept out of the merge path so the literal-vector-sum contract stays exact). *Merge* (both active, `dist2 < GARBAGE_MERGE_DIST²` = 12²): survivor velocity becomes the **literal vector sum** (`survivor.vx += other.vx; vy += other.vy`), `survivor.pieces += other.pieces`, `other.dead = true`; the survivor keeps its **base mass** (FORK-2 — a clump tows as one normal mass-1.0 canister, `pieces` is purely the threat trigger, decoupled from tow weight). A survivor stays in the inner loop after a merge (`continue`) so it can absorb multiple pieces in one pass — twelve co-located pieces collapse in a single frame. *Transform:* at `survivor.pieces >= HUNTER_COALESCE_COUNT` (12), `survivor.dead = true`, `game.hunters.push(new HunterSatellite(x, y, 3))`, `AudioSys.hunterborn()`, and `break` (survivor is gone). Coalesced Hunters are **independent of `game.hunterTimer`** (FORK-1 — multiple concurrent lineages allowed; the timed spawn stays one-at-a-time). **A merge does NOT increment `game.stats.garbageDecayed`** — coalescing is a *distinct* fate from decaying and must not trip Waste Not (verified). Relies on the existing end-of-frame dead-filter (`game.garbage = game.garbage.filter(...)`) for compaction — no new filter added. **Pickup (FORK-2) needed no change** — the existing gate pushes one node per hooked piece, so a `pieces>1` clump hooks & delivers as one mass-1.0 node (confirmed via the real `update()`). **New `AudioSys.hunterborn()`** — a distinct ominous cue on `this.sfx` (guarded `if (!this.ctx) return`): two low sawtooth voices, ~196→138 Hz and ~139→98 Hz (a dissonant ~√2/tritone apart), both descending over ~0.4 s — darker/longer than `explosion`; audio isn't spatialized so it plays whether the Hunter forms on- or off-screen (the only off-screen tell). **New constants** in the salvage/chain block near `GARBAGE_DECAY`: `GARBAGE_COALESCE_DELAY` (1.0), `GARBAGE_MAGNET_RANGE` (140), `GARBAGE_MAGNET_PULL` (40), `GARBAGE_MERGE_DIST` (12), `HUNTER_COALESCE_COUNT` (12), `GARBAGE_CLUMP_MAXSPD` (Infinity) — all tunable. **Headless-tested** (`scratchpad/test-v31-coalesce.js`, 29 assertions, new — drives the REAL `coalesceGarbage`/`update`/`destroyDebris`/`Garbage`): (0) constants sane + default-ctor/`fromNode`/real-`destroyDebris`-emission all inherit `pieces=1`+full delay; (1) no merge before 1 s (driven through the real `update()` countdown at 0 s and 0.6 s), merge after; (2) two active pieces in contact merge — survivor velocity == exact vector sum, `pieces==2`, earlier piece is survivor; (3) twelve active pieces → exactly one new large Hunter, all twelve dead, `hunterborn` stub fired exactly once; (4) a merge across the world x-seam (wrap-aware) with the sum-velocity preserved; (5) `garbageDecayed` unchanged by both a plain merge and a 12→Hunter transform; (6) a `pieces=5` clump on the ship hooks as exactly one mass-1.0 chain node via the real `update()`; (7) attraction is 1 s-gated too (fresh pieces feel no pull; active pieces get an equal-and-opposite pull). Full regression re-run green across **all 18 test files** (**815 assertions**, up from 774 — the new file adds 29 and prior `test-f9`/others were unchanged; `test-starfield`'s documented intermittent flake ran clean). `node --check` clean on the extracted script. GDD updated in place: **new §2.5.1** (coalescence subsection — 1 s delay, mutual magnetism, literal vector-sum merge, 12→Hunter, FORK-1/FORK-2, Waste-Not-safe, Magnet as counter-play), §2.5 large-core wording (timed spawn still one-lineage-at-a-time, coalescence adds independent concurrent lineages — replacing the "one lineage at a time" absolute), §2.7 (coalesced Hunters raise `TARGETS`/block wave-clear, and can happen on *any* wave incl. off-screen), §2.8 (`hunterborn`), the top Version line, §7 Version History (new v3.1 P3 entry), and Architecture-Map Constants + AudioSys + game-object + update(dt) rows. No PLANNED-FEATURES-v3.1.md change (Spec A scope matched the plan exactly; the doc pair is archived at round end per its own housekeeping note). **Next up per `PLANNED-FEATURES-v3.1.md`: v3.1 is now down to deferred/open items** (Waste-Not degenerate edge case, off-screen threat awareness, the `GARBAGE_CLUMP_MAXSPD` clamp playtest, and post-round doc archival) — the three scoped v3.1 gameplay changes (world shrink P1, wrap fix P2, coalescence P3) are all landed. **Prior session: v3.1 Phase 2 — fix the `+120` wrap-jump discontinuity.** `wrap()` (~line 1010) and `wrapNode()` (the chain node-wrap inside `updateChain`, ~line 2271) were teleporting entities by `WORLD_W + 120` / `WORLD_H + 120` on wrap, but every distance/aim/render helper (`dist2`, `angleTo`, `shortDelta`, `wrapOffset`, `wrapPos`) measures against period `WORLD_W`/`WORLD_H` exactly — the undocumented +120 px mismatch shifted every entity ~120 px relative to the ship at each seam crossing. **Fix:** both wrap jumps now shift by exactly `WORLD_W`/`WORLD_H`; the ±60 px trigger margins (anti-flicker hysteresis) are untouched — a jump of exactly `WORLD_W` from `x = −61` lands at `WORLD_W − 61`, safely inside `[−60, WORLD_W+60]`, so it can't re-trigger the same frame. `wrapNode` shifts `px`/`py` by the same delta as `x`/`y` so implied verlet velocity survives the teleport (GDD §3 rule) — verified directly rather than assumed. **Headless-tested** (`scratchpad/test-v31-wrap.js`, 14 assertions, new): two entities placed straddling each seam (x and y, both directions of approach) — `shortDelta` between them is bit-exact continuous before/after one wraps (no ~120 px jump), and the wrapped entity/node always lands in `[0, WORLD_W)`/`[0, WORLD_H)`; the chain-node case additionally asserts `x−px` (implied velocity) is unchanged by the teleport on both axes. Full regression re-run green across all **16** test files (**774 assertions total**, up from 760 — the new file adds 14; no existing test needed adjustment, confirming no other code depended on the old +120 behavior). `node --check` clean on the extracted script. GDD §2.11 (now "retuned v3.1 P1/P2"), the top Version line, §4 roadmap block, and the Version History v3.1 entry all updated in place — no PLANNED-FEATURES-v3.1.md change needed since this phase's scope matched the plan exactly. **Next up per `PLANNED-FEATURES-v3.1.md`: Phase 3 — mutual garbage magnetism + coalescence** (loose canisters clumping into a Hunter). Prior session: The old 9×-area world (v1.2) left too much dead space between spawns; per `PLANNED-FEATURES-v3.1.md`, the new world is 2×2 viewports (~44% of the old area). The binding clamp limit is `min(WORLD_W,WORLD_H)/2 − 60 = 660` (a ring reaching past half the shorter axis could wrap and place a spawn near-on-top of the ship) — both `SPAWN_MAX_DIST` and `DOCK_MAX_DIST` now sit safely under it, with `DOCK_MAX_DIST < SPAWN_MAX_DIST` preserved. **`STAR_DENSITY` deliberately untouched** — `STAR_COUNT` derives from world area (`STAR_DENSITY * WORLD_W*WORLD_H / (VIEW_W*VIEW_H)`), so it auto-scaled down with the smaller world and visual star density is unchanged with zero edits. **No wrap/camera/render logic touched** — `wrap`/`dist2`/`angleTo`/`shortDelta`/the camera transform are all expressed symbolically against `WORLD_W/WORLD_H` and needed no changes (the separate `+120` wrap-jump discontinuity bug is v3.1 Phase 2, not this phase — left untouched here). Headless-tested (`scratchpad/test-v31-world.js`, 12 assertions): constants at their new/clamped values; many real `nextWave()` debris spawns and real `Dock` placements (25 and 200 trials respectively, ship position randomized) land within `[SPAWN_MIN_DIST, SPAWN_MAX_DIST]` / `[DOCK_MIN_DIST, DOCK_MAX_DIST]` of the ship via the real wrap-aware `dist2`; `STAR_COUNT` matches the area-derived formula for the new world size. Full regression re-run green across all 15 existing test files (`test-starfield.js` has a pre-existing intermittent flake unrelated to this change — reruns clean). GDD §2.11, the top Version line, §4 roadmap, and the Architecture Map Constants row updated; Version History gained a v3.1 entry. **v3.0 is now fully closed out in the docs** — confirmed "Phase 9" never existed (per `PLANNED-FEATURES-v3.1.md`'s locked decisions), so v3.0's 8 phases are complete and its planning docs are archived (`archive/PLANNED-FEATURES-v3.md`, `archive/IMPLEMENTATION-PHASES-v3.md`); GDD preamble/companion-docs section and the Enhancement Roadmap (§4) were stale (still described v3.0 as "in progress, Phase 9 remains" and pointed at repo-root paths for already-archived files) and were corrected as part of this session's doc sync, not a code change. Added **seven new tiered lifetime achievements** (reusing P7's `tiers` machinery) + **one new weekly**, taking the pools to **WEEKLY 16 / LIFETIME 19 (14 tiered)**. The seven: **Sharpshooter** (small-saucer total, new SUM `l.smallSaucerKills`, diamond **7,500** per FLAG A-8-a — capped below Saucer Hunter's combined 10k), **Freight Baron** (≥12-canister dock visits, new SUM `l.heavyHaulerEvents`, 1/10/50/150/400/1000), **Zen Master** (5-in-a-row no-fire hauls, new SUM `l.pacifistTowEvents`, same ladder), **Daredevil** (Glass-Cannon games, new SUM `l.glassCannonGames`, **softened 1/5/20/50/125/300** per FLAG A-12-a), **Wave Rider** (Shield-Surfer games, new SUM `l.shieldSurferGames`, same softened ladder), **Salvage King** (**MAX** best canisters delivered in one game, new `l.bestDeliveredGame`, 10/20/40/75/125/200), **Field Sweeper** (**MAX** best Debris destroyed in one game, new `l.bestDebrisGame`, 15/40/80/150/250/400). **FORK A-fork-1 resolved per-EVENT:** `heavyHaulerEvents` latches **once per dock visit** at the literal `deliveryCount === 12` (fixed-12 per P6/B-8-b, NOT `cargoMax` — now sharing the exact `if (game.deliveryCount === 12)` block with `fullChains`); `pacifistTowEvents` latches **once per haul** at `pacifistStreak === 5` (reuses the reset-on-fire machinery — a streak passes through 5 exactly once per haul). **Daredevil/Wave Rider are inherently once-per-game** (per-event ≡ per-game) — incremented once at **`killShip`** from existing per-game state (`game.wave >= 5 && healthPicked === 0`; `deflects >= 10`), *before* the game-over `evaluate()` so the tier toast fires. The two MAX counters are `Math.max`'d at the dock-offload / `destroyDebris` sites as the per-game count climbs. **New weekly "Flawless Run"** (`flawless_run`, clear **wave 8+** damage-free — FLAG A-6-b, harder than + distinct from No Scratches's wave-3): `s.flawlessLateWave` set in the wave-clear damage-free block when `game.wave >= 8`. Hooks are all **one-liners at existing event sites** (observe-at-source): the two saucer-kill sites (`smallSaucerKills` lifetime, distinct from the per-game `game.stats.smallSaucerKills` Small Ball reads), the dock-offload block, `destroyDebris`, `killShip`, the wave-clear block. **New per-game stat** `flawlessLateWave` in `resetGameStats`; the seven lifetime counters added to `Achievements.lifetime`. **Persistence unchanged (`afd_achievements_v2`, no schema bump):** new counters flow through the existing known-key `loadCounters` loop + the `save` lifetime spread, and **`deriveLifetime()` auto-back-fills the seven new tiers' badges silently on load** (a returning player's past totals recompute their tier with no toast storm). **§A-5 audit confirmed in-code:** Pacifist Tow / Close Shave / No Scratches / Heavy Hauler all match — Heavy Hauler + Freight Baron + The Long Haul all key the **fixed 12**. **Rotation recompute (FLAG A-7-a):** `poolIndex` uses `.length` so it self-adjusts, but `test-f9.js`'s hardcoded values were recomputed for length 16 — `poolIndex(2026,2)` 9→**10**, `poolIndex(2026,6)` 13→**14**, both 5-slices updated (Flawless Run appended at index 15). **`test-f9.js` 169→277 assertions:** new config asserts (16/19/14-tiered, all 7 new counters + `flawlessLateWave` + the `flawless_run` weekly), per-tier boundary sweeps for all 7 new ladders, real-driven event-cadence tests (Freight Baron once-per-≥12-visit incl. an 11-visit no-count + a second visit → 2; Zen Master once-per-5-streak-haul + reset-and-recount → 2), Flawless Run wave-8-not-7 via the real wave-clear loop, Daredevil/Wave Rider at real `killShip` incl. the Health / under-10-deflects disqualifications, and Field Sweeper/Salvage King MAX via the real `destroyDebris`/dock paths. **Viewer relayout (a design call the plan didn't scope — FLAGGED):** 19 lifetime rows overflowed the old single-column list, so `drawAchievements` widened the panel to **1200×660** and splits lifetime across **two columns** (three total: weekly | lifetime | lifetime-cont., `ceil(len/2)` split) — a pure look-call tunable, browser-unverified (see new playtest ask). Regression green across f2–f9 + P4–P6 (**760 assertions across 15 files**). **Prior session: v3.0 Phase 7 — tiered achievements infrastructure + convert existing lifetime set (Part A, §A-1..A-3/A-5/A-7).** Added an optional ascending **`tiers:[bronze,silver,gold,titanium,platinum,diamond]`** ladder to a lifetime definition (replacing its single `goal`), with `TIER_NAMES` + a warm-metal→white-hot **`TIER_COLOR[]`** palette beside `COLOR` (FLAG A-1-a — no teal/green/lime so a badge never reads as a Hunter/Debris/salvage tell; all six are look-call tunables). **`evaluate()`** now forks: single-goal entries (all 15 weekly + the 5 non-tiered lifetime) keep the F9 `cur >= goal` path; a tiered entry unlocks **every newly-crossed tier in one pass, ascending** (a big lifetime jump pops several toasts at once, each naming its tier — "Recycling Magnate — Gold"), persisting the **highest tier index reached per id** in a new **`lifetimeTiers{id:idx}`** map. **Converted the seven flagged lifetime achievements** to the exact §A-3 ladders: Recycling Magnate (1k→100k), Ghost Protocol (50→10k), Saucer Hunter (250→10k), Perfect Wave (5→500), Iron Hull (100→10k) — all **SUM** — plus the two **MAX / personal-best** ones (FLAG A-1-b): Master of the Field (`l.maxWave`, 5→100) and No Powerups Needed (**new `l.maxWaveNoPowerup`**, 2→29). The MAX counter is updated `Math.max(current, wave)` **at each `nextWave` wave-clear only while `game.stats.powerupsPicked === 0`**, so it records the deepest wave reached before *any* powerup and **freezes** for the rest of the game the moment one is picked. **Reworded The Long Haul** (`l.fullChains`, still non-tiered) to a **fixed ≥12-delivered-in-one-visit ×10**, decoupled from the growing `game.cargoMax` (FLAG A-5-a / B-8-b) — the dock hook already latches on the literal `deliveryCount === 12`, so only the desc + an in-code comment changed. **Viewer rewrite:** `drawAchRow` renders a tiered lifetime row as **one row** — name + right-aligned **tier badge + progress-to-next** in the tier's colour ("Gold · 12,431 → 25,000"; "Diamond ✓ MAX" at the top; "— · 0 → 1,000" pre-bronze), via new `tierIndex`/`tierStatusText` helpers + a locale-independent `fmtCommas`. **Persistence bumped to `afd_achievements_v2`** (new shape `{lifetime, lifetimeTiers{id:idx}, lifetimeUnlocked[non-tiered], weekly{key,unlocked[]}}`); on load, if only the old `_v1` key exists, its raw lifetime **counters** are kept and tiers + non-tiered unlocks **re-derive silently** via a new **`deriveLifetime()`** (no toast storm — evaluate() then only toasts *new* crossings this session), old flat `_v1` unlock sets ignored (FLAG A-2 / A-2-a). `deriveLifetime()` also runs after every load so badges match counters before the first in-game `evaluate()`; a stale-low stored tier is advanced (never lowered) and a stored idx is clamped to the ladder length. **The module still observes, never drives — `cur()` untouched.** Kept the `typeof`-guard + try/catch (a corrupt v2 payload can't crash `init`). **`test-f9.js` heavily rewritten** (62→**169 assertions**): per-tier boundary sweeps for all 7 ladders (threshold-1 stays on the prior tier, exact threshold advances one), an explicit cross-bronze-not-silver + a multi-tier jump firing three ascending tier-named toasts, both MAX counters driven through the **real `nextWave`** (incl. the freeze-on-powerup), a v2 tier round-trip, the `_v1→_v2` migration (keeps counters, re-derives tiers, silent), a stale-tier advance, and a corrupt-payload no-crash. Weekly rotation + all non-tiered/gameplay paths stay green. **Prior session: v3.0 Phase 6 — tow-capacity progression + chain physics retune (B-8).** The fixed `CHAIN_MAX = 12` is gone, replaced by a runtime **`game.cargoMax`** that starts at **`CARGO_BASE` (12)** and **grows +1 per `CARGO_GROW_PER` (30) canisters recycled (delivered) in the current run**, bounded by **`CARGO_CAP_MAX` (20)** — cap-growth FORK A-fork-2 resolved **per-delivery** (the on-pillar choice: capacity earned by engaging the salvage loop, not handed out by wave). The growth check sits at the dock-offload site right after the per-run `game.stats.delivered++`, computing `min(CARGO_CAP_MAX, CARGO_BASE + floor(delivered / CARGO_GROW_PER))`; since `delivered` climbs by 1 per canister the cap rises at most one step at a time, each bump pushing a brief **"TOW +1"** float. `startGame` resets `cargoMax` to base. The **pickup gate** (`game.chain.length < game.cargoMax`) and **HUD** (`CARGO n/{cargoMax}`) now read the runtime cap. **Part 2 — the physics retune (the real work):** a naive m=20 chain under the old coefficients would be ~33% thrust / 50% top speed (the "broken, not heavy" zone GDD §2.10.1 warns about), so the mass-penalty coefficients were **softened in lock-step** — `CARGO_THRUST` 0.10→**0.06**, `CARGO_MAXSPD` 0.05→**0.03**, `CARGO_MASS` (tug) 0.12→**0.07** — solved so a *full chain at the new ceiling* (m=20) lands on the **old full-12 feel: 1/(1+20·0.06)=45.5% thrust, 1/(1+20·0.03)=62.5% top speed**, and the tug's 1.4 cap is reached at m≈20 (was m≈12). The per-node penalty therefore shrinks as the ceiling rises: a base-12 chain is now genuinely lighter (~58% thrust / ~74% top speed / 0.84 tug), leaving handling headroom to grow into. **Stability revalidation (the load-bearing risk):** the constraint loop's literal `3` became **`CHAIN_ITER` (4)**. A headless stress at 20 nodes (hard thrust-flips across a wrap, 900 frames) shows **no NaN, no velocity explosion, worst-case link stretch ≈4 px on a 20 px link** — already stable at 3 iters (4.36 px), but 4 tightens the worst link (link 0, anchor→head — the tail doesn't sag) and restores the pre-B-8 margin §3.4 validated at 12/3. **FLAG B-8-a resolved:** Engine (halves effective mass) at 20 nodes behaves like plain 10 (headless-asserted), acceptable both with/without. **FLAG B-8-b (for the achievements phases):** Heavy Hauler / Freight Baron / The Long Haul must key on a **fixed ≥12** delivered-in-one-visit, NOT `game.cargoMax` — the existing dock full-visit hook still keys on a literal `12` (`if (game.deliveryCount === 12)`), so it's already decoupled; the achievements-overhaul phases (7–9) must keep it fixed as the cap grows. **FLAG B-8-c (perf):** more towed nodes + higher canister volume = more `shadowBlur` in dense late waves — a browser-only playtest watch (`GARBAGE_DECAY` + a glow toggle remain the levers). **Also fixed the long-standing `test-f3` Engine-drop flake** (its `resetShip` now zeroes `game.powerFx`, so a random Engine drop during a prior `update()` can't halve `chainMass` and fail the (D) mass-sum assertions — 10/10 green now). New/changed constants all in the salvage/chain block: `CARGO_BASE`/`CARGO_CAP_MAX`/`CARGO_GROW_PER`/`CHAIN_ITER` added, `CARGO_THRUST`/`CARGO_MAXSPD`/`CARGO_MASS` retuned, `CHAIN_MAX` removed. **Prior session: v3.0 Phase 5 — Difficulty Options screen + powerup expiry modes (B-4/B-5).** Added an **Options → Difficulty** sub-screen with two opt-in mode toggles — **shot powerups (Rapid + Triple) expire by Time | Shots**, **Magnet expires by Time | Pieces** — plus the count-based expiry logic behind them. Both settings default to **`"time"`**, so *nothing changes for anyone until they opt in* (headless-verified: default reproduces the shipped timed behaviour exactly). Design: two persisted fields in `afd_settings_v1` (`shotPowerupMode`, `magnetMode`) on a new module-level **`settings`** object (additive — an older save missing the keys keeps the default, a corrupt value is ignored on load; no schema bump). Count budgets live in a new **`game.powerBudget {rapid,triple,magnet}`** (reset in `startGame`, parallel to `powerFx`; Engine has no budget — it's always timed, never configurable). The single new coupling point is **`powerActive(type)`** (+ **`powerMode(type)`**): every "is this effect live?" read — the fire block's triple-fan branch and rapid-cadence line, `maxBullets()`, `chainMass()` (Engine), the Magnet pull, and the HUD — now routes through it, so the two modes can never diverge (in time mode it reads `powerFx` seconds; in a count mode it reads `powerBudget` count). Decrements: **Rapid/Triple** each spend one **per trigger-pull** in the fire block (a Triple 3-fan is ONE pull; with both active both budgets tick once per pull and end **independently** — FLAG B-4-b; decrement placed *after* the fan/cadence reads so the last pull is fully a rapid/triple pull); **Magnet** spends one **per canister hooked** at the pickup/hook site — counted at the hook, not the pull, so a magnet that draws-then-hooks a piece spends once (FLAG B-5-a). `applyPowerup` sets the timer (time mode) *or* the budget (count mode), and a same-type pickup **refreshes to full** either way (never stacks) — matching the shipped rule. HUD: the active-effect row now iterates `powerActive` and, in a count mode, draws the bar as `remaining/POWERUP_BUDGET[type]` with a numeric shots/pieces label beside it (one renderer, two data sources). Menu: `"Difficulty"` added to `MENU_OPTIONS` before `"Back"` (Controls stays idx 3, Achievements idx 4, Difficulty idx 5, Back idx 6 — no F8/F9/P4 test broke since those key off labels/idx≤4); a `"difficulty"` sub-screen (`DIFFICULTY_ROWS` + `menuDifficulty`, drawn by `drawDifficulty`) mirrors `menuOptions`/`drawOptionsMenu` — two ◄/► toggle rows (◄ = timed default, ► = count variant) + Back, navigable through the existing `menuInput` dispatcher, persisting on each flip; left **structured to grow** (add a row to `DIFFICULTY_ROWS` + a switch case + a renderer row — FLAG B-4-c). New constants in the Powerups block: `RAPID_SHOTS` (40), `TRIPLE_SHOTS` (30), `MAGNET_PIECES` (20) + a `POWERUP_BUDGET` lookup. **All four FLAGs resolved as recommended** (B-4-a default time; B-4-b independent per-pull budgets; B-4-c growable screen; B-5-a count-at-hook). **One documented edge case:** switching a mode mid-effect (via the pause Difficulty screen) makes that effect read the *other* counter, which is 0 unless refreshed — so an active effect can appear to end on a mid-game mode flip; acceptable ("you changed the rules"), and `startGame` zeroes both so a mode-consistent game never sees staleness. Prior session: **v3.0 Phase 4 (revised) — pause/menu reachable from any state, with the corrected control scheme (B-1/B-2).** **This SUPERSEDES the earlier B-1-a mapping** (Start→menu) that a playtest rejected; the previous P4 attempt was reset out and this started clean at the P3 commit. `openPause()` is now callable from `title`/`gameover` as well as `playing` (`update()` already skips the sim outside `playing`, so `game.paused` is safe in any state). The **root menu is context-aware**, keyed purely on `game.state`: `rootItems()` returns `MENU_ROOT_PLAY = ["Continue","Options","Quit"]` when playing, else `MENU_ROOT_SYS = ["Options","Achievements","Back"]` (the system menu). `menuRoot()` now dispatches **by label** so one handler serves both layouts. The **corrected control scheme, as shipped:** (1) controller **Start = session toggle** — title/gameover → START A GAME, playing → OPEN PAUSE, paused → DISMISS & RESUME (this was already wired correctly on the clean tree, left as-is). (2) Keyboard **"O"** and controller **B** OPEN the Options/Achievements system menu from title/gameover; **Back** closes the overlay → underlying screen. "O" is a literal inline check in the keydown normal branch (not a binding-table entry). (3) Keyboard **ESC** pauses during play and backs out inside a menu: `bindings.pause.keys` is now `["escape"]` (**"P" retired — FLAG P4-a resolved: retire**) and shares ESC with `bindings.back.keys`; `handleMenuKey` resolves **confirm→back→pause** in order, so in-menu ESC matches `back` first (= back), and outside a menu the normal branch's pause-during-playing check makes ESC pause. (4) Controller **B is context-aware, mirroring ESC**: menu-open → back; no menu on title/gameover → open the system menu. (5) A/Enter still start a game. **FLAG P4-b (a single confirm can't both nav a menu AND start a game)** is guaranteed structurally: the gamepad normal branch is an **else-if chain** (Start > B > A, so same-frame B+A opens the menu without also starting), and both input handlers hit the menu-open context (which returns) before the title "press to start" line. New `game.menu.achReturn` (`"root"`|`"options"`) tracks which parent the Achievements viewer backs out to, since it now has two entry points (system root directly, or via Options). `drawMenu()` now also overlays the **title** (added inside the title block before its early `return`) and **gameover** (moved to draw *after* the GAME OVER text so the menu sits on top); `drawRootMenu` reads `rootItems()` and titles the panel "PAUSED" (play) / "MENU" (system). Title/gameover control hints updated (title: "PAUSE: ESC" and "OPTIONS / ACHIEVEMENTS: O (controller: B)"; gameover gained the same options hint). **This closes the long-standing "menu / achievements reachable only via pause" limitation** flagged since F8/F9. Prior session: **v3.0 Phase 3 — "targets remaining" HUD (B-7).** Added a `TARGETS n` readout directly under `CARGO` (`draw()`, HUD block), where `n = game.debris.length + game.hunters.length` — literally the wave-clear gate one line below it in `update()` (`if (game.debris.length === 0 && game.hunters.length === 0)`), so `TARGETS 0` means the wave is about to clear. Went with the single-line `TARGETS n` form recommended in `PLANNED-FEATURES-v3.md` B-7 (not the two-part `DEBRIS n · HUNTERS m` fallback) — flagged for a playtest read on whether the count jump reads clearly. Lit (`COLOR.text`) while `n > 0`, dim otherwise, same convention as CARGO. No sim changes — purely a HUD read of two existing array lengths. The active-powerup row shifted down 20px (`prow` 130→150) to make room for the new line. Documented in-code and in STATUS/GDD that the count can **rise** mid-wave: a Debris split (large → 3 mediums, +2, FLAG B-7-a) or a Hunter lineage arriving on its own 14–32s timer from wave 2 on (FLAG B-7-b) — intentional, not a bug, since the wave genuinely isn't over until Hunters clear too. Prior session: **v3.0 Phase 2 — fire-rate rebalance (B-6).** A single `FIRE_COOLDOWN` (0.16 s) previously gated every shot, and Rapid Fire only raised `maxBullets()` — cadence never changed, the bug Paul felt. (1) Base `FIRE_COOLDOWN` retuned slower, 0.16→0.20 s. (2) New `RAPID_FIRE_COOLDOWN` (0.09 s), clearly faster than base. (3) The fire block in `Ship.update` now sets its post-shot cooldown to `RAPID_FIRE_COOLDOWN` when `game.powerFx.rapid > 0`, else `FIRE_COOLDOWN`. Triple Shot is untouched — it still fires the 3-bullet spread on the base cadence and keeps its higher `maxBullets()` cap — so Rapid+Triple together is now the strongest combo (fires fast *and* wide, FLAG B-6-a), flagged for playtest rather than pre-emptively rebalanced. GDD §2.2 updated with the new cooldown values and the combo flag; Architecture Map constants row updated. Prior session: **v3.0 Phase 1 — starfield legibility & parallax (B-3).** The starfield already existed (`drawStarfield`, `stars[]`, flat `STAR_DENSITY=40` at `#1a2a40`) — this enhances it in place, no second system. (1) Each star now carries a per-star brightness `a` (`STAR_BRIGHT_MIN/MAX` = 0.35–1.0) drawn in a brighter cool blue (`rgba(120,160,210,a)`) so the field reads without competing with entities (Pillar 1). (2) `STAR_DENSITY` raised 40→80 (scales by world area, one knob). (3) Added **one** nearer parallax layer (`starsNear[]`, `STAR_NEAR_DENSITY=22`, brighter `rgba(160,195,235,a)`, `STAR_PARALLAX_FACTOR=0.5`) so the ship's motion produces visible relative drift. Per FLAG B-3-a, a ≠1× layer isn't world-fixed, so it can't reuse `shortDelta`/wrap math — it's tiled in **screen space** instead (a 3×3 `VIEW_W×VIEW_H` tile stamp keyed off `camera * STAR_PARALLAX_FACTOR`, culled to viewport), which sidesteps seam artifacts entirely. The original far layer is untouched structurally (still world-fixed, wrap-aware, 1.0×) and remains the depth reference. Five new constants (`STAR_PARALLAX_FACTOR`, `STAR_BRIGHT_MIN/MAX`, `STAR_NEAR_DENSITY`, `STAR_NEAR_BRIGHT_MIN/MAX`) join the retuned `STAR_DENSITY` in the v1.2 world block. Purely cosmetic — zero gameplay effect, nothing in `update()` touched. Prior session: **added a dotted world-boundary line** — a dim, dashed `WORLD_W×WORLD_H` rectangle rendered at the wrap seam so the player can see the wrap-around coming and crossing it. `drawWorldBoundary()` (near `drawStarfield`) strokes the rect via `setLineDash([BOUNDARY_DASH, BOUNDARY_GAP])` + the existing `glowStroke` at `COLOR.dim` with low glow; it's the first thing drawn inside the camera transform (beneath every entity), positioned at the world-origin corner's **nearest wrapped image** relative to the camera (`wrapOffset` on both axes) so the seam shows whether the ship is near world x≈0 or x≈WORLD_W (y likewise) and stays continuous across the crossing. Purely cosmetic — no collision, no gameplay effect. Four new tuning constants (`BOUNDARY_DASH/GAP/WIDTH/GLOW`) live in the v1.2 world block. This is an **interim** wrap-visibility aid; full off-screen threat awareness stays parked for v3.0. Prior session: **housekeeping — archived the two spent v2.0 planning docs** (`PLANNED-FEATURES-v2.md` + `IMPLEMENTATION-PHASES.md`) into `archive/` after verifying every shipped v2.0 feature has its spec in GDD §2 (no doc-handoff gaps); no code change. Before that: implemented **F9 — Achievements System**, the final v2.0 phase. Added an `Achievements` module holding two pools — **15 weekly** (a calendar-deterministic 5-wide slice active each ISO week) and **12 lifetime** (cumulative across games) — that **observes** stats the rest of the game already produces via one-liner hooks at each event's existing site (kills in `destroyDebris`/`destroyHunter`, deliveries in the dock-offload block, hits in `damageShip`, deflects at the four shield-absorb sites, powerups in `applyPowerup`, expiries in `Garbage.update`, perfect waves in the wave-clear block, best-wave in `nextWave`) — **no new indirection layers, no restructured systems**. Per-game counters live in a flat `game.stats` bag (`resetGameStats()`, reset in `startGame`); lifetime counters + unlock sets persist to their **own** `localStorage` key (`afd_achievements_v1`, separate from settings), guarded + try/caught exactly like F8. Weekly rotation is pure math — pool index `(isoYear*52 + isoWeek) % 15`, 5 consecutive entries wrapping the pool, from the real UTC date at launch — so **every player worldwide sees the same 5 in the same calendar week**; a new week resets weekly unlocks while lifetime is always retained. An unlock pops a non-blocking gold **toast** (`AudioSys.achievement()` fanfare, aged out in the main loop by `updateToasts` so it animates in every state) and is browsable in a read-only **viewer at Pause → Options → Achievements** (`drawAchievements`: this week's 5 + the 12 lifetime, each with unlock status + `cur/goal` progress). `Achievements.evaluate()` runs once per frame (already-unlocked entries short-circuit) and again at game over (so end-of-game checks like Waste Not can fire).

**v2.0 (Phases 1–9), v3.0 (Phases 1–8), v3.1 (P1–P3), and v3.2 (P1–P3) are all shipped; v3.3 Phase 1 of 4 is now landed.** The **achievements overhaul (Part A) is complete** — P7's tier infrastructure + converted lifetime set and P8's seven new tiered lifetime/event achievements + the "Flawless Run" weekly + the 16-pool rotation — with its spec now fully in **GDD §2.17** (tier model + SUM/MAX note + the 16 weekly / 19 lifetime lists + `afd_achievements_v2`) + the Architecture Map Achievements/Constants rows; **Part A of `PLANNED-FEATURES-v3.md` is flipped to ✅ SHIPPED**. The tow-cap progression + chain retune (P6/B-8) spec lives in GDD §2.10 + **§2.10.2** + §3.4 (revalidated stability envelope). Starfield spec lives in GDD §2.11 (folded into the existing world/camera section, not a new one); the wave-clear gate + `TARGETS` HUD spec lives in GDD §2.7; the powerup-expiry modes + Difficulty screen (P5) spec lives in GDD §2.14 (powerups) + §2.16 (menu/persistence); the Architecture Map Constants/game-object/Flow/Menu/draw() rows were updated for all. See `PLANNED-FEATURES-v3.md` / `IMPLEMENTATION-PHASES.md` for the rest of the v3.0 phase plan (Phase 9 next).

## Working / verified

**Headless-verified this session (v3.2 P3, `scratchpad/test-v31-coalesce.js` 61→80 assertions + `scratchpad/test-f9.js` 277→283):** drives the REAL `update`/`destroyHunter`/`coalesceGarbage`/`evaluate`/`Garbage` code (no reimplementation); `destroyHunter` + emission/score constants added to the coalesce harness's returned-symbol list.
- **Salvage is permanent** — a lone canister survives a ~120 s `update()` run and is still alive (the old build killed it at `GARBAGE_DECAY` = 12 s); `garbageDecayed` is gone from `game.stats`.
- **The economy closes (FORK-B/B1)** — a `bornOfScrap` core killed through its FULL 13-member lineage emits **0** garbage at every tier, while awarding the **same** score (2900) as an identical normal lineage and still dropping its small-tier powerup; a **timer-spawned** lineage still emits the full **12 normal + 54 low = 66**; the `bornOfScrap` flag is present on all 3 medium children and all 3 small grandchildren (survives both split generations).
- **Waste Not, repurposed** — `hunterCoalesced` increments exactly once per 12-piece transform (the coalesced core is tagged `bornOfScrap`); Waste Not unlocks on a finished game with `hunterCoalesced === 0` and does **not** unlock after one Hunter is born from scrap.
- **Constants + rotation** — `GARBAGE_COALESCE_DELAY === 6.0`, `GARBAGE_MAGNET_RANGE === 260` asserted; the 1-s-gate test (section 1) reworked to advance past the 6 s delay via the constant; the weekly pool is still **length 16** with every `poolIndex` slice assertion in `test-f9` **unchanged**.
- **Shatter unchanged** — the P2 shatter path still yields exactly `pieces` hookable singles and doesn't coalesce anything; `test-f3` adjusted for the dropped ctor decay param (mass is the 5th arg; a sever-decay assertion became a "no `decay` field" check).
- Plus the retained v3.2 P2 + P1 + v3.1 P3 coverage (shatter, un-hookable clumps, mass-sum/derived-radius, momentum merge + attraction, cluster render, the delay gate, 12→Hunter, wrap-seam merge) all still green under permanent garbage.

**Headless-verified prior session (v3.2 P2, `scratchpad/test-v31-coalesce.js` extended 48→61 assertions):** drives the REAL bullet-collision loop / `update()` / `shatterClump` / `Garbage` code (no reimplementation); `Bullet` and `shatterClump` added to the test harness's returned-symbol list.
- **Shatter yields exactly `pieces` hookable singles** — one player bullet on a `pieces=7`, mass-3.5 clump destroys it and emits exactly 7 fresh `Garbage`, every one `pieces===1`, every one carrying a **full, re-armed `coalesceDelay`**, every one at `mass === clumpMass/7` (0.5), with **total emitted mass conserving the clump's mass** exactly.
- **The delay gate holds** — the 7 freshly-shattered pieces do NOT merge back together on the very next `update()` frame (driven through the real `coalesceGarbage` inside `update()`).
- **Only clumps are targets** — a player bullet passes straight **through** a `pieces=1` canister (neither dies), exactly as before; a hostile (saucer) bullet passes straight through a `pieces=6` clump (clump survives) — the hostile branch is untouched.
- **The P1 gate now lets shattered pieces through** — shattering a `pieces=3` clump sitting on the ship, then driving one further real `update()` frame, hooks **all 3** resulting singles onto the chain.
- **Waste Not stays safe** — shattering does not touch `game.stats.garbageDecayed`.
- Plus the retained v3.2 P1 + v3.1 P3 coverage (mass-sum/derived-radius, momentum-conserving merge + attraction, un-hookable clumps, cluster render, the 1 s gate, 12→Hunter, wrap-seam merge) all still green.

**Headless-verified prior session (v3.2 P1, `scratchpad/test-v31-coalesce.js` extended 29→48 assertions):** drives the REAL `coalesceGarbage`/`update`/`Garbage`/`destroyDebris` code (no reimplementation).
- **Mass sums, radius derives** — mass sums across a chain of 4 real merges (1+1+0.5+0.5 → 3.0), `pieces` → 4, `radius` = `7*sqrt(4)` = 14; a lone piece still reads `radius` 7.
- **Momentum-conserving merge** — a 2-piece merge's survivor velocity is the momentum sum (NOT the old literal vector sum, asserted both ways); a heavy anchor (mass 10, at rest) absorbing a fast light single (mass 1, 100 px/s) ends at exactly `100/11 ≈ 9.09` px/s — it barely speeds up. Preserved across the wrap seam.
- **Mass-weighted attraction reduces exactly** — two mass-1.0 singles get a per-frame kick bit-identical to the shipped `GARBAGE_MAGNET_PULL·dt`, still exactly equal-and-opposite (regression guard on the reduction, so the constant needs no retune).
- **Un-hookable clump** — a `pieces>1` clump sitting on the ship is NOT hooked (chain stays empty, clump alive) and the Magnet powerup does not pull it (velocity unchanged), driven through the real `update()`; a `pieces=1` canister still hooks as one mass-1.0 node and is still pulled toward the ship.
- **Cluster render** — `draw()` is crash-free at `pieces=1` and `pieces=11`.
- Plus the retained v3.1 P3 coverage (1 s gate on attraction+merge, 12→one Hunter with `hunterborn` once, wrap-seam merge, Waste-Not-safe, emission-site/`fromNode` inheritance) all still green under the new physics.

**Headless-verified prior session (v3.1 P3, `scratchpad/test-v31-coalesce.js`, 29 assertions):** drives the REAL `coalesceGarbage`/`update`/`destroyDebris`/`Garbage` code (no reimplementation).
- **Config + inheritance** — `GARBAGE_COALESCE_DELAY`=1.0, `GARBAGE_MERGE_DIST`=12, `HUNTER_COALESCE_COUNT`=12, magnet range (140) > merge dist; a fresh `Garbage` starts `pieces=1` + full `coalesceDelay`; `Garbage.fromNode` and real `destroyDebris`-emitted canisters inherit both defaults.
- **1 s gate on BOTH attraction and merge, via the real `update()` countdown** — two contacting pieces do NOT merge while fresh, still don't at 0.6 s elapsed, and DO merge once past 1 s; two in-range-but-not-contacting pieces feel NO pull while fresh and an equal-and-opposite pull once active.
- **Merge is a literal vector sum** ⚠️ *(superseded by v3.2 P1 — now the momentum sum; the assertion was rewritten)* — two active pieces in contact fuse: the earlier piece survives, `pieces==2`.
- **Twelve → exactly one Hunter** — twelve co-located active pieces coalesce in one pass to a single new large (size-3) Hunter, all twelve marked dead, `AudioSys.hunterborn` fired **exactly once**.
- **Wrap-aware** — two pieces straddling the world x-seam (8 px apart across the wrap, huge naive distance) merge, sum-velocity preserved.
- **Waste Not safe** — neither a plain merge nor a 12→Hunter transform bumps `game.stats.garbageDecayed`.
- **FORK-2 pickup** ⚠️ *(REVERSED by v3.2 P1 — clumps are now un-hookable; the test now asserts the opposite)* — under v3.1 a `pieces=5` clump hooked as one mass-1.0 node.

**Headless-verified prior session (v3.0 P8, `scratchpad/test-f9.js`, 277 assertions — extends the P7 suite):** drives the REAL `evaluate`/`deriveLifetime`/`nextWave`/`killShip`/dock-offload/`destroyDebris`/`init`/`save` code (no reimplementation).
- **Config (P8)** — WEEKLY **16** / LIFETIME **19**; **14 tiered** (the P7 seven + the P8 seven: sharpshooter, salvage_king, field_sweeper, freight_baron, daredevil, zen_master, wave_rider); all 7 new lifetime counters exist (`smallSaucerKills`/`bestDeliveredGame`/`bestDebrisGame`/`heavyHaulerEvents`/`pacifistTowEvents`/`glassCannonGames`/`shieldSurferGames`); the `flawlessLateWave` per-game stat + the `flawless_run` weekly exist; every tiered ladder still 6 strictly-ascending steps with no single-goal.
- **Weekly rotation recomputed for 16** — `poolIndex(2026,2)===10` (slice `[combo_collector,speed_recycler,powered_up,diamond_cutter,waste_not]`), `poolIndex(2026,6)===14` (wrapping slice `[waste_not,flawless_run,scrap_runner,heavy_hauler,glass_cannon]`); deterministic + the ISO-week year-boundary case still green.
- **Per-tier boundaries, all 7 new ladders** — `threshold-1` stays on the prior tier and the exact threshold advances exactly one, for sharpshooter/freight_baron/zen_master/daredevil/wave_rider (SUM) + salvage_king/field_sweeper (MAX).
- **Event-cadence hooks driven through the REAL dock offload / game over** — Freight Baron counts a ≥12 visit **exactly once** (not per canister past 12), a second visit → 2, an 11-canister visit doesn't count; Zen Master counts a 5-streak haul **exactly once**, and a fresh 5-streak after a reset counts again → 2; Daredevil + Wave Rider tally once at real `killShip` from a wave-6/no-Health/10-deflect game, and a Health-picked / under-10-deflect game tallies **neither**.
- **MAX-via-real-events** — Field Sweeper tracks best Debris/game through 15 real `destroyDebris` kills; Salvage King tracks best delivered/game (15 in one visit, 20 across a run) through the real dock path.
- **Flawless Run** — a damage-free **wave-7** clear does NOT arm it (floor is 8); a damage-free **wave-8** clear (driven through the real wave-clear loop) sets `flawlessLateWave` and unlocks the weekly.
- **Viewer** — `drawAchievements` renders the widened 3-column mixed tiered/single-goal layout (19 lifetime across two columns) crash-free, both a mixed-tier state and an all-pre-bronze state.
- **Prior session (v3.0 P7):** drives the REAL `evaluate`/`deriveLifetime`/`nextWave`/`init`/`save` code (no reimplementation).
- **Config/tier structure** — exactly **7 tiered** lifetime achievements (the P7 named set), every ladder is 6 strictly-ascending steps and carries **no** single-goal; `TIER_NAMES`/`TIER_COLOR` are length 6; `maxWaveNoPowerup` exists; `STORAGE_KEY === "afd_achievements_v2"`; The Long Haul is non-tiered goal-10 with a `12+` desc.
- **Per-tier boundaries (all 7 ladders)** — for every threshold, `threshold-1` leaves you on the prior tier and the exact threshold advances **exactly one** tier (also exercises multi-tier catch-up in one pass). An explicit **cross-bronze-not-silver** fires exactly one Bronze toast; a **10,000-delivered jump crosses bronze+silver+gold in one pass** → **three** ascending, tier-named toasts.
- **SUM vs MAX, through the real `nextWave`** — `maxWaveNoPowerup` rises to 15 on a powerup-free `nextWave` (→ No Powerups Needed Gold), then is **frozen at 5** when a powerup is picked before a deeper wave (only Silver); Master of the Field + Century Club coexist on `maxWave` (25 → Master Gold + Century Club).
- **Viewer text** — tiered status reads `"Gold · 12,431 → 25,000"`, `"Diamond ✓ MAX"` at the ceiling, `"— · 0 → 1,000"` pre-bronze; non-tiered rows still read `cur/goal` and the play-time `1.0/10h`.
- **Persistence v2 + migration** — a v2 save round-trips counters **and** per-id tier indices; a stale-low stored tier is **advanced** (never lowered) by `deriveLifetime`; a `_v1`-only save **migrates** (keeps raw counters, re-derives tiers — 26k delivered → Titanium, 60 Hunters → Bronze — ignores old flat unlock sets) and seeds **silently** (zero toasts); a corrupt v2 payload doesn't crash `init`.
- **Regression within F9** — the ISO-week math + deterministic 15-pool rotation, weekly week-reset/lifetime-retained persistence, and every C1–C6 real-gameplay unlock (debris/deliveries/close-shave/powerups/lineage/perfect-wave) + the toast lifecycle all stay green.

**Headless-verified prior session (v3.0 P6, `scratchpad/test-p6.js`, 37 assertions):** drives the REAL dock-offload path, `Ship.update`, and `updateChain` (no reimplementation).
- **Config** — `CARGO_BASE`=12, `CARGO_CAP_MAX`=20, `CARGO_GROW_PER`=30 (positive), `CHAIN_ITER`≥3; retuned `CARGO_THRUST`=0.06 / `CARGO_MAXSPD`=0.03 / `CARGO_MASS`=0.07; the old `CHAIN_MAX` constant is gone from the source.
- **Cap growth is real** — driving actual dock deliveries through `update()`: `cargoMax` starts at 12, stays 12 at 29 delivered, hits 13 at exactly 30, 14 at 60, 20 (ceiling) at 240, and **never exceeds** `CARGO_CAP_MAX` across 250 deliveries (exactly 8 increases, one per threshold); each bump pushes a "TOW +1" float; `startGame` resets it to 12.
- **Pickup gate + HUD read `cargoMax`** — at `cargoMax`=12 a 13th field canister is refused; raising `cargoMax` to 16 lets the chain grow past 12; `draw()` is crash-free with a 20-node chain / cargoMax-20 HUD.
- **Physics retune, measured through the real `Ship.update`** — a full chain at m=20 gives thrustMul ≈ 0.4545 (45%) and top-speed ratio ≈ 0.625 (63%), matching both the formula and the old full-12 feel; a base-12 chain is measurably lighter than the old-12 on both; Engine at 20 nodes ≡ plain 10 nodes (FLAG B-8-a); the momentum-tug massFactor uses the 0.07 coeff (0.84 at m=12 uncapped, capped 1.4 at m=20), backed out from the real `updateChain` tug impulse.
- **Constraint stability at `CARGO_CAP_MAX` (20) nodes** — 900 frames of hard thrust-flips across a wrap: no NaN, no node-velocity explosion, worst-case link stretch < one link length (≈4 px), no nodes lost/duplicated. (Iteration-count rationale reproduced in `scratchpad/diag-chain-iter.js`: 3→4→5 iters give 4.36→3.70→3.24 px worst stretch; 4 chosen.)

**Headless-verified prior session (v3.0 P5, `scratchpad/test-p5.js`, 57 assertions):** drives the REAL fire block, magnet/hook pass, menu state machine, and persistence (no reimplementation).
- **Config + defaults** — both modes default `"time"`; `MENU_OPTIONS` has a `"Difficulty"` row sitting immediately before `"Back"`; `POWERUP_BUDGET` maps rapid/triple/magnet to `RAPID_SHOTS`/`TRIPLE_SHOTS`/`MAGNET_PIECES` (all positive).
- **Mode routing** — `powerMode` sends rapid/triple to `shotPowerupMode`, magnet to `magnetMode`, and Engine to `"time"` **always** (even with count modes set — Engine isn't configurable).
- **Default time = shipped behaviour, exactly** — in time mode `applyPowerup` sets the timer (not the budget); firing does **not** touch `powerBudget`; rapid cadence still applies via the timer; a magnet hook spends **no** budget (and still hooks). Engine always uses the timer.
- **`"shots"` mode ends Rapid at EXACTLY `RAPID_SHOTS` pulls** — one pull spends one; still active with 1 left; inactive the pull that hits 0; the last rapid pull still fired at `RAPID_FIRE_COOLDOWN` and the first post-expiry pull returns to `FIRE_COOLDOWN`.
- **A Triple 3-fan is ONE pull** — one pull fires 3 bullets and spends exactly one `triple` shot.
- **Rapid + Triple budgets are independent** — driving `TRIPLE_SHOTS` pulls exhausts Triple while Rapid is left at exactly `RAPID_SHOTS − TRIPLE_SHOTS` and still active; with Triple gone a pull fires a single bullet at rapid cadence; Rapid then ends on its own budget.
- **`"pieces"` mode ends Magnet at EXACTLY `MAGNET_PIECES` hooks** — one hooked canister spends exactly one piece (B-5-a: a draw-then-hook doesn't double-spend); active with 1 left; inactive at 0; no further spend once inactive.
- **Same-type refresh restores the budget to full, never stacks** — a partially-spent Rapid/Magnet budget returns to full on re-pickup; a second refresh does not exceed full.
- **Difficulty screen** — reachable Options → Difficulty; ◄/► on each row selects time/count; down walks shot → magnet → Back; Back (and the `back` action) return to Options with the cursor on Difficulty; menu toggles persist and reload; a direct `saveSettings`/`loadSettings` round-trips; an **old save missing the mode keys** loads clean (defaults kept); a **bogus** mode value is ignored while a valid sibling still loads.
- **Draw crash-free** — the Difficulty screen (both toggle states), the Options screen with the new row, and the **count-mode HUD** (active Rapid/Triple/Magnet numeric bars + Engine timed bar) all render without throwing.

**Headless-verified prior session (v3.0 P4, `scratchpad/test-p4.js`, 38 assertions):** drives the REAL keydown/gamepad handlers + menu state machine (no reimplementation).
- **Corrected bindings** — `bindings.pause.keys` is `["escape"]` with `"p"` retired; `back` shares ESC; Start/B/A pad + Enter/A confirm unchanged.
- **Controller Start = session toggle** — Start on title → starts a game; Start while playing → opens the pause root (`MENU_ROOT_PLAY`); Start while paused → dismisses & resumes.
- **"O" (kbd) + B (pad) open the system menu** from both title and gameover, root = `MENU_ROOT_SYS` (`["Options","Achievements","Back"]`); **Back closes the overlay** back to the underlying title/gameover screen (state preserved). Opening the menu does not also start a game.
- **ESC pauses outside a menu, backs inside** — ESC while playing opens the pause root; ESC in Options backs to root, ESC at root resumes; ESC on the title with no menu is inert (O/B are the openers).
- **A/Enter still start a game** from title/gameover.
- **FLAG P4-b holds three ways** — Enter inside the system menu navigates and never restarts; a same-frame pad **B+A** opens the menu without also starting (else-if guard); A while the system menu is open confirms in-menu, no start.
- **No leak** — nav keys pressed while a menu is open are never written to `keys{}`, no menu keypress starts a game, and `update()` stays frozen while paused (a held gameplay key can't rotate the ship).
- **Achievements dual-entry backs correctly** — reached from the system root it backs to root (cursor on Achievements, `achReturn="root"`); reached via Options it backs to Options (`achReturn="options"`).

**Headless-verified prior session (v3.0 P3, `scratchpad/test-targets.js`, 12 assertions):**
- **Identity holds** — `targetsLeft` (the HUD's displayed number) always equals `game.debris.length + game.hunters.length`; wave-1 debris count matches the real `min(3+wave,9)` spawn rule, and no Hunters exist on wave 1 (FLAG B-7-b).
- **Debris split raises the count for real** — driving the actual `destroyDebris` on one large (size 3) debris + the real cleanup filter takes the count 1 → 3 (a +2 jump, matching Paul's 3→5 example at lineage scale).
- **Hunter spawn raises the count for real** — forcing `game.hunterTimer` to 0 on wave 2 and driving one real `update()` frame spawns exactly one Hunter core via the actual timer path and the count rises from 0.
- **Reads exactly 0 at the literal wave-clear gate** — `targetsLeft() === 0` when both arrays are empty, matching `game.debris.length === 0 && game.hunters.length === 0` verbatim.
- **`draw()` crash-free** with the new HUD line in both an empty-targets state and a populated one (debris + a spawned Hunter core).

**Headless-verified prior session (v3.0 P2, `scratchpad/test-firerate.js`, 12 assertions):**
- **Constants correct** — `FIRE_COOLDOWN` is 0.20, `RAPID_FIRE_COOLDOWN` is 0.09, and rapid < base.
- **Post-shot cooldown is regime-correct** — driving the real fire block one frame with Rapid inactive sets `ship.cooldown` to exactly `FIRE_COOLDOWN`; with `game.powerFx.rapid > 0` it sets exactly `RAPID_FIRE_COOLDOWN`.
- **Cadence relationship holds over real gameplay frames** — clearing `game.bullets` every frame (so the Rapid/Triple bullet-cap can't mask the cooldown difference) and counting fire events over 1 simulated second via the real `update()`/`Ship.update` fire block: base ≈5 volleys/s (matches `1/0.20`), Rapid ≈10 volleys/s (matches `1/0.09`), Triple-alone stays at ≈5 volleys/s (Triple doesn't touch cooldown, confirming it doesn't accidentally speed up cadence).
- **No crash** running 60 frames of `update()`+`draw()` with Rapid active.

**Headless-verified prior session (v3.0 P1, `scratchpad/test-starfield.js`, 17 assertions):**
- **draw() crash-free** with the two-layer starfield wired in, in both title and playing states.
- **Both layers exist and are correctly proportioned** — `starsNear[]` is populated and sparser than the original `stars[]`.
- **Per-star brightness present and varying** — every far-layer star's `a` falls in `[STAR_BRIGHT_MIN, STAR_BRIGHT_MAX]`, every near-layer star's `a` falls in `[STAR_NEAR_BRIGHT_MIN, STAR_NEAR_BRIGHT_MAX]`, and brightness actually varies across stars (not a uniform fill).
- **Near layer is screen-space tiled, not world-fixed** — shifting the camera by exactly one tile width (`VIEW_W / STAR_PARALLAX_FACTOR`) reproduces an identical set of rendered near-star screen positions (clean periodic tiling, no seam drift); a smaller, non-tile-aligned camera shift visibly moves the pattern (parallax is actually applied, not a static overlay).
- **Far layer unchanged as the world-fixed reference** — the same fractional camera shift moves far-layer stars by the expected 1:1 screen-space delta.
- **Purely cosmetic** — driving `update()` with the ship moving and wrapping across the seam changes no score, hull HP, or entity counts.

**Headless-verified prior session (v2.1, `scratchpad/test-boundary.js`, 20 assertions):**
- **draw() crash-free** with the boundary wired in, in both the title and playing states (recording-ctx harness).
- **Correct primitive** — `drawWorldBoundary` sets `setLineDash([BOUNDARY_DASH, BOUNDARY_GAP])`, strokes a rect sized exactly `WORLD_W×WORLD_H`, then resets the dash to `[]`.
- **Seam visibility from either side + continuity across the wrap** — near world x≈0 exactly one vertical seam is on-screen; near x≈WORLD_W (the *same* seam approached from the far side) still exactly one; the deep interior shows none; and the on-screen seam position sits at ~screen-center and doesn't jump as the camera crosses x=0 (verified via the rect corner's `wrapOffset`). Same three checks pass for the horizontal (y) seam.
- **Z-order** — the line-dash set happens *after* the camera `translate` in `draw()` (drawn inside the transform, beneath entities).
- **Purely cosmetic** — driving the real `update()` with the ship crossing and wrapping over the x=0 seam changes no score, no hull HP, and spawns/destroys no entities; the ship wraps into bounds normally.

**Needs a real-browser playtest (cosmetic — can't be judged headlessly):** does the dashed line **read clearly** at the seam as you approach and cross it? Is the `COLOR.dim` tone + low glow **unobtrusive** (a boundary, not a hazard), or too bright/distracting against a busy field? Are the dash length/gap (`BOUNDARY_DASH` 16 / `BOUNDARY_GAP` 24) a comfortable density at the 1280×720 viewport scale? See the new playtest ask below.

**Headless-verified prior session (v2.0, `scratchpad/test-f9.js`, 62 assertions):**
- **ISO-week math** — `isoWeekYear()` returns the right Monday-based/UTC week + week-year, including a year-boundary case (2027-01-01 → **ISO 2026-W53**, a 53-week year because 2026 starts on a Thursday).
- **Deterministic weekly rotation** — `poolIndex(2026,2)===9`; the week's 5-slice is the expected `[no_scratches, combo_collector, speed_recycler, powered_up, diamond_cutter]`; a wrapping index (`poolIndex(2026,6)===13`) correctly wraps to `[diamond_cutter, waste_not, scrap_runner, heavy_hauler, glass_cannon]`; selection is identical for a fixed date (no randomness).
- **Persistence** — weekly unlocks **reset** when the calendar week rolls over (stored week key ≠ current), while **lifetime progress + lifetime unlocks are retained**; a same-week weekly unlock round-trips through `localStorage`. (Serialize/deserialize only — real cross-session persistence still needs a browser; see playtest asks.)
- **Real-driven gameplay unlocks at exact thresholds** — 14 vs 15 debris kills (Satellite Buster); a real dock-offload run delivering 12 then 8 more (Heavy Hauler / Combo Collector / Speed Recycler / The Long Haul lifetime / Scrap Runner at 20); a real Hunter collision leaving 5 HP (Close Shave + below-half + lifetime hitsSurvived); all-four powerups (Powered Up) with Health disqualifying Glass Cannon and any powerup disqualifying No Powerups Needed; a full 13-kill Hunter lineage (Hunter's Bane) and 3 large cores (Diamond Cutter); a damage-free wave-3 completion driven through the real wave-clear loop (No Scratches + the 10th Perfect Wave); every lifetime counter at its just-below/at-goal boundary; and the play-time progress label formatting ("1.0/10h").
- **Toast lifecycle** — an unlock pushes a named toast; `updateToasts` ages it out.
- **`draw()` runtime-exercised** on all new paths (title with the new hint line, playing + toast, the updated Options menu with the Achievements row, the Achievements viewer with mixed locked/unlocked rows, game-over + toast) — no throw.

**Regression re-run green this session:** `test-f2.js` (54), `test-f3.js` (28), `test-f4.js` (30), `test-f5.js` (47), `test-f6.js` (39), `test-f7.js` (52), `test-f8.js` (40), `test-f9.js` (277), `test-boundary.js` (20), `test-starfield.js` (17), `test-firerate.js` (12), `test-targets.js` (12), `test-p4.js` (38), `test-p5.js` (57), `test-p6.js` (37), `test-v31-world.js` (12), `test-v31-wrap.js` (14), **`test-v31-coalesce.js` (29 — new this session)** — all pass (**815 assertions across 18 files**). `node --check` clean on the extracted script.

**✅ RESOLVED this session: the pre-existing `test-f3.js` chain-physics flake.** Its 5 `chainMass` "D" assertions used to fail on some runs with values exactly *halved* (`chainMass of 8×1.0 = 8` → got 4, etc.) — the `ENGINE_MASS_MULT` (0.5) signature: a prior `update()` in the test would randomly drop + apply an Engine powerup, leaving `game.powerFx.engine > 0` when the mass block ran. As flagged, the fix belonged to a chain/powerup-owning session (this one): **`test-f3`'s `resetShip` now zeroes `game.powerFx`**, so every (D) scenario starts powerup-free. Confirmed **10/10 green** after the fix (was ~4/6 failing before). Not a code change to the game — a test-harness fix only.

**The full v2.0 feature set, all headless-verified in its own phase and confirmed still green:**
- v1.2 (F1) larger 3840×2160 scrolling world + camera · v1.3 (F2) HP pool + knockback + hit-stun · v1.4 (F3/F5) Debris Satellites + variable-mass garbage · v1.5 (F10) difficulty ramp + saucer calming · v1.6 (F4) Hunter Satellite redesign · v1.7 (F6) five powerups · v1.8 (F7) gamepad support · v1.9 (F8) pause/options menu + control rebinding · **v2.0 (F9) achievements**.

## Known issues

- **Real-`localStorage` persistence is unverified across the board (v1.9 + v2.0 + v3.0 P7/P8).** Both the settings key (`afd_settings_v1`) and the achievements key (**`afd_achievements_v2`** — bumped in P7 from `_v1`, **unchanged in P8** since the seven new counters ride the existing known-key load/save) round-trip headlessly against a stub, but real cross-session persistence can only be confirmed in a browser running the file locally (the claude.ai artifact sandbox has no `localStorage`). All access is `typeof`-guarded + try/caught, so the sandbox worst case is "progress just doesn't persist," never a crash. **The P7 `_v1→_v2` migration (keep counters, re-derive tier badges, drop old unlock sets — FLAG A-2-a) is best-effort and browser-unverified: a returning v2.1 player's lifetime counters should survive and tiers recompute, but their current-week weekly unlocks may reset once.** **Top persistence playtest item.**
- **A few starter achievements are intentionally hard or degenerate (v2.0), by design of the starter pool — not bugs.** *Waste Not* ("zero canisters expired in a game") is near-impossible to earn legitimately (a lineage sheds 39–66 canisters and the chain caps at 12), yet is trivially satisfiable by dying before anything decays — it's implemented literally per F9's list. Curating/balancing the pools (retiring degenerate ones, tuning thresholds) is a **post-playtest pass**, deliberately not done this phase.
- **Weekly achievements reset each calendar week (v2.0), by design.** Unlocking a weekly one records it only for the current ISO week; when the week rolls over the 5 change and weekly-unlock progress starts fresh (lifetime never resets). This is the intended "weekly challenge" model — flagged so a mid-week reset (which won't happen — the week only changes on Monday UTC) isn't mistaken for data loss.
- ~~**Achievements viewer is reachable only from Pause → Options (v2.0).**~~ **RESOLVED in v3.0 P4.** The Options/Achievements system menu now opens directly from the title/gameover screen (keyboard "O", controller B); its root offers Achievements as a first-class entry. The old "start a game to browse achievements" limitation (and the parallel F8 "menu reachable only via pause" note) is closed.
- **Gamepad rebind drops that action's analog axis (v1.9), by design.** Capturing reads a button, not a stick direction; Return to Defaults restores the axis.
- **Audio unlock on a controller-only start is browser-dependent (v1.8).** Best-effort `AudioSys.init()`/`resume()` on Start/A, but the Web Audio autoplay policy generally wants a keyboard/pointer gesture — a controller-only player may hear nothing (incl. the new achievement fanfare + menu blips) until one keyboard press.
- **Soft weapon cap (v1.7), by design:** a Triple volley can transiently exceed the 12-bullet cap by up to 2.
- **Performance watch (v1.4/v1.6; sharper as of v3.2 P3):** heavy `shadowBlur`; garbage-dense late waves are the stress case (a cleared *timer-spawned* Hunter lineage sheds 66 canisters; a scrap-born one sheds 0). **v3.2 P3 removed the decay timer that used to bound coexistence — garbage is permanent now**, so late waves can carry more standing salvage; render is viewport-culled (draw cost is bounded), and the O(n²) `coalesceGarbage` pass is the watch item (FLAG D-2). Levers: a global glow toggle, then the drop-volume constants (`DEBRIS_GARBAGE`/`HUNTER_SMALL_GARBAGE`), then (only if a stress test shows it) a spatial hash for coalescence. The v2.0 achievement layer adds negligible per-frame cost (≈17 simple `cur()` compares, already-unlocked short-circuit).

## Balance notes

- **World shrink (v3.1 P1) — a first-pass size, browser feel unverified.** `WORLD_W/H` 3840×2160→2560×1440 (~44% of the old area) is meant to cut the dead-space feel of the old 9× world; the ship-relative spawn/dock rings were clamped to match so nothing is unreachable. Open question is purely feel: does the smaller world read as "denser, more populated" without feeling cramped or making the wrap seam come around too often during normal play? `WORLD_W/H` is the single knob if it needs another pass (re-derive the spawn-ring clamp and re-soften if changed again — see the note in "Notes for a future session").

- **Tow-cap progression + chain retune (v3.0 P6) — first-pass numbers, browser feel unverified.** `CARGO_BASE` (12), `CARGO_CAP_MAX` (20), `CARGO_GROW_PER` (30 delivered per +1) and the retuned `CARGO_THRUST` (0.06) / `CARGO_MAXSPD` (0.03) / `CARGO_MASS` (0.07) / `CHAIN_ITER` (4) are all single-knob tunables in the salvage/chain block. The retune math is exact (full 20-chain reproduces the old full-12 ratios), and stability is headless-solid, but three things are **feel/perf, browser-only:** (1) does a full max chain feel *heavy but drivable* (not broken) — and does the now-lighter base-12 chain feel *too* light? (2) does the cap-growth cadence feel *earned* — is +1 per 30 delivered (≈240 delivered to reach 20, roughly 8–16 waves of active hauling) too slow or about right? (3) any whip/perf issues (`shadowBlur`, FLAG B-8-c) in dense late waves at 20 nodes? `CARGO_GROW_PER` is the cadence lever; the three coefficients are the feel levers; a global glow toggle is the perf lever (decay is gone as of v3.2 P3). See the new playtest ask below.
- **Powerup expiry count budgets (v3.0 P5) — first-pass numbers, no playtest feedback yet, and OFF by default.** `RAPID_SHOTS` (40), `TRIPLE_SHOTS` (30), `MAGNET_PIECES` (20) only bite when the player opts into `"shots"`/`"pieces"` on the Difficulty screen; the default `"time"` path is untouched (still `POWERUP_DURATION` = 15 s). These three are single-knob tunables in the Powerups block. Open questions are all *feel*, browser-only: do 40 rapid / 30 triple pulls and 20 magnet hooks feel like a fair "use it or lose it" trade vs. the 15 s clock; is the Rapid+Triple *independent* decrement legible (Triple running out mid-fight while Rapid keeps single shots going); and is the count HUD (numeric label beside the bar) readable at a glance. Note the cross-mode interaction with the P2 fire-rate work: rapid cadence keys off `powerActive("rapid")`, so in shots mode the fast cadence lasts exactly `RAPID_SHOTS` pulls.
- **TARGETS HUD (v3.0 P3) — no balance knobs, a pure readout.** No new tuning constants; it's two existing array lengths (`game.debris.length + game.hunters.length`) read into the HUD. The one open question is presentational, not numeric — whether the single `TARGETS n` count (chosen) reads clearly when it jumps up on a Hunter arrival, vs. the two-part `DEBRIS n · HUNTERS m` fallback named in `PLANNED-FEATURES-v3.md` B-7 (see playtest ask below).
- **Fire-rate rebalance (v3.0 P2) — first-pass numbers, no playtest feedback yet.** `FIRE_COOLDOWN` 0.16→0.20 (base gun ~6.25→5 shots/sec) and new `RAPID_FIRE_COOLDOWN` 0.09 (~11 shots/sec) are single-knob tunables in the weapons block. **Rapid+Triple is now a strictly stronger combo than either alone** (fast cadence + wide spread, FLAG B-6-a) — previously Rapid only raised the cap, so this is a real balance shift, not just a bug fix. Wants a playtest read on whether the slower base feels sluggish and whether Rapid+Triple needs a cap/cooldown interaction to stay in check.
- **Starfield (v3.0 P1) — five first-pass tunables, no playtest feedback yet.** `STAR_DENSITY` (40→80), `STAR_PARALLAX_FACTOR` (0.5), `STAR_BRIGHT_MIN/MAX` (0.35–1.0), `STAR_NEAR_DENSITY` (22), `STAR_NEAR_BRIGHT_MIN/MAX` (0.55–1.0) are all single-knob tunables in the v1.2 world block. Density/brightness/parallax strength all want a playtest read (see Playtest asks below) before calling this feature done.
- **New achievements (v3.0 P8) — 8 first-pass ladders/floors + a viewer relayout, all browser-unverified.** The seven new tiered ladders (Sharpshooter 100→**7,500**, Freight Baron/Zen Master 1→1,000, Daredevil/Wave Rider 1→**300**, Salvage King 10→200, Field Sweeper 15→400) and the Flawless Run floor (`game.wave >= 8`) are single-line tunables; the FLAG-A-8-a diamond (7,500) and FLAG-A-12-a softenings (300) are the deliberate deviations from Paul's proposed numbers. Open questions are all feel (see Playtest asks 7a–7c): are the new goals earnable-but-meaningful, is wave 8 the right Flawless floor, do the per-EVENT counters climb at a satisfying rate. Salvage King's per-game-best ladder tops at 200 — re-check against the raised tow cap (FLAG A-9-a: cap ceiling is 20, so no nudge deemed needed, but browser-confirm). The viewer relayout (widened 1200×660 3-column) is a **look-call I picked, not spec'd** — ask 7d + the "surfaced" note in Next up.
- **Tiered achievements (v3.0 P7) — the tier badge palette + the seven ladders are first-pass, all browser-unverified.** `TIER_COLOR[]` (bronze→diamond) is a pure look-call tunable — the only real question is whether the six badge tints **read as ranks and never as hazard/enemy tells** (FLAG A-1-a), especially the pale top three (gold/titanium/platinum/diamond) against a busy field. The seven ladders come straight from the §A-3 table (kept as-proposed) — they want a read on whether the low tiers land early enough to feel rewarding and the diamond ceilings (esp. **No Powerups Needed** wave-29, FLAG A-6-a) read as an aspirational flex rather than a typo. Retuning a ladder = editing its `tiers:[…]` array; recolouring = editing `TIER_COLOR[]`. **One UX feel-question for the browser:** does a big lifetime jump popping **several toasts at once** (ascending, one per crossed tier) feel *good* (a satisfying cascade) or *spammy*? If spammy, the fix is to collapse a multi-crossing into a single "reached Gold" toast — noted, not done.
- **Achievements (v2.0) — thresholds are first-pass, no balance knobs beyond the four `ACH_*` constants.** `ACH_TOAST_TIME` (4.5 s), `ACH_WEEKLY_SHOWN` (5), `ACH_LINEAGE_FULL` (13 = a full Hunter lineage), `ACH_SAVE_EVERY` (30 s periodic save). The remaining single-goal numbers came straight from F9's starter pools — they want a playtest read on whether they're *earnable but meaningful* (see playtest asks). Adding/retiring achievements is just editing the `WEEKLY`/`LIFETIME` arrays; a longer weekly rotation before repeats is "add more weekly ideas," not a structural change.
- **Carried forward, unchanged:** menu `VOL_STEP` (0.1) and `GP_DEADZONE` (0.25) (v1.9/v1.8); `POWERUP_DROP_CHANCE` (0.10), `POWERUP_HEALTH_GAP` ([18,26]), `POWERUP_DURATION` (15), weapon caps (Rapid 8 / Triple 12), `MAGNET_*`, `ENGINE_MASS_MULT` (0.5) (v1.7); the Hunter tables + `HUNTER_FLOOR_FRAC` (0.58) + shared `RAMP_WAVES` (8) (v1.6/v1.5); wave spawn `min(3+wave, 9)` (v1.4). *(`GARBAGE_DECAY`/`GARBAGE_SEVER_DECAY` removed in v3.2 P3 — garbage is permanent.)*
- **Still open: the joint F3+F10 Debris-density retune.** The one remaining v2.0 tuning lever — with every combat/utility/UI system now in, judge the *combined* early-game field feel in a browser, then lean on coalescence (`GARBAGE_MAGNET_RANGE`/`GARBAGE_COALESCE_DELAY`) and the drop-volume constants for density (decay is gone as of v3.2 P3). A post-playtest pass, not a number to guess.

## Next up

**v3.2 Phase 3 of N (salvage permanent + economy closed) is landed this session.** Active plan docs: `PLANNED-FEATURES-v3.2.md` / `IMPLEMENTATION-PHASES-v3.2.md`. **Next up:** whatever Phase 4+ scopes (v3.2 is "Phase 3 of N"). Notes carried forward for a future session:
- **Garbage is permanent now (v3.2 P3) — there is no decay.** Never re-introduce a decay timer as a density lever (GDD §2.10.1 rewrote and explains why). A canister leaves the field only by being **recycled** or **coalescing into a Hunter**. Density levers are `GARBAGE_MAGNET_RANGE` / `GARBAGE_COALESCE_DELAY` / `HUNTER_COALESCE_COUNT` (coalescence throughput) and the drop-volume constants (`DEBRIS_GARBAGE`/`HUNTER_GARBAGE`/`HUNTER_SMALL_GARBAGE`, held in reserve).
- **A coalescence-born Hunter (`bornOfScrap`) drops NO garbage — do not "fix" this.** The flag is set at the transform site in `coalesceGarbage` and propagated to split children in `destroyHunter`; the emission skip is the FORK-B/B1 sink that keeps the economy closed (12 in, 0 out). Timer-spawned Hunters keep `bornOfScrap = false` and drop the full 66. If you add a new Hunter spawn path, decide its `bornOfScrap` explicitly.
- **`GARBAGE_MAGNET_RANGE` (260) is the number most likely to be wrong (FLAG D-1)** — flagged in-code. If the field silts up with inert junk (pieces that never clump and never get collected), raise it; if clumps form too aggressively, lower it. `GARBAGE_COALESCE_DELAY` (6.0 s) is the companion knob (how long a fresh burst drifts before it can re-clump).
- **`coalesceGarbage` is still O(n²)** with permanent garbage — no spatial hash added this phase (FLAG D-2, deliberately out of scope). Render is viewport-culled so free-canister count doesn't drive draw cost. If a late-wave stress test shows the pair-test cost, a spatial hash is the follow-up.
- **Waste Not now keys on `game.stats.hunterCoalesced === 0`** (not the removed `garbageDecayed`). The id + pool slot were kept, so the weekly-rotation math and `test-f9` slices are unchanged — don't recompute them.

**v3.0 (all 8 phases) is fully shipped and closed out — "Phase 9" never existed.** v2.0 and v3.0 are both archived under `archive/` (`archive/PLANNED-FEATURES-v2.md`/`archive/IMPLEMENTATION-PHASES-v2.md`, `archive/PLANNED-FEATURES-v3.md`/`archive/IMPLEMENTATION-PHASES-v3.md`, `archive/README.md`) — current design authority is `orbital-overhaul-GDD.md` §2 + active `PLANNED-FEATURES-v3.1.md`/`IMPLEMENTATION-PHASES.md` at the repo root; build reality is this file. **v3.1 Phase 1 of N (world shrink + spawn-ring clamp) is landed this session; Phase 2 (fix the `+120` wrap-jump discontinuity) is next.** Candidate directions from here:
- **Continue the v3.1 phase order** — **Phase 2 next** (see `IMPLEMENTATION-PHASES.md` for its scope: the `updateChain` wrap jumps by `WORLD_W/H + 120` instead of exactly `WORLD_W/H`, a pre-existing ~120px seam discontinuity — not touched this phase, flagged in the P1 prompt as explicitly out of scope).
- **⚠ SURFACED THIS SESSION (a design call the P8 plan didn't scope — needs Paul's read):** growing LIFETIME 12→19 overflowed the achievements viewer's single lifetime column. Implemented a reasonable fit — widened the panel to **1200×660** and split lifetime across **two columns** (three total: weekly | lifetime | lifetime-cont.). It's crash-tested and in-idiom, but the **layout/column-split/panel-width is a look-call I picked, not a spec'd one** — confirm it reads well in a browser (or say if you'd prefer a scroll, curated display, or different split). All layout numbers are one-line tunables in `drawAchievements`.
- **Playtest the P8 achievements** (new asks below): whether the new goals feel earnable-but-meaningful, whether Flawless Run @ wave 8 is the right floor, and whether the per-event counters (Freight Baron / Zen Master) climb at a satisfying rate. Plus the P7 tier badges/colours + toast cascade and the longer-standing full v2.0 consolidated asks — especially real `localStorage` persistence (now the `_v1→_v2` migration too, still browser-unverified) and achievement threshold feedback.
- **The joint F3+F10 Debris-density retune** (the last v2.0 balance lever — see Balance notes).

Notes for a future session:
- **The tow cap is runtime now (v3.0 P6), not a constant.** Read `game.cargoMax` (never a fixed 12) at any pickup-cap or HUD site; it grows 12→20 per `CARGO_GROW_PER` deliveries and resets in `startGame`. `CHAIN_MAX` no longer exists. The chain's stability envelope is revalidated at **20 nodes / `CHAIN_ITER` (4) iters** (GDD §3.4) — if you ever lengthen the chain or link again, re-soften `CARGO_THRUST`/`CARGO_MAXSPD`/`CARGO_MASS` *together* (so a full chain at the new ceiling ≈ old full-12 feel) and re-run the `test-p6` (E) stress, don't just bump the number. **Achievements must not key "full chain" on `cargoMax`** — use a fixed ≥12 (FLAG B-8-b).
- **The two persistence keys are independent by design** — `afd_settings_v1` (F8, now also the P5 difficulty modes) and **`afd_achievements_v2`** (F9, bumped in P7). Keep them separate. The P5 modes are **additive fields** on `afd_settings_v1` (no schema bump) — always tolerate a missing/unknown value on load.
- **Achievements persistence is now `afd_achievements_v2` (v3.0 P7), shape `{lifetime, lifetimeTiers{id:idx}, lifetimeUnlocked[non-tiered], weekly{key,unlocked[]}}`.** Tiered lifetime state lives in `lifetimeTiers` (highest tier idx per id), non-tiered lifetime unlocks in `lifetimeUnlocked`. `init()` reads `_v2`; absent it, it migrates the old `_v1` **counters** only (tiers/unlocks re-derive) then ignores `_v1`. **`deriveLifetime()` runs after every load** to seed tiers/unlocks from counters *silently* (no toast) — so anything that mutates a lifetime counter outside the normal hooks must let evaluate() catch up, and any NEW tiered achievement added later auto-back-fills its badge on next load without re-toasting. **A tiered entry has `tiers:[…]` and NO `goal`; a MAX-counter one updates via `Math.max` at its event (never `+=`).** The **four** MAX counters (v3.0 P8) are `maxWave`, `maxWaveNoPowerup`, `bestDeliveredGame`, `bestDebrisGame`. **P8 also proved the auto-back-fill claim:** its seven new tiered achievements added no persistence code — the new counters ride the existing `loadCounters` known-key loop + `save` spread, and `deriveLifetime()` seeds their badges silently on load.
- **Powerup expiry is mode-aware now (v3.0 P5).** Route every "is effect X active?" read through **`powerActive(type)`** (never `game.powerFx.X > 0` directly) so time/count modes stay consistent — the fire block, `maxBullets`, `chainMass`, the Magnet pull, and the HUD all do. Count budgets live in `game.powerBudget`; the two mode settings live on the module-level persisted `settings` object. **Engine is intentionally always time-based** (not in `powerBudget`, not on the Difficulty screen). Known edge: flipping a mode mid-effect can end that effect (it starts reading the other, zeroed counter) — acceptable ("you changed the rules"). If B-4-c grows the Difficulty screen, add a `DIFFICULTY_ROWS` entry + a `menuDifficulty` switch case + a `drawDifficulty` row.
- **Achievements observe, never drive.** New achievements = add a `{id, pool, name, desc, goal|tiers, cur, text?}` entry to `WEEKLY`/`LIFETIME` and, if a new stat is needed, add the counter to the right home — a **per-game** flag/count goes in `resetGameStats()` (read via `s.` in a weekly `cur`), a **lifetime** counter goes in `Achievements.lifetime` (read via `l.`) — plus **one hook at the event's existing site**. Don't route gameplay through the module. **P8 patterns worth reusing:** a **per-EVENT latch** = increment at the moment a monotonic per-game counter passes the threshold exactly (`deliveryCount === 12`, `pacifistStreak === 5`) so it fires once per event, not per tick past it; a **once-per-game** lifetime tally = increment at `killShip` from the final per-game state (before the game-over `evaluate()`); a **MAX personal-best** = `Math.max(l.best, s.perGameCount)` at the per-game counter's own event site.
- ~~**The menu is still reachable only via pause.**~~ **Done in v3.0 P4** — the title/gameover system menu (keyboard "O", controller B) delivers the options + achievements title-screen entry point.
- **The corrected P4 control scheme supersedes the planned B-1-a mapping (Start→menu).** If revisiting the menu/input design, note that Start is now the session toggle and B/"O" are the menu openers — don't reintroduce Start→menu. FLAG P4-a resolved as "retire P"; FLAG P4-b guaranteed by the gamepad else-if chain + keyboard menu-open early return.
- **`WORLD_W`/`WORLD_H` and the spawn/dock ring maxes are coupled (v3.1 P1) — never change one without the other.** The binding limit is `min(WORLD_W, WORLD_H)/2 − 60`; `SPAWN_MAX_DIST`/`DOCK_MAX_DIST` must stay under it or a ring can wrap around and place a spawn on the far side of the ship. If the world size changes again, recompute the limit and re-clamp both `_MAX_DIST` constants (mins are independent of world size and don't need to move). `STAR_DENSITY` never needs to change for a world-size edit — `STAR_COUNT` is already area-derived.

## Playtest asks (Paul — can't be checked headlessly)

**Consolidated across Phases 1–9 (v3.0) + v3.1 + v3.2 — everything below needs a real browser (some need a controller).** Ordered roughly newest-first.

**Salvage permanent + economy closed (v3.2 P3) — NEW this session (all mechanics/counters/tests are headless-solid; everything here is feel/perf/legibility):**
9a. **`GARBAGE_COALESCE_DELAY` (6.0 s):** do same-explosion siblings visibly drift apart and *later* find strangers — or do they still snap together, or never clump at all?
9b. **`GARBAGE_MAGNET_RANGE` (260) — the number most likely to be wrong (FLAG D-1):** does the field silt up with inert junk that never clumps and never gets collected? Too low → junk accumulates; too high → clumps form too aggressively.
9c. **Does the shatter loop actually fire?** Do you find yourself *hunting* clumps to break them for a near-full cargo run, or ignoring them until they Hunter?
9d. **Does a mass-11 clump read as heavy** (momentum-conserving merge + mass-weighted pull), or just as slow and boring?
9e. **Is the cluster silhouette legible** at speed against a busy field, and does it read as *garbage* rather than *enemy*?
9f. **Late-wave perf** with permanent garbage — frame time at wave 10+, given the O(n²) coalescence pass (FLAG D-2, no spatial hash yet).
9g. **Does B1 make letting a clump transform feel like a real choice** (a scrap-born lineage is pure score, no cleanup bill), or is shooting the clump always strictly correct?

**World shrink (v3.1 P1) — NEW this session:**
8a. **Does the smaller 2560×1440 world feel right?** Fly around — does it read as a denser, more populated field than before (less "empty space between spawns"), or does it now feel cramped / does the wrap seam come around distractingly often during normal play? `WORLD_W`/`WORLD_H` is the single knob (coupled to the spawn-ring clamp — see "Notes for a future session" if it needs another pass).
8b. **Do spawn/dock placements ever feel too close or too far now?** The rings were proportionally the same but clamped down (`SPAWN_MAX_DIST` 1100→640, `DOCK_MAX_DIST` 900→620) — confirm a fresh wave and a relocated dock both still feel reachable-but-not-trivial to reach.

**New tiered/event/weekly achievements (v3.0 P8) — NEW this session (all the counter/latch/rotation/test logic is headless-solid; everything here is feel/look):**
7a. **Do the new goals feel earnable-but-meaningful?** Play through and watch the seven new tiered rows in Pause → Options → Achievements. Do the low tiers land early enough to feel rewarding, and do the ceilings read as aspirational (esp. Sharpshooter's 7,500 small-saucer diamond, and Salvage King/Field Sweeper's per-game bests)? Ladders are one-line `tiers:[…]` tunables.
7b. **Is Flawless Run @ wave 8 the right floor?** A damage-free clear of wave 8+ — too hard (never happens in a normal run), too easy, or a good "weekly-worthy" feat distinct from No Scratches (wave 3)? The floor is the literal `game.wave >= 8` in the wave-clear block.
7c. **Do the per-EVENT counters (Freight Baron / Zen Master) climb at a satisfying rate?** Freight Baron ticks once per ≥12-canister dock visit; Zen Master once per 5-in-a-row no-fire haul. Over a session, do those accumulate at a rate that makes the 1/10/50/150/400/1000 ladder feel reachable-but-long, or too slow/fast? (Daredevil/Wave Rider are once-per-game by nature, hence their softened 1/5/20/50/125/300.)
7d. **Does the widened 3-column viewer read cleanly? (ties into the surfaced design call above)** 19 lifetime rows now span two columns (weekly | lifetime | lifetime-cont.) in a 1200-wide panel. Is it legible and uncluttered at the 1280×720 scale, do the long tier-status strings ("Titanium · 12,431 → 25,000") fit without clipping the names, and is the weekly-then-two-lifetime grouping clear — or would a scroll / different split read better? All layout numbers are tunable in `drawAchievements`.

**Tiered achievements (v3.0 P7) — (the tier/persistence/test logic is headless-solid; everything here is look/feel):**
6a. **Do the tier badges + colours read as ranks, not hazards?** Open Pause → Options → Achievements and look at the seven tiered lifetime rows. Does the `TIER_COLOR[]` ramp (warm bronze → cool white-hot diamond) read as an ascending rank ladder — and, crucially, do none of the six badge tints get mistaken for a Hunter/Debris/salvage/enemy tell (FLAG A-1-a)? The pale top three (titanium/platinum/diamond) are the ones to watch; all six are one-line tunables in `TIER_COLOR[]`.
6b. **Does "progress-to-next" make sense at a glance?** Each tiered row reads e.g. "Gold · 12,431 → 25,000" (badge · current → next threshold), "Diamond ✓ MAX" at the ceiling, "— · 0 → 1,000" before bronze. Is that legible and self-explanatory next to the single-goal `cur/goal` rows in the same panel, or does the mixed layout read as cluttered?
6c. **Does a big lifetime jump popping several toasts feel good or spammy?** A large delivery/kill session can cross two or three tiers in one `evaluate()` and stack that many ascending, tier-named toasts ("… — Bronze", "… — Silver", "… — Gold"). Does the cascade feel like a satisfying reward, or is it noisy? If spammy, the documented fix is to collapse a multi-crossing into a single "reached Gold" toast.
6d. **(Persistence, ties into ask #1)** Earn a tier, close the tab, reopen — does the badge come back? And for a returning player who had the old `afd_achievements_v1` save: do their lifetime counters survive and tier badges recompute (the best-effort `_v1→_v2` migration, FLAG A-2-a)? Their current-week weekly unlocks may reset once — expected.

**Growing tow cap + chain retune (v3.0 P6) (the math + stability are headless-solid; everything here is feel/perf):**
5a. **Does a full max chain feel *heavy but drivable*, not broken?** Grow the cap toward 20 (deliver a lot) and tow a full 20-chain. It's tuned to land on the *old full-12 feel* (~45% thrust / ~63% top speed). Does that read as "a heavy but manageable haul," or does 20 canisters *still* feel broken/whippy despite the retune?
5b. **Is the now-lighter base-12 chain a problem?** Because the per-node penalty shrank, a fresh-run 12-chain is lighter than it used to be (~58% thrust / ~74% top speed / 0.84 tug vs the old 45%/63%/1.4). Does the early game feel *too* floaty when towing, or is the extra early nimbleness fine (you grow into the weight)?
5c. **Does the cap-growth cadence feel *earned*?** +1 per 30 canisters delivered (≈240 to reach 20 — roughly 8–16 waves of active hauling). Too slow (never see 20 in a normal run)? Too fast? Does the "TOW +1" float read clearly as "your cap just went up," and does `CARGO n/20` on the HUD make sense?
5d. **Any whip/perf issues at 20 nodes in dense late waves (FLAG B-8-c)?** More towed nodes + the high canister volume = more `shadowBlur`. Watch frame rate towing a long chain through a busy late wave; if it drops, a global glow toggle is the lever (decay is gone as of v3.2 P3 — see ask 9f for the permanent-garbage perf watch).

**Difficulty options / powerup expiry modes (v3.0 P5) — (default `"time"` = nothing changes (default `"time"` = nothing changes; these all need opting in on Options → Difficulty):**
4a. **Do the count budgets feel right?** Turn on **shot powerups = Shots** and play with Rapid/Triple: do 40 rapid pulls / 30 triple pulls read as a fair "use it or lose it" alternative to the 15 s clock, or too stingy / too generous? Same question for **Magnet = Pieces** at 20 canisters hooked.
4b. **Is the count HUD legible?** In a count mode the active-effect bar shows `remaining/budget` with a **numeric** count beside it (instead of a draining timer). Does the number read at a glance during play, and does a count bar sit clearly alongside a *timed* Engine bar when both are up?
4c. **Is the Difficulty screen discoverable and clear?** Pause → Options → Difficulty. Do the two "Time | Shots" / "Time | Pieces" toggles read unambiguously (◄ = time, ► = count), and does ◄/► flipping them feel right on keyboard **and** gamepad? Does it persist across a reload (same `afd_settings_v1` caveat as F8 — real persistence is browser-only)?
4d. **Rapid+Triple independent expiry** — with both on in Shots mode, when Triple's 30 runs out first you keep firing *single* shots at rapid cadence until Rapid's 40 is spent. Does that transition read as intended, or confusing?

**Pause / menu from any state + corrected controls (v3.0 P4) (needs a controller for the first two):**
3a. **Does Start-as-session-toggle feel natural on a controller?** One button that starts a game on the title, opens the pause dialog mid-game, and dismisses/resumes while paused — does that read as intuitive (the standard console idiom), or do you reach for a separate "pause" and "start"?
3b. **Do "O" (keyboard) and controller "B" read as the options/achievements entry from the title/gameover?** Is it discoverable from the on-screen hint ("OPTIONS / ACHIEVEMENTS: O (controller: B)")? Does opening the system menu, browsing Achievements/Options, and backing out feel clean — or does "B to open" fight your instinct that B = cancel?
3c. **Is ESC-vs-B "back" ever ambiguous inside a menu?** ESC and B both mean "back" once a menu is open (and ESC pauses / B opens when no menu is up). Backing out step-by-step (Achievements → Options → root → resume) — does it ever feel like a keypress did the wrong thing, or backed out one level too many/few?
3d. **Is retiring "P" for ESC-as-pause the right call (FLAG P4-a)?** P no longer pauses. If muscle memory wants P, say so and it can be re-added as a secondary playing-pause alias.

**Targets-remaining HUD (v3.0 P3):**
2a. **Does `TARGETS n` under CARGO read clearly at a glance during play?** It's a plain number, no icon — does it register as "how many things are left" without a legend?
2b. **Does the count *rising* mid-wave read as intended, or as confusing/buggy-looking?** Two ways it can happen: a large Debris splitting (jumps +2 on one kill), and — the more likely one to raise an eyebrow — a Hunter lineage sliding in on its own timer well after the Debris field looks nearly clear, ticking `TARGETS` back up from a low number. Does that read as "a new threat arrived," or does it feel like the game moved the goalposts? If it's confusing, the documented fallback is splitting the readout into **`DEBRIS n · HUNTERS m`** so the Hunter arrival reads as an addition rather than a rewind — flag which you'd prefer.
2c. **Is the position (directly under CARGO, powerup row bumped down 20px) crowding the top-left HUD**, especially with 1–2 active powerup bars also showing?

**Fire-rate rebalance (v3.0 P2):**
-1a. **Does the slower base gun (0.20 s, was 0.16 s) feel sluggish, or is it barely noticeable?** Clear waves 1–3 at the base cadence (no Rapid Fire picked up) and compare against memory/feel of the old rate. Time it if useful.
-1b. **Does Rapid Fire now feel meaningfully faster**, not just "more bullets allowed"? Pick one up and fire continuously — does the cadence bump read as intentional and satisfying?
-1c. **Is Rapid+Triple together too dominant?** It's now the strongest combo by design of this fix (fast cadence + wide spread) — flagged as FLAG B-6-a, not yet rebalanced. Does it trivialize waves/Hunters when both are active, or does the 15 s shared duration keep it in check naturally?

**Starfield legibility & parallax (v3.0 P1):**
0a. **Does the field read as brighter/denser without competing with entities?** Compare the title screen and early gameplay against your memory of the old flat, near-invisible field. Does the varied per-star brightness read as a natural starfield rather than noise? Is `STAR_DENSITY=80` a good density, or does it feel sparse/cluttered at the 1280×720 viewport?
0b. **Does the parallax layer read as depth, or as a distracting second field?** Thrust/strafe the ship and watch the near (brighter, sparser) layer drift relative to the far layer. Does the 0.5× `STAR_PARALLAX_FACTOR` make ship motion legible as "something in the world," or is it too subtle to notice / too strong and busy? Is the near layer's brightness (`STAR_NEAR_BRIGHT_MIN/MAX` 0.55–1.0) a good contrast against the far layer?
0c. **Any visible seam or tiling artifact in the near layer** as the camera moves, especially near a world-boundary wrap? The near layer is screen-space tiled (not wrap-math), so this is the one thing headless tests can't fully rule out visually — the periodicity test only confirms the *math* repeats cleanly, not that it *looks* seamless. All five constants (`STAR_DENSITY`, `STAR_PARALLAX_FACTOR`, `STAR_BRIGHT_MIN/MAX`, `STAR_NEAR_DENSITY`, `STAR_NEAR_BRIGHT_MIN/MAX`) are single-knob tunables.

**World-boundary line (v2.1):**
0. **Does the dotted boundary read clearly, and is it the right brightness?** Fly toward a world edge until the dashed rectangle scrolls into view, then cross it. Does the line clearly say "here's the wrap seam" as you approach *and* as you pass through it (it should sit at screen-center at the moment of crossing)? Is the `COLOR.dim` tone + low glow unobtrusive — reads as a boundary, not a hazard/enemy — or is it too bright/distracting over a busy field, or conversely too faint to notice? Is the dash density (`BOUNDARY_DASH` 16 / `BOUNDARY_GAP` 24) comfortable at the viewport scale, or does it want longer/shorter dashes? All four `BOUNDARY_*` constants are single-knob tunables. *(Only judgeable in a real browser — the headless test confirms the geometry/z-order/no-gameplay-effect, not the look.)*

**Achievements (v2.0/F9):**
1. **Do settings AND achievements actually persist across a real reload?** This is the top ask. Earn an achievement (e.g. deliver 20 canisters → Scrap Runner, or destroy 15 debris → Satellite Buster), change some volumes/bindings, close the tab, reopen the file. Are the unlock, the lifetime counters, and the settings all still there? Two separate `localStorage` keys are in play (`afd_settings_v1`, **`afd_achievements_v2`** as of P7) — confirm both survive, including tier badges (see ask 6d for the `_v1→_v2` migration). *(Only confirmable outside the artifact sandbox — headless tests exercise only the serialize/deserialize logic.)*
2. **Does the unlock toast read clearly and feel good?** Non-blocking gold banner top-centre, ~4.5 s, fade in/out, stacks if several land at once, plus a rising-triad chime. Is it legible over a busy field? Too long/short? Does it distract during intense play (it's drawn on top of everything, including the pause menu)?
3. **Is the Achievements viewer legible?** Pause → Options → Achievements: this week's 5 (left) + the 12 lifetime (right), each with a ✓/`cur/goal`, earned rows in gold. 17 rows in one panel — does it feel cramped, and do the progress numbers read at a glance? Would you want a first-class title-screen entry (currently only the hint is there)?
4. **Do the achievement thresholds feel earnable-but-meaningful?** The 27 goals are first-pass from the starter pools. Any that feel unreachable, trivial, or degenerate (Waste Not especially — flagged as near-impossible-yet-cheesable)? This decides which to retune/retire in a curation pass.
5. **Sanity-check the "same 5 every week" premise informally** — the active weekly set is fixed by the ISO calendar week; it should be stable all week and change on Monday (UTC). No action needed unless it visibly misbehaves.

**Menu / options / rebinding (v1.9/F8):**
6. Does menu navigation feel natural on **keyboard and gamepad** (slider granularity at `VOL_STEP` 0.1; edge-detected one-press-one-move gamepad nav vs. wanting held auto-repeat)?
7. Does a **rebound control take effect immediately** in live play? Are the two quirks acceptable — a gamepad rebind **dropping that action's stick axis**, and **no conflict detection** when rebinding to an already-used key?

**Controller (v1.8/F7):** deadzone comfort at `GP_DEADZONE` 0.25; shield on RT/RB (a gap-fill — right button?); D-Pad-vs-stick feel; whether a controller-only start produces sound or needs one keyboard press to unlock audio (known Web Audio limitation).

**Powerups (v1.7/F6):** drop rate (`POWERUP_DROP_CHANCE` 0.10) + ambient Health cadence feel *earned but not stingy*; Magnet pull reads as a sweep not a jitter; the five icons + HUD duration row read clearly.

**Hunters (v1.6/F4):** early-game calm at `HUNTER_FLOOR_FRAC` 0.58 vs. a smooth escalation; the point-blank shield-kill of a medium (spawns 3 scattering smalls) feels *fair*, not a death trap.

**Saucers / pacing (v1.5/F10):** waves 1–4 feel calm; ramp intensity reads as a schedule, not a cliff.

**Salvage / garbage (v1.4/F3 + v1.3):** the field reads as "richly optional salvage," not "carpeted in clutter"; 12 s decay is long enough to collect a lineage without feeling frantic; knockback never yanks the ship somewhere that unfairly severs the whole haul; a fresh wave spawn can't instantly sever a fresh chain too often. **And the deferred joint F3+F10 Debris-density retune** — with every system now in, this is the last major early-game balance pass; judge it once the combined field feel is confirmed.
