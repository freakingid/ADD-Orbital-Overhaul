# CS020

**CS020 P1 — the towed/incidental delivery split, landed; a 60-second level-1 park went from 5,650,000 points and a level-1 SMD to 37,500 and no SMD.**

**Prior session (CS020 P2, landed — CS020 IS COMPLETE):** implemented CS020 Phase 2 (per `IMPLEMENTATION-PHASES-CS020.md` P2, spec `PLANNED-FEATURES-CS020.md` §9), the changeset's closing phase — retune, version bump, doc sweep, no new logic — reading `CLAUDE.md`/`STATUS.md`/`PLANNED-FEATURES-CS020.md`/`IMPLEMENTATION-PHASES-CS020.md` in full and grepping every anchor in `asteroids-deluxe.html`/`ORBITAL-OVERHAUL-GDD.md` by symbol rather than reading either whole. **Gate answers were missing from this session's own setup** — STATUS.md's Playtest asks still showed the P1b gate as blocking/unanswered, and the phase prompt is explicit that P2 must retune from Paul's actual answers, not an invented interpretation — so the four gate questions were put to Paul directly before any edit, per `CLAUDE.md`'s "stop and surface" rule. All four came back "no change" (Q3 — the cap — came back "Better," which is positive feedback with no code implication either way). **(1) No retune applied**, as the standing rule requires; confirmed `DOCK_COMBO_GRACE` (4.0) and the `dockComboGrace` `DEBUG_VARS` entry's `def: DOCK_COMBO_GRACE * 1000` still agree by construction (the `def` expression derives from the constant, so they cannot disagree — nothing to assert-and-fix). **(2) `GAME_VERSION` `"1.0.0.19"` → `"1.0.0.20"`, grepped across the whole repo rather than trusting the phase doc's own file list** — found **eight** live pins beyond the HTML itself, two more than the six the CS017 P7/CS019 P2 precedent listed: the same six carried-forward files (`test-cs010-p0.js`, `test-cs013-p4.js`, `test-cs015-p7.js`, `test-cs016-p5.js`, `test-cs017-p7.js`, `test-cs018-p10.js`) plus this round's own two new files (`test-cs020-p1.js`, `test-cs020-p1b.js`), exactly as the phase prompt's own history note predicted ("CS020 has added two test files since"). All eight bumped, both the `console.log` label and the assert message string in each. `test-cs017-p7.js`'s HEADER-COMMENT narrative (line 1, "GAME_VERSION 1.0.0.17" — describing what THAT phase historically shipped) was left alone, per the CS018 P10/CS019 P2 precedent; its separate live-tracking `(H)` pin was bumped normally. **(3) The two CS020 test files' own "unchanged this phase (bumps in P2)" pins repointed to the mirror image** — `test-cs020-p1.js`'s and `test-cs020-p1b.js`'s TRAP 1 assertions now read `assert(GAME_VERSION !== "1.0.0.19", ...)` instead of `eq(GAME_VERSION, "1.0.0.19", ...)`; `test-cs020-p1b.js` had already staged a forward comment naming this exact treatment, left by the P1b session. **(4) GDD updated, shipped-behaviour only.** §2.10's "Recycling dock" paragraph rewritten around the one-effort rule: what a towed node earns vs. what an incidental pays and why it counts toward nothing, the LIFO reasoning for tagging at capture instead of snapshotting a count, the towed-hook terminator, the grace window, and the `deliveryCount ≤ cargoMax` guarantee that falls out of it. §2.10.2 gained a bullet that the payload curve is now the *hard ceiling* on the combo itself, load-bearing for the SMD and the CS018 P8 reward tiers. §2.17 gained a bullet: Maxed Out is a level-12+ achievement by construction, and Recycling Magnate / Salvage King are no longer dock-farmable. §2.19 gained the `DELIVERY` header + `dockComboGrace` knob writeup and the registry count 33→34 (eight headers now: SHIP/GARBAGE/CHAIN GUARD/DELIVERY/JUNK/UFO MOVEMENT/UFO WEAPONS/GLOBAL). The Architecture Map's **Constants** row gained `DOCK_NEIGHBORHOOD_PAD`/`DOCK_COMBO_GRACE` (the row's own CS018–CS020 history is otherwise a pre-existing gap, not touched — see below), the **game object** row gained `comboGrace`, and the `game.chain` node-shape note gained the `towed` flag. **The top-of-file Current-build line was rewritten** to CS020 P2/COMPLETE/`"1.0.0.20"`, following the CS017 P7/CS018 P10/CS019 P2 shape. **Nothing from the gate lists that wasn't built entered the GDD** — in particular no HUD combo readout (FLAG-CS020-i is explicitly CS021). **(5) `DIFFICULTY-LEVERS.md` checked, confirmed untouched** — grepped for any delivery-combo mention; the three hits are all pre-existing and unrelated (the payload curve's own row, the SMD's fixed spawn ceiling, a historical note about the retired per-delivery growth curve) — `dockComboGrace` is a feel knob on a scoring rule, not a difficulty lever, and does not scale with anything, so no row was added. **(6) Archive step already done, verified not re-run** — `ls archive/` confirmed `PLANNED-FEATURES-CS019.md`/`IMPLEMENTATION-PHASES-CS019.md` are already there (per the P1b session's own note: staged by Paul's pre-P1 setup step); nothing moved this session. **(7) Appended ONE consolidated CS020 (P1+P1b+P2) entry to `GDD-VERSION-HISTORY.md`**, per the CS012/CS013/CS015/CS016/CS017/CS019 precedent — the only file opened specifically to append to, not read for context. **Noted in passing, not fixed (out of scope, per the phase prompt): CS018 still has no entry in that file** — CS019 P2 found the gap and correctly left it; still not this changeset's job. **Full regression, run twice consecutively for determinism: 85 test files, 31,702 assertions, 0 failures on both runs**, byte-identical between runs and identical to the P1b baseline count (no test file added or removed this phase; the version-pin edits changed assertion *content*, not *count*). `node --check`-equivalent clean — every test file evals the real extracted `<script>` block without throwing. Docs touched: `ORBITAL-OVERHAUL-GDD.md` (§2.10, §2.10.2, §2.17, §2.19, Architecture Map ×3, Current-build line), `GDD-VERSION-HISTORY.md` (new consolidated entry), this file. `DIFFICULTY-LEVERS.md` deliberately untouched (item 5). Committed per `CLAUDE.md`'s standing commit-per-phase rule; did NOT push. **CS020 IS COMPLETE.**

**Prior session (CS020 P1b, landed):** implemented CS020 Phase 1b (per `IMPLEMENTATION-PHASES-CS020.md` P1b, spec `PLANNED-FEATURES-CS020.md` §2/§5; FORK-CS020-E → one effort = one chain delivered in one go, terminated by a towed hook and not by moving, plus a grace window exposed as a millisecond debug knob; FLAG-CS020-g → `DOCK_COMBO_GRACE` ships at **4.0 s**, not the 2.0 first proposed; FLAG-CS020-h → the window runs whenever the ship is outside the ring regardless of chain contents), reading `CLAUDE.md`/`STATUS.md`/`PLANNED-FEATURES-CS020.md`/`IMPLEMENTATION-PHASES-CS020.md` in full and grepping every anchor in `asteroids-deluxe.html` by symbol rather than reading it whole.

**THE DIAGNOSIS WAS INDEPENDENTLY REPRODUCED BEFORE ANY EDIT**, by a throwaway probe (deleted before commit, per TRAP 4 — it lived outside the repo entirely) that seeded a bare-node chain, held the ship at `dock.radius − 20` for 12 real frames, at `dock.radius + 200` for 30, then returned, all through the real `startGame`/`update(1/60)`/dock-offload path. **Every pinned number in §2.2 reproduced to the digit, and reproduced IDENTICALLY on the working tree and on the pinned pre-fix SHA `09d443f` — confirming §2.1's claim that this predates CS020 and that P1 did not cause it:**

| Scenario | PRE-FIX (`09d443f` and CS020 P1) | POST-FIX (this session) |

|---|---|---|

| L1, 8-node, clean pass | `deliveryCount` 8, score 1100 | 8 / 1100 (unchanged) |

| **L1, 8-node, skirt + return** | **5 / 725** | **8 / 1100** |

| L12, 24-node, clean pass | 24 / 8100, **SMD fires** | 24 / 8100, SMD fires (unchanged) |

| **L12, 24-node, skirt + return** | **21 / 6525, SMD NEVER FIRES** | **24 / 8100, SMD FIRES** |

The 12-frame first stint delivers exactly 3 canisters (the offload timer needs 4 frames at `DOCK_OFFLOAD_INTERVAL` 0.05 and `dt` 1/60), which is precisely the spec's "deliver 3" staging. The fix is worth exactly the 375 points the skirt used to cost at level 1, and the whole Super Mega Delivery at level 12.

**Five edits, all in `asteroids-deluxe.html`.** (1) `const DOCK_COMBO_GRACE = 4.0;` beside `DOCK_BASE_SCORE`/`DOCK_BONUS_STEP`/`DOCK_NEIGHBORHOOD_PAD`; `DOCK_NEIGHBORHOOD_PAD`'s own comment rewritten, since its second reader changed identity (it was the combo reset, it is now the grace gate) and P1's `+40`-not-`+10` argument now rests on the new reason from §4.2 — at `+10` a parked player's magnet grabs would tag `towed` and therefore reset their own combo continuously. (2) **RULE 1, the hook reset:** `if (!inRing) game.deliveryCount = 0;` at the pickup capture gate, beside P1's `towed` tag and sharing the SAME single `inRing` const — no second radius is computed. (3) **RULE 2, the grace window:** the distance-based reset in the offload block's `else` branch is **deleted** (that branch is now only `game.offloadTimer = 0;`) and replaced by a countdown **hoisted to the top of the `if (game.dock)` block**, so it evaluates every frame independent of the offload branch rather than only while not delivering; it re-arms at the **ring** (`dock.radius + DOCK_NEIGHBORHOOD_PAD`), not the `+10` offload radius. (4) New run state `comboGrace: 0` in the game object literal, zeroed in `startGame()` alongside `deliveryCount`/`offloadTimer`. (5) `DEBUG_VARS` gains a `{ header: "DELIVERY" }` section after `CHAIN GUARD` and one knob, `dockComboGrace`, using the existing `unit: "ms"` + `toNative: v => v / 1000` idiom copied from `autoShieldRegenPause`/`garbageAttractDelay`; `def` derives from `DOCK_COMBO_GRACE * 1000` so the panel and the code cannot disagree. **`DEBUG_VARS` 33 → 34 value entries.** Persistence is the existing additive `afd_settings_v1.debug` path with known-value-else-default validation — **no schema bump, no new key, no frozen key touched**. `GAME_VERSION` is untouched at `"1.0.0.19"` (TRAP 1); the GDD, `GDD-VERSION-HISTORY.md` and `DIFFICULTY-LEVERS.md` are untouched (TRAP 2).

**WHY THE OBVIOUS FIX WOULD HAVE BEEN WRONG, and why the two rules both had to land.** A grace timer alone re-opens P1's exploit at a wider radius: `MAGNET_RANGE` is 380 px and the ring is 128 px from dock centre, so a player hovering at 130 px is *outside* the ring (their pickups tag `towed`) while reaching garbage out to 510 px — fill, dip inside 98 px, offload at escalating rates, drift out, and a timer carries the combo across trips without bound, for 60 px of travel. **The timer is not the safety mechanism; the hook reset is.** That is asserted, not argued: test §F drives a 29,030-frame randomized seeded session at level 12 and checks `deliveryCount <= game.cargoMax` on **every frame** — **0 violations, observed maximum exactly 24 of 24**, i.e. the session runs with *zero headroom* under the cap. Test §H drives the 130 px farm directly: five annulus trips scored **53,000, identical to five honest full loads**, with peak combo 24. Under a mutant with the hook reset removed, the same two sections give 8,113 cap violations, a maximum of 86, and a farm scoring **244,500 against the honest 53,000**.

**BASELINE MEASURED BEFORE EDITING, not inferred.** Swept all 84 `scratchpad/test-*.js` at the P1 commit `8072662` with the working tree clean: **84 files, 31,492 assertions, 0 failures** — fully green. **Full regression after the phase, run twice consecutively for determinism: 85 files, 31,702 assertions, 0 failures on both runs, byte-identical per-file results between the two runs**; the only per-file diff against the baseline is the new file. `node --check` clean on the extracted `<script>`.

**FOUR test files needed repointing, not the one the spec predicted** — the surface was wider than "small", and finding it took a full sweep rather than a grep. All four are mirror-image repoints with a `REPOINTED BY CS020 P1b` note; none was weakened or deleted. (1) `test-cs020-p1.js` §A's `DOCK_NEIGHBORHOOD_PAD` reader count: still exactly 2, but the *pair* changed (the tag + the combo reset → the tag + the grace gate), and it now counts over comment-stripped source with an added assertion that one of the two IS the grace gate and that the retired reset's comment is gone. (2) The same file's TRAP 3 `DEBUG_VARS === 33` → 34, plus a new assertion naming `dockComboGrace` as the single entry that moved it, so a second unplanned knob still fails. (3)–(4) **`test-cs018-p4.js` §H, `test-cs018-p6.js` §I and `test-cs018-p7.js` §H all carry the same live-registry count** (33 → 34) — three files the spec's "no existing test hardcodes `radius + 40`" note did not anticipate, each already repointed two or three times before for exactly this reason (a knob added). Each got the count plus the id of the entry that moved it, the established treatment.

**New `scratchpad/test-cs020-p1b.js` (187 assertions, (A)–(L), 0 failures, deterministic across repeated runs)** drives the REAL `startGame`/`update(1/60)`/`nextWave`/pickup gate/dock-offload/`applyDebug`/`saveSettings`/`loadSettings`/`returnToDefaults`/`scatterChain` path — nothing under test is reimplemented — and covers spec §8.2 items 1–13. **(B)** and **(C)** additionally build the pre-fix module from the **fixed SHA `09d443f`** (never `HEAD` — the trap `test-cs017-p3.js` fell into) as permanent red controls, so "5 / 725" and "21 / 6525, no SMD" are pinned against the actual previous build rather than a restated expectation. **(A)** `node --check` + source pins: exactly ONE `game.deliveryCount = 0` survives in the dock block and it is gated on `comboGrace <= 0`, not on distance; the offload `else` branch is *only* the timer disarm; the grace gate is hoisted ABOVE `const nearDock`; the registry entry's full shape including `toNative(4000) === 4` and `toNative(100) === 0.1`; `DEBUG_ROWS` still derives from the registry with no hardcoded count; and five `deliveryCount = 0` sites total, with no clamp anywhere — the cap must be a *consequence*, not a guard. **(D)** the window expires at grace + 0.5 s and the boundary is probed frame by frame (the flip lands within ±1 frame of 240), plus the re-arm-at-the-ring proof: 12 seconds loitering at `dock.radius + 20` (past the +10 offload zone, inside the +40 ring) does not expire the run. **(E)** the knob is driven through the real `applyDebug` in display units, not the constant: 0 ms kills the run on the first frame outside, 10,000 ms carries a nine-second excursion but not an eleven-second one, and a mid-run panel change takes effect on the very next re-arm. **(G)** the ±1 px discrimination — a towed hook at `dock.radius + 41` ends the effort, an incidental at `+39` does not reset it *and* does not join it. **(I)** P1's 60-second level-1 park re-run intact: still 37,500, still `deliveryCount` 0, still zero latches, still no SMD, still silent, and the board still clears. **(J)** ship death. **(K)** persistence, including nine bad-value fallbacks and TRAP 3's behavioural proof that `returnToDefaults()` resets bindings only. **(L)** an `AudioSys.ctx === null` smoke across a full skirt-and-return.

**The suite is mutation-tested against the five breakages the phase prompt named, and every one fails on BEHAVIOURAL assertions, not merely on source pins.** **Dropping the hook reset** → 8 behavioural failures: §F's cap (8,113 violations, max 86 against a cap of 24), §H's farm (244,500 vs 53,000, peak combo 120), and §G's `+41` reset. **Resetting on every hook including incidentals** → 2 behavioural, both §G: the `+39` incidental zeroed a run at 3, and the 5 originals then finished at 5 instead of 8 — the exact unfairness the `!inRing` guard exists to prevent. **Re-arming at the offload radius instead of the ring** → 4 behavioural, all §D's annulus loiter (`comboGrace` ran to −8.0 and the run finished at 5 / 725 instead of 8 / 1100). **Making the grace timer the only guard** (hook reset dropped *and* the window widened to 10 s, the "obvious fix" §2.3 rejects) → 10 behavioural: §F max **911** with **zero resets in the whole session**, §H's farm at 244,500. **`toNative` returning `v` instead of `v / 1000`** → 25 behavioural across §D/§E/§J/§K/§L — the window silently becomes 4,000 *seconds*, so nothing ever expires.

**Four things surprised me.** (1) **The randomized cap test was wrong on its first draft and the phase prompt's warning about exactly this was not hypothetical.** As first written it peaked at `deliveryCount` **10 against a cap of 24** — enormous headroom, and a mutant that merely doubled the combo would have sailed under it. It was rewritten to gather to a random fraction of *remaining capacity* and to bias 55% of legs to excursions shorter than the window (the farm shape), so the honest session now runs up to exactly 24 and the assertion pins `maxSeen === cargoMax`, i.e. zero headroom. (2) **Documenting a constant's readers in a block comment breaks source-count pins.** Two `(A)` assertions failed on my own prose, because the new comments legitimately contain the strings `DOCK_COMBO_GRACE` and `dock.radius + DOCK_NEIGHBORHOOD_PAD` while explaining them. Both files now count over comment-stripped source. Worth knowing for any future phase that documents a shared constant. (3) **The spec's §5.2 comment and the spec's §5.2 code disagree about a dead ship, and the code is what shipped.** The comment says a corpse "neither decays nor re-arms"; the specced `if (!ship.dead && outside) {decay} else {re-arm}` in fact sends a dead ship down the **re-arm** branch. Verified to have no observable consequence — `scatterChain` has already zeroed the counter, so re-arming a timer with nothing to protect is a no-op — so the code was shipped verbatim as instructed and the in-file comment was written to describe the actual behaviour rather than repeat the inaccurate claim. Flagged here rather than silently reworded; §J asserts what the code really does (never decays on a corpse) with a live-ship control at the same distance and duration that *does* expire. (4) **The `!!game.dock` guard on the hook reset is defensive only** — `game.dock` is assigned exactly once in the source, in `nextWave()`, and is never set back to null, so the "no dock" branch is unreachable in play. Behaviour there is a no-op either way (the counter can only become non-zero at a dock).

**Docs: this file only.** `ORBITAL-OVERHAUL-GDD.md`, `GDD-VERSION-HISTORY.md` and `DIFFICULTY-LEVERS.md` are deliberately untouched per TRAP 2 — P2 owns the first two and CS020 never touches the third (spec §6: `dockComboGrace` is a feel knob on a scoring rule, not a difficulty lever). Committed per `CLAUDE.md`'s standing commit-per-phase rule; did NOT push. **⛔ Next: the PLAYTEST GATE between P1b and P2** — four questions, in `IMPLEMENTATION-PHASES-CS020.md`'s gate section and restated under Playtest asks below. P2 must not run until Paul has played the P1b build; the one value P2 exists to carry is `DOCK_COMBO_GRACE`.

**Prior session (CS020 P1, landed):** implemented CS020 Phase 1 (per `IMPLEMENTATION-PHASES-CS020.md` P1, spec `PLANNED-FEATURES-CS020.md` §1–§3; FORK-CS020-A → per-node `towed` tag, FORK-CS020-B → in-ring pickups count toward nothing, FORK-CS020-C → an incidental pays flat `DOCK_BASE_SCORE`, FORK-CS020-D → `DOCK_OFFLOAD_INTERVAL` untouched), reading `CLAUDE.md`/`PLANNED-FEATURES-CS020.md`/`IMPLEMENTATION-PHASES-CS020.md` in full and grepping every anchor in `asteroids-deluxe.html` by symbol rather than reading it whole.

**THE DIAGNOSIS WAS INDEPENDENTLY REPRODUCED BEFORE ANY EDIT**, by a throwaway probe (deleted before commit, per TRAP 4) that parked a ship just inside the offload cutoff, fed one fresh `Garbage` piece every 6 frames and ran 3600 real `update(1/60)` frames against the unmodified build. **Agreed with the spec to the digit on every pinned number:** level 1 / 10 s = **168,750**; 30 s = **1,513,750**; 60 s = **5,650,000 from 600 canisters**; level 12 / 60 s = **5,650,000** (the park's level makes no difference, which is the tell); `superMegaDelivery()` **fired at level 1** with `cargoMax` 8. The probe also surfaced two things §1.3 does not state, both now pinned by test: `VoiceSys.dockDelivery` was called **600 times in 60 seconds** (Dan sizing up a haul twenty times a second — a pre-existing noise source the fix closes), and `lifetime.deliveryScore` took **4,522,500** of it. **ONE FIGURE DISAGREED and it is the one whose staging the spec does not pin:** the "10 separate full-cap visits at level 12" control measured **106,000**, not §1.3's 107,000 (240 canisters either way). Reproduced twice, including once with the level reached by driving the REAL `nextWave()` to 12 so `cargoMax` was genuinely 24. The 106,000 is arithmetically exact — 10 × Σ(50+25(n−1)) for n=1..24 = 81,000 of delivery pay, plus 10 milestone crossings × `REPAIR_FULL_BONUS` 2,500 = 25,000 — and 107,000 is not reachable from that base at 2,500 a crossing, so the spec's control almost certainly included some incidental non-delivery scoring. Proceeded rather than stopping: it is a ±1% comparator, not the diagnosis, and every number the mechanism actually rests on matched exactly.

**Five edits, all in `asteroids-deluxe.html`.** (1) `const DOCK_NEIGHBORHOOD_PAD = 40;` beside `DOCK_BASE_SCORE`/`DOCK_BONUS_STEP`, with the existing combo-reset test repointed to read it — **zero bare `radius + 40` occurrences survive in the file** (asserted). The `+ 10` offload radius is a DIFFERENT number and is byte-unchanged, deliberately not hoisted and deliberately not unified. (2) The tag, computed ONCE inside the capture gate and ABOVE the `pieces === 1` / clump branch (the same placement the CS017 P5 bonus-canister award uses, so one expression covers both push paths): `const pad = game.dock ? game.dock.radius + DOCK_NEIGHBORHOOD_PAD : 0;` / `const inRing = !!game.dock && dist2(game.ship, game.dock) < pad * pad;`. **The radius is the combo-reset one and that is load-bearing** — at `+10` a player hovering 20 px out hooks pieces tagged `towed` while never travelling far enough to reset the combo, then drifts in and offloads the farm at escalating rates; `+40` is the only radius under which "tagged incidental" and "combo not reset" are the same region. (3) Both `game.chain.push({…})` sites gain `towed: !inRing`; the clump-scoop loop reads the one shared binding, so every `take` node of a scoop carries the identical tag. (4) The offload block split on `const towed = node.towed !== false;` — the entire existing body moved unchanged into `if (towed) { … }` (`git diff -w` reads as a pure indent plus a new else), with `if (game.chain.length === 0) VoiceSys.dockDelivery(game.deliveryCount);` moved INSIDE that branch, and a new else that does `addScore(DOCK_BASE_SCORE)` + the same `FloatText` + `AudioSys.deliver(1)`. `game.offloadTimer = DOCK_OFFLOAD_INTERVAL;` runs after the if/else, once, for both branches. (5) Nothing else — no `deliveryCount` clamp, no separate SMD gate, no touch to the CS018 P8 reward latches, the `===12` Heavy Hauler latch, the `===CARGO_CAP_MAX` Maxed Out latch, `superMegaDelivery`, `breakChain`, `scatterChain`, `Garbage.fromNode`, the pickup gate's `cargoMax` test or `DOCK_OFFLOAD_INTERVAL`. All of those are already correct once incidentals stop advancing the counter; a second guard anywhere would be a second source of truth.

**POST-FIX, same probe scenario:** level 1 / 60 s parked = **37,500** (was 5,650,000 — a 151× cut), `deliveryCount` **0**, `stats.delivered` **0**, `bestCombo` **0**, `lifetime.delivered` **0**, `lifetime.deliveryScore` **0**, `pacifistStreak` **0**, SMD fired **0**, `VoiceSys.dockDelivery` calls **0**. Level 12 / 60 s parked: identical. **The legitimate-play control is unchanged at 106,000 / 240 delivered / 10 SMDs.** The residual 7,500 in that 37,500 is **FLAG-CS020-c made visible and measured**: 600 × 50 = 30,000 of flat pay, plus **exactly three** `REPAIR_FULL_BONUS` crossings (`addScore` trips `REPAIR_MILESTONE` every 10,000 and cashes 2,500 at full hull). At the probe's ~10 pieces/s that is one milestone every 20 s; at the 20/s offload ceiling it would be one every 10 s. That is the number for the playtest gate — see Playtest asks.

**BASELINE MEASURED BEFORE EDITING, not inferred.** Swept all 83 `scratchpad/test-*.js` at `09d443f` with the working tree clean: **83 files, 31,257 assertions, 0 failures** — a fully green baseline, unlike CS018 P3 / CS019 P1. **NO PRE-EXISTING TEST NEEDED REPOINTING, and none was touched** — the `!== false` default did exactly what §3.3 predicted across the 22-file hand-seeded-node surface, and that is now proved rather than assumed by test §F. **Full regression after the phase, run twice consecutively for determinism: 84 files, 31,490 assertions, 0 failures on both runs**, per-file diff against the baseline empty. `node --check` clean on the extracted `<script>`.

**New `scratchpad/test-cs020-p1.js` (233 assertions, (A)–(K), 0 failures, deterministic across repeated runs)** drives the REAL `startGame`/`update(1/60)`/`nextWave`/pickup gate/dock-offload path — nothing under test reimplemented. (A) `node --check` + source pins: `DOCK_NEIGHBORHOOD_PAD` declared once with exactly two `dock.radius + DOCK_NEIGHBORHOOD_PAD` readers and zero surviving bare `radius + 40`, the `+10` offload radius byte-unchanged, exactly two `chain.push` sites both carrying `towed: !inRing`, `inRing` derived exactly once (so the clump loop cannot re-evaluate it per node), the `!== false` read, `VoiceSys.dockDelivery` provably between the towed read and the `else`, `offloadTimer` re-armed after the if/else and in exactly one place, TRAP 1 (`GAME_VERSION` still `"1.0.0.19"`) and TRAP 3 (`DEBUG_VARS` still exactly 33 value entries). (B) **the regression**: the level-1 60-second park on the fixed build, plus the identical staging on the **pre-fix build at the pinned SHA `09d443f`** asserting 5,650,000 / 600 / SMD-fired / 600 voice calls as a permanent red control (a FIXED SHA, never `HEAD` — the `test-cs017-p3.js` trap CS017 P6 had to repoint), plus the 10 s and 30 s points of the §1.3 curve and the level-12 park. (C) the tag: `+41` towed, `+39` incidental, `+40` exactly **towed** (the test is `<`, strictly, so on-the-boundary is outside — the same convention the combo reset already used, since both now read one expression); both push paths; a partial clump scoop; a magnet hook; and two mirror cases proving the measured distance is the SHIP's and not the piece's (ship inside + piece far outside via a max-level scoop mouth ⇒ incidental; ship 1 px outside + piece inside ⇒ towed); plus the no-dock path. (D) **the annulus**: hover at `+20`, hook 20, drift inside `+10`, offload — all 20 incidental, `deliveryCount` ends 0, exactly 20×50 paid. (E) **the LIFO property**: a full 12-node towed load plus one incidental hooked during the offload window, asserted as an exact award SEQUENCE (`50, 50, 75, …, 325`) — the incidental pops first and takes flat 50 while every towed node keeps its own escalation. (F) the default: absent / `undefined` / `null` / `0` / `""` all deliver as TOWED, only explicit `false` demotes, and a 12-node bare-literal chain still trips Heavy Hauler. (G) the latches: 40 incidentals fire zero P8 reward powerups, no Heavy Hauler, no Long Haul, no Freight Baron, no Maxed Out, no cargo flash and **never call `superMegaDelivery` (spied, not inferred)**, while a real 24-piece towed visit at level 12 still fires all four — plus a mixed 24-node visit (12 towed, 12 hooked at the dock) that reaches Heavy Hauler and does NOT reach Maxed Out or the SMD, which is §1.4 closed behaviourally. (H) the stats: fifteen named fields asserted byte-unchanged one at a time, `game.score` moved by exactly 40×50, FLAG-CS020-a asserted in BOTH directions (an incidental neither advances the pacifist streak nor breaks it), FLAG-CS020-b, and Speed Recycler not latching. (I) Dan: zero `dockDelivery` calls across a 40-incidental park, exactly one on a real haul reporting the TOWED count, and the flat `deliver(1)` of FLAG-CS020-e proved by the sound sequence `1,1,2,3`. (J) **byte-identity**: four full 24-piece visits at level 12 under a shared seeded LCG produce bit-identical score, all fifteen counters, SMD count, powerup count, hull/`nextRepair`, **and every floater's text and position to six decimal places** against the pre-fix build. (K) `AudioSys.ctx` null smoke through a mixed visit plus `draw()`.

**The suite is mutation-tested.** Five deliberate breakages, each failing on BEHAVIOURAL assertions and not merely on source pins: **dropping the tag from the clump-scoop push** → caught by (C2) "ALL six clump-scoop nodes are towed=…" in both directions and (C3)'s partial scoop, 3 behavioural failures; **using `+10` instead of `+40`** → caught by (D) "all 20 pieces hooked at dock.radius + 20 are tagged INCIDENTAL" and "deliveryCount ends at 0" (the farm scored 5,750 instead of 1,000), plus (C1)/(C4)/(C6)/(G3), 14 behavioural failures; **truthiness instead of `!== false`** → caught by all five (F) default cases including the bare-literal 12-node chain losing Heavy Hauler, 12 behavioural failures; **leaving `VoiceSys.dockDelivery` outside the towed branch** → caught by (I) "40 incidental pops produce ZERO dockDelivery calls" (got 40) and (B1) "Dan says nothing across the whole park" (got 600), 2 behavioural failures; **the rejected count-snapshot design** → caught most precisely by (E), whose award sequence came out `50,75,…,350` instead of `50,50,75,…,325`, i.e. the escalation budget spent on the in-ring pickup and the towed load demoted, exactly as §3.1 predicts — 41 behavioural failures in all.

**Two things surprised me.** (1) **Teleporting a ship to the dock without moving its tow does not work** — a 400 px-stretched 12-node chain's `CARGO_TUG` drags the ship straight back out of the `+10` offload radius, so only the first canister ever delivers. The test's `placeShip` helper therefore translates the ship AND every chain node by the same delta, keeping the rope relaxed. Any future test that stages "arrive at the dock with a load" needs this; the existing suite dodged it by pushing nodes directly at the dock instead of hooking them for real. (2) **Zero pre-existing tests broke.** The spec predicted "nearly all" would stay green; it was all of them.

**Docs: this file only.** `ORBITAL-OVERHAUL-GDD.md`, `GDD-VERSION-HISTORY.md` and `DIFFICULTY-LEVERS.md` are deliberately untouched per TRAP 2 — P2 owns the first two and CS020 never touches the third. The CS019 planning docs' move into `archive/` and the two CS020 planning docs were already staged in the working tree by Paul's own pre-P1 setup step and are carried in this commit rather than left dangling; P2's item (6) should find the archive move already done. Committed per `CLAUDE.md`'s standing commit-per-phase rule; did NOT push. **⛔ Next: the PLAYTEST GATE between P1 and P2** — see Playtest asks below; P2 must not run until Paul has played the P1 build.

## Working / verified

**Headless-verified in CS020 P1b (`scratchpad/test-cs020-p1b.js`, 187 assertions, 0 failures, deterministic):** twelve sections (A)–(L), all driving the REAL `startGame`/`update(1/60)`/`nextWave`/the pickup gate/the dock-offload path/`applyDebug`/`saveSettings`/`loadSettings`/`returnToDefaults`/`scatterChain`, with (B) and (C) additionally building the pre-fix module from the pinned SHA `09d443f` so both "this used to be broken" numbers are checked against the actual previous build. **The four that carry the phase: (B)** the 8-node skirt — 8 / 1100 fixed against 5 / 725 pre-fix; **(C)** the level-12 headline — 24 / 8100 with the Super Mega Delivery FIRING, against 21 / 6525 and no SMD pre-fix; **(F)** the structural cap, `deliveryCount <= game.cargoMax` checked on every one of 29,030 frames of a randomized seeded session, 0 violations and an observed maximum of exactly 24 of 24 (zero headroom, deliberately — an earlier draft peaking at 10 would have been too weak to catch a mutant); **(H)** the 130 px annulus farm, which now scores 53,000, byte-equal to five honest full loads. Mutation-tested against the five deliberate breakages the phase prompt named, all caught behaviourally (see the phase log above for which assertion caught which). **`test-cs020-p1.js` was repointed, not weakened** — its `DOCK_NEIGHBORHOOD_PAD` reader-count pin now names the grace gate as the second reader, and its TRAP 3 registry count moved 33 → 34 naming `dockComboGrace`.

**Headless-verified in CS020 P1 (`scratchpad/test-cs020-p1.js`, 236 assertions after this session's repoint, 0 failures, deterministic):** eleven sections (A)–(K), all driving the REAL `startGame`/`update(1/60)`/`nextWave`/the pickup gate/the dock-offload path, with (B) and (J) additionally building the pre-fix module from the pinned SHA `09d443f` so "this used to be broken" and "normal play is unchanged" are both checked against the actual previous build. The four that carry the phase: **(B)** the level-1 60-second park — 37,500 fixed against 5,650,000 pre-fix, `deliveryCount` 0 against 600, SMD 0 against 1; **(D)** the annulus at `dock.radius + 20`, which is the assertion that fails if anyone later "simplifies" the tag radius to `+10`; **(E)** the LIFO award sequence, which is the assertion that fails against the rejected count-snapshot design; **(J)** four full 24-piece level-12 visits bit-identical to the pre-fix build under a shared seeded LCG, floater positions included. Mutation-tested against five deliberate breakages, all caught behaviourally (see the phase log above for which assertion caught which).

## Next up

**CS020 IS COMPLETE — all phases landed, per `IMPLEMENTATION-PHASES-CS020.md`.** P2 (retune + version bump + doc sweep) landed the session it shipped in — see the phase log above for the full breakdown; the playtest gate between P1b and P2 was cleared with all four questions answered "no change" (Q3, the cap, came back "Better" — positive feedback with no code implication), so no delivery-combo constant moved. **CS021 is now the open changeset (see above).** **The CS020 archive move IS already done** — verified by `ls archive/` in CS021 P1: `PLANNED-FEATURES-CS020.md` and `IMPLEMENTATION-PHASES-CS020.md` are both there and repo root now holds only the CS021 pair, so `IMPLEMENTATION-PHASES-CS021.md`'s P5 item 5 ("verify, do not re-run") will find nothing to do.

- **What CS020 P1b leaves for whoever touches the delivery combo next.** (a) **The counter is bounded STRUCTURALLY, not by a clamp** — `deliveryCount <= game.cargoMax` falls out of the towed-hook reset plus the pickup gate's `cargoMax` test, and there is deliberately no `Math.min` anywhere. Any future code that increments the counter outside the offload block, or that lets a chain exceed `cargoMax`, silently breaks the guarantee; `test-cs020-p1b.js` §F is the assertion that will catch it. (b) **The two rules are load-bearing TOGETHER and neither is sufficient.** The window is generous *because* the hook reset bounds the counter — widening the window is safe, dropping or ungating the reset is not. `test-cs020-p1b.js` §H is the annulus-farm assertion that fails the moment the reset stops discriminating. (c) **The `!inRing` guard on the reset is the fairness half.** Resetting on every hook makes a magnet grab at the dock kill the player's own run mid-offload — pinned by §G's ±1 px pair. (d) **The re-arm is at the RING (`+40`), not the `+10` offload radius**, on purpose: once inside the neighbourhood the run is safe indefinitely so the player can take their time closing the last 30 px. §D's 12-second annulus loiter is what fails if that moves. (e) **`DOCK_COMBO_GRACE` and `DEBUG_VARS.dockComboGrace.def` must move TOGETHER** (`def` is the const × 1000); §A asserts they agree, which is exactly the mismatch P2's retune could otherwise introduce.

- **What CS020 P1 leaves for whoever touches the dock next.** (a) **Chain nodes now carry a `towed` field, and its default is `!== false`, not truthiness.** Any new code that constructs a chain node must either set it or accept the TOWED default deliberately; any new code that READS it must keep the `!== false` form, or the 22-file hand-seeded-node surface silently reclassifies. (b) **The tag radius and the combo-reset radius are one constant on purpose** (`DOCK_NEIGHBORHOOD_PAD`). Splitting them apart re-opens the farmable annulus; if either ever needs to move, both move. (c) **The `+10` offload radius is a genuinely different number** and was left as a bare literal deliberately — do not "finish the job" by hoisting it into the same constant. (d) **Everything keyed on `game.deliveryCount` is now correct by construction**, because incidentals never advance it. Do not add a second guard at the SMD trigger, the P8 reward tiers or either achievement latch; that would create two sources of truth for one rule.

- **`GDD-VERSION-HISTORY.md` has no CS018 entry.** Noticed while appending this session's CS019 entry (grepped, not found) — CS018 P10's own doc sweep never added a consolidated round entry for CS018, unlike every round before and after it. Not backfilled this session (out of a bug-fix changeset's scope); worth a one-line administrative fix whenever a future session is touching that file for its own reasons.

## Playtest asks

**✅ CS020 P1b's own gate CLOSED** — all four answered "no change" (Q3, the cap, came back "Better" — positive feedback, no code implication). `DOCK_COMBO_GRACE` ships unretuned at 4.0 s. Kept below for the record; nothing there is outstanding. — all four answered "no change" (Q3, the cap, came back "Better" — positive feedback, no code implication). `DOCK_COMBO_GRACE` ships unretuned at 4.0 s. Kept below for the record; nothing there is outstanding.

**CS020 P1b — THE GATE BETWEEN P1b AND P2 (CLOSED, kept for the record).** Everything about the fix is headless-solid (187 assertions, mutation-tested against five deliberate breakages, and identical to a clean pass on every legitimate skirt) but nothing about how it FEELS has been seen in a browser. Four questions, priority order:

1. **FLAG-CS020-g — the window. This is the one value P2 exists to carry.** Skirt the dock at speed with a real load, several times, at different exit velocities. Does **4,000 ms** cover a normal overshoot-and-return? It ships at 4.0 s rather than the 2.0 first proposed because simulating `Ship.update`'s actual integrator says a 180° turn costs 0.75 s before thrust can even begin and a normal pilot exiting at 200–300 px/s with a real load is outside for 2.8–3.8 s — 2 s would have failed almost every real skirt. **Drag `Delivery one-effort window` in the hidden Debug Options panel** (new `DELIVERY` section, right after `CHAIN GUARD`) until it feels right and report the number. It is live: a change takes effect on the very next time you re-enter the ring, no restart. Since the hook reset is what bounds the counter, a generous window costs nothing — err long if in doubt.

2. **The one-effort rule in play (spec §5.4 — where this is STRICTER than the literal ask).** Skirt with 8, deliver 3, then hook one stray on the way back: the combo resets and the 5 originals deliver at 1…5 rather than 4…8, because you topped up mid-run and that makes it a new run. This is the load-bearing rule that keeps the counter capped, and it is the one case where the originals might have been expected to survive. **Does it ever read as unfair, or is it invisible?** If it reads badly, note it — but understand the fix is not "special-case it", it is a different bounding rule, and that is a CS021 conversation, not a P2 edit.

3. **The cap.** The combo ceiling is now exactly the payload capacity, by construction. Does that change how a full 24-load at level 12 feels — better (the ceiling is now something you can actually see yourself reach), or anticlimactic? Related: with no HUD readout of the combo anywhere (FLAG-CS020-i, deliberately CS021), is the ceiling even legible in play?

4. **P1 still good.** Does the parking behaviour still feel right after P1b, or did the one-effort rule make in-ring cleanup feel pointless? Headlessly it is unchanged — the 60-second level-1 park still pays exactly 37,500 with zero latches and no SMD, and the board still clears — but "unchanged number" and "still feels worth doing" are different questions.

**✅ CS020 P1's own gate CLOSED before this session** — FLAG-CS020-c, -d, -e and -f were all cleared in play and the parking exploit is dead. Its four questions are kept below for the record; nothing there is outstanding.

**CS020 P1 — THE GATE BETWEEN P1 AND P2 (CLOSED, kept for the record).** Everything about the fix is headless-solid (233 assertions, mutation-tested against five deliberate breakages, bit-identical to the pre-fix build on legitimate play) but nothing about how it FEELS has been seen in a browser. Four questions, priority order:

1. **FLAG-CS020-c — the repair residual, and this one comes with a measured number.** Incidentals still route through `addScore()`, which trips `REPAIR_MILESTONE` (10,000 points → 25 HP, or `REPAIR_FULL_BONUS` 2,500 points at full hull). Measured this session on the real path: a 60-second park fed ~10 canisters/s pays 30,000 flat and crosses **three** milestones; at the 20/s offload ceiling it would be one milestone every 10 seconds. **Park at the dock with a Magnet and a drained hull and see whether that reads as a healing station.** If it does, the fix is a new `DOCK_INCIDENTAL_SCORE` constant at ~10–15, a one-line change P2 can carry.

2. **FLAG-CS020-f — the straggler.** Tow a load in with a Magnet running. A piece dragged in during the final approach is tagged incidental and loses its multiplier, with no grace period. Does that read as unfair, or is it invisible? If unfair, the fix is a grace window and it is **not** a P2 change — it is CS021.

3. **FLAG-CS020-d/e — the feedback.** At high incidental throughput the dock can throw up to 20 `+50` floaters a second: legible, or confetti? And does the flat `AudioSys.deliver(1)` read as "recycled" or as "broken"?

4. **The feel of the fix overall.** Does parking still feel like a reasonable thing to do — clearing the board against the Kessler loop — or does it now feel pointless? Pointless would be a real loss; the mechanic was a nice find and the goal was to bound it, not kill it.

One thing the fix changes that is intended, not collateral, and worth watching for anyway: **Maxed Out is now effectively a level-12+ achievement** (a 24-piece TOW needs `payloadSlots` 24), and **Recycling Magnate / Salvage King are no longer farmable at the dock**.

## GDD version history

- **CS020 (P1–P2) — the delivery combo round: closing two defects in the same counter, the second surfaced by playtesting the fix for the first.** `IMPLEMENTATION-PHASES-CS020.md`, the whole changeset (this consolidated entry replaces the per-phase P1/P1b/P2 entries, following the CS012/CS013/CS015/CS016/CS017/CS019 precedent); spec `PLANNED-FEATURES-CS020.md` (FORK-CS020-A → per-node `towed` tag set at capture, not a count snapshot; FORK-CS020-B → in-ring pickups count toward nothing; FORK-CS020-C → an incidental pays flat `DOCK_BASE_SCORE`; FORK-CS020-D → `DOCK_OFFLOAD_INTERVAL` untouched; FORK-CS020-E → a delivery run is ONE EFFORT, terminated by a towed hook and not by moving, plus a tunable grace window). `GAME_VERSION` `"1.0.0.19"` → **`"1.0.0.20"`**, bumped at P2 as planned — P1/P1b deliberately left it alone. **P1 — the parking exploit.** A ship parked inside the dock's neighbourhood kept `game.deliveryCount` alive indefinitely — the only reset was distance-gated (`radius + 40`) and a parked ship never travelled that far — so garbage wandering into the ring earned the same escalating `50 + 25×(n−1)` award as a genuinely towed haul, at up to 20 canisters/second. Measured on the unmodified build: a 60-second level-1 park scored 5,650,000 (vs. 106,000 for ten honest full-cap visits at level 12) and fired the level-1 Super Mega Delivery outright. The fix: a new `DOCK_NEIGHBORHOOD_PAD` (40, hoisted out of the bare `radius + 40` literal) backs an `inRing` test computed once per capture; every `chain.push` site tags the new node `towed: !inRing` (read as `node.towed !== false`, so every pre-existing bare-object chain node in the test suite defaults to towed). The offload block splits on that tag: a towed node keeps the existing escalating award and its own `VoiceSys.dockDelivery` call; an incidental pays a flat `DOCK_BASE_SCORE` (50) and touches nothing else — not `deliveryCount`, not `stats.delivered`/`lifetime.delivered`, not `bestCombo`, not the pacifist streak, not `lifetime.deliveryScore`. Tagging at capture rather than snapshotting a count is load-bearing: the chain is LIFO, offload pops the tail, and both pickup paths push onto that same tail, so a piece hooked while parked jumps the queue and would spend a count-based scheme's escalation budget on the interloper. Post-fix, the same 60-second park scores 37,500 (a 151× cut) with zero latches and no SMD; legitimate play is unchanged. New `scratchpad/test-cs020-p1.js` (233 assertions), mutation-tested against five deliberate breakages, bit-identical to the pre-fix build on legitimate play, zero pre-existing tests needing repointing. **P1b — the one-effort rule.** Playtesting P1 surfaced a second defect predating CS020 entirely: the same distance-gated reset also fired on a *legitimate* skirt — a ship that delivers part of a towed load and swings back out for the rest lost the whole run, because the reset didn't know the same chain was still in tow. An 8-piece load delivered as 3-then-5 scored 725 instead of 1100; at level 12 a full 24-piece load delivered across one skirt reached 21, so the Super Mega Delivery never fired. A grace timer alone was rejected: `MAGNET_RANGE` (380px) reaches well past the 128px ring, so a player hovering just outside it could tag pickups `towed`, dip in to offload, and drift back out — a timer alone would carry that combo across trips without bound. Two cooperating rules instead. **Rule 1, the hook reset:** capturing any node with `!inRing` sets `game.deliveryCount = 0` — gathering again starts a new effort; an incidental hook is deliberately exempt, so parked cleanup can't cost a run in progress. This is what actually bounds the counter, and it yields a guarantee the build never had before: between any two resets only chain-resident nodes can be counted, and the pickup gate already bounds the chain at `game.cargoMax`, so **`deliveryCount ≤ cargoMax` holds structurally** — asserted across a 29,030-frame randomized seeded session (zero violations, observed maximum exactly 24 of 24) rather than merely argued. **Rule 2, the grace window:** the distance-based reset is deleted outright and replaced by a countdown, `DOCK_COMBO_GRACE` (**4.0 s**, not the 2.0 s first proposed — simulating `Ship.update`'s real integrator shows a 180° turn alone costs 0.75 s before thrust can even begin, and a normal pilot's real skirt runs 2.8–3.8 s outside the ring), hoisted to the top of the dock block so it evaluates every frame independent of the offload branch, and re-arming at the **ring** (`dock.radius + DOCK_NEIGHBORHOOD_PAD`) rather than the tighter offload radius. New run state `comboGrace` sits beside `deliveryCount`/`offloadTimer`; a new `DEBUG_VARS` `DELIVERY` section (registry 33→34) exposes the window in milliseconds via the established `toNative` idiom, `def` deriving from `DOCK_COMBO_GRACE * 1000` so panel and code can never disagree. New `scratchpad/test-cs020-p1b.js` (187 assertions), mutation-tested against five deliberate breakages (dropping the hook reset alone produces 8,113 cap violations and a 244,500-point farm against an honest 53,000); four pre-existing test files' live `DEBUG_VARS` counts were repointed (mirror image, not weakened). **P2 (round complete) — retune, version bump, doc sweep.** Paul's playtest gate answered all four questions "no change": `DOCK_COMBO_GRACE` at 4.0 s covers a real overshoot-and-return, the one-effort rule's mid-run top-up reset never read as unfair, the 24-piece cap at level 12 reads as *better* now that it's a genuine, reachable ceiling, and P1's in-ring cleanup still feels worthwhile — so no constant moved. `GAME_VERSION` bump grepped the whole repo rather than trusting the phase doc's own file list: eight live pins beyond the HTML itself (six carried over from CS017 P7/CS019 P2's own sweep — `test-cs010-p0.js`, `test-cs013-p4.js`, `test-cs015-p7.js`, `test-cs016-p5.js`, `test-cs017-p7.js`, `test-cs018-p10.js` — plus this round's own two new files, `test-cs020-p1.js`/`test-cs020-p1b.js`, each repointed from an "unchanged this phase (bumps in P2)" pin to the mirror image, `!== "1.0.0.19"`, not weakened). `test-cs017-p7.js`'s header-comment narrative ("GAME_VERSION 1.0.0.17", describing what THAT phase historically shipped) was left alone, per the CS018 P10/CS019 P2 precedent. GDD §2.10 rewritten around the one-effort rule (what earns the combo, what an incidental pays, the LIFO reasoning for tagging at capture, the towed-hook terminator, the grace window, and the `deliveryCount ≤ cargoMax` guarantee); §2.10.2 gained a note that the payload curve is now the hard ceiling on the combo itself, load-bearing for the SMD and the CS018 P8 reward tiers; §2.17 gained a note that Maxed Out is a level-12+ achievement by construction and Recycling Magnate/Salvage King are no longer dock-farmable; §2.19 gained the `DELIVERY` header/`dockComboGrace` knob and the 33→34 registry count; the Architecture Map's Constants/game-object/chain-node rows gained `DOCK_NEIGHBORHOOD_PAD`/`DOCK_COMBO_GRACE`, `comboGrace`, and the node-level `towed` flag respectively. `DIFFICULTY-LEVERS.md` checked and left untouched — `dockComboGrace` is a feel knob on a scoring rule, not a difficulty lever, and does not appear in it. CS019's planning docs were already archived by Paul's own pre-P1 setup step — verified via `ls archive/`, nothing moved. **Full regression, run twice consecutively for determinism: 85 test files, 31,702 assertions, 0 failures on both runs**, byte-identical between runs. Docs: this entry, GDD §2.10/§2.10.2/§2.17/§2.19/Architecture Map/Current-build line, `STATUS.md`. Did NOT push.
