# CS027

**CS027 P6 — CLOSE: version `1.0.0.27`, TRAP-CS027-A measured, the doc sweep. CS027 IS COMPLETE.** Per `archive/IMPLEMENTATION-PHASES-CS027.md` P6. No gameplay, lever, or registry change — the whole changeset is test/doc infrastructure.

**(1) TRAP-CS027-A, MEASURED RATHER THAN CLAIMED — AND THE PLAN'S OWN WORDING WAS WRONG.** `archive/PLANNED-FEATURES-CS027.md` §2 asserted the `<script>` block would differ from `89a9a3a` (CS026 P6, the changeset's start) by "exactly one line: the `GAME_VERSION` string." `test-cs027-p6.js` §B diffs it for real and finds **three** changed lines, not one: the `GAME_VERSION` swap, plus two doc-path comments (`PLANNED-FEATURES-CS026.md` → `archive/PLANNED-FEATURES-CS026.md`) that P0's own phase prompt explicitly authorized ("do not touch `asteroids-deluxe.html` except for doc-path references inside comments"). The plan's central assertion undercounted its own P0 scope. §B asserts the corrected, TRUE invariant instead — reconstructing HEAD from the parent via the three exact, enumerated substitutions and requiring byte equality — so a fourth, unlisted change still fails the test for real. Not silently absorbed: recorded here, and the registry/`LEVERS` counts are independently confirmed unmoved (85/18) against the same parent.

**(2) THE VERSION SWEEP: SEVEN LIVE PINS REPOINTED, ONE CLOSING-PHASE PIN AND ONE UNRELATED PIN REPAIRED.** `GAME_VERSION` `"1.0.0.26"` → `"1.0.0.27"`. The seven live pins (`test-cs010-p0`, `-cs013-p4`, `-cs015-p7`, `-cs016-p5`, `-cs017-p7`, `-cs018-p10`, `-cs021-p4`) repointed to the new literal, same set CS026 P6 named. **Two more breaks surfaced on the full-suite run that the phase prompt did not name:** `test-cs026-p6.js` §A — CS026's own closing-phase pin, which asserted the LIVE build carries `"1.0.0.26"` — flipped to its standing mirror image (`!== "1.0.0.25"`, the pre-CS026-P6 baseline), the same convention CS026 P6 applied to `test-cs025-p5.js`'s equivalent pin; and its `HighScores.add()` stamp check changed from a literal to `rec.build === X.GAME_VERSION`, so it never needs touching again. And `test-cs027-p2.js` §E, which had compared the CS027-P1-parent build's version against the *live* build's version (correct only because both were `"1.0.0.26"` when P2 wrote it) — repointed to the parent's own frozen literal (`"1.0.0.26"`), a parent-commit setup assertion that is now permanently correct.

**(3) TWO CONSECUTIVE FULL RUNS, DIFFED.** `node scratchpad/run-all.js` twice: **109 files, 109 passed, 0 failed, 0 skipped, 0 timed out**, both times (28.03s then re-confirmed). The two runs' raw output differs — but only in per-file wall-clock timings (one run showed a stray `-0.37s` for `test-cs025-p3.js`, almost certainly a `Date.now()` clock-jitter artifact in this environment, not a suite defect). Every PASS/FAIL line and the summary counts are byte-identical between runs. Zero occurrences of `SKIPPED (no git history)` in either run — the seeding work CS026 P1 did is what makes that assertion meaningful rather than vacuous.

**(4) THE DOC SWEEP — THE FIRST TIME THE NEW CLOSE-OUT RITUAL RUNS.** `PLANNED-FEATURES-CS027.md` / `IMPLEMENTATION-PHASES-CS027.md` moved to `archive/`, with the small number of bare-filename references inside `scratchpad/` (a header comment in `test-cs027-p6.js`, one in `run-all.js`) repointed to `archive/...`. This file (`log/CS027.md`) now carries the full CS027 narrative, relocated out of `STATUS.md` rather than rewritten; `STATUS.md` is reset from the `CLAUDE.md` template, carrying forward only what is still open (see `STATUS.md` for the live list). GDD §7 already pointed at `log/` as of CS027 P4 — confirmed, not re-edited.

**(5) §5 SUCCESS CRITERIA, MEASURED.** The actuals against `archive/PLANNED-FEATURES-CS027.md` §5's targets are in `## CS027 close-out` below — all five targets met or beaten. *(That table stood in `STATUS.md` until CS028 P1 moved it here. It could not stay: `STATUS.md` covers the current changeset only and is reset at every close, so a cross-reference into it from this file was pointing at something scheduled for deletion. Relocated verbatim, nothing rewritten.)*

---

**CS027 P5b — `destroyHunter()`'s marker reclassified `⚠ SETTLED` → `⛔ INVARIANT` (Paul's call).** FLAG-CS027-e: the rule reads as both — it *looks* like an oversight that one kill path (`destroyHunter`) is not levered like `destroyDebris`'s `junkSplit` (the `⚠` case), but changing it moves the 1+3+9 lineage total and breaks the shipped `ACH_LINEAGE_FULL = 13` (the `⛔` case). `⚠ SETTLED` invites "raise it with Paul"; this one was never open for discussion, so it was misclassified. Reasoning recorded at `RATIONALE.md#levers`. **First exercise of the two-marker scheme, and the boundary it sets: when a rule fits both, the tiebreak is whether the answer could change — if it can't, it's `⛔`.**

**CS027 P5 — `CLAUDE.md` states rules only; the "why" moved to `RATIONALE.md`; GDD §5 rewritten off the attach-everything workflow.** Per `archive/IMPLEMENTATION-PHASES-CS027.md` P5. `CLAUDE.md` 528 → 427 lines, with the two new markers (`⛔ INVARIANT` / `⚠ SETTLED`) introduced. `RATIONALE.md` created from the stripped prose, organized under 18 `#anchor` headings; `CLAUDE.md` points at 2 of them (`#pins`, `#voice-queue`) at the time of writing — the other 16 are addressable but unreferenced, deliberately, so a future rule can link one without a new file edit. GDD §5 (Session Handoff Protocol) rewritten to match reality: `CLAUDE.md` auto-loads, `STATUS.md` is one page, everything else is pulled on demand.

The rule-survival audit was mechanical, not eyeballed: all 73 bolded rules ≥12 chars in `89a9a3a:CLAUDE.md` were extracted and matched against `CLAUDE.md` ∪ `RATIONALE.md` (blockquote markers stripped). 64 survive byte-identically; the other 9 are restatements the new `CLAUDE.md` tightened deliberately (e.g. "Game logic in one file, vanilla JS, no modules" → the ⛔ one-`<script>`-block rule). Zero dropped. Suite re-run green after the doc edits: 108/108, 0 skipped, 28.90s.

**CS027 P4 — `STATUS.md` cut to one page; `archive/STATUS-HISTORY.md` and `GDD-VERSION-HISTORY.md` both folded into per-changeset `log/CS0##.md` files; `STATUS.md`'s own CS023–026 narrative joins them.** Per `archive/IMPLEMENTATION-PHASES-CS027.md` P4. A Python script in `scratchpad/` did the relocation (run-based walk keyed on (changeset, section), not paragraph-by-paragraph — 71% of `STATUS-HISTORY.md`'s 618 paragraphs are continuations with no `CS0NN` marker of their own). Conservation was reported and was zero unaccounted. One orphan paragraph (a marker-less opening note in the old archive file) parked at `log/UNSORTED.md` rather than guessed into a changeset. Two gaps in `GDD-VERSION-HISTORY.md` reported, not reconstructed: no CS018 entry (runs CS017 → CS019 directly, though CS018 shipped) and no CS014 entry (CS014 appears never to have existed). 18 live references across 11 `scratchpad/` files, plus 5 in the GDD and 1 in `DIFFICULTY-LEVERS.md`, repointed off the retired files; the 128 references inside `archive/` deliberately left untouched (historical accuracy, not live pointers). `_phase-ref.js`'s `outsideScope()` base gained `log/`. Suite re-confirmed green: 108/108, 0 skipped.

**CS027 P3 — the ~75-line global-count sweep: 28 files decoupled from hardcoded registry/lever/header counts, converted to `hasKnob`/`hasLever` presence-and-shape assertions or to `outsideScope(changed)`.** Per `archive/IMPLEMENTATION-PHASES-CS027.md` P3. Mechanical, phase-boundary-checked every 5 files rather than once at the end. `grep -rn "\.length,\? *=*=* *85" scratchpad/*.js` returns only `test-registry.js` afterward.

**CS027 P2 — `scratchpad/_harness.js`, `scratchpad/test-registry.js`, and `outsideScope()` in `_phase-ref.js`.** Per `archive/IMPLEMENTATION-PHASES-CS027.md` P2. `buildGame()` evaluates the RAW `<script>` block — proven, not asserted, byte-identical and behavior-identical to the inline idiom by `test-cs027-p2.js` §B. **Found: the suite's dominant two-regex comment-strip idiom is not a build path and must never become one** — run over the current build it deletes 80 live lines and *still parses*, because one line comment contains `/*` and the block-comment regex runs first (`asteroids-deluxe.html:6556`, `Achievements.weekKeyFor` and its neighbors). `execSource()` is a proper character scanner (strings/templates/regex-literals/both comment forms, one left-to-right pass) used only for tombstone greps, confined there and never in `buildGame()`'s path. `test-registry.js` centralizes the four global counts (85 registry entries, 9 headers, 18 levers, 5 `POWERUP_DROP_TYPES`) as the suite's one owner of each. `outsideScope(changed, extra)` replaces 15 files' hand-rolled "nothing else moved" allowlists.

**CS027 P1 — `scratchpad/run-all.js`, and the suite's true green state measured for the first time.** Per `archive/IMPLEMENTATION-PHASES-CS027.md` P1. Discovers every `scratchpad/test-*.js` (excluding `_`-prefixed helpers and `diag-*.js`), runs each in its own child process with a 120s per-file timeout, reports pass/fail/skip/timeout, wall time, and the five slowest files. Baseline: **106 files, 106 passed, 0 failed, 0 skipped, 0 timed out**, 28.43s total. No pre-existing failures — P2 proceeded without a repair detour (the gate FLAG-CS027-a set was clear).

**CS027 P0 — archived the CS026 planning pair, repointed 17 live bare-filename references to it.** Per `archive/IMPLEMENTATION-PHASES-CS027.md` P0. `PLANNED-FEATURES-CS026.md` / `IMPLEMENTATION-PHASES-CS026.md` moved to `archive/`; no game-code change, doc-path references only (two of the seventeen landed inside `asteroids-deluxe.html` comments — the fact TRAP-CS027-A's wording later missed, see the P6 entry above).

## CS027 close-out (measured, not targets)

*Relocated verbatim from `STATUS.md` by CS028 P1 — see item (5) above.*

| Metric | At `89a9a3a` (CS026 P6) | Target (`archive/PLANNED-FEATURES-CS027.md` §5) | Measured at close |
|---|---|---|---|
| Fixed session context (`CLAUDE.md` + `STATUS.md`) | ~164K tokens | < 12K | **~7.2K tokens** (CLAUDE.md 21.9 KB + STATUS.md ~6.8 KB = ~28.7 KB combined; ~4 chars/token estimate — the same rough method the §5 baseline used) |
| Files edited to add one registry knob | ~24 (~75 lines) | 1 | **4**: the game file, its own new test, `test-registry.js` (the one count owner), `STATUS.md`. The "1" target was specifically the count-sweep burden (`test-registry.js` now owns it alone, down from 22 non-owning files P3 decoupled) — the other 3 are inherent to shipping any knob, not sweep fallout. |
| Files edited to change how the build is loaded | 108 | 1 (for new tests) | **1** (`_harness.js`) for any *new* test file. The 105+ pre-existing files keep their own inline stub-eval idiom by design (FORK-A: no retroactive migration) — only 4 files use `_harness.js` today (`test-registry.js`, `test-cs027-p2.js`, `test-cs027-p6.js`, and `_harness.js` requiring itself doesn't count, so 3 consumers). |
| Suite pass state | unknown | known, and green | **Known and green: 109/109, 0 failed, 0 skipped**, confirmed by two consecutive full runs. |
| `STATUS.md` | 615 KB, 6 headings | < 40 KB, sectioned | **~6.7 KB, 49 lines, 8 headings** — well under target. |

Additional measurements item 6 asked for:
- **Repo-wide global-count assertion lines**: `grep -rn '\.length,\? *=*=* *85\b' scratchpad/*.js` returns exactly one line outside `test-registry.js` — `test-cs026-p6.js`'s own parent-commit setup literal (sanctioned: P3's rule 4, "parent-commit setup assertions stay"). Zero unsanctioned repeats.
- **Suite pass/fail and wall time vs. the P1 baseline**: P1 baseline (CS027 P1) was 106 files / 106 passed / 0 failed / 0 skipped / 28.43s. At close: **109 files / 109 passed / 0 failed / 0 skipped / 0 timed out**, ~28–31s (two runs, both green; wall time is not gated — counter-based budgets only, per the standing test rule). The 3-file growth is `test-registry.js`, `test-cs027-p2.js`, `test-cs027-p6.js` (`_harness.js` is excluded by its `_` prefix, same as always).
- **Two consecutive `run-all.js` runs, diffed**: non-empty diff, confined entirely to per-file wall-clock timings (one run showed a `-0.37s` reading for `test-cs025-p3.js`, almost certainly `Date.now()` clock jitter in this environment). Every PASS/FAIL line and the summary counts were byte-identical between runs. **Zero `SKIPPED (no git history)` occurrences in either run.**

⛔ Registry confirmed unmoved at **85**, `LEVERS` at **18** — `test-cs027-p6.js` §B measures both against the CS027 start point (`89a9a3a`), not just quotes them.

## Known issues (closed or superseded during CS027 — still-open items live in `STATUS.md`)

- **FLAG-CS027-d — twelve suite files grep a comment-stripped copy of the source missing the same 80 lines `execSource()` fixed.** Found at P2. Audited: no assertion any of them makes currently falls in the deleted region — latent, not live. Still open at close; carried into `STATUS.md`.
- `GDD-VERSION-HISTORY.md` had no CS018 entry (its bullets ran CS017 → CS019 directly) even though CS018 shipped, and no CS014 entry (CS014 appears never to have existed). Found and reported at P4, not reconstructed — writing a missing changelog entry would be composing history, not relocating it. No action pending; recorded here for the record.
- One paragraph — `archive/STATUS-HISTORY.md`'s own opening note explaining why the archive exists — carried no `CS0NN`/version marker of its own and was parked at `log/UNSORTED.md` rather than guessed into a changeset, per P4's own instruction not to guess an owner.

## GDD version history

CS027 (P0–P6) — the "engine room" changeset: `STATUS.md` cut to one page, global counts centralized to a single file, `CLAUDE.md` split into rules (stated) and rationale (relocated), and the closing-phase ritual itself rebuilt. No gameplay change.

**Build `1.0.0.27`.** Per `archive/PLANNED-FEATURES-CS027.md` §1: adding one registry knob used to mean editing ~24 files (~75 lines); the suite's true pass state was unknown; `STATUS.md` had grown to 615 KB across 6 headings; and `CLAUDE.md` mixed rules with the reasoning behind them, so a session paid for both on every read. All four are now measured fixed — see `STATUS.md`'s close-out table for the actuals.

**P0** archived the CS026 planning pair and repointed 17 references, two of them (unexpectedly) inside `asteroids-deluxe.html`'s own comments — the detail TRAP-CS027-A's wording missed until P6 measured it for real. **P1** built `scratchpad/run-all.js` and measured the suite's true baseline for the first time in the project's history: 106/106, 0 failed, 0 skipped. **P2** (Opus, xhigh) built the three load-bearing helpers — `_harness.js` (one shared `buildGame()`, proven byte-identical to the inline idiom every earlier test hand-rolled), `test-registry.js` (the one place a global count may be named), and `outsideScope()` — and found, by actually running it, that the suite's dominant comment-strip idiom silently eats 80 lines of the current build and still parses; it was never in a build path and `_harness.js` does not put it in one. **P3** swept 28 files off hardcoded counts onto `hasKnob`/`hasLever`. **P4** relocated two large historical documents (`archive/STATUS-HISTORY.md`, `GDD-VERSION-HISTORY.md`) into per-changeset `log/CS0##.md` files via a conservation-checked script, and cut `STATUS.md` itself to one page for the first time since it started growing. **P5** (Opus, xhigh) rewrote `CLAUDE.md` down to rules-only with two markers (`⛔ INVARIANT`, `⚠ SETTLED`) and relocated the reasoning to `RATIONALE.md`, verifying by mechanical extraction that all 73 bolded rules from the prior file survive somewhere. **P5b** used the new markers for the first time in anger, reclassifying `destroyHunter()`'s lever status and recording the tiebreak the exercise established. **P6** closed the changeset: `GAME_VERSION` → `1.0.0.27`, TRAP-CS027-A measured (and found the plan's own "exactly one line" claim undercounted by two, both legitimate P0 diffs), two consecutive suite runs diffed and found deterministic in outcome, and the doc sweep ran for the first time under the new ritual this changeset built. **No lever moved, the registry holds at 85, `LEVERS` at 18** — CS027 changed how the project is built, not what it plays.
