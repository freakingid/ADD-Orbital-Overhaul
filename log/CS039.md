# CS039 — Telemetry instrumentation: five counters, thirteen columns, a lever fingerprint

Full P1–P4 build log plus GATE T, moved out of `STATUS.md` at the CS039 closing phase per the
standing per-changeset log convention (`CLAUDE.md`). Baseline `1e76b51` ("CS038 P7 closed"),
`GAME_VERSION` `1.0.0.38` → **1.0.0.39**. Registry unchanged at **104** under **11** headers,
`LEVERS` unchanged at **18** — CS039 adds no registry row, confirmed against the live
`scratchpad/test-registry.js` `COUNTS` rather than assumed. Companion doc
`IMPLEMENTATION-PHASES-CS039.md` moves to `archive/` at this same closing phase (there was no
`PLANNED-FEATURES-CS039.md` — the implementation doc carried its own spec inline, per its own
header note). Suite closes at **zero skips**; counts in the closing session summary.

Three code phases plus a gate, all touching only the telemetry instrument — no gameplay change:
five new per-run counters on `game.stats` (P1), thirteen new telemetry columns and one envelope
bump (P2), a `#`-comment lever fingerprint prepended to the CSV export (P3), and GATE T's first
real capture, which caught a wrong claim about one of P2's new columns and forced a fourth,
unplanned column plus a second envelope bump before the changeset could close.

---

## §0 — Corrections to the source suggestions

The five instrumentation suggestions this changeset started from came out of an analysis session,
not a repo read. Three of them were wrong about the build. Recorded here so they are not silently
"fixed" back:

1. **"the three kill counters already in the CS033 leaderboard payload" — wrong, there are two.**
   `Leaderboard.submit()` sends `wave_reached`, `canisters_delivered`, `saucer_kills`,
   `debris_destroyed`. `hunter_kills` is registered on the Worker side and **deliberately
   unsent**, with the reason written into the comment above `LEADERBOARD_ENDPOINT`: no per-game,
   player-only Hunter-kill counter exists. `hunterLineageKills` resets per lineage,
   `Achievements.lifetime.hunterKills` is cross-game, and `largeHunterKills` counts large cores
   only. So a Hunter-kill telemetry column requires a **new counter**, not a reuse. See FORK-A.

2. **"`canistersDelivered`" — the field is `game.stats.delivered`.** `canisters_delivered` is the
   wire name in the leaderboard payload only. Do not introduce a second name.

3. **"cumulative `REPAIR_FULL_BONUS` and `SCOOP_MAX_BONUS` totals — two lines" — accurate, but
   note the asymmetry.** `REPAIR_FULL_BONUS` is added with a bare `game.score += …` inside
   `addScore()` itself; `SCOOP_MAX_BONUS` goes through `addScore(SCOOP_MAX_BONUS)` from
   `applyPowerup()`, so it can itself trip a repair milestone. Both counters are still one line
   each, but they are not symmetric code and the comments must say so.

Also confirmed as **not** needing work: new flat number fields on `game.stats` are carried
through save/resume for free. `buildSaveEntry()` spreads `game.stats`, and `resumeFromSave()`
runs a known-key type-matched `for (const k in game.stats)` loop. Nested objects need
special-casing; flat numbers do not. Every counter added in P1 is a flat number, deliberately.

## §1 — Forks: RESOLVED

All seven resolved by Paul, at the recommended default, before P1. Recorded here with the
refused options and their reasons, so nothing below can be re-proposed as an open question
without acknowledging the record.

**FORK-A — a per-game, all-tier Hunter kill counter. → YES.**
Added `game.stats.hunterKills`, incremented in the same `awardScore`-gated block that already
increments `hunterLineageKills` / `Achievements.lifetime.hunterKills`. Hunters did 78% of the
damage in the analysed run and there was no way to see how many the player killed.

**FORK-A.1 — also send `hunter_kills` to the leaderboard? → NO, not this changeset.**
The CS033/CS034 payload is settled ground and the counter's first job is offline analysis;
sending it is a separate, deliberate decision with a public-board consequence. P1's comment
records the counter exists for telemetry and that wiring it into `Leaderboard.submit()` is an
open, un-taken decision. `Leaderboard.submit()` and `makeRunResult()` are untouched by the whole
changeset.

**FORK-B — hits: one total, not ten per-source counts. → ONE TOTAL, `game.stats.hitsTaken`.**
The ten `dmgFrom*` sums plus one hit total lets you compute mean damage per hit and detect the
moment reconstruction breaks; ten more columns nearly doubles the damage family for a resolution
nobody has asked for yet. **Refused option recorded:** per-source hit counts. Reopen this fork —
and only this fork — if a damage multiplier lands, or if two sources ever come to share a damage
magnitude.

**FORK-C — per-game `game.stats.deliveryScore`. → IN SCOPE.**
Only `Achievements.lifetime.deliveryScore` existed. One line beside it at the dock payout site —
the single most direct fix for "score conflates deliveries, kills and refund bonuses." A sixth
suggestion beyond the original five, admitted deliberately.

**FORK-D — the lever fingerprint's carrier. → `#`-PREFIXED COMMENT LINES** above the CSV header,
one `key=value` per line. Pandas, R and most sheet importers skip `#` lines with one argument, so
the CSV stays a CSV. **Refused options recorded, with reasons:** a separate clipboard block (gets
separated from its data the first time someone pastes into two files); extra columns (repeats one
constant 400 times); a separate export action row (two things to remember to press).

**FORK-E — edited values or effective values? → EFFECTIVE**, i.e. what `DEBUG` actually resolves
to, plus one explicit `overrides=ON|OFF` line. With the master overrides toggle OFF, every
gameplay knob reads its own `def` no matter what the panel displays; a fingerprint listing
shown-but-inert edits would be actively misleading and would be believed. P4 pins this in
`CLAUDE.md` with a ⚠ SETTLED marker, because "surely it should show what the panel shows" is
exactly the wrong instinct here.

**FORK-F — fingerprint contents. → `GAME_VERSION`, `overrides`, the resolved `telemetryInterval`,
the row count, the source, and the lever list. NO PROFILE NAME OR ID.**
**Refused option recorded:** profile identity. A telemetry log gets pasted into chats and files;
it does not need to identify a player, and the analysis never reads it.

**FORK-G — the cheap extra columns. → FIRST THREE IN, LAST THREE OUT.**

| Field | What it measures | Decision |
|---|---|---|
| `cargoDamageEvents` | unguarded chain severs — cargo actually lost | **IN** — the tow loop is the game, and this is its failure counter |
| `hunterCoalesced` | Hunters born from neglected scrap | **IN** — the Kessler premise, currently invisible in the log |
| `deflects` | hits absorbed by the shield | **IN** — explains HP that *didn't* get taken |
| `largeHunterKills` | large cores only | **OUT** — achievement-shaped; `hunterKills` covers the analysis need |
| `smallSaucerKills` | small saucers only | **OUT** — same |
| `bestCombo` / `pacifistBest` | achievement maxima | **OUT** — not rates, not states |

---

## P1 — Five new per-run counters (no output change)

`game.stats.hunterKills`, `hitsTaken`, `deliveryScore`, `scoreRepairBonus`, `scoreScoopBonus` —
each a flat number, each incremented at exactly one site (`destroyHunter`'s `awardScore`-gated
block; `damageShip`'s HP-deducted path, same side of the shield/i-frame early return as the
`dmgFrom*` attribution switch; the dock delivery block beside
`Achievements.lifetime.deliveryScore`; `addScore()`'s full-HP milestone arm; `applyPowerup()`'s
scoop-maxed arm). Nothing reads them this phase — no telemetry column, no HUD, no leaderboard, no
achievement — deliberately invisible in play and in every export. Verified against save/resume
with no edit to either `buildSaveEntry()` or `resumeFromSave()`.

## P2 — Telemetry schema extension, one shape, one envelope bump

Thirteen new `TELEMETRY_FIELDS` entries appended ahead of the trailing `debugRun`/`resumedRun`
flags: `chainLen`, `cargoMax` (instantaneous state), then eleven cumulative counters —
`delivered`, `deliveryScore`, `cargoDamageEvents`, `debrisKills`, `hunterKills`, `saucerKills`,
`hunterCoalesced`, `deflects`, `hitsTaken`, `scoreRepairBonus`, `scoreScoopBonus`. Envelope bumped
`v: 1 → 2` in both `read()` and `write()`, so a stale v1 blob resolves to an empty buffer rather
than exporting `"undefined"` in thirteen columns. `afd_telemetry_v1`, the storage key, is
unchanged — the `_v1` suffix and the envelope `v` are different things.

## P3 — Lever fingerprint on the export

`telemetryHeaderLines()` prepends seven `#` lines to the CSV export: `orbital-overhaul telemetry
v2`, `build=`, `overrides=ON|OFF`, `telemetryInterval=`, `rows=`, `source=this run|storage`,
`levers=`. The `levers=` line walks `DEBUG_ENTRIES` through `debugNative()` — the same resolution
`rebuildDebug()` uses — so it reports what the game is actually using, not what the panel
displays; with overrides OFF it correctly reads `levers=none` regardless of what's been edited. A
`sessionSwitch` row (`telemetryCapture`) is exempt from that rule by design and reports its own
live value. Float comparisons use an epsilon, not `!==`, so a toNative round-trip doesn't read as
an edit. Both the clipboard and download-fallback paths in `copyTelemetry()` receive
byte-identical text.

## GATE T — capture a run, re-run the analysis

Not a code phase. First real capture: `LEVEL-5-TELEMETRY.csv`, 53 rows, waves 1–5, 795 s, final
score 133,185. Header `overrides=ON` / `levers=telemetryCapture=1` confirmed the run's tuning was
stock. Findings:

- **T1 — PASS, FORK-D holds.** 43 columns matching `TELEMETRY_FIELDS` exactly; sheets/Excel land
  the seven `#` lines as single-cell rows above the header (a delete-rows step, not a choke); no
  carrier change needed.
- **T2 — the pre-changeset ~14% delivery-income estimate is KILLED; measured 3.75%** for
  `scoreRepairBonus` specifically. `deliveryScore` 26.15%, `scoreRepairBonus` 3.75%,
  `scoreScoopBonus` 0.38%, residual 69.72% — overwhelmingly Hunter kills (~56% of the whole run's
  score), against 26% from delivery. Delivery is a minority income stream in this run.
- **T3 — EXACT.** `hitsTaken` (28) reconstructs exactly from the ten `dmgFrom*` sums divided by
  their damage constants. P1's counter is on the correct side of the shield/i-frame/lethal early
  returns.
- **T4 — NO, and the sign points the other way.** Mean tow length rises across waves 1–5 (3.0 →
  5.6) rather than collapsing; score rate tracks Hunter kills (ρ = +0.82 whole run) far more than
  chain length (ρ = −0.09). The worst scoring window has cargo sitting near the cap and *not
  moving* under hull pressure — a delivery drought, not a tow collapse. n=1, wants a second log.
- **T5 — FIRING, and accelerating.** Hunter coalescence rate rises monotonically per wave (0.67 →
  4.33/min across waves 1–5, wave 5 at 6.5× wave 1); ~76% of each 13-kill lineage is cleared.
  Hunters are also the dominant damage source (65% of all HP lost) and the dominant score source
  (~56%).

### T-DEFECT — `cargoDamageEvents` was never cumulative. Fixed at the gate.

The one integrity check that failed: 7 decreases in 53 rows, last row 0. `cargoDamageEvents` is
the chain-guard drop-weight **pity** counter, zeroed in `dropPowerup()` when a guard is *selected
to drop* (CS035 P6 / FORK-T) — it counts severs since the last guard drop, not the run. It had
been described as a monotone cumulative counter in **four places at once**: P2's spec (§P2 step
2, left as originally written — the record of what was specified, not corrected in place, so the
propagation is visible), the build's own comment above `TELEMETRY_FIELDS`, `test-cs039-p2.js`'s
§F (which drove the counters by hand and never reached the reset path), and
`TELEMETRY-ANALYSIS-GUIDE.md` §3.

**Applied at the gate** (Paul's call, retired here from `DECISIONS.md` per that file's own rule):

> ## CS039 GATE T — the telemetry envelope goes to v:3, overriding a P2 ⛔
>
> **Decided:** 2026-08-20, Paul's call, at GATE T (not a code phase, so no phase prompt covered
> it).
>
> GATE T's first real capture showed `cargoDamageEvents` decreasing 7 times in 53 rows. It is the
> chain-guard drop-weight **pity** counter, zeroed in `dropPowerup()` when a guard is *selected to
> drop* (CS035 P6 / FORK-T) — so it counts severs since the last guard drop, not the run, and the
> last row of a log routinely reads 0. It had been described as a monotone cumulative counter in
> four places at once: the P2 spec (§P2 step 2), the build's own comment above
> `TELEMETRY_FIELDS`, `test-cs039-p2.js`'s §F, and `TELEMETRY-ANALYSIS-GUIDE.md` §3. The test
> passed anyway because §F drove the counters by hand and never reached the reset path.
>
> **Decided:** add `game.stats.cargoSevers`, a genuine cumulative run total incremented on the
> line below `cargoDamageEvents++`, and emit it as a 44th telemetry column beside the pity
> counter. Both ship: `guardDropWeight()` needs the pity value, an analysis needs the total.
> Correct the other three sites; add `test-cs039-p2.js` §H, which drives the real
> `breakChain`/`dropPowerup` paths.
>
> **And:** take the persistence envelope to **v:3**, which overrides P2 step 3's ⛔ *"Any further
> row-shape change inside CS039 must reuse v2, not add v3. One shape per changeset."*
>
> **Why the override.** That ⛔ and P2's *reason* for versioning at all point opposite ways, and
> the reason won. P2 bumped v1 → v2 for one stated purpose: a stale blob missing the new keys
> makes `telemetryCSV` emit the literal string `"undefined"`, and *"silently dropping a stale run
> beats exporting a corrupt one."* A 44th column under a reused `v: 2` recreates precisely that
> failure — and unlike when the rule was written, a v:2 blob existed in the wild by then (GATE
> T's own capture, sitting in localStorage). The "one shape per changeset" line was written before
> anyone knew a column would be needed *after* P3 shipped; it never contemplated this case. ⛔ The
> **key name is untouched** — still `afd_telemetry_v1`; the `_v1` in the key and the `v:` in the
> envelope are different things, and P2 §3's rule on that stands.
>
> **What would change the answer:** nothing about `cargoSevers` — it is measured, not a judgment
> call. The envelope override would have gone the other way if a captured log had still been
> sitting unextracted in localStorage and mattered; it did not (already exported to
> `LEVEL-5-TELEMETRY.csv`), so dropping the stale recovery copy cost nothing.

Concretely: `game.stats.cargoSevers` (44th telemetry column, immediately after
`cargoDamageEvents`); envelope `v: 2 → 3`; `test-cs039-p2.js` §H, pinning `guardPicked` does not
move when the pity counter zeroes; `TELEMETRY-ANALYSIS-GUIDE.md` gained a third "neither
instantaneous nor cumulative" kind in §3, a §7 trap, a §2.3 monotonicity exception, and a v2
lower-bound reconstruction recipe for logs captured before this change.

**Not done, carried forward:** the gate's own log is waves 1–5, not the wave 10+ the gate asked
for, and is a v2 capture (no `cargoSevers`). A second, deeper log on the v3 build would turn T4's
n=1 finding into something actionable.

## P4 — Doc sweep, version bump, changeset close

`GAME_VERSION` `1.0.0.38` → **1.0.0.39**. `STATUS.md` rolled forward and pruned back to roughly
the last three changesets. `CLAUDE.md` gained a `### Telemetry (CS039)` pin (`TELEMETRY_FIELDS` as
the one source of truth for row shape and column order; row-shape changes bump the envelope `v`,
never the key name; the fingerprint reports effective, not edited, lever values — ⚠ SETTLED) and a
Save data pin for the sixth frozen-adjacent key, `afd_telemetry_v1` (additive, not frozen, lazy —
the doc debt CS037 P4 flagged and two changesets carried). GDD §2 was checked and left untouched:
telemetry is a dev instrument, not shipped player-facing behavior, and the GDD's only existing
mention of it (the `telemetryCapture` `sessionSwitch` mechanism, under the debug-registry
material) already covers the one player-visible consequence — nothing else about the feature
belongs in a shipped-behavior document. `IMPLEMENTATION-PHASES-CS039.md` archived. Suite counts in
the closing session summary.

---

## GDD version history

CS039 (P1–P4, GATE T) — Telemetry instrumentation: five new per-run counters on `game.stats`
(Hunter kills across all tiers, non-lethal hits taken, delivery score, and the two score-refund
bonuses), thirteen new telemetry columns built from them plus `chainLen`/`cargoMax` (envelope
`v: 1 → 2`), and a `#`-comment lever fingerprint prepended to the CSV export reporting the build
version, override state, capture interval, row count, source, and every non-default *effective*
lever value. **GATE T**, the first real capture, caught `cargoDamageEvents` misdocumented as
cumulative in four places at once — it is a pity counter that resets on each guard drop — and
added a genuine cumulative counterpart, `game.stats.cargoSevers`, as a 44th column, taking the
envelope to **v: 3** (a deliberate, reasoned override of P2's own "one shape per changeset" rule).
The gate's analysis also killed a standing estimate (delivery/refund income was ~14% of score; the
first real measurement puts the refund share at 3.75%, with Hunter kills the dominant score
source at ~56%) and found the Kessler coalescence loop firing and accelerating. Registry and
`LEVERS` unmoved at **104**/**18** — no gameplay change anywhere in the changeset. Suite counts in
the closing session summary.
