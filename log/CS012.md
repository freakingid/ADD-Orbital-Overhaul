# CS012

**Prior session (CS012 P5):** implemented CS012 Phase 5 — the **auto-shield** difficulty option (`IMPLEMENTATION-PHASES-012.md` P5): a reactive last-resort save at critical hull, **default OFF**. **The load-bearing part was the `damageShip` hook** — a block at the TOP of `damageShip`, *before* the `if (s.dead || s.shieldOn || s.invuln > 0) return false;` early-return: when `settings.autoShield` is on and `!s.dead && !s.shieldOn && s.invuln <= 0 && s.hp <= LOW_HP_THRESHOLD && s.energy >= SHIELD_HIT_COST`, it spends `SHIELD_HIT_COST` energy, applies the **same knockback (copied `shortDelta`/`atan2`/`KNOCKBACK_SPEED` body) + `s.invuln = HIT_STUN_DURATION`** a real hit would, sets `s.shieldOn = true`, deducts the new `AUTO_SHIELD_SCORE_PENALTY` (500) **directly from `game.score` (clamped ≥ 0) — bypassing `addScore`, never touching `game.nextRepair` (FLAG-4e)** — pushes a `COLOR.lowhp` "-500" floater, and returns `false` (0 HP dealt; same contract as the shielded early-return). **The stun i-frame is what rate-limits auto-saves to ~one per second** (same-frame follow-ups hit the raised-shield early-return; subsequent-frame hits are absorbed by the i-frame until it lapses), and since recharge (0.12/s) < cost (0.22/save) the energy pool self-exhausts after ~4–5 back-to-back saves — a deliberately expensive safety net a healthy run never notices. **What landed:** (1) `const AUTO_SHIELD_SCORE_PENALTY = 500;` by the `SHIELD_*` constants (PLAYTEST KNOB, FLAG-4d). (2) `settings.autoShield = false` default; `saveSettings` writes it; `loadSettings` accepts it only if `typeof data.autoShield === "boolean"` (additive — no schema bump, same rule as `captions`). (3) the `damageShip` hook. (4) `DIFFICULTY_ROWS` → `["shot","magnet","autoshield","back"]`; `menuDifficulty` gained an `autoshield` left/right branch (`settings.autoShield = a === "right"; saveSettings();`). (5) `drawDifficulty` added the "Auto-shield: Off | On" row, grew the panel (360 → 418), added a **per-row help line** for the focused row ("Auto-raises shield at critical hull. -500 points per blocked hit."), and shifted Back/help/hint down. **`GAME_VERSION` bumped `"1.0.0.11"` → `"1.0.0.12"`** (last player-visible phase); `test-cs010-p0.js`'s pinned literal updated. **New test `scratchpad/test-cs012-p5.js`** (48 assertions, drives the REAL `damageShip`/`saveSettings`/`loadSettings`/`menuDifficulty`): OFF baseline = full damage; ON = 0-HP save with energy/score/stun/knockback/floater and `nextRepair` untouched + score clamp at 0; same-frame no-op + i-frame absorption; energy self-limit; above-threshold + dead-ship = no save; `afd_settings_v1` round-trip (missing/non-boolean → false, default false); `DIFFICULTY_ROWS`/`menuDifficulty` toggle both ways + persist; headless `startGame()`/`update(1/60)` no-crash. **One pre-existing test updated** — `test-p5.js` section I (its Difficulty navigation assumed Back at index 2; the inserted auto-shield row moved Back to index 3, so it now steps through the new row and toggles it). **Full regression green: all 49 test files**, `node --check` clean. Docs: GDD §2.3 (auto-shield mechanic), §2.7 (the sanctioned `addScore` bypass), §2.9 (Difficulty row + per-row help; Persistence + `autoShield`), Architecture Map (`damageShip` hook, Difficulty rows), top-of-file Current-build → CS012 P5/"1.0.0.12"; CLAUDE.md (scoring non-negotiable's documented exception + frozen-keys `autoShield`); GDD §7 → `GDD-VERSION-HISTORY.md` consolidated CS012 (P1–P5) entry appended. Did NOT push. **Changed this session (P1–P5 recap):** P1 UFO accuracy knob (`SAUCER_ACCURACY_RAMP_SCALE`), P2 HUD segmented Scoop ring + fixed 5-row powerup stack, P3 Maxed-Out achievement + MAX HAUL celebration, P4 menu-IA collapse (Options sole hub, `achReturn` retired), P5 auto-shield option.

**Prior session (CS012 P4):** implemented CS012 Phase 4 — the menu information-architecture refactor (`IMPLEMENTATION-PHASES-012.md` P4, turning `PLANNED-FEATURES-CS012.md` FORK-CS012-C → (a) into code): collapse the title/gameover system-menu root so **O/Start/B open OPTIONS directly**, keep the paused-mid-game root as Continue/Options/Quit, make Achievements live ONLY under Options, and retire both `MENU_ROOT_SYS` and the `achReturn` tracker. **The load-bearing risk was the BACK PATH** (get "Back from Options" wrong and title/gameover navigation dead-ends) — tested in both entry contexts. Re-grepped every anchor first (they drift from the prompt's estimates): `openPause` (prompt ~L2282, actual L2286), the O-key open (L2126) + pad-B (L2186/2189), `MENU_ROOT_PLAY`/`MENU_ROOT_SYS`/`rootItems` (L2239/2242/2243), `menuRoot` (L2344, its Achievements-`achReturn`="root" branch L2353 + system "Back" L2355), `menuOptions` (L2363, Achievements-`achReturn`="options" L2370 + the two "Back" branches L2373/2375), `menuAchievements` (L2420, the `achReturn` conditional L2425), the `game.menu` literal (L3496) + `startGame` reset (L3566). **What landed (exactly the edits the prompt scoped):** **(1)** `openPause()` — `game.menu.screen = game.state === "playing" ? "root" : "options"` (title/gameover route straight to Options; rest unchanged). **(2)** `MENU_ROOT_SYS` deleted; `rootItems()` is now just `return MENU_ROOT_PLAY`. **(3)** `menuRoot()` lost its Achievements branch (which set `achReturn="root"`) and its system-menu "Back" branch — both only ran for the retired system root; it's now Continue → `closePause`, Options → `gotoScreen("options")`, Quit → `quitToTitle`. **(4)** `menuOptions()` "Back" (BOTH the confirm-label branch AND the `a==="back"` branch) is now context-aware: `if (game.state === "playing") gotoScreen("root", rootItems().indexOf("Options")); else closePause();` — paused mid-game returns to root, title/gameover (where Options is top-level) closes the overlay; the `achReturn="options"` assignment on the Achievements row was dropped. **(5)** `menuAchievements()` back always `gotoScreen("options", MENU_OPTIONS.indexOf("Achievements"))` — single parent, no `achReturn` read. **(6)** `achReturn` dropped from the `game.menu` literal AND the `startGame` reset. Plus the stale-comment cleanup the FORK implies: the O-key / pad-B / `back`-binding comments (they said "open the system menu" / "picks the system-menu root"), the `menuRoot`/`menuOptions`/`menuAchievements`/`menuHighScores` in-code comments, the two-context-root constant-block comment, and the title-overlay draw comment (`(Options/Achievements)` → generic) all updated — the intentional retire-note comments are the ONLY place `MENU_ROOT_SYS`/`achReturn` survive in the game file (verified: no `const MENU_ROOT_SYS`, no `.achReturn`). **Left as-is per the prompt (harmless):** `drawRootMenu`'s `"PAUSED"`/`"MENU"` title ternary (root is play-only now, so the `"MENU"` branch is unreachable dead text) and `drawOptionsMenu`'s `"OPTIONS"` title; `MENU_OPTIONS` order untouched. **New test `scratchpad/test-cs012-p4.js`** (33 assertions, drives the REAL open/nav handlers — `openPause`/`menuInput`/`closePause`/`rootItems` — no reimplementation): (A) `node --check` + source-grep that `const MENU_ROOT_SYS` and `.achReturn`/`achReturn:` are gone; (B) `rootItems()` returns only `[Continue,Options,Quit]` in every `game.state`; (C) title → `openPause()` lands on `"options"` (NOT `"root"`), Back closes the overlay to title; (D) gameover → same; (E) playing → `openPause()` → `"root"`, Options → `"options"`, Back → `"root"` (still paused, NOT closed), Back again resumes; (F) Achievements reached ONLY via Options from BOTH title and pause, its Back always → Options with the cursor on Achievements; (G) `AudioSys.ctx` null → `startGame()`/`update(1/60)` + a full title open/nav/close cycle never throw. **Four pre-existing tests updated — legitimate consequences of the IA change, not bugs:** `test-p4.js` (the v3.0 P4 system-menu test — dropped the `MENU_ROOT_SYS` import; sections C/D rewritten so O/B open Options directly and Back closes; the old root→Achievements path became Options→Achievements; section I's `achReturn==="options"` assertion dropped); `test-cs010-p4.js` (dropped the `MENU_ROOT_SYS` import + its two shape asserts, replaced with a `MENU_ROOT_PLAY` shape assert + a `!/const\s+MENU_ROOT_SYS/` source check; sections C / D-title-path / F rewritten for direct-to-Options entry + single-parent Achievements); `test-v36-scores.js` (dropped the `MENU_ROOT_SYS` import + assert; section J path-1 rewritten for O→Options-direct); `test-v34-p6.js` (section F's "Options 'Back' → root" now pins `game.state="playing"` via save/restore, since Back is context-aware and that section runs in the title context). **Full regression re-run green: all 48 test files** (47 prior + this session's new file), `node --check` clean. **No version bump** — `GAME_VERSION` stays `"1.0.0.11"` (an IA/navigation refactor, no new mechanic; the phase doc leaves a bump to Paul, recommended at P5). Docs: GDD §2.9 (control-scheme bullets — "Open OPTIONS" now, Options the sole hub; the entry/dying-inert "O/B open Options" phrasings; the context-aware-inputs note), GDD §2.16 (the State-machine `achReturn`-field bullet + the "Opened from any state" two-context-root bullet fully rewritten for the single hub + context-aware Back), GDD Architecture Map (game-object row: `game.menu` `achReturn` "added v3.0 P4, retired CS012 P4"; Menu/Options/Rebinding row: `rootItems()` returns only `MENU_ROOT_PLAY` + the collapsed-root note; draw() row: the "MENU" title branch now unreachable; High-Scores §-Rendering reachability note), GDD top-of-file Current-build line (CS012 P3 → CS012 P4), this file. **CLAUDE.md checked — it does not enumerate the menu tree nor name `achReturn`/`MENU_ROOT_SYS`, so nothing to update there** (grep clean). `PLANNED-FEATURES-CS012.md` / `IMPLEMENTATION-PHASES-012.md` left as-is (FORK-CS012-C was already the planning source; consistent with the P1–P3 convention of not trimming a grab-bag changeset's planning docs phase-by-phase). Did NOT push (Paul's call, every phase).

**Prior session (CS012 P3):** implemented CS012 Phase 3 per the phase prompt (`IMPLEMENTATION-PHASES-012.md` P3, turning `PLANNED-FEATURES-CS012.md` §3 / FORK-CS012-B → (a) into code). Re-grepped every anchor first (they drift): `resetGameStats()`'s `fullChainVisit: false,` line (actual L3459), the dock-offload block's `if (game.deliveryCount === 12) { ... }` latch (actual L4849) and the emptying-pop `if (game.chain.length === 0) VoiceSys.dockDelivery(...)` (actual L4866), `CARGO_CAP_MAX` (actual L332), the LIFETIME achievement pool + how existing non-tiered entries are shaped (actual L3932-3957), `FloatText` (actual L3398), `COLOR.ach`, and `game.cargoFlash`/`HUD_CAP_FLASH` (actual L4819). **What landed (exactly the four edits the prompt scoped, nothing else):** **(1)** a new per-game stat `game.stats.maxChainVisit = false` added beside `fullChainVisit` in `resetGameStats()`. **(2)** a one-line latch beside the existing `deliveryCount === 12` block: `if (game.deliveryCount === CARGO_CAP_MAX) { game.stats.maxChainVisit = true; ...celebration... }` — keyed on the **constant** `CARGO_CAP_MAX` (24), not the runtime `game.cargoMax`, per FLAG-CS012-3a, so it stays a fixed target even if the cap is ever raised. **(3)** one new LIFETIME (not weekly — the 16-slot weekly pool's `%16` rotation math must not grow) achievement in the pool: `{ id: "max_haul", name: "Maxed Out", desc: "Deliver a full 24-canister chain in one dock visit.", goal: 1, cur: s => s.maxChainVisit ? 1 : 0 }` — id/name/desc are explicitly marked as placeholders for Paul to finalize (FLAG-CS012-3b, not resolved this session). **(4)** the celebration, same instant as the latch: `FloatText` gained an optional trailing `size` constructor param (default `16`, so all eight pre-existing call sites are byte-identical) — a `FloatText("MAX HAUL", game.dock.x, game.dock.y - 22, COLOR.ach, 24)` pushes alongside the normal delivery floater, and `game.cargoFlash = HUD_CAP_FLASH` arms the same gold CARGO-ring pulse the tow-cap-up reward already uses (§2.10.2) — no new rendering machinery, per FINDING-C1's best-guess. **Deliberately NOT added:** any `dock_max` voice line — Dan's existing `dock_20` bark already covers any 20+ haul (`VoiceSys.dockDelivery`, unmodified); the dedicated max-haul line is deferred to a later changeset (FORK-CS012-B → (b), needs a `tools/voice-robot-lab.html` pass CS012 was scoped to avoid). **New test `scratchpad/test-cs012-p3.js`** (31 assertions, drives the REAL dock-offload path via `update()` + `Achievements.evaluate()`/`init()` — no reimplementation of the latch/offload logic, mirrors `test-f9.js`'s `fillChain`/`tickDock`/`parkOnDock` idiom): (B) `resetGameStats()` leaves `maxChainVisit` false; (C) a real 24-canister dock visit sets `maxChainVisit` true exactly once (confirmed it stays true if the same visit keeps delivering past 24), a 23-canister visit does NOT set it, and a 12-canister visit sets `fullChainVisit` but not `maxChainVisit`; (D) `Achievements.evaluate()` unlocks `max_haul` (confirmed registered as `pool === "lifetime"`) once the flag is set, and the unlock survives a `resetGameStats()` (which clears the per-game flag) followed by `Achievements.save()` + a fresh `Achievements.init()` (the real reload path) — true lifetime persistence, not just in-memory state; (E) the celebration fires (exactly one `"MAX HAUL"` floater in `COLOR.ach` at `size > 16`, `cargoFlash` armed and `<= HUD_CAP_FLASH`) only on the 24th delivery, never on a 12- or 23-canister visit; (F) exactly one `VoiceSys.say()` call across a full 24-delivery visit and it's the pre-existing `dock_20` tier, not a new max-haul-specific line — isolated from the unrelated deliveryCount===10 SALVAGE BONUS powerup-drop mechanic (which the long run also crosses and would otherwise confound the count with its own `collect_*` line) by clearing `game.powerups` each `tickDock` tick before it can be picked up; (G) `AudioSys.ctx` null → `startGame()`/`update(1/60)` don't crash. **One pre-existing test needed updating, a legitimate consequence of this change, not a bug:** `test-f9.js`'s config assertion hardcoded `Achievements.LIFETIME.length === 19` — updated to `20` (message text too). **Full regression re-run green: all 47 test files** (46 prior + this session's new file), `node --check` clean. **No version bump** — `GAME_VERSION` stays `"1.0.0.11"` (additive achievement + UI flourish, not a new mechanic, per the phase prompt's own note). Docs: GDD §2.17 (header gained "max-cap-haul CS012 P3"; pool counts 19→20 lifetime / 35→36 total everywhere they're stated; the "Six stay single-goal" list gained Maxed Out with its full latch/celebration/deferred-voice-line writeup; the stat-hooks bullet's dock-offload parenthetical gained the new latch), GDD Architecture Map (`Achievements` row: LIFETIME 19→20, tiered "14 of 20", the new stat + celebration hook noted; `Entity classes` row: `FloatText`'s new optional `size` param noted), GDD top-of-file `Current build` line (was still reading `CS011 P5` — corrected to `CS012 P3`, folding in the untouched-till-now P1/P2 bump), this file. `PLANNED-FEATURES-CS012.md` §3 and `IMPLEMENTATION-PHASES-012.md` P3 left as-is (FORK-CS012-B already marked ✅ RESOLVED → (a) from the planning pass — matches this repo's established convention of not trimming a grab-bag changeset's planning doc phase-by-phase, per FORK-CS012-A/§1-§2's same treatment after P1/P2 shipped). CLAUDE.md's achievement-pool note was checked — it doesn't enumerate counts, so nothing to update there. **No FLAGs resolved this session beyond what the phase prompt already locked** (FLAG-CS012-3b — the achievement's id/name/desc — remains an open placeholder for Paul). Did NOT push (Paul's call, every phase).

**Prior session (CS012 P2):** implemented CS012 Phase 2 per the phase prompt (`IMPLEMENTATION-PHASES-012.md` P2). Re-grepped every anchor first (they drift): `drawHUD()` (~L5573, actual L5575), the powerup-row loop (~L5688-5742, actual L5691-5745 pre-edit), the scoop-pip block (~L5745-5757, actual L5751-5760 pre-edit), `drawRingArc` (~L2692, actual L2694), `HUD_FX_BASE_Y`/`HUD_FX_ROW_H`/`HUD_FX_RING_R` (L261-263), `SCOOP_MAX_LEVEL` (~L470, actual L472), `POWERUP_DROP_TYPES` (~L419, actual L421). **What landed (exactly the three edits the prompt scoped):** **(1)** a new `drawRingSegments(x, y, r, segs, filled, litColor, dimColor)` helper beside `drawRingArc` — draws `segs` arc wedges separated by a new playtest-knob constant `HUD_RING_SEG_GAP` (0.12 rad, placed with the `HUD_FX_*` group); wedges `0..filled-1` glow-stroke in `litColor` (same `HUD_RING_W`/`HUD_RING_BLUR` as every other ring arc), the rest are a plain dim stroke at `HUD_RING_TRACK_W` — no fills, never `closePath()`'d. **(2)** the SCOOP pip block is replaced by a segmented-ring row at the powerup stack's row 0 (`y = HUD_FX_BASE_Y`) — `drawRingSegments(40, HUD_FX_BASE_Y, HUD_FX_RING_R, SCOOP_MAX_LEVEL, game.scoopLevel, POWERUP_COLOR.scoop, COLOR.dim)` plus a `SCOOP` label + the level number, reusing the powerup row's `drawText` x/y offsets (`64, y±`) — and the `if (game.scoopLevel > 0)` guard is gone, so the row is ALWAYS drawn (dim track, 0 lit segments at level 0). **(3)** the powerup-row loop is rewritten from `for (const t of POWERUP_DROP_TYPES) { if (!powerActive(t)) continue; ... slot++ ... }` to `for (let i = 0; i < POWERUP_DROP_TYPES.length; i++)` at a FIXED `y = HUD_FX_BASE_Y - (i+1)*HUD_FX_ROW_H` — the `slot` compaction cursor is gone. An active row's rendering (value arc / overcharge halo / low-timer pulse / bank pop / live number) is byte-for-byte the prior code, now gated on `powerActive(t)`; an inactive row draws the dim track + glyph + label + number in `COLOR.dim` only — no value arc, no pulse, no bank pop — and the existing `num` expression (`counted ? String(powerBudget[t]) : Math.ceil(powerFx[t]) + "s"`) is left completely unmodified, so idle state (`powerFx`/`powerBudget` both 0 at rest) naturally yields `"0s"`/`"0"` with no special-casing. **New test `scratchpad/test-cs012-p2.js`** (58 assertions, drives the REAL `drawHUD()`/`drawRingSegments()` via a recording-canvas stub that logs `arc`/`stroke`/`fillText`/`fill` calls plus the `strokeStyle`/`lineWidth`/`shadowBlur` state at each stroke — `shadowBlur > 0` at a stroke is how a `glowStroke()` call is told apart from a plain dim-track stroke, since `glowStroke` sets then resets it; no reimplementation of the ring/HUD math): (B) `drawRingSegments` in isolation — exactly `segs` wedges, the first `filled` lit via glow in `litColor`, the rest dim in `dimColor` at `HUD_RING_TRACK_W`, every wedge's angular span strictly under a full slot (a real gap), never `closePath()`'d; (C) all five rows (Scoop + 4 timed) render at their FIXED `y` across five active-combinations (none/one/two/all), and expiring one type's effect does not move another's row; (D) an inactive row logs no glow value-arc and no halo, an active row still does, and the underlying idle state (`powerFx`/`powerBudget` = 0) is confirmed to be what the unmodified `num` expression reads; (E) Scoop at level 0 renders all `SCOOP_MAX_LEVEL` wedges with 0 lit, level 3 lights exactly 3, and `ctx.fill()` is called ZERO times anywhere in `drawHUD()` at level 0, level 3, and a fully-active+banked HUD; (F) `AudioSys.ctx` null → `startGame()`/`update(1/60)` don't crash. **Two pre-existing tests needed updating, a legitimate consequence of this change, not a bug:** `test-cs009-p4.js` — sections A/C assumed magnet (index 2 in `POWERUP_DROP_TYPES`) compacted to slot 1 when it was the only active effect; fixed to assert against its own fixed row (`MAGNET_Y = HUD_FX_BASE_Y - 3*HUD_FX_ROW_H`) instead. Section F explicitly tested the OLD compaction behavior ("rapid expires → magnet compacts down to slot 1") — rewritten to assert the OPPOSITE (magnet stays at its own fixed row after rapid expires; rapid's now-inactive row still renders its dim track, not removed) — this is CS012 P2's entire point, so the old assertion had to invert, not just shift a constant. `test-cs009-hud.js` — a stale comment claiming "the SCOOP pip row's `ctx.fill()`" as a surviving fill was corrected (the test's actual assertions never counted `ctx.fill()`, only `fillRect`/`strokeRect`, so nothing behavioral changed, just documentation accuracy). **Full regression re-run green: all 46 test files** (45 prior + this session's new file), `node --check` clean. One flaky pre-existing test observed (`test-starfield.js` section E, a 120-frame headless drive that occasionally lets a randomly-spawned saucer land a hit) — reproduced identically with this session's changes stashed out via `git stash`, confirmed pre-existing and unrelated, not touched. **No version bump** — `GAME_VERSION` stays `"1.0.0.11"` (render-only change, no new mechanic). Docs: GDD §3.2 (removed the SCOOP-pip-row fill from the no-fills exception list — now **two** named exceptions, not three: death-flash and the CS010-P3 corner glow; noted `drawRingSegments` adds no fill), GDD §2.14 (the HUD/HUD-layout bullets rewritten for the fixed five-row stack — no more `powerActive`-gated compaction, inactive rows muted not hidden), GDD §2.14.1 (Scoop HUD bullet — segmented ring, always drawn including level 0), GDD Architecture Map (`draw()` row gained a CS012 P2 note; Constants row gained `HUD_RING_SEG_GAP`), CLAUDE.md (Rendering-conventions bullet + the no-fills-bar bullet both updated for `drawRingSegments`/the exception-count drop to two; code map's Helpers line gained `drawRingSegments`), this file. **Playtest ask carried over from the phase prompt, NOT resolved by this session** (a visual judgment call, not a headless-testable one): confirm the CS010-P3 low-health corner glow's "frame the corners, don't wash out the rows" intent still reads correctly now that the bottom-left stack is a stable, always-full five-row footprint rather than a variable 1-5-row stack — the corner glow's `LOWHP_GLOW_*` knobs were tuned against the old variable-height stack. Did NOT push (Paul's call, every phase).

**Prior session (CS012 P1):** implemented CS012 Phase 1 per the phase prompt (`IMPLEMENTATION-PHASES-012.md` P1, turning `PLANNED-FEATURES-CS012.md` FORK-CS012-A → (a) into code). Re-grepped every anchor first (they drift): `SAUCER_AIM_ERR_FLOOR`/`SAUCER_AIM_ERR_CEIL` confirmed at L399/400 (unchanged), the small-saucer aim line in `Saucer.update` confirmed at L3165 (pre-edit; now L3167 post-edit, two lines further down the file for the new constant), `ramp`/`difficultyFactor` confirmed at L2610/L2605. **What landed (exactly the two edits the prompt scoped, nothing else):** **(1)** a new playtest-knob constant `SAUCER_ACCURACY_RAMP_SCALE = 0.5` placed directly beside `SAUCER_AIM_ERR_FLOOR/CEIL` (now L401), commented as `<1: saucer aim sharpens slower than the global difficulty ramp. Pins the wave-1 floor exactly. PLAYTEST KNOB.` **(2)** the small-saucer aim line's wave ARGUMENT only — `ramp(SAUCER_AIM_ERR_FLOOR, SAUCER_AIM_ERR_CEIL, game.wave)` → `ramp(SAUCER_AIM_ERR_FLOOR, SAUCER_AIM_ERR_CEIL, 1 + (game.wave - 1) * SAUCER_ACCURACY_RAMP_SCALE)` (now L3167) — the scaled wave collapses to exactly `1` at `game.wave === 1`, so the wave-1 floor is preserved byte-for-byte; at any later wave the scaled wave sits BELOW the raw wave, so aim error closes toward `SAUCER_AIM_ERR_CEIL` more slowly than every other saucer parameter still sampling the raw `game.wave` (spawn gap, fire-rate multiplier, appearance chance — all untouched). **Confirmed untouched, per the prompt's explicit exclusions:** the big-saucer random fire (`a = rand(0, TAU)`, L3170), `rollFireTimer` (L3146), the small-saucer appearance-chance ramp (L4852), and `leverScale` (L2619) — this is a scaled-wave trick, not a difficulty lever, so it deliberately does not go through `leverScale`/`DIFFICULTY-LEVERS.md`. **New test `scratchpad/test-cs012-p1.js`** (51 assertions, drives the REAL extracted script — no reimplementation of the aim-error formula): forces one aimed shot from a real `Saucer(true)` with `Math.random` pinned to `1` (so `rand(-err,err)` collapses to exactly `+err`) and reads the ACTUAL applied error back off the fired bullet's angle via the real `angleTo`, rather than recomputing the formula independently; (B) wave-1 identity — the extracted error equals the real `ramp(FLOOR,CEIL,1)` byte-for-byte; (C) waves 9 and 17 — the extracted (scaled) error is LOOSER than the real `ramp(FLOOR,CEIL,wave)` at that same raw wave, and a 7-point wave sample (1,5,9,13,17,25,50) is monotonically non-increasing; (D) bounds — error never exceeds `SAUCER_AIM_ERR_FLOOR` nor drops below `SAUCER_AIM_ERR_CEIL` across waves 1–200; (E) `AudioSys.ctx` null → `startGame()`/`update(1/60)` don't crash. **One pre-existing test needed updating, a legitimate consequence of this change, not a bug:** `test-f4.js`'s end-to-end section (C) asserted the fired-bullet aim error against the OLD unscaled `ramp(FLOOR,CEIL,wave)` at wave 20 — updated to assert against the new scaled-wave expectation instead (`ramp(FLOOR,CEIL, 1 + (wave-1)*SAUCER_ACCURACY_RAMP_SCALE)`), with a comment explaining the raw-wave curve in section (B) is still a true statement about the `ramp()` primitive but no longer what the live `Saucer` samples for aim error; the wave-1-vs-wave-20 "much wider early" ratio assertion was relaxed from `>2.5×` to `>1.8×` to match the real (smaller, by design) ratio under the slower scaled ramp (measured ≈2.07×). **Full regression re-run green: all 45 test files** (44 prior + this session's new file), `node --check` clean. **No version bump** — `GAME_VERSION` stays `"1.0.0.11"`, per the phase prompt's own note (a tuning-only change, not a new mechanic). Docs: GDD §2.13 (new bullet on the small-saucer aim line sampling a scaled wave, `SAUCER_ACCURACY_RAMP_SCALE` added to the tuning-constants callout), GDD §3 Architecture Map Constants row (new `CS012 (P1)` bullet), this file. Did NOT push (Paul's call, every phase).

## GDD version history

- **CS012 (P1–P5) — Difficulty/UFO accuracy ramp, HUD fixed-stack rebuild, Maxed-Out achievement, menu-IA collapse, and the opt-in auto-shield.** `IMPLEMENTATION-PHASES-012.md`, the whole changeset (this consolidated entry was written at P5 — the version-history file is unattached by default and wasn't touched during P1–P4). **P1 — UFO accuracy knob:** `SAUCER_ACCURACY_RAMP_SCALE` scales the saucer's aim-error ramp by wave so early UFOs miss more and late ones bite, a single tuning knob over the existing accuracy ramp. **P2 — HUD segmented Scoop ring + fixed 5-row powerup stack:** the SCOOP pip row became a `drawRingSegments()` segmented ring (new `drawPoly`/`glowStroke`-family primitive, never `closePath()`s), and the four timed-effect rows (rapid/triple/magnet/engine) are now ALWAYS drawn at their own fixed row — muted when inactive — instead of compacting to active types only, so the bottom-left stack is a stable five-row layout that never reflows (§2.14, §3.2 no-fills membership updated). **P3 — Maxed Out achievement:** a max-cap-haul lifetime unlock (`game.stats.maxChainVisit`, latched in the dock-offload block on `deliveryCount === CARGO_CAP_MAX`) plus a UI-only MAX HAUL celebration reusing `FloatText`'s new optional `size` param and the existing gold ring-pulse idiom — no new voice line (§2.17). **P4 — menu IA collapse (FORK-CS012-C → a):** the title/gameover system-menu root was retired (`MENU_ROOT_SYS` deleted, `rootItems()` returns only `MENU_ROOT_PLAY`), so O/Start/B open OPTIONS directly; Options is the sole hub, Achievements/High-Scores live only under it, and the `achReturn` tracker is gone — "Back from Options" is context-aware (root while paused mid-game, `closePause()` from title/gameover). No `GAME_VERSION` bump (a navigation refactor). **P5 — auto-shield difficulty option (this phase, the round's last player-visible change):** a reactive last-resort save. A new **Difficulty** row "Auto-shield: Off | On" toggles `settings.autoShield` (additive in `afd_settings_v1`, default OFF; accepted on load only if literally boolean — same rule as `captions`); `drawDifficulty` grew the panel, added a **per-row help line** describing the focused row ("Auto-raises shield at critical hull. -500 points per blocked hit."), and shifted Back/help/hint down. The mechanic is a hook at the TOP of `damageShip`, before its shield/hit-stun/dead early-return: when enabled and the hull is at/below `LOW_HP_THRESHOLD` with `≥ SHIELD_HIT_COST` energy, it spends the energy, applies the **same knockback + `HIT_STUN_DURATION` i-frame a real hit would**, raises `shieldOn`, deducts the new `AUTO_SHIELD_SCORE_PENALTY` (500) **directly from `game.score` (clamped at 0), bypassing `addScore` and never touching `game.nextRepair` — FLAG-4e**, pushes a red "-500" floater, and returns `false` for 0 HP dealt (same contract as the shielded early-return). The i-frame is the rate-limiter (~one save/second; same-frame follow-ups hit the raised-shield early-return), and because recharge (0.12/s) < per-save cost (0.22) the energy pool self-exhausts after ~4–5 back-to-back saves, at which point hits land normally again — a deliberately expensive critical-hull safety net, invisible to a healthy run. This is the **only sanctioned `addScore` bypass** in the codebase (recorded in CLAUDE.md's scoring non-negotiable). **Version:** `GAME_VERSION` bumped `"1.0.0.11"` → **`"1.0.0.12"`** at P5 (last player-visible phase; `Major.Minor.Patch.Changeset`); `test-cs010-p0.js`'s pinned literal updated to match. Headless: `scratchpad/test-cs012-p5.js` (new, 48 assertions, drives the REAL `damageShip`/`saveSettings`/`loadSettings`/`menuDifficulty` — no reimplementation) covers the 0-HP save (energy/score/stun/knockback/floater, `nextRepair` untouched), the same-frame + i-frame rate-limit, energy self-exhaustion, off-by-default + above-threshold + dead-ship baselines, the additive `afd_settings_v1` round-trip (missing/non-boolean → false), and the Difficulty toggle both directions with persistence. `test-p5.js` section I updated for the inserted row (Back moved index 2 → 3). Full regression green: **49 test files**, `node --check` clean. Docs (final CS012 doc pass): GDD §2.3 (auto-shield mechanic), §2.7 (the `addScore` bypass), §2.9 (Difficulty row + per-row help; Persistence gained `autoShield`), Architecture Map (`damageShip` hook, Difficulty rows), top-of-file Current-build → CS012 P5/"1.0.0.12"; CLAUDE.md (scoring non-negotiable's documented exception + frozen-keys `autoShield`); STATUS.md (CS012 complete). Did NOT push.
