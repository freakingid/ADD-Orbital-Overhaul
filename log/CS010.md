# CS010

**Prior session (CS010 P9):** ported the voice engine + the 24 shipping lines out of P8's lab into the build (spec `PLANNED-FEATURES-CS010.md` §11b–§11f, FORK-5 = b). **⚠ SIGN-OFF ASSUMPTION (FLAG-11a):** the phase prompt (and CLAUDE.md) say "do not start P9 unless Paul has signed off on P8's lab" — I treated **Paul handing over the full P9 implementation prompt as the sign-off** and proceeded. If you had NOT actually listened/approved, this is the moment to say so. **Two consequences of that assumption you should confirm:** (a) I shipped **radio-Dan (radio character default-ON**, `VOICE_PARAMS.radio.on = true`, exactly as the lab's committed default) — if you preferred the clean synth, flip that one field. (b) I ported each line's `phon` from the **committed lab file's g2p output** (re-derived by running the lab's real `g2p` over each line's text); **any ✎ hand-fixes you made in a browser session were NOT committed to `tools/voice-lab.html`, so they are NOT in the build** — if you fixed any smearing words in the lab, re-open it, redo the ✎ edits, and send me the **Dump** (or commit it) and I'll re-port those `phon` strings. **Captions (P8 ask #2) were NOT built** — the P9 prompt didn't scope them; still your call (see playtest asks). **What landed:** `VoiceSys` as a SEPARATE module alongside AudioSys/MusicSys (never inside AudioSys); the acoustic engine (`PH`/`parsePhonTokens`/`buildUtterance`/`buildPitch`/`_render`) ported **verbatim**; the lab's g2p authoring layer deliberately NOT ported (dead code in the game — its output is the baked `phon` strings; **decision recorded**). The **four** bus sites all fixed (the spec said one — it was wrong): `vol.voice`, `init()`'s `voice`→master node, `setVol`'s `this[cat]` lookup (killing the ternary that would have written voice→music gain), and `loadSettings`'s `Object.keys(vol)` loop. A **Voice Volume** row on P4's Sound/Music screen (SOUND_ROWS/VOL_LABELS/VOL_CATS grown together; additive into frozen `afd_settings_v1`). **Lines are DATA** (event-keyed arrays, random pick). **§11e cooldown (1.2 s) + priority** (3 hull-critical / 2 health-milestone / 1 rest; superseded lines DROP, never queue) — a real bug the test caught: the "never played" sentinel had to be `busyUntil = -Infinity`, not 0, or every line in the AudioContext's first 1.2 s would be swallowed. **All 8 triggers wired at verified anchors** with the right latches: hull-low gets its OWN `game.lowHpVoiced` latch that menus DON'T tear down (fixes Dan re-announcing on every unpause — the regression this phase exists to prevent, asserted); hull-relief additive to P3's `hpReliefFlash`; hull-full a new `game.hpFullVoice` edge latch in `update()` (the spec's claimed `applyPowerup` full-HP seam **does not exist**; starts true at spawn); collected in `applyPowerup` (scoop early-return branch, Health silent); expired routed through `powerActive()` (catches count modes, `game.powerVoiced{}`); scoop-lost in `damageShip`; dock tiers on the emptying pop (gate-as-latch, `breakChain`/`scatterChain` silent); truck-full on the fill against runtime `game.cargoMax`. **Decided:** a same-type bank re-pickup re-fires `collect_<type>` and the cooldown drops the second line if in-window — acceptable. New test `scratchpad/test-cs010-p9.js` (75 assertions, drives the REAL code) + **four older mocks completed** (they now exercise the voice engine because gameplay emits voice when a ctx exists: `test-cs009-p1/p5`/`test-p5` Q-param methods, `test-v34-p5` the waveshaper/compressor factories) + `test-cs010-p4` SOUND_ROWS snapshot updated; **full 39-file regression green**, `node --check` clean. Docs: GDD §2.8/§2.16/§3, CLAUDE.md, `GDD-VERSION-HISTORY.md`, this file.

**CS010 is now complete (P0–P9).** Did NOT push (Paul's call, every phase).

**Prior session (CS010 P8): built the GATED voice lab per the phase prompt (spec `PLANNED-FEATURES-CS010.md` §11a). The game file was not touched — verified, `git status` shows only the two new files (`tools/voice-lab.html`, `scratchpad/test-cs010-p8.js`) — and NOTHING ports until Paul has listened and signed off (FLAG-11a).** THE LAB: a Klatt-style formant synthesizer in Web Audio, matched to the scoop-lab/music-lab instrument pattern (standalone, no imports, PORT-ME BLOCK A = engine, PORT-ME BLOCK B = params + lines). A glottal source (custom `PeriodicWave` whose harmonic-tilt "richness" is a slider; sawtooth/square selectable) on a scheduled F0 contour (declination + stressed-vowel accents + phrase-final fall; `flat` robot and `lively` variants) feeds a PARALLEL bandpass formant bank F1–F4 (F1 direct; F2–F4 via a damp bus so nasal murmurs damp the highs), plus a separate frication path that BYPASSES the bank (Klatt's parallel path) and an aspiration path THROUGH it (so /h/ and VOT aspiration take vowel coloring). **The four intelligibility decisions — what P9 must not lose in porting:** (1) formants RAMP through per-consonant LOCUS targets (pin-previous-then-ramp on each `AudioParam`; /b d g/ live in where F2 comes from, and the velar locus is vowel-dependent — front ≈2300 Hz, back ≈1300 Hz); (2) stops are closure → typed burst → aspiration with DELAYED voicing onset (VOT ≈45 ms voiceless, ≈10 ms + voice bar voiced); (3) fricative noise is spectrally typed (/s/ 5.8 kHz strong, /sh/ 2.9 kHz, /f θ/ weak + diffuse) on the bypass path; (4) the shipping lines never touch rule G2P — a HAND dictionary covers every corpus word, the naive rule guesser serves only freeform typing and flags its guesses, and the derived ARPAbet string is EDITABLE per line (✎ hand-fixes persist in-session and export). UI: every shipping line as a one-click button grouped by trigger; text input + editable phoneme box; a timeline canvas (F1/F2/F3 tracks on log-Hz, frication/burst bands, pitch trace, live playhead); 17 sliders + 3 selects across Pitch & pacing / Glottal source / Formants / Consonants / Radio; Play-all; a Dump panel emitting paste-ready `VOICE_PARAMS` + `VOICE_LINES` — THE porting source. **RADIO CHAIN (spec fallback (a)) BUILT, DEFAULT-ON:** highpass 320 → lowpass 3200 → tanh soft-clip → compressor, plus a squelch-keyed static bed; band-limiting concentrates energy where intelligibility lives (telephone-speech effect), so radio-Dan is expected to BEAT the clean synth, and the A/B is one checkbox. **CORPUS COUNT CORRECTED — the phase prompt says "25 including the dock/full tiers" but its own enumeration totals 24** (9 health + 5 collected + 5 expired + 4 dock + 1 full), and §11f agrees: built all 24, Paul's phrasing VERBATIM (test-pinned), invented nothing. **HONEST CEILING (pre-listen, stated for the record):** early-DECtalk/Votrax territory — an unmistakable talking machine; sibilants, stressed vowels and prosody carry it; expected weak spots are /θ ð/ (weak by nature), /l/-vs-/r/ (the F3-dip cue is approximated), and unstressed function words; it will NOT pass for a person and was never going to. New test `scratchpad/test-cs010-p8.js` (256 assertions — drives the REAL extracted lab script under a stubbed DOM, no AudioContext, per the house never-copy-the-logic rule): all 24 (event, text) pairs verbatim and in spec order; zero rule-guessed corpus words; every phoneme token valid; per-line compile sanity (duration bounds, sequential non-overlapping segments, fric/asp events in range, monotonic pitch ≥ 40 Hz); and the locus/VOT/stress-lengthening/nasal-damp/diphthong/pause mechanics each pinned individually. Full 38-file regression green; extracted script passes `node --check`. Docs: this file + CLAUDE.md Design instruments (voice-lab bullet). **NO GDD change — nothing shipped.** Did NOT push (Paul's call, every phase).

**CS010 P8 — LISTENING SESSION FOR PAUL (FLAG-11a — the phase ends HERE; P9 is blocked on your sign-off):** open `tools/voice-lab.html` in a browser, hit **Play all 24**, then A/B the **Radio chain** checkbox on a few lines. Three decisions: **(1) Character** — radio-Dan (default, recommended: more intelligible AND fits the fiction) vs the clean synth vs "neither is acceptable." **(2) Captions (spec fallback (b), NOT built — your call):** recommend **yes, as belt-and-braces even if the audio lands** — cheap, accessible, and under §11e's drop-not-queue rule a missed line is otherwise simply gone; if you want captions, P9's prompt should grow a caption render (likely a `FloatText`-style HUD line). **(3) Per-line fixes** — any word that smears, fix its ARPAbet in the phoneme box (✎ saves to the line) and send the **Dump** back with your sign-off; the dump is exactly what P9 ports. Suggested first listens: "Hull integrity is critical." (dense consonant clusters), "A bigger pooper scooper." (the /p/ gauntlet), "There's at least 5 good pieces in there." (dock tier, longest line).

**Prior session (CS010 P7): implemented Phase 7 of CS010 per the phase prompt (spec `PLANNED-FEATURES-CS010.md` §8c). The spec was right that the whole item is TWO routing fixes + one new track, no new track-switch path.** GREPPED FIRST (step 1): confirmed `musicStateFor()` and its one caller `updateMusic()` verbatim, `MUSIC_TRACKS`/`MUSIC_TRACK_VALUES`/`menuActive()` (`return game.paused`)/`MUSIC_DUCK_GAIN` (0.5), and that the High Scores screen is entered via `gotoScreen("highscores")` (which leaves `game.paused` true — it's a menu). **(1) `musicStateFor()` is now MENU-AWARE:** `game.state` alone can't name the High Scores screen (it's a menu — `game.paused` is true while `game.state` is still `title`/`gameover`/`playing`), so a single leading clause `if (game.paused && game.menu.screen === "highscores") return "highscore";` was added, reading `game.menu` in the same idiom the function already reads `settings`. **Every other state×paused×screen combination is byte-identical to the pre-P7 mapping** — that regression is the one that matters, and the test hammers it exhaustively. **(2) DUCK EXEMPTION:** `setDuck(menuActive())` would have permanently ducked a menu-only track to 50%; changed to `setDuck(menuActive() && game.menu.screen !== "highscores")` — `setDuck` is already idempotent on its target, so the clause costs nothing per frame. **(3) THE TRACK — `highscore` (Fanfare):** composed/auditioned in `tools/music-lab.html`, ported VERBATIM (verified byte-identical between lab and game), registered as `highscore: buildHighScoreTrack()` in `MUSIC_TRACKS`. Bright C major, anthemic I–V–vi–IV: 16th bell arpeggio, soft four-on-the-floor kick, light shaker, triumphant triangle lead climbing to E6 then resolving to C — 6 layers, 100 BPM, 8 bars, 19.2 s (128 × 0.15). **Contextual like `title` — NOT added to `MUSIC_TRACK_VALUES`**, so it never appears in the Options picker; `settings.musicTrack`/`afd_settings_v1` untouched. No `tier` field, scheduler untouched, `playNote` voice branch untouched (uses only existing fields). **Node budget RE-MEASURED, not assumed:** the fanfare's worst step is **12** (bar-start: bell+sub+pad+kick; every lead onset is placed OFF the downbeat, steps ≡ 2/10 mod 16, so lead never stacks), so the overall worst case across all six tracks is still **13**. All standing guarantees hold for it too: loop 19.2 s ≥ 10 s, longest silent run **0** steps (the bell plays every step). **FLAG (SURFACED, NOT DECIDED — Paul's call, below):** should the cue ALSO play on the gameover post-entry high-score table? Built exactly the spec's scope (High Scores screen only); gameover left silent. New test `scratchpad/test-cs010-p7.js` (106 assertions): exhaustive `musicStateFor()` over 4 states × paused × 8 screens; the duck exemption driven through the REAL `updateMusic()` (every menu screen ducks to 0.5, highscores doesn't); the setState in/out crossfade; the registry (in `MUSIC_TRACKS`, absent from `MUSIC_TRACK_VALUES`); and the track's loop/gap/freeze/node re-measure. `test-v34-p6.js` re-run green (244 assertions — its all-tracks gap/loop/freeze/node checks now also cover `highscore`), `node --check` clean, full regression green. Docs: GDD §2.8 (menu-aware `musicStateFor()`, the new track descriptor, the duck exemption), `GDD-VERSION-HISTORY.md` (new CS010 P7 entry), this file. Did NOT push (Paul's call, every phase).**

**CS010 P7 — QUESTION FOR PAUL (SURFACED, DELIBERATELY NOT DECIDED):** should the celebratory `highscore` track ALSO play on the **gameover** screen's post-entry high-score table — where the player has just *earned* a top score — rather than only while the browsable High Scores screen is open? Today `musicStateFor("gameover")` returns `"off"`: the death spectacle deliberately ends in **silence** (`MUSIC_FADE_OUT` runs under the explosion, GDD §2.7). The §8c spec scoped this item to "while the High Scores screen is open," so P7 builds **exactly** that and leaves gameover silent — but the two screens show the *same* table, and a player who just placed sees it first on gameover, so extending the cue there is a reasonable design call. If you want it: `musicStateFor` would return `"highscore"` for `game.state === "gameover"` when `game.entry`/a fresh-placement flag is set (needs a rule for *which* gameovers qualify — every gameover, or only a qualifying placement — and a decision on whether it fights the death-fade-to-silence beat). Not built; awaiting your call.

**Prior session (CS010 P6): implemented Phase 6 of CS010 per the phase prompt (spec `PLANNED-FEATURES-CS010.md` §6). ONLY `buildDriftTrack`/`buildWarehouseTrack` — Beacon/Zen/Derelict untouched.** GREPPED FIRST (step 1): confirmed both builders and their `MUSIC_TRACKS` registration (~L1016), the `{stepDur, steps, layers:[{type|noise, gain, atk, rel, …, steps:[cell|null]}]}` shape via `mk`/`put`/`hit`, and Drift's pre-P6 form (BARS 8 / SPB 16 / STEPS 128 / stepDur 0.17, one 8-chord progression + a 16th organ arp). **Data-only, all standing non-negotiables held:** `MusicSys.update`/`scheduleStep`/`layerGates` gain-gating untouched; `playNote`'s voice branch untouched (the added `hook` layer uses only existing fields — `type`/`cutoff`/`cutoffTo`/`cutoffTime`/`q`); the intensity system stays DORMANT (no `tier` fields anywhere, `setIntensity()` still a no-op, `MUSIC_LAYER_THRESHOLD`/`layerGates`/the `nextWave()` call site all intact); track names `drift`/`warehouse`, `settings.musicTrack`, `afd_settings_v1` unchanged (saves keep working). **Workflow honoured:** composed/auditioned in `tools/music-lab.html`, then ported VERBATIM — verified byte-identical between lab and game for both builders. **Drift:** 8→16 bars (STEPS 128→256; 21.8→43.5 s), A→B — A (bars 0-7) is the original Am F C G Am F Dm E; B (bars 8-15) is a brighter C-major turn (C G Am Em F C Dm E) with the organ arp an octave up and a fuller lead rising to C6 in bar 13, resolving on E back into A. **Warehouse:** an 8-bar Am7 Dm7 Fmaj7 G vamp now played twice (12→16 bars; 23.0→30.7 s), A→B — A (bars 0-7) groove only, B (bars 8-15) adds a **6th `hook` layer**, a sawtooth-pluck riff on the offbeats (steps 2/6/10/14). **On §6's cut-hook steer — SURFACED HONESTLY:** the round-2 hook layer Paul remembered is **NOT recoverable from the repo.** `tools/music-lab.html` has a single commit — the round-3 "FINAL" that DELETED the gated-off upper-tier layers to freeze the approved mixes — and the round-2 candidates were never committed anywhere (git log/reflog, `archive/`, `scratchpad/`, all docs checked; only the FINAL warehouse builder, explicitly "deliberately NO melodic hook", exists). So the hook was **composed fresh in the spirit of the brief** (a house lead riff deployed as the intermittent chorus payload the cut layer was described to be — "too present as a *constant* element" is exactly what makes it a B-section). **This needs a playtest ear — flagged below and in my response.** **FLAG-6a (node budget) MEASURED, not assumed:** worst-case node creation for a single scheduled step is **still 13** (unchanged overall max — `zen`/`title`/`drift` step 0 tie at 13; Drift's B keeps the four-voice bar-start coincidence at 13; Warehouse's `hook` sits only on otherwise-empty offbeat steps so its worst step stays **11**, not the max). All three existing `scratchpad/test-v34-p6.js` guarantees hold: every loop ≥ 10 s (Drift 43.52, Warehouse 30.72), no silent run > 1 step checked circularly (Drift 0, Warehouse 1), per-step nodes ≤ 16 (13). `test-v34-p6.js` re-run green (236 assertions), `node --check` clean, full **36-file regression green**. Docs: GDD §2.8 (two track descriptors + a CS010-P6 re-measured-node-budget note), `GDD-VERSION-HISTORY.md` (new CS010 P6 entry), this file. **PLAYTEST ASK (needs ears, not just a passing test): does each B-section land as relief/lift — an A→B contrast inside one song — or as a different song bolted on? And specifically: does Warehouse's restored hook read as the chorus, or should it be re-voiced (register / rhythm / filter)?** Did NOT push (Paul's call, every phase).**

**Prior session (CS010 P5): implemented Phase 5 of CS010 per the phase prompt (spec `PLANNED-FEATURES-CS010.md` §8a). The prompt itself flagged that 180% CANNOT apply to both of `drawScoreTable`'s callers — did the arithmetic first, before touching code.** Grepped first (step 1) and confirmed `drawScoreTable(cx, topY, highlightId)` still has EXACTLY TWO callers, at the shapes the prompt named: `drawHighScores()` (`menuPanel(640, 460, "HIGH SCORES")`, table at `y + 90`, footer at `y + 420`) and the gameover block (`drawScoreTable(VIEW_W/2, VIEW_H/2 + 60, game.lastScoreId)`, "PRESS ENTER TO PLAY AGAIN" at `VIEW_H/2 + 280`, the O/B hint at `+312`). **The math (prompt's own): at 180% (header ~23, pitch ~32) the gameover screen's 10th row lands at ≈ 420 + 39 + 9·32 = ~747 — off the bottom of a 720-tall viewport, through both prompts. The ceiling there without re-flowing that screen is ~1.4×.** So: PARAMETERIZED, not globally bumped. **(1) `drawScoreTable(cx, topY, highlightId, scale = 1)`** — every font size, the row pitch (18), the header offset (22), AND all five column x-offsets (`cx±230/±170/+10/±130`) now multiply by `scale` together, per the prompt's own note that bigger glyphs need wider columns. **(2) The two callers deliberately diverge** — `drawHighScores()` passes a new `HS_TABLE_SCALE` (1.8); the gameover call site is unchanged (still 3 args, so `scale` defaults to 1) because its layout is already tight against the two prompts below it and the fresh entry is already highlighted gold there. A code comment at `drawScoreTable` states WHY, per the prompt's explicit warning that a future "unify these" pass would push the table off-screen. **(3) The High Scores panel GREW to fit, per the prompt (shrinking the font was explicitly ruled out)** — hand-computed for 10 rows at the 1.8 pitch (header offset 39.6, row pitch 32.4, row 10 baseline at `topY + 331.2`): `menuPanel` grew **640×460 → 1000×560**, `topY` moved **`y + 90` → `y + 120`**, footer moved **`y + 420` → `y + 497`** — all one-line tunables, not runtime-derived, with air to spare below the panel's own inner bottom. **(4) Did NOT touch `drawEntrySlots`** (already font 40, out of scope, confirmed by re-reading the prompt's explicit exclusion). **New test `scratchpad/test-cs010-p5.js`** (21 assertions): syntax; the no-scale-arg call reproduces the exact pre-P5 geometry byte-for-byte (font 13, pitch 18, header offset 22, unscaled columns); `scale = 1.8` scales fonts/pitch/header-offset/columns together; a recording-canvas capture of every `fillText` call from `drawHighScores()` with a FULL 10-row table stays within the panel's own `menuPanel` `strokeRect` bounds (recovered from the log, not hardcoded); the same capture from the gameover overlay — isolated from the preceding world/HUD draws by slicing the log from the "GAME OVER" fillText onward, since those earlier calls are under an untracked `ctx.translate` the stub doesn't apply and would otherwise give false failures — stays within `VIEW_H - 20` with the table itself still at font 13; and a source-level regex confirms the gameover caller still passes no 4th argument. Full 36-file regression green, `node --check` clean. Docs: GDD §2.18 (new Font size / CS010 P5 bullet + updated Architecture Map/`draw()` rows), `GDD-VERSION-HISTORY.md` (new CS010 P5 entry), this file. **PLAYTEST ASK (new): is 1.8 the right scale on the browsable High Scores screen, and — now that it's bigger — does the gameover table read small by comparison? If it does, the fix is re-flowing the gameover screen to afford a larger table too, which is a separate, un-scoped job — flagged, not built.** Did NOT push (Paul's call, every phase).

**Prior session (CS010 P3): implemented Phase 3 of CS010 per the phase prompt (spec `PLANNED-FEATURES-CS010.md` §5; FORK-2 = (a) sync everything; FORK-3 = (a) a FILL; FLAG-CS010-a = ship a visual relief fallback). The contentious phase — it REVERSES a CS009 decision written into the code and reopens the no-fills rule CS009 hardened; Paul signed off on both.** Grepped first (step 1) and confirmed every anchor: `LOW_HP_THRESHOLD` 100; the low-health STATE is a per-frame read, the AUDIO is edge-detected off `game.lowHpSiren` at one site in `update()` (right after camera-follow); `lowhpSet`'s rate line interpolated `LOWHP_PULSE_RATE_MIN`→`MAX` (0.9→2.4 Hz); and — correcting the spec, which said "two" — `HUD_PULSE_HZ` (1.2) had **THREE** call sites: the critical-hull ring (`hpCrit && !hpMax`), the cargo-gold ring (`cargoFull || cargoFlash>0`), and the powerup **low-timer** row (`powerFx[t] <= HUD_FX_LOW`, which also recolors to `COLOR.lowhp` — a third red pulsing element). Max-hull is gold-only, NOT pulsed. The low-health chevron stays; the glow is additive. **(1) RATE SHARED BY TRIGGER, NOT WIDGET:** extracted `function lowhpPulseRate(t)` (one formula, placed by the `LOWHP_*` audio knobs); `AudioSys.lowhpSet` now READS it instead of duplicating the interp, and the new phase accumulator reads it too — so the siren, the critical-hull ring, and the corner glow are ONE HP-ramped instrument. Cargo-gold and the powerup low-timer KEEP flat `HUD_PULSE_HZ` (a distinct rate is how the player tells a hull alarm from a timer expiring), so `HUD_PULSE_HZ` now has exactly its two correct consumers. **(2) PHASE ACCUMULATOR (the step-3 trap):** you can't just make `ω` in `phase = ω·clock` a function of HP — the phase would SNAP (~30 rad on a 0.5 rad/s Δ at t=60 s) each HP change and stutter. New `game.lowHpPhase`, advanced `(+= TAU·lowhpPulseRate(t)·dt) % TAU` at the EXISTING `lowHpSiren` edge-detect site (reused the latch, no second one), reset to 0 on disengage + `startGame`; the hull ring and the glow both read `Math.sin(game.lowHpPhase)`. It NEVER reads a Web Audio LFO value (unreadable; `ctx` may be null pre-gesture — the glow must render with audio unavailable) and lives in `update()`, so it FREEZES while paused (correct — `openPause` tears the siren down; same rationale as CS009's `hudHull`/`cargoFlash`). **(3) THE GLOW (FORK-3 = FILL, built as signed off — no stroke variant, no `LOWHP_GLOW_MODE` toggle):** four `createRadialGradient` corner fills in `COLOR.lowhp`, drawn first in `drawHUD()` (Capture's **H** hides it, **P** exports clean); intensity scales with BOTH urgency `t` (`LOWHP_GLOW_ALPHA_MIN`→`_MAX`, brighter near death) AND the shared pulse; alpha rides the gradient stop so it never touches `globalAlpha`. New `LOWHP_GLOW_*` knobs (all PLAYTEST). **(4) RELIEF CUE (FLAG-CS010-a, built — Paul wants a fallback; PERMANENT, additive if Dan's voice lands):** `LOWHP_RELIEF_FLASH` + `game.hpReliefFlash`; armed on the falling edge ONLY IF `!ship.dead`, decayed in `update()` beside `cargoFlash`, zeroed at all four direct siren teardowns (`openPause`/`quitToTitle`/`startGame`/`killShip`). Render is a GEOMETRY-ONLY (radius + width) NON-pulsing exhale on the hull ring, reusing `HUD_BANK_POP`'s radius pop + the cargo cap-up's `+2` width bump; color untouched (composes with the `hpMax`/`hpCrit`/normal precedence chain for free). **The alive-guard is load-bearing** (dying also drives the low-health state false) — traced that `killShip` zeroing the latch + the `dying` early-return already prevent the edge from being reached at death, but guarded explicitly anyway. **(5) Comments rewritten** (they'd otherwise contradict the code): the `HUD_PULSE_HZ` comment (now "flat rate for NON-HP alarms only"), the hull-ring in-line comment, and GDD §3.2's no-fills list. Headless-tested (`scratchpad/test-cs010-p3.js`, new, 32 assertions, all green) + full 34-file regression. Did NOT push (Paul's call, every phase).

**CS010 P3 — PLAYTEST ASK (needs eyes in a browser, not just a passing test):** (a) **The `LOWHP_GLOW_*` numbers.** Shipped first-pass: `LOWHP_GLOW_ALPHA_MIN` 0.10, `LOWHP_GLOW_ALPHA_MAX` 0.26, `LOWHP_GLOW_RADIUS` 280 px, two-stop linear gradient (opaque corner → transparent at radius). Two of the four corners are occupied — top-right is the HULL/CARGO rings (the HULL ring is pulsing red *in phase* with the glow), bottom-left is the powerup rows + SCOOP pips — so the open question is whether the glow **frames** those corners or **washes out** the rings sitting ~150 px in. If it washes: drop `ALPHA_MAX`/`RADIUS`, or concentrate the falloff by adding a mid color-stop (e.g. `0.55 → peak·0.25`) so the glow is bright at the very corner and faint by mid-radius. (b) **Does the synced hull-ring + glow + siren read as ONE alarm?** That's the whole point of FORK-2(a) — three red HP tells at one HP-ramped rate instead of two red pulses beating out of phase. If the single rate reads as *too* much red at once, the lever is to keep the siren/glow synced but let the hull ring keep a hair of independent character — but try the fully-synced version first, it's what was asked for. (c) **`LOWHP_RELIEF_FLASH` 0.5 s** — long enough to register as "you're okay," short enough not to linger. (d) The corner glow is FORK-3(a) a fill, shipped as signed off; there is no `LOWHP_GLOW_MODE` toggle to compare against a stroke variant (deliberately — Paul picked). If the fill ever needs re-litigating, §3.2 and the `drawHUD` block comment record exactly why a fill beat a `glowStroke` arc.

**Prior session (CS010 P2): implemented Phase 2 of CS010 per the phase prompt (spec §3 towed mass, §4 Engine, §10b rotation slider) — three items in one phase because §3 and §10b both own the two `Ship.update` turn lines.** Grepped first (step 1): confirmed `SHIP_TURN` (4.2) read at exactly two sites (both in `Ship.update`); `CARGO_MASS`/`CARGO_THRUST`/`CARGO_MAXSPD` at exactly three (`Ship.update` thrustMul+maxSp, `updateChain` massFactor); `ENGINE_MASS_MULT` only in `chainMass()`; the settings/save/load/`menuControls`/`returnToDefaults`/`REBINDABLE` machinery. **(1) One turn source:** added `shipTurnRate()` = `SHIP_TURN × settings.shipTurnScale / (1 + chainMass()×CARGO_TURN)` directly after `chainMass()`; both turn sites now read `shipTurnRate()×dt` — they can't diverge. **(2) Rotation slider (§10b):** `settings.shipTurnScale` (0.5–1.5, 0.10 steps, default 1.0), persisted **additively** in `afd_settings_v1` (missing→default; non-numeric/out-of-range→1.0; loaded value snapped to the 0.10 grid); a new *Ship Rotation* row on the **existing** Controls screen — `adjustShipTurnScale()` in `menuControls`, `drawTurnScaleRow` (percentage readout + an always-visible gold 100% "home" tick) by `drawControlsMenu`, panel grown 470→512px; `turnRow`/defaults/back indices **derived** from `REBINDABLE.length`, never hardcoded. **FLAG-10a honoured:** `returnToDefaults()` does NOT reset `shipTurnScale` (a comment at the function says so, so a future session won't "fix" it). **`e.repeat` guard traced, not assumed:** the menu-open keydown branch early-returns on `e.repeat` before `handleMenuKey`, so the new ◄/► row nudges once per press. **(3) Towed-mass retune (§3, all playtest knobs):** `CARGO_THRUST` 0.06→0.07, `CARGO_MAXSPD` 0.03→0.035, `CARGO_MASS` 0.07→0.10; the stale B-8 comment (which still solved `1/(1+20·k)` for a *20*-node ceiling) rewritten to the real 24-node build. **(4) `CARGO_TURN` = 0.0, shipped DORMANT** — the real "inertia" fix (mass resisting rotation), routed through `shipTurnRate()`; byte-identical to today × the player scale at 0.0, so Paul enables real rotational inertia with one number. **(5) Engine (§4): no code change** — `ENGINE_MASS_MULT` stays 0.5; §3 making mass matter is what makes Engine matter; FLAG-4a (empty-chain no-op) accepted, not fixed — re-judged after the §3 numbers, still wouldn't touch it. Headless-tested (`scratchpad/test-cs010-p2.js`, new, 45 assertions) + `test-p6.js` (A)/(D) coefficient+handling assertions updated to the new balance; full 34-file regression green, `node --check` clean. Did NOT push (Paul's call, every phase).

**CS010 P2 — towed-mass retune: decisions, tables, and the FLAG-3a stress.** All four mass coefficients are PLAYTEST KNOBS; the numbers below are a first pass, not finals. *Why lean on the tug:* thrust/top-speed are `1/(1+m·k)`, which **asymptotes** — raising `k` can't bite the mid range without over-penalising the full 24-chain (which v3.0 P6 deliberately tuned) — so those got only a modest firming, and the **tug** (`CARGO_MASS`, `min(1.4, m·k)`) carried the mid-range fix: 0.07→0.10 moves the 1.4 cap from m≈20 to m=14, strengthening the low-to-mid pull-back **without touching the full-24 endpoint** (capped in both).

Handling multipliers, OLD → NEW:

| towed mass m | thrust (k 0.06→0.07) | top speed (k 0.03→0.035) | tug min(1.4,m·k) (0.07→0.10) |

|---|---|---|---|

| 6  | 73.5% → 70.4% | 84.7% → 82.6% | 0.42 → 0.60 |

| 12 | 58.1% → 54.3% | 73.5% → 70.4% | 0.84 → 1.20 |

| 24 | 41.0% → 37.3% | 58.1% → 54.3% | 1.40 → 1.40 (capped both) |

`CARGO_TURN` candidate table (step 4) — turn-rate multiplier `1/(1+m·CARGO_TURN)` a heavy chain keeps. Shipped at **0.0** (no penalty). If enabling, suggest ~0.02–0.03 (a full 24-chain still turns at ~58–68%; 0.05 is sluggish at 45%):

| CARGO_TURN | m=6 | m=12 | m=24 |

|---|---|---|---|

| 0.01 | 94.3% | 89.3% | 80.6% |

| 0.02 | 89.3% | 80.6% | 67.6% |

| 0.03 | 84.7% | 73.5% | 58.1% |

| 0.05 | 76.9% | 62.5% | 45.5% |

FLAG-3a chain-stability re-run (`test-cs010-p2.js` (D), 24 nodes / 900 frames; HEAD = pre-CS010 coeffs):

| stress | dt | HEAD | worktree | Δ | note |

|---|---|---|---|---|---|

| kinematic v=420/260 (documented (E) method) | 1/60 | 4.11px | 4.11px | 0.000 | envelope holds; retune neutral (kinematic isolates the solver) |

| faithful, mass 24 | 0.05 | 8.33px | 8.17px | −0.16 | retune marginally *stabilising* |

| faithful, mass 12 | 0.05 | 10.09px | 9.19px | −0.90 | " |

| faithful, mass 6 (light+fast) | 0.05 | 12.61px | 11.61px | −0.99 | " |

| kinematic v=420/260 (over-stress) | 0.05 | 14.80px | 14.80px | 0.000 | big-timestep artifact, both builds |

No NaN, no velocity blowup anywhere. **Record-correction:** the documented 4.11px is a **dt=1/60** figure (GDD §3.4's "dt ≤ 0.05" bounds sim validity, not the measurement dt); the deliberately-unphysical v=420 (21px/frame) at the 0.05 clamp gives ~14.8px on *both* builds. The tug's 1.4 cap is unchanged and a stronger tug is a stronger restoring force, so the retune is stability-neutral at the documented method and marginally stabilising under realistic dt=0.05 physics.

**Proposed, NOT shipped — the missing half of "inertia" (step 4).** Towed mass still doesn't affect **stopping**: `SHIP_DRAG` is constant and `maxSp` is a thrust ceiling, not a coasting clamp — so a heavy chain speeds up slower but stops just as fast. If turn (`CARGO_TURN`) + tug isn't enough after playtest, the honest next lever is a load-scaled coast: e.g. an effective drag of `SHIP_DRAG × (1 − m·k_stop)` (rough `k_stop ≈ 0.01` lets a mass-24 chain coast ~24% longer) or clamping coasting speed to `maxSp`, not only thrusting speed. Left as a proposal with sketch numbers (to be tuned in a lab), NOT shipped, per the phase's "don't silently change the thrust/maxspd formula" rule.

**Prior session (CS010 P1): implemented Phase 1 of the CS010 changeset per the phase prompt (spec: `PLANNED-FEATURES-CS010.md` §7) — a render-only revert of the Scoop's on-ship mouth back to the open prong-V it used through v3.5, superseding the v3.6 P1c capture-box fill; `inScoopBox()`'s capture math was not touched, character for character.** Grepped the live scoop block first per the prompt's step 1 — confirmed `Ship.draw()`'s `if (game.scoopLevel > 0)` branch (asteroids-deluxe.html:~1944-1959) built the box `[[-SHIP_RADIUS,-hw],[d,-hw],[d,hw],[-SHIP_RADIUS,hw]]`, filled it at `globalAlpha` 0.15 in `COLOR.dock`, then `glowStroke`'d it; confirmed `SCOOP_CONFIG` is still `maxWidthMult: 5.0`/`maxDepth: 60` (v3.6 P1c's bump from 3.0/36); confirmed the `SCOOP_WIDTH[0] !== 0 || SCOOP_DEPTH[0] !== 0` load-time throw is untouched. **Audited before porting (FLAG-7a):** reverted `tools/scoop-lab.html`'s duplicate render to the V first, at the live 5.0/60 config, and screenshotted it (a scratch Playwright harness driving the cached headless Chromium already present in this environment — the lab has no other way to eyeball a render here) rather than assume the old V, designed for the pre-P1c 3.0/36 scale, still reads right at nearly double the size. It does: a clear, moderately wide flared funnel, not enormous or degenerate — no re-tune of `SCOOP_CONFIG` needed, and none attempted (that config is Paul's playtest knob, not this phase's to touch). **Ported into `Ship.draw()`:** the box fill and its `glowStroke` outline are gone; the scoop block is now exactly `drawPoly([[d,-hw],[16,0],[d,hw]], this.x, this.y, this.angle, COLOR.dock, false)` — an open, unfilled poly, throat at the ship's nose `(16,0)`, corners still derived from `SCOOP_WIDTH`/`SCOOP_DEPTH`, still hidden at level 0. The lab and game reverts landed in the same commit. **Docs:** GDD §2.14.1's Render bullet was rewritten (history preserved, not overwritten) to carry the full V→box→V supersession history plus an explicit note not to "fix" it back a fourth time; GDD §3.2's no-fills exception list dropped from three named exceptions to two (death-flash, SCOOP pip row), with a note that the capture-box fill was the retired third; `CLAUDE.md`'s no-fills paragraph was grepped for a stale reference — it only names the SCOOP pip row, unaffected, so no edit was needed there; `GDD-VERSION-HISTORY.md` got a new CS010 P1 entry. **New test `scratchpad/test-cs010-p1.js`** (685 assertions): (A) `node --check` on the extracted script; (B) `inScoopBox()` diffed between the pre-edit `HEAD` script and the working tree's script across 6 scoop levels × 6 ship angles × 19 forward/lateral/wrap-adjacent offsets (684 poses) — every boolean matched, confirming the capture region genuinely didn't move. **Fixed the one pre-existing test built against the box render:** `scratchpad/test-v33-p3.js` section (10) (from v3.6 P1c) asserted the drawn box's rear edge equalled `-SHIP_RADIUS` — that's exactly the tell the V never draws, so it now failed by design. Repurposed rather than deleted: it now recovers the V's 3 path points (open, no `closePath`) and checks the front edge and half-width still match `SCOOP_DEPTH`/`SCOOP_WIDTH`, plus a new throat-at-nose (`x===16`) check; the `inScoopBox()` accept/reject boundary probes (including the rear `-SHIP_RADIUS` edge) are untouched and still run directly against the real function, independent of the render. Full regression re-run: all 33 `scratchpad/test-*.js` files green after this fix, `node --check` clean. Did NOT push (Paul's call, every phase).

**Prior session (CS010 P0): implemented Phase 0 of the CS010 changeset per the phase prompt (spec: `PLANNED-FEATURES-CS010.md` §2, FORK-1 resolved as v1.0.0.10) — a version-numbering scheme change plus its first-ever visible display, no other gameplay code touched.** Grepped `GAME_VERSION` first per the prompt's step 1 — confirmed exactly one read survives as of `fef7ea2`, the `build` field stamped by `HighScores.add()` (asteroids-deluxe.html:~3229) — so no other call site needed updating. **Changed `GAME_VERSION` (asteroids-deluxe.html:~204) from the bare `"3.6"` to `"1.0.0.10"`, unprefixed** (the "v" prefix is added only at the display site, never baked into the constant, so the `build` field on a fresh high-score record stays exactly `"1.0.0.10"`). **Rewrote the comment above it** to document the new scheme: **Major.Minor.Patch.Changeset** — Major = new mechanics/levels, Minor = tweaks to existing systems, Patch = bug fixes (where "bug" includes the game not doing what the design intends), Changeset = the `CS###` number, monotonically increasing, never resets; the comment states explicitly that the 4th segment **is** the changeset number (this build shipped in CS010, hence `.10`). **Rendered on the title screen only**, lower-right corner (`draw()`'s `game.state === "title"` branch, asteroids-deluxe.html:~4419): `drawText("v" + GAME_VERSION, VIEW_W - VERSION_STAMP_INSET_X, VIEW_H - VERSION_STAMP_INSET_Y, 12, COLOR.dim, "right")`, using the existing `VIEW_W`/`VIEW_H` constants (never literals) plus two new named inset constants (`VERSION_STAMP_INSET_X`/`VERSION_STAMP_INSET_Y`, both 16px, declared beside `GAME_VERSION`) rather than inline literals, per the "tuning constants at the top" rule. Small (12px), dim (`COLOR.dim`), right-aligned so it can't collide with the centered title art or the drifting debris field — a build stamp, not a design element, exactly as scoped. **Did NOT migrate existing high-score records** — `afd_scores_v1` entries carrying `build:"3.6"` stay exactly as they are; the field is a free-form provenance string and a mixed table across builds is expected by design, per the phase prompt's explicit instruction. **New test `scratchpad/test-cs010-p0.js`** (5 assertions, following the established GDD §5.4 rule 7 idiom — stub window/document/localStorage, eval the real `<script>` block, drive the real `HighScores.add()`/`load()`/`save()`, no reimplementation): (A) `GAME_VERSION === "1.0.0.10"`; (B) a fresh `HighScores.add()` stamps `build === "1.0.0.10"`; (C) a hand-seeded legacy record with `build:"3.6"` survives both a `HighScores.load()` and a subsequent `save()` round-trip with its `build` field byte-identical, proving no migration touches old records. `node --check` clean on the freshly extracted script block. **Full regression re-run green** (`test-v36-scores.js`, all 66 assertions — the `GAME_VERSION`-dependent `build` field checks pass unchanged since they compare against the live constant, not a literal; `test-cs009-hud.js`, all 37 assertions — no HUD-adjacent regression from the new title-screen text). **Docs:** bumped the GDD's "Current build" header line (no top-of-file Version mega-line to touch — that was split out in CS009 P0), appended a new CS010 P0 entry to `GDD-VERSION-HISTORY.md` (not the GDD's §7 stub). Did NOT push (Paul's call, every phase).**

## Working / verified

**Headless-verified this session (CS010 P9, `scratchpad/test-cs010-p9.js`, new, 75 assertions):** drives the REAL `update`/`applyPowerup`/`damageShip`/`breakChain`/`scatterChain`/dock-offload/`AudioSys`/`VoiceSys`/`saveSettings`/`loadSettings` (no reimplementation). **(A)** `node --check`. **(B) The voice bus:** `AudioSys.init()` builds a distinct `voice` gain; `setVol("voice",0.5)` moves the VOICE gain and leaves MUSIC at 1 (the old-ternary bug); voice volume round-trips through `afd_settings_v1` and defaults to 1 when the key is missing. **(C)** total no-op with `AudioSys.ctx` null — `say`/`dockDelivery` return null and a real low-HP `update()` runs without throwing. **(D) cooldown/priority** against the real `say()` with a mock advanceable clock: an equal/lower line while one plays is DROPPED (busyUntil unchanged — proof it's not queued), a higher one PRE-EMPTS (curPriority→3), a new line inside the 1.2 s post-line gap is dropped, past it speaks; the prompt's burst case (hit→low HP→grab a powerup) keeps hull-critical and drops the grab. **(E) every trigger** driven through the real code with a `say()` spy: health_low rising edge once; **pause+resume at low HP does NOT re-fire it** (the `lowHpVoiced` latch — asserted explicitly, the one regression that would silently return); health_relief on the falling edge with `hpReliefFlash` still arming; health_full edge-once on heal-to-full and silent at spawn; each `collect_<type>` per pickup (scoop via its branch, Health silent); `expire_<type>` on the `powerActive()` falling edge in BOTH time and count mode; expire_scoop on a scoop damage-loss; the dock tiers on the emptying pop (1–4 silent, `breakChain`/`scatterChain` silent); cargo_full once on a clump-fill. **Four older mocks were completed** (they now exercise the voice engine because gameplay emits voice when a ctx exists): `test-cs009-p1`/`test-cs009-p5`/`test-p5` (AudioParam methods on `Q`; `cancelScheduledValues` on gain), `test-v34-p5` (`createWaveShaper`/`createDynamicsCompressor`/`createPeriodicWave` + `setPeriodicWave`); `test-cs010-p4`'s SOUND_ROWS snapshot updated for the new row. **Full 39-file regression green.**

**Headless-verified prior session (CS010 P2, `scratchpad/test-cs010-p2.js`, new, 45 assertions):** drives the REAL `shipTurnRate()`/`saveSettings()`/`loadSettings()`/`returnToDefaults()`/`updateChain()`/`Ship.update()` (no reimplementation). (A) `node --check`; (B) `shipTurnRate()` across scale 0.5/1.0/1.5 × chain mass 0/12/24 equals `SHIP_TURN×scale` (mass-independent while `CARGO_TURN`=0), plus a `CARGO_TURN=0.02` rebuild proving the mass divisor is actually consumed; (C) `shipTurnScale` round-trips through `afd_settings_v1`, a non-grid value snaps to 0.10, a missing key → 1.0, corrupt values (string / 99 / 0 / −1 / bool / unreadable-JSON) → 1.0, and `returnToDefaults()` leaves it untouched (FLAG-10a); (D) the FLAG-3a stress at 24 nodes / 900 frames on both the `HEAD` and working-tree builds (see the tables above). `test-p6.js` (A)/(D)'s coefficient+handling assertions were updated to the new balance (they had pinned the old 0.06/0.03/0.07 and 41%/58%). Full 34-file regression green.

**Headless-verified prior session (CS010 P1, `scratchpad/test-cs010-p1.js`, 685 assertions):** drives the REAL `inScoopBox()`/`startGame()` (no reimplementation), comparing the pre-edit `HEAD` script against the working tree's script — `inScoopBox()` returns byte-identical booleans across 6 scoop levels × 6 ship angles × 19 forward/lateral/wrap-adjacent offsets (684 poses), confirming the render-only revert to the prong-V didn't move the capture region; `node --check` also passes on the extracted script. The `SCOOP_WIDTH[0] !== 0 || SCOOP_DEPTH[0] !== 0` load-time invariant guard was left untouched, as scoped.

## Next up

**CS010 IS COMPLETE (P0–P9 all shipped).** P9 (VoiceSys — Dan speaks) landed this session as the final phase; the game build is now CS010 P9. **The next session picks a new changeset** — whether to open a CS011 planning cycle or resume an older still-open queue (v3.6 P5 Game Over spectacle, v3.5 P3, the v2.0 balance levers below) is Paul's call. **Before closing CS010 out administratively**, archive `PLANNED-FEATURES-CS010.md`/`IMPLEMENTATION-PHASES-CS010.md` under `archive/` (the CS009 pattern) — NOT done this session (P9 was scoped to the build + docs, and the archive move is a clean-boundary chore best done when the round is confirmed closed).

**⚠ CARRIED FROM P9 — needs Paul (see the header's SIGN-OFF ASSUMPTION):** (1) confirm the FLAG-11a sign-off was real; (2) confirm radio-Dan (default-on) is the intended character vs the clean synth (one field: `VOICE_PARAMS.radio.on`); (3) if you made ✎ per-line phoneme fixes in a browser lab session, they were never committed to `tools/voice-lab.html` so they are NOT in the build — send the Dump / commit the lab and I'll re-port those `phon` strings; (4) captions (P8 ask #2, "recommend yes as belt-and-braces") remain UNBUILT — decide whether a future phase adds a caption render (a missed line under the drop-not-queue rule is otherwise simply gone).

**CS010 Phase 6 — Music: Drift + Warehouse B-sections (§6) — SHIPPED (P6).** (build order `IMPLEMENTATION-PHASES-CS010.md`). Compose in `tools/music-lab.html`, port verbatim (the standing `MusicSys` rule) — tracks are DATA, no `tier` fields, scheduler untouched. **CS010 P5 (this session) landed exactly as scoped** — the one open question the prompt itself flagged (180% can't apply to both `drawScoreTable` callers) was resolved by parameterizing (`scale`, default 1) rather than picking one interpretation; no other design decision surfaced. **New playtest ask from P5, carried forward:** is 1.8 right on the browsable High Scores screen, and does the gameover table now read small by comparison — if so, re-flowing the gameover screen to afford a bigger table is a separate, un-scoped follow-up, not this phase's job. **CS010 P3's own playtest ask (still open, not superseded by P4/P5):** which `LOWHP_GLOW_*` values, and does the synced hull-ring/glow/siren read as one alarm. Two things carried forward from P2, neither a blocker for anything since: `CARGO_TURN` ships **dormant at 0.0** — enabling it (suggest 0.02–0.03, see the table above) is what turns on real rotational inertia; and the "stopping half of inertia" is a **proposal in the P2 notes above, not shipped**. The V's known incompleteness from P1 (no rear-edge tell) remains a deliberate, already-made trade — not an open question.

## GDD version history

- **CS010 P0 — version stamp.** Specs in `PLANNED-FEATURES-CS010.md` §2 (FORK-1 resolved as v1.0.0.10). `GAME_VERSION` changed from the bare `"3.6"` to `"1.0.0.10"`, unprefixed, under a new scheme: **Major.Minor.Patch.Changeset** — Major = new mechanics/levels, Minor = tweaks to existing systems, Patch = bug fixes (where "bug" includes the game not doing what the design intends), Changeset = the `CS###` number, monotonically increasing, never resets; the 4th segment **is** the changeset number, so this build (shipped in CS010) reads `1.0.0.10`. Grepped every read of `GAME_VERSION` first (`CLAUDE.md`'s grep-first rule) — confirmed exactly one, the `build` field stamped by `HighScores.add()` — before touching the constant, so no other call site needed updating. Rendered on the **title screen only**, lower-right corner, as `"v" + GAME_VERSION` at 12px `COLOR.dim`, via two new named inset constants (`VERSION_STAMP_INSET_X/Y`, both 16px) rather than inline literals — a build stamp, deliberately out of the way of the title art and the drifting debris, not a design element. **Existing high-score records are not migrated** — `build:"3.6"` entries in `afd_scores_v1` stay exactly as they are (the field is a free-form provenance string; a mixed table across builds is expected and by design). Headless-tested: `GAME_VERSION === "1.0.0.10"`, a fresh `HighScores.add()` stamps the new build string, and a legacy `build:"3.6"` record round-trips through `HighScores.load()`/`save()` unchanged. No gameplay/balance numbers touched.

- **CS010 P1 — scoop render reverted to the prong-V, capture-box fill retired.** Spec `PLANNED-FEATURES-CS010.md` §7. Render-only: `Ship.draw()`'s scoop block now draws an open `drawPoly` V (`[[d,-hw],[16,0],[d,hw]]`, throat at the nose, no fill) instead of the v3.6 P1c translucent-fill rectangle; `inScoopBox()`'s capture math — the box from `forward = -SHIP_RADIUS` to `SCOOP_DEPTH[lvl]`, `|lateral| ≤ SCOOP_WIDTH[lvl]/2` — was not touched. `tools/scoop-lab.html`'s duplicate render was reverted in the same commit (audited first, at the live `5.0/60` `SCOOP_CONFIG`, before porting — the V was originally sized for the old `3.0/36` config and needed a look-check at the wider scale; it read fine, no re-tune needed). This is the third rewrite of this render (V through v3.5 → box in v3.6 P1c → V again now): Paul decided the cleaner open-prong look is worth losing the box's one advantage, the tell for the capture region's rear edge (`forward = -SHIP_RADIUS`, behind the nose) — a canister drifting into that strip is now captured with no visual cue. GDD §2.14.1's Render bullet was rewritten to carry this history (not just overwritten) so a future session doesn't "fix" the V back to the box without knowing the trade was already made. GDD §3.2's no-fills exception list dropped from three to two (the capture-box fill retired; the death-flash and SCOOP-pip-row exceptions stand). `CLAUDE.md`'s no-fills paragraph was checked for a stale reference — it only names the SCOOP pip row, which is unaffected, so no edit was needed there. Headless-tested (`scratchpad/test-cs010-p1.js`, new): `node --check` on the extracted script, plus `inScoopBox()` diffed HEAD-vs-working-tree across 6 scoop levels × 6 ship angles × 19 ship/garbage offsets (684 poses) — byte-identical booleans throughout, confirming the capture region truly didn't move. Also repurposed `scratchpad/test-v33-p3.js` section (10), which had asserted the old box render's rear edge equalled `-SHIP_RADIUS` — now checks the V's front edge/half-width/nose-throat instead, while its `inScoopBox()` boundary probes (rear edge included) still run unmodified; full 33-file regression green.

- **CS010 P2 — `shipTurnRate()` owns both turn sites; rotation-speed slider; towed-mass retune + `CARGO_TURN` knob.** Specs `PLANNED-FEATURES-CS010.md` §3 (towed mass), §4 (Engine), §10b (rotation slider). Three items in one phase because §3 and §10b both own the two `Ship.update` turn lines. **(1) One turn source:** new `shipTurnRate()` = `SHIP_TURN × settings.shipTurnScale / (1 + chainMass()×CARGO_TURN)` (placed after `chainMass()`); both `Ship.update` turn sites now read it, so they can't diverge. **(2) Rotation-speed slider (§10b):** new `settings.shipTurnScale` (0.5–1.5 in 0.10 steps, default 1.0), a *player preference* not a balance value, persisted **additively** in `afd_settings_v1` (missing key → default; non-numeric / out-of-range → clamps to 1.0; loaded value snapped to the 0.10 grid). New *Ship Rotation* row on the **existing** Controls screen (`adjustShipTurnScale()` in `menuControls`, `drawTurnScaleRow` by `drawControlsMenu` — a percentage readout with an always-visible gold "home" tick at 100%; panel grown 470→512 px for the row). `turnRow`/defaults/back indices are **derived** from `REBINDABLE.length`, never hardcoded. **FLAG-10a:** `returnToDefaults()` deliberately does **not** reset `shipTurnScale` (Paul's call — the player returns to 100% manually via the slider); a comment at `returnToDefaults` says so. The v3.5 P1 `e.repeat` menu-open guard was traced (not assumed) to cover the new row — a held ◄/► nudges once. **(3) Towed-mass retune (§3), all playtest knobs:** `CARGO_THRUST` 0.06→0.07, `CARGO_MAXSPD` 0.03→0.035 (modest — the `1/(1+m·k)` asymptote caps how much they can bite the mid range without breaking the full chain), and the real mid-range lever `CARGO_MASS` 0.07→0.10 (the momentum tug: its 1.4 cap now hits at m=14 not m≈20, strengthening the low-to-mid pull-back **without** moving the full-24 endpoint, which stays capped). Full-24 handling moved ~41%/58% → **~37%/54%**. The stale B-8 comment (which still reasoned about a *20*-node ceiling) was rewritten to the actual 24-node build. **(4) `CARGO_TURN` (§3), the "inertia" fix shipped DORMANT at 0.0:** a towed-mass turn-rate penalty routed through `shipTurnRate()`; at 0.0 it's byte-identical to the old turn × the player scale, so Paul enables real rotational inertia with one number. STATUS.md carries the candidate turn-mult table (m=6/12/24 × `CARGO_TURN` 0.01–0.05). **(5) Engine (§4): no code change** — `ENGINE_MASS_MULT` stays 0.5; because §3 makes mass matter more, halving it matters more as a consequence; FLAG-4a (no-op on an empty chain) accepted, not fixed. **FLAG-3a re-run (`scratchpad/test-cs010-p2.js` (D)):** at the documented 1/60 stress methodology the worst-case link stretch is **still 4.11 px**, byte-identical HEAD-vs-worktree (delta 0.000 — the tug's 1.4 cap is unchanged and the kinematic stress isolates the constraint solver); under a realistic *faithful* stress at the true 0.05 clamp the retune is marginally *stabilizing* (worktree ≤ HEAD at every mass — 8.17 vs 8.33 px at mass 24, 11.61 vs 12.61 px at mass 6). Record-corrected: the 4.11 px figure is a dt=1/60 measurement, not 0.05 (the same over-stress velocity at 0.05 gives ~14.8 px on *both* builds — a big-timestep artifact, no NaN/blowup). Headless-tested: `shipTurnRate()` across scale 0.5/1.0/1.5 × mass 0/12/24 (dormant `CARGO_TURN`) plus a `CARGO_TURN=0.02` rebuild proving the divisor is live; `shipTurnScale` round-trip / missing-key / corrupt-value / `returnToDefaults`-isolation; the FLAG-3a stress; `test-p6.js` (D)'s coefficient/handling assertions updated to the new balance. Docs: GDD §2.10/§2.14/§3.4 (turn penalty + envelope re-validation), this entry, STATUS.md (with the `CARGO_TURN` table and a proposed-not-shipped "stopping" lever for the missing half of inertia).

- **CS010 P3 — low-health corner glow + relief pop; `lowhpPulseRate()` + `game.lowHpPhase` sync the hull ring, glow and siren.** Spec `PLANNED-FEATURES-CS010.md` §5 (FORK-2 = (a) sync everything; FORK-3 = (a) a fill; FLAG-CS010-a resolved: ship a visual relief fallback, not voice-only). **The contentious one:** it reverses a decision CS009 made deliberately and wrote into the code, and reopens the no-fills rule CS009 hardened one changeset ago — Paul signed off on both. **(1) One shared rate:** extracted `lowhpPulseRate(t)` = `LOWHP_PULSE_RATE_MIN + (MAX−MIN)·t` (0.9→2.4 Hz); `AudioSys.lowhpSet` now reads it instead of duplicating the interpolation, and a new **phase accumulator** `game.lowHpPhase` (advanced `(+= 2π·lowhpPulseRate(t)·dt) % TAU` in `update()` while engaged) drives the visuals — so the audio siren, the critical-hull ring pulse, and the corner glow are **one instrument at one HP-ramped rate**. `HUD_PULSE_HZ` (1.2) is now the flat rate for the **non-HP** alarms only (cargo-gold, powerup low-timer) — its stale/wrong CS009 comment ("critical/max/full-cargo … the siren already ramps") was rewritten (max-hull isn't pulsed; the powerup low-timer, a third red element, is). **Why an accumulator, not `phase = ω·clock`:** `ω` is HP-dependent, so `ω·clock` would snap the phase (~30 rad on a 0.5 rad/s Δ at t=60 s) each HP change and visibly stutter; the glow **never** reads a Web Audio LFO value (unreadable, and `ctx` may be null pre-gesture — it must render with audio unavailable). Lives in `update()`, so it freezes while paused (correct — `openPause` tears the siren down) and resets to 0 on disengage + `startGame`. **(2) The glow itself (FORK-3 = FILL, a named 4th→3rd GDD §3.2 exception):** four `createRadialGradient` corner fills in `COLOR.lowhp`, drawn first in `drawHUD()` (so Capture's **H** hides it, **P** exports clean), engaging on the exact same `game.lowHpSiren` predicate; intensity scales with **both** urgency `t` (brighter near death, `LOWHP_GLOW_ALPHA_MIN`→`_MAX`) **and** the shared pulse — brighter, not merely faster; alpha rides the gradient stop so it never touches `globalAlpha`. A fill *by design*: a peripheral, edgeless, low-frequency, large-area alarm reads out of the corner of the eye without competing with the two red rings, and costs no `shadowBlur` (a `glowStroke` arc would be a third red line and burn the renderer's hot spot). `LOWHP_GLOW_*` are PLAYTEST KNOBS, sized to **frame** the two occupied corners, not wash them out. **(3) The no-fills bookkeeping:** GDD §3.2's exception list was **rewritten** — count unchanged at **three**, but membership changed across CS010 (P1 retired the scoop capture box; P3 added the corner glow); the reasoning is recorded there and in `CLAUDE.md`'s (also-updated) no-fills paragraph so a future cleanup pass doesn't strip it. **(4) Relief cue (FLAG-CS010-a):** §5 specced Dan's voice line, but voice is a later gated phase, so P3 ships a **permanent** visual relief that stands alone (Dan's line is additive if it lands): on the falling edge, **only if the ship is alive** (`game.hpReliefFlash = LOWHP_RELIEF_FLASH`), a **geometry-only** (radius + width) **non-pulsing** exhale on the hull ring, decaying over `LOWHP_RELIEF_FLASH` — reusing CS009's `HUD_BANK_POP` radius pop and the cargo cap-up width bump. Color deliberately untouched (on relief `hpCrit` is already false so `hullColor` is back to `COLOR.hp`; hpMax gold still wins), so it composes with the precedence chain and can't leak `globalAlpha`. **The alive-guard is load-bearing:** dying also drives the low-health state false, so without `!ship.dead` the ring would pop a cheerful "you're okay now" at the instant of death; the flash is also zeroed at every direct siren teardown (pause/quit/start/death). Headless-tested (`scratchpad/test-cs010-p3.js`, new, 32 assertions): `lowhpPulseRate(0/1)` = MIN/MAX; `lowHpPhase` advances by exactly `2π·rate·dt` (mod TAU), is 0 after `startGame` and on disengage; `drawHUD()` renders without throwing with `AudioSys.ctx` null (glow engaged and off) and never leaks `globalAlpha` (== 1 after return); relief flash arms exactly once on heal and decays to 0, arms **zero** times on death (both the `killShip` drive and the explicit `!ship.dead` guard) and across a pause→resume, and is zeroed by `startGame`. Full 34-file regression green (`test-starfield.js` is pre-existingly RNG-flaky — the swept-ship test occasionally clips a randomly-placed debris; unrelated to this diff, which touches no movement/spawn/collision code). Docs: GDD §2.12 (the four synced tells + relief cue), §3.2 (rewritten exception list), `CLAUDE.md` no-fills paragraph, this entry, STATUS.md (with the PLAYTEST ASK: which `LOWHP_GLOW_*` values, and does the synced hull-ring/glow/siren read as one alarm).

- **CS010 P4 — Sound/Music sub-dialog; High Scores nested under Options; both hardcoded menu indices killed.** Spec `PLANNED-FEATURES-CS010.md` §8b (reachability), §9 (Sound/Music sub-dialog), §10a (final `MENU_OPTIONS`); FORK-4 resolved: remove "High Scores" from `MENU_ROOT_SYS`. A relocation, not a redesign — same control style throughout, no new widgets. **(1) FLAG-8b fixed first, before the restructure landed on top of it:** `menuControls`'s two `gotoScreen("options", 3)` sites and `menuAchievements`'s one `gotoScreen("options", 4)` site all now resolve via `MENU_OPTIONS.indexOf("Controls")`/`.indexOf("Achievements")`, matching the pattern `menuDifficulty` already used — no index-fragile call site survives anywhere in the menu tree. **(2) New "Sound / Music" sub-screen (§9):** `SOUND_ROWS = ["SFX Volume","Music Volume","Master Volume","Music Track","Back"]` + `menuSound(a)` + `drawSound()`, built to the `menuDifficulty`/`drawDifficulty` template exactly (a `"sound"` case in `menuInput`'s switch, a branch in `drawMenu`'s dispatch, Back resolving via `MENU_OPTIONS.indexOf("Sound / Music")`). The three volume sliders + Music Track cycler moved off Options onto it; `VOL_LABELS`↔`VOL_CATS` stayed a paired lookup (re-verified: the coupling was already label-driven, not "parallel to the first three `MENU_OPTIONS` rows" as originally assumed — the spec's re-grep step caught this before it caused a bug). `drawOptionsMenu` is now a plain centered nav list, matching `drawRootMenu`'s layout, since Options no longer carries any adjustable value rows. Persistence unchanged — still round-trips through `saveSettings()`/`afd_settings_v1`. `SOUND_ROWS` is left additive-shaped for P9's planned Voice Volume row (not built this phase). **(3) Final `MENU_OPTIONS` (§10a):** `["Sound / Music","Controls","Achievements","High Scores","Difficulty","Back"]`. **(4) FORK-4 — "High Scores" removed from `MENU_ROOT_SYS`:** the old back-path (`gotoScreen("root", rootItems().indexOf("High Scores"))`) would have resolved to `indexOf === -1`, and since `gotoScreen` does `index || 0` — where **`-1` is truthy** — the cursor would have landed off the list; rewritten to `gotoScreen("options", MENU_OPTIONS.indexOf("High Scores"))`. With one entry point (nested under Options, reachable from both the system menu and the pause menu — the entire point of §8b), no `scoreReturn` tracker (mirroring `achReturn`) was needed. The now-dead "High Scores" label branch was dropped from `menuRoot()`. P2's *Ship Rotation* row on the Controls screen was traced and confirmed untouched. Headless-tested (`scratchpad/test-cs010-p4.js`, new, 32 assertions): syntax; `MENU_ROOT_SYS`/`MENU_OPTIONS`/`SOUND_ROWS` shapes; a source-level regex assertion that no `gotoScreen("options", <numeric literal>)` or negative-literal-index call survives; full navigation coverage by LANDING LABEL (not index) for root→Options→Sound/Music→Back, Options→High Scores→Back from both the system-menu and pause-menu entry paths, Options→Controls→Back (+ the rotation row), Options→Achievements→Back from both entry paths (`achReturn` still tracks two), Options→Difficulty→Back, and the Sound/Music sliders/track-cycler/persistence. Three pre-existing test files broke on the restructure and were fixed in this commit (not reimplemented, just re-routed through the new screen/labels): `test-f8.js` §B (volume sliders now dispatch via the "sound" screen), `test-v34-p6.js` §F/§O (added an `openSoundAt()` helper alongside the existing `openOptionsAt()`), `test-v36-scores.js` §J (High Scores reachability rewritten for both entry paths, per the FORK-4 change). Full 35-file regression green. Docs: GDD §2.16 (new CS010 P4 bullet + Architecture Map rows), §2.18 (Rendering/reachability bullet), this entry, STATUS.md (noting High Scores is now two keypresses from the title — O → Options → High Scores — instead of one, accepted per FORK-4).

- **CS010 P5 — `drawScoreTable` takes a `scale`; browsable High Scores at 180%, gameover left at 1.0.** Spec `PLANNED-FEATURES-CS010.md` §8a. The prompt's own arithmetic ruled out applying 180% to both of `drawScoreTable`'s two callers (re-grepped and confirmed still exactly two): the gameover table sits at `VIEW_H/2 + 60` in a 720-tall viewport with "PRESS ENTER TO PLAY AGAIN"/the O-hint at `+280`/`+312` below it — at 180% the 10th row lands at ≈747, off the bottom of the viewport, through both prompts; the ceiling there without re-flowing that screen is ≈1.4×. **So the fix is a parameter, not a global bump:** `drawScoreTable(cx, topY, highlightId, scale = 1)` now multiplies every font size, the row pitch (18), the header offset (22), AND all five column x-offsets (±230/±170/+10/±130 around `cx`) by `scale` together — bigger glyphs need wider columns, so the columns scale too, not just the type. **The two callers deliberately diverge:** `drawHighScores()` passes a new `HS_TABLE_SCALE` (1.8); the gameover call site is byte-identical (still 3 args, so `scale` defaults to 1) — its layout has no room to grow without a separate, un-scoped re-flow, and the fresh entry is already highlighted gold there, so it doesn't need the bigger type to stand out. A code comment at `drawScoreTable` explains the divergence so a future "unify these two calls" pass doesn't push the table off the gameover screen. **The browsable panel grows to fit, per spec** (shrinking the font was explicitly ruled out): hand-computed for 10 rows at the 1.8 pitch — header offset 39.6, row pitch 32.4, row 10 baseline at `topY + 331.2` — `menuPanel` grew 640×460 → **1000×560**, the table's `topY` moved to `y + 120` (up from `y + 90`), and the footer moved to `y + 497` (from `y + 420`), all with air to spare below the panel's inner bottom; none of these are runtime-derived, they're one-line tunables re-derived by hand if `HS_TABLE_SCALE` ever changes. Headless-tested (`scratchpad/test-cs010-p5.js`, new, 21 assertions): syntax; the no-scale-arg call reproduces the exact pre-P5 geometry (font 13, pitch 18, header offset 22, unscaled column offsets) byte-for-byte; `scale = 1.8` scales fonts/pitch/header-offset/columns together; a recording-canvas capture of every `fillText` call from `drawHighScores()` with a full 10-row table stays within the panel's own `menuPanel` `strokeRect` bounds; the same capture from the gameover overlay (isolated from the preceding world/HUD draws, which are under an untracked `ctx.translate` the stub doesn't apply) stays within `VIEW_H - 20` and the table itself stays at font 13; and a source-level regex confirms the gameover caller still passes no 4th argument. Full 36-file regression green. Docs: GDD §2.18 (new Font size bullet + Architecture Map/`draw()` rows), this entry, STATUS.md (with the PLAYTEST ASK: is 1.8 right on the browsable screen, and does the gameover table now read small by comparison — if so, the fix is re-flowing the gameover screen, a separate un-scoped job, not touched here).

- **CS010 P6 — Drift and Warehouse get B-sections; longer loops, hook layer restored to Warehouse.** Spec `PLANNED-FEATURES-CS010.md` §6. Only `buildDriftTrack`/`buildWarehouseTrack` touched; Beacon/Zen/Derelict unchanged. Data-only per the standing rule — `MusicSys.update`/`scheduleStep`/`layerGates`, `playNote`'s voice branch, and the frozen intensity machinery (no `tier` fields, `setIntensity()` still a no-op) were **not** touched; track names `drift`/`warehouse`, `settings.musicTrack`, and `afd_settings_v1` unchanged, so existing saves keep working. Composed and auditioned in `tools/music-lab.html`, then ported **verbatim** (verified byte-identical between the two files). **Drift:** 8→**16 bars** (STEPS 128→256, stepDur 0.17 unchanged → 21.8→**43.5 s**), A→B: bars 0-7 are the original Am F C G Am F Dm E; bars 8-15 a **brighter C-major turn** (C G Am Em F C Dm E) with the organ arp an octave up and a fuller lead rising to C6 in bar 13, resolving on E back into A. **Warehouse:** an 8-bar Am7 Dm7 Fmaj7 G vamp now **played twice** (12→**16 bars**, STEPS 192→256, stepDur 0.12 → 23.0→**30.7 s**), A→B: bars 0-7 groove only, bars 8-15 add a **sixth `hook` layer** — a sawtooth-pluck riff on the offbeats (steps 2/6/10/14). **On the cut hook (§6's steer):** the round-2 layer Paul remembered was cut is **not recoverable from the repo** — `tools/music-lab.html` has a single commit, the round-3 "FINAL" that *deleted* the gated-off upper-tier layers, and the round-2 candidates were never committed anywhere (git history, archives, scratchpad all checked). The hook was therefore **composed fresh in the spirit of the brief** — a melodic house riff, deployed as the intermittent chorus payload the cut layer was described to be ("too present as a *constant* element" → perfect as a B-section). **This needs a playtest ear.** **FLAG-6a (node budget) MEASURED, not assumed:** worst-case node creation for a single scheduled step is **still 13** — the overall max is unchanged (`zen`/`title`/`drift` step 0 all tie at 13; Drift's B keeps the same bar-start four-voice coincidence at 13; Warehouse's `hook` sits only on otherwise-empty offbeat steps so its worst step stays **11** and it is not the overall max). All three existing test guarantees hold: every loop ≥ 10 s (Drift 43.52 s, Warehouse 30.72 s), no silent run > 1 step checked circularly (Drift 0, Warehouse 1), per-step nodes ≤ 16 (13). Headless: `scratchpad/test-v34-p6.js` (the music-system contract) re-run green — 236 assertions — plus `node --check` clean and full **36-file regression green**. Docs: GDD §2.8 (the two track descriptors + a CS010-P6 note on the re-measured node budget), this entry, STATUS.md (with the PLAYTEST ASK below). **PLAYTEST ASK:** does each B-section land as *relief/lift* (an A→B contrast within the same song) or as a *different song* bolted on — and specifically, does Warehouse's restored hook read as the chorus Paul remembers, or should it be re-voiced?

- **CS010 P7 — sixth track for the High Scores screen; `musicStateFor()` is menu-aware, duck exempted.** Spec `PLANNED-FEATURES-CS010.md` §8c. The spec's diagnosis was exactly right that the whole item is **two routing problems plus one new track** — no new track-switch code path. **(1) `musicStateFor()` is now menu-aware:** the High Scores screen is a *menu* (`game.paused` true while `game.state` is still `title`/`gameover`/`playing`), so `game.state` alone could never name it. Added a single leading clause — `if (game.paused && game.menu.screen === "highscores") return "highscore";` — reading `game.menu` in the same idiom the function already reads `settings`. Switching in/out is then a normal `setState()` crossfade. **Every other state×paused×screen combination is byte-identical to the pre-P7 mapping** (the regression the test hammers). **(2) Duck exemption:** `setDuck(menuActive())` would have dropped the bus to 50% on *every* open menu — so a track that plays *only* while a menu is open would be permanently ducked. Changed to `setDuck(menuActive() && game.menu.screen !== "highscores")`; `setDuck` is already idempotent on its target, so the extra clause costs nothing per frame. **(3) The track — `highscore` (Fanfare):** composed/auditioned in `tools/music-lab.html` and ported **verbatim** (verified byte-identical between the two files), registered as `highscore: buildHighScoreTrack()` in `MUSIC_TRACKS`. Bright C major, an anthemic I–V–vi–IV: a 16th bell arpeggio, a soft four-on-the-floor kick, a light shaker, and a triumphant triangle lead climbing to E6 before resolving to C — 6 layers, 100 BPM, **8 bars, 19.2 s** (128 steps × 0.15 s). **Contextual, like `title`** — deliberately **NOT** added to `MUSIC_TRACK_VALUES`, so it never appears in the Options picker; `settings.musicTrack`/`afd_settings_v1` unchanged. Same freeze constraints as every track: no `tier` field, scheduler untouched. **Node budget re-measured (not assumed):** the fanfare's worst step is **12** (bar-start: bell+sub+pad+kick; the lead onsets are all placed off the downbeat, steps ≡ 2/10 mod 16, so they never stack), so the **overall worst case across all six tracks is still 13**. All standing guarantees hold for it too: loop 19.2 s ≥ 10 s, longest silent run 0 steps (the bell plays every step). **FLAG (SURFACED, NOT DECIDED — for Paul):** should the cue *also* play on the **gameover** screen's post-entry high-score table, where the player has just *earned* a score? Today `musicStateFor("gameover") → "off"` — the death spectacle ends in silence by design. The spec scoped this item to "while the High Scores screen is open," so P7 builds exactly that and **leaves gameover silent**; the question is parked in STATUS.md. Headless: `scratchpad/test-cs010-p7.js` (new, 106 assertions) — exhaustive `musicStateFor()` over 4 states × paused × 8 screens (highscore IFF paused+highscores, byte-identical to pre-P7 everywhere else), the duck exemption driven through the real `updateMusic()` (every menu screen ducks, highscores doesn't), the setState in/out crossfade, the registry (in `MUSIC_TRACKS`, absent from `MUSIC_TRACK_VALUES`), and the track's loop/gap/freeze/node re-measure. `test-v34-p6.js` re-run green (244 assertions — its all-tracks gap/loop/freeze/node checks now also cover `highscore`), `node --check` clean, full regression green. Docs: GDD §2.8 (the menu-aware `musicStateFor()`, the new track descriptor, the duck exemption), this entry, STATUS.md (with the gameover-cue question for Paul).

- **CS010 P9 — VoiceSys: Dan speaks. Voice bus + volume slider, 24 lines across 8 trigger classes.** Spec `PLANNED-FEATURES-CS010.md` §11b–§11f; FORK-5 = b (fire on the pop that empties the chain). Ships after P8's lab (`tools/voice-lab.html`, FLAG-11a); the engine + phrase data were ported **verbatim** from it (the standing MusicSys/music-lab rule). (P8 was lab-only and touched no build code, hence no version-history entry — this is the first CS010 entry with a game-file change since P7.) **(0) Design decision — the g2p authoring layer is NOT ported.** The "engine, verbatim" rule protects the *acoustic* synth (the `PH` ARPAbet table, `parsePhonTokens`/`buildUtterance`/`buildPitch`, and the render scheduler), so what the lab auditioned is what ships; those are byte-identical. The lab's text→ARPAbet layer (`WORDS` dictionary + rule guesser + `g2p`) is a *composition tool* whose output — each line's `phon` string — is already baked into `VOICE_LINES`, so it is dead code in the game and stays in the lab (which is where a new line's `phon` is derived, then its `{text,phon}` pasted in). This matches the `tools/` philosophy: port the *result*, not the instrument. **(1) `VoiceSys` — a SEPARATE module alongside AudioSys/MusicSys**, never folded into AudioSys (a flat bag of one-shots that must not grow a sequencer — MusicSys is the exact precedent). Owns its radio-character processing chain (highpass/lowpass/tanh-shaper/compressor + a squelch-gated static bed, default-on radio character) feeding **`AudioSys.voice`**. Every entry point is `if (!AudioSys.ctx) return;`-guarded — a total no-op headless (what makes it headless-safe). **(2) THE VOICE BUS IS FOUR SITES, not the one the spec claimed** — all verified: (a) `AudioSys.vol` gained `voice:1`; (b) `init()` builds `this.voice` → master mirroring sfx/music; (c) `setVol`'s node picker is now a `this[cat]` **lookup** — the old `master?…:sfx?…:music` ternary would have silently written `setVol("voice",…)` to the *music* gain; (d) `loadSettings`'s volume loop now derives its category list from `Object.keys(AudioSys.vol)` so a new bus persists without a fifth edit. A **Voice Volume** row was added to P4's Sound/Music screen by growing `SOUND_ROWS`/`VOL_LABELS`/`VOL_CATS` together (`voice`), persisted **additively** into the frozen `afd_settings_v1` (no schema bump); the `menuSound`/`drawSound` handlers are label-driven, so the row was purely additive (panel grown 460→510 px). **(3) LINES ARE DATA:** `VOICE_LINES` is keyed by event, each event an **array** of `{text,phon}` alternatives — adding a line is a one-line data edit. Selection is a **plain random pick** (§11d). **(4) Cooldown / priority (§11e), designed not hacked:** `VOICE_COOLDOWN` (1.2 s) — no line starts while one plays or within that of the last one ending; `VOICE_PRIORITY` (default 1; 3 = hull critical, 2 = health milestones) lets a higher line **pre-empt** a lower one that's playing, an equal/lower one is **dropped**. **Superseded lines DROP — never queue.** **A real bug the test caught:** the "nothing has played" sentinel had to be `busyUntil = -Infinity`, not 0 — with 0, every line in the AudioContext's first 1.2 s (`now < 0 + VOICE_COOLDOWN`) would have been swallowed by a phantom cooldown. The burst case (`hit → low HP → grab a powerup` in one instant) is tested: hull-critical takes the channel, the powerup grab is dropped. **(5) Triggers + latches — every anchor verified against the live code, not the spec:** **hull-low** rides `update()`'s existing rising edge but gets its **own** latch `game.lowHpVoiced` that `openPause`/`quitToTitle` do NOT tear down (they reset the *siren* latch, which is why the siren rising edge legitimately re-fires on unpause — the voice latch is the fix for Dan re-announcing "Hull integrity is critical." on every unpause; cleared only on the genuine falling edge + `startGame`); **hull-relief** on the falling edge, **additive** to P3's `hpReliefFlash` exhale (not a replacement, and not gated on voice availability — parity, four onset tells vs. two relief tells); **hull-full** a new edge latch `game.hpFullVoice` on `hp >= SHIP_MAX_HP` (the spec's claimed "full-HP special-case seam" in `applyPowerup` **does not exist** — a bare `hp = min(max, hp+amt)` — so the latch lives in `update()`, catching healing from the Health powerup AND `addScore`'s repair milestone for free; starts **true** at spawn so a fresh full-HP run doesn't blurt "brand new ship"); **collected** in `applyPowerup` (scoop in its early-return branch; Health silent — it speaks via the hull-full latch; a same-type bank re-pickup re-fires and the §11e cooldown drops the second line if it lands in-window — a decided, acceptable behavior); **expired** routed through **`powerActive(type)`** per timed type, never `powerFx` (so it catches count modes — the assertion the test proves with a pieces-mode Magnet whose `powerFx` is always 0), stored in `game.powerVoiced{}`; **scoop-lost** at the damage-decay site in `damageShip`; **dock delivery** (FORK-5 = b) on the pop that **empties** the chain (5–9/10–14/15–19/20+ tiers by `game.deliveryCount`; 1–4 silent) — the offload block runs only while `chain.length > 0`, so the emptying pop happens once and **the gate is the latch**, no flag; `breakChain`/`scatterChain` also zero `deliveryCount` but never reach the site, so they stay silent (asserted); **truck-full** on the pickup where `chain.length` reaches the **runtime** `game.cargoMax` (12→24), placed after the single/clump branches so both trip it, again gate-as-latch, re-arming after a delivery (a clump-scoop that fills the chain fires exactly once — asserted). `startGame` resets the three latches + calls `VoiceSys.reset()`. Headless: `scratchpad/test-cs010-p9.js` (new, 75 assertions) — the four bus sites (setVol moves voice not music; volume round-trips `afd_settings_v1`, defaults to 1); the ctx-null total-no-op (say + a real low-HP `update()`); cooldown/priority (drop-not-queue, pre-empt, cooldown gap, the burst case) driven against the real `say()` with an advanceable mock clock; and **every trigger driven through the real `update`/`applyPowerup`/`damageShip`/dock-offload/`breakChain`/`scatterChain`** with a `say()` spy — each edge fires exactly once, **pause+resume at low HP does NOT re-fire hull-critical** (the `lowHpVoiced` regression this phase exists to prevent, asserted explicitly), relief fires with `hpReliefFlash` still arming, hull-full is edge-once and silent at spawn, count-mode expiry fires, the dock tiers map correctly (1–4 silent), and a clump-fill fires truck-full once. **Four older test mocks were completed** (they now exercise the voice engine because gameplay emits voice when a context exists): `test-cs009-p1`/`test-cs009-p5`/`test-p5` gained the AudioParam methods on their `Q` node (and `test-p5` `cancelScheduledValues` on gain), and `test-v34-p5` gained the `createWaveShaper`/`createDynamicsCompressor`/`createPeriodicWave` factories + `setPeriodicWave`; `test-cs010-p4`'s SOUND_ROWS snapshot was updated for the new row. Full **39-file regression green**. Docs: GDD §2.8 (new VoiceSys subsection + the four-bus gain-routing note), §2.16 (Voice Volume slider), §3 Architecture Map (new VoiceSys row + AudioSys/update rows updated), CLAUDE.md (VoiceSys non-negotiables), this entry, STATUS.md.
