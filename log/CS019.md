# CS019

**Prior session (CS019 P2, landed — CS019 IS COMPLETE):** implemented CS019 Phase 2 (per `IMPLEMENTATION-PHASES-CS019.md` P2), the changeset's closing phase — retune, version bump, doc sweep, no new logic — reading `CLAUDE.md`/`STATUS.md`/`PLANNED-FEATURES-CS019.md`/`IMPLEMENTATION-PHASES-CS019.md` in full and grepping every anchor in `asteroids-deluxe.html` by symbol rather than reading it whole. **(1) No retune applied.** Paul's playtest results named all three tunables — `chainGuardCooldown`, `chainGuardIntercepts`, `GUARD_ABSORB_SPARKS` — "no change", so per the phase's own standing rule ("if a line says no change, change nothing there") none of the three was touched; confirmed still at their P1-landed values (`chainGuardCooldown` def 0.75/[0.1,3]/0.05, `chainGuardIntercepts` def 3/[1,10]/1, `GUARD_ABSORB_SPARKS` 7) after the session. **(2) `GAME_VERSION` `"1.0.0.18"` → `"1.0.0.19"`, grepped across the whole repo rather than trusting the phase doc's own file list** — the CS016 P5/CS017 P7 precedent held again: the grep found **seven** live pins, one more than the six the P2 prompt named. `asteroids-deluxe.html` plus six of the prompt's named test files (`test-cs010-p0.js`, `test-cs013-p4.js`, `test-cs015-p7.js`, `test-cs016-p5.js`, `test-cs017-p7.js`, `test-cs018-p10.js`) all bumped, both the `console.log` label and the assert message string in each; a **seventh, `test-cs019-p1.js`'s own P1-authored live pin** (not named in the prompt, found only by the grep) bumped the same way. `test-cs017-p7.js`'s header-comment narrative ("GAME_VERSION 1.0.0.17", describing what THAT phase historically bumped to) was left alone, per the same CS018 P10 precedent that first drew this distinction. **(3) The five CS018-phase tests' stale `GAME_VERSION` pins were repointed to their mirror image, not weakened.** `test-cs018-p1/p3/p4/p6/p7.js` each asserted `GAME_VERSION === "1.0.0.17"` under the message "unchanged this phase (bumps in P10)" — a claim that was **already false and already failing before this session**, since CS018 P10 bumped the version and deliberately left those five red as historically-scoped claims (confirmed in the P1 session entry below, which swept the pre-fix commit `6928ff3` and found the same five failures at baseline). Each is now `assert(GAME_VERSION !== "1.0.0.17", ...)` — the version has moved past what that phase shipped — the same claim (an exact historical pin) at the same strength, just pointed at what is now true. `test-cs019-p1.js`'s own P1-authored "unchanged this phase (bumps in P2)" pin got the identical treatment, now that P2 is the phase in question: `assert(GAME_VERSION !== "1.0.0.18", ...)`. **(4) GDD updated, with one correction applied against the P2 prompt's own stale anchor.** §2.14.2 (retitled "absorb cooldown CS019") gained a new bullet describing the mechanic exactly as landed in P1: the source-hazard `guardT` stamp, the scan-side skip gated on `powerActive("guard")` being true (not merely on the stamp), the count-mode unit reconfirmed as one absorbed break EVENT (a claim §2.14.2 already made and this fix is what makes it actually hold), and guard-down behaviour explicitly called out as byte-unchanged. **§2.19's DEBUG_VARS count was corrected to 32 → 33, not the "15 → 16" the P2 prompt's own anchor table literally said** — `STATUS.md`'s own "Next up" note from the P1 session had already caught this staleness (both `PLANNED-FEATURES-CS019.md` FLAG-CS019-a and the P2 anchor table carry a count left over from a much older planning pass) and flagged grepping the live GDD instead of trusting either doc; done — the registry held 32 value entries before CS019 P1 added `chainGuardCooldown`, 33 after. A fourth `CHAIN GUARD` knob bullet was added alongside the existing three. **The GDD's top-of-file Current-build line was also updated** (not explicitly named in the P2 prompt's item (4), but every prior version-bump/round-closing phase — CS017 P7, CS018 P10 — has rewritten this exact line for its own commit, confirmed by checking those two commits' diffs; leaving it reading "CS018 P10" after CS019 shipped would make the GDD's own build-tracking line wrong): now "CS019 P2 — CS019 COMPLETE, both phases landed … `"1.0.0.19"`", with a one-line summary of the fix and the CS018 P10 recap folded down to a shorter "Prior build" pointer. **(5) `DIFFICULTY-LEVERS.md` checked, left untouched, as the prompt allowed** — grepped for any chain-guard mention; none exists (the two "guard" hits are both the unrelated `DEBRIS_SPEED_CAP` guard-rail prose), so no row was added for what is a defensive powerup, not a difficulty lever. **(6) Appended ONE consolidated CS019 (P1+P2) entry to `GDD-VERSION-HISTORY.md`**, per the CS012/CS013/CS015/CS016/CS017 precedent — the only file opened specifically to append to, not read for context. **Noted in passing, not fixed (out of this phase's scope): CS018 has no corresponding entry in that file at all** — grepped and confirmed neither "CS018" nor "CS019" appeared before this session's append, so CS018 P10's own doc sweep never added one; a gap from that round, left for whoever next has reason to touch that file, since backfilling it is not this bug-fix changeset's job. **(7) Archive step was already done, verified not re-run** — `ls archive/` confirmed `PLANNED-FEATURES-CS018.md`/`IMPLEMENTATION-PHASES-CS018.md` are already there (commit `a7a817a`, "Prepare documents for CS019", CS019's own P1 prerequisite, exactly as the P1 session's "Next up" note flagged); nothing moved this session, as expected. **Full regression: 83 test files, 31,259 assertions, 0 failures** — every file green, including the five repointed CS018-phase files and `test-cs019-p1.js`'s own repointed pin. `node --check` clean on the extracted `<script>`. Docs touched: `ORBITAL-OVERHAUL-GDD.md` (§2.14.2, §2.19, Current-build line), `GDD-VERSION-HISTORY.md` (new consolidated entry), this file. `DIFFICULTY-LEVERS.md` deliberately untouched (item 5). Committed per `CLAUDE.md`'s standing commit-per-phase rule; did NOT push. **CS019 IS COMPLETE.**

**Prior session (CS019 P1, landed):** implemented CS019 Phase 1 (per `IMPLEMENTATION-PHASES-CS019.md` P1, spec `PLANNED-FEATURES-CS019.md` §1–§2, FORK-CS019-A → (a) per-hazard absorb cooldown, FORK-CS019-B → (a) one charge per absorbed break EVENT), reading `CLAUDE.md`/`PLANNED-FEATURES-CS019.md`/`IMPLEMENTATION-PHASES-CS019.md` in full and grepping every anchor in `asteroids-deluxe.html` by symbol rather than reading it. **The diagnosis was independently confirmed BEFORE any edit**, by a throwaway probe that staged the phase-prompt's exact scenario against the unmodified build and printed per-frame state: budget 3→2→1→0 on frames 1/2/3, chain severed 10→3 nodes on frame 4, 6 canisters cut loose, 3 floaters, 3 `shieldPing()`s. Agreed with the prompt's diagnosis in full; the numbers are now a permanent cross-build control in the new test file, not a one-off measurement. **Five edits, all in `asteroids-deluxe.html`.** (1) `DEBUG_VARS` gains `{ id: "chainGuardCooldown", label: "Chain guard cooldown", unit: "s", def: 0.75, min: 0.1, max: 3, step: 0.05 }`, appended inside the existing `{ header: "CHAIN GUARD" }` group after `chainGuardMinTow`, no `toNative`, **no shadowing const** — the registry entry is the sole source of truth, and the group's doc comment was rewritten from "three knobs" to four. (2) `DebrisSatellite` and `HunterSatellite` each gain `this.guardT = 0` in the constructor plus `if (this.guardT > 0) this.guardT -= dt;` in their existing `update(dt)`. **`Bullet` deliberately gains nothing** — a hostile round is marked dead *before* the break and filtered at end of frame, so it can never re-present; that asymmetry is the whole shape of the fix. (3) `breakChain(i)` → `breakChain(i, src = null)`, with `if (src) src.guardT = DEBUG.chainGuardCooldown;` added inside the existing guard branch AFTER the spend and AFTER the tell; nothing else in that branch changed, and the decrement stays exactly `- 1` per absorbed break (FORK-CS019-B (a)). The `= null` default is load-bearing: `test-cs017-p6.js` drives `C.breakChain(4)` / `D.breakChain(6)` / `S.breakChain(6)` with one argument, including a `while (S.powerActive("guard")) S.breakChain(6)` drain loop, and all of those keep their exact prior behaviour (absorb, spend once, stamp nothing) — verified green, untouched. (4) The `chainScan:` inner hazard loop gains `if (h.guardT > 0 && powerActive("guard")) continue;` immediately after `if (h.dead) continue;`. **Both halves of that line are load-bearing and both are pinned by test:** it lives in the SCAN and not at the choke point because the scan `break chainScan`s on the first overlapping pair, so a cooldown test inside `breakChain` would let a stamped hazard on an early node consume the scan's one slot and hide a *different, unstamped* hazard genuinely hitting a later node (test (E) — and a mutant implementing exactly that rejected design fails it); and the `&& powerActive("guard")` clause is what stops a stale stamp granting free passage once the guard ends mid-contact (test (G), plus a mutant dropping the clause fails both (F) and (G)). (5) Both call sites pass their source: `breakChain(i, b)` and `breakChain(i, h)`. **FLAG-CS019-a's open question — "step 0.05 is the first sub-1.0 step in the registry, verify the panel handles it" — was checked and the premise turned out to be false:** CS018 P6/P7 already ship **eight** entries at `step: 0.1` (`ufoDirChangeFreq*`, `ufoFireFreq*`, several with `min: 0.1`), so a fractional step needs no new machinery. Verified anyway by DRIVING the real paths rather than by inspection: `menuDebug("right")`/`("left")` step by exactly 0.05 and clamp to exactly 0.1 / 3 with no float undershoot, `DEBUG_ENTRY_CHARS` already admits `.`, "0.35" is four characters against `DEBUG_ENTRY_MAXLEN` 8, `debugEntryCommit()` commits it exactly (unsnapped to step, which is the documented intent of typing a value), and it round-trips through the real `saveSettings`/`loadSettings` into `afd_settings_v1.debug` additively. **New `scratchpad/test-cs019-p1.js` (138 assertions, (A)–(I), 0 failures)** drives the REAL `startGame`/`update(1/60)`/`breakChain`/`applyPowerup`/`applyDebug`/`menuDebug`/`debugEntry*` — nothing under test reimplemented. (A) `node --check` + source pins: one `function breakChain` definition and exactly two call sites *under `test-cs017-p6.js` §A's own regex*, both passing a second argument, the guard branch ordered spend→tell→stamp→return→(sever below), the skip proven to be in the scan and not the choke point, the registry entry's exact spec with exactly one consumer of `DEBUG.chainGuardCooldown` and no shadowing const, group order `[time, intercepts, minTow, cooldown]` with cooldown last (appended, not inserted), TRAP 3 (the other three chain-guard knobs byte-unchanged) and TRAP 2 (`GAME_VERSION` still `"1.0.0.18"`). (B) **the regression**, in three parts: (B1) 45 frames — exactly one `chainGuardCooldown` period — spends exactly ONE charge with exactly one floater, one ping and one 7-particle burst; (B2) the prompt's 60-frame window; (B3) the same staging run against the **pre-fix build at the pinned SHA `6928ff3`**, asserting the old build's frame-1/2/3 burn and frame-4 sever as a permanent red control (a fixed SHA, never `HEAD` — the `test-cs017-p3.js` trap CS017 P6 repointed). (C) time mode: the clock falls by exactly `60 × dt` and nothing else, a non-zero **sentinel** budget comes out bit-identical (the `test-cs017-p6.js` §D idiom — "still 0" would pass even against an unconditional decrement), tells never closer together than the cooldown. (D) cooldown expiry at 0.2 s / 10 intercepts: a RANGE (4–6, ideal 5), asserted as a range and explained in the message, because `guardT` is decremented by repeated float subtraction of `1/60` and an event can land a frame either side of its ideal tick — pinning an integer would make the assertion about IEEE-754 rather than about the mechanic; plus the live-knob check that raising the cooldown mid-run immediately slows the billing. (E) the §2.1 no-shadowing property. (F) FLAG-CS019-c: with the guard never picked up, the fixed and pre-fix builds produce a **bit-identical** break under a shared seeded RNG — same frame, same surviving chain length, same loose-canister count, and every surviving node and every cut-loose canister at an identical position to six decimal places — and nothing is ever stamped; plus a hand-stamped body still severs an unprotected tow. (G) budget exhaustion mid-contact at 1 intercept with a 3 s cooldown (so the stamp is provably still live): frame 1 absorbs and stamps, frame 2 severs. (H) the bullet path. (I) `AudioSys.ctx` null smoke. **The suite is mutation-tested:** four deliberate breakages of the shipped code — dropping the `&& powerActive("guard")` clause, removing the stamp, dropping the `h` argument at the hazards call site, and implementing the §2.1-rejected "cooldown test inside `breakChain`" design — each fail it, on behavioural assertions and not merely on source pins. **Full regression: 83 test files, 31,252 assertions passing, 5 failing — and those 5 are pre-existing, not caused by this phase.** Measured properly rather than assumed: the pre-fix commit `6928ff3` was checked out into a scratch `git worktree` and swept with the identical runner, giving 82 files / 31,098 passed / **the same 5 failures**. A per-file diff of the two sweeps shows *no file's failure count changed*. **Four pre-existing files were REPOINTED, not weakened** (the standing CS017 P7 / CS018 precedent), all for the same reason — this phase legitimately grows `DEBUG_VARS` from 32 to 33 value entries: `test-cs018-p4.js` §H, `test-cs018-p6.js` §I and `test-cs018-p7.js` §H each pinned an exact registry count of 32 ("as of CS018 P7"); each is now an exact count of **33**, *plus a new assertion naming `chainGuardCooldown` as the entry that moved it* — the same claim (an exact live-registry count, so a silent add or drop still fails) at strictly greater strength, since the bare number could previously be satisfied by any addition. `test-cs018-p2.js` §M is the panel's scale check: it inflates the row model to a target and asserted the live registry "has not yet passed CS018's 32 value rows", which this phase makes false in the direction the section can't handle (`need` goes negative and it would try to remove rows); the target now tracks `Math.max(32, live)` and the floor assertion flips to its **mirror image** — "is at or past CS018's 32" — with the exact-count assertion preserved against the derived target. Both are exact bounds on the live count; neither is softer. **Not touched, and note this before running P2:** the five CS018-phase files (`test-cs018-p1/p3/p4/p6/p7.js`) that assert `GAME_VERSION` is `"unchanged this phase (bumps in P10)"` are the 5 failures above. `PLANNED-FEATURES-CS019.md` §6 and the P1 prompt's TRAP 2 both say to "expect [them] to be green at P1 and to stay green" — **that expectation is simply wrong, and was already wrong before this session started**: CS018 P10 bumped the version to `"1.0.0.18"` and deliberately left those five red as historical, phase-scoped claims (its own STATUS entry says so in as many words), and the baseline sweep at `6928ff3` confirms all five were already failing. P1 did not bump the version and did not touch them; P2's item (3) already plans to repoint them. **Docs: this file only.** The GDD, `GDD-VERSION-HISTORY.md` and `DIFFICULTY-LEVERS.md` are deliberately untouched per TRAP 2 — P2 owns all of them. Committed per `CLAUDE.md`'s standing commit-per-phase rule; did NOT push. **⛔ Next: the PLAYTEST GATE between P1 and P2** — see Playtest asks below and `IMPLEMENTATION-PHASES-CS019.md`'s gate section; P2 must not run until Paul has the three numbers.

## Working / verified

**Headless-verified in CS019 P1 (`scratchpad/test-cs019-p1.js`, new, 138 assertions, 0 failures):** nine sections, all driving the REAL `startGame`/`update(1/60)`/`draw`/`breakChain`/`scatterChain`/`applyPowerup`/`applyDebug`/`menuDebug`/`enterDebug`/`debugEntryKey`/`debugEntryCommit`/`saveSettings`/`loadSettings` plus the real `DebrisSatellite`/`HunterSatellite`/`Bullet` classes — no logic under test is reimplemented, and the chain-guard absorb path is EXECUTED through `update()`'s real collision passes rather than by poking state. Sections (B) and (F) additionally build the **pre-fix module from the pinned commit `6928ff3`** and run it through byte-identical staging, so both "this used to be broken" and "the guard-down path is unchanged" are checked against the actual previous build rather than against a restated expectation. Three idioms carried forward from earlier suites and worth reusing: the **object-identity tell counter** (CS018 P8) — `game.floaters`/`game.particles` are REASSIGNED by `update()`'s end-of-frame `.filter()` and a `FloatText` only lives 1.1 s, so a `.push` spy does not survive and an end-of-run array read undercounts; sample the arrays every frame into a `Set` instead; the **non-zero sentinel budget** (CS017 P6 §D) — time mode leaves `powerBudget.guard` at 0 in real play, so "still 0" would pass even against an unconditional decrement; and the **fixed-SHA cross-build control**, never `HEAD`. The suite is mutation-tested against four deliberate breakages of the shipped code, each of which fails it on behavioural assertions and not merely on source pins (see the session entry). Staging notes for anyone extending it: a large debris (radius 46, reach 53 px) parked on node 5 of a straight 10-node tow also covers nodes 3 and 7, so the scan's first overlapping pair is node **3** — still mid-chain, six nodes aft — and node 5 sits 120 px behind the ship, comfortably outside the ship's own `13 + 46 = 59` px contact test, so the hazards-vs-**ship** pass can never fire and confound the count.

## Playtest asks

**⛔ CS019 P1 — THE GATE BETWEEN P1 AND P2. NEW this session, and blocking: P2 must not run until these three have answers.** Everything about the fix is headless-solid (138 assertions, mutation-tested, cross-checked against the pre-fix build at `6928ff3`); all three questions below are feel calls in a real browser, all three are one-line edits in P2, and all three are live in the hidden dev panel under the **CHAIN GUARD** header right now. Write the answers into P2's prompt where marked, or say "no change" for each.

20a. **`chainGuardCooldown` — is 0.75 s the right "one contact, one charge" window?** At 0.75 s a large debris tumbling through the tow should cost exactly one charge and read as one event. Try **0.3** and **1.5** against a **homing small Hunter**, which is the pathological case — it loses contact, re-acquires, and comes straight back, so it is the one hazard that can legitimately bill twice on what *looks* like a single attack. If it still can at 0.75, the number is too low. Reference points from the spec's own contact-time table: a medium debris pass-through is ~0.5 s, a large debris ~1.2–1.5 s, a large Hunter core ~1.5 s. The knob steps in 0.05 and accepts typed values; the range is [0.1, 3].

20b. **`chainGuardIntercepts` — is 3 a powerup or a rounding error now?** (FLAG-CS019-b.) 3 was chosen when an intercept was, unknowingly, worth about one frame. It now buys three genuinely blocked hits. Against a full 24-node haul in a busy late level, is that enough to change how you play, or does it evaporate? The max is already 10 — find the number in the panel. If the answer exceeds 10, P2 raises `max` as well as `def`.

20c. **The tell — did the machine-gun accidentally carry the readability?** One absorb now produces one `GUARD_ABSORB_SPARKS` burst (7 guard-hued particles), one "GUARDED" floater and one `shieldPing()`, where before it produced roughly sixty of each per second. Legible on its own, or too quiet to notice mid-fight? `GUARD_ABSORB_SPARKS` is a one-line constant next to the chain-guard block and P2 can take a new value; the floater and the ping are one-per-absorb by design and should stay that way.

**Debug panel scrolling / headers / numeric entry (CS018 P2) — NEW this session. Every behaviour is headless-solid (359 assertions); everything here is look/feel in a real browser, on a hidden dev screen:**

19a. **Does the 14-row window feel like enough at once?** `DEBUG_ROWS_VISIBLE` is the single knob — raising it raises the derived panel height (14 → 640 px; 17 would be 736 px, i.e. past `VIEW_H`, so ~16 is the ceiling at the current 32 px row step). If it reads cramped, the other lever is `DEBUG_ROW_STEP` (32, down from the old 46) — tightening it to 30 buys another row.

19b. **Is the `▲ n / total ▼` indicator enough of a "there is more below" tell**, or does the panel still read as if it ends at the last visible row? A scrollbar rail would be the next step up, but it is chrome the vector look does not otherwise use.

19c. **Do the headers read as grouping rather than as disabled rows?** They draw as `— SHIP —` in `COLOR.dim` at size 15, left-aligned with the labels. They can never be selected, but a dim row next to a dim *unselected* value row is the thing to check.

19d. **Does typing a value feel right?** Land on a knob, type digits, ENTER. The pending value replaces the live one in `COLOR.garbageBonus` (hot yellow) with a trailing `_` caret. Specifically: is it obvious nothing has been committed until ENTER, and does ESC-to-cancel feel safe? (`-` toggles the sign, Backspace deletes, and the ◄► chevrons disappear while typing because arrows are inert then.)

19e. **The 660 px width** (up from 560) was sized for CS018's longer tier labels, which do not exist yet. Worth a glance at P6/P7 that e.g. "UFO direction change freq (normal)" still fits the label column before the value column at +420.

**Chain guard (CS017 P6) — NEW this session. The mechanics are headless-solid (356 new assertions drive the real intercept at both call sites, both expiry modes, the drop gate byte-compared against the pre-P6 build, the menu row and the HUD). What no headless test can answer is whether it FEELS right, and three of these are look-calls I made that are cheap to reverse:**

18a. **⚠ THE ONE MOST LIKELY TO NEED A CHANGE — the guarded chain drops its mass tint.** While the guard holds, every chain node renders in the guard's orange instead of `COLOR.garbageLight` (low-mass Hunter scrap) vs `COLOR.garbage`. I judged "this load cannot be cut" the more urgent read over a short window, but it does destroy real information for the duration. If you'd rather keep the mass tint, the fix is one ternary in `drawChain()` — the brighter/wider/bluer *links* alone would still carry the tell.

18b. **Does orange (`#ff9a3c`) read as a distinct powerup at a glance, or does it blur into Rapid's amber (`#ffd24a`)?** It is the only orange in the palette and no hazard uses it, but Rapid is the nearest neighbour and both are warm. The glyph (a shield over a link) is the backup tell. Pure look-call.

18c. **⚠ Does the 6-row HUD stack now crowd the low-health corner glow?** The bottom-left stack grew from five rows to six and its top row now sits at y=440, 200 px higher than the footprint the CS010 P3 corner glow was tuned against. This was flagged in planning (FLAG-CS017-c) and is genuinely not headless-testable — it needs eyes on a critical-hull moment while several powerups are up.

18d. **Is the absorb tell loud enough?** Seven guard-hued sparks + a "GUARDED" float + `AudioSys.shieldPing()`. I deliberately did NOT use `boom()`, because it carries the explosion sound and a blocked hit shouldn't sound like a destroyed canister — but that also makes the moment quieter than a real break. If an absorb goes unnoticed mid-fight, `GUARD_ABSORB_SPARKS` is the first knob.

18e. **Which expiry mode should be the default, and are 30 s / 3 intercepts the right numbers?** It ships on Time/30 s (matching every other powerup's default), with Intercepts/3 opt-in via Options → Difficulty. All three numbers are live Debug-panel knobs (`chainGuardTime`, `chainGuardIntercepts`, `chainGuardMinTow`) so you can retune in-session without a reload.

18f. **Is `chainGuardMinTow` = 5 the right gate?** Below 5 towed nodes the guard can't drop at all. Too high and you'll rarely see it; too low and you'll get one with nothing worth protecting. Set the knob to **0** to make it always-eligible while you're evaluating the powerup itself, then put it back.

18g. **Weight 1 (out of 11) — roughly a 9% share when eligible — is my pick, not a decision from the spec.** It puts the guard in the same rare tier as Magnet and Engine. If it feels like it never shows up when you actually have a big load at risk, that weight is the knob.

18h. **Dan says nothing about it — by design this phase.** P7 adds the intercept line once you've done the lab pass. Worth noting while you play: with the guard up, an absorbed break is currently the *only* chain event that is silent, whereas a real break still says `chain_broken`. If that asymmetry is jarring before P7 lands, that's useful signal for how urgent P7 is.

**Rare bonus canister (CS017 P5) — NEW this session. The mechanics are headless-solid (302 new assertions drive the real spawn, the real intake, the real coalesce/shatter paths and a recording canvas); every question below is a *feel* or *look* question a browser has to answer.**

17a. **⚠ THE ONE THAT MOST LIKELY NEEDS A CHANGE — can you actually reach it before it decays?** The canister inherits the ordinary `DEBUG.garbageLifetime` clock (**default 10 s**) and spawns 220–640 px away. Unencumbered that's an easy run; already towing a heavy chain (~54–70% top speed) it often will not be, and if you don't spot it in the first second or two you'll simply never see one. This was **not specified either way and was deliberately left at the shared default rather than invented around**. Play a few cycles and count how many you see versus how many you actually get. If the answer is "I keep watching them blink out," the fix is one line at the spawn site (seed `decay` from a new `BONUS_CANISTER_LIFETIME` knob) — but decide first whether the short fuse *is* the urgency that makes it exciting.

17b. **Does `COLOR.garbageBonus` (`#ffe23a`) read as "special salvage" at a glance, or does it just look like ordinary garbage?** It is deliberately kept in the salvage hue family (a hot yellow pushed off `COLOR.garbage`'s yellow-green `#c8ff50`) so it can never read as a hazard — but that same choice is what risks it not standing out. The halo ring (`drawRingArc` at `radius + BONUS_RING_PAD`) is the second channel carrying the tell; if the colour alone is too subtle, widen the ring or push the colour further toward gold **before** reaching for `COLOR.ach` (which means "at cap" everywhere else and shouldn't be diluted).

17c. **Is 6 pieces the right size?** Half a base tow cap in one scoop. Watch whether it reads as a windfall or as "my ship suddenly handles terribly." It is also the number that decides whether a chain-full player gets a satisfying partial or a frustrating one.

17d. **Does 250 feel like a reward?** It's ≈ two-and-a-bit canisters' worth of dock delivery. Deliberately modest, because the *cargo* is meant to be the reward. If it reads as an afterthought, raise `BONUS_CANISTER_SCORE`; the payout routes through `addScore` normally, so it also feeds the HP-repair milestone.

17e. **Is 50%-down-to-10% the right rarity?** ~2.7 per 9-wave cycle, front-loaded. The design intent is "a reward-flavoured reason to keep playing the low-stakes early waves" — so the question is whether the early waves now feel worth playing, and whether late-cycle waves feel correctly stingy or just barren.

17f. **Do you ever shoot one by mistake?** A clump is a bullet target, and shattering a bonus canister **forfeits the 250** by design (children inherit no marker — GDD §2.10.1). That rule only works if the tell teaches it. If you find yourself losing them to reflex fire, that's evidence 17b's tell isn't loud enough — not evidence the rule is wrong.

17g. **Dan says nothing about it.** Voice lines were deliberately out of P5's scope (they need a `tools/voice-robot-lab.html` pass). If a bonus canister deserves a line, that's a future changeset — note the wording you'd want while it's fresh.

**Sawtooth cycle escalation (CS017 P3) — NEW this session, and the round's first player-visible change. The maths is headless-solid (1,930 new assertions drive the real spawn/split/ctor paths and compare against the actual pre-P3 build), but every question below is a *feel* question that only a browser can answer. The instrument is already built: play several cycles, then dump the CSV from the hidden debug panel ("Dump difficulty log") and read `debrisCount` / `debrisSpeedMul` / `hunterSpeedFrac` against what you felt. All five answers are `CYCLE_GAIN` / `CYCLE_LENGTH` / `DEBRIS_COUNT_MAX` retunes, not code changes.**

16a. **Does the cycle reset read as RELIEF, or as the game going soft?** This is the whole bet of FORK-CS017-A. At wave 9→10 the field drops from 12 pieces to 4 and the speed multiplier from 1.64 to 1.12. If that lands as "I earned a breather" the model works; if it lands as "the game gave up," `CYCLE_GAIN` needs to be much higher (or `CYCLE_LENGTH` shorter, so the breather comes more often and matters less each time).

16b. **Is 12 → 13 → 15 → 16 → 18 across cycle tops enough escalation to feel like progress?** The tops are what a player experiences as "how hard did it get this time." A 12%/cycle gain is deliberately gentle.

16c. **Does the rounding quantization matter in play, or only on paper?** Cycle openings run 4, 4, 5, 5, 6, 6, 7, 7 — so cycle 1 opens with exactly the same 4 pieces as cycle 0. On paper that reads like the spiral failing; in play the opening wave may be too short to notice. If it does read badly, raise `CYCLE_GAIN`.

16d. **Does a Hunter carried across a cycle boundary undercut the reset?** It keeps its spawn cycle's speed and turn rate for life (accepted design, 16d is about whether it *feels* right). This is the one place where "reset = relief" can be actively contradicted: everything else slows down and the Hunter chasing you does not.

16e. **Are waves 7–9 now too full?** They are the only part of the ease-in window P3 changed: 10/11/12 large debris where the old build capped at 9. Waves 1–6 are byte-identical to the pre-P3 build, so if the early game feels worse the cause is here, and `DEBRIS_COUNT_MAX` is the dial.

**Title menu (CS016 P2) — NEW this session. The navigation is headless-solid (196 new assertions drive the real handlers, the real keydown listener and a real fake gamepad); everything here is look/feel:**

15a. **Do the four rows sit right against the art?** Rows land at y 342/380/418/456 between `O V E R H A U L` (baseline 300) and `BEWARE THE HUNTER SATELLITE` (baseline 480), with the control hint at 530. **The 24px gap between the last row and the flavour line is the tightest measurement on the screen** — a 24px row's descender reaches ~463 so it does clear, but if it reads cramped the knobs are `TITLE_MENU_STEP` (try 34–36), `TITLE_MENU_Y`, and, last resort, moving the flavour line down from its original `VIEW_H/2 + 120`.

15b. **Is chrome-less the right call (FORK-CS016-B → b)?** The rows draw straight onto the title art with no dim and no panel, so the `titleDebris` field drifts *behind* the text. Does the menu stay readable when a debris chunk passes under a row, or does it need a faint local backdrop? A per-row dim would be a small, contained addition — but it starts down the road FORK-CS016-B rejected.

15c. **Does dropping the keyboard legend actually cost a first-time player anything?** The two `ROTATE/THRUST` + `FIRE/SHIELD/PAUSE` lines are gone on the theory that Options → Controls now lists every binding authoritatively and is one cursor stop away. Worth watching someone who has never played find the controls.

15d. **Is `"↑↓ move    ENTER / A select    ESC / B options"` the right hint?** It differs from `ROOT_MENU_HINT` deliberately — at the title root, ESC/B *open Options* rather than backing out, and the hint says so. If it reads confusing next to every other screen's `ESC / B back`, the alternative is to drop the trailing clause and let the visible Options row speak for itself.

15e. **Does `Start Game` as the default cursor preserve the muscle memory?** Boot, press ENTER — it should start a run with no cursor movement, exactly as `PRESS ENTER TO START` used to. Same after `Quit to Title` from a gameover root.

15f. **ESC at the title opening Options (new, §7):** does that read naturally, or is it surprising that the "back" key opens something? It is the same key gamepad B has always used there, which is the argument for it.

**Wave-clear on debris-empty + Scoop mouth capturing powerups (CS015 P3) — NEW this session (the gate logic and pickup math are headless-solid; everything here is feel/balance):**

14a. **Does a Hunter (or a whole neglected garbage field) carrying across multiple waves read as rising pressure, or as clutter the player can't do anything about?** The wave now advances the moment Debris hits zero regardless of what's still alive — play a run where you deliberately ignore a Hunter and see whether it becomes a real threat over 2-3 waves or just an ignorable straggler.

14b. **Does the carried Hunter's frozen difficulty (flagged in STATUS/GDD §2.5 — its speed/turn ceiling is sampled once at spawn and never re-samples) ever read as obviously wrong** — e.g. a wave-3-speed Hunter still drifting around at wave 10? If it's noticeable, that's the trigger to revisit re-sampling on carry-over.

14c. **Does a leveled-up Scoop mouth catching powerups feel like a satisfying payoff for the investment**, or does it make powerups feel too passive/easy to collect once the mouth is wide (level 4-5)? No Magnet-style multiplier was added — the mouth is purely a wider *catch shape* — so this is really "is the shape alone worth it."

14d. **Does the field ever feel like it silts up faster now** (more Hunters + more loose garbage stacking across waves, since neither is force-cleared before the wave advances)? If so, the fix would be a balance lever (spawn rate, garbage decay), not a re-opening of the wave-clear gate.

**VoiceSys / Dan (CS010 P9) — NEW this session (all the routing/latch/cooldown logic is headless-solid; everything here is feel/mix and needs a real browser + audio):**

13a. **Does Dan cut through the mix?** With SFX + music playing, is the Voice Volume default (1.0) balanced against them, or does he get buried / dominate? The Voice slider (Options → Sound / Music → Voice Volume) is the per-player lever; the in-mix default is a look-call.

13b. **Is the 1.2 s `VOICE_COOLDOWN` right?** Play a busy stretch — do the lines feel like an occasional bark (good) or does Dan chatter over himself / go too quiet? And does the **priority** read right: a hull-critical line should clearly interrupt a lower one, and you should NEVER hear Dan narrate an event that finished seconds ago (superseded lines drop, by design).

13c. **Do the trigger anchors feel right in play?** Especially: hull-relief landing on the way back up (does it feel like relief, alongside P3's ring-exhale?); the dock tiers reading as a fair size-up of the haul; "Truck is full, let's go." landing exactly when the chain fills (which moves as `cargoMax` grows 12→24).

13d. **The unpause case (the whole reason this phase was careful):** drop to low HP, pause, wait, resume — Dan should **NOT** re-say "Hull integrity is critical." (the siren tone restarting is correct; the *voice* must not). Confirm in a browser.

13e. **Sign-off/character/captions** — see the four carried items under "Next up" (was the FLAG-11a sign-off real; radio-Dan vs clean synth; any uncommitted ✎ phoneme fixes to re-port; captions yes/no).

**High Scores table size (CS010 P5) — NEW this session:**

12a. **Is 1.8× the right scale on the browsable High Scores screen** (Options → High Scores)? The panel grew to 1000×560 to fit it with air — check it doesn't feel oversized/cramped in a real browser.

12b. **Now that the browsable table is bigger, does the gameover table (left at 1.0×, unchanged) read small by comparison?** If so, the fix is re-flowing the gameover screen (moving "PRESS ENTER TO PLAY AGAIN"/the O-hint) to afford a bigger table there too — a separate, un-scoped job, not built this phase.

## GDD version history

- **CS019 (P1–P2) — the chain-guard absorb-repeat fix: a one-bug changeset closing a hole in CS017's rule-changing powerup.** `IMPLEMENTATION-PHASES-CS019.md`, the whole changeset (this consolidated entry replaces the per-phase P1/P2 entries, following the CS012/CS013/CS015/CS016/CS017 precedent); spec in `PLANNED-FEATURES-CS019.md` (FORK-CS019-A → (a) per-hazard absorb cooldown, FORK-CS019-B → (a) one charge per absorbed break EVENT). `GAME_VERSION` `"1.0.0.18"` → **`"1.0.0.19"`**, bumped at P2 as planned — P1 deliberately left it alone. **P1 — the fix.** `breakChain(i)`'s guard branch returned without removing the absorbed node — correct behaviour, and also the bug: in the unguarded path the node is destroyed so an overlapping hazard stops overlapping and the contact self-terminates, but in the guarded path the node survived in place, still inside `h.radius + 7`, so the hazards-vs-chain scan re-fired the identical break every frame — a single large debris grazing the tow could burn the whole default 3-intercept count-mode budget in three frames (~0.05 s) before severing normally on the fourth. The fix, in one sentence: a hazard that has already paid for a contact may not present that same contact again while the guard holds. `DebrisSatellite` and `HunterSatellite` each gained `this.guardT = 0`, decremented toward 0 in their existing `update(dt)`; `breakChain(i)` became `breakChain(i, src = null)` (the `= null` default load-bearing — it keeps `test-cs017-p6.js`'s direct single-argument calls byte-identical), stamping `src.guardT = DEBUG.chainGuardCooldown` after the existing spend and tell. The hazards-vs-chain scan skips a stamped hazard **while, and only while,** `powerActive("guard")` is true — the skip lives in the scan, not inside `breakChain`, so a stamped hazard on an early node can never hide a different, unstamped hazard hitting a later node in the same frame, and a guard that expires or is spent elsewhere mid-contact lets a stale stamp sever normally on the very next frame rather than granting free passage. Bullet gained nothing — a hostile round is already dead and filtered before it can ever re-present, which is the whole asymmetry the fix rests on. New `DEBUG_VARS` knob `chainGuardCooldown` (0.75 s default, the registry's first knob with a sub-1.0 `step`; the panel's numeric-entry/persistence paths needed no changes to handle it). New `scratchpad/test-cs019-p1.js` (138 assertions) — mutation-tested against four deliberate breakages of the shipped code, plus a fixed-SHA cross-build control against the pre-fix commit `6928ff3` proving both "this used to be broken" (the pre-fix build genuinely burns its budget in three frames) and "the guard-down path is unchanged" against the actual previous build rather than a restated expectation. **P2 (round complete) — retune, version bump, doc sweep.** Playtest found all three tunables (`chainGuardCooldown`, `chainGuardIntercepts`, `GUARD_ABSORB_SPARKS`) correct at their P1 defaults — no retune applied, deliberately, per the standing "no change means change nothing" rule. `GAME_VERSION` bump grepped the whole repo rather than trusting the phase doc's own file list (the CS016 P5/CS017 P7 precedent that the grep always finds more live pins than planned): seven live scratchpad pins beyond the HTML itself, one more than the six the prompt named (`test-cs019-p1.js`'s own P1-authored pin). The five CS018-phase tests (`test-cs018-p1/p3/p4/p6/p7.js`) asserting `GAME_VERSION` "unchanged this phase (bumps in P10)" — already stale and failing since CS018 P10 itself landed, not caused by CS019 — were repointed to their mirror image (the version has moved past what each of those phases shipped), and `test-cs019-p1.js`'s own "unchanged this phase (bumps in P2)" pin got the same treatment now that P2 is the phase in question. GDD §2.14.2 documents the absorb cooldown (source-hazard stamp, scan-side skip gated on the guard being up, the count-mode unit reconfirmed as one absorbed break EVENT — a claim §2.14.2 already made and CS019 is what makes it actually true — and guard-down behaviour unchanged); §2.19 corrected to **33** value entries — the phase doc's own P2 anchor table carried a stale "15 → 16" left over from a much older planning pass, caught and corrected against the live registry rather than trusted. `DIFFICULTY-LEVERS.md` checked and left untouched: the chain guard is a defensive powerup, not a difficulty lever, and does not appear in it. CS018's planning docs were already archived (commit `a7a817a`, CS019's own P1 prerequisite) — verified via `ls archive/`, not re-moved. **Full regression: 83 test files, 31,259 assertions, 0 failures.** Docs: this entry, GDD §2.14.2/§2.19/Current-build line, `STATUS.md`. Did NOT push.
