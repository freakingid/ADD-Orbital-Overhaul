# CS032 — Save Game / Load Saved Game / 3 Slots

Full P1–P7 build log, moved out of `STATUS.md` at the CS032 closing phase per the standing
per-changeset log convention (`CLAUDE.md`). Baseline `faa8d35`, `GAME_VERSION` `1.0.0.31` →
`1.0.0.32`. Companion docs: `archive/PLANNED-FEATURES-CS032.md` is not archived (still at repo
root as `PLANNED-FEATURES-CS032.md`/`IMPLEMENTATION-PHASES-CS032.md`, per the standing "in-flight
docs stay at root, only fully-spent ones move to `archive/`" pattern — CS032's own docs move at the
next changeset's close, mirroring how CS031's moved this session).

## P1 — `SaveSlots` store + `buildSaveEntry()`

`SaveSlots` store (`afd_saves_v1`, per-profile via `Profiles.keyFor()`, lazy, guarded
read/write/clear/count with a boolean `write()`) + pure `buildSaveEntry()` reading live `game`
state. No restore logic, no menu wiring, no `game.resumedRun` — P2/P3/P4's territory.

## P2 — `resumeFromSave()` + the sticky `game.resumedRun`

`startGame()`'s whole body was **extracted verbatim** into a new shared `resetRun(wave, debugRun)`;
`startGame()` is now `resetRun(startLevel-1, startLevel>1)` + `nextWave()`, and `resumeFromSave()`
is its sibling — same reset, then the save-moment overwrite, then `resumedRun = true`, then
`nextWave()`. The two seeds became parameters because the reset reads `game.wave` (`saucerTimer`'s
`ufoAppearInterval()` seed) before it returns. `resumedRun` is declared in the `game` literal and
cleared in `resetRun()` (both-places rule), set true at exactly one site, cleared at exactly one;
it ORs into `Achievements.save()` and the initials-entry arm, and draws `"RESUMED RUN"` as an
`else if` under `"DEBUG RUN"`. Restore is known-value-else-default (type-matched, over the fresh
defaults) and deep-copies `stats`/`powerUsed` so a resumed run can never write back through into
the slot it came from — the mirror of `buildSaveEntry()`'s own copy rule. No menu wiring;
`resumeFromSave()` has no caller until P3.

## P3 — the `"slots"` screen

One screen, `game.menu.slotMode` = `"save"` | `"load"`, following `drawProfiles()`'s panel/row-step/
prefix/footer idiom exactly. Row state is `empty` / `occupied` / `unreadable` (FORK-H: an
unrecognised `kind`, or malformed data, reads as unreadable — never guessed at). Save on empty
writes straight through (`buildSaveEntry()` + `SaveSlots.write()`); save on occupied/unreadable
raises an `openModal()` overwrite confirm naming what's lost; a failed `write()` sets
`game.menu.slotMsg` and the screen stays open (spec §4.4 — never reports success on failure). Load
confirm on an occupied row calls `resumeFromSave()`, whose own `resetRun()` already clears the menu
and lands `game.state = "playing"` — no extra teardown needed here. `slotMode` has no separate
return-screen field: Back derives its destination FROM the mode itself (`"save"` → the paused root,
`"load"` → the title), since those are the only two callers the spec allows (§0.2/§4.3) — a stored
back/backIndex pair (nameCtx's own shape) would just duplicate what the mode already implies.
`slotMode`/`slotMsg` follow the CS016 P3 both-places rule (game literal + `resetRun()`);
`gotoScreen()` also unconditionally clears `slotMsg` on every screen change, the same treatment
`achTab` already gets. Reachable only from the test harness this phase — `MENU_ROOT_PLAY`,
`MENU_TITLE` and both menus' dispatch are untouched; P4 wires the two real callers.

## P4 — menu wiring

`"Save"`'s three-piece unavailable-row idiom unpicked in one commit: `menuRoot()` gained a
`"Save"` branch (`slotMode = "save"` → `gotoScreen("slots")`) and `drawRootMenu()`'s
`it === "Save" ? COLOR.dim` ternary was **deleted** — `MENU_ROOT_PLAY` itself is byte-identical,
label and position both. `MENU_TITLE` gained `"Load Saved Game"` at index 1, beside `"Start Game"`;
N went 5 → 6 with **no** `TITLE_MENU_*` edit (Y unmoved at 324, step self-healed 33 → 26.4, pinned
byte-identical against P3's SHA). The row is the idiom's new consumer: present, focusable, dim and
inert at `SaveSlots.count() === 0`, asked fresh in `drawTitleMenu()` (draw time) and `menuTitle()`
(confirm time), never cached. All four `unavailable-row idiom` citations repointed at
`menuProfiles()`'s `Add Profile` cap, now the canonical example — including `menuDifficulty()`'s,
which the prompt did not list. Six suite files repointed; `test-cs032-p4.js` mutation-tested eight
ways.

## P5 — the purge

`removeProfileStores(id)` gained a third `ls.removeItem(SAVES_KEY + ":" + id)` beside the two
existing removes. `blankLegacyStores()` clears `p0`'s three slots through `SaveSlots.clear(i)` —
the guarded write path, never a raw `removeItem`, matching how its two sibling stores are
reset-in-place. `Profiles.activate()` needed no change: `SaveSlots` is lazy and reads through
`keyFor()` at call time, confirmed by test rather than inspection (write under profile A, switch to
B, read empty, switch back, read A's write intact — no flush/reload). `quitToTitle()` is confirmed
untouched: no `SaveSlots`/`buildSaveEntry` reference in its body, and a spied `SaveSlots.write`
counts zero calls across a mid-run quit. `test-cs032-p5.js`: (A) non-legacy delete removes just
that id's saves key, leaving siblings' intact; (B) `p0` delete blanks `p0`'s slots in both the
p0-active and p0-not-active shapes; (C) deleting the ACTIVE profile hands `activeId` to the
roster's next survivor (per `profileDelete()`'s own unreordered ordering) and purges only the
deleted id's slots — the newly-active profile's own slot, written before the delete, reads back
unchanged; (D) per-profile isolation across `activate()`; (E) `quitToTitle()` writes nothing. Full
suite: **127 files, 127 passed, 0 failed, 0 skipped.**

## P6 — the playtest gate

Blocking, no code — Paul played the built file from `file://` and answered all eleven questions
(`IMPLEMENTATION-PHASES-CS032.md`). **Result: clean across the board.** G1 (save at wave 12, quit
to title, close the browser, reopen, load — score/HP/wave all came back exactly as saved) and G2
(save/reload-without-playing, then play into wave 13, save again, reload — both loads landed
correctly) both **passed**. G3 (a debug-knob run above level 1, saved and loaded, still shows
`DEBUG RUN` and still suppresses initials entry) **passed**. **G4 — the gate's real question — also
passed**: a mid-level save, loaded, played to a genuinely high-score game over, showed
`"RESUMED RUN"` throughout and correctly omitted initials entry — the direct, real-browser
confirmation that `resumedRun`'s eligibility wiring (FORK-A's save-moment values, made safe by
FORK-F's sticky flag) has no gap. G5–G11 all came back **keep** / **unchanged** / **reads
correctly** / **3** — the tag wording, the slot-row copy (`LEVEL 12 · 48,300 · 3 days ago`), the
six-row title-menu step (≈26.4px, still reads cleanly against the title art), the overwrite-confirm
copy, the dim-and-inert `"Load Saved Game"` row on a fresh profile, Save's position in the pause
root (2nd, after Continue), and three slots as the right count.

## P7 — closing phase

**Because P6 came back clean on every number, this phase applied zero code changes for gate
answers** — the second CS0## closing phase in a row to reach P7 with nothing to retune (CS031 was
the first).

Documentation: a new GDD §2.22 documents the save/load mechanism — save-moment capture and its copy
semantics, the shared `resetRun()`/`resumeFromSave()` reset path and its two ordering invariants,
the `resumedRun` flag and why it's what makes save-moment values safe to ship, the slots screen's
three row states, the menu-wiring unpick, and the purge integration — not just the mechanics of
slots. §2.16 grew the sixth `MENU_TITLE` row (`"Load Saved Game"`) and its derived-layout numbers
at N=6, and its "unavailable row" idiom passage was corrected: the `"Save"` row it used to cite as
the disabled example is now live, so the passage was rewritten to say so and repoint the canonical
example at `menuProfiles()`'s `"Add Profile"` cap — the same repoint P4's own code comments already
made. Three other GDD spots that named `startGame()` as the site of the shared reset list (the
level-banner clear, the world-resize contract, `game.worldSize`'s both-places note) were corrected
to name `resetRun()` instead, and §3's Flow function list gained `resetRun`/`resumeFromSave`.
`CLAUDE.md`'s Save data section gained the fifth `localStorage` key, `afd_saves_v1`, with its
per-profile routing and its one point of difference from its four siblings — laziness. The free
`tools/orbit-lab.html` sweep the spec flagged (§0.4) turned out to be a no-op: the entry was already
absent from `CLAUDE.md`'s Design instruments list, contrary to the spec's claim that CS031's sweep
missed it — verified by grep before touching anything, nothing to delete.

`GAME_VERSION` bumped `"1.0.0.31"` → `"1.0.0.32"`. The seven live version pins that track HEAD
across changesets (`test-cs010-p0.js`, `test-cs013-p4.js`, `test-cs015-p7.js`, `test-cs016-p5.js`,
`test-cs017-p7.js`, `test-cs018-p10.js`, `test-cs021-p4.js`) were re-pointed to the new literal;
phase-local pins are untouched (per `CLAUDE.md`'s pin rule — none of CS032's own phase tests carry
a version-literal pin, so there was nothing to flip to its mirror image this time). Full suite on a
full clone: **127 files, 127 passed, 0 failed, 0 skipped, 0 timed out**. Registry holds at **87**,
`LEVERS` at **18** — this changeset added no new knob or lever, only a store, a reset extraction,
and a screen. Confirmed untouched: `MENU_ROOT_OVER` still carries no `"Save"` row, and no comment
anywhere still describes `"Save"` as a placeholder (every `unavailable-row idiom` citation resolved
to `menuProfiles()`'s `"Add Profile"` cap, checked by grep).

## Carried-forward known issues (not resolved this changeset)

- **⛔ FLAG-CS032-a — `drawTitleMenu()` calls `SaveSlots.count()` every frame**, a
  `localStorage.getItem` + `JSON.parse` per title-screen frame at 60fps. Deliberate per spec §4.3 (a
  profile switch or delete changes the answer, so it can't be cached) and `drawSlots()` already reads
  per frame the same way — the build's first **unconditional** per-frame storage read, since it runs
  on the title screen whether or not the player ever opens a menu. If it ever measures, the fix is a
  cache invalidated at the three sites that can change the answer (profile activate, profile delete,
  slot write), not a moved question.
- **Back from the slots screen in LOAD mode lands the title cursor on `"Options"`, not on
  `"Load Saved Game"`.** `menuSlots()`'s back calls `returnToTitleMenu()`, which hardcodes
  `MENU_TITLE.indexOf("Options")` — correct for its other callers (menuOptions' Back, profile
  SWITCH), slightly off here: the player backs out of Load and the cursor has jumped four rows.
  Shipped in P3, player-reachable since P4. Not fixed — changing it is a `returnToTitleMenu()`
  signature question, which is design, not wiring. Save mode is unaffected (it restores the cursor
  to `"Save"` correctly).
- **FLAG-CS031-c** — `test-f2.js` flakes ~3% of runs (CS030's celebration-panel `game.celebration`
  leaking across sections in `resetShip()`); pre-existing, not fixed here, one-line fix identified
  (`game.celebration = null;` in `resetShip()`). 29 suite files reach a death/gameover and never
  mention `game.celebration` — the class is latent beyond `test-f2`.
- **`test-registry.js`'s `FLAG-CS027-d`** — twelve suite files grep a comment-stripped copy of the
  source missing the same 80 lines `execSource()` fixed. Latent, not live.
- **Piece-distinctness concern (CS028)** — Hubble's pieces 1/2 and Skylab's 0/2 share a polyline
  vertex-count signature. Paul's gate call: leave as is.
- **Ten suite files still hard-fail, not skip, on a shallow clone (CS026).**
- **Satellite-vs-satellite elastic bounce and mutual collision damage never playtested (CS023).**
- **The milestone floaters touch the dock anchor at 0.0px clearance at the picked gate value
  (CS029)** — zero crossing, no air; Paul picked 0.50 anyway.

## GDD version history

CS032 (P1–P7) — Save Game / Load Saved Game / 3 Slots. **P1** built the `SaveSlots` store
(`afd_saves_v1`, per-profile, lazy) and the pure `buildSaveEntry()` capture, with no restore path
and no menu wiring yet. **P2** built `resumeFromSave()`: `startGame()`'s old body was extracted
verbatim into a shared `resetRun(wave, debugRun)` so a resumed run and a fresh run can never drift
apart, and the new sticky `game.resumedRun` flag was wired into the same two persistence choke
points `debugRun` already gated (`Achievements.save()`, the initials-entry arm) plus its own HUD
tell — the mechanism that makes the chosen save-moment capture (exact score/progress, not a
wave-start snapshot) safe to ship without reopening a save-scum score loop. **P3** built the
`"slots"` screen — one screen, two modes, three row states (empty/occupied/unreadable), reachable
only from tests. **P4** made `"Save"` live in the pause root (unpicking CS016 P4's three-piece
placeholder in one commit) and added `"Load Saved Game"` as `MENU_TITLE`'s sixth row, both using
the shared unavailable-row idiom. **P5** wired the purge into both profile-delete paths and
confirmed `quitToTitle()` never autosaves. **P6** was a blocking playtest gate — all eleven
questions came back clean, including the one that mattered most: a real mid-run save, load, and
play-to-game-over cycle correctly tagged `"RESUMED RUN"` throughout and correctly withheld
initials entry, confirming the eligibility mechanism has no gap outside the test harness. **P7**
closed: applied zero code changes (nothing needed retuning), documented the feature in new GDD
§2.22 and the grown §2.16/§3, repointed three GDD passages and `menuRoot()`-adjacent comments off
`startGame()` and onto `resetRun()`, bumped `GAME_VERSION` to `1.0.0.32`, and added `afd_saves_v1`
as the build's fifth `localStorage` key — lazy, unlike its four siblings. Registry holds at 87,
levers at 18 — CS032 added a store, a reset extraction and a screen, no new debug knob or lever.
