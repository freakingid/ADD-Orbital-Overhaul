# CS011

**Prior session (CS011 P5, the final phase of the changeset):** implemented CS011 Phase 5 per the phase prompt (`IMPLEMENTATION-PHASES-CS011.md` P5). Re-grepped every anchor first: `VOICE_LINES` (actual L1498), `VOICE_PRIORITY` (actual L1630, drifted from the prompt's ~L1550), `breakChain` (actual L4456, drifted from ~L4270) and its two call sites (a hostile-bullet hit ~L4919, a Hunter collision ~L4975), and `scatterChain` (actual L4467, drifted from ~L4281) — the ship-death path, confirmed it must stay silent. **P0's lab-audited `chain_broken` block was still sitting, uncommitted, in `tools/voice-robot-lab.html`** (same PASTE-READY comment P4 drew its level/number data from), already verified against `parsePhonTokens` with zero errs. **What landed: (1)** `VOICE_LINES.chain_broken` — the five `{text,phon}` pairs, ported **verbatim**, text unchanged: "Junk is gone!" / "Lost my scrap!" / "Trash is loose!" / "There goes the garbage!" / "There goes my junk!". **(2)** `VOICE_PRIORITY.chain_broken = 2` (joins `health_relief`/`health_full`/`level` at that tier). **(3)** `breakChain(i)` gained exactly one line — `VoiceSys.say("chain_broken");` — appended after `chain.length = i; game.deliveryCount = 0;`, the single choke point both call sites already route through, so a hit that severs several nodes in one frame still speaks once (collapsed by the standard cooldown/priority gate, same as every other event). `scatterChain()` was **not** touched — ship death stays silent by design (FLAG-E). **(4) Version:** `GAME_VERSION` bumped `"1.0.0.10"` → **`"1.0.0.11"`**, following the established `Major.Minor.Patch.Changeset` scheme (CS010 P0) unprompted by any new-mechanic/tweak/bugfix distinction — CS011 hadn't bumped the changeset digit in P1–P3 (that phase's own note left the exact string "Paul's call, set it whichever phase"), so P5 closes it out as CS011's last phase, the same "last player-visible change before the round ends" placement CS010 P0 used. **New test `scratchpad/test-cs011-p5.js`** (34 assertions, drives the REAL extracted script; used the full Web-Audio-Proxy-mock from `test-cs010-p9.js`'s idiom rather than P4's bare fake ctx, because `breakChain`'s `boom()` → `AudioSys.explosion()` needs a real node graph — `createBuffer`/`createPeriodicWave` etc. — even on a captions-only, `voiceStyle:"off"` run): the 5 lines all present with non-empty `text`/`phon`, every `phon` parsing with zero `errs` through the real `parsePhonTokens`; `VOICE_PRIORITY.chain_broken === 2`; `breakChain(0)` on a built 3-node `game.chain` sets `game.caption.text` to one of the 5 lines and truncates the chain to `i`; a second `breakChain` fired 0.5 s later (well inside the 1.2 s `VOICE_COOLDOWN`) leaves the caption untouched (dedup — equal priority drops, never queues) while the chain still truncates; with `VoiceSys.curPriority` forced to 3 (simulating a busy `health_low`) a `chain_broken` `_emit` drops (2 ≤ 3) though the chain still breaks; `AudioSys.ctx` null → `startGame()`/`update(1/60)` don't crash. **Two pre-existing regressions surfaced and fixed** (both legitimate consequences of this phase's changes, not bugs): `test-cs010-p0.js` hardcoded `GAME_VERSION === "1.0.0.10"` (3 spots incl. message text) — updated to `"1.0.0.11"`. `test-cs010-p9.js`'s (E7) asserted `breakChain` "fires no line" — rewritten (spied `say()` log) to assert it now fires exactly `["chain_broken"]`, while `scatterChain` (checked separately, log cleared between the two calls) still fires nothing. **Full regression re-run green: all 44 test files, 3,718 assertions.** `node --check` clean. **Doc pass (final CS011 doc pass, this phase, per the phase's own note):** GDD §2.8 (chain_broken folded into the Triggers & latches bullet; "Lines are DATA" bullet's count corrected 24→29 lines across 8→9 `VOICE_LINES`-indexed classes, level kept as the separate composed 10th; the cooldown/priority bullet now spells out the full ladder: `health_low` 3, `health_relief`/`health_full`/`level`/`chain_broken` 2, everything else 1), §2.18's stale `GAME_VERSION ("3.6"...)` parenthetical corrected to the real scheme, top-of-file Current-build line → CS011 P5 / `"1.0.0.11"`, Architecture Map VoiceSys row (added `chain_broken` + the `tools/voice-robot-lab.html` phon-port credit for `NUM_PHON`/`LEVEL_PHON`/`chain_broken`). CLAUDE.md — added a **dedicated `tools/voice-robot-lab.html` design-instruments bullet** (it previously existed only as inline mentions inside the VoiceSys non-negotiable and the code map, despite being the P1/P4/P5 port source — now a proper sibling entry to the existing `voice-lab.html` bullet). `GDD-VERSION-HISTORY.md` — appended both the CS011 P4 entry (deferred here per that phase's own note) and this P5 entry. **This closes CS011** — player-selectable voice styles + ring-mod (P1), captions (P2), the Options picker + persistence (P3), level announcements (P4), and the chain_broken frustration line (P5) are all shipped. **No further CS011 work pending.** Did NOT push (Paul's call, every phase).

**Prior session (CS011 P4):** implemented CS011 Phase 4 per the phase prompt (`IMPLEMENTATION-PHASES-CS011.md` P4). Re-grepped every anchor first — three drifted from the prompt's estimate (`VOICE_LINES` ~L1458→actual L1498, `VOICE_PRIORITY` ~L1550→actual L1590, `nextWave`/`game.wave++` ~L3387→actual L3526/3527); `VoiceSys`'s `_emit` (from P2) confirmed at its documented shape. **P0's lab output was already sitting in `tools/voice-robot-lab.html` as an uncommitted PASTE-READY comment block** (Paul's audition step, per that phase's own "do not commit" instruction) — `LEVEL_PHON`, the 28-entry `NUM_PHON` (including `zero`, FLAG-I), and the five `chain_broken` lines (deferred to P5) were all present and had already been verified against the lab's `parsePhonTokens` with zero errs; ported the level/number data **verbatim**, composed nothing new. **What landed:** **(1)** `LEVEL_PHON`/`NUM_PHON`/`DIGIT_WORD` placed right after `VOICE_LINES` (`DIGIT_WORD` is new, not in the lab dump — a trivial `"0".."9"` → word map needed by the ≥100 fallback, not phon data itself, so nothing to audition). **(2)** `numberToWords(n)` (pure): `n<20` → one ones/teens word; else tens word + ones word only if `n%10`. **(3)** `levelPhon(n)` (pure): `n<=99` → `LEVEL_PHON + " / " + numberToWords(n).map(NUM_PHON lookup).join(" / ")`; `n>=100` → digit-by-digit via `DIGIT_WORD`+`NUM_PHON`, same `" / "` cosmetic-gap convention (parsePhonTokens treats `/` as a word gap, not a pause). **(4)** `VoiceSys.sayLevel(n)` added right after `say(event)`: `return this._emit({text:"Level "+n, phon:levelPhon(n)}, VOICE_PRIORITY.level)` — `this._emit` is correct here (unlike P2's `voiceEnabled()` deviation, `_emit` genuinely is a `VoiceSys` method). **(5)** `VOICE_PRIORITY.level = 2`. **(6)** `nextWave()`: `VoiceSys.sayLevel(game.wave)` right after `game.wave++` — since `startGame()` calls `nextWave()` for wave 1 immediately after `VoiceSys.reset()` clears the gate, **Level 1 is announced on every fresh run**, not just on wave transitions. **REAL REGRESSION SURFACED AND FIXED, not just the new test:** forcing a voice line on *every* `startGame()` (not just gameplay-triggered events, which is all any prior phase did) meant `_emit`→`ensure()`/`_schedule()` now runs the FULL Web-Audio graph build on every headless `startGame()` call — and **11 pre-existing test files** had Web-Audio mocks built before VoiceSys's ring/radio/formant graph existed at that call site, so they were missing methods the graph now touches unconditionally (`Q.setValueAtTime`, `cancelScheduledValues`, `createWaveShaper`, `createDynamicsCompressor`, `createPeriodicWave`, `disconnect`, `setPeriodicWave`) and crashed. Fixed by completing each file's mock to the full node/ctx shape (mirroring the "same shape as test-cs010-p9.js" pattern already used by `test-cs011-p1/p2/p3` and the CS010 P9 precedent of updating older mocks when a new call path starts exercising them): `test-cs009-hud/p2/p4.js`, `test-cs010-p4.js`, `test-cs010-p5.js`, `test-f7.js`, `test-p4.js`, `test-v36-death.js`, `test-v36-scores.js` (Proxy-catch-all node shape — gave `gain`/`frequency`/`Q` the full AudioParam method set, added `threshold`/`ratio`/`attack`/`release`/`curve`/`disconnect`/`setPeriodicWave`); `test-cs010-p7.js` and `test-v34-p6.js` (concrete recording ctx, used for real node-creation counting — added `createWaveShaper`/`createDynamicsCompressor`/`createPeriodicWave` factories + `disconnect`/`setPeriodicWave` on the existing node factories, without touching their instrumentation). **No test's actual assertions changed** — only their audio mocks grew the methods VoiceSys already needed; this is expected fallout of a trigger this project-wide (every `startGame()`), not scope creep. New test `scratchpad/test-cs011-p4.js` (23 assertions, drives the REAL extracted script): `numberToWords` spot checks (1,7,13,20,23,40,99 incl. the 23→twenty·three/40→forty cases the prompt named); `levelPhon(n)` for n=1,7,23,40,99,100,123 all parse through the REAL `parsePhonTokens` with zero `errs`; `VOICE_PRIORITY.level===2`; a captions-only `startGame()` (bare fake ctx, `AudioSys.voice` stays null so `ensure()` no-ops, `voiceStyle:"off"`) asserting `game.caption.text==="Level 1"`; and `AudioSys.ctx===null` → `startGame()`/`update(1/60)` don't crash. **Full 43-file regression green** (42 prior + this session's new file, all 11 touched mocks re-passing), `node --check` clean. Docs: GDD §2.8 (new "Level announcement" bullet after Captions; the "Lines are DATA" bullet's 24-lines/8-classes count gained a note that the level path is a 9th, *composed* class, not a `VOICE_LINES` pick; Architecture Map VoiceSys row gained `sayLevel(n)`/the pure helpers/the ported data), CLAUDE.md (code map gained `LEVEL_PHON`/`NUM_PHON`/`DIGIT_WORD`/`numberToWords`/`levelPhon`/`sayLevel`), this file. **`GDD-VERSION-HISTORY.md` NOT touched this phase — the phase doc says P5 folds both P4 and P5 into one CS011 entry.** **Still pending (CS011 P5): the `chain_broken` frustration event** (its five lines are already sitting in the same `tools/voice-robot-lab.html` PASTE-READY block, auditioned alongside the level/number data). Did NOT push (Paul's call, every phase).

**Prior session (CS011 P3):** implemented CS011 Phase 3 per the phase prompt (`IMPLEMENTATION-PHASES-CS011.md` P3). Re-grepped every anchor first per the prompt's own instruction (they drift between sessions) — all six landed close to where the prompt said (`SOUND_ROWS` ~L2195, `VOL_LABELS`/`VOL_CATS` ~L2196-2197, `MUSIC_TRACK_LABELS` ~L2220, `menuSound` ~L2322 (Music-Track cycler branch ~L2333), `drawSound` ~L5050 (`menuPanel(600,510…)`, else-branch = Music Track, footer at `y+490`), `saveSettings` ~L2470, `loadSettings` ~L2484) — a mechanical phase, no anchor surprises. **Mirrors two existing idioms exactly, as scoped: the Music-Track cycler (`menuSound`) and the known-value-else-default persistence pattern (`loadSettings`) already used by `musicTrack`/`shipTurnScale`.** **(1) `SOUND_ROWS`** gained `"Voice"`/`"Captions"` between "Voice Volume" and "Music Track" (additive — `VOL_LABELS`/`VOL_CATS` untouched, so the two new rows stay outside the slider machinery). **(2) `VOICE_STYLE_LABELS`** (near `MUSIC_TRACK_LABELS`): `{off:"Off", comms:"Comms", comms_f:"Comms ♀", flat:"Monotone", flat_f:"Monotone ♀", vintage:"Computer", vintage_f:"Computer ♀"}`. **(3) `menuSound`'s left/right block** gained two branches: `"Voice"` cycles `settings.voiceStyle` through `VOICE_STYLE_VALUES` (wraps both directions) and calls `VoiceSys.setStyle()` so the style change is audible immediately, then `saveSettings()`; `"Captions"` flips `settings.captions` (either direction), then `saveSettings()` — both `AudioSys.ui(false)` on confirm, same as every other row. **(4) `drawSound`**: panel height `510→602` (+92px for the two new rows), footer `y+490→y+582` (same 20px-from-bottom-edge convention as before); the single Music-Track `else` branch became a three-way value-column dispatch (`Music Track`→`MUSIC_TRACK_LABELS`, `Voice`→`VOICE_STYLE_LABELS[settings.voiceStyle]`, `Captions`→On/Off text) — all three still plain `drawText`, no new fill/stroke idiom. **(5)/(6) `saveSettings`/`loadSettings`**: `voiceStyle`/`captions` added to the saved blob (additive, no schema bump); on load, `voiceStyle` only overwrites the runtime default if it's a real `VOICE_STYLE_VALUES` id (else stays `"comms"`), and — **FLAG-B, built exactly as specified** — `captions` only overwrites if `typeof data.captions === "boolean"` (a stray non-boolean like `"yes"`, or an absent key from an older save, leaves the `true` default untouched; **no `else` branch that could snap it to `false`**). `loadSettings` also calls `VoiceSys.setStyle(settings.voiceStyle)` after the restore so a loaded style is actually applied to the live acoustic engine at startup, not just stored inertly. New test `scratchpad/test-cs011-p3.js` (**39 assertions**, drives the REAL extracted script, follows the `test-cs011-p2.js`/`test-v34-p6.js` idioms — Web Audio Proxy mock + a real in-memory `localStorage` stub, no reimplementation): `SOUND_ROWS` shape/order (additive, Back stays last); the Voice cycler advances one step AND wraps both directions (`vintage_f↔off`), spying `VoiceSys.setStyle` to confirm it's called with the new id each time; the Captions toggle flips in either direction; a full save→fresh-instance-startup-load→explicit-`loadSettings()` round trip for both fields; the FLAG-B tolerance matrix (unknown `voiceStyle`→stays `"comms"`, `captions` omitted→stays `true`, `captions:"yes"`→stays `true` not coerced, `captions:false`→**is** accepted, not permanently locked true); `drawSound()` renders all three new-row selection states without throwing; headless `startGame()/update(1/60)` no-crash. **Fixed one pre-existing stale snapshot while in the area:** `test-cs010-p4.js`'s `expectedSound` array still listed the pre-P9 5-row `SOUND_ROWS` (missing "Voice Volume", which P9 added without updating this assertion) — updated to the current 8-row shape. **Full 42-file regression green** (41 prior + this session's new file), `node --check` clean. Docs: GDD §2.16 (new Voice & Captions bullet after the Music Track bullet; the Persistence bullet gained the two additive fields + the FLAG-B tolerance rule; the CS010-P4 menu-restructure bullet's inline `SOUND_ROWS` quote was also stale — pre-dated P9's Voice Volume row — fixed to the current 8-element array while touching that exact line), CLAUDE.md (frozen-keys bullet now notes the two CS011 P3 additive fields on `afd_settings_v1`), `GDD-VERSION-HISTORY.md`, this file. **`GAME_VERSION` left at `"1.0.0.10"`** — matching CS011 P1/P2 precedent (neither bumped it either); no instruction to bump this phase. **This closes out CS011 P3 — the Options voice-style picker + Captions toggle are now both selectable and persist.** **Still pending (later CS011 phases): the level/chain caption lines (P4/P5).** Did NOT push (Paul's call, every phase).

**Prior session (CS011 P2):** implemented CS011 Phase 2 per the phase prompt (`IMPLEMENTATION-PHASES-CS011.md` P2). Re-grepped every anchor first (they drift — and one did: see the DEVIATION below). **The tricky part** (flagged `ultrathink`): extracting the scheduler out of `say()` so the cooldown/priority gate arithmetic stays byte-identical and ONLY the pure `buildUtterance` line moves. **What landed:** **(1) `_render` → `_schedule(utt)`** — the Web-Audio scheduler renamed, its ONLY change the removal of the `const utt = buildUtterance(…)` line (utt is now the argument); `ensure()`/the `!AudioSys.ctx` guard/`ctx.resume()`/`_stop()`/the entire node graph + scheduling untouched (verbatim PORT-ME BLOCK A). **(2) `_emit(line, p)`** — the new voice CHANNEL: `!AudioSys.ctx → null`, `ensure()`, `now = AudioSys.now()`, the EXACT gate lifted from the old `say()` (equal-or-lower drops, in-cooldown drops, higher pre-empts), then `utt = buildUtterance(line.phon, VOICE_PARAMS)` (moved up, pure), `if (settings.captions) showCaption(line.text, utt.dur)`, `if (voiceEnabled()) _schedule(utt)`, `curPriority = p; busyUntil = now + 0.10 + utt.dur`. **Gate arithmetic byte-identical** — past the ctx guard `buildUtterance` always returns a real object, so the old `(utt ? utt.dur : 0)` collapses to `utt.dur`; `busyUntil`/`curPriority` advance whenever a line PASSES, even captions-only, so a caption honours the same drop-not-queue/pre-empt as the audio. **(3) `say(event)`** = lines-empty guard → random pick → `_emit(line, VOICE_PRIORITY[event]||1)`. **(4) Captions:** `game.caption {text,life,dur}` on the `game` literal; re-init in `startGame` ABOVE the `VoiceSys.reset()/nextWave()` pair (FLAG-D ordering — seeded before any future `sayLevel(1)` inside `nextWave`); cleared (`life=0`) in `quitToTitle`. `showCaption(text,dur)` overwrites `game.caption` wholesale (supersede; `life = 0.10 + dur + CAPTION_LINGER`); aged `-= dt` in `update()`'s playing body beside the floaters; `drawCaption()` a screen-space `drawText` at `VIEW_W/2, VIEW_H − CAPTION_Y`, gated `game.state==="playing" && !game.paused && game.caption.life>0` (FLAG-C — `caption.life` freezes while not-playing, so a `!paused`-only guard would stick a frozen line on gameover), `ctx.globalAlpha`-fading over its last `CAPTION_FADE` s. **`drawCaption()` is a SIBLING of `drawHUD()`** — called right after `if (Capture.hudVisible) drawHUD();`, NOT inside it (FLAG-G — captions survive the `H` capture toggle; they're accessibility text, not a HUD readout). New look-call knobs `CAPTION_LINGER 1.5 / CAPTION_FADE 0.4 / CAPTION_Y 64 / CAPTION_SIZE 20` (grouped by the VOICE knobs). **Net behaviour:** captions show for exactly the lines that clear the gate, never late, **independent of Voice Volume and of the Off style**; audio still only plays when `voiceEnabled()`. **DEVIATION worth recording (a real anchor-drift the prompt's shorthand missed):** the prompt (and the phase-doc exact-work) wrote `if (this.voiceEnabled())`, but `voiceEnabled()` is a **GLOBAL** function (added in CS011 P1), NOT a `VoiceSys` method — `this.voiceEnabled()` would throw a TypeError in the live build. Shipped the global `voiceEnabled()`. **Second consequence, surfaced for your confirmation:** P2 is the FIRST phase where selecting voice **Off actually silences the AUDIO** — in P1 `voiceEnabled()` was defined but **unwired**, so `say()` scheduled audio regardless of `settings.voiceStyle`; P2's `if (voiceEnabled()) _schedule(utt)` is what makes Off functional. Intended (the picker in P3 needs a working Off), but it IS a behaviour change from P1 — flag it if you were relying on P1's always-on audio. `sayLevel` deliberately **NOT** added (needs P4's `levelPhon`). **Untouched (phase scope):** MusicSys/AudioSys buses; the scheduler body (only the `buildUtterance` line moved out); `_stop`/`reset`; persistence (`saveSettings`/`loadSettings`/`afd_settings_v1` all untouched — `voiceStyle`/`captions` stay P1's runtime defaults); no Options picker/toggle yet. New test `scratchpad/test-cs011-p2.js` (**34 assertions**, drives the REAL extracted script): captions-only `_emit` sets `game.caption` with NO audio scheduled (bare `ctx` stub + `voice` null so `ensure` no-ops), `showCaption` supersede, `update(1/60)` aging, a dropped equal-priority line leaves `game.caption` byte-for-byte untouched (mirror), headless `startGame()/update(1/60)` no-crash, and a full-Web-Audio-mock voice-enabled path proving `_schedule` still schedules audio AND shows the caption post-split (plus captions-off ⇒ audio-only). **Full 41-file regression green**, `node --check` clean. Docs: GDD §2.8 (new Captions bullet + the `_emit`/`_schedule` split in the opening + Architecture Map VoiceSys row), §3.2 (caption adds no new no-fills exception — it's `drawText`), the `draw()` Architecture Map row (`drawCaption` sibling), CLAUDE.md (VoiceSys non-negotiable gained a 4th point), `GDD-VERSION-HISTORY.md`, this file. **Still pending (later CS011 phases): the Options voice-style picker + Captions toggle (P3), persistence into `afd_settings_v1` (P3), and the level/chain caption lines (P4/P5).** Did NOT push (Paul's call, every phase).

**Prior session (CS011 P1):** implemented CS011 Phase 1 per the phase prompt (`IMPLEMENTATION-PHASES-CS011.md` P1; the base phase for CS011's voice-picker work). Re-grepped every anchor first (they drift). **What landed:** **(1) `VOICE_STYLES` table** above `VoiceSys` — six selectable robot voices (`comms`/`comms_f`/`flat`/`flat_f`/`vintage`/`vintage_f`), each a FULL `VOICE_PARAMS`-shaped object PLUS a top-level `ring:{on,freq,mix}`, ported **verbatim** from the `tools/voice-robot-lab.html` PRESETS (Comms robot/♀, Flat monotone/♀, Vintage computer DECtalk/♀). **FLAG-A expansion done right:** those lab presets are `Object.assign` diffs, so every field a diff did NOT restate (`pitchRange 4`/`finalFall 5`/`srcType "pulse"`/`richness 0.85`/`breath 0.12`/`bwScale 1.0`/`fricGain 1.0`/`burstGain 1.0` + the unstated radio fields) was filled from the lab's base `P` so `buildUtterance` never reads undefined; `robot.ring`→`ring`, and `robot.flange`/`robot.crush` **dropped** (they do not ship — ring-mod is the only robot stage ported). **(2) `const VOICE_PARAMS` → `let VOICE_PARAMS = VOICE_STYLES.comms`** — a live binding every consumer reads fresh; the retired human-ish `declarative` default is **gone**, `comms` is the new default (Dan audibly changes character). `VOICE_STYLE_VALUES` = the Off-first id list. **(3) Ring-mod stage in `VoiceSys.ensure()`:** `voiceBus → ringMult (0-baseline gain, sine `ringCarrier`→`ringMult.gain`) → ringWet`, `voiceBus → ringDry`, `ringWet+ringDry → ringOut`; the dry/radio **fan-out re-pointed from `voiceBus` to `ringOut`** (the two tricky edges), the squelch **static bed still feeds `rIn` directly** (downstream of the ring — correct), `ringCarrier.start()` once, `applyRing()` called alongside `applyRadio()`. **(4) `applyRing()`** (guarded like `applyRadio`) sets carrier freq + wet/dry per the active style. **(5) `setStyle(id)`** re-points `VOICE_PARAMS` for a real id then re-applies radio+ring; **`"off"` never reassigns `VOICE_PARAMS`** (keeps the last real style so `buildUtterance`/the graph stay valid); an unknown id is a no-op. **(6) `voiceEnabled()`** = `settings.voiceStyle !== "off"`. **(7) `settings`** gained `voiceStyle:"comms"` + `captions:true` — **runtime defaults only, NOT persisted** (`saveSettings`/`loadSettings` untouched, so the frozen `afd_settings_v1` is unchanged; persistence is a later CS011 phase). **DECISION worth recording (a risk the prompt's shorthand didn't spell out):** the prompt wrote ring `{on:false}` for flat/vintage, but `applyRing` sets `ringCarrier.frequency.value = R.freq` **unconditionally** (the carrier always runs), so a bare `{on:false}` would write `undefined`→NaN in the live build. Shipped the ring-off styles as the FULL `{on:false, freq:40, mix:0.5}` — exactly the lab's base-`P` ring values those presets leave unchanged — so `applyRing` never reads undefined; verbatim port, not a re-tune. **Untouched (as the phase scoped):** MusicSys/AudioSys buses; the `_render` scheduler body (only the `dry`/`rIn` source edges moved); `_stop`/`reset` (ring nodes are persistent, not per-utterance); no UI/picker, no persistence, no captions yet. New test `scratchpad/test-cs011-p1.js` (**387 assertions**, drives the REAL extracted script): 6 full styles with no undefined field + a valid ring block, verbatim-value spot checks against the lab presets, finite `buildUtterance` `dur` for every style (the FLAG-A guard), the `setStyle`/`"off"`/`voiceEnabled` refactor, and the ring stage wired end-to-end through the Web Audio mock (comms ring-on gains vs flat ring-off pass-through). **Full 40-file regression green**, `node --check` clean. Docs: GDD §2.8 (signal-path ring stage + opening + Architecture Map VoiceSys row), CLAUDE.md (VoiceSys non-negotiable now covers `VOICE_STYLES`/ring + the code map), this file. **Still pending (later CS011 phases): the Options voice-style picker, persistence into `afd_settings_v1`, and on-screen captions.** Did NOT push (Paul's call, every phase).

## GDD version history

- **CS011 P1 — VoiceSys: `VOICE_STYLES` table, active-style refactor, ring-mod stage.** `IMPLEMENTATION-PHASES-CS011.md` P1 — the base phase for CS011's player-selectable voice work (no UI/persistence/captions yet; those are later phases). Ported **verbatim** from the second voice instrument `tools/voice-robot-lab.html` (the same MusicSys/music-lab port-verbatim rule now extended to it). **(1) `VOICE_STYLES` — six selectable robot voices** (`comms`/`comms_f`/`flat`/`flat_f`/`vintage`/`vintage_f`) above `VoiceSys`, each a **full** `VOICE_PARAMS`-shaped object PLUS a top-level `ring:{on,freq,mix}`, sourced from the lab's "Comms robot (announcer)"/♀, "Flat monotone"/♀ and "Vintage computer (DECtalk)"/♀ presets. **FLAG-A (the diff→full-object trap), handled:** the lab presets are `Object.assign` diffs over the lab's base `P`, so they do **not** restate `pitchRange`/`finalFall`/`srcType`/`richness`/`breath`/`bwScale`/`fricGain`/`burstGain` (nor the unstated radio fields); each style was **expanded to a full object** by merging those base-`P` values with the preset overrides, so `buildUtterance` can never read `undefined` (which would surface as a `NaN` utterance duration). The lab's `robot.ring` was flattened to `ring`; **`robot.flange`/`robot.crush` were dropped** — they do not ship (ring-mod is the only robot stage ported). Female = male + raised `basePitch` + `f1Scale 1.14 / f2Scale 1.18 / f3Scale 1.16`. **(2) Active-style refactor:** `const VOICE_PARAMS` → **`let VOICE_PARAMS = VOICE_STYLES.comms`**, a live binding every consumer (`buildUtterance`/`applyRadio`/`applyRing`/`_render`) reads fresh — the retired human-ish `declarative` default is **gone**, `comms` is the new default, so Dan audibly changes character. `VOICE_STYLE_VALUES = ["off", …6 ids]` (Off-first, for the future picker). **(3) Ring-mod stage in `VoiceSys.ensure()`** (ported from the lab): `voiceBus → ringMult` (a 0-baseline gain the sine `ringCarrier` drives, i.e. a multiply) `→ ringWet`, `voiceBus → ringDry`, `ringWet + ringDry → ringOut`; **the dry/radio fan-out was re-pointed from `voiceBus` to `ringOut`** (the two load-bearing edges — the ring now sits in front of both paths) while the squelch static bed still feeds the radio input `rIn` **directly**, downstream of the ring (correct). `ringCarrier.start()` once (static-bed precedent); `applyRing()` called alongside `applyRadio()`. Ring-off styles are a **transparent pass-through** by construction (`ringWet 0 / ringDry 1`), so a non-ring voice behaves exactly as the pre-CS011 direct fan-out did. **(4) `applyRing()`** (guarded like `applyRadio`: `if (!AudioSys.ctx || !this.ringOut) return;`) sets `ringCarrier.frequency` + the wet/dry gains from `VOICE_PARAMS.ring`. **(5) `setStyle(id)`** re-points `VOICE_PARAMS` for a real id then re-applies radio+ring (both self-guard headless); **`"off"` never reassigns `VOICE_PARAMS`** (keeps the last real style so `buildUtterance`/the graph stay valid), and an unknown id is a no-op. **(6) `voiceEnabled()`** = `settings.voiceStyle !== "off"` (single source of truth — the "enabled flag" is satisfied by reading the setting). **(7) `settings`** gained `voiceStyle:"comms"` + `captions:true` as **runtime defaults only** — `saveSettings`/`loadSettings` were **not** touched, so the frozen `afd_settings_v1` is unchanged and persistence is deferred to a later phase. **Decision recorded (a risk the prompt's shorthand left implicit):** it wrote ring `{on:false}` for flat/vintage, but `applyRing` sets `ringCarrier.frequency.value = R.freq` **unconditionally** (the carrier always runs), so a bare `{on:false}` would write `undefined`→NaN in the live build — shipped the ring-off styles as the full `{on:false, freq:40, mix:0.5}` (the lab base-`P` ring values those presets leave unchanged), so `applyRing` never reads undefined; a verbatim port, not a re-tune. **Untouched (phase scope):** MusicSys/AudioSys buses; the `_render` scheduler body (only the `dry`/`rIn` source edges moved); `_stop`/`reset` (ring nodes are persistent chain nodes, not per-utterance). Headless: `scratchpad/test-cs011-p1.js` (new, **387 assertions**, drives the real extracted script) — 6 full styles with no undefined field + a valid `ring` block + no stray flange/crush keys, verbatim-value spot checks against the lab presets, a **finite `buildUtterance` `dur` for every style** (the FLAG-A guard), the `setStyle`/`"off"`/unknown-id/`voiceEnabled` refactor, and the ring stage wired end-to-end through the Web Audio mock (comms ring-on gains `wet 0.25 / dry 0.75 / freq 55` vs flat ring-off `wet 0 / dry 1` pass-through). Full **40-file regression green**, `node --check` clean. Docs: GDD §2.8 (signal-path ring stage + the opening `VOICE_PARAMS`-is-now-an-active-style note + Architecture Map VoiceSys row), CLAUDE.md (VoiceSys non-negotiable extended to `VOICE_STYLES` + ring; code map), this entry, STATUS.md (with the still-pending picker/persistence/captions).

- **CS011 P2 — VoiceSys: `_emit`/`_schedule` split + screen-space captions.** `IMPLEMENTATION-PHASES-CS011.md` P2. The **one tricky part** (flagged `ultrathink`): extract the Web-Audio scheduler out of `say()` so the cooldown/priority gate arithmetic is **byte-identical** and only the pure `buildUtterance` line moves. **(1) `_render` → `_schedule(utt)`:** renamed; the sole change to the scheduler body is **removing its `const utt = buildUtterance(phonStr, VOICE_PARAMS)` line** — `utt` is now the argument. `ensure()`, the `!AudioSys.ctx` guard, `ctx.resume()`, `_stop()`, and the entire node graph/scheduling are untouched (verbatim PORT-ME BLOCK A). **(2) `_emit(line, p)`** — the new voice **channel**: `!AudioSys.ctx → null`; `ensure()`; `now = AudioSys.now()`; the **exact** gate lifted from the old `say()` (`now < busyUntil` & `p <= curPriority` → drop; else `now < busyUntil + VOICE_COOLDOWN` → drop; higher-priority falls through and `_schedule` pre-empts); then `utt = buildUtterance(line.phon, VOICE_PARAMS)` (moved up, pure), `if (settings.captions) showCaption(line.text, utt.dur)`, `if (voiceEnabled()) _schedule(utt)`, and `curPriority = p; busyUntil = now + 0.10 + utt.dur`. Because `busyUntil`/`curPriority` advance whenever a line **passes** — even captions-only (voice Off) — the gate is byte-identical to the pre-split `say()` (the old `(utt ? utt.dur : 0)` collapses to `utt.dur`, since past the ctx guard `buildUtterance` always returns a real object). **(3) `say(event)`** shrinks to: lines-empty guard → random pick → `return this._emit(line, VOICE_PRIORITY[event]||1)`. **Spec-drift correction (re-grep, per CLAUDE.md):** the phase prompt wrote `this.voiceEnabled()`, but `voiceEnabled()` is a **global** function (CS011 P1), not a `VoiceSys` method — `this.voiceEnabled()` would throw — so `_emit` calls the global `voiceEnabled()`. `sayLevel` deliberately **NOT** added (needs P4's `levelPhon`). **(4) Captions.** `game.caption {text, life, dur}` on the `game` literal, re-init in `startGame` **above** the `VoiceSys.reset()/nextWave()` pair (FLAG-D ordering, so a future `sayLevel(1)` inside `nextWave` has it seeded) and cleared (`life = 0`) in `quitToTitle`. **`showCaption(text, dur)`** overwrites `game.caption` wholesale (`life = 0.10 + dur + CAPTION_LINGER`) — supersede, matching voice pre-emption. Aged `-= dt` in `update()`'s playing body beside the floaters. **`drawCaption()`** renders a screen-space `drawText` at `VIEW_W/2, VIEW_H − CAPTION_Y`, gated `game.state === "playing" && !game.paused && game.caption.life > 0` (**FLAG-C** — `caption.life` freezes while not-playing, so a `!paused`-only guard would stick a frozen caption on gameover forever), fading over its last `CAPTION_FADE` s via `ctx.globalAlpha` (`drawText` leaves `globalAlpha` alone). It is a **sibling** of `drawHUD()` in `draw()`, called right after `if (Capture.hudVisible) drawHUD();` (**FLAG-G** — inside `drawHUD` the `H` capture key would hide captions too; as a sibling they survive it — accessibility text, not a HUD readout). New knobs (look-call): `CAPTION_LINGER` 1.5, `CAPTION_FADE` 0.4, `CAPTION_Y` 64, `CAPTION_SIZE` 20. **Net behaviour:** captions show for exactly the lines that clear the gate, never late, **independent of voice volume and of the Off style** — but audio still only plays when `voiceEnabled()`. (As a side-effect, this is the first phase where selecting voice Off actually **silences** the audio: in P1 the `voiceEnabled()` helper existed but was unwired, so `say()` scheduled audio regardless; P2 wires the gate.) Headless: `scratchpad/test-cs011-p2.js` (new, **34 assertions**, drives the real extracted script) — captions-only `_emit` sets `game.caption` with **no** audio (bare-ctx stub, `voice` null so `ensure` no-ops), `showCaption` supersede, `update(1/60)` aging, a dropped equal-priority line leaves `game.caption` untouched (mirror), headless `startGame()/update()` no-crash, and a full-mock voice-enabled path proving `_schedule` still schedules audio **and** captions after the split (plus captions-off = audio-only). Full **41-file regression green**, `node --check` clean. Docs: GDD §2.8 (new Captions bullet + the `_emit`/`_schedule` split in the opening + Architecture Map VoiceSys row), §3.2 (caption adds no new fills exception — it's `drawText`), the `draw()` Architecture Map row (`drawCaption` sibling), CLAUDE.md (VoiceSys non-negotiable), this entry, STATUS.md (captions live; menu picker/persistence + level/chain lines pending).

- **CS011 P3 — Options: Voice picker + Captions toggle, additive persistence.** `IMPLEMENTATION-PHASES-CS011.md` P3. Mechanical — mirrors two idioms already in the file: the Music-Track cycler (`menuSound`) and the known-value-else-default persistence pattern (`loadSettings`) already used by `musicTrack`/`shipTurnScale`. **(1) `SOUND_ROWS`** gained `"Voice"`/`"Captions"` between "Voice Volume" and "Music Track" — additive; `VOL_LABELS`/`VOL_CATS` untouched, so the two new rows stay outside the slider machinery entirely. **(2) `VOICE_STYLE_LABELS`** (near `MUSIC_TRACK_LABELS`) — display labels for the seven `VOICE_STYLE_VALUES` (`Off`/`Comms`/`Comms ♀`/`Monotone`/`Monotone ♀`/`Computer`/`Computer ♀`). **(3) `menuSound`** gained two label-dispatched left/right branches: **Voice** cycles `settings.voiceStyle` through `VOICE_STYLE_VALUES` (wraps both directions, `off ↔ vintage_f`) and calls `VoiceSys.setStyle()` so the switch is audible immediately, then `saveSettings()`; **Captions** flips `settings.captions` (either direction), then `saveSettings()`. **(4) `drawSound()`** — panel height `510→602` (+92 px for the two rows), footer `y+490→y+582` (same 20-px-from-bottom convention); the single Music-Track `else` branch became a three-way value-column dispatch (Music Track → `MUSIC_TRACK_LABELS`, Voice → `VOICE_STYLE_LABELS[settings.voiceStyle]`, Captions → On/Off text) — still plain `drawText`, no new fill/stroke idiom introduced. **(5)/(6) `saveSettings`/`loadSettings`** — `voiceStyle`/`captions` added to the saved blob, additive (no schema bump). On load, `voiceStyle` only overwrites the runtime default if it's a real `VOICE_STYLE_VALUES` id (unknown/corrupt → stays `"comms"`); **`captions` only overwrites if `typeof data.captions === "boolean"`** — an absent key (older save) or a non-boolean value leaves the `true` default untouched, with **no `else` branch that could snap it to `false`**. `loadSettings` also calls `VoiceSys.setStyle(settings.voiceStyle)` after the restore, so a loaded style is actually applied to the live acoustic engine at startup, not just stored inertly. Headless: `scratchpad/test-cs011-p3.js` (new, **39 assertions**, drives the real extracted script, same Web-Audio-Proxy-mock + in-memory-`localStorage` idiom as `test-cs011-p2.js`/`test-v34-p6.js`) — `SOUND_ROWS` shape/order; the Voice cycler advances and wraps both directions with `VoiceSys.setStyle` spied on each step; the Captions toggle flips either direction; a full save → fresh-instance-startup-load → explicit-`loadSettings()` round trip for both fields; the tolerance matrix (unknown `voiceStyle` → stays `"comms"`, `captions` omitted → stays `true`, `captions:"yes"` → stays `true` not coerced, `captions:false` → **is** accepted, not permanently locked true); `drawSound()` renders every new-row selection state without throwing; headless `startGame()/update(1/60)` no-crash. **Also fixed a pre-existing stale snapshot found while touching the same array:** `test-cs010-p4.js`'s `expectedSound` still listed the pre-P9 5-row `SOUND_ROWS` (CS010 P9 added "Voice Volume" without updating this assertion) — corrected to the current 8-row shape. Full **42-file regression green**, `node --check` clean. `GAME_VERSION` left at `"1.0.0.10"`, matching CS011 P1/P2 precedent. Docs: GDD §2.16 (new Voice & Captions bullet after Music Track; the Persistence bullet gained the two additive fields + the boolean-only captions rule; the CS010-P4 menu-restructure bullet's inline `SOUND_ROWS` quote — also stale since P9 — corrected in the same pass), CLAUDE.md (frozen-keys bullet notes the two new additive fields), this entry, STATUS.md. This closes out CS011 P3 — the level/chain caption lines (P4/P5) remain.

- **CS011 P4 — VoiceSys: level announcements (natural words, digit fallback ≥100).** `IMPLEMENTATION-PHASES-CS011.md` P4. Re-grepped every anchor first — three drifted from the prompt's estimate (`VOICE_LINES` ~L1458→actual L1498, `VOICE_PRIORITY` ~L1550→actual L1590, `nextWave`/`game.wave++` ~L3387→actual L3526/3527); `VoiceSys`'s `_emit` (from P2) confirmed at its documented shape. **P0's lab output was already sitting in `tools/voice-robot-lab.html` as an uncommitted PASTE-READY comment block** (Paul's audition step) — `LEVEL_PHON`, the 28-entry `NUM_PHON` (including `zero`, FLAG-I), and the five `chain_broken` lines (deferred to P5) were all present and already verified against the lab's `parsePhonTokens` with zero errs; ported the level/number data **verbatim**, composed nothing new. **(1)** `LEVEL_PHON`/`NUM_PHON`/`DIGIT_WORD` placed right after `VOICE_LINES` (`DIGIT_WORD` is new, not in the lab dump — a trivial `"0".."9"` → word map needed by the ≥100 fallback, not phon data itself). **(2)** `numberToWords(n)` (pure): `n<20` → one ones/teens word; else tens word + ones word only if `n%10`. **(3)** `levelPhon(n)` (pure): `n<=99` → `LEVEL_PHON + " / " + numberToWords(n).map(NUM_PHON lookup).join(" / ")`; `n>=100` → digit-by-digit via `DIGIT_WORD`+`NUM_PHON`, same `" / "` cosmetic-gap convention. **(4)** `VoiceSys.sayLevel(n)` added right after `say(event)`: `return this._emit({text:"Level "+n, phon:levelPhon(n)}, VOICE_PRIORITY.level)`. **(5)** `VOICE_PRIORITY.level = 2`. **(6)** `nextWave()`: `VoiceSys.sayLevel(game.wave)` right after `game.wave++` — since `startGame()` calls `nextWave()` for wave 1 right after `VoiceSys.reset()`, Level 1 is announced on every fresh run, not just wave transitions. The caption shows the numeral ("Level 23") from `line.text`; the audio speaks natural words ("Level twenty-three") from `line.phon`. Headless: `scratchpad/test-cs011-p4.js` (new, 32 assertions) — `numberToWords` spot-checks (1/7/13/20/23/40/99), every `levelPhon(n)` for n∈{1,7,23,40,99,100,123} parses with zero `errs` through the real `parsePhonTokens`, `VOICE_PRIORITY.level === 2`, a headless `startGame()` with a bare fake ctx (captions-only, `voiceEnabled()` false) sets `game.caption.text === "Level 1"`, and `ctx` null → `startGame()`/`update(1/60)` don't crash. Full regression green. `node --check` clean. Docs: GDD §2.8 (new "Level announcement" bullet; trigger-class count updated to include the level sub-path), CLAUDE.md Code map (`NUM_PHON`/`LEVEL_PHON`/`numberToWords`/`levelPhon`/`sayLevel`), STATUS.md. Did NOT push.

- **CS011 P5 — VoiceSys: `chain_broken` frustration lines + `breakChain` hook; CS011 docs (round complete).** `IMPLEMENTATION-PHASES-CS011.md` P5, the final phase of the changeset. Re-grepped `VOICE_LINES` (actual L1498, drifted from the prompt's ~L1458), `VOICE_PRIORITY` (actual L1630), `breakChain` (actual L4456) and its two call sites (a bullet hit, a Hunter collision), and `scatterChain` (actual L4467, the ship-death path) — all confirmed against the live build before editing. **(1) `VOICE_LINES.chain_broken`** — the five `{text,phon}` pairs from P0's lab-audited dump, ported **verbatim**: "Junk is gone!" / "Lost my scrap!" / "Trash is loose!" / "There goes the garbage!" / "There goes my junk!". **(2) `VOICE_PRIORITY.chain_broken = 2`** (same tier as `health_relief`/`health_full`/`level`). **(3)** `breakChain(i)` gained one line, `VoiceSys.say("chain_broken");`, at its single choke point (after `chain.length = i; game.deliveryCount = 0;`) — both call sites (bullet, Hunter) route through it, so a hit that severs several nodes at once still speaks only once, collapsed by the standard cooldown/priority gate. **`scatterChain()` (ship death) is deliberately NOT hooked** — the `dying`-state death spectacle already carries the moment; Dan doesn't editorialize over it. **Version:** `GAME_VERSION` bumped `"1.0.0.10"` → **`"1.0.0.11"`** (Major.Minor.Patch.Changeset scheme, CS010 P0 — the changeset segment tracks the CS### number; not bumped in P1–P3 per that phase's "Paul's call, set it whichever phase" note, so P5 closes it out as the last phase of the round). Headless: `scratchpad/test-cs011-p5.js` (new, 34 assertions, Web-Audio-Proxy-mock per `test-cs010-p9.js`'s idiom since `breakChain`'s `boom()`→`AudioSys.explosion()` needs a real graph even in a captions-only run) — the 5 lines parse with zero `errs`; `VOICE_PRIORITY.chain_broken === 2`; `breakChain(0)` on a built `game.chain` sets `game.caption.text` to one of the 5 lines and truncates the chain; a second `breakChain` within `VOICE_COOLDOWN` (currentTime advanced by 0.5s < 1.2s) is dropped, not replacing the caption (dedup — a multi-node break speaks once); with `curPriority` forced to 3 (simulating a busy `health_low`), a `chain_broken` `_emit` drops (2 ≤ 3) though the chain still breaks; `ctx` null → `startGame()`/`update(1/60)` clean. **Also fixed two pre-existing regressions surfaced by this phase's changes** (both legitimate behavior changes, not bugs): `test-cs010-p0.js` hardcoded `GAME_VERSION === "1.0.0.10"` — updated to `"1.0.0.11"` (3 spots, incl. message text). `test-cs010-p9.js`'s (E7) asserted `breakChain` "fires no line" — rewritten to assert it now fires exactly `["chain_broken"]` (via the spied `say()`), while `scatterChain` (checked separately, log cleared between) still fires nothing. Full regression re-run green: **all 44 test files, 3,718 assertions**. `node --check` clean. **Doc pass (final CS011 doc pass, this phase):** GDD §2.8 (new "chain_broken" trigger detail folded into the Triggers & latches bullet; trigger-class count 8→9 hooked classes / 24→29 `VOICE_LINES`-indexed lines across 9 event classes, level kept as the separate 10th composed class; the priority-ladder sentence spelled out in full: `health_low` 3, `health_relief`/`health_full`/`level`/`chain_broken` 2, everything else 1), §2.18's stale `GAME_VERSION ("3.6"...)` parenthetical corrected to the real `Major.Minor.Patch.Changeset` scheme, top-of-file Current-build line → `CS011 P5`/`"1.0.0.11"`, Architecture Map `VoiceSys` row (added `chain_broken`/the `breakChain` hook/the `tools/voice-robot-lab.html` phon-port credit). CLAUDE.md — added a dedicated **`tools/voice-robot-lab.html`** design-instruments bullet (a sibling of the existing `voice-lab.html` entry — it existed only as inline mentions until now, despite being the P1/P4/P5 port source); the VoiceSys non-negotiable itself already covered `chain_broken`/`VOICE_STYLES`/ring-mod from earlier phases. STATUS.md — CS011 marked complete, P0–P5 summarized. This closes CS011 — player-selectable voice styles, ring-mod, captions, level announcements, and the chain_broken frustration line are all shipped. Did NOT push.
