# UNSORTED

Archived from `STATUS.md` on 2026-08-05 during a size cleanup (see CS023 planning session — STATUS.md had grown to 856 KB, most of it from a formatting bug that fused years of session write-ups onto a handful of single mega-lines), and again on 2026-08-10 at CS024 P7's closing archive check, which relocated **all CS021 and CS022 material** here — straight, unsummarised, each entry its own paragraph, newest-first within every section. Contains everything **CS022 and older**, including the pre-CS009 v2.0–v3.6 changelog era. ⚠ Three pre-existing mega-lines from the original fusion bug survive in the recap section (each holding several `**Prior session…**` write-ups on one physical line); they predate both archive passes and were deliberately left as-is rather than re-split, since this file is a faithful record and re-flowing it would rewrite content. Entries are newest-first, matching the live STATUS.md convention. **Not attached to planning/implementation sessions by default** — per repo convention, archive files are excluded from session context.
