# CS026

**CS026 P6 — THE CLOSING PHASE: THE GATE APPLIED, `GAME_VERSION` 1.0.0.26, AND THE FULL DOC SWEEP. CS026 IS COMPLETE.** Per `IMPLEMENTATION-PHASES-CS026.md` P6 / `PLANNED-FEATURES-CS026.md` §9/§11, against the gate answers recorded inline in `## Playtest asks`.

**(1) SIX OF THE EIGHT QUESTIONS CAME BACK CLEAN AND MOVED NOTHING.** Q1 pacing — *"none are wrong. Pacing is much better"* — which is **this changeset's central bet confirmed** and validates CS026 P2's `junkSplit` lever at its shipped 2-through-L10 / 3-from-L11 shape. Q2 the smaller early world *"is fine"*. Q3 the score curve *"is fine"* — **the answer that clears FORK-CS026-C's accepted risk**, which was the one place this changeset could have made the early game *harder* (a leaner debris score curve also halves how often the fixed 10,000-point `REPAIR_MILESTONE` repairs the hull); it did not. Q4 garbage density fine, and **re-checked against the post-split shed volume in the same session, which was the entire point of asking it there** — 220/300 hold, and FLAG-CS024-c is discharged. ⛔ Paul explicitly declined to give Q4 a number (*"I do not know what number to report, so forget about that unless you have to have something"*), and the correct handling of that is to change nothing. Q6 the incidental delivery *"is fine"*. Q7 the level banner *"looks great"* — so all four knobs P5 promoted stand unmoved at their CS025 first guesses, **now playtested rather than guessed**.

**(2) Q5 — THE DELIVERY PAYOFF. A POSITION CHANGE PLUS TWO DIRECTIONAL RETUNES, NONE OF THEM A NUMBER PAUL SUPPLIED.** *"It needs to be closer to the ship, and the score numbers need to fade more slowly, and they need to travel upwards more slowly. The text of the score numbers is a good size."* Applied as `deliveryFloatRise` 300 → **160**, `deliveryFloatLife` 0.55 → **1.2**, and a move of **both** delivery branches' floater origin from `node.x, node.y` to `game.ship.x, game.ship.y - DELIVERY_FLOAT_DY` (22). **The origin was the real defect:** the offload pops from the chain's **tail**, so on a full haul the `"+pts"` was being drawn at the canister ~24 links *behind* the ship — precisely where the player is not looking. Size 16 is untouched, as reported.

**⛔ (3) THE TRADE PAUL WAS SHOWN AND THE OPTION HE DECLINED — RECORDED SO IT IS NOT RE-LITIGATED AS A BUG.** Spawning at the node meant the floaters inherited the chain's own spatial spread for free. One origin removes it, so separation becomes **purely `rise × DOCK_OFFLOAD_INTERVAL`** — a nominal **8 px** (160 × 0.05) against P4's 15 px, and **~10.7 px on screen**, because `game.offloadTimer` is decremented by `dt` and quantises to every *fourth* frame at 60 fps rather than every third (0.05 is not binary-representable; the third subtraction leaves a hair above zero). He was offered the alternative — cadence to 0.10 s, restoring a 16 px nominal at the cost of doubling a 24-canister dock visit from 1.2 s to 2.4 s, with a new `dockOffloadInterval` registry row — and **chose to hold delivery pacing and accept the tighter column**. So `DOCK_OFFLOAD_INTERVAL` stays 0.05, **no knob was added, and the registry holds at 85**. ⛔ If a later gate wants the column opened back up, **the cadence is the lever; a bigger rise is not**, that being the one thing Q5 explicitly asked to reduce. The milestone floaters (`SALVAGE BONUS`, `MAX HAUL`) deliberately did **not** move — they belong to the dock, and keeping them there is what stops them colliding with the column now rising off the ship (spec §3.6).

**(4) Q8 — THE LOOK-CALL BACKLOG, THE ONLY QUESTION THAT RETURNED A LIST.** Four named Achievements-screen defects, all fixed at look-call constants. `ACH_TAB_MARK` (`" ▼"`) on the selected tab, **additive** to the existing lit/dim colour split (Q8.1: brightness alone did not read as selected). `ACH_ROW0_Y` +130 → **+152**, opening row 0's air under the tab baseline from 22 px to 44 — a full text-height (Q8.2). `ACH_DESC_DY` 46 → **30** (Q8.3), which also opens the description-to-next-name gap from 26 px to 42: **more space between rows than inside one**, which is what makes a row read as a row. `ACH_ROW_STEP` is deliberately unchanged, so the panel's pitch and scroll ceiling keep their shape. **(Q8.4) `ACH_STATUS_DY` 24 → 0** — the status rejoins the name's baseline — tied to it by `achLeader()`, a dotted leader run drawn on **both** `drawAchRow` branches.

**⛔ (5) Q8.4 IS THE SECOND DECISION THAT WAS PUT TO PAUL FIRST, AND THE ANSWER CHANGED WHAT GOT BUILT.** His own suggestion was an alternating row background. That would have been a **fill**, i.e. a **third** named GDD §3.2 exception (there are exactly two: the death flash and the low-health corner glow). Shown the trade, he chose the leader instead — which is `drawText`, the already-sanctioned `fillText` path. **The exception count stays at TWO**, pinned by `test-cs026-p6.js` §F with a recording proxy over both row branches. ⛔ **`achLeader()` divides by a measured glyph width and is the first code in the Achievements render path to do arithmetic on `ctx.measureText()` at all.** Several suite stubs return `{width: 0}`; `"·".repeat(span / 0)` is `repeat(Infinity)`, a `RangeError` thrown out of the **menu** renderer on a path a dozen test files drive. Both guards are written `!(x >= n)` / `!(x > 0)` rather than `x < n` / `x <= 0` **deliberately** — NaN fails every ordinary comparison, so only the negated form bails. Zero, NaN, absent, negative and enormous widths are all driven against the real renderer in §D.

**(6) THE VERSION SWEEP SPLIT THREE WAYS, AS PREDICTED.** `GAME_VERSION` `"1.0.0.25"` → `"1.0.0.26"`. **Seven live pins repointed** (`test-cs010-p0`, `-cs013-p4`, `-cs015-p7`, `-cs016-p5`, `-cs017-p7`, `-cs018-p10`, `-cs021-p4` — the phase prompt predicted "roughly six"). **Five CS026 phase pins flipped to the standing mirror image** (`!== "1.0.0.25"`, permanently true) with an inline note not to re-point them to a literal again: P1–P5 each asserted the version was unmoved *and named P6 as the phase that owns the bump*, so P6 falsifies the literal form **by instruction**. The planning pair (`PLANNED-FEATURES-CS026.md`, `IMPLEMENTATION-PHASES-CS026.md`) was **not** rewritten — it is the record of what the changeset set out to do.

**(7) THE DOC SWEEP, INCLUDING THREE STALE §2 CLAIMS THE PHASE PROMPT DID NOT LIST.** ⛔ **The prompt named "four present-tense 3-way split claims in §2 (lines 55, 67, 78, 204)" and that count is WRONG — only ONE of the four is a debris claim.** Lines 67, 78 and 204 are **Hunter** claims, and CS026 P2 deliberately left `destroyHunter()` at a hardcoded 3-way split because `ACH_LINEAGE_FULL = 13` (1 + 3 + 9) depends on it; moving them would have falsified the GDD and broken an achievement's rationale. Only §2.4's line 55 moved. Three further stale claims were found and fixed while in there: §2.4's *"1 large → 3 mediums → 9 smalls = 13 kills → 52 canisters"* (now levered: 7 kills / 28 canisters at 2-way, 13 / 52 at 3-way); §2.10's `COMBO n/N` readout, still asserted in the present tense though CS026 P4 **removed** it (now marked `[REMOVED — CS026 P4]`, paragraph kept as the record of what it was); and **§2.11.1's *"as of CS024 P1 every level runs at one size"*, falsified by CS026 P3** and now marked `[SUPERSEDED — CS026 P3]`. §3's Flow-functions row carried two stale claims and both are corrected: the long-dead `min(3+wave, 9)` debris formula, and *"pushes 3 children"*. §2.4 gains the retired `DEBRIS_MASS` conservation derivation (FORK-B); §2.10 gains the full delivery-feedback spec; §2.8's banner bullet gains the four knobs and the clean Q7 result. **GDD §3's five dangling `§2.13.1` cross-references (across three lines) are retired** to `the orbit-archetype spec [RETIRED — CS024]` — the `[RETIRED …]` marker style is load-bearing and was preserved, not deleted.

**(8) `DIFFICULTY-LEVERS.md`.** §3 gains **`junkSplit` as row 5 — the table's only two-state lever** (floor 2, ceil 3, steps 2: exactly one step, ever), `↳`-carried by `junkCount`, with the note that it is consumed by `destroyDebris()` **only** and why `destroyHunter()` must not be levered with it; the header count goes 17 → **18**. §4 gains not-a-lever rows for `deliveryFloatRise`/`deliveryFloatLife` (and `DELIVERY_FLOAT_DY`, frozen, not even a knob) and for `earlyWorldLevels`. §5 records that the garbage density ceiling was **re-checked against the new volume and held**, and — per the standing rule — states explicitly that **the early-world shrink changes no ceiling**: the soft/hard maxima are absolute piece counts, not areal densities, so a 56%-area torus raises density per unit area without raising any count. §6 gains this gate's outcome in full, including the declined knob and the declined fill.

**(9) THE REGRESSION WAS RUN TWICE CONSECUTIVELY AND THE RUNS DIFFED — the first closing phase able to treat a non-empty diff as a real finding, which is what CS026 P1's seeding bought.** See `## Working / verified`.

**(10) THE `STATUS.md` SIZE CHECK WAS PERFORMED, AND THE PHASE PROMPT'S PREDICTION WAS SLIGHTLY OFF.** It expected the rolling window to cover CS024 + CS025 + CS026. It actually still covers **CS023** as well (the P4b/P1–P4c closed-out-as-superseded entries). CS023 is **exactly 3 rounds** behind CS026 — at the threshold, not past it — so **no prune is due this phase**, which is the same conclusion the prompt reached by a different route. ⛔ **CS023 is the changeset to prune into `archive/STATUS-HISTORY.md` at CS027's close**, and it will be more than 3 rounds behind by then.

**CS026 P5 — THE LEVEL BANNER CAN FINALLY BE ANSWERED WITH A NUMBER, AND THE BLOCKING GATE IS NOW WRITTEN.** Per `IMPLEMENTATION-PHASES-CS026.md` P5 / `PLANNED-FEATURES-CS026.md` §6. CS025 P5 built the level banner (critical voice line + large fading centre-screen "Level N") out of that changeset's own gate answer (Q6), so it arrived **after** that gate closed — nobody has ever seen it in motion, and its four constants (`LEVEL_BANNER_TIME` 2.2, `LEVEL_BANNER_FADE` 0.5, `LEVEL_BANNER_SIZE` 72, `LEVEL_BANNER_Y` 24) were first-guess numbers with no registry row, which collides with the house rule that a gate reports a number. This phase promotes all four to GLOBAL debug knobs and then writes the CS026 playtest gate itself.

**(1) Four new GLOBAL registry rows, appended after `startLevel`: `levelBannerTime` (s, def 2.2, 0–8, step 0.1), `levelBannerFade` (s, def 0.5, 0–3, step 0.1), `levelBannerSize` (px, def 72, 16–160, step 4), `levelBannerY` (px, def 24, −200–200, step 4). Registry 81 → 85.** Each constant stays in place as the row's `def` — the standing "retune the const, never the `def`" convention. None of the four is a lever: no floor/ceil/steps triple, no `▼`/`↳`, no `carriesTo`, no `LEVERS` entry — a banner's size and duration are look-calls, not pressure axes.

**(2) `nextWave()` and `drawLevelBanner()` BOTH now read `DEBUG.levelBannerTime` — never one reading the constant while the other reads the knob.** `drawLevelBanner()` derives `elapsed` by subtracting `game.levelBanner.life` from the SAME value `nextWave()` seeded it with; if the two sites ever diverged, a mid-level slider drag would send `elapsed` negative or make it jump, and the banner would flicker or vanish. The existing `Math.max(0, ...)` guard is what makes a mismatch (or, now, a genuine mid-level retune) degrade gracefully instead — `test-cs026-p5.js` §C reproduces the drag directly (seed life under one `levelBannerTime`, change the knob, confirm alpha stays finite and in [0, 1] in both directions) rather than trusting the guard exists.

**(3) `DIFFICULTY-LEVERS.md` §4 gains one not-a-lever row covering all four ids — the one design-doc edit this phase makes.** No GDD edit: the banner is already in §2.8 as shipped behaviour, and its *values* were never the GDD's subject.

**(4) THE SUITE COST: THE REGISTRY BUMP (81 → 85) TOUCHED 24 FILES, ALL COUNT/ORDER/ALLOW-LIST REPOINTS, NO BEHAVIOURAL CHANGES.** Same repoint-and-annotate idiom every registry-growing phase this changeset has used: every live "the registry holds 81 rows" pin, every GLOBAL-section membership list, `test-cs024-p6b.js`'s collapsed-order comparison (which needed its `levelBannerTime..Y` strip added *before* the existing `,startLevel$` strip, since the four new rows are now the true tail), `test-cs024-p6c.js`'s non-lever count (27 → 31), and every `LATER`/`LATER_ROWS` allow-list (`test-cs025-p1/p2/p5.js`, `test-cs026-p2/p3/p4.js`) now names the four `levelBanner*` ids explicitly. The whole suite (105 `scratchpad/test-*.js` files) runs green after the repoint pass.

**(5) THE GATE HANDOFF IS WRITTEN, PER §10, AND IT IS OPEN AND BLOCKING.** All eight questions from `PLANNED-FEATURES-CS026.md` §10 are now in `## Playtest asks`, verbatim, with the full P2–P5 briefing, the play range (levels 1 → 12, including at least one level in the 8–11 band), and the standing report-the-number instruction. **P6 must not run until the answers are recorded there.**

**CS026 P4 — DELIVERY FEEDBACK IS LEGIBLE AGAIN: THE FLOATERS SEPARATE, THE HUD `COMBO` ROW IS GONE, AND INCIDENTALS ARE QUIETED, NOT FOLDED IN.** Per `IMPLEMENTATION-PHASES-CS026.md` P4 / `PLANNED-FEATURES-CS026.md` §3, FORK-CS026-E → **spread the existing floaters** (not a single updating tally — see the correction below), FORK-CS026-F → **drop** the HUD row, FORK-CS026-G → **quieter**, never folded in. The defect: `DOCK_OFFLOAD_INTERVAL` dropped from 0.13 to 0.05 in v3.4 P1 (20 canisters/sec) but `FloatText`'s rise (30 px/s) and life (1.1 s) were never re-tuned, so consecutive delivery floaters were born **1.5 px** apart — a 12-canister haul stacked twelve of them inside a 33 px band. Nothing had ever been removed; the escalating `+50/+75/+100` floater still fired every delivery, it was just unreadable.

**(1) `FloatText` gained trailing, optional `rise`/`life` params — `constructor(text, x, y, color, size = 16, rise = 30, life = 1.1)`.** All ten pre-existing call sites (SCOOP ±1/MAX, powerup pickups, the auto-shield penalty, GUARDED, SALVAGE BONUS, MAX HAUL) omit them and stay byte-identical — the CS012 P3 trailing-`size` precedent, repeated. **⛔ THE TRAP WAS THE ALPHA DIVISOR.** `draw()` used to divide by the literal `1.1`; if `life` became a parameter without also changing the divisor, a short-lived floater would fade in at a fraction of full opacity and never reach 1. Fixed by storing the constructed value as `this.life0` and dividing by that instead — `test-cs026-p4.js` §C pins the trap directly (a `life=0.2` floater reaches alpha 1.0 at birth, not ~0.18) and states the mutant it catches inline.

**(2) Two new DELIVERY registry knobs, right after `dockComboGrace`: `deliveryFloatRise` (px/s, def **300**, 30–600, step 10) and `deliveryFloatLife` (s, def **0.55**, 0.2–2.0, step 0.05). Registry 79 → 81.** Both are first-guess numbers — the playtest gate is what settles them, which is the whole reason they are knobs rather than constants. At the shipped defaults, consecutive floaters separate by **15 px** (was 1.5) and each travels **165 px** before expiring (was 33) — a 12-canister haul now reads as a rising column instead of a smear. `test-cs026-p4.js` §E measures this off a REAL 24-canister visit driven through the actual offload block (not computed): the two nearest towed floaters' actual birth-frame gap, fed through the real `FloatText.update()`, separate by exactly `rise × gap`.

**(3) ⛔ THE INCIDENTAL BRANCH IS QUIETED, NOT FOLDED IN.** There are two floater pushes in the offload block: the *towed* branch (`COLOR.dock`, size 16) and the *incidental* branch (a piece hooked while already parked at the dock — `COLOR.dim`, size 12 as of this phase). Both now read the same live `deliveryFloatRise`/`deliveryFloatLife` knobs so a Magnet-parked run of incidentals separates too, but they share **neither colour nor tally** — CS020 P1 fixed the delivery counter precisely so an incidental is not part of the haul, and merging their *feedback* would re-merge the two concepts at the exact point they were separated. `AudioSys.deliver(1)`'s flat pitch is untouched.

**(4) THE HUD `COMBO n/24` READOUT IS REMOVED — WHICH CLOSES FLAG-CS020-i, THE READOUT, NOT THE CLAIM IT WAS BUILT TO ANSWER.** `HUD_COMBO_X/Y/SIZE` and the `game.deliveryCount > 0` block are gone from `drawHUD()`. The bet: legible dock feedback (item 2, above) now carries "where am I in this haul" better than a duplicate HUD line. **⛔ THE RECORDED RISK: the readout was the only thing showing the DENOMINATOR (`/cargoMax`) — the dock floaters show none.** If the gate finds the cargo ceiling illegible without it, this is the first thing to reconsider; it should not have to be rediscovered. `test-cs021-p4.js` (CS021 P4's own test, which proved the readout's presence) is kept as living documentation rather than deleted or left broken — same precedent as `test-cs017-p6.js` §G for a fully-retired feature — its every scenario (a full skirt-and-return, incidentals, window expiry, ship death) now proves the readout's **absence**, while still driving the real counter to confirm CS020 P1/P1b's tracking machinery is untouched underneath.

**(5) ⛔ A CORRECTION TO THIS SESSION'S OWN PRIOR `## Next up` SUMMARY: FORK-CS026-E SHIPPED AS "SPREAD," NOT "ONE UPDATING TALLY."** Earlier entries in this file described the scoped work as "one updating tally" — that was `PLANNED-FEATURES-CS026.md` §3.3's option **(a)**, the doc author's own suggested reading, not Paul's actual resolution. **Paul's resolution (§0.1) and the buildable phase prompt (`IMPLEMENTATION-PHASES-CS026.md` P4) both say "spread the existing floaters,"** which is what shipped: one `FloatText` per canister, separated by tuned `rise`/`life`, not a single object whose text is rewritten in place. Flagging this now so the "one tally" phrasing doesn't get treated as ground truth by a future session skimming old paragraphs.

**(6) THE OFFLOAD BLOCK'S LOGIC IS BYTE-UNCHANGED — THIS PHASE CHANGED HOW A DELIVERY LOOKS, NOT WHAT IT PAYS.** `deliveryCount`, the towed/incidental test, the 8/12/16/20 reward tiers, Heavy Hauler, Maxed Out, and the Super Mega Delivery trigger all fire exactly as before; `AudioSys.deliver()` is untouched. `test-cs026-p4.js` §E drives a full 24-canister visit through the real offload block and checks every one of those latches against the unchanged `DOCK_BASE_SCORE`/`DOCK_BONUS_STEP` formula, alongside `SALVAGE BONUS`/`MAX HAUL` keeping their **old** default rise/life (30/1.1) — deconfliction, spec §3.6, both stay anchored at the dock and untouched by this phase.

**(7) THE SUITE COST: THE REGISTRY BUMP (79 → 81) TOUCHED TWENTY-SIX FILES, PLUS TWO REAL BEHAVIOURAL REPOINTS.** Same repoint-and-annotate idiom CS026 P3 used for its own bump: every live "the registry holds 79 rows" pin, every "N non-lever knobs," every registry-order collapse comparison, and every `LATER`/`LATER_ROWS` allow-list (`test-cs025-p1/p2/p5.js`, `test-cs026-p2.js`) now names `deliveryFloatRise`/`deliveryFloatLife` explicitly rather than being wildcarded. Two files needed a real behavioural update, not just a count: `test-cs020-p1.js`'s incidental-floater string pin (now `COLOR.dim`/size 12/the new knobs) and `test-cs021-p4.js` (item 4, above). The whole suite (all `scratchpad/test-*.js`) runs green after the repoint pass.

**(8) DOC OBLIGATIONS: NONE THIS PHASE.** No design doc was touched — `PLANNED-FEATURES-CS026.md` §3 already carries this spec, and the GDD §2.10 delivery-feedback rewrite is P6's, same as P2/P3 deferred their sections. `DIFFICULTY-LEVERS.md` needs nothing — neither new knob is a lever; `LEVERS`/`leverState` are byte-identical to the parent at every level 1..200 (`test-cs026-p4.js` §F).

**CS026 P3 — A 1920×1080 WORLD FOR LEVELS 1–5, AND `resizeWorld()` FIRES IN A REAL RUN FOR THE FIRST TIME SINCE CS024 P1.** Per `IMPLEMENTATION-PHASES-CS026.md` P3 / `PLANNED-FEATURES-CS026.md` §2, FORK-CS026-D. Three things landed and no more: `const WORLD_SIZE_EARLY = 2.25` (a third entry in the size table), `worldSizeFor(level)` returning it while `level <= DEBUG.earlyWorldLevels`, and one GLOBAL registry knob. The problem it answers is the **tail**, not the combat: at 2560×1440 the viewport shows 25% of the world and the farthest a body can be from the ship is 1,469 px, so the last two or three smalls of an early level are a search rather than a fight. At 1920×1080 it is 44% and 1,101 px. **No combat number moved** — not a lever, not a speed, not a count.

**(1) ⛔ 2.25 IS THE FLOOR OF THE SIZE TABLE AND THE BOUND IS A RENDERING ONE, RECORDED HERE SO NOBODY TRIES SMALLER LATER.** `drawEntity()` renders each body at **exactly one** wrapped image, and `onScreen()`'s reach is `VIEW/2 + CULL_MARGIN(100) + radius` — so for the largest body (a size-3 satellite, `DEBRIS_RADII[3]` = 46) the world's **half-period must clear 786 × 506** or bodies clip at the seam instead of crossing it. At 1920×1080 the half-period is 960 × 540 and clears both. At **1600×900** (size 1.5625) it is 800 × **450**, and 450 < 506: bodies clip at the top and bottom seams. At **1280×720** (size 1) they clip **and** a `SPAWN_MAX_DIST` spawn folds to **80 px** from the ship — inside `SPAWN_MIN_DIST` (220) and inside `DOCK_RADIUS` (88) — so a fully-visible world would need the flat spawn/dock rings scaled too, which is a different and much larger change. `test-cs026-p3.js` §A re-derives all of this from `CULL_MARGIN`/`DEBRIS_RADII` rather than quoting it, and asserts the two smaller sizes **fail**, so the floor is a live check and not a comment.

**(2) THE KNOB IS `earlyWorldLevels`, AND ITS 0 IS THE GATE'S CLEAN A/B.** GLOBAL section, `def: 5, min: 0, max: 20, step: 1`, inserted after `debrisBounceRestitution` and **before** P6d's `startLevel` so the gate-tooling knob keeps reading as the registry's trailing dev tool. **Registry 78 → 79.** No shipped constant backs the default — the `chainGuardIntercepts` idiom, so the entry itself is the source of truth. **Not a lever:** no floor/ceil/steps triple, no `▼`/`↳`/`(inv)`, no `carriesTo`, no `LEVERS` entry — the world period is a **stability/legibility property, not a difficulty axis**, and TRAP 3 pins `LEVERS` and `leverState` byte-identical to the parent at every level 1..200. At **0 no level satisfies `level <= 0`**, so `worldSizeFor` returns `WORLD_SIZE_FIELD` for everything, `resizeWorld()` never fires, and the build behaves exactly as CS025 shipped — driven for real in §D as a twelve-level climb that never leaves 2560×1440, not reasoned about. It is read **live at the wave boundary**, never cached: §D drags the band down mid-run and the very next level is the field world, then widens it and the level after is small again.

**(3) ⛔ THE SIZE DELIBERATELY DOES NOT FOLLOW THE ODOMETER'S SAWTOOTH, AND THAT IS THE ANSWER TO §2.3(ii)'s ODD QUESTION.** Under the odometer level 11 and level 21 are identical to level 1 in every lever, so a reader will eventually ask why the world is not. Because it is not a lever: the small world is a **one-time on-ramp**, not a difficulty rung, and making it wrap would re-shrink the world at level 11 for a player who has been in the big one for five levels. `test-cs026-p3.js` §A states this as an assertion (`worldSizeFor(1) !== worldSizeFor(11)`, `worldSizeFor(11) === worldSizeFor(21)`) rather than a comment, so a future "tidy it into the sawtooth" change fails loudly.

**(4) THE FLAT RINGS WERE CHECKED AND LEFT FLAT — AND THE OBVIOUS INSTRUMENT FOR CHECKING THEM IS THE WRONG ONE.** `SPAWN_MIN/MAX` 220/640 and `DOCK_MIN/MAX` 260/620 do not scale with the world. The tempting test is `max <= min(W,H)/2 - 60`, which **both maxima fail** at this size (that figure is 480 here) — and that would be the wrong conclusion, because `min(W,H)/2 - 60` is `resizeWorld()`'s **`dmax`**, the radius a *carried* body is clamped to when the period changes, not a bound on a *spawn*. A spawn is not clamped, it is **wrapped**: a nominal offset longer than the half-period folds back the short way. The worst case is a purely vertical offset, where 640 folds to `1080 - 640 = 440` against `SPAWN_MIN_DIST` 220, and 620 folds to 460 against `DOCK_MIN_DIST` 260 — both comfortably clear, and neither maximum even reaches the *horizontal* half-period (960), so only one axis ever folds at all. §C sweeps the whole circle analytically and then measures **hundreds of real `spawnFieldSatellites()` and 200 real `Dock` placements** in the small world. Leaving the rings flat is a checked decision, and the `dmax`-is-not-the-instrument point is recorded at the assertion so it is not "fixed" later.

**(5) ⛔ THE PHASE'S REAL SUBJECT WAS THE LEVEL 5 → 6 TRANSITION, AND IT IS DRIVEN FOR REAL ACROSS A POPULATED FIELD.** `worldSizeFor()` had returned a constant since CS024 P1 removed the orbit archetype, so `resizeWorld()` — the six-step carried-entity re-homing pass — **had not fired in live play for two changesets**; it was exercised only by `test-cs024-p1.js` §E, and *tested* and *shipped* are different claims. §E parks the ship near a corner (deliberately: **with the ship at world centre nothing can straddle a seam at all**, so staging it there would have tested step 1's old-period snapshot vacuously), loads the board with Hunters, garbage, powerups, particles, floaters, six seam-hugging pieces and a ten-node tow with a real verlet velocity, then crosses the boundary through the **real `nextWave()`**. Nothing is stranded outside the new period (bodies, chain nodes, ship, the new dock), the ship is re-centred and the camera synced, the chain's link spacing is preserved **to 1e-9** and every node's implied verlet velocity survives, and 240 real frames afterwards throw nothing.

**⛔ (5a) "NOTHING ENDS UP CLOSER TO THE SHIP" IS THE WRONG CLAIM AND WRITING IT FIRST IS HOW THE RIGHT ONE GOT FOUND.** Step 4 keeps each body's **bearing** and sets its magnitude to `min(d, dmax)`, so an over-range body **is** pulled inward, by design — FLAG-CS022-k's shell at exactly `dmax`. The checkable claim is the arithmetic itself: **new distance === `min(old distance, dmax)`, body by body, bearing unmoved to 1e-9**, with controls proving both branches ran (some bodies clamped, some did not). The player-facing version — nothing is *touching* the ship afterwards — follows from `dmax` being 660 in the new world, and is asserted separately.

**⛔ (5b) DEBRIS IS NEVER CARRIED ACROSS A TRANSITION, BY CONSTRUCTION, AND THE TEST SAYS SO RATHER THAN QUIETLY OMITTING IT.** Wave clear is `game.debris.length === 0` — debris only — so at a real transition the debris array is empty and there has never been a satellite to re-home. `resizeWorld()` still snapshots `game.debris`, which costs nothing and is what makes it safe if that ever changes. The first draft of §E populated debris and then watched the wave-clear step drop it on the floor before the resize ran, which is exactly the shape of a vacuous test; the premise is now an assertion.

**(6) `startGame()` NEEDED NO CHANGE, AND THAT IS CONFIRMED RATHER THAN ASSUMED.** It still calls `applyWorldSize(WORLD_SIZE_FIELD)` and then `nextWave()`, which compares `worldSizeFor(1)` against `game.worldSize` and resizes — on an **empty** field, where `resizeWorld()` is trivially safe. §F pins that its executable source is byte-identical to the parent and that it never mentions `WORLD_SIZE_EARLY`, that the resize precedes `new Dock()` in `nextWave()` (the order is load-bearing — the dock is placed ship-relative in the *new* period), and then drives a fresh run to confirm every satellite and the dock land in their rings measured in the world they are actually in. `resizeWorld()` and `applyWorldSize()` are pinned **byte-identical including comments**: this phase gave `resizeWorld` a caller, not an edit.

**(7) THE STARFIELD WAS NOT TOUCHED, AND `WORLD_SIZE_EARLY` IS DELIBERATELY ABSENT FROM `WORLD_SIZE_MAX`.** `WORLD_SIZE_MAX` still reads `Math.max(WORLD_SIZE_FIELD, WORLD_SIZE_ORBIT)`, unedited — 2.25 is below both operands so adding it could not change the answer, and the "generate once at the biggest size, filter per world" contract treats a *smaller* world as the easy case. §G pins the declaration's text, `STAR_COUNT`/`STAR_DENSITY`/`stars.length` against the parent, and the property that actually matters: the small world's active stars are a strict **subset** of the field world's, not a fresh roll — which is what makes the transition look stable rather than like the sky changing.

**(8) THE SUITE COST: 28 FILES REPOINTED, AND FOUR CLASSES OF THEM — ONE OF WHICH WAS A REAL FLAKE THIS PHASE CREATED AND FIXED.** Measured the CS026 P2 way, not guessed: the suite was run whole against a `git worktree` at this phase's parent first (**102 files, fully green, zero skips**), then against the working tree, so every failure was provably caused by this phase. **(a) The registry count**, 78 → 79, in twenty-one places across sixteen files — the established repoint-and-annotate idiom. **(b) Four "no later phase added a row" pins** (`test-cs018-p6.js` §B's GLOBAL membership, `test-cs024-p6b.js` §G's collapsed order string, `test-cs024-p6c.js` §G's non-lever count 24 → 25, and the `LATER`/`LATER_ROWS` allow-lists in `test-cs025-p1/p2/p5`) — all widened by **naming** CS026 P3, never by wildcarding. **⛔ `test-cs026-p2.js` §J got the narrowing it had itself applied to three predecessors one phase earlier**: its "exactly THREE rows were added" was a claim about the working tree, and is now a claim about P2. **(c) Two expected mirror-image repoints**, `test-v31-world.js` §A/§B and `test-cs024-p1.js` §D, both inverted at the same strength — and the surviving half of CS024 P1's claim is now stated separately and explicitly, because **a level-number band is not an archetype revival**: levels 3, 6, 9 and 12 no longer differ from their neighbours by *type*, and "no level runs at `WORLD_SIZE_ORBIT`" is asserted level by level exactly as before. **(d) Nine world-geometry files**, below.

**⛔ (8a) THE NINE GEOMETRY FILES SPLIT INTO TWO TREATMENTS, AND CHOOSING BETWEEN THEM PER FILE IS THE WHOLE JUDGEMENT.** Every one of them was staging boards off the **module-load `WORLD_W`/`WORLD_H` snapshot** — 2560×1440, the world the game boots in — which stopped being the world a fresh run is in. **Where the file's subject IS the geometry, it was repointed to the LIVE period** (`worldDims(game.worldSize)`, the CS022 P1 idiom): `test-cs015-p3.js` (the wrap antipode and the world centre), `test-cs025-p2.js` §D/§J (the seam the magnet push is measured across — `WORLD_W - 20` had been putting the ship 620 px *outside* the world), `test-v31-coalesce.js` §4/§27, `test-v33-p3.js` §2 (whose `captured()` now takes its ship position as a **function of the live period**, because the period is not knowable at the call site — `beginPlaying()` is what sets it), and `test-v34-p5.js` §E. **Where the file's subject is world-INDEPENDENT and its boards are pinned to exact counts, the feature was switched OFF at `build()` with the reason inline** — `test-cs020-p1.js`, `test-cs020-p1b.js` (both **cross-build** comparisons against the pre-fix build at `09d443f`, which predates the world-size seam entirely, and whose `quiet()` parks the dock at the snapshot centre *precisely so both builds agree on the point*), `test-cs024-p3.js` (the garbage density ceiling — `GARBAGE_SOFT_MAX`/`HARD_MAX` are counts that do not scale, and its 300-piece grid folded onto itself and reported 32,802 pair visits instead of the derived C(300,2) = 44,850), and `test-cs023-p2.js` §H's 720-body sandbox alone (not the whole file). **That is what the knob's 0 is for**, and using it beats re-laying boards to test a claim the world size has no bearing on.

**⛔ (8b) `test-f3.js` HAD A GENUINE ~1-IN-20 FLAKE, IT WAS THIS PHASE'S FAULT, AND IT IS THE BEST ARGUMENT FOR THE LIVE-DIMS TREATMENT.** `cx`/`cy` — where `resetShip()` parks the ship — was the snapshot centre `(1280, 720)`, which in a 1920×1080 world is an **arbitrary point 367 px off-centre**. The dock is placed on a random `DOCK_MIN_DIST`..`DOCK_MAX_DIST` ring around the **spawn** point, so roughly one run in twenty put the dock (radius 88) on top of that arbitrary point and §C's canister was swallowed by the dock instead of hooked onto the chain. Measured before and after: **2 failures in 25 runs** on the working tree beforehand, **0 in 60** after parking at the live centre, and **0 in 40** on the parent (so it is new, not inherited). The invariant the file was written against is restored rather than papered over: the spawn point can never be inside the dock, because `DOCK_MIN_DIST` is three times the dock radius. `test-v34-p5.js` §G had a milder version of the same class — a plain-object dock stub with no `draw()`, which never mattered while `drawEntity()` had no *wrapped* image to draw for it, and became a `TypeError` the moment the smaller world gave it one.

**(9) THE SUITE IS GREEN AND SILENT — 103 FILES, ZERO FAILURES, ZERO SKIPS, and the new file is byte-deterministic.** `test-cs026-p3.js` is 563 assertions across eight sections (A)–(H) and produced **identical output across 25 consecutive runs** (`installSeed(20260812)` at the top of the file, before the first build, per CS026 P1 §5.2 — this file samples hundreds of real spawns and drives thousands of real frames). Its no-git path was exercised for real, in a copy of the tree with no `.git`: **2 loud, counted skips, zero failures, exit 0.**

**(10) DOC OBLIGATIONS DEFERRED TO P6.** No design doc was touched (§2 already carries this spec; the test pins that both ways — no `.md` in the diff, and the GDD passage still present). P6 owes the **GDD §2.11.1 world-size rewrite**: the GDD currently describes the world as a single 2560×1440 torus, which is now true only from level 6 on. `DIFFICULTY-LEVERS.md` needs **nothing** — `earlyWorldLevels` is not a lever, and that is the point of §2 above.

**CS026 P2 — `junkSplit` JOINS THE ODOMETER AS A CARRIED LEVER; THE DEBRIS TREE HALVES THROUGH LEVEL 10.** Per `IMPLEMENTATION-PHASES-CS026.md` P2 / `PLANNED-FEATURES-CS026.md` §1, FORK-CS026-A → **(b)** and FORK-CS026-B → **(a)**. `{ id: "junkSplit", floor: 2, ceil: 3, steps: 2 }` joins `LEVERS` immediately after `junkSpeedSmall`, and `"junkSplit"` is appended to `junkCount`'s `carriesTo`, which becomes four entries. **The only `LEVERS` diff is that one new row and that one array element** — pinned per entry against the phase's own parent, so a retuned floor/ceiling/step count anywhere else in the table fails rather than being absorbed by a count. `destroyDebris()`'s hardcoded `for (let i = 0; i < 3; i++)` becomes a loop over `Math.round(lv.junkSplit)`, off the **same** `liveLevers(game.wave)` resolution the child speed already used.

**(1) ⛔ CARRIED, NOT A DRIVER — AND THE RULE IS ENFORCED AT LOAD TIME, NOT BY CONVENTION.** Only a driver may wrap (CS024 P6b); `buildLeverOrder()` throws on a lever that declares `carriesTo` without `everyNLevels`, and `test-cs026-p2.js` §F exercises that throw against a deliberately malformed copy of the shipped table rather than trusting it. A **wrapping** split count would take children *back* at every wrap — the same "an odometer must never revoke capability" objection that keeps `payloadSlots` outside it entirely. Carried means it **plateaus**, and the plateau is the design: `junkCount` wraps every ten levels, so one carry arrives at level 11 and the lever sits at its ceiling forever after. **2 for levels 1–10, 3 from level 11 on, and never 2 again.**

**(2) THE SHAPE, RE-ASSERTED AT EVERY LEVEL 1..40 AND DRAINED OFF A REAL FIELD RATHER THAN COMPUTED.** §B starts a build at each level through the real `startLevel` knob + `startGame()`, then drains the field the real `nextWave()` spawned with the real `destroyDebris()`, counting bodies by tier. Level 1 = 21 bodies (was 39), level 10 = 84 (was 156), level 11 = 39, level 20 = 156 — the four rungs the prompt names, plus the 36 it does not. **The early levels were the SHORTEST in the game by body count and this does not change that**; it halves the whole sawtooth, so a 2-way level 10 (84) is still busier than a 3-way level 1 was (39). The plateau is checked out to level 1000 on the pure odometer, and the 10/11 boundary is pinned exactly on both sides.

**(3) ⛔ `destroyHunter()` KEEPS ITS OWN HARDCODED `i < 3`, AND NOW SAYS WHY AT THE LOOP.** It sits forty lines below the debris one and reads almost identically, which is exactly the hazard. `ACH_LINEAGE_FULL = 13` is `1 + 3 + 9`; a two-way Hunter split makes a lineage **seven** bodies and **Hunter's Bane structurally unreachable** — an unwinnable achievement, not a difficulty tweak. The comment names `ACH_LINEAGE_FULL` and says that a future changeset wanting a Hunter split lever must derive that constant from it **in the same edit**. Pinned three ways: three children at every sampled level, three children *with the `junkSplit` panel knob dragged to 7* (the check that would catch a "unify the two loops" refactor), and a full 13-body lineage driven through the real `destroyHunter` that really sets `hunterLineComplete`. `destroyHunter`'s **executable** source is byte-identical to the parent — only a comment was added, and a non-vacuity check proves the comment is really there.

**(4) `DEBRIS_MASS` — THE COMMENT MOVED, NOT ONE NUMBER (FORK-CS026-B → (a), PAUL'S CALL).** CS023 P2 derived 9/3/1 from mass being *conserved* through the 3-way split; under a 2-way split a large of 9 becomes 12 and then 4, so that derivation is simply false now. **Conservation is recorded as a RETIRED property, with the changeset that retired it, and the ratio is restated as FITTED** — chosen so a small ricochets off a large instead of shoving it. The numbers stayed because **nothing in the game reads a total mass**: `debrisBounce()` reads `DEBRIS_MASS` on exactly one line, and that line takes the mass of the two bodies in contact (§G pins both facts). Retuning to a conserving 4/2/1 would have reopened every satellite-vs-satellite bounce — a CS023 mechanic that came through its own gate clean — and flattened the ricochet ratio from 9:1 to 4:1. `DEBRIS_SCORE` is likewise untouched (FORK-CS026-C → (a)): Paul accepted the leaner curve, and compensating is explicitly not this phase's job.

**(5) THE PANEL AND THE LOG.** Three rows via `leverKnob("junkSplit", "Junk split", "")`, in **JUNK**, immediately after the three `junkSpeed*` triples so the chain reads in table order; min/max/step are **derived** by the helper off the row (span 1 → a 1–4 range at step 1), and the label wears `↳` because the table says it is carried. **Registry 75 → 78** — the first repoint since P6c to move the *lever* half of that count (51 → 54) rather than the non-lever half. `DIFFLOG_FIELDS` gains a `junkSplit` column beside the other JUNK-chain entries, logged **unrounded** like every other lever column (`junkCount` is the one exception, and only because `nextWave()` hands its already-rounded local in verbatim).

**(6) ⛔ THE REAL COST OF THIS PHASE WAS NOT THE LEVER — IT WAS TWENTY-SEVEN SUITE FILES, AND THE PATTERN IS THE ONE CS026 P1 EXISTS TO NAME.** The change is ~15 lines of game code; a green suite took repointing pins in **twenty-six other test files**. Measured, not guessed: the suite was run whole against a `git worktree` at the phase's parent first (**fully green**), then against the working tree, so every failure below is provably caused by this phase and not inherited. Four classes: (a) the **registry count** literal, 75 → 78, in fifteen files — the established repoint-and-annotate idiom, unchanged; (b) the **lever count**, 17 → 18, in five; (c) the **split shape** — `test-f3.js`, `test-v33-p3.js`, `test-cs017-p3.js`, `test-cs023-p2.js`, `test-cs023-p3.js` all pinned a literal `3` — repointed **off the lever** (`Math.round(liveLevers(game.wave).junkSplit)`) rather than onto a new literal, so a future retune moves them with it instead of breaking them; and (d) **eight "byte-identical to the parent/HEAD" pins**, which is the interesting class.

**⛔ (6a) THREE PINS WERE WRITTEN AGAINST `HEAD` AND HAD BEEN PASSING VACUOUSLY EVER SINCE THEIR OWN PHASE COMMITTED.** `test-cs024-p6c.js` §J, `test-cs024-p6d.js` §H and `test-cs024-p6f.js` §L each resolved their reference as literal `"HEAD"`. P6c's own comment argued the case explicitly — *"this phase pins a build it must NOT change, so HEAD is the correct reference and stays correct after the commit lands"* — and that is **true of the phase it was written in and false of every phase after it, in both directions**: once the phase landed, `HEAD` followed whatever committed most recently, so the pin compared the live build **against itself**; and the first phase to legitimately touch the odometer made it fail for a change the section has no opinion about. All three are repointed to **their own commit's literal SHA** (`273dbb2`, `c4f924c`, `82eae5a`), which is §4.1's rule applied to the exact defect it was written for. This is the tenth-through-thirteenth repair of this class and the first where the pin was *silently vacuous* rather than noisily wrong.

**⛔ (6b) THE "LEVERS IS BYTE-IDENTICAL" PINS WERE NARROWED, NOT DELETED — AND THE NARROWING IS THE SAME ONE, FIVE TIMES.** `test-cs024-p6b/p6c/p6f` and `test-cs025-p1/p2/p5` each compared the whole `LEVERS` table (or its whole `leverState` output) to a fixed parent. **A whole-table byte pin says "no phase may ever add a lever", which was never any of their claims** — each claims only that *its own* phase added none and moved none. All six now assert, per lever: every lever the reference shipped is still present, **in the reference's order**, field for field, with the one documented exception (`junkSplit` appended to `junkCount`'s `carriesTo`). An **added** lever passes; a moved, renamed, deleted or reordered one still fails, and so does a changed floor, ceiling or step count. Same shape for `leverState`, walked over the reference's keys. `test-cs023-p3.js` §H got the mirror treatment: it compared the live child count to a pre-CS023 build's, and now asserts each side against **its own source** — the lever live, the hardcoded 3 in the reference — so the one legitimate difference is *stated* rather than absorbed.

**⛔ (6c) `test-cs026-p1.js`'s OWN TRAPs 3 AND 4 WERE THE SAME DEFECT, IN THE FILE THAT INTRODUCED THE FIX.** They claimed "P1 changed no game code" and "P1 added no knob" — both **measured on the live build**, which turns a claim about one phase into a claim about every phase after it. Both now resolve **P1's own commit** via `ownCommit(PARENT_SHA, "cs-26 p1:")` and build from it, which is exactly the decomposition §4.1 prescribes, using the helper P1 itself shipped. Before P1 commits, `own` is null and both fall back to the live build — which *is* P1's build then. Two smaller repairs came out of the same sweep: `test-cs024-p6b.js` §G **crashed** (`TypeError`, not a failure) on a lever that post-dates its reference, now guarded; and the standing comment-strip idiom (`replace(/\s\/\/.*$/)` **then** `startsWith("//")`) turns a whole-line comment into a *whitespace-only line* rather than removing it, so an "executable source is identical" pin has to drop blank lines too or an added comment still reads as an added line.

**(7) THE SUITE IS GREEN AND SILENT — 27 files touched, zero failures, ZERO SKIPS, and the new file is byte-deterministic.** `test-cs026-p2.js` is 587 assertions and produced **identical output across 25 consecutive runs**; §K's long real run was measured flaking **2 in 12** unseeded (it reached level 10 rather than crossing the plateau, because the ship's fate varies), so it uses P1's `installSeed(20260811)` **at the top of the file, before the first build** — the game's own module-load randomness is spent inside the factory, so a seed installed after it fixes nothing. Its no-git path was exercised for real, in a copy of the tree with no `.git`: **2 loud, counted skips, zero failures, exit 0.** ⛔ **AND ITS TRAP-2 SCOPE PIN SKIPS WHEN `ownCommits` RETURNS `null`, WHICH `test-cs026-p1.js`'s TWIN DOES NOT** — there, a missing-history run leaves the scope pin neither checked nor counted, which is the silent-vacuum FORK-CS026-H exists to abolish. Left alone deliberately (one phase, one scope); **P6 should fold it into the same sweep as the ten shallow-clone files.**

**(8) DOC OBLIGATIONS DEFERRED TO P6 — RECORDED HERE SO THE SWEEP DOES NOT REDISCOVER THEM.** No design doc was touched this phase (§1 already carries the spec, and the test pins that fact both ways — no `.md` in the diff, and the target passages still present). P6 owes: **(a)** a `DIFFICULTY-LEVERS.md` **§3 row** for `junkSplit` (there is none — the test asserts its absence, so P6 adding one will trip that assertion and should repoint it); **(b)** the GDD's **four present-tense "3-way split" claims** — `ORBITAL-OVERHAUL-GDD.md` lines **55** (§2.4 debris splits, plus the "1 large → 3 mediums → 9 smalls = 13 kills → 52 canisters" arithmetic in line **56** that follows from it), **67** and **78** (Hunter — **these two are still CORRECT and must NOT be changed**), **118** (wave-clear "a large → 3 mediums, +2") and **204** (the sweep's "normal 3-way split" — Hunter, also still correct); and **(c)** GDD §3's Flow-functions row (line **547**), whose right-hand column says `destroyDebris` *"pushes 3 children if `size > 1`"* — that one is now wrong, while the `destroyHunter` *"pushes 3 homing children"* beside it is right. **The debris/Hunter distinction is the whole job here: half these passages are still true.**

**CS026 P1 — TWO SCRATCHPAD UTILITIES AND FIVE PINNED TEST PATHS. NO GAME CODE CHANGED.** Per `IMPLEMENTATION-PHASES-CS026.md` P1 / `PLANNED-FEATURES-CS026.md` §4 and §5. `asteroids-deluxe.html` is byte-identical to this phase's parent (`0927743`), and it is pinned **by diff rather than by eye, twice over** — absent from the phase's own file list, and its `<script>` block compared byte-for-byte against the parent's (`test-cs026-p1.js` §H). Two new files, both Node CommonJS: `scratchpad/_phase-ref.js` and `scratchpad/_seeded-random.js`. **This is not a no-modules violation** — that non-negotiable binds `asteroids-deluxe.html`, which never loads either; `scratchpad/` has been CommonJS with `require` since the first headless test.

**(1) THE PHASE-REFERENCE HELPER, AND ITS DECOMPOSITION IS THE OPPOSITE OF THE OBVIOUS ONE.** Ten repairs across four changesets were pins written against a moving reference. The plumbing that gets it right was already inlined in `test-cs025-p1/p2`; it now lives in one place, lifted verbatim rather than re-derived (`test-cs025-p2.js` :174, :1185, :1191). **⛔ NOT `parentOf(subjectPrefix)`.** The **parent is a hardcoded literal SHA** — a fixed reference, and knowable at write time because it is simply `HEAD` before the phase commits — and it is the phase's **own commit** that is resolved dynamically, by subject, inside the bounded `PARENT_SHA..HEAD` range. Exports: `parentSource(sha)`, `ownCommit(parentSha, subjectPrefix)`, `changedFiles(fromSha, toSha)`, plus two additions each earning their place. **`ownCommits` (plural) is the raw list**, because the count carries three meanings the singular cannot: `1` is the normal reading, `0` is a pre-commit run, and `>1` is AMBIGUOUS and must stay a **failure**, not a skip — folding it into `ownCommit` would hide it behind a `null` that also means "not committed yet", which is the opposite situation. **`SKIP_TAG` is one exported literal** so every skipping file prints the same string and the closing phase has exactly one thing to grep for. `changedFiles(from, **null**)` means **the working tree**, untracked files included — the pre-commit fallback that same site already carried, folded in under a natural signature. **THE RETURN CONTRACT IS THE PART TO PRESERVE: `null` always means "could not ask"; an empty array is a real answer meaning "nothing matched".**

**(2) ⛔ FORK-CS026-H RESOLVED AS (c) — SKIP, LOUDLY; THE CLOSING PHASE ASSERTS ZERO SKIPS. THE INCONSISTENCY WAS REPRODUCED FIRST, ON A REAL `git clone --depth 1`, BEFORE ANYTHING WAS CHANGED.** Confirmed exactly as `PLANNED-FEATURES-CS026.md` §4.2 describes: `test-cs025-p1.js` and `-p2.js` skipped their git-dependent pins **silently** and passed (279 / 454 assertions), while `test-cs025-p5.js` §G **hard-failed** — `FAIL: G: the parent commit (cs-25 p4) resolved`, 88 passed / 1 failed. All three are now uniform: the pin **skips**, but prints `SKIPPED (no git history)` and counts the skip in its own summary line (`N passed, N failed, N skipped`), so a vacuous run is **visible** instead of silent. Re-verified on the same shallow clone afterwards: **4 / 4 / 1 skips, zero failures, all three exit 0**. Assertion counts on a full checkout are unchanged (373 / 554) except `test-cs025-p5.js` 94 → 93, which is the `assert(ps, …)` line **becoming** the skip. **⛔ AND `test-cs025-p5.js`'s PARENT IS NOW A LITERAL SHA (`fa9a543`, cs-25 p4).** It was the one file resolving its parent by **subject search** across all of history — precisely the shape §4.1 corrects — so it was repointed to the literal while the rest of the file was being touched. Same commit on a full checkout; the difference is that a literal cannot drift onto a future commit that happens to share a subject.

**(3) ⛔ THE SEED HAD TO BE INSTALLED AROUND THE FACTORY INVOCATION, AND THAT IS THE WHOLE POINT OF THE PHASE.** `test-starfield.js` and `test-p5.js` contain **zero** `Math.random` calls of their own — their nondeterminism is the **game's**, and `stars`/`starsNear` are built at **module load**, inside the factory (`starsNear.push({ x: Math.random() * STAR_NEAR_TILE_W, … })`, `asteroids-deluxe.html:8896`). A star's position is fixed the instant `new Function(...)(...)` is invoked, so a seed installed after it fixes nothing. `_seeded-random.js` ships `mulberry32` (pure, seeded, stable — int32 ops only) plus **two** entry points. `withSeed(seed, fn)` is the scoped form §5.3 names. **`installSeed(seed)` is the unscoped sibling, and all five pinned files use it, for a measured reason rather than a theoretical one:** in every one of the five the test *drives the game after the build*, so randomness lands on both sides of the factory. `test-cs018-p4.js` §I asserts twice per surviving Hunter after a 30-level driven run — its count is decided inside `update()`, long after the build. And `test-starfield.js` was **tried** with the seed scoped to the factory alone: §D settled, but §E still failed once in 200 runs (the ship met something that had spawned at random). Unscoped: 0 in 200. **Both entry points restore whatever `Math.random` they found, never a captured "real" one, so the existing `withRandom`/`withPinnedRandom` sites in `test-cs017-p1/p3` and `test-cs018-p4` nest inside the seeded stream unchanged.**

**(4) THE FIVE PATHS, MEASURED BEFORE AND AFTER RATHER THAN ASSERTED.** Before (on this phase's parent): `test-cs017-p3.js` 50/50 passing but the count split **1570 ×42 / 1569 ×8**; `test-cs018-p4.js` 50/50 passing, **541 ×23 / 535 ×27**; `test-starfield.js` **49/50**, the failure being §D's `a fractional camera shift moves the near layer by STAR_PARALLAX_FACTOR x the shift` (a second probe hit it at run 63 of 200); `test-cs017-p1.js` **399/400**, failing at run 101 with `F: level 10: forcing the spawn timer produced exactly one saucer`; `test-p5.js` **400/400 at a constant 102** — see `## Known issues`, its listed §C flake did not reproduce and appears already defused. After: **every one of the five is byte-identical across repeated runs** — `test-cs017-p3` 1570 ×100, `test-cs018-p4` 541 ×100, `test-starfield` 23 ×400, `test-p5` 102 ×400, `test-cs017-p1` 461 ×400, zero failures anywhere.

**(5) ⛔ SEEDING IS NOT THE SAME AS FIXING, AND `test-starfield.js` §D IS WHERE THE DIFFERENCE HAS TEETH — SO THE SEED CHOICE IS NOW AN ASSERTION, NOT A COINCIDENCE.** `expectedNearScreenX()` returns `null` when no tile offset puts `starsNear[0]` on screen and the assertion then fails on a `null` comparison, so a seed that merely lands the star somewhere convenient would make the test pass **without the assertion becoming meaningful**. The arithmetic was worked out rather than assumed: `STAR_NEAR_TILE_W === VIEW_W === 1280` and `STAR_PARALLAX_FACTOR === 0.5`, so at `camA = 1000` the offset is 500 and at `camA + 37` it is 518.5; the two lookups pick **different tiles** — failing by a whole tile width — for exactly `starsNear[0].x ∈ [496, 514.5)`, which is 18.5 of 1280, **~1 in 69** (observed 1 in 63 and 1 in 50). Under `SEED = 1` the star sits at **x = 796.60**, 282 px clear of that band and rendering at screen x = 296.60 — genuinely on screen and genuinely in the same tile at both camera positions. **Two new assertions in §D fail loudly if a later reseed moves it**, and `test-cs026-p1.js` §E re-derives the same claim from the live build so the two cannot drift apart silently. `test-cs017-p1.js` §F got the same treatment in comment form: its failure mechanism was **already on record** (`test-cs017-p3.js`:576 characterised the identical class) — §F pins `Math.random` to 0, fixing the spawned saucer's entry position to one point, and CS023 P3's UFO↔debris pass destroys it inside the same `update(0)` if a randomly-scattered satellite happens to sit there. `test-cs017-p3` could clear debris to defuse it; §F cannot, because it is asserting on the junk count `nextWave()` just spawned — so the **scatter** is what got pinned instead.

**(6) ⛔ A TRAP THE PROMPT DID NOT FLAG, FOUND BY THIS PHASE'S OWN TEST: TEN MORE SUITE FILES HARD-FAIL ON A SHALLOW CLONE, AND THE CLOSING PHASE NEEDS THE LIST.** `test-cs026-p1.js` §F runs each pinned file as a **subprocess**, which is how `test-cs017-p3.js` was caught crashing outright on `git clone --depth 1` (`fatal: invalid object name '683de82'` — its §F builds a pre-P3 reference commit). That file was already being edited by this phase, so its §F was routed through the same skip convention; **the other ten were deliberately left alone — one phase, one scope** — and are inventoried in `## Known issues` for P6, which owes FORK-H's zero-skips assertion and therefore needs the real list rather than the three-file one.

**CS026 P0 — DOCS AND TEST-COMMENT REPOINTING ONLY, NO GAME CODE CHANGED.** Per `IMPLEMENTATION-PHASES-CS026.md` P0 / `PLANNED-FEATURES-CS026.md` §0.2. `asteroids-deluxe.html` is byte-identical to this phase's parent (`8fcea68`).

**(1) THE CS025 PLANNING PAIR IS ARCHIVED.** `git mv PLANNED-FEATURES-CS025.md IMPLEMENTATION-PHASES-CS025.md archive/`, the CS023/CS024 `git mv` precedent — not copy-delete. `PLANNED-FEATURES-CS026.md` and `IMPLEMENTATION-PHASES-CS026.md` were already committed at repo root ("Doc changes for CS026") ahead of this session.

**(2) SEVEN REFERENCES REPOINTED IN THREE LIVE TEST FILES — THE LIST WAS GREPPED, NOT TRUSTED, AND ONE OF THE SEVEN TURNED OUT ALREADY CORRECT.** `test-cs025-p1.js` (:6, :29), `test-cs025-p2.js` (:7, :23) and `test-cs025-p3.js` (:7, :384) each had a bare `PLANNED-FEATURES-CS025.md` comment reference; all six now read `archive/PLANNED-FEATURES-CS025.md`, the full-path-with-extension form CS025 P0 established. **`test-cs025-p5.js`'s :502, the seventh named reference, needed no edit** — it already reads `archive/PLANNED-FEATURES-CS025.md` in a comment explaining its own reference-resolution test's scoping (why a bare grep for that exact string also matches historical prose in `STATUS.md`'s own CS025 P0 entry), not a stale link. A full repo-wide re-grep confirmed every remaining hit resolves to either the archived live pair, the deliberately-untouched `archive/…-CS025-old.md` pair (`DIFFICULTY-LEVERS.md:61`, `asteroids-deluxe.html:674`, `test-cs024-p6b.js:6,383` — all three still resolve, none touched), or prose about a path in `STATUS.md`'s/`GDD-VERSION-HISTORY.md`'s own history.

**⛔ (2a) THE ARCHIVE MOVE ITSELF TRIPPED AN EIGHTH, UNLISTED PIN — THE EXACT "MOVING REFERENCE" LESSON THIS CODEBASE KEEPS RE-LEARNING.** `test-cs025-p5.js` §G carried `eq(git diff --name-only 2cd73e8 -- archive/, "")`, a true statement about CS025 P5's own scope (that phase didn't touch `archive/`) and a false one the instant a later changeset legitimately archives its own superseded planning pair — which git-mv-ing the CS025 pair into `archive/` in step (1) above necessarily does. Confirmed pre-existing-shape rather than a new defect: same class as `test-cs024-p6e.js` §G (CS025 P1) and the nine pins CS024 P7 retired at once. **Retired in place with the reason inline**, following that precedent — the TRAP 3 reference-resolution sweep two sections above it (untouched) is what actually still protects the invariant that matters (no live reference points at a path that doesn't exist).

**(3) THE HALF-STRENGTH MEGA DELIVERY IS CANCELLED, NOT DEFERRED.** Paul decided against it outright. The two forward-looking `STATUS.md` sections carrying it as a live mandate — `## Next up` and `## Known issues` — are rewritten to record the decision; the historical mentions in `## Playtest asks` (Gate B Q13, verbatim) and `## Working / verified` (CS024 P7's gate-outcome record) are deliberately left unedited, per the same forward-looking-vs-record distinction CS025 P0 drew for its own `-old` disambiguation.

**(4) CS026'S OWN ENTRY OPENED, NAMING THE PHASE MAP AND ALL EIGHT RESOLVED FORKS.** See `## Next up`. **`STATUS.md`'s size was checked, not assumed, and CS023 material is still present** — the rolling recap still covers CS023 through CS026. The rule prunes when the oldest covered changeset is *more than* ~3 rounds behind the one just shipped; CS023 is exactly **3** rounds behind CS026 (023 → 024 → 025 → 026), which is at the boundary, not past it, so no prune is due this phase. **The next changeset to land should re-check this first** — one more changeset shipping without a prune would put CS023 four rounds behind, which does cross the line.

## Working / verified

**Headless-verified this session (CS026 P6, `scratchpad/test-cs026-p6.js`, new, 207 assertions, 0 failures, 0 skips):** seven sections (A)–(G). **(A)** `node --check`; `GAME_VERSION` is `"1.0.0.26"` in the live build **and** in the source literal, the `Major.Minor.Patch.Changeset` shape holds with `26` as the 4th segment, CS024 P7's ".23 is skipped" tombstone survives, and a fresh `HighScores.add()` stamps the new build. **(B)** the Q5 retune at the registry — both defs, their P4 ranges still containing them, the `applyDebug` round-trip proving overrides-OFF now falls back to **160**, and ⛔ **the knob Paul DECLINED**: `dockOffloadInterval` appears nowhere in `DEBUG` or in the source, `DOCK_OFFLOAD_INTERVAL` is pinned unmoved at 0.05 as a literal, and the registry count is asserted **holding at 85** on all four maps. `DELIVERY_FLOAT_DY` is pinned as a constant that did **not** become a knob. **(C)** the retune **driven** through a real full-cargo dock visit: all 24 towed floaters born at the ship on **both** axes (compared against the ship's position snapshotted at the instant of each push, so the claim survives the chain tugging the ship), the incidental branch sharing that one origin, the cadence measured off real birth frames, and the separation measured through the **real** `FloatText.update()`. **(D)** the Q8 layout: four constants, the tab mark on the selected tab only and following the cursor, a leader run on every visible row on **both** `drawAchRow` branches. **(E)** the layout is **coherent**, not merely changed — row 0 clears the tab baseline by a full text-height, the clip window still contains it, `achMaxScroll()` agrees on both tabs and the Lifetime tab still scrolls, and the **widest shipped name+status pair** still leaves room for a leader. **(F)** ⛔ **no new fill** — a recording proxy counts every method `drawAchRow` calls on both branches; `fill`/`fillRect`/`rect`/`stroke`/`arc`/`createLinearGradient`/`createRadialGradient` appear in none, the leader is a `fillText`, and the GDD still reads "two deliberate exceptions" and nowhere says three. **(G)** the TRAPs, all against **this phase's own parent SHA** via `_phase-ref.js`, never `HEAD`: `LEVERS` byte-identical field-for-field and `leverState` identical at **every level 1..200**; the registry delta measured at **0** by building both sides, with row ids and order compared so a swap cannot net out; the version confirmed to have **moved off** the parent; and the scope pin, which uniquely asserts the design docs **ARE** in the diff.

**⛔ THE MEASURED NUMBER THAT DIFFERS FROM EVERY FIGURE THIS CHANGESET HAS QUOTED, FOUND BY DRIVING IT RATHER THAN COMPUTING IT: the delivery cadence is FRAME-QUANTISED.** `game.offloadTimer` is re-armed to `DOCK_OFFLOAD_INTERVAL` (0.05 s) and decremented by `dt`, so at 60 fps a canister pops every **fourth** frame (0.0667 s), not the third the arithmetic implies — 0.05 is not representable in binary and the third subtraction leaves a hair above zero. So every "separation" figure on record is a *nominal*: P4's 15 px was really ~20 px on screen, and this phase's 8 px is really **~10.7 px**. Both forms are pinned in §C, and the GDD/DIFFICULTY-LEVERS entries say both.

**⛔ THE FULL REGRESSION WAS RUN TWICE CONSECUTIVELY AND THE RUNS DIFFED, PER THE PHASE INSTRUCTION — AND THE FIRST DIFF WAS NOT EMPTY, WHICH IS EXACTLY WHAT CS026 P1's SEEDING WAS BOUGHT FOR.** Final state: **106 test files, 106 exiting 0, 47,739 assertions passed, 0 failed, 0 skipped** (FORK-CS026-H's closing-phase zero-skip assertion satisfied), and the two runs are **byte-identical** once three `update(dt) ms` lines are excluded — wall-clock timings in `test-cs023-p2.js` that are explicitly `[REPORTED, never gated]` and cannot be seeded, being measurements of the machine rather than of the game.

**⛔ WHAT THE DIFF CAUGHT, AND IT WAS REAL. FIVE MORE UNSEEDED FILES — P1 PINNED FIVE PATHS, AND NOBODY HAD EVER DIFFED TWO FULL-SUITE RUNS TO FIND THE REST.** `test-cs024-p6f.js`, `test-v31-world.js`, `test-v33-p2.js`, `test-v33-p3.js` and `test-p6.js`, all now seeded at the top of the file before the first build, each carrying a comment saying what was checked about the seeded values. **Two of the five were genuinely FLAKY, not merely noisy:** `test-cs024-p6f.js` §F ("five nodes were hooked") failed ~1 run in 10, and `test-p6.js` §C/§F ("a 13th canister is refused" / "exactly 24 canisters delivered") failed ~1 run in 10 — in both cases because randomly-placed canisters could fall outside the capture region on the single frame the count is taken. Both are now stable across repeated runs, verified non-vacuous rather than merely quiet.

**⛔ AND IT CAUGHT A REAL BUG IN THIS PHASE'S OWN NEW CODE, ON A PATH THE NEW TEST'S OWN DEGENERATE-WIDTH SWEEP MISSED.** `achLeader()` was written to survive a `measureText()` returning `{width: 0}`, NaN, negative, enormous, or an object with no `.width` — but not the case where **`measureText()` returns `undefined` outright**. The standing headless stub is a Proxy that answers every unmodelled method with `() => {}`, so any test file that never modelled `measureText` lands exactly there: `test-f9.js` does, and it crashed the real `drawAchievements()` with *"Cannot read properties of undefined"*. `achTextW()` now answers `NaN` instead of dereferencing, which trips `achLeader()`'s negated guards and costs the row its leader rather than taking the menu renderer down. The case is added to §D's sweep.

**Nine existing test files were repointed by this phase's own changes, all of them legitimately** — and each repoint is annotated in place with why. `test-cs013-p3.js` (233 assertions lost to one hardcoded `py + 130`; `ry0` now imports `ACH_ROW0_Y`), `test-cs015-p2.js` (⛔ **the dedicated regression for the very CS015 P2 three-line split that Q8.4 reversed** — updated to the new shape, with the supersession and the reason recorded rather than the file deleted), `test-cs016-p5.js` (tab-label matching, and a row is four `fillText`s now, not three), `test-cs020-p1.js` (§J's floater byte-identity **narrowed to text-and-order**, because position was only ever a proxy for CS020's claim and is now a proxy that must fail — with a non-vacuity assertion that the positions really did move), `test-cs026-p4.js` (its own first-guess defs and both headline numbers, repointed by the gate they were shipped to feed), and `test-cs025-p5.js` + `test-cs026-p2/p3/p5.js` (version pins). ⛔ **`test-cs026-p2.js`'s TRAP 2 inverted rather than being deleted:** it pinned the *absence* of the `DIFFICULTY-LEVERS.md` `junkSplit` row to prove P2's deferral to the closing sweep was real; P6 has now written it, so the absence pin becomes a presence pin and the deferral is both honoured and discharged. Its sibling GDD pin was **narrowed, not flipped** — "3-way split" still appears in the GDD and must, because three of those four passages are HUNTER claims that are correct as written.

**Headless-verified this session (CS026 P3, `scratchpad/test-cs026-p3.js`, new, 563 assertions, 0 failures, 0 skips, and byte-identical output across 25 consecutive runs):** eight sections (A)–(H). **(A)** `node --check`; `WORLD_SIZE_EARLY` is 2.25 and `worldDims` gives exactly 1920×1080, **derived** as `VIEW × sqrt(size)` rather than compared to a literal; the 25%/44% visibility and 1,469/1,101 px reach figures the change was argued from, recomputed; the **rendering floor re-derived** from `CULL_MARGIN` + `DEBRIS_RADII[3]` (786 × 506 required, 960 × 540 available) with 1600×900 and 1280×720 asserted to **fail** it, plus size 1's second failure mode (an 80 px folded spawn, inside both `SPAWN_MIN_DIST` and `DOCK_RADIUS`); the two-band rule at every level 1..12 and at 15/21/42/63/99/100/1000/2001; the boundary pinned on **both** sides; and the deliberate non-sawtooth (`worldSizeFor(1) !== worldSizeFor(11) === worldSizeFor(21)`). **(B)** the size and the exact live dimensions at every level 1..12, reached **twice by different real paths** — one fresh build per level through the `startLevel` knob, and one build climbing 1 → 12 through the real `nextWave()` — with the whole climb pinned as a single string and exactly **one** world change counted across twelve levels. **(C)** the flat rings: the fold swept over the whole circle (640 → 440, 620 → 460), both clear of their own minimums and of `DOCK_RADIUS`; the `min(W,H)/2 - 60` = 480 figure recorded as **`resizeWorld`'s `dmax`, not a bound on a spawn**, with both maxima noted as exceeding it; then **30 real level-1 fields** (every satellite in its ring wrap-aware, none touching the ship) and **200 real `Dock` placements** (in ring, inside the world). **(D)** the knob's registry row field by field, its GLOBAL placement between `debrisBounceRestitution` and `startLevel`, 79 rows in the registry / `DEBUG` / `debugShown`, **no `Floor`/`Ceil`/`Steps` siblings and no `LEVERS` entry**, the `debugOverride` OFF/ON round-trip, live re-reading at the wave boundary in **both** directions mid-run, and the **0 = off** A/B driven as a real twelve-level climb that never leaves 2560×1440. **(E)** the level 5 → 6 transition across a populated field (ship parked near a corner so the carried bodies genuinely straddle seams; Hunters, garbage, powerups, particles, floaters, six seam-huggers, a ten-node tow with real verlet velocity): nothing outside the new period — bodies, chain nodes, ship, dock — the ship re-centred and camera synced, `min(old, dmax)` and unmoved bearings **body by body** with controls on both branches, nothing touching the ship, link spacing and per-node verlet velocity preserved to 1e-9, and 240 real frames afterwards with everything finite. **(F)** `startGame()`'s executable source byte-identical to the parent and free of `WORLD_SIZE_EARLY`; the resize preceding `new Dock()` in `nextWave()`; and a fresh run really landing in 1920×1080 with the ship centred, every satellite in its ring and the dock in its own, all measured after the resize. **(G)** TRAPs 1–5 — version, no design doc, `LEVERS`/`leverState` byte-identical to the parent at every level 1..200 key for key, the starfield (including the `WORLD_SIZE_MAX` declaration's own text and the active-stars-are-a-subset property), and `resizeWorld`/`applyWorldSize` byte-identical **including comments** with `worldSizeFor` asserted to have changed as the non-vacuity control — plus the scope pin against this phase's own commit. **(H)** an `AudioSys.ctx === null` smoke run of 4,000 real frames from level 3 that crosses the boundary in a live sim, visits **both** sizes and no third, and leaves every surviving body inside the world it ended in.

**Headless-verified this session (CS026 P2, `scratchpad/test-cs026-p2.js`, new, 587 assertions, 0 failures, 0 skips, and byte-identical output across 25 consecutive runs):** eleven sections (A)–(K). **(A)** `node --check`; `junkSplit`'s table entry is exactly `{ id, floor: 2, ceil: 3, steps: 2 }` and nothing more, declares **no** `everyNLevels` and **no** `carriesTo`, and sits immediately after `junkSpeedSmall`; `junkCount.carriesTo` is its three speeds plus `junkSplit`, appended last. **(B)** the table at **every level 1..40** — `junkCount`, `junkSplit` and the body count **drained off the real field** with the real `destroyDebris`, tier by tier — plus the four named rungs asserted by name, the plateau checked to level 1000, and the 10/11 boundary pinned exactly. **(C)** one large → N mediums → N² smalls at levels 1/5/10/11/25, and a small still terminates. **(D)** the consumer follows a **panel override** (so it reads `liveLevers`, not `leverState`), lands on the **next kill** rather than the next level, and **rounds**: at `steps: 3` the middle position is 2.5 and spawns **three** children, which `Math.floor` would have shaved to two. **(E)** `destroyHunter` splits three ways at every sampled level *and with the `junkSplit` knob dragged to 7*, a full lineage is 13 bodies and really sets `hunterLineComplete`, and its executable source is byte-identical to the parent. **(F)** the load-time drivers-only guard throws on a carried lever given a `carriesTo`, naming it; the duplicate-id and unknown-target throws still fire; the shipped table still passes. **(G)** `DEBRIS_MASS` is 9/3/1 unmoved, the comment says *fitted* and records conservation as retired, and `debrisBounce` reads the table on exactly one line — the two bodies in contact. **(H)** registry 78, the triple adjacent and in JUNK after `junkSpeedSmall`, ranges **derived**, label wearing `↳` and no `(inv)`. **(I)** `DIFFLOG_FIELDS` carries `junkSplit` beside the JUNK chain, every lever has a column, and the CSV cell carries the value at levels 1 and 11. **(J)** TRAPs 1–5 against the phase's own parent SHA. **(K)** a seeded 4000-frame `AudioSys.ctx === null` run across the plateau. **Whole-suite regression: the suite was run against a `git worktree` at this phase's parent first (100 files, fully green), then against the working tree — so all 27 touched files' failures are provably this phase's. Final state: every file passing, ZERO skips.**

**Headless-verified this session (CS026 P1, `scratchpad/test-cs026-p1.js`, new, 131 assertions, 0 failures, 0 skips):** eight sections (A)–(H). **(A)** both new files pass `node --check`, export their named contract, and are checked to be **CommonJS with no `import`/`export` keyword** — a later ES-module keyword would make them unloadable by every existing test file. **(B)** `_phase-ref.js` against real history, including the **bounded-range** claim stated positively and negatively: CS026 P0's own subject resolves to exactly one commit from *its* parent and to **nothing** from a later one, which is what makes `PARENT_SHA..HEAD` meaningful rather than decorative. **(C) ⛔ THE NO-GIT PATH IS EXERCISED FOR REAL, NOT SIMULATED** — the helper is copied to a temp directory outside any checkout and run in a **child process**, because that is the only way to measure the second half of the claim: **stderr must be empty**. `execFileSync` *inherits* stderr by default, so git's own `fatal: bad revision …` used to land in the run's output before the throw was even caught (visible in the shallow-clone reproduction above); `stdio: ["ignore", "pipe", "pipe"]` in the helper's `git()` wrapper is what stops it, and it is load-bearing rather than tidiness. **(D)** the PRNG's purity (two generators sharing a seed do not share state, proven by interleaving), restore-on-throw, nesting, and **pinned first-three vectors for `mulberry32(1)`** so swapping the algorithm fails loudly instead of silently re-seeding five test files. **(E)** §5.2's correction stated as a test against the **real** build: the same seed across the factory gives the same starfield, a different seed a different one, **two unseeded builds differ from each other**, and a seed applied *after* the factory cannot move an already-placed star. **(F)** each of the five requires the helper and installs the seed **positionally before** the factory, and the determinism is measured as a **repeated run** (4 rounds by default, comparing entire stdout byte-for-byte — which pins the assertion *count* and the pass/fail *outcome* in one comparison; `CS026_P1_ROUNDS=40` deepens it). **(G)** the loud-skip convention across all three CS025 files, including explicit guards that neither the silent form nor the hard-fail form comes back. **(H)** TRAPs 1–5. **Full regression run twice consecutively: RUN A 101 files / 0 failures, RUN B 101 files / 0 failures — and every file's summary line is IDENTICAL between the two runs, which is the claim the two count-wobble entries in `## Known issues` used to make false.** The two runs' full stdout still differs in 28 lines, all of them in files this phase did not touch and none of them a count or an outcome: performance timing reports (`update(dt) ms — median …`, explicitly `[REPORTED, never gated]`) and sampled-value readouts in `test-cs024-*` (`saw [224.1, 638.6]` vs `[225.7, 639.0]`). Recorded because it is the honest shape of the result — five files are pinned, not the suite.

**Headless-verified this session (CS026 P0, no new test file — a docs/repoint-only phase ships no new behaviour to pin):** the existing suite is the verification, per the phase's own "run the full suite once and report" instruction. **Full regression run twice consecutively: RUN A 100 files / 0 failures, RUN B 100 files / 0 failures, 23,478 total assertions passed.** One pin needed repair along the way and is recorded above under (2a): `test-cs025-p5.js` §G's `archive/` untouched-since-CS025-P0 check, a true claim about that phase's own scope that this phase's legitimate archiving necessarily falsifies — retired in place with the reason inline, the same shape as every other moving-reference repair in this file's history. `asteroids-deluxe.html` confirmed byte-identical to the parent commit (`8fcea68`) via `git diff --stat`.

## Known issues

- **CS026 P3 — THE SHALLOW-CLONE LIST IS UNCHANGED AT TEN FILES, WHICH IS WORTH SAYING BECAUSE THIS PHASE TOUCHED SIX OF THEM.** `test-cs020-p1.js`, `test-cs020-p1b.js`, `test-cs023-p2.js`, `test-cs023-p3.js`, `test-cs024-p1.js` and `test-cs024-p4.js` were all edited this phase, and **none of the edits added or removed a git dependency** — the repoints were registry counts, allow-lists and world-geometry staging. `test-cs026-p3.js` itself follows the `test-cs026-p2.js` §J shape and skips **loudly and counted** on every git-dependent pin, verified on a real no-`.git` copy of the tree (2 skips, 0 failures, exit 0). P6 still owes FORK-H's zero-skips assertion against the ten-file reality recorded in the CS026 P1 entry below, not against a shorter list.

- **⛔ CS026 P2 — `test-cs026-p1.js`'s TRAP-2 SCOPE PIN DOES NOT SKIP WHEN GIT HISTORY IS ABSENT, IT SIMPLY DOES NOT RUN.** Its `if (!changed) { if (shas !== null && shas.length <= 1) skip(...) }` guard means a `shas === null` reading (no checkout, shallow clone) leaves that pin **neither checked nor counted** — the silent-vacuum FORK-CS026-H exists to abolish, in the file that introduced the fix. `test-cs026-p2.js` §J carries the corrected shape (skip unless the *ambiguous* case already reported a failure) and is the model. **Left alone deliberately — one phase, one scope — and it is small: one guard.** P6 should fold it into the same sweep as the ten shallow-clone files, since both feed the zero-skips assertion.

- **⛔ CS026 P1 — TEN SUITE FILES HARD-FAIL ON A SHALLOW CLONE, AND P6 OWES FORK-H'S ZERO-SKIPS ASSERTION AGAINST THAT REALITY, NOT AGAINST THE THREE-FILE ONE.** Measured, not estimated: a `git clone --depth 1` of this repo runs **101 files, 91 passing, 10 failing** — `test-cs017-p6.js`, `test-cs019-p1.js`, `test-cs020-p1.js`, `test-cs020-p1b.js`, `test-cs023-p2.js`, `test-cs023-p3.js`, `test-cs024-p1.js`, `test-cs024-p2.js`, `test-cs024-p4.js`, `test-cs024-p6b.js`. Each reaches for a reference or parent commit and **throws** rather than skipping, which is the behaviour FORK-CS026-H's option (b) explicitly rejected. FORK-H was scoped to three files and those three are done; `test-cs017-p3.js` made four, because CS026 P1's own §F runs each pinned file as a subprocess and caught it crashing (`fatal: invalid object name '683de82'`) and the file was already being edited. **The remaining ten were deliberately not touched — one phase, one scope.** ⛔ **The closing phase's "zero skips" assertion is only meaningful once these can skip**, so P6 has a choice to make and should make it explicitly: convert the ten (mechanical, ~10 small edits, all the same shape as the four already done), or state that the zero-skips assertion covers only files that *can* skip and that a shallow clone still cannot run the suite. Do not let it be decided by accident.

- **CS026 P1 — `test-p5.js` §C's ~1-IN-30 FLAKE DID NOT REPRODUCE IN 400 RUNS ON THE PARENT BUILD, AND THE REASON LOOKS LIKE "ALREADY FIXED".** `PLANNED-FEATURES-CS026.md` §5.1 lists it among the five, but 400 unseeded runs on this phase's parent were **400/400 passing at a constant 102 assertions**. The file's own `isolate()` carries a `FLAKE FIX` comment (added by CS024 P3's regression sweep) that nulls `game.dock`, because `nextWave()` places the dock at a random offset and `isolate()` teleports the ship to a fixed point — on the unlucky rolls the ship landed inside the dock, the offload pass emptied the chain, and the "one hooked canister spends exactly one piece" assertion failed. That is the same mechanism §5.1 describes. **The file was seeded anyway** (it was on the list, and the seed costs nothing), and **`game.dock = null` was deliberately left in place** — the seed pins the roll, the null keeps the test honest if the seed is ever changed. Recorded so a later session does not go hunting for a flake that is already dead.

- ✅ **RESOLVED BY CS026 P1 —** **CS025 P5 — TWO FILES HAVE NON-DETERMINISTIC ASSERTION *COUNTS* (`test-cs018-p4.js` 535 or 541; `test-cs017-p3.js` 1569 or 1570), BOTH PRE-EXISTING, AND BOTH ALWAYS PASS.** Found by `diff`-ing the two consecutive full-regression runs this phase, which is exactly what that diff is for. Each reports `failed: 0` either way — it is the **count** that varies, never the outcome. **Both confirmed pre-existing on a clean `git stash` at this phase's parent**, which is worth recording because the two needed different amounts of evidence: `test-cs018-p4` showed both values within four runs there, but `test-cs017-p3` came up 1570 six times in a row and only revealed 1569 at **3 in 20 runs** — long enough to look like this phase had caused it. It had not; the working tree shows the same split (5 in 14). Cause in both: unpinned `Math.random()` (3 and 5 call sites) on a path where a random-length branch runs a variable number of assertions. **This is a cosmetic determinism gap, not a flake** — unlike `test-starfield.js` §D below, nothing ever fails. Whoever next touches either file should seed or pin the random path, the same fix the CS023 P4 entry below prescribes for the two genuine `Math.random()` flakes. Recorded rather than fixed because it is outside this phase's named scope and carries no risk.

## Balance notes

- **CS026 P5 — FOUR FIRST-GUESS BANNER NUMBERS, NOW KNOBBED, HAVE NEVER BEEN SEEN IN PLAY.** `levelBannerTime` (2.2 s), `levelBannerFade` (0.5 s), `levelBannerSize` (72 px) and `levelBannerY` (24 px) are all CS025 P5's untested guesses, tuned by eye against nothing. Does "Level N" at that size, held that long, with that fade, read as *briefly, large, easy to read, fading* — or too big, too long, too easy to miss in a busy frame? It sits at screen centre, where the ship usually is — does it ever obscure something that matters at the instant a level starts? All four are sliders now, in the GLOBAL registry section right after `Start level`. **Report the numbers you land on.**

- **CS026 P4 — TWO FIRST-GUESS DELIVERY-FLOATER NUMBERS, AND ONE READOUT REMOVED WITH A NAMED RISK.** `deliveryFloatRise` (300 px/s) and `deliveryFloatLife` (0.55 s) are both untested guesses picked from the arithmetic alone — at the shipped `DOCK_OFFLOAD_INTERVAL` (0.05 s), consecutive floaters separate by 15 px and each travels 165 px before fading. Watch a full 24-canister haul, ideally with a Magnet running (that's what puts incidentals back-to-back too): does it read as a rising column, or does it still smear at the top of a long haul? Both knobs live in the DELIVERY registry section, right after `dockComboGrace`. **Separately, and not fixable by these two knobs:** the HUD `COMBO n/24` row is gone, and it was the only thing showing the delivery **denominator** — if the cargo ceiling starts feeling arbitrary without a number attached to it, that is the first thing to reconsider, not the floater tuning.

- **CS026 P3 — THE EARLY WORLD IS 44% VISIBLE INSTEAD OF 25%, AND NOT ONE COMBAT NUMBER MOVED.** Levels 1–5 run at 1920×1080 rather than 2560×1440. The lever table, the spawn counts, the speeds, the scores and the drop weights are all untouched and pinned so at every level 1..200 — this is a **legibility** change, not a difficulty one. What it actually changes is the tail: the farthest a body can be from the ship drops from **1,469 px to 1,101 px**, and the viewport covers nearly half the board instead of a quarter, so hunting the last two or three smalls is a shorter walk. The band is `DEBUG.earlyWorldLevels`, default 5, and **0 turns it off completely** for a same-session A/B. Two things to watch for at the gate that the numbers cannot answer: whether five levels is the right length for the on-ramp (it is a knob, and 1–3 was the other candidate in §2.3), and whether the **one** world change at 5 → 6 reads as a moment or as a glitch — the field is re-homed around the ship, so a Hunter that was 900 px behind arrives 660 px behind on the far side, which is the `dmax` shell working as designed but is also the most likely thing to feel odd.

## Next up

**✅ CS026 IS COMPLETE AND CLOSED — P0 THROUGH P6 ALL LANDED, GATE APPLIED, `GAME_VERSION` 1.0.0.26.** There is **no live mandate carried forward from CS026**. The gate resolved everything it asked: six questions clean, Q5 applied as two retuned `def`s plus a floater-origin move, Q8 applied as five Achievements look-call constants plus `achLeader()`. **No lever moved, the registry holds at 85 and `LEVERS` at 18.**

**WHAT THE NEXT CHANGESET INHERITS, and none of it is urgent:**

- **⛔ THE ONE STANDING OBLIGATION IS A `STATUS.md` PRUNE.** The rolling window still covers **CS023** (its P4b and P1–P4c closed-out-as-superseded entries), which is exactly 3 rounds behind CS026 — at the threshold, so this phase correctly did not prune. **At CS027's close it will be past it, and CS023's entries must move, section by section, newest-first, each its own paragraph, into `archive/STATUS-HISTORY.md`.** A straight relocation, not a summary.

- **The delivery floater column is TIGHTER than it was, by Paul's own choice, and is the thing to watch next time it is played.** Nominal 8 px separation, ~10.7 px on screen. If it reads as a smear again, **`DOCK_OFFLOAD_INTERVAL` (0.05 → 0.10) is the lever — not `deliveryFloatRise`**, which Q5 explicitly asked to reduce. That option was costed at the gate (a 24-canister visit goes 1.2 s → 2.4 s) and declined; it is not a new design question, just a number Paul has already seen the trade for.

- **`COMBO n/N`'s denominator is still unrepresented.** FORK-CS026-F dropped the HUD row and named this as the accepted risk; gate Q5 was what would have caught it and did not — Paul did not ask for it back. Recorded so a future "the cargo cap is invisible" report is recognised as this, not as a new bug.

- **Out of scope and still recorded (`PLANNED-FEATURES-CS026.md` §11):** the half-strength Mega Delivery is **cancelled, not deferred — dead**; a spatial grid for `coalesceGarbage()` only becomes the answer if a future gate wants the garbage ceiling past ~350 (it did not this time); the three `localStorage` keys have still never been round-tripped in a real browser (one manual set-reload-confirm at a gate, worth doing because the failure mode is silent and total); the satellite sprite redesign is its own changeset with two clarifying questions open; music intensity composition is deferred, unchanged.

- **FLAG-CS026-a is still open and is now cheap to answer:** `DEBRIS_GARBAGE` held at 4 through CS026 deliberately, so as not to confound the gate's only clean read on garbage density. That read is now taken and held, so moving it is a normal retune whenever a changeset wants it.

**⛔ CS026 P4 HAS LANDED — DELIVERY FEEDBACK IS SPREAD, KNOBBED AND QUIETED; NEXT IS P5.** The delivery floaters separate at the shipped defaults (`deliveryFloatRise` 300 px/s, `deliveryFloatLife` 0.55 s — both first-guess, both playtest-gate knobs), the HUD `COMBO n/24` row is gone (closing FLAG-CS020-i's readout, not its claim), and the incidental floater is dimmed/shrunk rather than folded into the towed tally. **Two things the gate should specifically be asked, per this phase's own recorded risks:** whether the cargo ceiling (`/cargoMax`) is still legible without the readout's denominator, and whether 300 px/s / 0.55 s reads as "a rising column" or still smears at a full 24-canister haul with a Magnet running. Both are also recorded under `## Balance notes`. **P5 should assume the DELIVERY registry section now ends `dockComboGrace, deliveryFloatRise, deliveryFloatLife` before JUNK** — no doc obligation carries forward (GDD §2.10 is P6's).

**⛔ CS026 P3 HAS LANDED — THE WORLD HAS TWO SIZES AGAIN, AND `resizeWorld()` IS BACK ON THE CRITICAL PATH.** Levels 1–5 are 1920×1080, level 6 on is 2560×1440, and the six-step carried-entity re-homing pass now fires **once per run**, at the 5 → 6 boundary, with a full field crossing it. **P4 and P5 should assume nothing about the world period**: a test that stages a board must ask `worldDims(game.worldSize)` rather than reading the module-load `WORLD_W`/`WORLD_H` snapshot, or set `DEBUG.earlyWorldLevels = 0` and say why — see this session's recap (8a) for which treatment fits which case, and (8b) for what the wrong one costs. **P6 owes one doc obligation from this phase**: the GDD §2.11.1 world-size rewrite (the GDD still describes a single 2560×1440 torus, true only from level 6 on). `DIFFICULTY-LEVERS.md` needs nothing — the world size is not a lever and must not become one; `test-cs026-p3.js` §G pins `LEVERS` and `leverState` byte-identical to the parent at every level 1..200 in both directions. **The gate should be asked whether five is the right band length and whether the single 5 → 6 change reads as a moment or a glitch** — both are recorded under `## Balance notes`, and the knob at 0 is the clean A/B for the whole feature.

**⛔ CS026 P2 HAS LANDED — THE SPLIT COUNT IS NOW A LEVER, AND P6 INHERITS A THIRD OBLIGATION FROM IT.** The debris tree is 2-way through level 10 and 3-way from 11 on, forever; level 1 is 21 bodies rather than 39. **P6's doc sweep owes three things, itemised in this session's recap entry above so they are not rediscovered:** a `DIFFICULTY-LEVERS.md` §3 row for `junkSplit` (adding it will trip `test-cs026-p2.js` §J's deliberate "no row yet" assertion — repoint it, don't delete it), the GDD's present-tense "3-way split" passages at lines 55/56/118 and the §3 Flow-functions row at 547, **and nothing else** — the Hunter passages at 67/78/204 say 3-way and are still CORRECT. **Half of those passages are true; the sweep is a debris/Hunter distinction, not a find-and-replace.** ⛔ **And this phase is the largest single lever on the garbage density ceiling** (`GARBAGE_SOFT_MAX` 220): a 3-way level 10 shed 624 canisters, a 2-way sheds 336. §1.7 says the split count and the ceiling must be answered in the **same gate** or neither answer means anything — the blocking playtest gate between P5 and P6 is where that happens.

**⛔ CS026 P1 HAS LANDED — THE TEST-INFRASTRUCTURE PHASE. NO GAME CODE TOUCHED, SO NOTHING IN THE BUILD MOVED.** `scratchpad/_phase-ref.js` and `scratchpad/_seeded-random.js` now exist, and **every later CS026 phase should use them** rather than re-inlining git plumbing: `PARENT_SHA` stays a hardcoded literal in each phase's own test file, `ownCommits(PARENT_SHA, "cs-26 pN:")` resolves that phase's own commit, `changedFiles(PARENT_SHA, sha)` (or `changedFiles(PARENT_SHA, null)` for the pre-commit working-tree reading) writes the "nothing else moved" pin. **A phase whose test drives the game and asserts on counts should `installSeed(SEED)` at the top, before its factory** — that ordering is the requirement, not a style choice. **Next: P2, the split count becomes a carried `junkSplit` lever** (FORK-CS026-A: 2 through L10, 3 from L11).

**⛔ P6 INHERITS TWO OBLIGATIONS FROM P1, AND ONE OF THEM IS A DECISION.** (a) FORK-CS026-H's resolution (c) makes the **closing phase** assert the suite runs with **zero skips** — grep for the single exported literal `SKIPPED (no git history)`, which is why it is exported rather than written out per file. (b) That assertion is only worth having once the files that *should* skip actually can: **ten more suite files still hard-fail on a shallow clone**, listed in `## Known issues`. Converting them is mechanical and all the same shape as the four already done; not converting them is also defensible. **Decide it explicitly — do not let it be decided by accident.**

**⛔ CS026 IS THE OPEN CHANGESET — P0 LANDED (docs and test-comment repointing only, no game code touched).** Per `PLANNED-FEATURES-CS026.md` / `IMPLEMENTATION-PHASES-CS026.md` (the live root-level pair; the CS025 pair is now archived, see below). **Phase map:** P0 archive/repoint/scope-cancellation (this session) → P1 phase-reference + seeded-random test helpers, pinning the five flaky/nondeterministic test paths → P2 the debris split count (2-way vs 3-way, `FORK-CS026-A`) → P3 a smaller world for levels 1–5 (the tail-search fix) → P4 delivery floaters spread into one updating tally, `COMBO` HUD row dropped, incidental deliveries quieted → P5 the level banner's four look-call constants promoted to knobs → **⛔ GATE** (blocking: pacing, tail fix, delivery payoff, garbage density, banner, look-call backlog) → P6 retune + version bump + doc sweep. **All eight forks are already resolved by Paul** (`PLANNED-FEATURES-CS026.md` §0.1): **A** debris split count is a **flat knob** (`debrisSplit`, not a lever); **B** `DEBRIS_MASS` keeps 9/3/1, comment rewritten as a fitted ratio rather than a conservation law; **C** the halved score curve is **accepted**, no compensation; **D** the tail fix is a **smaller world only** (no chevron), 1920×1080, levels 1–5; **E** delivery feedback **spreads the existing floaters** in place (one updating tally, not a ladder); **F** the HUD `COMBO` readout is **dropped**; **G** the incidental-delivery floater goes **quieter** (dimmed/smaller), never folded into the towed tally; **H** a `--depth 1` clone **skips** git-dependent test pins loudly, with the closing phase asserting zero skips. Read `PLANNED-FEATURES-CS026.md` in full before starting P1 — every fork's reasoning and the corrections to the original brief (§9) matter to how each phase is built.

**⛔ THE HALF-STRENGTH "MEGA DELIVERY" IS CANCELLED, NOT DEFERRED — SEE `## Known issues`.** It is no longer CS026's opening scope or any future changeset's. Paul decided against it outright; do not re-file it a third time.

**[LANDED — see the CS026 P5 entry at the top of this section. Kept for the record of what P5 was scoped against.]** CS026 also inherited one small thing from CS025's gate, already half-answered: Q6's answer asked for the level announcement to be unmissable and CS025 P5 built both halves it named (critical voice line + large fading centre-screen "Level N"). What CS025 P5 did *not* do was playtest it — the banner's four look-call knobs were first-guess numbers tuned by eye against nothing, and the banner had never been seen in motion. CS026 P5 promoted all four to GLOBAL-section knobs before the gate, so the standing "report the number" instruction now has something to report against. See `## Playtest asks`.

**[SUPERSEDED — CS025 IS COMPLETE; kept for the record of why P1 and P2 shipped together.]** **CS025 P1 HAS LANDED. P2 IS THE REPULSION KICK (`PLANNED-FEATURES-CS025.md` §2), AND IT IS THE OTHER HALF OF THE SAME FIX.** P1's own spec (§1.5) records the limit it leaves behind: **suppression alone does not disperse a cloud that has already gathered.** Pieces already inbound keep their velocity, and `MAGNET_DAMP` lives *inside* the attraction branch, so the light in-range damping stops with the pull. §2's one-shot outward kick on the rising edge of "the chain just became full" is the answer, and the two features ship together for that reason. P2 also adds the other two §5 knobs (`magnetPushKick`, `magnetPushSpread`), taking the registry **73 → 75**. **P3 then reads `magnetPulling()` from `Ship.draw()` for the scoop energy tell — read the one-frame-lag entry in `## Known issues` before writing it.**

## Playtest asks

**✅ CS026's PLAYTEST GATE IS CLOSED. All eight questions were answered by Paul and applied by CS026 P6 on 2026-08-12; the answers are preserved inline below, under each question, as the record of what was decided and why.** **SIX OF THE EIGHT CAME BACK CLEAN and moved nothing** (Q1 pacing *"none are wrong. Pacing is much better"* — the changeset's central bet confirmed; Q2 the smaller world *"fine"*; Q3 the score curve *"fine"*, which clears FORK-CS026-C's accepted hull-repair risk; Q4 garbage density *"fine"*, re-checked against the post-split volume in the same session and held at 220/300; Q6 the incidental delivery *"fine"*; Q7 the level banner *"looks great"*, so all four P5 knobs stand). **TWO ANSWERS WERE NOT NUMBERS, both were put to Paul before a line of code was written, and both changed what got built** — the CS025 P5 precedent, and the standing rule. **Q5** asked for the delivery floaters to be closer to the ship and to rise and fade more slowly; he was offered a third knob (`dockOffloadInterval`, to buy back the separation a single origin costs) and **declined it**, choosing to hold delivery pacing, **so the registry did not grow at all this phase**. **Q8** returned four Achievements-layout defects; his own suggested fix for the fourth (an alternating row background) was **not** built, because it would have been a third GDD §3.2 fill exception — shown that trade, he chose a dotted leader run instead. **⛔ Q4 is the one question Paul explicitly declined to put a number on** (*"I do not know what number to report"*), and the correct handling was to change nothing rather than to invent one. **No lever moved.** All code phases (P0–P6) are landed. The questions are reproduced **verbatim** from `PLANNED-FEATURES-CS026.md`'s "§10 — ⛔ THE PLAYTEST GATE" section, which is the single source; write the answers inline under each one (`Paul says: …` is the established style), or paste them into the next session's opening message and tell it to record them here first.

**THE STANDING INSTRUCTION: for anything on a slider, retune live and REPORT THE NUMBER YOU LANDED ON, not a yes/no. "Fine" is a complete answer** — four closing phases on record have been clean (CS020 P2, CS022 P4, CS024 P7, CS025 P5). Do not manufacture changes to justify the gate.

**⛔ THE BRIEFING — WHAT CHANGED ACROSS P2–P5.**

**P2 — the debris split count is now a carried `junkSplit` lever: 2-way through level 10, 3-way from level 11 on, forever.** Level 1 is 21 bodies instead of 39; a 2-way level 10 (84 bodies) is still busier than a 3-way level 1 (39) was. `destroyHunter()` still splits 3-way always — only debris moved. Slider: the JUNK section's split knob (set back to 3 for the clean A/B).

**P3 — levels 1–5 run in a 1920×1080 world instead of 2560×1440; level 6 on is unchanged.** The farthest a body can be from the ship drops from 1,469 px to 1,101 px, and the viewport covers 44% of the board instead of 25%, so hunting the last two or three smalls at the end of an early level is a shorter walk. No combat number moved. Knob: `earlyWorldLevels` (GLOBAL), default 5 — 0 turns the whole feature off for a same-session A/B. Watch the 5 → 6 transition itself: the field re-homes around the ship (the `dmax` shell), so something that was 900 px behind arrives 660 px behind on the far side.

**P4 — delivery floaters spread instead of stacking, the HUD `COMBO n/24` row is gone, and an incidental delivery (hooked while already parked at the dock) is dimmed/shrunk rather than sharing the towed tally's colour.** At the shipped knobs, consecutive floaters separate by 15 px (was 1.5) and each travels 165 px before fading (was 33). Sliders: `deliveryFloatRise`/`deliveryFloatLife` (DELIVERY section, right after `dockComboGrace`). **Named risk:** the HUD row was the only thing showing the `/cargoMax` denominator — the dock floaters show none.

**P5 (this phase) — the level banner's four look-call constants are now GLOBAL knobs, so the gate below can finally put numbers to them.** Nothing about the banner's behaviour changed, only that it is now retunable live. Sliders: `levelBannerTime`/`Fade`/`Size`/`Y` (GLOBAL section, right after `Start level`).

**THE GARBAGE DENSITY CEILING (§7, live since CS024, never moved) IS URGENT THIS SESSION SPECIFICALLY BECAUSE OF P2** — the split change moved the shed volume by roughly half at every level, so the ceiling that was tuned against the OLD volume needs re-checking against the new one in the SAME session, or neither answer means anything. Sliders: `Garbage soft max` / `Garbage hard max` (GARBAGE section). Frame budget bounds any raise: 24,090 pair visits/frame at 220 (the current soft max), 44,850 at 300, ~99,900 at 450 — quadratic, so ~300–350 is affordable; beyond that wants a spatial grid, not a bigger number.

**WHAT TO PLAY: levels 1 → 12 minimum, including at least one level in the 8–11 band** — `junkCount` is 10–12 there and the tree is four times level 1's, so the pacing question (Q1) is not answerable from levels 1–3 alone. `startLevel` (GLOBAL) can sample a deep level directly but gives a level-N field with a level-1 ship — no scoop upgrades, no banked powerups — so it is the wrong tool for anything reading on scoop level or banked powerups.

**CS026's eight questions, verbatim from `PLANNED-FEATURES-CS026.md` §10:**

**Q1 — pacing, the changeset's central bet.** With the split at its new value, does a level *end* at about the right time? Play level 1 and a level in the 8–11 band and say which one is wrong, if either. Slider: the split knob (JUNK section) — set it back to 3 for the clean A/B. **Report the number.**

Paul says: None are wrong. Pacing is much better.

**Q2 — did the tail fix do the work?** With the split change alone the last few satellites are still somewhere in the other 75% of the world. Does the tail fix make "where is it" into "go there"? If it is the chevron: does ≤3 remaining feel like the right trigger, or should it show earlier? Slider if built. **Report the number.**

Paul says: This is fine.

**Q3 — the score curve.** Whatever FORK-C resolved to: does the run feel like it pays what it should, and does the hull repair often enough? This is the question that catches a wrong answer to FORK-C.

Paul says: Yes, score curve is fine.

**Q4 — garbage density, and it must be answered in this session.** The split moved the shed volume by roughly half at every level. Do you ever *see* a canister vanish? (The cull is silent by design; catching one means `garbageSoftMax` is too low.) Does a level-10 field read as salvage-rich or as noise? Sliders: `Garbage soft max`, `Garbage hard max`. **Report the number.** ⛔ Do not raise past ~350 without saying so — beyond that the O(n²) walk, not readability, is what binds.

Paul says: Garbage density is fine. I do not know what number to report, so forget about that unless you have to have something.

**Q5 — the delivery payoff, the other central bet.** Deliver a full haul. Does the escalation *read* now — can you see it climbing? Is the dock the right place for it, or does it want to be nearer the ship? And the specific failure to watch for: does it collide with `SALVAGE BONUS` (at 8) or `MAX HAUL` (at 24)?

Paul says: It needs to be closer to the ship, and the score numbers need to fade more slowly, and they need to travel upwards more slowly. The text of the score numbers is a good size.

**Q6 — the incidental case.** Park at the dock with a Magnet and let it feed you. Whatever FORK-G resolved to: does that read as sensible, or as the same smear in a new place?

Paul says: It is fine.

**Q7 — the level banner, first sight.** It has never been playtested. Does "Level N" at **72 px**, held **2.2 s** with a **0.5 s** ramp each end, read as *briefly, large, easy to read, fading*? Too big, too long, too easy to miss in a busy frame? It sits at screen centre, where the ship usually is — does it ever obscure something that matters at the instant a level starts? **All four are sliders as of P5. Report the numbers.** Also: `P` exports a frame with the banner across it for the first 2.2 s of every level (FLAG-CS026-c). Correct, or annoying?

Paul says: The level banner looks great.

**Q8 — the look-call backlog, in words.** No sliders here, so words are the answer: the modal dialog's proportions; the title menu's row spacing (the tightest in the game); the two-tab Achievements layout; and specifically — **does the Options screen's dim "Save" row read as *coming soon* or as *broken*?**

Paul says: Most things look great. These are the issues, below:

1. In the two-tab Achievements layout, it is difficult to understand which of the tabs is currently selected. Something other than just the brightness of the text needs to happen when selected. One idea is simply a downward-facing triangle just to the right of the text for the selected tab.

2. Also in the Achievements layout, There is no whitespace above the title of the achievement. This seems to occur throughout the Achievements panel. Build in an appropriate amount of space.

3. Also in the Achievements layout, there is too much vertical space beneath the name of the achievement and the description of the achievement.

4. Also in the Achievements layout, there is so much horizontal space between the name of the achievement (on the left) and the status of the achievement (on the right) that it is difficult to discern which status belongs with which achievement. One solution is to alternate a light background every-other row in the listings, so we have a visual line connecting left to right.

## GDD version history

CS026 (P0–P6) — early-level pacing, a smaller early world, and the delivery payoff

**Build `1.0.0.26`.** The changeset's subject is the *early game*: how long a level takes to end, how far you walk to end it, and whether emptying a full haul reads as a payoff. Eight forks were resolved by Paul before any phase ran, and eight gate questions after.

**P0** retired CS025, archived its planning pair, repointed references — and **cancelled the half-strength Mega Delivery outright** (Gate B Q13, re-filed twice; dead, not deferred). The four `STATUS.md` mentions were split correctly: the two *forward-looking* sections were rewritten, the `Playtest asks`/`Working / verified` ones preserved as historical record. **P1** added `scratchpad/_phase-ref.js` (a phase's own parent SHA, its own commits by subject, and the changed-file set — never `HEAD`) and `_seeded-random.js`, and seeded the five nondeterministic suite paths; three of the five were the *game's* randomness at module load, not the test's, which is why the seed installs at the top of a file, before the first build. **P2** made the debris split count a carried lever — **`junkSplit`, 2-way through level 10, 3-way from level 11 on** (FORK-A). Level 1 is a 21-body tree instead of 39, while a 2-way level 10 (84 bodies) is still busier than a 3-way level 1 ever was. `destroyHunter()` keeps its own hardcoded 3-way split: `ACH_LINEAGE_FULL = 13` depends on it. `DEBRIS_MASS`'s conservation *derivation* died with the change and was retired in place (FORK-B) — the 9/3/1 table is kept as a fitted feel ratio, because nothing reads a total mass and a conserving 4/2/1 would have reopened every satellite bounce and flattened the ricochet ratio. `DEBRIS_SCORE` was left alone (FORK-C), accepting that a leaner curve also repairs the hull less often. **P3** re-armed the world-size seam CS024 P1 had left deliberately live: levels 1–5 run at **1920×1080** (`WORLD_SIZE_EARLY`, a hard floor at 1.5× the viewport), level 6 on unchanged, one resize per run (FORK-D). The viewport covers 44% of the board instead of 25% and the farthest a body can be from the ship drops 1,469 → 1,101 px. **No combat number moved.** That the future per-level scheme turned out to be a data change rather than a re-plumb is the whole return on having kept the seam. **P4** rebuilt the delivery feedback: the floaters got their own rise/life knobs instead of `FloatText`'s ambient defaults (they had been separating by 1.5 px at the dock's 20-per-second cadence and rendering as one smear), the HUD `COMBO n/N` row was dropped (FORK-F), and an incidental delivery was quieted to `COLOR.dim`/size 12 rather than folded into the towed tally (FORK-G). **P5** promoted the level banner's four look-call constants to GLOBAL knobs — it had shipped in CS025 P5 *out of that changeset's own gate*, so nobody had ever seen it move — and wrote the CS026 gate.

**P6 applied the gate.** **Six of eight questions came back clean** and moved nothing: pacing (*"none are wrong. Pacing is much better"* — the central bet confirmed), the smaller world, the score curve (which is what cleared FORK-C's accepted risk), garbage density (**re-checked against the post-split volume in the same session, which was the point, and held at 220/300** — FLAG-CS024-c discharged), the incidental case, and the banner (*"looks great"* — all four knobs stand). **No lever moved**; `LEVERS` and `leverState` are pinned byte-identical at every level 1..200 against the phase's own parent. The two questions that returned work were both look-calls and both were confirmed with Paul before any code was written. **Q5** moved the delivery floaters' origin from the popped chain node — which is the chain's *tail*, the canister farthest from the ship — to the ship itself, with `deliveryFloatRise` 300 → **160** and `deliveryFloatLife` 0.55 → **1.2**. A third knob was offered and **declined**: one origin makes separation purely `rise × DOCK_OFFLOAD_INTERVAL`, so the tighter rise costs column spacing, and buying it back meant halving the offload cadence and doubling a 24-canister dock visit to 2.4 s. He held delivery pacing instead, **so the registry did not grow at all this phase — it stays at 85**. The milestone floaters (`SALVAGE BONUS`, `MAX HAUL`) deliberately did not move: they belong to the dock. **Q8** returned four named Achievements-screen defects — an unreadable tab selection, no air above a row's title, too much air under it, and a 1140 px gap between a name and its status. Fixed at look-call constants plus `achLeader()`, a dotted name→status leader run on both row branches. Paul's own first suggestion, an alternating row background, was **not** built: it would have been a fill and therefore a third named §3.2 exception, and shown that trade he chose the leader, which is `drawText`. **The §3.2 exception count stays at two.** `achLeader()` is the first Achievements code to do arithmetic on `ctx.measureText()`, so its guards are NaN-safe by construction and five degenerate widths are driven against the real renderer. The sweep also retired GDD §3's five dangling `§2.13.1` cross-references and corrected two §3 Flow-functions claims that had gone stale two changesets earlier (`min(3+wave, 9)` debris, and a hardcoded 3-child split).
