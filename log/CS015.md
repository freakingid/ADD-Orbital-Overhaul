# CS015

**Prior session (CS015 P7, landed):** picked up immediately after the prior session's deferred gate check (below) — Paul composed and zero-err-verified `phon` for `c5eae7b`'s five chain_broken lines in `tools/voice-robot-lab.html` himself and handed back the five `{text, phon}` pairs directly in chat. **Independently re-verified before pasting** (didn't just trust the claim): built the real extracted `<script>` block headlessly and ran all five `phon` strings through the actual `parsePhonTokens` — confirmed `errs.length === 0` for every line. **Replaced `VOICE_LINES.chain_broken` (L1622–1628) verbatim** with the five verified pairs — `Payload damaged.` / `Payload disrupted.` / `Payload lost.` / `Payload adrift.` / `Payload is getting away.`, each now real ARPAbet (e.g. `"P EY1 L OW D / D AE1 M IH JH D ."`) instead of `c5eae7b`'s plain-English placeholders. `VOICE_PRIORITY.chain_broken` untouched (still 2) — lines are data, no engine/scheduler edit. **`GAME_VERSION` needed no change** — already `"1.0.0.15"` (bumped in P6); re-grepped `scratchpad/` for the old `"1.0.0.13"` literal again, still zero live hits. **New test `scratchpad/test-cs015-p7.js`** (44 assertions, drives the REAL `parsePhonTokens`/`buildUtterance`/`VoiceSys.say`/`startGame`/`update` — no reimplemented logic): the 5 `chain_broken` entries match the approved text+phon exactly (both pinned, so a future accidental hand-edit is caught immediately); every phon parses/builds with zero unknown tokens; `VOICE_PRIORITY.chain_broken === 2`; `VoiceSys.say("chain_broken")` is headless-safe (`AudioSys.ctx` null → no throw); `GAME_VERSION === "1.0.0.15"` pin; `node --check` + a `startGame`/`update(1/60)` no-crash smoke. Full regression green: **60 test files, 4,844 assertions**, `node --check` clean (existing `test-cs011-p5.js` section B, which checks `chain_broken` generically rather than pinning old text, needed no changes — confirms the harness convention paid off). **No genuinely new design question surfaced this session** — the wording was already Paul's own call from the prior session, and the phon composition was explicitly his to do in the lab, not mine to invent. Docs: GDD §2.8 (voice-lines note — `chain_broken` set replaced, register described as functional/matter-of-fact per FORK-CS015-D's rationale even though the final wording is `c5eae7b`'s, not the original FORK-CS015-D text), top-of-file Current-build → CS015 P7 / `"1.0.0.15"`; `GDD-VERSION-HISTORY.md` — **consolidated the six existing per-phase CS015 bullets (P1–P6) plus this P7 into one round-closing entry** (`CS015 (P1–P7)`), per this phase's own docs instruction ("a consolidated CS015 entry") and the CS012/CS013 precedent for round-closing consolidation; this file (CS015 round complete). Did NOT push.

**Prior session (CS015 P7 attempt — gate check only, deferred):** re-grepped `GAME_VERSION` — already `"1.0.0.15"` (P6 carried the bump last session per `FLAG-CS015-g`, since P7 hadn't run yet) — and grepped `scratchpad/` for the old `"1.0.0.13"` literal: zero live hits (only historical references remain, in `archive/` and the planning docs), confirming P6's bump already covered every scratchpad file. **Nothing left to bump.** Checked the P7 precondition (`PLANNED-FEATURES-CS015.md` FORK-CS015-D / the phase's own "⛔ STOP" block): the five FORK-CS015-D-approved lines (`Payload damaged.` / `Payload disrupted.` / `Payload scattered.` / `Bounty broke free.` / `Bounty lost.`) have **not** been composed or zero-err-verified in `tools/voice-robot-lab.html` — grepped the lab file for `Payload`/`Bounty`, zero hits anywhere. **Gate unmet, so per the phase's own instructions P7 did not run** — `VOICE_LINES.chain_broken` was not touched. **Surfaced rather than silently resolved:** while re-grepping, found `git log` shows a commit **`c5eae7b` "Changed 'chain broken' voice phons"** (Paul, dated today, sitting directly on top of P6, ahead of this session) that already hand-edited `VOICE_LINES.chain_broken` **directly in the live build**, outside the lab-verbatim pipeline. Its text only partially matches the FORK-CS015-D approved set — 2 of 5 exact (`Payload damaged.` / `Payload disrupted.`), the other 3 diverge (`Payload lost.` / `Payload adrift.` / `Payload is getting away.` vs. the approved `Payload scattered.` / `Bounty broke free.` / `Bounty lost.`) — and its `phon` fields are plain English, not ARPAbet (e.g. `"Payload DAM-ijd."`, `"Payload uh-drift."`, `"Payload is gehting uh-way."`). Checked against `parsePhonTokens`'s own token regex (`^([A-Z]+)([012])?$`): mixed-case words fail the match, so virtually every token in those five strings lands in `errs` — at runtime, `chain_broken` currently synthesizes as broken/near-silent audio. **This is a live, uncaught regression already on `main`, introduced by `c5eae7b`, not by any CS015 build phase.** Asked Paul directly (rather than guessing which wording to keep, or hand-authoring ARPAbet myself — either would break the VoiceSys port-verbatim non-negotiable): his call was to **leave `chain_broken` exactly as `c5eae7b` left it for now**, do the lab composition pass himself when ready, and **treat CS015 as complete for this round with P7 explicitly deferred** (not abandoned — the approved text is still locked, only the lab phon step remains, same as before this session). **No code changed this session; no commit** (nothing shipped — `GAME_VERSION`/scratchpad were already correct, and touching `chain_broken` without a verified lab phon would violate the gate). **Next session picking up P7:** don't re-verify the gate from scratch — check whether Paul has since composed+verified phon in the lab (for whichever wording he settles on, `c5eae7b`'s or the original FORK-CS015-D set) and paste verbatim once he has.

**Prior session (CS015 P6):** implemented CS015 Phase 6 (item 10 of `PLANNED-FEATURES-CS015.md`, per `IMPLEMENTATION-PHASES-CS015.md`), reading `STATUS.md` and `CLAUDE.md` first as required. **FLAG-CS015-a was already resolved before this session** (Paul's call: default **10 s**, deliberately not biased toward the old `GARBAGE_DECAY` 22). Re-grepped every anchor before editing (all confirmed at the file's current lines, no drift worth flagging): `GARBAGE_DECAY = 22` and its ctor seed `this.decay = GARBAGE_DECAY`; the decay gate `if (this.pieces === 1) { this.decay -= dt; if (this.decay <= 0) this.dead = true; }`; the single blink-out tell in `draw()` (`this.decay < GARBAGE_FADE`) and the clump draw branch (no fade tell, pre-P6); the merge site in `coalesceGarbage` (survivor `a` absorbs `b`: `a.pieces += b.pieces`, `a.radius = 7*sqrt(a.pieces)`); `Garbage.fromNode` (still routes through the ctor). **Appended a sixth `DEBUG_VARS` entry, `garbageLifetime`** (unit `s`, `[1,60]` step 1, `def: 10` — deliberately NOT derived from `GARBAGE_DECAY`, per FLAG-CS015-a). **This phase changes behavior, not just adds a knob, exactly as specced:** the `Garbage` ctor now seeds `this.decay = DEBUG.garbageLifetime` instead of the frozen `GARBAGE_DECAY`; the `pieces === 1` gate is REMOVED from `update()`'s decay countdown — every piece, single or clump, now counts down and dies at `<= 0`; `coalesceGarbage`'s merge branch sets `a.decay = DEBUG.garbageLifetime` right after the survivor absorbs `b` (a merge is activity — the clock resets to full, so an actively-growing lineage never dies but a stalled one does); `draw()`'s `pieces > 1` branch gained the identical `GARBAGE_FADE` blink tell the `pieces === 1` branch already had, mirrored verbatim. The Hunter-coalesce transform (`a.pieces >= HUNTER_COALESCE_COUNT`) is unaffected — confirmed in test that it still kills `a` and spawns a Hunter even when `a.decay` is nearly zero at the moment of a threshold-crossing merge. `GARBAGE_DECAY` (22) stays in the source as a historical/shipped-value comment only, no longer read live anywhere. **New test `scratchpad/test-cs015-p6.js`** (39 assertions, drives the REAL `applyDebug`/`menuDebug`/`Garbage`/`coalesceGarbage` — no reimplemented logic): registry shape (6 entries, the new one's id/label/unit/min/max/step/`def` exact, no `toNative`); seeded default is exactly 10 (not 22); `menuDebug` clamp at `[1,60]` step 1; a fresh `Garbage` seeding `decay` from the live knob, independent per-instance at construction time (dialing the knob after construction doesn't retroactively change an existing piece); a single still dying on schedule and blinking in its last `GARBAGE_FADE`; a stalled clump now aging out where it never did pre-P6; a merge resetting the survivor's clock exactly to the dialed lifetime; a lineage that keeps merging every cycle (4 cycles, each under the dialed lifetime, total elapsed time well past one bare lifetime) never dying — ending at 5 pieces, safely under the 12-piece Hunter threshold — versus an identically-shaped clump that merges once then stalls and dies; the Hunter-transform edge case (pieces one merge shy of `HUNTER_COALESCE_COUNT`, `decay` almost zero) still transforming into a Hunter rather than dying of age, confirming the phase prompt's own "watch" note. **Three pre-existing test files needed updates — a legitimate consequence of the behavior change, not bugs either caught, same category as prior phases' registry-length updates:** `test-cs015-p5.js` section (B) hardcoded `DEBUG_VARS.length === 5`; changed to `>= 5` since the registry only ever grows (same precedent P4→P5 set for `test-cs013-p4.js`). `test-f3.js` section (C) asserted severed garbage's `decay === GARBAGE_DECAY`; repointed to `DEBUG.garbageLifetime` (added `DEBUG` to that file's `RETURN`/destructure list) and updated the config log line's wording. `test-v31-coalesce.js` test (16) asserted a lone single dies at ~22s and that a clump *never* ages out even 66s later — both premises are now wrong by design, so the block was rewritten to drive off the live `DEBUG.garbageLifetime` and to assert the stalled clump **does** now age out (FORK-4 reversed), keeping the same "single ages out, `garbageDecayed` stat stays gone" checks (also added `DEBUG` to that file's `RETURN`/destructure list). Full regression green: **59 test files, 4,800 assertions**, `node --check` clean. **`GAME_VERSION` bumped `"1.0.0.13"` → `"1.0.0.15"` this phase, per `FLAG-CS015-g`:** P7 (item 6, the voice-lab-gated chain-broken lines) has not run this round — the manual lab-phon composition step in `tools/voice-robot-lab.html` is still outstanding — so per the round's own instructions this P6 session is the last-landed phase and carries the bump. Bumped the two scratchpad files that hardcoded the old literal (`test-cs010-p0.js`, `test-cs013-p4.js`) alongside it, as instructed. **No genuinely new design question surfaced beyond FLAG-CS015-a**, which was already resolved before this session began. Docs: GDD §2.10.1 (rewrote the decay rule across the section's two density-governor bullets to describe all-sizes decay + merge-reset + the live `garbageLifetime` knob, and flagged the tighter default margin against `garbageAttractDelay` — 10s/3s now vs. the old 22s/3s), plus three other call-sites that asserted the now-superseded "clumps never decay" rule as current fact (§2.4's intro parenthetical, §2.5.1's exits bullet, §2.10's "Garbage drops" paragraph); §2.19 (new sixth-knob bullet, section header gains "; a fifth CS015 P6"); Architecture Map Constants row + Entity row (`DEBUG_VARS` grew 5→6, `Garbage.decay`'s behavior rewritten); top-of-file Current-build → CS015 P6 (`GAME_VERSION` "1.0.0.15"); `GDD-VERSION-HISTORY.md` (new CS015 P6 entry appended); this file. Did NOT push.

**Prior session (CS015 P5):** implemented CS015 Phase 5 (items 7, 11, 12 of `PLANNED-FEATURES-CS015.md`, per `IMPLEMENTATION-PHASES-CS015.md`), reading `STATUS.md` and `CLAUDE.md` first as required. Re-grepped every anchor before editing (all confirmed at the file's current lines, no drift worth flagging): `SCOOP_HITS_PER_LEVEL` and its one consumer (`if (game.scoopHits >= SCOOP_HITS_PER_LEVEL)` in `damageShip`'s scoop-decay block); `GARBAGE_COALESCE_DELAY` and its two direct consumers (the `Garbage` ctor's `this.coalesceDelay = GARBAGE_COALESCE_DELAY` seed and the partial-scoop leftover's re-arm site `g.coalesceDelay = GARBAGE_COALESCE_DELAY`) — also checked for a separate shatter re-arm as the prompt flagged, and confirmed `shatterClump`'s emitted pieces need no separate repoint since they're plain `new Garbage(...)` calls already routed through the one ctor line; `GARBAGE_MAGNET_RANGE` and its one consumer (`coalesceGarbage`'s pair-range gate `d2 >= GARBAGE_MAGNET_RANGE * GARBAGE_MAGNET_RANGE`); `GARBAGE_MAGNET_PULL` and its one consumer (the same function's mutual-attraction accel term). **Appended four entries to `DEBUG_VARS`, same registry/`toNative` idiom P4 established — no panel/persistence code touched, exactly as P4's own docs predicted:** `scoopHitsPerLevel` (unit-less, `[1,20]` step 1, `def: SCOOP_HITS_PER_LEVEL`, no `toNative`), `garbageAttractDelay` (ms, `[0,10000]` step 250, `def: GARBAGE_COALESCE_DELAY*1000`, `toNative: v=>v/1000`), `garbageAttractRadius` (px, `[0,600]` step 10, `def: GARBAGE_MAGNET_RANGE`, no `toNative`), `garbageAttractForce` (px/s², `[0,200]` step 5, `def: GARBAGE_MAGNET_PULL`, no `toNative`). Repointed all five consumer sites to read `DEBUG.*` instead of the frozen consts; the four consts themselves stay in place as the documented shipped defaults (and the source of each entry's `def`). **One consumer nuance flagged in-code and here:** `garbageAttractDelay` is captured **per-piece at `Garbage` construction time**, not read live — a piece born before a knob change keeps its original delay for its whole life, while `garbageAttractRadius`/`garbageAttractForce` are read live every `coalesceGarbage` pass. This matches the prompt's own note ("captured per-piece at ctor time — applies to new garbage — fine"). **New test `scratchpad/test-cs015-p5.js`** (64 assertions, drives the REAL `applyDebug`/`menuDebug`/`damageShip`/`coalesceGarbage`/`Garbage` — no reimplemented logic): registry shape (5 entries total, the 4 new ones' id/label/unit/min/max/step/`def`/`toNative` presence-or-absence all checked against the real registry); seeded-default round-trip for all four (`garbageAttractDelay`'s ms↔s conversion exercised at two values, the other three's display===native no-conversion checked); `menuDebug` left/right clamp at each new var's own `[min,max]` plus a single-step nudge off the floor; a byte-identical-at-defaults section (`DEBUG.*` equals the pre-P5 shipped consts exactly, the consts themselves confirmed unchanged, and a fresh `Garbage`'s `coalesceDelay` still equals the shipped `GARBAGE_COALESCE_DELAY` at a fresh build); the scoop-loss consumer exercised at both the unchanged default (5 hits — cross-checked against the existing `test-v33-p3.js` behavior) and dialed down to 3 (drops a level on the 3rd hit, reading the live `DEBUG` value not the frozen const); the attraction-delay consumer confirmed inert-then-active exactly at a dialed 0.5 s (not the frozen 3.0 s default); the attraction-radius consumer confirmed an 80 px-apart active pair feels nothing at a tightened 50 px radius but attracts once widened to 100 px; the attraction-force consumer confirmed the per-frame kick is bit-exact `force*dt` at two different dialed values (111 and 20), not the frozen `GARBAGE_MAGNET_PULL` (40). **`test-cs015-p4.js` needed two small updates — a legitimate consequence of the registry growing from 1 to 5 entries, not a bug either caught:** its section (B) had hardcoded `DEBUG_VARS.length === 1` and read the entry off `DEBUG_VARS[0]`; repointed to look the entry up by id (`DEBUG_VARS.find(v => v.id === "autoShieldRegenPause")`) instead of assuming index/length. Its section (F) had hardcoded the Back row at index 1 (`N+1` with the then-only `N=1`); repointed to compute `backRow = A.DEBUG_VARS.length` off the real registry and drive `down` that many times to reach it, rather than assume a literal `1`. No assertion's *intent* changed in either case — both still verify the exact same behavior, just off the real symbol instead of a stale literal (mirrors how P4's own session updated `test-cs013-p4.js` for the same reason). Full regression green: **58 test files, 4,757 assertions** (57 prior + this session's new 64-assertion file, net of the one now-stale length-assertion dropped from `test-cs015-p4.js`), `node --check` clean. **No `GAME_VERSION` bump** — P5 of a multi-phase round, the bump rides whichever phase lands last (`FLAG-CS015-g`), stays `"1.0.0.13"`. **No genuinely new design question surfaced** — all four knobs, their ranges/steps, and the consumer repoints were fully specified in the phase prompt and followed as given; the "also check for a shatter re-arm" instruction was investigated and resolved (no separate site exists — `shatterClump`'s emissions already route through the one repointed ctor line). Docs: GDD §2.19 (four new knob bullets appended, same pattern as P4's first-knob bullet; section header gains "; four more knobs CS015 P5"), §2.10.1 (the late-wave-clutter lever bullet now notes `GARBAGE_MAGNET_RANGE`/`GARBAGE_COALESCE_DELAY`/`GARBAGE_MAGNET_PULL` are dev-tunable live via the panel now, and that `GARBAGE_DECAY` is NOT one of the four and still needs a code edit), §2.14.1 (the scoop-decay bullet notes `SCOOP_HITS_PER_LEVEL` is also dev-tunable live now), Architecture Map Constants row (`DEBUG_VARS` grew 1→5, each new entry's source const named), top-of-file Current-build → CS015 P5 (`GAME_VERSION` unchanged), `GDD-VERSION-HISTORY.md` (new CS015 P5 entry appended), this file. Did NOT push.

**Prior session (CS015 P4):** implemented CS015 Phase 4 (items 4, 5 of `PLANNED-FEATURES-CS015.md`, per `IMPLEMENTATION-PHASES-CS015.md`), reading `STATUS.md` and `CLAUDE.md` first as required. Re-grepped every anchor before editing (all confirmed at/near the prompt's own CS013-era estimates, no drift worth flagging): the `menuInput` dispatch (actual L2351) + its per-screen label-dispatched handlers, `drawMenu`'s render dispatch (L5178) + `menuPanel` (L5191), `gotoScreen`/`openPause`/`closePause` (L2333/L2301/L2316), the three-context `keydown` listener (L2077, `AudioSys.init()`/`resume()` at L2079), `update()`'s early-return (`if (game.state !== "playing" || game.paused) return;`, L4672 — **confirmed it early-returns**, so the idle timeout can't tick there), `loop()` (L6060, runs unconditionally), the AudioSys one-shot pattern (`ui`/`scooploss`, L800/L844 — `this.ctx` guard → `this.now()` → oscillator/gain envelope → `this.sfx`), `settings`/`saveSettings`/`loadSettings`/`STORAGE_KEY` (L2276/L2571/L2587/L2567), and the item-5 consumer `s.autoShieldRegenLock = AUTO_SHIELD_REGEN_PAUSE` (L4309) + the `AUTO_SHIELD_REGEN_PAUSE = 1.0` const (L132). **Built as four cohesive pieces, exactly per the prompt.** **(1) Registry + runtime (FLAG-CS015-h — debug vars as DATA):** a `DEBUG_VARS` array of `{id,label,unit,def,min,max,step,toNative?}` (placed just after the `settings` object) is the single source of truth driving render/adjust/persistence generically; two parallel maps back it — `DEBUG` (native-unit live values consumers read) and `debugShown` (display-unit values the panel shows/persists) — with `applyDebug(id, shown)` writing both (display→native via the entry's `toNative`, identity when absent) and a `for (const v of DEBUG_VARS) applyDebug(v.id, v.def)` seed loop at module load. The one entry's `def` derives from `AUTO_SHIELD_REGEN_PAUSE*1000` (single source of truth for the default); the item-5 consumer was repointed to `s.autoShieldRegenLock = DEBUG.autoShieldRegenPause` (native seconds), the const left in place as the documented shipped default. **(2) The `"debug"` panel screen:** added to the `menuInput` + `drawMenu` dispatch; `drawDebug()` renders `menuPanel(560, 220+N*46, "DEBUG OPTIONS")` with one `label ◄ value unit ►` row per registry entry (only the focused row shows chevrons) + a centered Back row; `menuDebug(a)` moves the cursor over N+1 rows and, on left/right over a value row, adjusts the focused knob by its `step` clamped `[min,max]` then `applyDebug` + `saveSettings` + `AudioSys.ui(false)`; `openDebug()` (sibling of `openPause`, sets `game.paused`, `resetMenuNav`) opens it from the title, and `debugReturn()` mirrors `menuOptions`' routing (`gotoScreen("root", rootItems().indexOf("Options"))` while playing/gameover, else `closePause()`). Deliberately **NOT** added to `MENU_OPTIONS`/`rootItems` — secret-only. **(3) Persistence (FLAG-CS015-c — nested sub-object, no new key):** `saveSettings` adds `debug: { ...debugShown }` (display units) to the existing frozen `afd_settings_v1` blob; `loadSettings` validates each registry entry independently after the other additive fields (`Number.isFinite(dv) && dv >= e.min && dv <= e.max` → `applyDebug`, else keep the seeded default) — same known-value-else-default guard every field on that key uses. **(4) Secret-code entry + beeps:** a module-level `DebugCode {armed,buf,last}` + consts `DEBUG_CODE = "EvilG3niu$"` / `DEBUG_CODE_IDLE_MS = 4000`, plus two AudioSys one-shots `secretArm()` (3 ascending square blips) / `secretDisarm()` (3 descending) modeled on `ui`/`scooploss` (both `this.ctx`-guarded). The `keydown` listener gained a block right after `AudioSys.init()`, **above** the three context branches, gated to fire ONLY when `game.state === "title" || (game.paused && game.state === "playing")` and NOT `game.menu.rebinding`/`game.entry`: the backtick arms (`buf=""`, `last=performance.now()`, `secretArm()`, `return`), each subsequent `!e.repeat` key stamps `last` and — if `e.key.length === 1` — appends the raw `e.key` to `buf` kept to the last `DEBUG_CODE.length` chars (so case + shifted `G`/`3`/`$` survive while multi-char Shift/Arrow/Enter/Esc are dropped), and a `buf.endsWith(DEBUG_CODE)` match disarms + opens (`openDebug()` from title, `gotoScreen("debug")` from a paused game). Every code char is inert on those screens until the code lands (none are nav/confirm/pause/O keys); code keys are not `preventDefault`'d. In `loop()`, unconditionally right after `update()`, an idle tick disarms + `secretDisarm()`s once `performance.now() - DebugCode.last > DEBUG_CODE_IDLE_MS` — **it MUST live in `loop()`, not `update()`** (which early-returns while paused/at title, the very states the code arms in): this loop()-vs-update() seam was the phase's genuine architectural call, resolved as the prompt directed. Touched no invariant guard, the MusicSys scheduler, or the VoiceSys engine; no new frozen key name. **New test `scratchpad/test-cs015-p4.js`** (62 assertions, drives the REAL functions — no reimplemented logic): a `build({audio, storage})` helper evals the extracted `<script>` per-instance; **two instance flavors** because the keydown handler calls `AudioSys.init()` up front, which does `new (window.AudioContext||…)()` and would throw with the usual `AudioContext: undefined` stub — so the secret-code section uses an **audio-Proxy instance** (a chainable Web-Audio Proxy where `gain`/`frequency`/`destination` resolve to the proxy, `currentTime` is a number, every method returns the proxy) whose `addEventListener` **captures the real keydown listener**, while every other section uses a plain `AudioContext: undefined` instance (AudioSys stays null-guarded, `loop()`/music/draw all no-op) with a **controllable injected `performance` clock** to drive the idle timeout deterministically; the startup default/restore path is exercised by building **fresh instances seeded with a given `localStorage` blob** (so the module's own seed + startup `loadSettings()` run against it). Coverage: `applyDebug` display↔native round-trip (ms/1000) + registry-shape sanity; `menuDebug` adjust clamping at both rails (and a single-step nudge); a real `saveSettings`→(clobber)→`loadSettings` cycle round-tripping `debug.autoShieldRegenPause`, plus seeded-fresh-load of a valid value (restored) and out-of-range/non-numeric/negative/missing-sub-object values (all snapping to the seeded default); the item-5 consumer via the REAL `damageShip` auto-shield branch arming `autoShieldRegenLock` to the live `DEBUG.autoShieldRegenPause` (default 1.0 s, then re-read at 3.0 s after a knob change — proving the const→live repoint); `drawDebug()`/`menuDebug()` no-throw headless with Back/back return routing (title→closePause, paused→root); the `loop()` idle path disarming after `DEBUG_CODE_IDLE_MS`; and the secret-code state machine on the REAL captured keydown listener — backtick arms, the full code opens `"debug"` from title (openDebug) and from a paused game (gotoScreen), a wrong stream never opens, interleaved modifier/arrow keys never pollute `buf`, and the code is inert mid-play / while rebinding. **`test-cs013-p4.js` updated — a legitimate consequence of the consumer repoint, not a bug it caught:** its section-(A) source-marker regex pinned the old literal `s.autoShieldRegenLock = AUTO_SHIELD_REGEN_PAUSE`; repointed it to match `= DEBUG.autoShieldRegenPause` (the arm site as of this phase) with a comment noting the default is unchanged (1.0 s), so its behavioral sections B–F (which check the runtime lock equals `AUTO_SHIELD_REGEN_PAUSE`) stay green untouched. Full regression green: **57 test files, 4,694 assertions** (56 prior + this session's new 62-assertion file), `node --check` clean. **No `GAME_VERSION` bump** — P4 of a multi-phase round, the bump rides whichever phase lands last (`FLAG-CS015-g`), stays `"1.0.0.13"`. **No genuinely new fork surfaced** — the three P4 flags were all pre-resolved best-guesses in the planning doc and followed as specified: FLAG-CS015-c (nested `debug` sub-object in `afd_settings_v1`, not a new key), FLAG-CS015-h (single `DEBUG_VARS` registry driving render/adjust/persistence), FLAG-CS015-e (make `AUTO_SHIELD_REGEN_PAUSE` tunable, default 1000 ms = today's behavior). **Playtest ask, NOT resolvable headless:** the panel's exact layout/legibility, the secret-code feel (arm/idle-timeout window length, beep pitches), and whether dialing the regen pause up actually makes auto-shield losable are Paul's in-browser calls — the constants (`DEBUG_CODE_IDLE_MS`, the beep frequencies, the panel width/height math) are all one-line tunables. Docs: GDD new **§2.19 "Debug Options"** (registry-is-DATA, the first knob, secret entry, the panel, persistence location) + the `> Structure` pointer; Architecture Map — Constants row (`DEBUG_VARS`/`DEBUG`/`debugShown` + the secret-entry consts, marked structural), AudioSys row (`secretArm`/`secretDisarm`), Input row (the `DebugCode` secret-code keydown block), Menu row (the `"debug"` screen + `openDebug`/`menuDebug`/`drawDebug`/`debugReturn`), Main-loop row (the idle-timeout tick); §2.16 persistence bullet gains the additive `debug` sub-object; top-of-file Current-build → CS015 P4 (`GAME_VERSION` unchanged), `GDD-VERSION-HISTORY.md` (new CS015 P4 entry appended), this file. Did NOT push.

**Prior session (CS015 P3):** implemented CS015 Phase 3 (items 9, 8 of `PLANNED-FEATURES-CS015.md`, per `IMPLEMENTATION-PHASES-CS015.md`), reading `STATUS.md` and `CLAUDE.md` first as required. Re-grepped every anchor before editing (all confirmed at or near the prompt's own estimates, no drift worth flagging): the wave-clear condition (actual L5108 pre-edit, `game.debris.length === 0 && game.hunters.length === 0`), `nextWave()` (actual L3642 — confirmed it clears neither `game.hunters` nor `game.garbage`, only resets per-wave stats/timers, relocates the dock, and spawns fresh Debris), the powerup pickup test (actual L4839–4841 pre-edit, `const r = p.radius + SHIP_RADIUS; if (dist2(p, game.ship) < r*r) {...}`), `inScoopBox(g)` (actual L3830 — confirmed it reads `g.x`/`g.y` generically and returns `false` unconditionally at `scoopLevel` 0), and confirmed by full-file grep that there is no second wave-clear path anywhere. **Item 9 — wave clears on debris-empty alone.** Dropped the `&& game.hunters.length === 0` clause from the wave-clear gate (now L5110): `if (game.debris.length === 0) {`. The surrounding comment was rewritten to record that Hunters and loose garbage carry over by design and can accumulate if neglected. `nextWave()` was **not** touched — it never wiped `game.hunters`/`game.garbage` even before this phase, so this is a pure gate loosening: a Hunter (or several concurrent lineages) can now survive from one wave straight into the next, carried under a freshly-spawned Debris batch, instead of being force-required-dead before the wave advances. **Item 8 — the Scoop mouth now also captures powerups.** The pickup test (now L4842) widened from `dist2(p, game.ship) < r*r` to `dist2(p, game.ship) < r*r || inScoopBox(p)` — one added clause, zero changes to `inScoopBox` itself (it was already generic over any `{x,y}`-bearing object). Scoop only, per the phase prompt — no Magnet-style pull or multiplier was added for powerups, and the base circle is completely unchanged; at `scoopLevel` 0 `inScoopBox` stays unconditionally `false`, so the change is a no-op until the player has picked up at least one Scoop level. **New test `scratchpad/test-cs015-p3.js`** (23 assertions, drives the REAL `update()`/`nextWave()`/`inScoopBox()`/`applyPowerup()` — no reimplemented game logic): **(B)** with `game.debris` forced to `[]` and a live Hunter + a loose garbage piece placed at the wrap-toroidal antipode of the ship's spawn point (the maximum possible separation on this world, so neither the Hunter's slow "last stand" drift nor the ship can close that gap inside the ~2.6 s the real wave-clear timer takes to fire, and neither hazard is reachable by the player's — nonexistent, since no keys are simulated — bullets either), driving the real `update()` loop in 0.1 s steps until `game.wave` advances (the same idiom `test-f9.js` already uses to trigger a real `nextWave()`): the wave advances by exactly 1, the *same* Hunter and garbage instances (`Array.includes` identity, not a count) survive into the new wave and are still alive, and fresh Debris is added on top. **(C)** a powerup positioned just outside the base pickup circle (`r = p.radius + SHIP_RADIUS`, read off the real constructed instance rather than assumed, since `POWERUP_RADIUS`'s lever (`LEVER_POWERUP_SIZE`) is disabled-but-still-scaling at 2×) is NOT collected at `scoopLevel` 0 — confirmed both by a direct `inScoopBox(p) === false` sanity check and by driving a real `update()` frame and observing `p.dead` stays `false` and `game.stats.powerupsPicked` doesn't move; the identically-derived placement (same forward-axis math, `scoopLevel` bumped to 5 for the widest mouth) but chosen to land inside `SCOOP_DEPTH[5]`/`SCOOP_WIDTH[5]` while still outside its own `r` IS collected — `p.dead` flips `true`, `powerupsPicked` increments by exactly 1, and the rolled `"rapid"` type's `game.powerFx.rapid` actually gained duration, confirming the real `applyPowerup` ran (not just that the entity died). Full regression green: **56 test files, 4,632 assertions** (55 prior + this session's new 23-assertion file), `node --check` clean. **No `GAME_VERSION` bump** — P3 of a multi-phase round, the bump rides whichever phase lands last (`FLAG-CS015-g`), stays `"1.0.0.13"`. **Flagged, not resolved — a genuine design question the phase prompt didn't cover, surfaced rather than silently decided:** `HunterSatellite`'s speed/turn-rate ceiling (`ramp(ceiling * HUNTER_FLOOR_FRAC, ceiling, wave)`) is sampled once at spawn and never re-sampled. Before this phase that was harmless by construction — Hunters used to block wave-clear, so a Hunter's lifetime could never outlast the wave it spawned in. As of this phase a carried Hunter can now fight at its *spawn* wave's difficulty long after the game has moved several waves past it (e.g. a Hunter surviving wave 3 → wave 8 still moves at wave-3 speed/turn scaling) — noted in GDD §2.5 for Paul/a future session to decide whether that should re-sample against the current wave on carry-over, not decided here. Docs: GDD §2.7 (wave-clear gate description rewritten to `game.debris.length === 0`; the "still able to rise mid-wave" bullet rewritten to describe Hunters/garbage carrying over rather than blocking, and to note `nextWave()` was never the thing that changed), §2.5 (the Hunter difficulty-scaling bullet's stale "Hunters block wave-clear" parenthetical corrected, with the carried-Hunter staleness consequence above flagged inline), §2.14 (the Pickup/lifecycle bullet gains the `|| inScoopBox(p)` clause and the no-Magnet-multiplier note), §2.14.1 (a new bullet under the Scoop's intro paragraph documenting the mouth now capturing powerups), top-of-file Current-build → CS015 P3 (`GAME_VERSION` unchanged), `GDD-VERSION-HISTORY.md` (new CS015 P3 entry appended), this file. Did NOT push.

**Prior session (CS015 P2):** implemented CS015 Phase 2 (item 3 of `PLANNED-FEATURES-CS015.md`, per `IMPLEMENTATION-PHASES-CS015.md`), reading `STATUS.md` and `CLAUDE.md` first as required. Re-grepped every anchor before editing (all confirmed at or near the current file, no drift worth flagging): `drawAchRow` (actual L5460 pre-edit, both the tiered and single-goal branches), `ACH_SCALE`/`ACH_ROW_STEP` (actual L2249/L5397 pre-edit), `achMaxScroll()` (actual L5406), the achievements clip-region consts (`ACH_PANEL_W/H`, `ACH_PANEL_Y`, `ACH_ROW0_Y`, `ACH_ROW_CLIP_TOP/BOTTOM`, `ACH_ROW_VISIBLE_H`, actual L5394–5400 pre-edit). **The bug:** CS013 P3's ×1.5 readability pass grew every row's fonts but left name (left-aligned) and status/tier-medal text (right-aligned) sharing one baseline inside the same `colW` (350) — at the bigger size a long name and a long status string could visually run into each other in the middle of the row. **The fix — uniform three-line rows in both `drawAchRow` branches, exactly per the phase prompt:** two new tuning consts, `const ACH_STATUS_DY = 24;` and `const ACH_DESC_DY = 46;`, grouped with the other achievement-geometry consts (`ACH_ROW0_Y`/`ACH_ROW_STEP`/etc., L5394–5402 cluster). Name still draws at `ry` (unchanged); status now draws at `ry + ACH_STATUS_DY` (still right-aligned at `x + w`, just no longer sharing the name's line); description moved from a bare `ry + 22` to `ry + ACH_DESC_DY`. `ACH_ROW_STEP` bumped from `40 * ACH_SCALE` to `48 * ACH_SCALE` (at the shipped `ACH_SCALE` = 1.5 that's 60px→72px) so the third stacked line seats without touching the next row's name line. `achMaxScroll()`'s content-height tail term — which has to agree with what the renderer actually draws or the scroll ceiling either clips content or over-scrolls into empty space — changed from a bare `22 + 10 * ACH_SCALE` to `ACH_DESC_DY + 10 * ACH_SCALE`, so it reads the same named constant the renderer now uses instead of carrying its own hardcoded copy of the old offset. **`ACH_STATUS_DY`/`ACH_DESC_DY` are browser-tuned look-calls** (picked by reasoning about approximate font-ascent/descent margins at the shipped sizes, not measured against a real rendered frame) — flagged per the phase prompt's own note for Paul to nudge in playtest if the stack reads too tight or too loose. **New test `scratchpad/test-cs015-p2.js`** (179 assertions, drives the REAL `drawAchievements`/`drawAchRow`/`achMaxScroll`/`menuAchievements` via a recording-canvas stub — mirrors `test-cs013-p3.js`'s canvas-recording idiom, no menu-render or scroll-clamp logic reimplemented): every row's name/status/desc `fillText` lands at exactly `ry` / `ry + ACH_STATUS_DY` / `ry + ACH_DESC_DY` across both the weekly column and both lifetime columns, covering BOTH the tiered and plain `drawAchRow` branches; status stays right-aligned and every size is unchanged from CS013 P3 (15/13-or-14/10 × `ACH_SCALE`); the stacking order holds (`0 < ACH_STATUS_DY < ACH_DESC_DY < ACH_ROW_STEP`); `achMaxScroll()`'s real return value is cross-checked against an independent recompute built from the exported real symbols (`ACH_ROW_STEP`/`ACH_DESC_DY`/`ACH_SCALE`/`ACH_ROW_VISIBLE_H`) — the "pull the real per-row height and the value `achMaxScroll()` assumes from the real symbols and assert they match" check the phase prompt asked for, catching render geometry and the scroll-ceiling formula ever drifting apart — plus `achMaxScroll() >= 0` and `menuAchievements("up"/"down")` clamping `game.menu.scroll` to `[0, maxScroll]`; headless `drawAchievements()` no-throw at scroll 0 and at `scroll === maxScroll`, with both a tiered and a plain (non-tiered) row confirmed present in the pool so both `drawAchRow` shapes actually render; the standard `AudioSys.ctx`-null `startGame()`/`update(1/60)` + full open/scroll/close-cycle smoke, from both `playing` and `gameover`. **`test-cs013-p3.js` updated — a legitimate consequence of the layout change, not a bug the update is masking:** sections (B) and (C) had pinned the old shared-line geometry (`step === 60` hardcoded; status/readout asserted at the name's own `(x, ry)`; description asserted at a bare `ry + 22`). Added `ACH_STATUS_DY`/`ACH_DESC_DY`/`ACH_ROW_STEP` to the file's `RETURN` list and repointed every position assertion to read the real constants instead of re-deriving stale literals — no assertion's *intent* changed (sizes/contrast/scroll-clamp/clip-bracket coverage all identical), only the Y-positions they read off the real render. Re-ran that file standalone before and after to confirm it still fails the way it should if the geometry regresses (spot-reverted one offset by hand, confirmed section B fails, restored). Full regression green: **55 test files, 4,609 assertions** (54 prior files + this session's new 179-assertion file; `test-cs013-p3.js`'s count is unchanged at 267 — only its assertions' Y-positions were repointed, not their count), `node --check` clean. **No `GAME_VERSION` bump** — P2 of a multi-phase round, the bump rides whichever phase lands last (`FLAG-CS015-g`), stays `"1.0.0.13"`. **Docs drift noted, not silently followed:** the phase prompt named the target section "GDD §2.16" for this note, but the Achievements viewer's content has lived at **§2.17** since CS013 P3's own docs pass (§2.16 is Pause Menu/Options/Rebinding) — edited §2.17 (the correct, current section) rather than propagate the stale number; flagging here per the standing "flag drift, don't resolve silently" convention (mirrors the CS015 P1 session's `IMPLEMENTATION-PHASES-CS015.md` draft-mismatch flag). Docs: GDD §2.17 (the "Notification + viewer" bullet's CS013 P3 sentence gains a "Row-layout fix (CS015 P2)" continuation describing the three-line stack + the `achMaxScroll()` tail-term change; the tuning-constants pointer paragraph gains `ACH_STATUS_DY`/`ACH_DESC_DY`), top-of-file Current-build → CS015 P2 (`GAME_VERSION` unchanged), `GDD-VERSION-HISTORY.md` (new CS015 P2 entry appended), this file. No Architecture Map (§3) edit — the achievement geometry consts aren't listed there, only in §2.17's own tuning-constants footnote (consistent with how CS013 P3 handled the same consts). Did NOT push.

**Prior session (CS015 P1):** implemented CS015 Phase 1 (items 1, 2 of `PLANNED-FEATURES-CS015.md`, per `IMPLEMENTATION-PHASES-CS015.md`), reading `STATUS.md` and `CLAUDE.md` first as required. **New round:** CS014 was built then reverted (its version tag stays reserved — `.14` is deliberately skipped, not lost — so this round targets `GAME_VERSION` "1.0.0.15", hosted on whichever phase lands last per `FLAG-CS015-g`). Re-grepped every anchor before editing (all within a line or two of the planning doc's own CS013-build estimates): `drawRootMenu` (actual L5201, the `menuPanel(360, 300, …)` call + the `drawMenuHint` hint call), `MENU_HINT_SIZE`/`drawMenuHint` (actual L2248/L5150), the Scoop HUD block (actual L5915–5920 pre-edit — `drawRingSegments(..., POWERUP_COLOR.scoop, COLOR.dim)` + two `drawText` calls), and a timed powerup row's `active ? POWERUP_COLOR[t] : COLOR.dim` convention (actual L5860) as the pattern to mirror. **Item 1 — the root/PAUSED panel didn't fit its own control hint.** CS013 P2 bumped the footer from size 12 to `MENU_HINT_SIZE` (16) but never widened the panel it renders inside (`menuPanel(360, 300, …)`); the ~43-char hint at 16px monospace measures wider than 360 and bled past both borders. Rather than hand-pick a new fixed width, hoisted the hint text to a `const ROOT_MENU_HINT` and added `const ROOT_MENU_HINT_MARGIN = 28` (playtest knob); `drawRootMenu` now sets `ctx.font = MENU_HINT_SIZE + "px monospace"`, measures `ROOT_MENU_HINT` via `ctx.measureText`, and passes `Math.max(360, Math.ceil(measuredWidth) + 2*ROOT_MENU_HINT_MARGIN)` as the panel width — floors at the old 360 for a short hint, grows for a wide one, self-heals if the hint or font size ever changes again. Height, the fixed 46px row step, and `menuPanel`'s internals are all untouched — only the `w` `drawRootMenu` passes in changed. Since both `MENU_ROOT_PLAY` (paused mid-game) and `MENU_ROOT_OVER` (gameover, CS013 P1) render through the one `drawRootMenu`, the fix covers both roots identically. **Item 2 — Scoop HUD row now follows the active/idle color convention (FLAG-CS015-b resolved as specified).** The segmented ring's lit segments, the `SCOOP` label, and the level number were hardcoded `POWERUP_COLOR.scoop` (violet) at every level including 0 — the one bright row among otherwise-dim ones at rest, unlike the four timed rows which already dim to `COLOR.dim` when inactive. Added `const scoopCol = game.scoopLevel > 0 ? POWERUP_COLOR.scoop : COLOR.dim;` and repointed all three (litColor, label, number) to it — mirrors the timed rows' `active ? POWERUP_COLOR[t] : COLOR.dim` rule exactly. The dim ring track itself stayed `COLOR.dim`, unchanged. No new fills either change (GDD §3.2). **New test `scratchpad/test-cs015-p1.js`** (23 assertions, drives the REAL `drawRootMenu`/`drawHUD` via a recording-canvas stub — mirrors `test-cs013-p2.js`'s canvas-recording idiom, extended with a deterministic `measureText` mock so the panel-width formula can be verified against real symbols, not re-derived): panel width equals `max(360, measured+2*margin)` at a default (8px/char) measure; floors at 360 for an artificially narrow measured hint; grows past 360 for an artificially wide one; Scoop label/level-number colors are `COLOR.dim` at level 0 and `POWERUP_COLOR.scoop` at level ≥1 (read off the real HUD render at the fixed row-0 position — `HUD_FX_BASE_Y ± {4,14}` — not a re-derived expression); headless `draw()` no-crash at title (unpaused and with the Options overlay open) and mid-game paused on the root screen. **`test-cs013-p2.js`'s recording-ctx stub needed one addition, a legitimate consequence of this phase, not a bug:** `drawRootMenu` now calls `ctx.measureText(...).width` before ever reaching `menuPanel`, and that stub's prior "unhandled method → no-op returning `undefined`" fallback made `undefined.width` throw the moment its own drawRootMenu-rendering section (C) ran; added a `measureText` mock (`fontSize*0.6*length`, a plausible monospace-width stand-in) — no assertions in that file changed, since every checked fillText position/color is `VIEW_W/2`-centered and independent of panel width. **Checked the rest of the 53-file suite for the same landmine** (grepped for the generic `new Proxy({}, { get: () => () => {} })` no-op-canvas pattern combined with any `drawRootMenu`/`draw()`/pause-state exercise — 15 candidate files) and ran the full suite before and after the fix: every other file that reaches `draw()` headlessly does so only while unpaused/not-gameover, so none of them ever hit the new `measureText` call; `test-cs013-p2.js` was the only file that needed the addition. Full regression green: **54 test files, 4,429 assertions** (53 prior + this session's new file), `node --check` clean. **No `GAME_VERSION` bump** — P1 of a multi-phase round, the bump rides whichever phase lands last (`FLAG-CS015-g`), stays `"1.0.0.13"`. Docs: GDD §2.14.1 (Scoop HUD bullet extended with the active/idle color rule), §2.16 (new "Root panel width now fits its hint" note after the CS013 P2 readability-pass note; the CS013 P1 "the panel is still 360×300, no resize needed" sentence annotated as historical-at-the-time, since width is no longer fixed), top-of-file Current-build → CS015 P1 (`GAME_VERSION` unchanged), `GDD-VERSION-HISTORY.md` (new CS015 P1 entry appended), this file. Did NOT push. **Noticed, not touched:** `IMPLEMENTATION-PHASES-CS015.md` in the working tree (Paul's local copy, with FLAG-CS015-a/FORK-CS015-D marked RESOLVED and a "How to use this doc" section) differs from what's committed at `HEAD` (`c2c1bb9` "Docs cleanup for cs015", an older draft missing both) — this predates and is unrelated to this session's work; flagged to Paul rather than resolved unilaterally, since it's ambiguous which draft should win and reconciling it isn't this phase's job.

## GDD version history

- **CS015 (P1–P7) — Pause-panel/Scoop-HUD polish, Achievements row-stack fix, wave-clear/Scoop-pickup loosening, a hidden Debug Options panel (six knobs), tunable garbage lifetime, and clearer chain-broken voice lines.** `IMPLEMENTATION-PHASES-CS015.md`, the whole changeset (CS014 was built then reverted — its version tag is kept, `.14` deliberately skipped, so this round targets `"1.0.0.15"`). **P1 — Pause-panel width fit + Scoop HUD indicator color:** `drawRootMenu` now measures its own hint text (`ctx.measureText`) and sizes the panel to fit instead of a hardcoded 360px, fixing hint-text bleed introduced by CS013 P2's larger hint font; the Scoop HUD row's lit-violet/dim color now follows the same `active ? POWERUP_COLOR.scoop : COLOR.dim` convention the four timed rows already use (FLAG-CS015-b resolved), instead of always being lit. **P2 — Achievements viewer three-line rows:** name/status/description each get their own line (`ACH_STATUS_DY`=24, `ACH_DESC_DY`=46, `ACH_ROW_STEP` 40→48×`ACH_SCALE`), fixing CS013 P3's ×1.5 pass where a long name and long status string could collide on one shared baseline; `achMaxScroll()`'s tail term repointed to the same consts the renderer uses. **P3 — wave-clear loosens, Scoop mouth gains powerups:** the wave-clear gate drops its `hunters.length === 0` clause (debris-empty alone now advances the wave; Hunters/loose garbage carry over into the next wave by design — flagged, not fixed, that a carried Hunter keeps its stale spawn-wave difficulty ramp); the Scoop capture box (`inScoopBox`) now also collects powerups, not just garbage. **P4 — Debug Options foundation (FLAG-CS015-h):** a data-driven `DEBUG_VARS` registry (`{id,label,unit,def,min,max,step,toNative?}`) backs a hidden `"debug"` menu screen, opened by a secret keyboard code (`EvilG3niu$`, arm/idle-timeout + beeps) from the title or a paused game; persists as an additive `debug` sub-object on the frozen `afd_settings_v1` key (FLAG-CS015-c). First knob: Auto Shield Regen Pause (`autoShieldRegenPause`, default 1000ms = prior `AUTO_SHIELD_REGEN_PAUSE` behavior). **P5 — four more knobs (registry-only, no new machinery):** Scoop hits-per-level, garbage attraction delay/radius/force — all defaulting to the pre-existing shipped consts, so default behavior is unchanged. **P6 — garbage lifetime becomes tunable and universal (FLAG-CS015-a resolved: default 10s, not biased toward the old 22):** every garbage piece (not just un-merged singles) now counts down via the sixth knob `garbageLifetime`; a coalesce merge resets the survivor's clock to the live knob value, so an actively-growing lineage never dies of age but a stalled one does; `GARBAGE_DECAY` (22) stays only as a historical comment. `GAME_VERSION` bumped `"1.0.0.13"` → `"1.0.0.15"` at P6 (FLAG-CS015-g: rides whichever phase lands last; P7 was still lab-gated at the time). **P7 — chain-broken voice lines replaced (item 6), round closed:** the original CS011 P5 `chain_broken` set ("Junk is gone!" etc.) didn't say *what* was lost or that it was worth points; `PLANNED-FEATURES-CS015.md`'s FORK-CS015-D approved a functional/matter-of-fact replacement set, but before it could ship, an out-of-band commit (`c5eae7b`, made directly by Paul outside any build session) had already hand-edited `VOICE_LINES.chain_broken` in the live build to a different five-line wording with placeholder, non-ARPAbet `phon` strings (e.g. `"Payload DAM-ijd."`) that would have parsed as near-total `errs` and played as broken/near-silent audio — caught by this phase's gate check (re-grepping `tools/voice-robot-lab.html` for the FORK-CS015-D text found nothing composed there either) and surfaced rather than silently overwritten or silently shipped. Paul's call: keep `c5eae7b`'s wording, compose its `phon` in the lab himself, and hand the five verified `{text,phon}` pairs back. **Final shipped set** (pasted verbatim, zero parse errs confirmed via the real `parsePhonTokens`/`buildUtterance`): `Payload damaged.` / `Payload disrupted.` / `Payload lost.` / `Payload adrift.` / `Payload is getting away.` — `VOICE_PRIORITY.chain_broken` unchanged (2). No `GAME_VERSION` change at P7 (already `"1.0.0.15"` since P6). Headless: seven new test files across the round (`test-cs015-p1.js` through `-p7.js`; P7 is 44 assertions covering the exact pasted text/phon, zero-err parsing, `VOICE_PRIORITY`, ctx-null headless-safety, and the version pin); full regression green at round's end: **60 test files, 4,844 assertions**, `node --check` clean. Docs: GDD §2.14.1/§2.16 (P1), §2.17 (P2), §2.5/§2.7/§2.14 (P3), new §2.19 "Debug Options" + Architecture Map rows (P4–P6), §2.8's voice-lines note + `chain_broken` register description (P7); top-of-file Current-build → CS015 P7/`"1.0.0.15"`; STATUS.md → CS015 round complete. Did NOT push.
