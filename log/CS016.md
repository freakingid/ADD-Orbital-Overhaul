# CS016

**Prior session (CS016 P4, landed):** implemented CS016 Phase 4 (per `IMPLEMENTATION-PHASES-CS016.md` P4, spec `PLANNED-FEATURES-CS016.md` §4/§5), reading `STATUS.md` and `CLAUDE.md` first as required, and grepping every named anchor in context before editing — `MENU_ROOT_PLAY`, `MENU_ROOT_OVER`, `rootItems`, `menuRoot`, `drawRootMenu`, `DIFFICULTY_ROWS`, `menuDifficulty`, `drawDifficulty`, and the `COLOR` block (confirming the existing `COLOR.menuIdle`/`COLOR.dim` distinction the prompt named). **One new shared visual idiom, built once and used by two independent consumers: a menu row that is visible and focusable but cannot be actioned** — the cursor lands on it (and, for a value row, still shows its current value, so the player can read it), but the action is inert (no state change, no `saveSettings()`, no `AudioSys.ui()` confirmation blip — silence is the tell that nothing happened) and it renders `COLOR.dim` rather than the usual `COLOR.text`(selected)/`COLOR.menuIdle`(unselected) pair, even while focused. **(1) SAVE ROW.** `MENU_ROOT_PLAY` grows `["Continue","Options","Quit"]` → `["Continue","Save","Options","Quit"]`; `MENU_ROOT_OVER` is untouched (no run in progress at gameover for a save to apply to). `menuRoot()` gets no branch for `"Save"` — an unmatched label already falls off the label-dispatch if/else chain as a no-op, which is the inert half of the idiom for free — and `drawRootMenu` special-cases the label to force `COLOR.dim` regardless of the selected/unselected state every other row uses. **`drawRootMenu`'s panel height is now DERIVED from `items.length`** rather than hardcoded 300 — the CS015 P1 self-healing precedent (which only ever touched width) extended to height: rows still start at `y+118` and step 46px apart; the hint sits 62px below the last row's baseline (the extra breathing room the original design already had past the row step); the panel keeps 28px below the hint. Formula: `h = 118 + (N-1)*46 + 62 + 28`, which reproduces the old fixed 300 exactly at `N=3` — so the 3-row gameover root and the new 4-row pause root both fit through the one function with no branch, and neither layout needed re-verifying by hand. **(2) DIFFICULTY LOCK.** `menuDifficulty` computes `locked = game.state === "playing" && row !== "back"` and gates the left/right branch on `!locked` (up/down navigation is untouched, so a locked row stays fully reachable and readable). Keyed on `game.state === "playing"` specifically — NOT `game.paused` (the title also carries `paused === false` as of CS016 P2, so that alone can't distinguish "no run" from "a live run") and NOT gameover (the run is already over; changing settings for the next one is harmless and stays allowed). `drawDifficulty` renders all three value rows (label, both toggle sides, the focused row's ◄► chevrons) `COLOR.dim` while locked regardless of which one has focus, and swaps the per-row help line for a new constant, `DIFFICULTY_LOCK_HELP` ("Difficulty can't be changed during a run.") — routed through the SAME `drawMenuHint` call the three per-row tips already used, not a second help mechanism, exactly as the prompt asked. `"back"` is excluded from the lock at both the input and render layers — it stays fully live at every `game.state`. **Tests:** new `scratchpad/test-cs016-p4.js`, **106 assertions** across (A)–(H), driving the REAL `menuRoot`/`menuDifficulty`/`drawRootMenu`/`drawDifficulty` via a canvas-recording stub (the `test-cs013-p2.js` idiom) — no menu logic reimplemented. Coverage: (A) `node --check` + config shapes (`MENU_ROOT_PLAY` 4 rows with Save at index 1, `MENU_ROOT_OVER` still 3 rows with no Save); (B) the cursor CAN land on Save, confirming it is a byte-identical no-op (state/paused/screen/index all unchanged, zero storage writes), and Save renders `COLOR.dim` both focused and unfocused; (C) the derived panel height fits every row + the hint for BOTH the 4-row pause root and the 3-row gameover root, read off the real `strokeRect` bounds rather than a re-derived formula, plus a check that the two panels are genuinely different heights (not a coincidental 300 that happens to also fit 4) and that the untouched 3-row case still reproduces exactly 300; (D) locked Difficulty (`game.state "playing"`) — left/right/left on each of the three value rows leaves `settings.shotPowerupMode`/`magnetMode`/`autoShield` byte-identical and fires zero `localStorage.setItem` calls (spied); (E) unlocked Difficulty from BOTH `"gameover"` and `"title"` — the same presses DO flip the settings and DO persist, proving the lock doesn't leak outside a live run; (F) the Back row navigates normally in both states; (G) render — locked rows (label/toggle-sides/chevrons) all read `COLOR.dim` and show `DIFFICULTY_LOCK_HELP`, unlocked rows keep the normal selected/unselected contrast and show the normal per-row tip, Back's own contrast is unaffected by the lock; (H) an `AudioSys.ctx` null smoke across the full Save + Difficulty-lock cycle. **Grepped `scratchpad/` for existing assertions on root-menu row indices and Difficulty navigation before editing, as instructed, and found MORE than the prompt's own example (`test-p5.js`) flagged — nine files total needed changes, listed and explained:** four files (`test-cs010-p4.js`, `test-cs012-p4.js` [×2 sites], `test-cs013-p1.js` [×2 sites], `test-cs016-p2.js`) pinned `MENU_ROOT_PLAY`'s exact pre-P4 3-item shape via a hardcoded `["Continue","Options","Quit"]` literal — each updated to the new 4-item shape (most re-pointed to assert against the live `MENU_ROOT_PLAY` constant instead of a re-hardcoded array, so a future insertion can't cause the same drift a second time). Two files (`test-f8.js` §A/§F, `test-p4.js` §E) navigated the pause root by counting a FIXED number of `"down"` presses to land on "Options" — the inserted Save row shifted that by one, so a bare index/press-count assertion silently landed on the wrong row; `test-f8.js` was extended to explicitly assert the cursor passes through Save inertly before reaching Options (one extra Down + one extra release), `test-p4.js` switched to `.indexOf("Options")`. Four files (`test-p5.js` §I, `test-cs012-p5.js` §H, `test-cs013-p2.js` §F, `test-v34-p6.js` §O) drove the Difficulty toggle from a live `"playing"` state — a scenario the new lock legitimately intercepts — each repointed to an unlocked `"gameover"` state rather than weakened, since verifying the toggle/persist mechanics (not the lock) was always their actual point; the lock itself is exhaustively covered by the new P4 file. `test-cs013-p2.js` §C (drawRootMenu render geometry) also needed rework beyond the state fix: it selected "Options" by a hardcoded index (1, valid only in the old 3-item array) and computed panel `y` from the old fixed height (300) — both replaced with `rootItems().indexOf("Options")` and a read of the real `strokeRect` box off the render log, plus a `COLOR.dim`-when-Save special case in its per-item color assertion. **Full regression: 64 test files, 5,533 assertions, 0 failures** (was 63/5,420), `node --check` clean. Docs: GDD (top-of-file Current build → CS016 P4, §2.16 — the `MENU_ROOT_PLAY` literal + panel-height text updated in place, the Difficulty-screen bullet gains the lock addendum, a new unavailable-row-idiom blockquote alongside the CS016 P2/P3 ones, the Architecture Map **Menu** row), `GDD-VERSION-HISTORY.md` (a CS016 P4 entry), this file. `GAME_VERSION` NOT bumped — still "1.0.0.15", P5 carries it. Did NOT push.

**Prior session (CS016 P3, landed):** implemented CS016 Phase 3 (per `IMPLEMENTATION-PHASES-CS016.md` P3, spec `PLANNED-FEATURES-CS016.md`), reading `STATUS.md` and `CLAUDE.md` first as required, and grepping every named anchor in context before editing — `menuInput` (specifically its `if (game.menu.rebinding) return;` exclusive-mode guard, the precedent this follows), `menuRoot`, `menuControls`, `returnToDefaults`, the `game` literal's `menu:` field, `gotoScreen`, `closePause`, `startGame`'s reset, `drawMenu`, `menuPanel`, `drawMenuHint`, plus `drawRootMenu`'s CS015 P1 measured-width block as the sizing precedent. **New machinery: the build's first modal dialog**, built generic and wired to exactly two consumers. **(1) STATE.** `game.menu` gains `modal` — `{text, confirmLabel, cancelLabel, index, onConfirm}` when open, `null` otherwise. Added to **BOTH** `game.menu` object literals (the `game` object's and `startGame()`'s reset), which is the direct answer to the latent `scroll` divergence CS016 P2 logged in Known issues below — that note said a P3/P5 field must land in both or inherit the same bug, and it does; the reset literal carries a comment saying why. Cleared at every *menu-state reset* that clears `rebinding`: `openPause`, `closePause`, `gotoScreen`, `quitToTitle`, `openDebug`, and **`returnToTitleMenu`** — the site CS016 P2's own hand-off note flagged as one the P3 prompt could not name (the helper post-dates the plan). **Deliberately NOT cleared** by the three *capture-specific* teardowns (`captureKeyRebind`, `capturePadRebind`, and the keydown Esc-cancel at the top of the rebind branch): those end a hardware capture, not a menu state, and clearing a dialog there would be a category error. **(2) HELPER.** `openModal(text, confirmLabel, onConfirm, cancelLabel = MODAL_CANCEL_LABEL)` — sets the field and plays the confirm blip; `cancelLabel` defaults to `"CANCEL"` via a default parameter, so the constant is the single source of truth and an explicit label still overrides it. **`index` defaults to 1 (CANCEL), and that is the phase's safety property, not a tidy-able default** — both consumers are destructive and ENTER is the same key that *opened* the dialog, so with index 0 a double-tap (or a held key that got past the v3.5 P1 `e.repeat` guard) would wipe a run or a whole rebind set with no deliberate act. The player must move the cursor to destroy something; the test asserts it explicitly and also exercises it (open, then ENTER, then assert the callback did not run). **(3) INPUT.** `menuInput` gained a second exclusive-mode guard, `if (game.menu.modal) { menuModal(action); return; }`, placed **after** the `rebinding` early-return and **before** the screen `switch`. **The order is the contract**: a live rebind capture means the very next keypress *is* data rather than navigation, so it must out-rank a dialog; the two are mutually exclusive in practice (you cannot reach a modal's opener while capturing, nor a rebind row while a dialog is up), which is exactly why the ordering is asserted in source as well as behaviour rather than left to observation. Placing it before the switch is what keeps every per-screen handler ignorant of modals — none needed an edit. `menuModal(a)`: any of up/down/left/right toggles `index` 0↔1 (two rows, so every direction is the same toggle — a stick nudge never reads as dead input); `confirm` on row 0 **captures `onConfirm`, clears the modal FIRST, then invokes** (a callback that navigates — `quitToTitle` re-arms the title menu, `gotoScreen` changes screen — must not be clobbered by a teardown running after it; the test proves the callback observes `modal === null`, and that a callback which opens a *second* dialog keeps it); `confirm` on row 1 dismisses with the back tone; `back` dismisses; **`pause` is treated as cancel and never passed through** — it must never close the whole menu out from under an open confirmation, which is precisely what `menuControls`' own `pause → closePause()` would otherwise have done. Unknown actions (e.g. the keyboard-only `"options"`) fall off the if/else chain as inert no-ops rather than reaching the screen beneath. **(4) RENDER.** `drawModal()` is called **unconditionally as the last act of `drawMenu()`** and self-gates on the field, so ordering it last is what puts a dialog on top of whichever panel raised it without inventing a z-index concept. It reuses `menuPanel()` for the box and `drawMenuHint()` for the footer, and draws two rows in the shared `"▶ "` prefix / `COLOR.text`-vs-`COLOR.menuIdle` convention. **Width is measured, not hardcoded** (the CS015 P1 self-healing precedent named in the prompt): both the per-consumer prompt *and* the fixed `MODAL_HINT` are measured with `ctx.measureText` at their real render fonts, the wider wins, plus `MODAL_MARGIN` on each edge, floored at `MODAL_MIN_W` — a longer prompt added later widens the box instead of bleeding past the border. No extra backdrop dim is painted: `drawMenu` already laid one over the world and `menuPanel`'s own 0.95 fill hides the panel underneath. New `MODAL_*` constant block sits beside `ROOT_MENU_HINT` (`MODAL_TITLE` "CONFIRM", `MODAL_CANCEL_LABEL`, `MODAL_HINT`, `MODAL_MIN_W` 420, `MODAL_MARGIN` 28, `MODAL_H` 300, `MODAL_TEXT_SIZE` 20 / `MODAL_TEXT_Y` 110, `MODAL_ROW_Y` 172 / `MODAL_ROW_STEP` 46 / `MODAL_ROW_SIZE` 24, `MODAL_HINT_Y` 272) — **the height, row step and row size are `drawRootMenu`'s one-for-one on purpose**, so the dialog reads as the same family of panel rather than a new widget. **(5) CONSUMER ONE — Return to Defaults.** `menuControls`' `defaultsRow` branch was `{ returnToDefaults(); AudioSys.ui(true); }` and is now `openModal("Reset all controls to defaults?", "RESET", returnToDefaults)`; `returnToDefaults()` itself is byte-unchanged (and still leaves `shipTurnScale` alone — FLAG-10a intact, re-asserted). **(6) CONSUMER TWO — mid-run Quit.** `menuRoot`'s `"Quit"` label now opens `openModal("Quit to title? Progress in this run will be lost.", "QUIT", quitToTitle)`. **Gameover's `"Quit to Title"` stays UNCONFIRMED** and calls `quitToTitle()` directly — the run is already over and there is nothing left to destroy, so a dialog there would be pure friction on that screen's most-pressed row; because `menuRoot` dispatches by label and the two rows carry *different* labels, the split needed no `game.state` check, exactly as the prompt anticipated. **Tests:** `scratchpad/test-cs016-p3.js`, new, **177 assertions** across (A)–(K), driving the REAL `menuInput`/`menuModal`/`openModal`/`menuRoot`/`menuControls`/`returnToDefaults`/`drawMenu`/`drawModal` plus the REAL captured keydown listener and the REAL `handleGamepadMenu` with a fake pad — no dialog logic is reimplemented anywhere in the file. Coverage: (A) `node --check` + `modal: null` in both literals (source-asserted, not just runtime); (B) the CANCEL-default safety property, on a synthetic dialog and on both real consumers; (C) cancel/back dismiss without running `onConfirm`, confirm runs it exactly once, the clear-before-invoke ordering, and a null callback confirming cleanly; (D) `pause` cancels and leaves the underlying screen open and paused — checked on the Controls screen, where `pause` normally *does* `closePause()`, plus a sanity check that it still does once the dialog is gone; (E) rebind → CANCEL keeps the custom binding → reopen → CONFIRM restores `DEFAULT_BINDINGS` and writes the `afd_settings_v1` blob (payload inspected, not just "a key exists"); (F) mid-run Quit both ways, then the same flow end-to-end through the real keydown listener *and* the real gamepad path, including pad START inside a dialog cancelling rather than punching through; (G) gameover Quit-to-Title opens no dialog at any point, plus a sweep proving **no other row of either root and no other Controls row** grew one; (H) input priority over the screen beneath (neither `m.row`, `m.col` nor the root's `index` moves, and an unhandled action reaches nothing); (I) the guard order, asserted both behaviourally (both modes live → nothing moves, and nothing was queued) and by inspecting the source order of the two guards and the switch; (J) all seven clear sites, `draw()` with a dialog over each screen that can raise one, `drawModal()` drawing literally nothing when closed, and an `AudioSys.ctx === null` smoke run of the full open/cancel/confirm cycle; (K) render detail — measured self-healing width (a longer prompt widens the box; both real prompts fit with margins), the two rows' geometry/colour/prefix, the `drawMenuHint` footer, and proof the dialog's box is stroked *after* the panel beneath and its footer is the last text drawn. **Grepped `scratchpad/` for existing assertions on `returnToDefaults` and on `menuRoot`'s Quit branch before editing, as instructed: NO existing test file needed a change.** The three files touching `returnToDefaults` (`test-f8.js` §E/§F/§G, `test-cs010-p2.js` §C, and its `FULL` export list) all call the function **directly**, never through `menuControls`' row — and the function is unchanged, so they still pass verbatim. No test anywhere drives a confirm on the mid-run `"Quit"` row: the only Quit-row assertion in the suite is `test-cs013-p1.js` §D, which drives **`"Quit to Title"`** at gameover — the row this phase deliberately left ungated — so it too is untouched. The three files that name row indices on the Controls screen (`test-cs010-p2.js`, `test-cs010-p4.js`, `test-cs013-p2.js`) address the *slider* and *Back* rows, never `defaultsRow`. **Full regression green: 63 test files, 5,420 assertions, 0 failures** (was 62 / 5,243), `node --check` clean. Docs: GDD (top-of-file Current build → CS016 P3, §2.16 state-machine field list + the `menuRoot` dispatch line + the Return-to-Defaults line + a new modal subsection covering state shape, the cancel-default rationale, the exclusive-input precedent and its ordering contract, the render contract, both consumers and the one deliberate non-consumer, Architecture Map **game object** / **Menu** / **draw()** rows), `GDD-VERSION-HISTORY.md` (a CS016 P3 entry), this file. `GAME_VERSION` NOT bumped — still `"1.0.0.15"`, P5 carries it. Did NOT push.

**Prior session (CS016 P2, landed):** implemented CS016 Phase 2 (per `IMPLEMENTATION-PHASES-CS016.md` P2, spec `PLANNED-FEATURES-CS016.md` §1/§2/§7, FORK-CS016-A → single-parent IA and FORK-CS016-B → (b) chrome-less title menu), reading `STATUS.md` and `CLAUDE.md` first as required, and re-grepping every named anchor in context before editing. **The largest and highest-risk phase of the round.** **RESOLVED FLAG-CS016-l FIRST, as the P2 prompt in `IMPLEMENTATION-PHASES-CS016.md` instructs** (note: the prompt Paul pasted was the pre-addendum copy and omitted that STOP-AND-READ paragraph; the repo's own copy has it, and the gap is real, so it was resolved rather than inherited). `menuActive()`'s second operand went from `=== "titlemenu"` to **`game.menu.screen !== null`** — the candidate fix the flag recorded, verified as the invariant that actually holds (`screen` is non-null iff some menu screen owns input). Without it, `gotoScreen("achievements"|"highscores")` from the title menu renames the screen while `game.paused` stays false, so `menuActive()` read false again at that exact moment: Up/Down/Back dead on both viewers and Confirm falling through to the title's `startGame()` shortcut. **Found and closed a SECOND, mirror-image hole the flag did not name:** the opposite case, a title screen left with `screen === null`. `closePause()` produced exactly that from any title-context screen (the `pause` action on any Options sub-screen, and `debugReturn()` out of the hidden debug panel), leaving a cursor-less title that reads `menuActive()` false — dead to input. Fixed by making `closePause()` delegate to the new `returnToTitleMenu()` when `game.state === "title"`: at the title there is no "no menu" state to close back to, because the title menu **is** the title's base UI rather than an overlay. **This is a deliberate, flagged addition beyond the prompt's literal scope** (the prompt scoped only Options' Back `else` branch) — reported because shipping without it would have knowingly introduced a dead-input title screen; the resulting invariant, which the rest of the round must preserve, is **at the title, `game.menu.screen` is never null**. **Built as specced otherwise:** new `MENU_TITLE = ["Start Game","Achievements","High Scores","Options"]` beside `MENU_ROOT_PLAY`/`MENU_ROOT_OVER`; new `menuTitle(a)` following the `menuRoot` label-dispatch template exactly (dispatch by LABEL, never index) plus `case "titlemenu":` in `menuInput`'s switch; the `game` literal boots at `screen: "titlemenu"`; `quitToTitle()` arms it and **also resets `index`/`row`/`col`/`scroll`** (needed but unstated in the prompt — a stale root index 2 from "Quit" would otherwise land the title cursor on "High Scores" instead of "Start Game"); `startGame()`'s reset verified to still set `screen = null` (in play, no menu) and left untouched. `MENU_OPTIONS` shrank 6 → 4. **Positional-access audit reported as the prompt asked: ZERO hardcoded index accesses found** — 6 `MENU_OPTIONS.indexOf(...)` call sites (`menuSound` ×2, `menuDifficulty` ×2, `menuControls` ×2), one cursor-relative `MENU_OPTIONS[m.index]` read in `menuOptions`, and one label-driven `forEach` in `drawOptionsMenu`. Both Back destinations repointed to `gotoScreen("titlemenu", MENU_TITLE.indexOf(...))`; `returnToTitleMenu()` added beside `closePause` exactly as specified and used by **both** of `menuOptions`' back branches. `openPause()` deliberately left routing the title to `"options"` with `game.paused = true`, so Options keeps its dimmed-panel chrome. **Title render:** removed both keyboard control-legend lines, the blinking `PRESS ENTER TO START`, and the `OPTIONS / ACHIEVEMENTS: O` hint; kept ORBITAL / O V E R H A U L, the flavour line, `titleDebris`, the version stamp, `drawToasts()`. New `drawTitleMenu()` is chrome-less (no dim, no `menuPanel`) and reuses the `"▶ "` prefix, the `COLOR.text`/`COLOR.menuIdle` convention and the shared `drawMenuHint()` footer. **Y-OFFSETS CHOSEN, ALL FOUR ARE PLAYTEST KNOBS:** `TITLE_MENU_Y = VIEW_H/2 - 18` (342), `TITLE_MENU_STEP = 38`, `TITLE_MENU_SIZE = 24`, `TITLE_MENU_HINT_Y = VIEW_H/2 + 170` (530) — rows land at y **342 / 380 / 418 / 456**, sitting in the band the two removed legend lines vacated, 42px below `O V E R H A U L`'s baseline (300) and 24px above `BEWARE THE HUNTER SATELLITE`'s (480); the hint sits 50px below the flavour line and 174px above the version stamp. Verified by dumping a real title frame's `fillText` log, not by arithmetic alone. **The 24px gap between the last row and the flavour line is the tightest measurement on the screen and is the specific thing to look at in a browser.** **Draw-gate change the prompt's note did not anticipate:** the title branch's `if (game.paused) drawMenu();` became `if (screen === "titlemenu") drawTitleMenu(); else if (screen !== null) drawMenu();`. The prompt correctly noted that `game.paused` won't fire for the title menu — but Achievements/High Scores reached FROM the title menu are also unpaused, so a `game.paused` gate would have rendered **nothing at all** on either (a blank title screen with live input). The two branches are mutually exclusive by construction, so the title menu never draws under an open panel. **Routing decisions, stated as the prompt required.** (1) **Gamepad Start at the title → routed through `menuTitle`'s `pause` branch → `startGame()`.** Chosen over an explicit special case in `handleGamepadMenu` because it keeps all title routing in one handler with no state-sniffing bolted onto the pad dispatcher, and it is symmetric with branch (3)'s `pressedStart → startGame()` for non-`playing` states. Side effect, judged acceptable: keyboard has no distinct pause key (`bindings.back` and `bindings.pause` BOTH bind `escape`, and `handleMenuKey` resolves back first), so this branch is gamepad-Start-only in practice. (2) **`menuTitle`'s `back` opens Options, NOT "nothing".** The prompt asked for `back → nothing` in item (2) and for ESC-opens-Options in item (8); those are in tension, because ESC *is* the back binding and normalizes to the `back` action — there is no way to distinguish it from gamepad **B** at `menuInput` level, and no reason to want to. Decisive factor: **gamepad B at the title already opened Options** (`handleGamepadMenu` branch (3), `pressedBack && onTitleOrOver`), so leaving `back` inert would have silently REGRESSED the pad. One branch therefore satisfies §7/FLAG-CS016-e's ESC shortcut and preserves B, and `back` was free anyway (a root has nothing to back out to). (3) **`handleMenuKey` gained one line, `else if (k === "o") menuInput("options")`.** Branch (2) now swallows every key at the title, so branch (3)'s title-only `k === "o"` shortcut had gone inert; mapping it as a menu action keeps the one-action-vocabulary architecture, and it is an inert no-op on every other screen (all handlers are if/else-if chains over known action names). Branch (3)'s confirm/`o` lines and `handleGamepadMenu`'s `onTitleOrOver` branch are **unreachable at the title but left intact and still required at gameover**, with comments recording that. **DEBUG SECRET CODE CHECKED AND ASSERTED:** `DEBUG_CODE` is `"EvilG3niu$"` — no `w`/`a`/`s`/`d` in either case, and (belt-and-braces) not a single one of its characters resolves to ANY menu action via `handleMenuKey`. This was incidental before and is **load-bearing now** that the code's keystrokes fall through into the nav mapper; the test drives the real listener from an armed title menu, from both a default and a non-default cursor row, and confirms the panel still opens with the cursor untouched. **New test `scratchpad/test-cs016-p2.js` (196 assertions, all real handlers + the real captured keydown listener + a real fake gamepad; no reimplemented nav):** (A) `node --check` + both array shapes + a no-hardcoded-`gotoScreen("titlemenu", N)` source grep; (B) boot and `quitToTitle()` both land on `"titlemenu"` unpaused with the cursor on Start Game, and `returnToTitleMenu()` lands on Options with `paused` cleared; (C) 4-row wrap both directions + every row's confirm destination through `menuInput`; (D) both viewers' `back` AND `confirm` return to `"titlemenu"` with the cursor on their own row, `menuActive()` asserted true on each (the FLAG-CS016-l regression), plus the scroll reset on re-entry; (E) the full Options round trip via both of `menuOptions`' back branches, plus the `pause`-out-of-a-sub-screen path; (F) pause/gameover roots unchanged, including every Options sub-screen's Back in both states; (G) unreachability proved by driving EVERY Options row in all three contexts, not just by an `includes` check; (H) ESC/`o`/ENTER/pad Start/pad A/pad B at the title, plus branch-(3) regression guards at gameover; (I) the DEBUG_CODE properties above; (J) `AudioSys.ctx` null smoke over `startGame`/`update`/`draw` and a full nav cycle; (K) additive render coverage — the four rows at the stated offsets in the right colors/size/prefix, one `drawMenuHint` footer, no `fillRect`/`strokeRect` from `drawTitleMenu`, the removed lines absent and the kept ones present in a real `draw()` frame, and Options-over-title still getting its dim + panel while the title menu is NOT drawn underneath. **Nine pre-existing test files updated — expected and legitimate, per-file rationale:** `test-cs016-p1.js` (sections B and D — B's byte-identity sweep narrowed to the PRE-P2-REACHABLE `(paused, screen)` pairs it was always about, since the discarded rows asserted a property of impossible states, plus a new full-product check of the widened formula; D flipped from pinning the gap to pinning the fix); `test-cs010-p7.js` (the musicStateFor oracle's FIRST operand moved `game.paused` → `menuActive()` — CS010 P7's intent, "the fanfare plays iff the High Scores screen owns input", preserved exactly); `test-cs010-p4.js` (§8b's 6-row `MENU_OPTIONS` pin → 4 rows; the High Scores and Achievements walks repointed to the title menu, with the two ex-parent paths asserted ABSENT rather than deleted); `test-cs012-p4.js` (C/F/G — "Back from Options closes to a null screen" → "returns to the title menu unpaused"; F's single-parent property intact, parent repointed); `test-cs013-p1.js` (D/H/J — quitToTitle and the title-Options Back now land on `"titlemenu"`; added a cursor-reset assertion); `test-cs015-p4.js` (F — debugReturn from the title now lands on the title menu); `test-p4.js` (C/D/E/I + a new `atTitle()` helper — the sections used to force `screen = null` to mean "at the title", which now exercises shadowed branch-(3) paths no player can reach; **E's "ESC on title is inert" assertion was pinning the OPPOSITE of the new spec and was repointed, not deleted**); `test-v34-p6.js` (F — the Achievements Options row is gone, so the label-dispatch proof moved to the title menu's identical template); `test-v36-scores.js` (J — High Scores' parent repointed, both ex-routes asserted unreachable, plus a new assertion that browsing it from the title does NOT pause). **No assertion was weakened to make the suite pass**; `test-cs013-p2.js` needed only a comment fix and legitimately loses 4 assertions (two fewer `MENU_OPTIONS` rows × two per row), noted in place. **Full regression green: 62 test files, 5,243 assertions, 0 failures**, `node --check` clean. Docs: GDD (top-of-file Current build → CS016 P2, §2.9 control scheme + the removed title lines, §2.16 state machine / the concept split / the new CS016 P2 subsection / the CS010 P4 restructure note, §2.17 + §2.18 reachability, Architecture Map Menu + `game object` + `draw()` + Main-loop rows), `GDD-VERSION-HISTORY.md` (a CS016 P1–P2 entry — P1 never got one, and P5 consolidates the round), `PLANNED-FEATURES-CS016.md` (FLAG-CS016-l and FLAG-CS016-k marked resolved/shipped, plus the second hole recorded), this file. `GAME_VERSION` NOT bumped (P5 carries it). Did NOT push.

**Prior session (CS016 P1, landed):** implemented CS016 Phase 1 (per `IMPLEMENTATION-PHASES-CS016.md` P1, turning `PLANNED-FEATURES-CS016.md` §1.1's resolved seam into code), reading `STATUS.md` and `CLAUDE.md` first as required. **New round: CS016 — Title & Menu Navigation Pass**, target `GAME_VERSION` "1.0.0.16" (rides the last-landed phase per FLAG-CS016-i; unchanged this session, P1 of 5). Re-grepped all four anchors first (`menuActive`, `updateMusic`, `musicStateFor`, the `MusicSys.setDuck(` call site) — all confirmed at the plan's own estimates. **Made exactly the three specified edits, no more:** `menuActive()` gained `|| game.menu.screen === "titlemenu"`; the `setDuck` call in `updateMusic()` flipped its first operand `menuActive()` → `game.paused` (ducking reads the "sim is frozen" concept — there's no gameplay under the title, so reading `menuActive()` there would permanently duck the title track once P2 lands); `musicStateFor()`'s first line flipped `game.paused` → `menuActive()` (High Scores moves onto the title menu in P2, where `game.paused` is false, so its check must read the "a menu is open" concept or the fanfare silently stops). Each site carries an inline comment recording that reasoning. Safe today because no screen is named `"titlemenu"` yet — verified byte-identical to the old `game.paused`-only behavior for every currently-reachable state. **New test `scratchpad/test-cs016-p1.js`** (84 assertions, drives the REAL `menuActive`/`musicStateFor`/`updateMusic`/`startGame`/`update` — no reimplemented logic): (A) `node --check`; (B) the byte-identity proof — `menuActive() === game.paused` across the full `{title,playing,dying,gameover} × {true,false} × 9 menu.screen values` cross product (72 combinations, all pass); (C) the forward-looking `screen="titlemenu"`+`paused=false` case — `menuActive()` true, `updateMusic()` does NOT duck, `musicStateFor("title")` still `"title"`; (D) — see FLAG-CS016-l below; (E) today's real paused-High-Scores path is unaffected (still `"highscore"`, still no duck), plus a sanity check that a paused non-highscores screen still ducks; (F) `AudioSys.ctx` null → `startGame`/`update`/`updateMusic` no-crash smoke. **FLAG-CS016-l — a genuine gap discovered while writing test (D), not invented or silently resolved:** the phase prompt's own spec for test (D) (force `screen="highscores"`, `paused=false` — "the shape P2 creates" — and assert `musicStateFor()` returns `"highscore"`) is mathematically impossible under the exact three-edit `menuActive()` formula, because it special-cases `"titlemenu"` only. Per `PLANNED-FEATURES-CS016.md` §2, P2 reaches High Scores/Achievements via `gotoScreen("highscores"|"achievements")`, which sets `game.menu.screen` to THAT name (not `"titlemenu"`) while `game.paused` stays false — so `menuActive()` reads false again at that exact moment. This breaks not just the highscore-fanfare music state but, more seriously, ALL keyboard/gamepad menu input on those two screens (`handleMenuKey`/`handleGamepadMenu` are both `if (menuActive())`-gated, confirmed by direct read) — Up/Down/Back would go dead and Confirm would fall through to the title's `startGame()` shortcut, silently starting a new run from the High Scores screen. **Did not silently patch edit (1) beyond its exact three-edit scope, and did not weaken the test to hide it** — test (D) now pins today's real (gap-exposing) behavior instead of the prompt's aspirational one, with an inline comment explaining why. Flagged in `PLANNED-FEATURES-CS016.md` (new FLAG-CS016-l, §1.1 addendum + Resolved Forks list) with a candidate fix — `game.menu.screen !== null` in place of `=== "titlemenu"`, verified equally byte-identical on today's build (every code path that sets `game.menu.screen` non-null already sets `game.paused = true` in the same call) and one that covers every title-descendant screen without an ever-growing OR chain — and a STOP-AND-READ pointer added directly into `IMPLEMENTATION-PHASES-CS016.md`'s P2 paste-ready prompt so the next session resolves it explicitly before wiring Achievements/High Scores into `MENU_TITLE`. Full regression green: **61 test files, 4,928 assertions** (60 prior + this session's new 84-assertion file), `node --check` clean, **zero edits to any existing test file** (this phase's own strict acceptance criterion). Docs: `PLANNED-FEATURES-CS016.md` (FLAG-CS016-l added), `IMPLEMENTATION-PHASES-CS016.md` (P2 prompt gained the STOP-AND-READ pointer), this file. Did NOT push.

## Working / verified

**Headless-verified this session (CS016 P5, `scratchpad/test-cs016-p5.js`, new, 516 assertions):** drives the REAL `drawAchievements`/`drawAchRow`/`achMaxScroll`/`achTabIndex`/`achRows`/`menuAchievements`/`menuInput`/`gotoScreen` via the `test-cs013-p3.js` canvas-recording stub — every tab change goes through a real `"right"` press, never a direct write to `game.menu.achTab`, and no scroll/ceiling/geometry logic is reimplemented. Sections (A)–(I): the `"1.0.0.16"` pin and `ACH_TABS`' shape; Weekly/scroll-0 on **every** entry (first, second, third) plus `startGame()`'s reset carrying the field; left/right in both directions **wrapping** at both ends with the scroll zeroed on each switch and nothing else on the screen moving; an unknown or `undefined` `achTab` degrading to tab 0 and still rendering; only the active tab's names *and descriptions* in the log with none of the inactive tab's (guarded by a no-shared-names precondition); the two ceilings differing and each matching an independent recompute from `ACH_ROW_STEP`/`ACH_DESC_DY`/`ACH_SCALE`/`ACH_ROW_VISIBLE_H`, up/down clamped per tab, and the render-clamp-equals-input-clamp proof done both behaviourally and by source inspection; the `save→beginPath→rect→clip→rows→restore` bracket with the exact clip rect and all chrome outside it, on both tabs; both `drawAchRow` branches at the new x/w; back **and** confirm from the non-default tab mid-scroll returning to `"titlemenu"` with the cursor restored; and an `AudioSys.ctx` null smoke of a full open/tab-switch/scroll/wrap/close cycle from the title. **`test-cs013-p3.js` (267 → 311) and `test-cs015-p2.js` (→ 261) were migrated to the single-column two-tab layout with every assertion's intent preserved** — see the session entry above. **Full suite: 65 files, 6,175 assertions, 0 failures.**

**Headless-verified prior session (CS016 P4, `scratchpad/test-cs016-p4.js`, new, 106 assertions):** drives the REAL `menuRoot`/`menuDifficulty`/`drawRootMenu`/`drawDifficulty`/`menuInput`/`openPause`/`closePause`/`gotoScreen`/`quitToTitle`/`startGame` via a canvas-recording stub — no menu logic is reimplemented. **(A)** `node --check`; `MENU_ROOT_PLAY` is `["Continue","Save","Options","Quit"]` with Save at index 1; `MENU_ROOT_OVER` is still 3 rows and carries no Save. **(B)** the cursor CAN land on Save; confirming it leaves `game.state`/`paused`/`menu.screen`/`menu.index` all byte-identical and fires zero storage writes; Save renders `COLOR.dim` both while focused (`"▶ Save"`) and while merely passed over (`"   Save"`); the cursor also freely walks through it (two Downs from Continue reach Options). **(C)** the derived panel height fits every row + the footer for BOTH the 4-row pause root and the 3-row gameover root — read off the real `strokeRect` bounds, not a re-derived formula — and the two panels are provably different heights (not a coincidental 300 that also happens to fit 4), with the untouched 3-row case still reproducing the old fixed 300 exactly. **(D)** locked Difficulty (`game.state "playing"`): left/right/left on each of shot/magnet/autoshield leaves `settings.shotPowerupMode`/`magnetMode`/`autoShield` byte-identical, fires zero `localStorage.setItem` calls, and doesn't even move the cursor. **(E)** unlocked Difficulty from BOTH `"gameover"` and `"title"`: the same presses DO flip the setting and DO persist (the written blob inspected, not just "a key exists") — the lock does not leak outside a live run. **(F)** the Back row navigates normally (`confirm` and `back` both tested) in both the locked and unlocked state. **(G)** render: every locked row's label/left-side/right-side/◄►chevrons reads `COLOR.dim` regardless of focus and shows `DIFFICULTY_LOCK_HELP`; every unlocked row's focused label reads `COLOR.text`, unfocused reads `COLOR.menuIdle`, and shows the normal per-row tip; Back's own colour contrast is unaffected by the lock either way. **(H)** an `AudioSys.ctx === null` smoke across the full Save-confirm + Difficulty-lock/unlock cycle, both states, with a `draw()` at every step. **Nine pre-existing test files needed updates** (the inserted Save row shifted root-menu indices/press-counts; the new lock intercepted four files that drove Difficulty toggles from a live `"playing"` state) — see this session's `GDD-VERSION-HISTORY.md` entry for the full per-file list. **Full suite: 64 files, 5,533 assertions, 0 failures.**

**Headless-verified prior session (CS016 P3, `scratchpad/test-cs016-p3.js`, new, 177 assertions):** drives the REAL `menuInput`/`menuModal`/`openModal`/`menuRoot`/`menuControls`/`returnToDefaults`/`drawMenu`/`drawModal`/`startRebind`/`gotoScreen`/`closePause`/`quitToTitle`/`returnToTitleMenu`/`openDebug`/`startGame`, plus the REAL captured `keydown` listener and the REAL `handleGamepadMenu` with a fake pad — **no dialog logic is reimplemented anywhere in the file**, and the two entry points every section uses (`atMidRunQuit()` / `atControlsDefaults()`) navigate there the real way, through the root and Options. **(A)** `node --check`; `game.menu.modal` defaults `null` in BOTH object literals (asserted at runtime *and* by regexing every `menu:`/`menu =` literal in the source, so it cannot regress to a runtime-only assignment). **(B)** THE safety property: a freshly-opened dialog parks on `index` 1 (CANCEL) — on a synthetic dialog, on the real Return-to-Defaults dialog, and on the real Quit dialog — plus the exact state shape `{text,confirmLabel,cancelLabel,index,onConfirm}`, the `"CANCEL"` default, an explicit-`cancelLabel` override, and the property *exercised* (open, ENTER, callback did not run). **(C)** ENTER-on-CANCEL and `back` both dismiss without running `onConfirm` (including `back` from the *confirm* row, the harder direction); confirm runs it **exactly once** and a second ENTER cannot re-run it; all four directions only ever visit indices 0 and 1; the **clear-before-invoke** ordering (the callback observes `modal === null`, and a callback that opens a *second* dialog keeps it); a `null` callback confirms cleanly instead of throwing. **(D)** `pause` inside a dialog cancels — on the root, and on the Controls screen where `pause` normally *does* `closePause()` — leaving the screen open, paused, and the cursor on the row that asked; plus a sanity check that `pause` still closes once the dialog is gone. **(E)** Return to Defaults: rebind away from default → opening the dialog resets nothing → CANCEL keeps the custom binding → reopen (on CANCEL again, no sticky index) → CONFIRM restores every `REBINDABLE` action to `DEFAULT_BINDINGS` and writes `afd_settings_v1` (the persisted **payload** is inspected, not merely the key’s existence); `returnToDefaults()` called directly still works and still leaves `shipTurnScale` alone (FLAG-10a). **(F)** Mid-run Quit: cancel → still `playing`, still paused, cursor still on Quit; confirm → `state "title"` + `screen "titlemenu"` + unpaused + `menuActive()` true; then the whole flow again through the **real keydown listener** (Enter/Escape/ArrowUp) and the **real gamepad path** (A/B/D-Pad), including pad **START** inside a dialog cancelling rather than punching through to `closePause()`. **(G)** Gameover `"Quit to Title"` opens no dialog at any point and quits directly; the two labels are proven distinct; and a sweep confirms **no other row of either root layout, and no other Controls row, grew a dialog**. **(H)** Priority over the screen beneath: with a dialog up, `down` moves the dialog cursor and never `m.row`, `m.col`, or the root’s `index`; an unhandled action (`"options"`) neither dismisses the dialog nor reaches the screen. **(I)** Guard order — asserted **in source** (rebinding guard before modal guard before the screen `switch`) *and* behaviourally: with both modes live nothing at all moves, and clearing the capture proves the presses were swallowed rather than queued; a real `startRebind` + keydown capture lands in the **binding** while the dialog sits untouched beneath, still on CANCEL. **(J)** All seven clear sites (`gotoScreen`/`closePause`/`startGame`/`quitToTitle`/`openPause`/`openDebug`/`returnToTitleMenu`), plus `closePause`’s title-aware delegation still landing on the title menu; `draw()` with a dialog over the play root, the Controls screen and the gameover root; `drawModal()` emitting **zero** draw calls when nothing is open; and an `AudioSys.ctx === null` smoke run of the full open/nav/cancel/reopen/confirm cycle for both consumers with a `draw()` at every step. **(K)** Render detail: the panel is `MODAL_H` tall and centred; the prompt/rows/footer land at their named offsets in the right sizes and colours with the shared `"▶ "` prefix; the footer went through `drawMenuHint`; moving the cursor re-renders the other row as selected; the dialog’s box is stroked **after** the panel beneath (it paints over, not under) and its footer is the **last** text drawn; and the width genuinely self-heals — a longer prompt widens the box, a tiny one floors at `MODAL_MIN_W`, both real prompts fit with both margins, and no numeric literal width is passed to `menuPanel`.

**Headless-verified prior session (CS016 P2, `scratchpad/test-cs016-p2.js`, new, 196 assertions):** drives the REAL `menuTitle`/`menuInput`/`menuOptions`/`menuAchievements`/`menuHighScores`/`openPause`/`closePause`/`returnToTitleMenu`/`quitToTitle`/`gotoScreen`/`startGame`/`handleMenuKey`/`drawTitleMenu`/`draw`, the REAL captured `keydown` listener, and the REAL `handleGamepadMenu` against a fake Standard-Gamepad — no navigation logic is reimplemented anywhere in the file. (A) `node --check`, `MENU_TITLE`/`MENU_OPTIONS` shapes, both roots untouched, and a source grep proving neither repointed Back destination passes a numeric literal. (B) boot lands on `"titlemenu"` unpaused with the cursor on Start Game; `quitToTitle()` from a live run lands identically and clears a deliberately-staled `index`/`row`/`col`/`scroll`; `returnToTitleMenu()` clears `paused` and lands on Options. (C) 4-row wrap both directions and every row's confirm destination, driven through `menuInput` so the new switch case is exercised. (D) both viewers' `back` AND `confirm` return to `"titlemenu"` with the cursor on their own row, with `menuActive()` asserted **true** on each — the FLAG-CS016-l regression — plus the scroll reset on re-entry. (E) the full `titlemenu → Options → Controls → Back → Options → Back → titlemenu` trip through BOTH of `menuOptions`' back branches, plus the `pause`-out-of-a-sub-screen path that `closePause`'s title delegation covers. (F) the pause and gameover roots unchanged, including every Options sub-screen's Back in both states. (G) unreachability proved by driving EVERY Options row in all three contexts, not merely by an `includes` check, and by checking both root layouts. (H) ESC, `o`, ENTER (on the default row AND a non-default row), pad Start, pad A and pad B at the title — plus branch-(3) regression guards proving ENTER/`o`/pad A still work at gameover. (I) `DEBUG_CODE` contains no `w`/`a`/`s`/`d` and none of its characters resolves to any menu action; typing the full code through the real listener from an armed title menu (default AND non-default cursor row) still opens the debug panel, and backing out lands on the title menu. (J) `AudioSys.ctx` null smoke over `startGame`/`update`/`draw` plus a full open-enter-return cycle on every row. (K) render coverage — the four rows at the stated offsets with the right colour/size/`"▶ "` prefix, one `drawMenuHint` footer, **no `fillRect`/`strokeRect` from `drawTitleMenu`** (chrome-less), the removed title lines absent and the kept ones present in a real `draw()` frame, Options-over-title still getting drawMenu's dim + panel with the title menu NOT drawn underneath, and Achievements/High Scores still rendering their panel from the title despite `game.paused` being false. **Full suite: 62 files, 5,243 assertions, 0 failures.**

## Next up

**CS016 IS COMPLETE — all 5 of 5 phases landed, per `IMPLEMENTATION-PHASES-CS016.md`.** P1 (the `menuActive()` concept split), P2 (the navigable title menu + single-parent IA — the round\'s highest-risk phase), P3 (the modal confirmation system), P4 (the shared unavailable-row idiom: the disabled Save row + the mid-run Difficulty lock) and P5 (the two-tab Achievements viewer + the `"1.0.0.16"` version bump) are all in, each its own commit on `main`. **Before closing CS016 out administratively**, archive `PLANNED-FEATURES-CS016.md` / `IMPLEMENTATION-PHASES-CS016.md` under `archive/` (the CS009/CS010 pattern) — still NOT done (deferred behind CS017; do it whenever a session next has a clean administrative moment).

**What CS016 leaves for whoever touches menus next:**

- **`menuInput` now has TWO exclusive-mode guards and their order is load-bearing** — `rebinding` first, then `modal`, then the screen `switch`. `test-cs016-p3.js` §I pins that order **in source**, so a reorder fails the suite rather than silently regressing.

- **`drawModal()` is called from `drawMenu()` only**, so a modal is invisible on the chrome-less title menu — see the Known-issues LIMITATION entry before wiring a title-menu confirmation.

- **Two menu panels now self-heal their own size** and must not be re-hardcoded: `drawRootMenu`'s width (CS015 P1, measured hint text) *and* its height (CS016 P4, derived from `items.length`), and `drawModal`'s measured width. A row inserted into either root array needs no geometry edit.

- **Row insertions still break index/press-count assertions.** CS016 P4's Save row needed nine pre-existing test files updated and CS016 P5's tab rebuild needed two geometry suites migrated — in both cases found by grepping `scratchpad/` **before** editing, which is the habit to keep. Prefer `.indexOf(label)` over a literal index in new assertions.

- **The Achievements viewer is now tab-driven and its ceiling is per tab.** `achMaxScroll()` reads `achRows()`, so adding a tab to `ACH_TABS` (a pure data edit) automatically gets its own correct ceiling — but **do not let `drawAchievements()` grow its own copy of that maths**; `test-cs016-p5.js` §E asserts it hasn't, by behaviour and by source. `game.menu.achTab` is runtime-only: never persist it.

- **Both round-level cautions still hold:** the two latent rebinding bugs (FLAG-CS016-g — `captureKeyRebind` destroying shipped dual bindings, and no rebinding conflict detection at all) remain deliberately out of scope and want their own changeset; no phase of CS016 added, renamed or restructured a `localStorage` key.

## GDD version history

- **CS016 (P1–P5) — the title/menu navigation round: `menuActive()` decoupled from `game.paused`, a navigable title menu + single-parent IA, the build's first modal confirmation, a shared “unavailable row” idiom, and a two-tab Achievements viewer.** `IMPLEMENTATION-PHASES-CS016.md`, the whole changeset (this consolidated entry replaces the per-phase P1–P2 / P3 / P4 entries, following the CS012/CS013/CS015 precedent); specs in `PLANNED-FEATURES-CS016.md` (FORK-CS016-A → single-parent IA, FORK-CS016-B → (b) chrome-less title menu with the keyboard legend dropped). `GAME_VERSION` `"1.0.0.15"` → **`"1.0.0.16"`**, bumped at P5 as planned — P1–P4 deliberately left it alone. **P1 (pure no-op refactor, zero test-file edits by design):** `menuActive()` split off a bare `game.paused` so *“a menu owns input”* and *“the sim is frozen”* can diverge, and the two downstream consumers swapped which concept they read in **opposite** directions — `MusicSys.setDuck` → `game.paused` (ducking makes room for gameplay audio under an overlay; there is none under the title, so `menuActive()` would permanently duck the title track), `musicStateFor()`'s High Scores check → `menuActive()` (that screen moves onto the unpaused title menu, so `game.paused` would silently kill its fanfare). Byte-identical on the pre-P2 build, which is what made both verifiable before the state existed. **P2 — the title screen becomes a navigable menu:** new `MENU_TITLE = ["Start Game","Achievements","High Scores","Options"]` + `menuTitle(a)` (the `menuRoot` label-dispatch template, never an index) + a `case "titlemenu"` in `menuInput`; the title boots on it (`game` literal `screen: "titlemenu"`), `quitToTitle()` re-arms it with the cursor on `Start Game`, `startGame()` still clears it to `null`. `drawTitleMenu()` renders **chrome-less** — no backdrop dim, no `menuPanel` box, so the title art and `titleDebris` stay visible — reusing the `"▶ "` prefix, the `COLOR.text`/`COLOR.menuIdle` convention and the shared `drawMenuHint()` footer at four playtest-knob constants (`TITLE_MENU_Y`/`STEP` 38/`SIZE` 24/`HINT_Y`; rows land at y 342/380/418/456). Removed from the title: both keyboard control-legend lines, the blinking `PRESS ENTER TO START`, and the `OPTIONS / ACHIEVEMENTS: O` hint (gameover's equivalents untouched). **Single-parent IA:** `MENU_OPTIONS` shrank 6 rows → 4 and Achievements + High Scores hang off `MENU_TITLE` alone, their Back destinations repointed to `gotoScreen("titlemenu", MENU_TITLE.indexOf(…))` with **no nav stack and no return-context tracker added** — deliberately reversing CS010 P4 §8b and CS012 P4, so both viewers are now unreachable mid-run and at gameover (accepted, signed off; the gameover screen's inline top-10 table and in-play unlock toasts are the mitigations). **Two gaps found and closed rather than shipped:** (1) **FLAG-CS016-l**, raised by P1 — `menuActive()`'s `=== "titlemenu"` operand covered only the title's *root*, so it read false the instant `gotoScreen("achievements"|"highscores")` renamed the screen, killing Up/Down/Back on both viewers and letting Confirm fall through to `startGame()`; widened to `game.menu.screen !== null`. (2) the mirror-image hole the flag did not name — `closePause()` left the title with `screen === null` (a cursor-less title dead to input); it now delegates to a new `returnToTitleMenu()` when `game.state === "title"`, establishing the invariant **at the title, `game.menu.screen` is never null**. Input-routing consequences handled deliberately: `handleMenuKey` gained one `"o" → menuInput("options")` mapping; `menuTitle`'s `back` opens Options (delivering §7/FLAG-CS016-e's **ESC opens Options from the title**, inert there before) and its `pause` starts a game (keeping gamepad **Start** working with no state-sniffing in the pad dispatcher); `draw()`'s title branch gates on `game.menu.screen`, **not** `game.paused`, since the two viewers now run unpaused. `DEBUG_CODE` containing none of `w/a/s/d` was incidental and is now **load-bearing** (its characters fall through into `handleMenuKey`), asserted in test. **P3 — the build's first modal confirmation, built once and wired to two rows.** **State:** one nullable field, `game.menu.modal` — `{text, confirmLabel, cancelLabel, index, onConfirm}` open, `null` closed — declared in **both** `game.menu` literals (the `game` object's and `startGame()`'s reset), deliberately unlike `scroll`, which CS013 P3 only added to the former. Cleared wherever `rebinding` is cleared as a *menu-state reset* (`openPause`, `closePause`, `gotoScreen`, `quitToTitle`, `openDebug`, and `returnToTitleMenu` — a site the P3 plan could not name, since that helper post-dates it), but **not** by the three *capture-specific* teardowns, which end a capture rather than a menu state. **`index` defaults to 1 (CANCEL) — the safety property of the phase, not a tidy-able default:** both consumers are destructive and ENTER is the same key that opened the dialog, so with index 0 a double-tap would wipe a run or a rebind set with no deliberate act. **Input:** `menuInput` gained a second exclusive-mode guard — `if (game.menu.modal) { menuModal(action); return; }` — placed **after** the existing `rebinding` early-return and **before** the screen `switch`; that order is the contract (a live hardware capture, where the next keypress *is* data, must out-rank a dialog) and the placement is what keeps every per-screen handler ignorant of modals. `confirm` on row 0 captures `onConfirm`, **clears the modal first, then invokes** (so a callback that navigates cannot be clobbered by its own teardown); **`pause` is treated as cancel and never passed through**. **Render:** `drawModal()` runs unconditionally as the **last** act of `drawMenu()` and self-gates on the field, so ordering it last is what puts a dialog over the screen that raised it without inventing a z-index concept; it reuses `menuPanel()`/`drawMenuHint()` and **measures its own width** (the CS015 P1 self-healing idiom), never a hardcoded one. The new `MODAL_*` block mirrors `drawRootMenu`'s height/row step/row size one-for-one, so the dialog reads as the same family of panel rather than a new widget. **Consumers:** Return to Defaults (*“Reset all controls to defaults?”* / **RESET**, `returnToDefaults()` itself unchanged — the gate lives in the caller) and mid-run **Quit** (*“Quit to title? Progress in this run will be lost.”* / **QUIT**). **The deliberate non-consumer:** gameover's `"Quit to Title"` stays ungated — the run is already over; because `menuRoot` dispatches by label and the two rows carry *different* labels, the split needed no `game.state` check. **One rendering gap logged, not built:** `drawTitleMenu()` bypasses `drawMenu()`, so a modal opened at `screen === "titlemenu"` would take input and draw nothing — no code does that today. **P4 — the shared “unavailable row” idiom, built once and used twice:** a row stays focusable (the cursor lands on it and, for a value row, still shows its value) but its action is inert — no state change, no `saveSettings()`, no `AudioSys.ui()` blip; silence is the tell — and it renders `COLOR.dim` **even while focused**. Focusable-but-inert beats skip-over: a row the cursor silently skips is invisible to the player. **Consumer one — the Save row:** `MENU_ROOT_PLAY` grows to `["Continue","Save","Options","Quit"]` (a placeholder — save-game itself is not implemented this round); `MENU_ROOT_OVER` is untouched (no run in progress at gameover for a save to apply to); `menuRoot()` gets no branch for `"Save"`, since an unmatched label already falls off the label-dispatch chain as a no-op. **`drawRootMenu`'s panel height became DERIVED from `items.length`** (CS015 P1's self-healing precedent, extended from width to height): `h = 118 + (N−1)*46 + 62 + 28`, which reproduces the old fixed 300 exactly at `N=3`, so the 3-row gameover root and the 4-row pause root both fit through one function with no branch. **Consumer two — the mid-run Difficulty lock:** changing shot/magnet/auto-shield mid-run retroactively changes the rules of a run already in progress, corrupting the meaning of its score and achievement progress, so `locked = game.state === "playing" && row !== "back"` gates the left/right branch only (up/down navigation is untouched, so a locked row stays reachable and readable). **Keyed on `game.state === "playing"` specifically** — not `game.paused` (the title also carries `paused === false` as of P2) and not gameover (the run is over; changing settings for the next one is harmless). `DIFFICULTY_LOCK_HELP` replaces the per-row tip through the *same* `drawMenuHint` call, not a second help mechanism. **P5 (round complete) — the Achievements viewer becomes two tabs, and the version bumps.** The three 350px columns (weekly | lifetime | lifetime-cont., v3.0 P8) become **two tabs** — Weekly (`Achievements.activeWeekly()`) and Lifetime (`Achievements.LIFETIME`) — one visible at a time down a **single full-width column** at `ACH_COL_W` (1140). That **resolves CS013 P3's own FLAG-CS013-2d complaint** that `colW = 350` was marginal for a long description at `ACH_SCALE` 1.5: the one column is wider than all three old ones put together. **Tabs are DATA** — `ACH_TABS` is a two-entry `{id, label, rows()}` table whose `rows` is a **thunk**, not a snapshot, so the weekly rotation and any pool resize are read live; `achTabIndex()` resolves `game.menu.achTab` and **defaults to tab 0 for any unknown or undefined id**, so a stale field degrades to “show Weekly” rather than crashing or blanking; a third tab would be one more entry and no new branch. **Input:** `menuAchievements` gained `left`/`right`, genuinely **unused** on this screen before (it handled only up/down/confirm/back/pause), so nothing was displaced; they **wrap** rather than clamp — with two tabs either direction always lands on the other one, and `setAchTab`'s modulo keeps that a carousel if a third is added. Every switch **zeroes `game.menu.scroll`**: the tabs are very different heights, so carrying a Lifetime offset onto Weekly would open it scrolled into empty space. **State:** `game.menu.achTab` (default `ACH_TAB_DEFAULT` = `"weekly"`) is **runtime-only and never persisted** — a tab position is not a setting; it is reset in `gotoScreen` beside the existing scroll reset so **every** entry is Weekly/unscrolled, and it lands in **both** `game.menu` literals per P3's rule. `ACH_TAB_DEFAULT` deliberately sits in the early menu-constants block with `ACH_SCALE`, not with the viewer geometry, because the `game` literal is evaluated far earlier in the file and a `const` referenced ahead of its definition is a TDZ **throw at load**; `ACH_TABS[0].id` reads it so the two cannot drift. **`achMaxScroll()` became tab-aware and is still ONE function with two callers:** it was `max(activeWeekly().length, ceil(LIFETIME.length / 2))` — the tallest of three columns — and is now simply the active tab's own row count. Because Lifetime is no longer halved across two columns, that tab is ~twice as tall as before and scrolls considerably more (ceiling **923 px**), while Weekly's 5 rows fit outright (**ceiling 0** — that tab does not scroll at all). Render clamp and input clamp still call the one helper, which is exactly why CS013 P3 hoisted it to top level; the renderer grew no copy of the ceiling maths (asserted in test, by behaviour *and* by source inspection). **Chrome:** the three static `COLOR.satellite` column headers become a **tab strip** — both labels always drawn, active in `COLOR.text`, idle in `COLOR.menuIdle`, the same selected/idle convention every other menu uses, because these are now a cursor position rather than captions. The footer hint (`ACH_HINT`, through `drawMenuHint`) gained `◄►  switch tab` and **dropped its stale “return to Options” destination**, which CS016 P2 had invalidated when it repointed the viewer's one parent to the title menu. `drawAchRow` is **byte-unchanged** — only the `w` it is handed (350 → `ACH_COL_W`) and the number of callers (three → one) changed. New geometry consts: `ACH_PANEL_X`, `ACH_COL_X`/`ACH_COL_W`, `ACH_TAB_Y`/`ACH_TAB_STEP`. **Tests:** new `scratchpad/test-cs016-p5.js` (**516 assertions** across (A)–(I)) driving the real `drawAchievements`/`drawAchRow`/`achMaxScroll`/`menuAchievements`/`menuInput`/`gotoScreen` through the `test-cs013-p3.js` canvas-recording idiom — no scroll, ceiling, tab-resolution or row geometry reimplemented. **The two geometry suites written against the three-column layout were migrated, not deleted:** `test-cs013-p3.js` (267 → **311** assertions) and `test-cs015-p2.js` (→ **261**) each kept every assertion's intent — sizes, contrast, the clip bracket, the scroll clamp — and repointed only the positional expectations the single-column layout genuinely invalidates (row x → `ACH_COL_X`, row width → `ACH_COL_W`, three `COLOR.satellite` headers → two selected/idle tab labels), with each section now looping over **both tabs** rather than three columns of one render; coverage went up, not down. The three files pinning the old version literal (`test-cs010-p0.js`, `test-cs013-p4.js`, `test-cs015-p7.js`) were bumped. **Full regression green: 65 test files, 6,175 assertions, 0 failures** (was 64 / 5,533). Docs: §2.17 rewritten for the two-tab viewer + its constants blockquote, the top-of-file Current-build line → CS016 P5 / `"1.0.0.16"`, Architecture Map **game object** (`achTab`) and **Menu** rows, this consolidated entry, `STATUS.md` round-complete. Did NOT push.
